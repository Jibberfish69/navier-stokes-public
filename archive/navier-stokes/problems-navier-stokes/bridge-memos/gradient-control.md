# Gradient-Control Bridge Memo (Referee-Facing Audit)

## Bridge Target

State the fourth bridge honestly.

The handed source family uses a curvature-based regularity bridge.
The live lane also contains a later Euclidean gradient-transfer package.
Those are related, but they are not the same statement.

The lane is now choosing the source-faithful curvature route first.
The Euclidean package remains available only as an explicit fallback theorem-upgrade branch.

So the live bridge question is now ordered rather than ambiguous:

1. what the original source stack actually says, and
2. whether the later Euclidean package ever needs to be advanced as an explicit theorem upgrade.

## Exact Hypotheses In The Original Source Family

- a Riemannian reformulation of Navier-Stokes;
- gradient evolution expressed through Ricci curvature, viscosity, and nonlinear growth;
- a damping mechanism of the form
  `Ricci + viscosity against cubic gradient growth`.

The original source bridge is therefore geometric in posture.

## Exact Hypotheses In The Later Euclidean Package

- the original Euclidean Navier-Stokes equation on `(0,T) x R^3`;
- dyadic Littlewood-Paley projectors and high-frequency tail objects;
- dangerous-scale control via scale-barrier plus compactness input;
- a continuation mechanism stated without manifold geometry.

This is a later Euclidean replacement candidate, not an automatic restatement of the original bridge.

## Source-Grounded Fourth Bridge

The canonical source file `Section_5-_Curvature-Based_Regularity.tex` uses:

```math
\partial_t u + \nabla_u u = -\mathrm{grad}_g(p) + \nu \Delta_g u,
```

and the gradient evolution

```math
\frac{\partial}{\partial t} |\nabla u|^2
=
- \mathrm{Ric}(g)(\nabla u,\nabla u)
+ \nu \Delta_g |\nabla u|^2
+ C|\nabla u|^3.
```

So the original fourth bridge is:

- curvature-based,
- geometric,
- and not purely Euclidean in its written source form.

## Later Euclidean Upgrade Candidate

The live theorem-construction package instead tries to prove a Euclidean gradient-transfer inequality using:

- dyadic high-frequency enstrophy tails,
- a coercivity object,
- and scale-barrier plus compactness input.

That package may be mathematically valuable.
But it must be described honestly as:

- an explicit later theorem upgrade candidate,
- or a replacement bridge that still needs to justify why it can stand in for the original fourth slot.

## Lane Route Selection

The declared fourth-bridge order for the lane is now:

1. primary branch: original curvature-based regularity;
2. fallback branch: later Euclidean replacement only if the curvature route fails or cannot be pulled through the final theorem surface honestly.

## Honest Minimum Phrasing

The minimum honest bridge phrasing for the lane is:

The original source family closes the fourth slot through curvature-based
regularity, and that source-faithful branch is now the primary fourth-bridge
route in the lane. On that source branch, the bridge itself is discharged. The
live lane also contains a Euclidean gradient-transfer package, but it is retained
only as an explicit fallback theorem-upgrade branch. What remains open is not the
existence of the fourth bridge; it is the pullback of that discharged curvature
bridge into the declared final theorem surface.

## Final Verdict

`discharged on the source-faithful branch; final theorem-surface pullback still open`

The source-grounded fourth bridge is clear and locally discharged.
The live Euclidean upgrade candidate is also clear.
What remains unresolved is whether the source-faithful curvature route can be
carried all the way through the final theorem surface without needing the
fallback Euclidean replacement.
