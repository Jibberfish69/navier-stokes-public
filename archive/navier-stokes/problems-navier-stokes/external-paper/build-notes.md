# External Paper Build Notes

This folder now contains the working external package for the Navier-Stokes conditional route paper lane.

## What Is Ready

- `metadata.tex` drives the title block macros used by `main.tex`, with the claim level aligned to the current conditional route theorem surface.
- `references.bib` contains a compact core bibliography spanning Leray/Hopf weak-solution foundations, standard monographs, partial regularity, blow-up criteria, and critical-space well-posedness.
- `sections/introduction.tex`, `sections/setup-notation.tex`, and `sections/prior-work-limits.tex` now carry in-text citations and explicit literature positioning.
- `main.tex` builds a clean referee-facing PDF at `main.pdf`.
- `dist/navier-stokes-full-claim-arxiv-source.tar.gz` and `dist/navier-stokes-full-claim-journal-package.tar.gz` are legacy bundle names from the earlier overclaim stage and should not be treated as current certification language.
- `main.tex` builds a clean referee-facing PDF at `main.pdf`.

## Remaining Before Final External Submission

- Add author-confirmed contact fields if a target venue requires them.
- Choose any venue-specific class/front-matter adjustments if a target journal requires them.
- Discharge the closed-loop warrant on the declared Euclidean theorem surface before treating the package as a full-claim submission.
