## `Section1-Introduction/index.tex`

Insert after line 6, before the first `\input`:

```tex
One smooth, rapidly decaying datum and one fixed viscosity generate a single maximal classical history, and the regularity problem asks whether that same history can stop being smooth at a finite time.
This manuscript calls the direct continuation route Gold and the contrapositive class-exit route Silver; both confront the gap between the energy control supplied by the equations and the Sobolev control needed to restart the solution.
The question is then carried back into the fluid itself: can an imagined failure still be the equation-generated motion when velocity, pressure, viscosity, incompressibility, and transport continue to act together?
Following that question through participation, the Euler comparison, the derivative tower, Dead, Jump, Blown, and restart produces the one-fluid law carried into \cref{sec:FromCriticalHeightToCompatibleFiniteViews}.
```

This first-use sentence is enough to mark Gold and Silver as names local to the manuscript, so I would not otherwise rewrite 1.3 or the approved 1.4 movement.

## `Section2-ProofProgram/index.tex`

Insert after line 6, before the first `\input`:

```tex
Section 1 leaves one alleged endpoint in view: the same fluid may remain classical at every earlier time while a higher response concentrates into smaller scales.
Critical height identifies the Sobolev reading that survives compression, finite time ties an escaping response to the rung above it, and the equations open those responses into one pressure-complete mixed jet.
Following the jet's spatial column through every finite Sobolev request leads to adjacent rectangles cut from that same history.
The decisive requirement is not finiteness after a strict cutoff has been chosen, but one bound for each fixed rectangle that survives every cutoff \(T<T_*\); this is the family carried into \cref{sec:TheCompatibleIntersectionAtTheTerminalTime}.
```

## `Section1-Introduction/1.5-ClassMembershipAndParticipation.tex`

Replace line 32 with:

```tex
For this fixed \(\varepsilon>0\), let \(\mathcal Q\) be the ambient collection of candidate velocity--pressure pairs on \([0,T_*+\varepsilon)\times\mathbb R^3\) with enough distributional regularity for the membership conditions above to be tested, and define
```

Replace lines 42--43 with:

```tex
Inside this schematic universe, Gold starts with \(Q\in\mathcal M\) and proves \(Q\in\mathcal S\), while Silver starts with \(Q\notin\mathcal S\) and must prove \(Q\notin\mathcal M\).
The formal closure in \cref{sec:FromCriticalHeightToCompatibleFiniteViews,sec:TheCompatibleIntersectionAtTheTerminalTime} does not range over arbitrary \(Q\in\mathcal Q\); it follows the fixed pressure-normalized classical history generated by \((u_0,\nu)\), constructs its endpoint, and restarts that same history.
```

## `Section1-Introduction/1.8-DeadJumpBlownAndRestart.tex`

Replace line 240 with:

```tex
To cross a candidate endpoint, this restart needs a continuation-grade bound on the incoming slice, and it needs velocity, pressure, and every required response to arrive there as one compatible field.
```

Delete lines 196--206, from `The critical scaling gives the concentration a second face.` through `it is not a constructed singular solution.`
The finite-velocity/infinite-derivative-scale example at lines 179--194 remains the only Blown scale example in 1.8, and the paragraph beginning `Blown can meet the other two scenes.` follows it without a replacement bridge.

## `Section1-Introduction/1.9-OneFluidOneCoherentFieldAndTheVPILaw.tex`

Replace lines 7--11 with:

```tex
That second requirement is the VPI law: viscosity, pressure, and incompressibility acting through one transported velocity field.
Transport carries each parcel into a changing neighbourhood, viscosity reads its local velocity curvature there, pressure transmits the divergence-free response of the whole field, and incompressibility constrains the motion they produce together.
Dead, Jump, and Blown are possible breaks in this simultaneous relation, not independent failures attached to separate points or derivatives.
Following a material packet makes the relation literal.
```

Replace lines 140--158, including `\divider{A Moving Material Graph}`, with exactly these two sentences:

```tex
Take one parcel together with the nearby parcels carried beside it by the exact flow map.
As their distances and directions change, viscosity reads the curvature of their shared velocity neighbourhood, pressure responds to the whole divergence-free field containing them, and the resulting acceleration remains the next response of that same parcel.
```

## `Section2-ProofProgram/2.1-CriticalHeightAtAFiniteEndpoint.tex`

Replace lines 88--118 with:

```tex
The public-estimates gap in \cref{sub:HistoricalEstimates} already showed that successively shorter parabolic episodes can fit before one finite time; it did not show that the same Navier--Stokes history generates them.
The exact coordinate retained under that compression is \(H(t)=\frac12\norm{u(t)}_{\dot H^{1/2}}^2\): rescaling can preserve this height while shortening both the active length and the time available.
One history must nevertheless move through the intervening states by its own equation, so critical height alone does not measure the speed required to reach the next state.
At a finite endpoint that missing speed becomes a relation between one response and the response immediately above it.
```

## `Section2-ProofProgram/2.2-FiniteTimeResponseEscalation.tex`

Replace lines 13--71 with:

```tex
The material escalation needed here was established in \cref{sub:SingularityAndTheDerivativeTower}: along one parcel, \(A_r(a,t)-A_r(a,s)=\int_s^t A_{r+1}(a,\tau)\,d\tau\), so escape of \(A_r\) at a finite \(T_*\) forces \(A_{r+1}\) to have infinite time integral and hence to be unbounded.
The statement remains conditional; steady and zero responses are lawful, and no escalation begins until one rung actually escapes.
The particle identity reveals the finite-time pressure, but it does not yet supply the fixed functional setting required by the Sobolev proof.
```

Keep lines 73--110, beginning `The same dependence appears before choosing a particle`, unchanged.

Replace lines 112--133 with:

```tex
Material and Eulerian responses remain distinct for the reason already made explicit in \cref{sub:SingularityAndTheDerivativeTower}: \(D_t=\partial_t+u\cdot\nabla\), so every higher material response combines Eulerian time rows with spatial derivatives and nonlinear products of lower rows.
The fixed-\(B\) implication above therefore says only what escape forces after one Eulerian row and its Bochner derivative have been placed in the same functional setting.
What the particle feels as one higher response must now be written in the temporal, spatial, and pressure coordinates opened by the equation.
The next object must record that full mixed response without splitting it into separate derivative stories.
```

## `Section2-ProofProgram/2.3-ThePressureCompleteMixedJet.tex`

Replace lines 8--90 with:

```tex
The pressure-complete temporal recurrence has already been derived in \cref{sub:SingularityAndTheDerivativeTower}.
For the fixed history, \(V_n=\partial_t^n u\) and \(Q_n=\partial_t^n p\): the \(n\)-th momentum row couples \(V_{n+1}\) to \(\Delta V_n\), every binomial split of the lower velocity rows, and \(\nabla Q_n\), while the normalized pressure Poisson row recovers \(Q_n\) from those same velocities.
Thus time differentiation creates neither an independent ladder nor a detachable pressure sequence.
What the temporal notation still conceals is how spatial derivatives distribute through each nonlinear product.
```

Resume at current line 91, `Differentiate one transport factor in the coordinate direction \(x_\ell\):`, and preserve the spatial product split, full mixed recurrence, and pressure coordinates from there onward.
