# Legacy Manuscript Root

This path keeps its historical name for compatibility, but it is machine-owned inside `papers/**`.

Use this tree only for paper-factory manuscript text, section ordering, and machine packaging decisions.
Human opening-phase drafting belongs outside `papers/**`.
