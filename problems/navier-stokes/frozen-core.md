# Navier--Stokes Global Regularity Frozen Core

This file freezes current proof authority. Older route files remain source
history only and cannot override this core.

## Theorem target

- `theorem_target`: silver-vpi-participation-complement-contrapositive
- `status`: logical-failure-classification-terminal-retention-unproved
- `authority`:
  `problems/navier-stokes/theorem-construction/mpp-silver-participation-complement-contrapositive-repair-20260722.md`
- `gold_relation`: parallel-open-research-nonblocking

## Fixed object

Fix one smooth divergence-free datum, one positive viscosity, and the one
no-reset original Navier--Stokes fluid history issuing from that datum. Full
VPI participation is the same history's transport, simultaneous nonlocal
pressure response, viscosity, incompressibility, material ancestry, and
coherent field realization. It is stronger than weak or distributional
equation satisfaction.

## Frozen Silver proof

```math
\neg\operatorname{Smooth}(Q)
\Longrightarrow
\neg\operatorname{Member}(Q)
\Longrightarrow
\neg\operatorname{VPIParticipation}(Q)
```

and, contrapositively,

```math
\operatorname{VPIParticipation}(Q)
\Longrightarrow
\operatorname{Member}(Q)
\Longrightarrow
\operatorname{Smooth}(Q).
```

The exhaustive negative inventory is Dead, Blown, Jump, Part failure, Field
failure, and a conditional other-PDE description of lost original
participation. Part and Field locate the one participation loss. Pack is
outside CM and may be used only as Field-window evidence.

Euler is not a starting equation or a reverse transfer. It appears only when
the original fixed-viscosity history is taken forward to a departure at which
the viscous term and every stress, forcing, pressure, incompressibility, and
ancestry defect vanish. Other residuals select their corresponding limiting
law. Every such law is evidence that the candidate is no longer the original
full VPI participant.

## Historical quarantine

Positive-obstruction/pass-exit, no-third-branch, no-genuine-exit, Pack-first,
terminal anti-atom, source-reserve, Tail.Hs export, and witness-bridge programs
are historical or support surfaces. They may supply a finer description of a
negative-side loss, but none is the live Silver engine or a remaining Silver
burden. Gold signed-height and other direct-continuation work remains parallel
research and does not block Silver, the smoothness theorem, or release.

## Authority order

1. `target-operating-contract.yaml`
2. `live-theorem-edge.yaml`
3. `source-frontier.yaml`
4. `theorem-packet.yaml`
5. `theorem-repair.yaml`
6. this frozen core

Generated mirrors and dated theorem-construction notes below this order are
provenance unless a current authority surface explicitly promotes them.
