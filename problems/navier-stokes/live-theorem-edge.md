# Navier-Stokes Live Theorem Edge

This is the narrow live authority cut for the current Navier-Stokes lane.

## Active Route

- Preferred branch: `TPS strong/defect bridge route`
- Internal branch surface: `torus-first periodic spectral/operator route`
- Exact unresolved theorem burden: `selector-adapted defect-to-strain theorem`
- Exact unresolved obstruction downstream of that burden: `SG.4`
- Secondary context only: `WERS.3Cap-BO`

## Exact Cut

The current live burden is the selector-adapted defect-to-strain theorem on the
torus-first TPS strong/defect bridge. The defect-PDE bridge and coarse
transport packet are formalized, while the exact remaining splice is:

```text
spectral / bundle transversality
+ pair-gap cocycle / loop control
-> selector calibration (PD.70)
-> SG.4
```

This is the only irreducible torus-first live theorem wall. The older
seven-item frontier count should be read only as a route dashboard:
torus-first branch lock, bridge-route label, strong-entry transfer, conditional
endpoint, downstream `\lambda_2^+` reduction, and the Euclidean strict-shadow
branch are not seven independent theorem lemmas.

On the cycle-exact / exact-potential side, the live theorem cut is the
polarization or alignment theorem, or on the normal-covector subroute one of
`(CN.13j)`, `(CN.13q)`, or `(CN.13ab)`-`(CN.13ad)`. On the general Hodge side,
once loop-from-harmonic export is granted, the genuine new core is
`(PD.70z12)`-`(PD.70z14)`. Any downstream `\lambda_2^+` continuation step still
requires the bridge `BR.lambda2` together with local strong-solution entry on
the continuation window. Any whole-space statement on `R^3` remains a separate
downstream discharge/export task. YM-heat/deformation geometry is upstream of
this splice; it is not the direct SG.4 theorem.

## Safe Claim Boundary

It is safe to claim that the lane has selected the TPS strong/defect bridge
route on a torus-first internal branch and that downstream theorem/paper
surfaces must preserve that branch choice explicitly.

## Unsafe Overclaim

It is not safe to claim unconditional periodic or whole-space regularity
without the `SG.4` directional lower-envelope theorem, the bridge
`BR.lambda2`, local strong-solution entry, and, for whole-space `R^3` claims,
an explicit downstream discharge/export theorem.

## Primary Surfaces

- `problems/navier-stokes/theorem-construction/live-frontier-honest-cut-reduction.md`
- `problems/navier-stokes/theorem-construction/tps-strong-bridge-sg-lemma-family.md`
- `problems/navier-stokes/theorem-construction/tps-dynamic-source-fit-and-open-frontier.md`
- `problems/navier-stokes/theorem-construction/tps-coarse-flow-pair-gap-theorem-candidate.md`
- `problems/navier-stokes/theorem-construction/spectral-transversality-in-measure-implies-pd70.md`
- `problems/navier-stokes/theorem-packet.yaml`
- `problems/navier-stokes/proof-assembly.yaml`
- `problems/navier-stokes/source-frontier.yaml`
- `problems/navier-stokes/proof-obligation-matrix.yaml`

## Paper Rule

Paper-facing exports must present the TPS strong/defect bridge as the live
torus-first route and keep `WERS.3Cap-BO` only as broader lane context.
