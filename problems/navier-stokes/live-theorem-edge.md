# Navier-Stokes Live Theorem Edge

This is the narrow live authority cut for the current Navier-Stokes lane.

## Active Route

- Preferred branch: `WERS.3Cap-BO`
- The live endpoint is a capwise operator theorem first, not a symbol-seminorm theorem first.
- Secondary fallback only: `WERS.3R`

## Exact Cut

The current live burden is to upgrade the resonant-shell endpoint through the
capwise branch `WERS.3Cap-BO`, with `WERS.3Cap-TT` as the tandem capwise
transfer path, while keeping bare `WERS.3R` symbolic arguments context-only.

## Safe Claim Boundary

It is safe to claim that the lane has selected the capwise operator theorem
route as the narrow authority edge and that downstream theorem/paper surfaces
must preserve that branch choice explicitly.

## Unsafe Overclaim

It is not safe to claim that the capwise endpoint already closes the Clay
problem, or that the symbolic fallback `WERS.3R` has been promoted to the live
edge.

## Primary Surfaces

- `problems/navier-stokes/theorem-construction/weighted-exact-projection-resonant-shell-theorem-candidate.md`
- `problems/navier-stokes/theorem-packet.yaml`
- `problems/navier-stokes/proof-assembly.yaml`
- `problems/navier-stokes/source-frontier.yaml`
- `problems/navier-stokes/proof-obligation-matrix.yaml`

## Paper Rule

Paper-facing exports must present `WERS.3Cap-BO / WERS.3Cap-TT` as the live
route and keep `WERS.3R` as fallback context only.
