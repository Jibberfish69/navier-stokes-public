---
version: 1
identity:
  problem_id: navier-stokes
  display_name: Navier-Stokes Global Regularity
  plan_id: post-release-plan-navier-stokes
  generated_at: '2026-06-08T03:11:19Z'
  source_release_decision_id: release-decision-navier-stokes
  source_review_verdict_id: review-verdict-navier-stokes
current_release:
  disposition: respawn
  release_posture: revise-loop
  completion_tier_target: full-mpp-closure
  completion_tier_achieved: paper-complete-theorem-open
tracks:
  theorem-upgrade:
    applicable: true
    target_tier: full-mpp-closure
    theorem_gap:
    - The theorem packet localizes definitions, lemma surfaces, theorem statement,
      and proof dependencies strongly enough to support export-facing review.
    - The local package is stable enough to review, and `draft-v8.md` now carries
      real prose, but it is still a revise-stage draft rather than an export-ready
      paper.
    - The `draft-v7.md` surface is now a prose artifact, so this pass can review a
      real package rather than only planning notes.
    - The formalization ledger still marks stand-alone export as `revise`, which blocks
      full-MPP closure until the remaining scope boundary is rewritten or discharged.
    reviewer_risks:
    - Recovered support is still classified as strong, so the local package remains
      a revise-stage workboard.
    recommended_next_cell_type: debt-extract
    recommended_next_stage: theorem-upgrade-requested
    recommended_next_action: 'Reopen the current checkpoint package toward `full-mpp-closure`
      and localize the remaining theorem delta beginning with: The theorem packet
      localizes definitions, lemma surfaces, theorem statement, and proof dependencies
      strongly enough to support export-facing review. Start with `debt-extract` so
      the reopened lane is typed again before the next review cycle.'
    rationale: The current checkpoint package is below `full-mpp-closure`, so the
      next honest move is to reopen the theorem delta explicitly rather than pretending
      the lowest safe claim is the final proof.
  publication-finalize:
    applicable: true
    target_tier: paper-complete-theorem-open
    draft_surface_kinds:
      draft_v6: draft
      draft_v7: draft
    missing_publishability_surfaces:
    - No dedicated publication-finalization pass has yet compressed the current checkpoint
      package into a referee-facing final manuscript.
    - There is still no typed publication checklist artifact for export/submission
      handling.
    recommended_next_cell_type: prose-realization
    recommended_next_stage: publication-finalize-requested
    recommended_next_action: Reopen the current checkpoint package on `prose-realization`
      and compress it into a referee-facing final draft, using `draft-v7.md` as the
      publishability surface rather than leaving the package at lowest-safe-claim
      checkpoint posture only.
    rationale: The package reached a real checkpoint, but it still lacks a dedicated
      final-paper compression pass.
selected_track: theorem-upgrade
summary:
  reopen_track: theorem-upgrade
  reopen_next_cell_type: debt-extract
  reopen_next_stage: theorem-upgrade-requested
  reopen_next_action: 'Reopen the current checkpoint package toward `full-mpp-closure`
    and localize the remaining theorem delta beginning with: The theorem packet localizes
    definitions, lemma surfaces, theorem statement, and proof dependencies strongly
    enough to support export-facing review. Start with `debt-extract` so the reopened
    lane is typed again before the next review cycle.'
  rationale: The current checkpoint package is below `full-mpp-closure`, so the next
    honest move is to reopen the theorem delta explicitly rather than pretending the
    lowest safe claim is the final proof.
