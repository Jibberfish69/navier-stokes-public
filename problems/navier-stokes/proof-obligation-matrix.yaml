---
version: 1
identity:
  problem_id: navier-stokes
  display_name: Navier-Stokes Global Regularity
  matrix_id: proof-obligation-matrix-navier-stokes
  generated_at: '2026-04-13T00:10:00Z'
  generator: system/runner/lib/proof_obligation_matrix_builder.rb
  source_theorem_packet_id: theorem-packet-navier-stokes
  source_review_verdict_id: review-verdict-navier-stokes
posture:
  theorem_target: no finite-time class exit from shared packing, shared participation, and shared one-field coherence
  current_package_status: conditional lane-closure packet installed under retained-window receiver hypotheses, bounded pack gauge, finite required parameter set, post-LCI.A source closure, and accepted endpoint certificates
  standalone_status: receiver/source/endpoint assembly now records `CFI.A + End_NS => no finite-time class exit` conditionally on the installed theorem packets
  completion_tier_achieved: route-complete
  review_verdict: conditional-pass
  live_theorem_edge_present: true
  live_theorem_edge_route_label: Class-membership forward-invariance / endpoint-exclusion route
object_law_refoundation:
  status: active
  route_table: problems/navier-stokes/ns-proof-program-route-table.yaml
  closure_ecology: problems/navier-stokes/ns-proof-program-route-table.yaml#closure_ecology
  branch_status:
    class_membership_core: constitutive-root
    dead_blown_jump: object-law exclusion branch
    selector_sg4: downstream one-field-coherence consumer wall after direct matrix work
    exact_potential: derived readout subbranch on the Law-2-survivor side
    hodge_normal_covector: derived readout subbranch on the Law-2-survivor side
    ym_heat_shadow_carrier: derived supplier/export branch on the Law-2-survivor side
    euclidean_strict_shadow: downstream export branch on the Law-2-survivor side
  theorem_queue:
    - "record `ORIGIN.Retain` as installed: original maximal-solution/class-membership setup supplies retained-window hypotheses on compact classical intervals and endpoint persistence follows from `CFI.A+End_NS`"
    - "paperize the retained receiver spine as `RSCB.NKF => ACT.A => RCF.A => LCI.A`, with `NKF.Native`, `ACT.KX-Boot`, and `RWS.C_scale` kept as internal retained-window mechanisms"
    - "paperize the finite-energy-native receiver replacement as `SCF_base + SGC.A_a.e + ATD_m^epsilon => SCF_avg^m => AACT.KX off B_epsilon^Phi => DTC.A_avg => AVG.RCV.A => LCI.A_avg => CSP.A_avg => OFP.A_avg => CFI.A_avg => AVG.MAIN.A`, with finite energy supplying only `SCF_base`, `ATD_m^epsilon` as the local epsilon-regularity tower supplier, `AVG.COVER.A := SGC.A_a.e + ATD_m^epsilon + CAVG.J => full same-fluid tail coverage`, `CAVG.J` folding nonempty `B_epsilon^Phi` directly into `Jump_avg`, `AVG.MAIN.A := SCF_base + ATD_m^epsilon + CAVG.J + AVG.RCV.A + AVG.END.A => no averaged finite-time class exit`, `DTC.Read^epsilon` parked for pointwise endpoint/tower readout, and `Field.Read^epsilon` / `CM.Read` required before old `CFI.A`"
    - "keep the source side post-`LCI.A`: `FCC.C1 -> FSCR.C -> FPCR.C -> FCI.5f`"
    - "paperize the retained endpoint spine as `DTC-to-TowerBound + END.Exh + END.Cross => End_NS`, with `END.Pack` and `END.Field` as installed certificate exclusions"
    - "paperize the averaged endpoint spine as `CFI.A_avg + AVG.END.Cert => End_NS_avg`, with `END.Exh_avg` returning to the four faces `Dead | packing-detached | tower-blown_avg | Jump_avg`, `CAVG.J := B_epsilon^Phi nonempty => Jump_avg`, `END.Cross_avg` locked, `READ.END := End_NS_avg + Field.Read^epsilon + DTC.Read^epsilon => End_NS`, and `SGC.A_strong` parked off the main line as regularity-equivalent"
    - "paperize the terminal spine as `CFI.A + End_NS => no finite-time class exit`"
    - "preserve the boundary: retained-window receiver hypotheses, bounded pack gauge, finite required parameter set, post-`LCI.A` source closure, and accepted endpoint certificate maps"
    - "keep TPS / SG.4, exact-potential, Hodge / normal-covector, and Euclidean strict-shadow quarantined as downstream context only"
live_theorem_edge:
  route_id: live-authority-edge-navier-stokes
  route_label: Class-membership forward-invariance / endpoint-exclusion route
  current_route_name: Class-membership forward-invariance / endpoint-exclusion route
  exact_cut_summary: >-
    The class-membership route now carries a conditional terminal closure
    packet, with the receiver side split into two typed branches. The retained
    branch uses installed `RSCB.NKF` as the retained smooth center-ball native
    forcing certificate and runs
    `RSCB.NKF => NKF.Native => ACT.KX => ACT.X-Readout => ACT.A => RCF.A => LCI.A`.
    The finite-energy-native replacement targets
    `SCF_base + SGC.A_a.e + ATD_m^epsilon => SCF_avg^m => AACT.KX off B_epsilon^Phi => DTC.A_avg => AVG.RCV.A => LCI.A_avg => CSP.A_avg => OFP.A_avg => CFI.A_avg => AVG.MAIN.A`, with `AVG.COVER.A` covering every same-fluid approach tail: good material region gives averaged propagation and nonempty `B_epsilon^Phi` gives `Jump_avg` by `CAVG.J`, and `AVG.MAIN.A` compressing `SCF_base + ATD_m^epsilon + CAVG.J + AVG.RCV.A + AVG.END.A => no averaged finite-time class exit`.
    `DTC.Read^epsilon` stays parked for pointwise endpoint/tower readout, and
    `Field.Read^epsilon` / `CM.Read` is required before old `CFI.A`. Retained source closure is the post-`LCI.A`
    frozen-family packet `FCC.C1 -> FSCR.C -> FPCR.C -> FCI.5f`, with cutoff
    absorption, strain/cascade low-carrier control, and pressure
    low-high/high-low plus resonant/far-tail control. The organized stronger
    source branch also closes as `FFPB.A0+FFSC.C1+FCC.C1 => FFSRC.A => FCI.5f`,
    with `FFPB.A0` solved by reading `H^{press,loc}` as the packet-local
    weighted pressure carrier; a centerwise-only reading reopens `FIPCB.A0`.
    Retained one-field assembly then
    runs `LCI.A+FCI.5f => CSP.A => OFP.A => CFI.A`; averaged assembly runs
    `LCI.A_avg+FCI.5f => CSP.A_avg => OFP.A_avg => CFI.A_avg => End_NS_avg => AVG.MAIN.A`.
    Retained endpoint closure runs
    `DTC-to-TowerBound + END.Exh + END.Cross + END.Pack + END.Field => End_NS`;
    averaged endpoint closure runs `CFI.A_avg+AVG.END.Cert => End_NS_avg`,
    with `READ.END := End_NS_avg+Field.Read^epsilon+DTC.Read^epsilon => End_NS`.
    The two-lane
    terminal closure packet is now recorded as endpoint
    analytic readout (`DTC-to-TowerBound`) plus source channels
    `FCC.C1 -> FSCR.C -> FPCR.C -> FCI.5f`, with `END.Exh` and `END.Cross`
    recorded in parallel as certificate/semantic cells. Therefore
    `CFI.A + End_NS => no finite-time class exit`. `ORIGIN.Retain` is now
    installed as the original-data upgrade: it derives retained-window
    hypotheses on compact classical intervals and uses `CFI.A+End_NS` to
    exclude first finite retained-window/class endpoints. The older TPS /
    `SG.4` selector wall remains only downstream context.
  paper_treatment: '["problems/navier-stokes/theorem-construction/mpp-class-membership-contradiction-frontier-packet.md",
    "problems/navier-stokes/theorem-construction/mpp-class-membership-forward-invariance-theorem-program.md",
    "problems/navier-stokes/theorem-construction/mpp-one-field-forward-preservation-theorem-program.md",
    "problems/navier-stokes/theorem-construction/mpp-lower-carrier-interval-integrability-theorem-program.md",
    "problems/navier-stokes/theorem-construction/mpp-act-kx-conditional-route-completion-packet.md",
    "problems/navier-stokes/theorem-construction/mpp-act-kx-hidden-spending-audit-note.md",
    "problems/navier-stokes/theorem-construction/mpp-averaged-act-kx-scale-critical-forcing-packet-note.md",
    "problems/navier-stokes/theorem-construction/mpp-lci-a-pre-csp-still-live-receiver-admission-note.md"]'
  branch_choice_labels:
  - CFI.A / OFP.A / LCI.A class-membership route
obligations:
- obligation_id: theorem-statement
  kind: theorem-statement
  label: The official Clay target allows either the whole-space branch on R^3 or the
    periodic branch on T^3.
  status: grounded
  source_anchor: problems/navier-stokes/marvin-ingest-notes.yaml
  blocking: false
  notes: []
- obligation_id: marvin-upstream-eb3fabeeac46
  kind: definition
  label: 'Define or prove: Reduced the live Navier-Stokes route stack against the
    Clay whole-space target into exact source-backed ingredients already present on
    the lane. On the classical side, isolated the mixed-jet system, the exact rung-level
    can'
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260405T014725Z-6ece27230fc9.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-ada9835aa864
  kind: definition
  label: 'Define or prove: For the mixed-jet tower J_ m,alpha  = \partial_t^m \partial_x^\alpha
    u and differentiated pressure \Pi_ m,alpha , fix s > 3/2. Using the H^s algebra
    property and the order-1 Calderon-Zygmund bound for the pressure operator,'
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260405T015459Z-d5f90422800c.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-6d929e8a052a
  kind: definition
  label: 'Define or prove: Added three theorem-facing reductions on the Navier-Stokes
    lane. First, weighted-mixed-jet-next-time-envelope-reduction.md proves the exact
    whole-tower inequality A_next_ s-1 (tau,r) <= nu D_s(tau,r) + C_s A_s(tau,r)^2,
    red'
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260405T020719Z-04949830208e.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-954339945793
  kind: definition
  label: 'Define or prove: Added four theorem-construction reductions. (1) strict-shadow-no-remainder-corollary.md:
    under the exact strict-shadow identities already present on the lane, ShadowDef(X)
    and E_sh(X) vanish identically, so the exact Route '
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260405T020853Z-1ed78e356031.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-96892bab139f
  kind: definition
  label: 'Define or prove: Added two new theorem-facing reductions on the Clay whole-space
    target.'
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260405T022825Z-ed5d2bb9bea4.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-409f183ee495
  kind: definition
  label: 'Define or prove: Completed the Hopf/shuffle line as a theorem-program refinement.
    The correct algebraic fit for the time tower is the divided-power Hopf law on
    one primitive generator, and for the mixed jet tower it is the shuffle/unshuffle'
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260407T191622Z-e3045e4591c9.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-c8f7cb511776
  kind: definition
  label: 'Define or prove: Formalized the refined direct Lagrangian route as a six-lemma
    mainline: differentiated Lagrangian decomposition, deformation-adapted energy,
    ellipticity window, coefficient-commutator absorption, no-loss pressure recovery, '
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260407T194404Z-c14350fd2eb1.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: marvin-upstream-e2f5b0a915cf
  kind: definition
  label: 'Define or prove: Updated lemma-4b-no-loss-pressure-recovery-attack.md with
    a geometry-testing ladder for the elliptic test field W_test = A^T grad Phi solving
    -div_a(G grad Phi)=div_a Z_alpha. The note now distinguishes three rungs: (1)
    ene'
  status: unresolved
  source_anchor: system/meta/research-os/promotions/navier-stokes/capture-20260407T201618Z-94497e50e8c4.md
  blocking: true
  notes:
  - Current packet status is `needs-localization`.
  - This obligation is not yet discharged locally.
- obligation_id: claim-ladder-1
  kind: lemma
  label: The Clay target is the official 3D incompressible Navier-Stokes problem on
    `R^3` or `T^3`
  status: unresolved
  source_anchor: "`obstruction-capture: One-Sentence Thesis`, `spine.1`"
  blocking: true
  notes:
  - Current packet status is `claim-ladder`.
  - This obligation is not yet discharged locally.
- obligation_id: claim-ladder-2
  kind: lemma
  label: Success for regularized or morphic-regularized equations does not close the
    original problem
  status: unresolved
  source_anchor: "`obstruction-capture: Why Prior Attempts Fail`, `spine.2`, `target-fidelity-discharge-source-pack.md`,
    `theorem-construction/target-fidelity-bridge-lemma.md`"
  blocking: true
  notes:
  - Current packet status is `claim-ladder`.
  - This obligation is not yet discharged locally.
- obligation_id: claim-ladder-3
  kind: lemma
  label: The original framework names a monotone functional `Q(t)` as a bridge, not
    just unnamed baseline energy
  status: unresolved
  source_anchor: "`source-grounded-framework-map.md`, `source-forensics/master-program-analysis.md`,
    `source-forensics/canonical-equation-ledger.md`, `spine.3`, `theorem-construction/monotone-functional-package.md`,
    `theorem-construction/named-monotone-bridge-explanation.md`, `bridge-memos/monotone-functional.md`"
  blocking: true
  notes:
  - Current packet status is `claim-ladder`.
  - This obligation is not yet discharged locally.
- obligation_id: claim-ladder-4
  kind: lemma
  label: Global energy or enstrophy control is necessary but not sufficient
  status: unresolved
  source_anchor: "`obstruction-capture: Core Mechanism`, `spine.4`, `theorem-construction/energy-baseline-bridge-lemma.md`,
    `theorem-construction/monotone-functional-package.md`"
  blocking: true
  notes:
  - Current packet status is `claim-ladder`.
  - This obligation is not yet discharged locally.
- obligation_id: claim-ladder-5
  kind: lemma
  label: The live obstruction is the small-scale or high-frequency cascade that feeds
    blow-up
  status: unresolved
  source_anchor: "`obstruction-capture: The Real Obstruction`, `spine.5`, `theorem-construction/energy-baseline-bridge-lemma.md`"
  blocking: true
  notes:
  - Current packet status is `claim-ladder`.
  - This obligation is not yet discharged locally.
- obligation_id: claim-ladder-6
  kind: lemma
  label: A periodic shell or dyadic tail estimate must suppress dangerous high-frequency
    transfer on the chosen torus-first theorem surface
  status: unresolved
  source_anchor: "`source-forensics/canonical-equation-ledger.md`, `theorem-construction/scale-barrier-package.md`,
    `theorem-construction/torus-projected-flow-extraction.md`, `bridge-memos/scale-barrier.md`,
    `spine.6`"
  blocking: true
  notes:
  - Current packet status is `claim-ladder`.
  - This obligation is not yet discharged locally.
- obligation_id: warrant-theorem-2.1
  kind: theorem-warrant
  label: Theorem 2.1 (Classical Closure)
  status: grounded
  source_anchor: working-notes/ontic-nc-ns-ymg-hodge-program.md
  blocking: false
  notes:
  - Theorem-to-warrant marks this theorem claim as `conditional`.
  - The lane now contains concrete discharge loci for scale, monotone, compactness,
    and the torus-first projected-flow continuation side. The authoritative theorem
    remains conditional because SG.4 and its downstream continuation handoff are still
    open. Any Euclidean whole-space theorem on `R^3` is downstream export, not part
    of the internal warrant carried here.
  - The lane now contains concrete discharge loci for scale, monotone, compactness,
    and the torus-first continuation side. However, the authoritative theorem remains
    conditional because SG.4 and its continuation handoff are still open, and any
    whole-space Euclidean claim remains downstream discharge/export.
  - Current packet status is `localized-support`.
- obligation_id: proof-dependency-1
  kind: proof-dependency
  label: The Clay target is the official 3D incompressible Navier-Stokes problem on
    `R^3` or `T^3` -> Success for regularized or morphic-regularized equations does
    not close the original problem
  status: bridge-pending
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain direction is visible in the recovered route, but this bridge
    still needs explicit local discharge before submission.
- obligation_id: proof-dependency-2
  kind: proof-dependency
  label: Success for regularized or morphic-regularized equations does not close the
    original problem -> The original framework names a monotone functional `Q(t)`
    as a bridge, not just unnamed baseline energy
  status: bridge-pending
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
  - Dependency chain direction is visible in the recovered route, but this bridge
    still needs explicit local discharge before submission.
- obligation_id: proof-dependency-3
  kind: proof-dependency
  label: The original framework names a monotone functional `Q(t)` as a bridge, not
    just unnamed baseline energy -> Global energy or enstrophy control is necessary
    but not sufficient
  status: structurally-supported
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: false
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
  - Dependency chain is strongly supported by the recovered local object layer, but
    it is not yet discharged as a formal submission-safe bridge.
- obligation_id: proof-dependency-4
  kind: proof-dependency
  label: Global energy or enstrophy control is necessary but not sufficient -> The
    live obstruction is the small-scale or high-frequency cascade that feeds blow-up
  status: unresolved
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
- obligation_id: proof-dependency-5
  kind: proof-dependency
  label: The live obstruction is the small-scale or high-frequency cascade that feeds
    blow-up -> A periodic shell or dyadic tail estimate must suppress dangerous high-frequency
    transfer on the chosen torus-first theorem surface
  status: unresolved
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
- obligation_id: proof-dependency-6
  kind: proof-dependency
  label: A periodic shell or dyadic tail estimate must suppress dangerous high-frequency
    transfer on the chosen torus-first theorem surface -> The monotone functional
    must stabilize the global energy/enstrophy layer and enable the compactness package
    on that same periodic classical surface
  status: bridge-pending
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
  - Dependency chain direction is visible in the recovered route, but this bridge
    still needs explicit local discharge before submission.
- obligation_id: proof-dependency-7
  kind: proof-dependency
  label: The monotone functional must stabilize the global energy/enstrophy layer
    and enable the compactness package on that same periodic classical surface ->
    The scale-barrier plus monotone-functional input must yield strong local compactness
    and nonlinear tensor closure on the periodic/operator approximation family
  status: bridge-pending
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
  - Dependency chain direction is visible in the recovered route, but this bridge
    still needs explicit local discharge before submission.
- obligation_id: proof-dependency-8
  kind: proof-dependency
  label: The scale-barrier plus monotone-functional input must yield strong local
    compactness and nonlinear tensor closure on the periodic/operator approximation
    family -> A heat-governed deformation or continuation bridge must force continuation
    control on the same periodic theorem surface
  status: unresolved
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain is still only structural; the proof links are not yet discharged
    into a submission-safe theorem packet.
- obligation_id: proof-dependency-9
  kind: proof-dependency
  label: A heat-governed deformation or continuation bridge must force continuation
    control on the same periodic theorem surface -> If the scale-barrier, monotone-functional,
    compactness, and continuation packages all hold on the active periodic branch,
    then the periodic Clay branch with `f = 0` follows; any whole-space statement
    on `R^3` is a downstream discharge theorem, not the internal branch itself
  status: bridge-pending
  source_anchor: Later theorem prose depends on the earlier claim being localized
    first.
  blocking: true
  notes:
  - Dependency chain direction is visible in the recovered route, but this bridge
    still needs explicit local discharge before submission.
- obligation_id: marvin-route-authority-4
  kind: branch-choice
  label: 'Theorem packet: Added four theorem-construction reductions. (1) strict-shadow-no-remainder-corollary.md:
    under the exact strict-shadow identities already present on the lane, ShadowDef(X)
    and E_sh(X) vanish identically, so the exact Route A'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-5
  kind: branch-choice
  label: 'NC Route A: theorem-construction/euclidean-strict-shadow-clay-discharge-criterion.md'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-6
  kind: branch-choice
  label: 'Live task: Completed the Hopf/shuffle line as a theorem-program refinement.
    The correct algebraic fit for the time tower is the divided-power Hopf law on
    one primitive generator, and for the mixed jet tower it is the shuffle/unshuffle
    or sy'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-8
  kind: branch-choice
  label: 'Live task: Built an honest attack package for the three remaining hopf/shuffle
    endgame gaps. Gap 1: the jet quotient is explicit after killing pressure and transport,
    but the lifted quotient still fails to close because scale/time/divergenc'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-9
  kind: branch-choice
  label: 'Live task: Formalized the refined direct Lagrangian route as a six-lemma
    mainline: differentiated Lagrangian decomposition, deformation-adapted energy,
    ellipticity window, coefficient-commutator absorption, no-loss pressure recovery,
    and La'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-12
  kind: branch-choice
  label: 'Live task: Updated lemma-4b-no-loss-pressure-recovery-attack.md with a geometry-testing
    ladder for the elliptic test field W_test = A^T grad Phi solving -div_a(G grad
    Phi)=div_a Z_alpha. The note now distinguishes three rungs: (1) energy-le'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-13
  kind: branch-choice
  label: 'Headline: Verified Route A reduction and isolated the remaining coefficient-window
    obstruction'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-14
  kind: branch-choice
  label: 'Live task: Updated lemma-4b-no-loss-pressure-recovery-attack.md to replace
    the old formal Route A candidate with an exact verified reduction. The note now
    proves div X_pr = -L_G P_alpha - div R_alpha for X_pr = div_a(AG∇_a U_alpha),
    P_alpha'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-19
  kind: branch-choice
  label: 'Live task: Pushed the theory forward on the Lagrangian Stokes bridge. Verified
    that a genuine frozen NS time slice with F_*=∇_a X_*, A_*=F_*^{-1}, G_*=A_*A_*^T
    is exactly conjugate to classical Stokes by the frozen nonlinear map x=X_*(a);
    s'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-22
  kind: branch-choice
  label: 'Live task: Exact frozen-flow-map reduction now discharges the genuine frozen
    spatially varying slice: the frozen Lagrangian Stokes system is exactly conjugate
    to classical Stokes by the frozen flow map. The evolving bridge is now rewritten'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-24
  kind: branch-choice
  label: 'Live task: The short-slab bridge has been sharpened again. U1 is now effectively
    discharged on the slab surface: in the Bogovskii-corrected formulation one may
    take Pi = pi exactly, so there is no independent pressure-transfer term, and the'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-25
  kind: branch-choice
  label: 'Live task: Current far-corona route has been tightened materially. On the
    shell-pair commutator surface, an amplitude-free absorption theorem |C_{m,j}|
    <= eps nu D_j + K_N is structurally false by scaling, because C_{m,j} is cubic
    in the ve'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-26
  kind: branch-choice
  label: 'Headline: Far-corona route pivot: amplitude-free shell absorption false;
    coefficient-bearing shell-pair branch closes on upgraded carrier'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-27
  kind: branch-choice
  label: 'Headline: Fixed-collar coefficient branch closes on upgraded carrier; exact
    external bridge measures are nu_N and mu_N'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-28
  kind: branch-choice
  label: 'Live task: Pushed the far-corona route forward in theorem-grade form. First,
    the amplitude-free shell-pair absorption fantasy is ruled out by scaling: the
    shell-pair commutator is cubic in amplitude while D_j is quadratic, so no estimate
    of'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-31
  kind: branch-choice
  label: 'Live task: Sharpened the reserve globalization program after the capwise
    signed defect step. The safe sign scale is now the net cap-graph coercive coefficient
    kappa_j^{net}=inf_omega(d_{j,omega}-o_{j,omega}) rather than a prematurely fixed'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-37
  kind: branch-choice
  label: 'Define or prove: The Navier-Stokes selector program has been sharpened further
    on disk. The new packet transported-phase-space-selector-theorem-candidate.md
    now treats the remaining carrier verification as a selector/localizer theorem
    on to'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-38
  kind: branch-choice
  label: 'Live task: The Navier-Stokes selector program has been sharpened further
    on disk. The new packet transported-phase-space-selector-theorem-candidate.md
    now treats the remaining carrier verification as a selector/localizer theorem
    on top of t'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-40
  kind: branch-choice
  label: 'Live task: Navier-Stokes selector packet sharpened further. In transported-phase-space-selector-theorem-candidate.md,
    the dynamic burden is no longer only the vanishing-bad-set lemma `(TPS.4h)`-`(TPS.4i)`.
    Added `TPS-ns-crit`, reducing `(TP'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-42
  kind: branch-choice
  label: 'Live task: Transported selector dynamic packet improved further. In transported-phase-space-selector-theorem-candidate.md,
    added two reserve sufficient mechanisms for the bespoke dynamic burden `(TPS.4i)`:
    `(TPS.4r)`-`(TPS.4w)` measure-line'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-44
  kind: branch-choice
  label: 'Live task: Further dynamic-route sharpening landed. In transported-phase-space-selector-theorem-candidate.md,
    added `TPS-drift-collapse` `(TPS.4ab)`-`(TPS.4ah)`: if bad-pair mass vanishes,
    initial selector gaps are tight, good active pairs'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-45
  kind: branch-choice
  label: 'Live task: Internet-backed route sweep integrated into the selector packet.
    Added normalized theorem family `TPS.4(*)` `(TPS.4ai)`-`(TPS.4am)`: one common
    Lagrangian bad set, one common overlap law, three dynamic mechanisms (selector-label'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-48
  kind: branch-choice
  label: 'Live task: Promoted the remaining dynamic wall to a single named theorem
    in transported-phase-space-selector-theorem-candidate.md: `TPS-ns-good-set-transversality`.
    This bundles the good-set measure condition with pairwise measurable linear'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-50
  kind: branch-choice
  label: 'Live task: Refined the dynamic core theorem in transported-phase-space-selector-theorem-candidate.md.
    Replaced the placeholder transversality theorem with a cleaner named packet `TPS-ns-good-set`
    using dedicated GS.* hypotheses: good select'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-51
  kind: branch-choice
  label: 'Live task: Further corrected the dynamic selector packet. The scalar-label
    section in transported-phase-space-selector-theorem-candidate.md no longer presents
    `alpha_j` as a candidate primitive object on the live carrier. It now states expl'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-53
  kind: branch-choice
  label: 'Live task: The dynamic selector packet is now internally corrected and tightened.
    The local pairwise nonshadowing criterion uses an oriented observable m_{Q,Q''}
    with one-sided drift in the shadow zone, not d/dt|m|. The good-set theorem and'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-63
  kind: branch-choice
  label: 'Headline: Formalized HWY contradiction packet and direct SG.4 realization
    from fresh-angle lambda2-plus feed'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-71
  kind: branch-choice
  label: 'Live task: Tightened the live Navier-Stokes SG.4 bridge packet inside problems/navier-stokes
    only. In the selector-observability proof attempt, promoted the already implicit
    residual pairwise closure into the exact named theorem: after the'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-74
  kind: branch-choice
  label: 'Live task: Integrated the user-supplied theorem-bearing refinement into
    the Navier-Stokes SG.4 packet. The live theorem notes now make the branch-sensitive
    endgame explicit: the exact remaining general burden is still the residual pairwise'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-79
  kind: branch-choice
  label: 'Live task: Integrated the new theorem-bearing clarification of the open
    SG.4 bridge. The exact-potential / cycle-exact branch is now packaged explicitly
    as the preferred local endgame: a cycle-exact polarization lemma defines the expanding'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-82
  kind: branch-choice
  label: 'Live task: Refined the exact-potential PD.70z endgame one step further.
    The selector bridge note now proves the formal simple-top reduction from the angle
    variable alpha_J=1-(e_ab·n_J)^2 to the one-sided directional deficit and strip
    budget'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-84
  kind: branch-choice
  label: 'Live task: Refined the cycle-exact normal-covector route further. The route
    note now proves a finite-measure square-root upgrade from the weighted L4 closure
    CN.13 to the directional ledger E_dir^perp on selector-good strips: if the pair/ti'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-86
  kind: branch-choice
  label: 'Live task: Integrated the exact theorem-bearing closure the user supplied.
    The selector bridge note now contains a formal theorem-schema no-go proposition:
    signless W_J, its current PDE coercive bounds, and cycle exactness do not suffice
    to'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-90
  kind: branch-choice
  label: 'Live task: Searched for abstract non-NS replacements for the cycle-exact
    alignment idea and did not find a source-backed route that is genuinely better.
    The nearest abstract competitors are Davis-Kahan style eigenspace perturbation,
    dominat'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-92
  kind: branch-choice
  label: 'Live task: Abstract external search did not produce a source-backed proof
    of the final selector calibration, but it did produce a cleaner theorem formulation
    than the older polarity-map packet. The exact-potential simple-top branch is now
    b'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-96
  kind: branch-choice
  label: 'Live task: Audited the projective endgame against the external sources and
    split the remaining burden formally. In problems/navier-stokes/theorem-construction/cycle-exact-projective-synchronization-packet.md,
    transport coherence is now clos'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-98
  kind: branch-choice
  label: 'Live task: Updated the projective synchronization packet and canonical Navier-Stokes
    surfaces so the live open theorem is explicitly PSJ+, not the vague older request
    for some synchronization observable from W_J. The exact remaining burden'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-99
  kind: branch-choice
  label: 'Define or prove: Audited the proposed normalized Cauchy-Green lift against
    the live exact-potential packet. Formalized Proposition D.7ma in problems/navier-stokes/theorem-construction/tps-selector-observability-to-sg4-bridge-proof-attempt.m'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-101
  kind: branch-choice
  label: 'Live task: Updated the cycle-exact projective packet so the surrogate-construction
    side is now formal: the localized projector-moment tensor \widetilde S_J built
    from transported pair geometry is measurable, symmetric, and PSD, and local co'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-104
  kind: branch-choice
  label: 'Live task: Updated the cycle-exact projective packet so the open burden
    is no longer raw projector concentration in the abstract. The packet now proves
    that if one has a persistent simple-top local strain regime, then PSJ+.6 follows
    by pair'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-105
  kind: branch-choice
  label: 'Live task: Corrected the theorem-facing route wording across the canonical
    Navier-Stokes surfaces. The overall exact-potential open theorem remains PSJ+,
    not the narrower persistent simple-top local strain regime PSJ+.4. On the localized
    pr'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-107
  kind: branch-choice
  label: 'Live task: Tightened the cycle-exact projective synchronization packet to
    make the persistent simple-top regime a named assumption packet `PSJ+.SST` rather
    than an anonymous inline hypothesis block. `PSJ+.4` now explicitly depends on
    `PSJ+.'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-111
  kind: branch-choice
  label: 'Live task: Added Proposition PSJ+.5 to the cycle-exact projective packet.
    It shows that on the intended active family, Assumption PSJ+.SST follows from
    exact segment transport (eta=0), a simple-top gap floor, a corotational drift
    budget der'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-116
  kind: branch-choice
  label: 'Live task: Strengthened the YM/deformation theorem schema in the spectral
    transversality bridge note. The better repo-style target is not a monolithic theorem
    ''YM/deformation implies PSJ+.SST''. That over-hides the live obligations. The
    shar'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-118
  kind: branch-choice
  label: 'Define or prove: Improved the YM/deformation route target beyond the earlier
    DG-PSJ stack. The sharper repo-native factorization is now DG-Line (deformation
    drain plus DG-SF builds a rank-one carrier inside the coarse expanding plane with
    g'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-119
  kind: branch-choice
  label: 'Live task: Improved the YM/deformation route target beyond the earlier DG-PSJ
    stack. The sharper repo-native factorization is now DG-Line (deformation drain
    plus DG-SF builds a rank-one carrier inside the coarse expanding plane with gap/dri'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
- obligation_id: marvin-route-authority-123
  kind: branch-choice
  label: 'Live task: Sharpened the YM/deformation exact-potential closure route on
    the Navier-Stokes lane. In problems/navier-stokes/theorem-construction/spectral-transversality-cocycle-bridge-to-sg4.md,
    replaced the vague A-D bridge discussion with'
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
  - 'Exact branch reduced further: the Hessian/coercivity wall is equivalent to
    a tangent-frame lower bound for the first derivative D W_J(N_J).'
  - 'General branch reduced further: local projective gluing is an O(1) connection-Laplacian
    synchronization theorem on overlap windows after local lifted recovery.'
  - 'Upstream shadow transfer reduced further: the off-diagonal blocks are the
    double commutators [N,[N,S]] and [P,[P,S]], so the live shadow wall is a
    commutator plus plane-leakage theorem.'
- obligation_id: class-membership-forward-invariance-route
  kind: live-theorem-edge
  label: CFI.A / OFP.A / LCI.A class-membership route
  status: frontier
  source_anchor: route-synthesis
  blocking: true
  notes:
  - This route choice or blocked frontier is still inside the theorem packet.
  - The Apr 19 class-membership packet set is route authority here; TPS / `SG.4`
    is downstream consumer status only after direct matrix work
    closes.
  - The source/receiver split stays explicit under `OFP.A`; receiver-side `LCI.A`
    remains separate from source-side `(FCI.5f)`, whose pressure child `FPCR.C`
    is the exact same-depth frozen-net resonant wall; `FFPR.B1` / `FFPR.C2`
    are only the adjacent stronger sufficient pressure route.
  - >-
    The receiver-side oscillation stack has now been sharpened past the
    `VCB.A` / `HSP.A` / `FEI.A` return loop: after `OFP.CellA`, the immediate
    repo-native queue is the widened receiver return `(EOC.27)` as `WRR.A`,
    paired with the anchored-center ledger to close the pulled-back
    `\nabla_xU_1` halo carrier through `U2H.A`; with the `OFP.C1e2`
    first-rung/deformation inputs this obtains `(OFP.10cb)`. This older
    thickened-collar queue is now downstream context: the active retained-window
    receiver/source packet supplies `LCI.A+FCI.5f => CSP.A => OFP.A => CFI.A`,
    while the averaged branch supplies
    `LCI.A_avg+FCI.5f => CSP.A_avg => OFP.A_avg => CFI.A_avg`.
    The retained still-live admission packet
    `SLR.A`--`SLR.D` is only a sufficient shortcut for those inputs. Without
    that admission, the route-native widened-family compression remains the
    old `LCI` / `CSP` fixed point. The end-to-end certificate map records the
    audit corrections: `U2H.A` closes only the pulled-back halo-gradient
    residue, the enlarged `LCI` instances remain parameter-shifted obligations
    of the same theorem family, and the terminal no-exit certificate remains
    conditional on the endpoint-exclusion matrix. The finite-parameter
    lower-carrier closure now discharges the uniform `LCI.B2d` / `LCI.C`
    Gronwall step from the corresponding transported-center package. The
    direct transported-center package note gives the non-smuggling reduction
    `DTC.A_{\mathfrak p} => EOC.A_{\mathfrak p}+AFD.A_{\mathfrak p}/AFD.B_{\mathfrak p}+RCF.A_{\mathfrak p}`;
    the remaining analytic target is now `DTC.A_{\mathfrak p}` for the finite
    required parameter set, with the pre-base parameter subset used before
    deriving base `EOC.A`. For the full lane, `(FCI.5f)` remains a separate
    source-side block unless promoted as installed; with `(FCI.5f)` installed,
    the terminal theorem blocks are exactly `DTC.A_{\mathfrak p}` across
    `\mathscr P_{\rm req}` and `\mathsf{End}_{NS}`. The direct PDE core inside
    `DTC.A` has sharpened from the older `DTC.M-Linear` slogan into the
    simultaneous affine-center package `ACT.KX => ACT.X-Press_cell =>
    ACT.Kcore => ACT.Actr_core => ACT.X-Readout => ACT.A => DTC.M-Affine =>
    DTC.PRE`. This package supplies
    `DTC.M^\infty` without spending `EOC.A`,
    `AFD.A`, `RCF.A`, `LCI.A`, `CSP.A`, `OFP.A`, or `Field`; it also keeps
    `A_buf` as readout/top-viscous buffer data rather than pre-pressure
    Gronwall data. `RWS.C_scale` is the fixed-readout compatibility bridge, not
    a fixed-radius smallness theorem. The concrete realization is the
    affine-normalized local packet inequality carrying endpoint first-rung
    action in the frame `G_j`; it remains conditional until
    `B_{\mathfrak p}^{aff},F_{\mathfrak p}^{aff}\in L^1(I)` are proved by
    direct pre-`DTC.A` estimates, and it does not itself install `(FCI.5f)` or
    the endpoint block. The full bridge ledger expands this into
    `DTC.MA-Energy -> DTC.MA-Readout -> DTC.M-to-VPF -> DTC.PRE-to-DTC.A`,
    with the center zero-mode requirement for the affine readout explicit.
    The endpoint tower face also keeps the separate `DTC-to-TowerBound`
    bridge: finite-cover `DTC.A` must read out both `U_k` and `K_k`, including
    local pressure/viscous response, before `END.TowerAmp` is available.
    In the current conditional completion packet, `(FCI.5f)` is supplied by
    the exact three-channel source theorem `FPCR.C + FSCR.C + FCC.C1`, not by
    the formal split bridge alone. `END.Exh` is recorded as
    witness-certificate completeness under the accepted endpoint certificate
    maps. Inside `DTC.MA-Energy`, the remaining noncircular affine-center
    coefficient subledger is `\mathfrak D_1^{aff}\in L^1`,
    `\mathfrak F^{ctr,res}\in L^1`, and `\mathfrak H^{osc,\alpha}\in L^2`,
    with far pressure tail and zero-rung center ledger direct. The bundled
    target for that subledger is now `ACT.A`: one affine-center master packet
    should close all three ledgers together. It is conditional until `ACT.E1`
    or `ACT.E2` proves the affine-center energy inequality with external or
    dissipatively absorbed `L^1` coefficients; using
    `E_ACT in L^\infty => B_ACT,F_ACT in L^1 => E_ACT in L^\infty` is marked
    circular. The valid completion route is finite-stage:
    `ACT.Stage(0)..ACT.Stage(m+2) => ACT.E2`, with lower affine rungs external
    at each stage. The middle block is structurally triangular once
    `DTC.AFF-L2` is accepted; the remaining live estimates are the stage
    pressure response on affine balls and the stage cutoff/frame estimate,
    without importing `CSP.A`, `OFP.A`, or `Field`. The structural affine-local-equation gate `DTC.AFF-L1` now passes
    modulo the tower-tensor representation compatibility of `\mathcal C_G`:
    the center `U_1(c_j)` endpoint action is absorbed by the frame derivative
    and the moving affine cutoff sees only `u(x)-u(c_j)-A_j(x-c_j)`. If that
    representation compatibility is not already installed, it is the first
    sub-obligation; otherwise the remaining `DTC.MA-Energy` work is the
    middle-block, pressure/forcing, and moving cutoff estimates. The
    middle-block structural gate `DTC.AFF-L2` also passes the same-rung break
    test: after endpoint removal, only lower affine rungs occur. However the
    `L^1` middle-block coefficient estimate is still part of `ACT.E1` /
    `ACT.E2`; it cannot be read back from the desired
    `\mathcal E_{ACT}\in L^\infty` conclusion. The pressure/forcing gate
    `DTC.AFF-L3` and moving-cutoff gate `DTC.AFF-L4` pass structurally:
    pressure uses local Poisson response plus far-tail energy rather than
    `CSP.A`, top viscosity is read from the included `m+2` rungs, and the
    cutoff sees the affine defect rather than full center amplitude. Their
    `L^1` coefficient estimates remain part of `ACT.E1` / `ACT.E2`.
    A named affine-excess propagation theorem `AXP.A` now packages the
    conditional scale-small alternative `ACT.ScaleSmall`:
    if the scale-normalized affine excess obeys
    `dX_exc+cN <= L X_exc + (C_cut+C_press) X_exc^{1/2}N + F` with
    `L,F in L^1`, and the initial excess plus additive source budget is below
    the absorption threshold, then the
    nonlinear residue is absorbed and `X_exc in L^\infty`, `N in L^1` follow on
    that affine interval. With `ACT.X-Readout`, `AXP.A` supplies exactly the
    `DTC.MA-Energy` subledger readouts `H^{osc,alpha} in L2`, `D_1^aff in L1`,
    and `F^{ctr,res} in L1`. This is not unconditional: center zero modes must
    be split into `A_core^ctr` and `A_buf^ctr` because the raw `q=0` center term
    is not small as `R -> 0`, and a full-strip proof needs a small-budget
    scheduler, not merely finite `L_0,F_0,F_K`. The ordered retained-branch receiver package is
    `NKF.Native+NKF.Ann+NKF.Quad => ACT.X-Press`, then
    `ACT.X-Cut+ACT.X-Press+ACT.X-MidRaw+ACT.X-TopVisc => ACT.KX`, then
    `ACT.KX+ACT.X-Scale+RWS.C_scale => ACT.X-Readout => ACT.A => RCF.A =>
    LCI.A`. The averaged replacement package is
    `SCF_base + SGC.A_a.e + ATD_m^epsilon => SCF_avg^m => AACT.KX off B_epsilon^Phi`, then
    `AACT.KX+RWS.C_scale => DTC.A_avg`, then
    `AVG.RCV.A => LCI.A_avg => CSP.A_avg => OFP.A_avg => CFI.A_avg`. It must not
    be read as pointwise `DTC.A` until `DTC.Read^epsilon` is proved, and it must not be
    read as old `CFI.A` until `Field.Read^epsilon` or `CM.Read` is proved.
    `ACT.X-MidRaw` carries the zero-mode linearized middle terms through
    `A_core`; `RWS.C_scale` transfers the small-radius cover to the fixed
    readout scale; and `ACT.X-Readout` spends
    `Y_read=A_core^ctr+A_buf^ctr+X_exc` after `ACT.KX` supplies `A_core^ctr`.
    The scheduler restarts only `X_exc` at budget endpoints using the current
    transported center frame. `AXP.A` does not by itself prove `ACT.KX`, `(FCI.5f)`,
    or any endpoint entry.
    `ACT.X-Scale` supplies the scale-small restart seed from retained smooth
    center-ball regularity on the retained branch. On the averaged branch, the
    seed is the `X_R(s_a)` component of `SCF_avg^m`, not point-center smoothness.
    `RWS.C_cover` supplies the dynamic same-fluid
    cover from compact retained label tubes and the pack gauge. `RWS.C_scale` is
    the finite-cover readout bridge that recovers fixed-radius readouts from small-radius restarted packets; it
    does not assert fixed-radius packet smallness. This
    is a conditional receiver discharge under the retained-window hypotheses,
    not an unconditional energy result; the retained native forcing certificate is
    `RSCB.NKF`, which supplies the retained smooth center-ball route
    `NKF.Moll + NKF.Point => NKF.Native`, together with the scheduler budgets.
    The finite-energy-native route must instead combine the finite-energy
    `SCF_base` supply with `SGC.A_a.e` and `ATD_m^epsilon` to form
    `SCF_avg^m` off `B_epsilon^Phi`, then feed the
    averaged receiver bridge `AVG.RCV.A` instead of forcing pointwise recovery.
    `ACT.X-Scale` and `RWS.C_scale` are installed conditional bridges, not
    fixed-radius smallness claims.
    Conditional closure: `ACT.KX` is now the simultaneous core/excess theorem
    replacing the linear route `AXE.A -> ACT.X-Boot -> ACT.Kcore ->
    ACT.Actr_core`. This is necessary because `ACT.X-MidRaw` contains genuine
    zero-mode linear terms `U_tilde R + R U_tilde`, so `A_core^ctr` is a live
    coefficient in the affine-excess equation. The joint block uses
    `J=X_exc+theta A_core` while only `X_exc` enters the small absorption term.
    The top `m+1,m+2` modes remain `A_buf^ctr` readout/top-viscous buffer data,
    not pre-pressure Gronwall data unless an optional stronger top-forcing route
    is chosen. On the retained branch, the named pressure supplier is `NKF.Native+NKF.Ann+NKF.Quad =>
    ACT.X-Press`; the energy atoms `ACT.X-Cut+ACT.X-Press+ACT.X-MidRaw+
    ACT.X-TopVisc` feed `ACT.KX`, and the joint block yields `X_exc in Linfty`,
    `N in L1`, `K_{\le m}^{ctr} in L1`, and `A_core^ctr in Linfty`.
    `ACT.Kcore` remains the ledger identity `K0.Core + E1.Aff + Kmid.Core`;
    its pressure cells are read after `ACT.KX`.
    `NKF.Native` is supplied only by `RSCB.NKF`, the retained smooth center-ball
    certificate for pointwise center pressure/viscous forcing; finite energy
    plus pressure Poisson response alone does not supply those center
    derivatives, as shown by the energy-preserving rescaling obstruction for
    point pressure derivatives. The retained condition is local
    `L^1_t C^{m+2,\alpha}` velocity plus local `L^1_t C^{m+1,\alpha}` pressure
    near the retained centers. The installed route to `NKF.Native` is
    `RSCB.NKF => NKF.Moll + NKF.Point`: prove a mollified
    transported-center forcing ledger from local pressure response,
    finite-energy far tails, and viscous integration by parts, then recover
    point-center forcing by retained smoothness at the restart scale.
    On the averaged branch this point-center sentence is replaced by
    `SCF_base + SGC.A_a.e + ATD_m^epsilon => SCF_avg^m => AACT.KX off B_epsilon^Phi => K_{\le m}^{avg,R} in L1 => AVG.RCV.A => LCI.A_avg`, with `AVG.COVER.A` covering every same-fluid approach tail: good material region gives averaged propagation and nonempty `B_epsilon^Phi` gives `Jump_avg` by `CAVG.J`, and `AVG.MAIN.A` as the terminal averaged no-class-exit wrapper.
    Pressure is split into the in-bootstrap normal-form estimates and the
    post-`ACT.KX` `ACT.X-Press_cell` readout. The cell form supplies the
    residual pressure-cell ledger behind `ACT.Kcore`: the
    native affine normal-form pressure `p_j^aff` is subtracted from `p_j^loc`,
    and its center-cell functionals annihilate both affine and affine-linear
    pressure modes. The cell theorem controls `pi_j^loc=p_j^loc-p_j^aff` through
    a quadratic affine-excess core plus projected annular/harmonic/far-tail terms; its
    `L^1` consequence uses `X_exc in Linfty` and `N in L1` from `ACT.KX`.
    The energy form is the same residual local pressure decomposition inside
    `AXE.A`: the quadratic core produces the scale-small residue
    `C_press X_exc^(1/2) N`, while projected annular and harmonic terms enter the
    `L_press X_exc + F_press` side. The finite pressure far-tail is
    energy-controlled in `L^\infty(I)`. The top-viscous part reads `m+1,m+2`
    only through `A_buf` inside `Y_read`, not through the pre-pressure core.
    `ACT.KX` must absorb `(C_cut+C_press)X_exc^(1/2)N`. It spends no
    `LCI.A`, `CSP.A`, `OFP.A`, or `Field`.
    The endpoint block is now closed under the endpoint packet:
    `DTC-to-TowerBound` supplies `END.TowerAmp` from a finite transported-center
    cover, `DTC.A` through depth `N+2`, Morrey readout for `U_k`, and the local
    pressure/viscous readout for `K_k`; `END.TowerBound` is formal from
    `END.TowerAmp`. `END.Exh` is witness-certificate completeness, and
    `END.Cross` reduces blown, jump, and dead cross-entries to installed rows
    plus `END.TowerBound`, `END.Pack`, and `END.Field`. The proposed
    `DTC.LINEAR` affine-frame route remains
    conditional unless its displayed coefficients are supplied by direct
    pre-`DTC.A` estimates, since those coefficients include `AFD.A`, `EOC.A`,
    and `RCF.A`-level ledgers. `END.COMPLETE` is formal endpoint assembly from
    `END.TowerAmp` plus the accepted `END.Exh` / `END.Cross` certificate maps.
summary:
  total_obligations: 73
  grounded_count: 2
  structural_only_count: 0
  structurally_supported_count: 1
  bridge_pending_count: 5
  unresolved_count: 17
  frontier_count: 48
  bridge_discharge_blocking_count: 0
  theorem_warrant_blocking_count: 0
  blocking_count: 70
  all_discharged: false
  live_theorem_edge_present: true
  live_theorem_edge_branch_count: 1
  live_theorem_edge_blocked_count: 0
submission_blockers:
- There are 17 unresolved proof obligations.
- There are 5 bridge obligations still awaiting local discharge.
- There are 48 frontier obligations still inside the theorem packet.
