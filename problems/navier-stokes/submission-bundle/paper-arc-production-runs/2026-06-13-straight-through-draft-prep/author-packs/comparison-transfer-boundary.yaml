---
version: 1
kind: ns-paper-arc-author-pack
arc_id: comparison-transfer-boundary
batch_id: comparison-transfer-boundary-author-pack-20260613T231942Z
ready_to_apply: true
source_rewrites:
- op: replace_section
  path: problems/navier-stokes/submission-bundle/euler-boundary-theorem-appendix.tex
  section_title: Selected-Packet Euler Boundary Theorem
  spine_id: selected-packet-euler-boundary-theorem
  source_ref: problems/navier-stokes/submission-bundle/euler-boundary-theorem-appendix.tex:1
  author_replacement_required: false
  replacement: |
    \section{Selected-Packet Euler Boundary Theorem}

    This appendix records the only Euler material used by the Navier--Stokes
    submission.  Euler enters as a boundary reading of the same selected terminal
    packet after the Navier--Stokes class test has already named the packet and its
    first Pack, Part, or Field face.  Its proof role is comparison after the
    selected packet has already been typed.

    \begin{definition}[Base Euler boundary reading]
    For a selected terminal packet \(Q\), write
    \[
      \Member_E(Q):=\mathrm{EVol}_Q(u)\wedge \mathrm{EMom}_Q(u,p).
    \]
    Here \(\mathrm{EVol}_Q\) means incompressible flux-volume readability on
    \(Q\), and \(\mathrm{EMom}_Q\) means weak inviscid momentum-pressure
    readability on the same packet.  This base Euler reading consists of that
    volume-momentum pair; fixed-viscosity Navier--Stokes participation and
    positive-scale Navier--Stokes Field coherence are stronger predicates.
    \end{definition}

    \begin{definition}[Clean selected-packet Euler boundary]
    A selected Navier--Stokes terminal packet \(Q\) is in the clean Euler boundary
    case at depth \(N\) when:
    \begin{enumerate}
    \item \(Q\) is the exact packet already selected by the Navier--Stokes terminal
          claim.
    \item \(Q\) retains enough carrier information to support the flux-volume
          reading \(\mathrm{EVol}_Q\).
    \item \(\mathrm{EVol}_Q(u)\wedge\mathrm{EMom}_Q(u,p)\) holds on \(Q\).
    \item The Navier--Stokes failure under discussion is exactly loss of
          fixed-viscosity participation or loss of positive-scale one-field
          coherence.  Reynolds stress, source measure, pressure-source residue,
          concentration residue, oscillation defect, and compactness gap payments
          have all been settled on the same packet.
    \end{enumerate}
    \end{definition}

    \begin{theorem}[Clean Part face has an Euler boundary reading]
    Assume \(Q\) is in the clean Euler boundary case and the first
    Navier--Stokes face lost after Pack is
    \[
      \Pack_Q\wedge\neg\Part_{N,Q}.
    \]
    Then the selected packet supports the Navier--Stokes class-exit conclusion
    \[
      \Exit(Q;\Owork)
    \]
    through the Part face, and the same packet has the base Euler reading
    \[
      \Member_E(Q).
    \]
    \end{theorem}

    \begin{proof}
    The Navier--Stokes conclusion is the Pack-before-Part audited CM conclusion on the selected
    record.  Pack has survived, and \(\neg\Part_{N,Q}\) says that the packet no
    longer carries the same differentiated pressure-viscosity-incompressibility
    participation law through depth \(N\).  Since that participation law is one
    requirement in \(\Member(Q;\Owork)\), the first failed face supports
    \(\Exit(Q;\Owork)\).

    The Euler conclusion is separate and weaker.  The clean boundary hypotheses
    state that the same selected packet still has the incompressible volume reading
    and weak inviscid momentum-pressure reading.  That conjunction is exactly
    \(\Member_E(Q)\).  This records the clean Part-side case where a
    Navier--Stokes exit also has a base Euler boundary reading.
    \end{proof}

    \begin{theorem}[Clean Field face has an Euler boundary reading]
    Assume \(Q\) is in the clean Euler boundary case, \(\Pack_Q\) and
    \(\Part_{N,Q}\) survive for the branch split, and the first remaining
    Navier--Stokes face lost is
    \[
      \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}.
    \]
    Then the selected packet supports \(\Exit(Q;\Owork)\) through the Field face,
    and the same packet has the base Euler reading \(\Member_E(Q)\).
    \end{theorem}

    \begin{proof}
    After Pack and Part survive, the remaining Navier--Stokes membership
    requirement is a positive-scale coherent Field record.  The hypothesis
    \(\forall r>0\,\neg\Field_{N,r,Q}\) removes every positive-scale
    Navier--Stokes Field record on \(Q\).  That is the first failed face of
    \(\Member(Q;\Owork)\), so it supports \(\Exit(Q;\Owork)\).

    The base Euler reading requires the flux-volume and inviscid momentum-pressure
    readings already included in the clean boundary hypotheses.  Hence the same
    packet may leave the fixed-viscosity Navier--Stokes class through Field while
    still having the base Euler boundary reading.
    \end{proof}

    \begin{center}
    \small
    \setlength{\tabcolsep}{2.5pt}
    \renewcommand{\arraystretch}{1.16}
    \begin{tabular}{L{0.30\linewidth}L{0.30\linewidth}L{0.30\linewidth}}
    \textbf{Terminal surface} & \textbf{Navier--Stokes role} & \textbf{Euler role}\\
    \hline
    Clean Part face after Pack survives & \(\Pack_Q\wedge\neg\Part_{N,Q}\) &
    base boundary reading \(\Member_E(Q)\)\\
    Clean Field face after Pack and Part survive &
    \(\Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\) &
    base boundary reading \(\Member_E(Q)\)\\
    Pack failure, detached ancestry, raw source residue, pressure-source residue,
    Reynolds-stress limit, or oscillation defect & first repair is still native
    Pack/Part/Field landing & native CM role until the same packet is cleanly typed\\
    Smooth-side comparison & support for intuition or boundary comparison &
    smooth-side support for comparison\\
    \end{tabular}
    \end{center}

    The table keeps endpoint names tied to their selected-packet proof role.  An
    Euler boundary branch has three payments: the same packet has already been
    selected inside the Navier--Stokes terminal test, the Navier--Stokes face is
    Part or Field in the clean sense above, and the base Euler volume-momentum
    reading is still present.

    \begin{corollary}[Euler retention rule]
    The manuscript may keep Euler material in two roles.  The first role is the
    selected-packet boundary reading proved above.  The second role is smooth-side
    comparison, used only as support for explaining why the fixed-viscosity
    Navier--Stokes class is stricter than the base inviscid boundary reading.  All
    Euler material in this manuscript serves one of those two support roles.
    \end{corollary}
  repo_sources_used:
  - problems/navier-stokes/submission-bundle/euler-boundary-theorem-appendix.tex:1
  endpoint_status: proved in this arc
  reader_requirements:
  - plain_explanation
  - derivation_or_proof_argument
  - endpoint_status
  math_reader_critic:
    claim_truth_attack: The appendix proves only the selected-packet Euler boundary
      reading after the Navier-Stokes packet and first Part or Field face have already
      been typed.
    reader_thread_attack: The reader gets the base Euler predicates, the clean boundary
      hypotheses, two clean face theorems, and the retention rule in that order.
    open_derivation_attack: The proof arguments are present for the clean Part and
      Field cases; Pack failures, residues, Reynolds stress, and oscillation defects
      are explicitly kept native until separately typed.
    repair_decision: passes after repair
    unresolved_failures: []
- op: replace_section
  path: problems/navier-stokes/submission-bundle/navier-stokes-submission.tex
  section_title: Comparison And Transfer Boundary
  spine_id: comparison-and-transfer-boundary
  source_ref: problems/navier-stokes/submission-bundle/navier-stokes-submission.tex:1245
  author_replacement_required: false
  replacement: |
    \section{Comparison And Transfer Boundary}

    The Navier--Stokes class proof is a same-solution Navier--Stokes proof.  Euler comparison enters through a typed boundary use.  For the same selected packet, Pack remains readable in the shared flux-volume sense, the fixed-viscosity Part or Field requirement fails through loss of viscous participation or one-field coherence, and the inviscid momentum-pressure law remains readable.  Then the fail branch has a concrete Euler boundary reading as well as its Navier--Stokes face decision.

    \begin{proposition}[Euler boundary comparison role]
    Let \(C\) be a comparison branch.  It enters the present Navier--Stokes class proof only through a theorem of the form
    \[
    \begin{aligned}
    C &\Longrightarrow
    \text{a selected packet }Q\\
      &\Longrightarrow
    \text{a Pack, Part, or Field decision for }Q .
    \end{aligned}
    \]
    In the clean loss-of-viscosity case, the same theorem also reads the escaped packet as
    \[
      \mathrm{EVol}_Q+\mathrm{EMom}_Q .
    \]
    That theorem is the point where comparison pressure becomes Navier--Stokes proof material.  Smooth Euler to smooth Navier--Stokes remains a separate support bridge.  It can reassure the smooth side of the boundary while the class-exit mechanism stays with the selected Navier--Stokes packet and its typed face.
    \end{proposition}

    \begin{proof}
    The working class object \(\Owork\) is built from the original Navier--Stokes datum, pressure, viscosity, and same-fluid participation.  Comparison branches may share shapes with Navier--Stokes branches.  The class proof first needs the exact same-solution packet and its first failed face.  The Euler reading is added only after that packet has kept the shared inviscid flux-momentum law while losing the fixed-viscosity participation or one-field requirement.  The selected packet and typed face are what let comparison enter \(\Owork\); comparison outside them remains support.
    \end{proof}
  repo_sources_used:
  - problems/navier-stokes/submission-bundle/navier-stokes-submission.tex:1245
  endpoint_status: proved in this arc
  reader_requirements:
  - plain_explanation
  - derivation_or_proof_argument
  - endpoint_status
  math_reader_critic:
    claim_truth_attack: 'The section states the correct comparison rule: Euler comparison
      becomes proof material only through a selected Navier-Stokes packet and a Pack,
      Part, or Field decision.'
    reader_thread_attack: The reader is not asked to accept broad transfer language;
      the proposition says exactly where comparison enters and where smooth-side comparison
      remains support.
    open_derivation_attack: The derivation is supplied as a boundary-role proposition.
      No broad smooth Euler-to-Navier-Stokes transfer theorem is asserted.
    repair_decision: passes after repair
    unresolved_failures: []
- op: replace_section
  path: problems/navier-stokes/submission-bundle/navier-stokes-submission.tex
  section_title: Whole-Space \texorpdfstring{\(\mathbb R^3\)}{R3} CM Exterior-Source
    Branch
  spine_id: whole-space-mathbb-r-3-r3-cm-exterior-source-branch
  source_ref: problems/navier-stokes/submission-bundle/navier-stokes-submission.tex:1269
  author_replacement_required: false
  replacement: |
    \section{Whole-Space \texorpdfstring{\(\mathbb R^3\)}{R3} CM Exterior-Source Branch}

    The periodic branch and the whole-space branch use the same class test.  Their entry costs differ.  On \(\mathbb T^3\), the transported atlas lives on a compact spatial domain.  On \(\mathbb R^3\), a terminal claim can try to hide high derivatives in the far field.  The paper handles that risk before it imports the Pack, Part, and Field decision.

    Fix \(s>5/2\).  Let \(u\) be the smooth Navier--Stokes branch on \([0,T_*)\times\mathbb R^3\) from smooth rapidly decaying divergence-free data, and let \(\eta_R\) be an exterior cutoff.  The strong positive export theorem would prove
    \[
      \lim_{R\to\infty}\sup_{t<T_*}\|\eta_R u(t)\|_{H^s(\mathbb R^3)}=0.
    \]
    That theorem is useful, and it is stronger than the CM branch needs.  The CM branch starts from the Duhamel split and asks what remains after the harmless exterior pieces are removed:
    \[
      u(t)=e^{\nu t\Delta}u_0
      -\int_0^t e^{\nu(t-\tau)\Delta}\mathbb P\nabla\cdot(u\otimes u)(\tau)\,d\tau .
    \]

    The initial heat tail vanishes at spatial infinity because the datum is rapidly decaying.  A fixed compact-core nonlinear source has vanishing far-field effect because the heat kernel is off the diagonal at distance comparable to \(R\).  The remaining source is genuinely exterior:
    \[
      W_R(t):=
      \eta_R\int_0^t e^{\nu(t-\tau)\Delta}
      \mathbb P\nabla\cdot\bigl(\chi_{\ge R/4}u\otimes u\bigr)(\tau)\,d\tau .
    \]
    If \(W_R\) vanishes in exterior \(H^s\), the whole-space branch has the ordinary continuation tail.  If \(W_R\) survives, the survivor is the terminal object that must enter the CM matrix.

    \begin{proposition}[Whole-space exterior source split]
    The whole-space exterior \(H^s\) burden reduces to the genuinely exterior nonlinear source \(W_R\).  The initial heat tail and the fixed compact-core far-field source vanish at spatial infinity.  Hence the whole-space branch has the following two outcomes:
    \[
    \begin{array}{ll}
    \displaystyle \lim_{R\to\infty}\sup_{t<T_*}\|W_R(t)\|_{H^s}=0,
      & \text{member-side exterior readout},\\[0.8em]
    \displaystyle \limsup_{R\to\infty}\sup_{t<T_*}\|W_R(t)\|_{H^s}>0,
      & \text{terminal exterior survivor to classify.}
    \end{array}
    \]
    \end{proposition}

    \begin{proof}
    Rapid decay of \(u_0\) controls \(\eta_R e^{\nu t\Delta}u_0\) in \(H^s\) on every finite preterminal interval.  For a fixed compact source, the kernel of \(\Lambda^s e^{\nu(t-\tau)\Delta}\mathbb P\nabla\cdot\) is smooth away from the diagonal, while the cutoff \(\eta_R\) keeps the readout far from that source.  Off-diagonal heat-kernel decay gives a negative power of \(R\).  After those two terms are removed, the displayed exterior nonlinear source is the only term that can carry a terminal exterior \(H^s\) tail.
    \end{proof}

    \begin{lemma}[Bounded exterior frequencies vanish]
    For every fixed dyadic cutoff \(N_0\),
    \[
      \lim_{R\to\infty}\sup_{t<T_*}
      \|P_{\le N_0}W_R(t)\|_{H^s}=0 .
    \]
    \end{lemma}

    \begin{proof}
    On bounded frequency,
    \[
      \|P_{\le N_0}f\|_{H^s}\le C_sN_0^s\|f\|_{L^2}.
    \]
    The heat/Leray operator maps the exterior source from \(L^1_tL^{3/2}_x\) into the fixed-band \(L^\infty_tL^2_x\) response.  With \(F_R=\chi_{\ge R/4}(u\otimes u)\),
    \[
      \|F_R(t)\|_{L^{3/2}}
      \le
      \|\chi_{\ge R/4}u(t)\|_{L^3}\|u(t)\|_{L^3}.
    \]
    The energy class gives \(u\in L^4_tL^3_x\) on \([0,T_*)\).  The exterior factor tends to zero as \(R\to\infty\), and the second factor is square integrable in time on the same finite interval.  Thus \(F_R\to0\) in \(L^1_tL^{3/2}_x\), and the fixed-band \(H^s\) response vanishes.
    \end{proof}

    \begin{proposition}[Exterior survivor lands in the CM faces]
    Assume the exterior nonlinear source survives in \(H^s\).  Then the selected terminal exterior survivor lands in the CM matrix:
    \[
      \neg\Pack_Q\;\vee\;\neg\Part_{M,Q}\;\vee\;
      \forall \rho>0\,\neg\Field_{M,\rho,Q}.
    \]
    \end{proposition}

    \begin{proof}
    The bounded-frequency lemma removes every fixed dyadic window.  A surviving exterior source then selects radii \(R_j\to\infty\), times \(t_j<T_*\), and dyadic frequencies \(N_j\to\infty\) such that
    \[
      N_j^s\|P_{N_j}W_{R_j}(t_j)\|_{L^2}\ge c>0 .
    \]
    Let \(Q\) be the same-solution terminal record selected by that survivor.  A survivor lacking a retained material address in the original branch fails Pack.  A survivor detached from the same Navier--Stokes equation, pressure law, viscosity, or source ancestry fails Part.

    It remains to read the retained Pack+Part branch.  A fixed positive Field scale \(\rho>0\) through depth \(M>s\) gives a finite derivative tower on the selected record.  Littlewood--Paley decay on that fixed record gives
    \[
      N^s\|P_N f\|_{L^2}\le C_{\rho,M,Q}N^{s-M}\to0 .
    \]
    That decay contradicts the surviving sequence above.  Every fixed positive Field scale fails on the retained Pack+Part branch.
    \end{proof}

    \begin{theorem}[Whole-space CM classification state]
    For smooth rapidly decaying divergence-free data on \(\mathbb R^3\), the whole-space exterior branch has the same pass-or-exit form as the periodic branch, with its own noncompact payment.  The exterior Duhamel split selects the relevant terminal object.  A vanishing exterior nonlinear source gives the member-side exterior \(H^s\) readout.  A surviving exterior nonlinear source lands in Pack, Part, or Field failure and supports \(\Exit(Q;\Owork)\).
    \end{theorem}

    \begin{proof}
    The Duhamel split removes the initial heat tail and the fixed compact-core far-field source.  The remaining exterior nonlinear source either vanishes in \(H^s\) or survives.  In the vanishing case, the whole-space branch has the continuation tail needed by the member-side readout.  In the surviving case, bounded exterior frequencies vanish, so the survivor is a dyadic high-frequency escape.  The preceding proposition reads that escape as Pack failure, Part failure, or Field failure on the same terminal record.  The CM embedding consumes the first failed face as \(\Exit(Q;\Owork)\).  The direct positive exterior \(H^s\) theorem remains a stronger gold/export theorem.
    \end{proof}

    This is the whole-space branch result used later in the paper.  It keeps the periodic atlas and the noncompact exterior-source branch separate.  It also keeps the direct \(H^s\) tail theorem in its proper role: a stronger forward-positive route, valuable when proved, while the silver whole-space branch uses the CM face landing written above.
  repo_sources_used:
  - problems/navier-stokes/submission-bundle/navier-stokes-submission.tex:1269
  endpoint_status: silver CM face landing
  reader_requirements:
  - plain_explanation
  - derivation_or_proof_argument
  - endpoint_status
  math_reader_critic:
    claim_truth_attack: The whole-space section derives the exterior Duhamel split,
      removes heat tail and fixed compact-core source, proves bounded exterior frequencies
      vanish, and routes a surviving exterior source to Pack, Part, or Field.
    reader_thread_attack: The reader can follow the noncompact payment because the
      section first names the far-field risk, then defines W_R, then proves the survivor
      classification.
    open_derivation_attack: The direct exterior H^s tail theorem remains a stronger
      gold/export theorem; the silver branch is paid by the exterior survivor face
      classification already written on the page.
    repair_decision: passes after repair
    unresolved_failures: []
author_contract:
  arc_id: comparison-transfer-boundary
  must_write:
  - finished TeX prose, not a report
  - reader-facing explanation before compressed notation
  - full derivation/proof steps available in the current repo context
  - gold status and obstruction stated without redefining gold
  - CM/silver counterpart written where the arc touches class exit
  - endpoint status stated as proved, support, obstruction, obsolete, contradiction,
    or open continuation
  must_criticize:
  - attack the replacement's strongest claim and state how the replacement now pays
    or limits it
  - name the exact sentence, definition, or proof step where a reader could lose the
    thread
  - name any derivation jump that remains open, or state why the derivation is supplied
    on the page
  - record a repair decision before integration
  must_not_write:
  - boilerplate that presents the work as only a status snapshot
  - internal automation or progress-report voice
  - unfilled prose
  - unsupported theorem claim hidden as narrative confidence
  required_patterns:
  - "(Euler|comparison|transfer)"
  - "(boundary|support)"
  source_material_rule: every repo note, section, obstruction, and support item in
    the packet must be turned into prose, proof argument, endpoint status, or a sourced
    exclusion
  integration_path: verify_author_pack -> integrate_author_pack -> targeted changed-section
    build
