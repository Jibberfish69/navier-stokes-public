% Generated by problems/navier-stokes/tools/build_surface_derivation_appendix.rb
% Internal source names stay in surface-derivation-inventory.yaml.
% The visible content below is mathematical branch expansion only.

\section{Expanded CM Branch Obligations}

Let \(B_i\) denote the \(i\)-th selected terminal branch in the non-Euler CM exhaustion. Each item below records the mathematical branch law used by the proof spine: the branch either loses its first continuation service, inherits an already installed exhaustion, or remains support until it selects a terminal packet.

\paragraph{CM expansion 1.}
Let \(B_{1}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 2.}
Let \(B_{2}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{2}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 3.}
Let \(B_{3}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{3}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 4.}
Let \(B_{4}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{4}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 5.}
Let \(B_{5}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{5}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 6.}
Let \(B_{6}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{6}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 7.}
Let \(B_{7}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{7}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 8.}
Let \(B_{8}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{8}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 9.}
Let \(B_{9}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{9}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 10.}
Let \(B_{10}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{10}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 11.}
Let \(B_{11}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{11}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 12.}
Let \(B_{12}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{12}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 13.}
Let \(B_{13}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{13}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 14.}
Let \(B_{14}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{14}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 15.}
Let \(B_{15}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{15}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 16.}
Let \(B_{16}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{16}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 17.}
Let \(B_{17}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{17}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 18.}
Let \(B_{18}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{18}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 19.}
Let \(B_{19}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{19}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 20.}
Let \(B_{20}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{20}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 21.}
Let \(B_{21}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{21}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 22.}
Let \(B_{22}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{22}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 23.}
Let \(B_{23}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{23}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 24.}
Let \(B_{24}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{24}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 25.}
Let \(B_{25}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{25}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 26.}
Let \(B_{26}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{26}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 27.}
Let \(B_{27}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{27}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 28.}
Let \(B_{28}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{28}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 29.}
Let \(B_{29}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{29}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 30.}
Let \(B_{30}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{30}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 31.}
Let \(B_{31}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{31}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 32.}
Let \(B_{32}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{32}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 33.}
Let \(B_{33}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{33}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 34.}
Let \(B_{34}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{34}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 35.}
Let \(B_{35}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{35}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 36.}
Let \(B_{36}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{36}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 37.}
Let \(B_{37}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{37}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 38.}
Let \(B_{38}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{38}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 39.}
Let \(B_{39}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{39}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 40.}
Let \(B_{40}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{40}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 41.}
Let \(B_{41}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{41}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 42.}
Let \(B_{42}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{42}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 43.}
Let \(B_{43}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{43}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 44.}
Let \(B_{44}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{44}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 45.}
Let \(B_{45}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{45}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 46.}
Let \(B_{46}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{46}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 47.}
Let \(B_{47}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{47}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 48.}
Let \(B_{48}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{48}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 49.}
Let \(B_{49}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{49}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 50.}
Let \(B_{50}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{50}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 51.}
Let \(B_{51}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{51}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 52.}
Let \(B_{52}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{52}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 53.}
Let \(B_{53}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{53}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 54.}
Let \(B_{54}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{54}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 55.}
Let \(B_{55}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{55}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 56.}
Let \(B_{56}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{56}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 57.}
Let \(B_{57}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{57}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 58.}
Let \(B_{58}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{58}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 59.}
Let \(B_{59}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{59}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 60.}
Let \(B_{60}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{60}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 61.}
Let \(B_{61}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{61}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 62.}
Let \(B_{62}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{62}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 63.}
Let \(B_{63}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{63}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 64.}
Let \(B_{64}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{64}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 65.}
Let \(B_{65}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{65}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 66.}
Let \(B_{66}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{66}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 67.}
Let \(B_{67}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{67}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 68.}
Let \(B_{68}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{68}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 69.}
Let \(B_{69}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{69}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 70.}
Let \(B_{70}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{70}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 71.}
Let \(B_{71}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{71}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 72.}
Let \(B_{72}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{72}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 73.}
Let \(B_{73}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{73}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 74.}
Let \(B_{74}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{74}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 75.}
Let \(B_{75}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{75}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 76.}
Let \(B_{76}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{76}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 77.}
Let \(B_{77}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{77}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 78.}
Let \(B_{78}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{78}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 79.}
Let \(B_{79}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{79}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 80.}
Let \(B_{80}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{80}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 81.}
Let \(B_{81}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{81}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 82.}
Let \(B_{82}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{82}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 83.}
Let \(B_{83}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{83}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 84.}
Let \(B_{84}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{84}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 85.}
Let \(B_{85}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{85}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 86.}
Let \(B_{86}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{86}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 87.}
Let \(B_{87}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{87}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 88.}
Let \(B_{88}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{88}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 89.}
Let \(B_{89}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{89}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 90.}
Let \(B_{90}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{90}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 91.}
Let \(B_{91}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{91}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 92.}
Let \(B_{92}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{92}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 93.}
Let \(B_{93}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{93}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 94.}
Let \(B_{94}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{94}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 95.}
Let \(B_{95}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{95}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 96.}
Let \(B_{96}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{96}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 97.}
Let \(B_{97}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{97}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 98.}
Let \(B_{98}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{98}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 99.}
Let \(B_{99}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{99}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 100.}
Let \(B_{100}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{100}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 101.}
Let \(B_{101}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{101}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 102.}
Let \(B_{102}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{102}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 103.}
Let \(B_{103}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{103}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 104.}
Let \(B_{104}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{104}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 105.}
Let \(B_{105}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{105}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 106.}
Let \(B_{106}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{106}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 107.}
Let \(B_{107}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{107}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 108.}
Let \(B_{108}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{108}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 109.}
Let \(B_{109}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{109}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 110.}
Let \(B_{110}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{110}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 111.}
Let \(B_{111}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{111}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 112.}
Let \(B_{112}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{112}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 113.}
Let \(B_{113}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{113}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 114.}
Let \(B_{114}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{114}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 115.}
Let \(B_{115}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{115}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 116.}
Let \(B_{116}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{116}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 117.}
Let \(B_{117}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{117}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 118.}
Let \(B_{118}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{118}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 119.}
Let \(B_{119}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{119}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 120.}
Let \(B_{120}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{120}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 121.}
Let \(B_{121}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{121}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 122.}
Let \(B_{122}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{122}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 123.}
Let \(B_{123}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{123}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 124.}
Let \(B_{124}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{124}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 125.}
Let \(B_{125}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{125}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 126.}
Let \(B_{126}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{126}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 127.}
Let \(B_{127}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{127}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 128.}
Let \(B_{128}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{128}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 129.}
Let \(B_{129}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{129}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 130.}
Let \(B_{130}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{130}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 131.}
Let \(B_{131}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{131}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 132.}
Let \(B_{132}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{132}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 133.}
Let \(B_{133}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{133}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 134.}
Let \(B_{134}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{134}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 135.}
Let \(B_{135}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{135}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 136.}
Let \(B_{136}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{136}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 137.}
Let \(B_{137}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{137}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 138.}
Let \(B_{138}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{138}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 139.}
Let \(B_{139}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{139}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 140.}
Let \(B_{140}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{140}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 141.}
Let \(B_{141}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{141}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 142.}
Let \(B_{142}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{142}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 143.}
Let \(B_{143}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{143}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 144.}
Let \(B_{144}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{144}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 145.}
Let \(B_{145}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{145}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 146.}
Let \(B_{146}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{146}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 147.}
Let \(B_{147}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{147}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 148.}
Let \(B_{148}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{148}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 149.}
Let \(B_{149}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{149}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 150.}
Let \(B_{150}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{150}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 151.}
Let \(B_{151}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{151}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 152.}
Let \(B_{152}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{152}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 153.}
Let \(B_{153}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{153}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 154.}
Let \(B_{154}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{154}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 155.}
Let \(B_{155}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{155}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 156.}
Let \(B_{156}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{156}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 157.}
Let \(B_{157}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{157}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 158.}
Let \(B_{158}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{158}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 159.}
Let \(B_{159}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{159}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 160.}
Let \(B_{160}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{160}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 161.}
Let \(B_{161}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{161}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 162.}
Let \(B_{162}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{162}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 163.}
Let \(B_{163}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{163}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 164.}
Let \(B_{164}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{164}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 165.}
Let \(B_{165}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{165}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 166.}
Let \(B_{166}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{166}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 167.}
Let \(B_{167}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{167}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 168.}
Let \(B_{168}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{168}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 169.}
Let \(B_{169}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{169}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 170.}
Let \(B_{170}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{170}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 171.}
Let \(B_{171}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{171}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 172.}
Let \(B_{172}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{172}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 173.}
Let \(B_{173}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{173}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 174.}
Let \(B_{174}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{174}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 175.}
Let \(B_{175}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{175}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 176.}
Let \(B_{176}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{176}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 177.}
Let \(B_{177}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{177}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 178.}
Let \(B_{178}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{178}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 179.}
Let \(B_{179}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{179}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 180.}
Let \(B_{180}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{180}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 181.}
Let \(B_{181}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{181}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 182.}
Let \(B_{182}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{182}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 183.}
Let \(B_{183}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{183}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 184.}
Let \(B_{184}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{184}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 185.}
Let \(B_{185}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{185}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 186.}
Let \(B_{186}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{186}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 187.}
Let \(B_{187}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{187}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 188.}
Let \(B_{188}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{188}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 189.}
Let \(B_{189}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{189}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 190.}
Let \(B_{190}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{190}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 191.}
Let \(B_{191}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{191}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 192.}
Let \(B_{192}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{192}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 193.}
Let \(B_{193}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{193}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 194.}
Let \(B_{194}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{194}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 195.}
Let \(B_{195}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{195}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 196.}
Let \(B_{196}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{196}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 197.}
Let \(B_{197}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{197}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 198.}
Let \(B_{198}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{198}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 199.}
Let \(B_{199}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{199}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 200.}
Let \(B_{200}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{200}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{200}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 201.}
Let \(B_{201}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{201}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 202.}
Let \(B_{202}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{202}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{202}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 203.}
Let \(B_{203}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{203}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{203}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 204.}
Let \(B_{204}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{204}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{204}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 205.}
Let \(B_{205}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{205}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{205}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 206.}
Let \(B_{206}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{206}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{206}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 207.}
Let \(B_{207}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{207}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{207}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 208.}
Let \(B_{208}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{208}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{208}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 209.}
Let \(B_{209}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{209}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{209}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 210.}
Let \(B_{210}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{210}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 211.}
Let \(B_{211}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{211}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{211}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 212.}
Let \(B_{212}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{212}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 213.}
Let \(B_{213}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{213}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{213}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 214.}
Let \(B_{214}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{214}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 215.}
Let \(B_{215}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{215}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 216.}
Let \(B_{216}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{216}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{216}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 217.}
Let \(B_{217}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{217}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{217}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 218.}
Let \(B_{218}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{218}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{218}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 219.}
Let \(B_{219}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{219}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{219}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 220.}
Let \(B_{220}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{220}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 221.}
Let \(B_{221}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{221}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{221}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 222.}
Let \(B_{222}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{222}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 223.}
Let \(B_{223}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{223}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{223}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 224.}
Let \(B_{224}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{224}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 225.}
Let \(B_{225}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{225}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{225}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 226.}
Let \(B_{226}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{226}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{226}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 227.}
Let \(B_{227}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{227}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{227}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 228.}
Let \(B_{228}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{228}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{228}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 229.}
Let \(B_{229}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{229}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 230.}
Let \(B_{230}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{230}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 231.}
Let \(B_{231}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{231}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{231}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 232.}
Let \(B_{232}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{232}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 233.}
Let \(B_{233}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{233}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 234.}
Let \(B_{234}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{234}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 235.}
Let \(B_{235}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{235}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 236.}
Let \(B_{236}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{236}\Rightarrow\neg\Pack_Q\), hence \(B_{236}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 237.}
Let \(B_{237}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{237}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{237}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 238.}
Let \(B_{238}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{238}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{238}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 239.}
Let \(B_{239}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{239}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 240.}
Let \(B_{240}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{240}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 241.}
Let \(B_{241}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{241}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 242.}
Let \(B_{242}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{242}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 243.}
Let \(B_{243}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{243}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{243}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 244.}
Let \(B_{244}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{244}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{244}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 245.}
Let \(B_{245}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{245}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 246.}
Let \(B_{246}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{246}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{246}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 247.}
Let \(B_{247}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{247}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 248.}
Let \(B_{248}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{248}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 249.}
Let \(B_{249}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{249}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{249}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 250.}
Let \(B_{250}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{250}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{250}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 251.}
Let \(B_{251}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{251}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 252.}
Let \(B_{252}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{252}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 253.}
Let \(B_{253}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{253}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{253}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 254.}
Let \(B_{254}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{254}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{254}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 255.}
Let \(B_{255}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{255}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{255}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 256.}
Let \(B_{256}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{256}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{256}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 257.}
Let \(B_{257}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{257}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 258.}
Let \(B_{258}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{258}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{258}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 259.}
Let \(B_{259}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{259}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{259}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 260.}
Let \(B_{260}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{260}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 261.}
Let \(B_{261}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{261}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 262.}
Let \(B_{262}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{262}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{262}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 263.}
Let \(B_{263}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{263}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{263}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 264.}
Let \(B_{264}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{264}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 265.}
Let \(B_{265}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{265}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{265}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 266.}
Let \(B_{266}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{266}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{266}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 267.}
Let \(B_{267}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{267}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{267}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 268.}
Let \(B_{268}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{268}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 269.}
Let \(B_{269}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{269}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 270.}
Let \(B_{270}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{270}\Rightarrow\neg\Pack_Q\), hence \(B_{270}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 271.}
Let \(B_{271}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{271}\Rightarrow\neg\Pack_Q\), hence \(B_{271}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 272.}
Let \(B_{272}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{272}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 273.}
Let \(B_{273}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{273}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{273}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 274.}
Let \(B_{274}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{274}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{274}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 275.}
Let \(B_{275}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{275}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{275}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 276.}
Let \(B_{276}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{276}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 277.}
Let \(B_{277}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{277}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{277}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 278.}
Let \(B_{278}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{278}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 279.}
Let \(B_{279}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{279}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 280.}
Let \(B_{280}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{280}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 281.}
Let \(B_{281}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{281}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 282.}
Let \(B_{282}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{282}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{282}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 283.}
Let \(B_{283}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{283}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{283}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 284.}
Let \(B_{284}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{284}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 285.}
Let \(B_{285}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{285}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{285}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 286.}
Let \(B_{286}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{286}\Rightarrow\neg\Pack_Q\), hence \(B_{286}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 287.}
Let \(B_{287}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{287}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{287}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 288.}
Let \(B_{288}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{288}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 289.}
Let \(B_{289}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{289}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 290.}
Let \(B_{290}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{290}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 291.}
Let \(B_{291}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{291}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 292.}
Let \(B_{292}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{292}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 293.}
Let \(B_{293}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{293}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{293}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 294.}
Let \(B_{294}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{294}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{294}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 295.}
Let \(B_{295}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{295}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 296.}
Let \(B_{296}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{296}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 297.}
Let \(B_{297}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{297}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 298.}
Let \(B_{298}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{298}\Rightarrow\neg\Pack_Q\), hence \(B_{298}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 299.}
Let \(B_{299}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{299}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{299}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 300.}
Let \(B_{300}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{300}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 301.}
Let \(B_{301}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{301}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 302.}
Let \(B_{302}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{302}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{302}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 303.}
Let \(B_{303}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{303}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 304.}
Let \(B_{304}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{304}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 305.}
Let \(B_{305}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{305}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{305}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 306.}
Let \(B_{306}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{306}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{306}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 307.}
Let \(B_{307}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{307}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 308.}
Let \(B_{308}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{308}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 309.}
Let \(B_{309}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{309}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{309}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 310.}
Let \(B_{310}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{310}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 311.}
Let \(B_{311}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{311}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 312.}
Let \(B_{312}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{312}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 313.}
Let \(B_{313}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{313}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 314.}
Let \(B_{314}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{314}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{314}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 315.}
Let \(B_{315}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{315}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{315}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 316.}
Let \(B_{316}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{316}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{316}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 317.}
Let \(B_{317}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{317}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{317}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 318.}
Let \(B_{318}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{318}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 319.}
Let \(B_{319}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{319}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{319}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 320.}
Let \(B_{320}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{320}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{320}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 321.}
Let \(B_{321}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{321}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 322.}
Let \(B_{322}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{322}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{322}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 323.}
Let \(B_{323}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{323}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 324.}
Let \(B_{324}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{324}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{324}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 325.}
Let \(B_{325}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{325}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 326.}
Let \(B_{326}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{326}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 327.}
Let \(B_{327}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{327}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{327}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 328.}
Let \(B_{328}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{328}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{328}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 329.}
Let \(B_{329}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{329}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{329}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 330.}
Let \(B_{330}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{330}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{330}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 331.}
Let \(B_{331}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{331}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{331}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 332.}
Let \(B_{332}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{332}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{332}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 333.}
Let \(B_{333}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{333}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{333}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 334.}
Let \(B_{334}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{334}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{334}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 335.}
Let \(B_{335}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{335}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{335}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 336.}
Let \(B_{336}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{336}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{336}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 337.}
Let \(B_{337}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{337}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{337}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 338.}
Let \(B_{338}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{338}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{338}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 339.}
Let \(B_{339}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{339}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{339}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 340.}
Let \(B_{340}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{340}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 341.}
Let \(B_{341}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{341}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{341}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 342.}
Let \(B_{342}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{342}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{342}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 343.}
Let \(B_{343}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{343}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{343}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 344.}
Let \(B_{344}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{344}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{344}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 345.}
Let \(B_{345}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{345}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 346.}
Let \(B_{346}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{346}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 347.}
Let \(B_{347}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{347}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{347}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 348.}
Let \(B_{348}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{348}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{348}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 349.}
Let \(B_{349}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{349}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{349}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 350.}
Let \(B_{350}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{350}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{350}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 351.}
Let \(B_{351}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{351}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 352.}
Let \(B_{352}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{352}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{352}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 353.}
Let \(B_{353}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{353}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 354.}
Let \(B_{354}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{354}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{354}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 355.}
Let \(B_{355}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{355}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 356.}
Let \(B_{356}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{356}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 357.}
Let \(B_{357}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{357}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 358.}
Let \(B_{358}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{358}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{358}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 359.}
Let \(B_{359}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{359}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{359}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 360.}
Let \(B_{360}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{360}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{360}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 361.}
Let \(B_{361}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{361}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{361}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 362.}
Let \(B_{362}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{362}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{362}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 363.}
Let \(B_{363}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{363}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{363}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 364.}
Let \(B_{364}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{364}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{364}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 365.}
Let \(B_{365}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{365}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{365}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 366.}
Let \(B_{366}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{366}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 367.}
Let \(B_{367}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{367}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{367}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 368.}
Let \(B_{368}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{368}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{368}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 369.}
Let \(B_{369}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{369}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{369}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 370.}
Let \(B_{370}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{370}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{370}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 371.}
Let \(B_{371}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{371}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{371}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 372.}
Let \(B_{372}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{372}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{372}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 373.}
Let \(B_{373}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{373}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 374.}
Let \(B_{374}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{374}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{374}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 375.}
Let \(B_{375}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{375}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{375}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 376.}
Let \(B_{376}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{376}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{376}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 377.}
Let \(B_{377}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{377}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{377}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 378.}
Let \(B_{378}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{378}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{378}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 379.}
Let \(B_{379}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{379}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{379}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 380.}
Let \(B_{380}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{380}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{380}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 381.}
Let \(B_{381}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{381}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{381}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 382.}
Let \(B_{382}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{382}\Rightarrow\neg\Pack_Q\), hence \(B_{382}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 383.}
Let \(B_{383}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{383}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 384.}
Let \(B_{384}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{384}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 385.}
Let \(B_{385}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{385}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{385}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 386.}
Let \(B_{386}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{386}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{386}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 387.}
Let \(B_{387}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{387}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 388.}
Let \(B_{388}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{388}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{388}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 389.}
Let \(B_{389}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{389}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 390.}
Let \(B_{390}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{390}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{390}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 391.}
Let \(B_{391}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{391}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 392.}
Let \(B_{392}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{392}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 393.}
Let \(B_{393}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{393}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 394.}
Let \(B_{394}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{394}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{394}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 395.}
Let \(B_{395}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{395}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 396.}
Let \(B_{396}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{396}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 397.}
Let \(B_{397}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{397}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{397}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 398.}
Let \(B_{398}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{398}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{398}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 399.}
Let \(B_{399}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{399}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 400.}
Let \(B_{400}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{400}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 401.}
Let \(B_{401}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{401}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{401}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 402.}
Let \(B_{402}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{402}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{402}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 403.}
Let \(B_{403}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{403}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{403}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 404.}
Let \(B_{404}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{404}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 405.}
Let \(B_{405}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{405}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{405}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 406.}
Let \(B_{406}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{406}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{406}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 407.}
Let \(B_{407}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{407}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{407}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 408.}
Let \(B_{408}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{408}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{408}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 409.}
Let \(B_{409}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{409}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 410.}
Let \(B_{410}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{410}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{410}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 411.}
Let \(B_{411}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{411}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{411}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 412.}
Let \(B_{412}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{412}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{412}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 413.}
Let \(B_{413}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{413}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{413}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 414.}
Let \(B_{414}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{414}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 415.}
Let \(B_{415}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{415}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{415}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 416.}
Let \(B_{416}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{416}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{416}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 417.}
Let \(B_{417}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{417}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{417}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 418.}
Let \(B_{418}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{418}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{418}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 419.}
Let \(B_{419}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{419}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{419}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 420.}
Let \(B_{420}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{420}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{420}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 421.}
Let \(B_{421}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{421}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 422.}
Let \(B_{422}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{422}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 423.}
Let \(B_{423}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{423}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{423}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 424.}
Let \(B_{424}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{424}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 425.}
Let \(B_{425}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{425}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{425}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 426.}
Let \(B_{426}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{426}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 427.}
Let \(B_{427}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{427}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{427}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 428.}
Let \(B_{428}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{428}\Rightarrow\neg\Pack_Q\), hence \(B_{428}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 429.}
Let \(B_{429}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{429}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{429}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 430.}
Let \(B_{430}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{430}\Rightarrow\neg\Pack_Q\), hence \(B_{430}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 431.}
Let \(B_{431}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{431}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 432.}
Let \(B_{432}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{432}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{432}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 433.}
Let \(B_{433}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{433}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 434.}
Let \(B_{434}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{434}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 435.}
Let \(B_{435}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{435}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 436.}
Let \(B_{436}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{436}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{436}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 437.}
Let \(B_{437}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{437}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 438.}
Let \(B_{438}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{438}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 439.}
Let \(B_{439}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{439}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 440.}
Let \(B_{440}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{440}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{440}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 441.}
Let \(B_{441}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{441}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 442.}
Let \(B_{442}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{442}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{442}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 443.}
Let \(B_{443}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{443}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{443}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 444.}
Let \(B_{444}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{444}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{444}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 445.}
Let \(B_{445}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{445}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{445}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 446.}
Let \(B_{446}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{446}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{446}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 447.}
Let \(B_{447}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{447}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 448.}
Let \(B_{448}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{448}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{448}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 449.}
Let \(B_{449}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{449}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{449}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 450.}
Let \(B_{450}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{450}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{450}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 451.}
Let \(B_{451}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{451}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{451}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 452.}
Let \(B_{452}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{452}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{452}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 453.}
Let \(B_{453}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{453}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{453}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 454.}
Let \(B_{454}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{454}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 455.}
Let \(B_{455}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{455}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{455}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 456.}
Let \(B_{456}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{456}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 457.}
Let \(B_{457}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{457}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{457}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 458.}
Let \(B_{458}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{458}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{458}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 459.}
Let \(B_{459}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{459}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{459}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 460.}
Let \(B_{460}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{460}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{460}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 461.}
Let \(B_{461}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{461}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{461}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 462.}
Let \(B_{462}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{462}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{462}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 463.}
Let \(B_{463}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{463}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{463}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 464.}
Let \(B_{464}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{464}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{464}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 465.}
Let \(B_{465}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{465}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{465}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 466.}
Let \(B_{466}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{466}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{466}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 467.}
Let \(B_{467}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{467}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{467}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 468.}
Let \(B_{468}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{468}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{468}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 469.}
Let \(B_{469}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{469}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 470.}
Let \(B_{470}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{470}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{470}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 471.}
Let \(B_{471}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{471}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 472.}
Let \(B_{472}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{472}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 473.}
Let \(B_{473}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{473}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{473}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 474.}
Let \(B_{474}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{474}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{474}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 475.}
Let \(B_{475}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{475}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{475}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 476.}
Let \(B_{476}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{476}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{476}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 477.}
Let \(B_{477}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{477}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{477}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 478.}
Let \(B_{478}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{478}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{478}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 479.}
Let \(B_{479}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{479}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{479}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 480.}
Let \(B_{480}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{480}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{480}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 481.}
Let \(B_{481}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{481}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{481}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 482.}
Let \(B_{482}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{482}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{482}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 483.}
Let \(B_{483}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{483}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{483}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 484.}
Let \(B_{484}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{484}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{484}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 485.}
Let \(B_{485}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{485}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{485}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 486.}
Let \(B_{486}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{486}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{486}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 487.}
Let \(B_{487}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{487}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{487}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 488.}
Let \(B_{488}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{488}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{488}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 489.}
Let \(B_{489}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{489}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{489}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 490.}
Let \(B_{490}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{490}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 491.}
Let \(B_{491}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{491}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{491}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 492.}
Let \(B_{492}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{492}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 493.}
Let \(B_{493}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{493}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 494.}
Let \(B_{494}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{494}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 495.}
Let \(B_{495}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{495}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 496.}
Let \(B_{496}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{496}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 497.}
Let \(B_{497}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{497}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{497}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 498.}
Let \(B_{498}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{498}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 499.}
Let \(B_{499}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{499}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 500.}
Let \(B_{500}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{500}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 501.}
Let \(B_{501}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{501}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 502.}
Let \(B_{502}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{502}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 503.}
Let \(B_{503}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{503}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 504.}
Let \(B_{504}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{504}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 505.}
Let \(B_{505}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{505}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 506.}
Let \(B_{506}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{506}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 507.}
Let \(B_{507}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{507}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 508.}
Let \(B_{508}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{508}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{508}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 509.}
Let \(B_{509}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{509}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 510.}
Let \(B_{510}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{510}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 511.}
Let \(B_{511}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{511}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 512.}
Let \(B_{512}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{512}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 513.}
Let \(B_{513}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{513}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{513}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 514.}
Let \(B_{514}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{514}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 515.}
Let \(B_{515}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{515}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 516.}
Let \(B_{516}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{516}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{516}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 517.}
Let \(B_{517}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{517}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{517}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 518.}
Let \(B_{518}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{518}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{518}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 519.}
Let \(B_{519}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{519}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 520.}
Let \(B_{520}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{520}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{520}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 521.}
Let \(B_{521}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{521}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 522.}
Let \(B_{522}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{522}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 523.}
Let \(B_{523}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{523}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{523}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 524.}
Let \(B_{524}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{524}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{524}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 525.}
Let \(B_{525}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{525}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{525}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 526.}
Let \(B_{526}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{526}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{526}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 527.}
Let \(B_{527}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{527}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{527}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 528.}
Let \(B_{528}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{528}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{528}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 529.}
Let \(B_{529}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{529}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{529}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 530.}
Let \(B_{530}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{530}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{530}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 531.}
Let \(B_{531}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{531}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{531}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 532.}
Let \(B_{532}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{532}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{532}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 533.}
Let \(B_{533}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{533}\Rightarrow\neg\Pack_Q\), hence \(B_{533}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 534.}
Let \(B_{534}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{534}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{534}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 535.}
Let \(B_{535}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{535}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 536.}
Let \(B_{536}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{536}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{536}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 537.}
Let \(B_{537}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{537}\Rightarrow\neg\Pack_Q\), hence \(B_{537}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 538.}
Let \(B_{538}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{538}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{538}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 539.}
Let \(B_{539}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{539}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{539}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 540.}
Let \(B_{540}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{540}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{540}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 541.}
Let \(B_{541}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{541}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{541}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 542.}
Let \(B_{542}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{542}\Rightarrow\neg\Pack_Q\), hence \(B_{542}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 543.}
Let \(B_{543}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{543}\Rightarrow\neg\Pack_Q\), hence \(B_{543}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 544.}
Let \(B_{544}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{544}\Rightarrow\neg\Pack_Q\), hence \(B_{544}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 545.}
Let \(B_{545}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{545}\Rightarrow\neg\Pack_Q\), hence \(B_{545}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 546.}
Let \(B_{546}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{546}\Rightarrow\neg\Pack_Q\), hence \(B_{546}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 547.}
Let \(B_{547}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{547}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{547}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 548.}
Let \(B_{548}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{548}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{548}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 549.}
Let \(B_{549}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{549}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{549}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 550.}
Let \(B_{550}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{550}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{550}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 551.}
Let \(B_{551}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{551}\Rightarrow\neg\Pack_Q\), hence \(B_{551}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 552.}
Let \(B_{552}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{552}\Rightarrow\neg\Pack_Q\), hence \(B_{552}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 553.}
Let \(B_{553}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{553}\Rightarrow\neg\Pack_Q\), hence \(B_{553}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 554.}
Let \(B_{554}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{554}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 555.}
Let \(B_{555}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{555}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 556.}
Let \(B_{556}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{556}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 557.}
Let \(B_{557}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{557}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 558.}
Let \(B_{558}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{558}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 559.}
Let \(B_{559}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{559}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 560.}
Let \(B_{560}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{560}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 561.}
Let \(B_{561}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{561}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 562.}
Let \(B_{562}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{562}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 563.}
Let \(B_{563}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{563}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 564.}
Let \(B_{564}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{564}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 565.}
Let \(B_{565}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{565}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 566.}
Let \(B_{566}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{566}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 567.}
Let \(B_{567}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{567}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 568.}
Let \(B_{568}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{568}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 569.}
Let \(B_{569}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{569}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 570.}
Let \(B_{570}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{570}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 571.}
Let \(B_{571}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{571}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 572.}
Let \(B_{572}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{572}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 573.}
Let \(B_{573}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{573}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 574.}
Let \(B_{574}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{574}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 575.}
Let \(B_{575}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{575}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{575}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 576.}
Let \(B_{576}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{576}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{576}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 577.}
Let \(B_{577}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{577}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{577}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 578.}
Let \(B_{578}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{578}\Rightarrow\neg\Pack_Q\), hence \(B_{578}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 579.}
Let \(B_{579}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{579}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{579}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 580.}
Let \(B_{580}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{580}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 581.}
Let \(B_{581}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{581}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 582.}
Let \(B_{582}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{582}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 583.}
Let \(B_{583}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{583}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 584.}
Let \(B_{584}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{584}\Rightarrow\neg\Pack_Q\), hence \(B_{584}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 585.}
Let \(B_{585}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{585}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 586.}
Let \(B_{586}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{586}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{586}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 587.}
Let \(B_{587}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{587}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{587}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 588.}
Let \(B_{588}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{588}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{588}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 589.}
Let \(B_{589}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{589}\Rightarrow\neg\Pack_Q\), hence \(B_{589}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 590.}
Let \(B_{590}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{590}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{590}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 591.}
Let \(B_{591}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{591}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{591}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 592.}
Let \(B_{592}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{592}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 593.}
Let \(B_{593}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{593}\Rightarrow\neg\Pack_Q\), hence \(B_{593}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 594.}
Let \(B_{594}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{594}\Rightarrow\neg\Pack_Q\), hence \(B_{594}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 595.}
Let \(B_{595}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{595}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 596.}
Let \(B_{596}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{596}\Rightarrow\neg\Pack_Q\), hence \(B_{596}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 597.}
Let \(B_{597}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{597}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{597}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 598.}
Let \(B_{598}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{598}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{598}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 599.}
Let \(B_{599}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{599}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{599}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 600.}
Let \(B_{600}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{600}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 601.}
Let \(B_{601}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{601}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 602.}
Let \(B_{602}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{602}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{602}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 603.}
Let \(B_{603}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{603}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{603}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 604.}
Let \(B_{604}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{604}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{604}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 605.}
Let \(B_{605}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{605}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{605}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 606.}
Let \(B_{606}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{606}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 607.}
Let \(B_{607}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{607}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{607}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 608.}
Let \(B_{608}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{608}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{608}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 609.}
Let \(B_{609}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{609}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{609}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 610.}
Let \(B_{610}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{610}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 611.}
Let \(B_{611}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{611}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 612.}
Let \(B_{612}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{612}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 613.}
Let \(B_{613}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{613}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 614.}
Let \(B_{614}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{614}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{614}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 615.}
Let \(B_{615}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{615}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{615}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 616.}
Let \(B_{616}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{616}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 617.}
Let \(B_{617}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{617}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 618.}
Let \(B_{618}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{618}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 619.}
Let \(B_{619}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{619}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 620.}
Let \(B_{620}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{620}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 621.}
Let \(B_{621}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{621}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{621}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 622.}
Let \(B_{622}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{622}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 623.}
Let \(B_{623}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{623}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 624.}
Let \(B_{624}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{624}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 625.}
Let \(B_{625}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{625}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 626.}
Let \(B_{626}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{626}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 627.}
Let \(B_{627}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{627}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 628.}
Let \(B_{628}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{628}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 629.}
Let \(B_{629}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{629}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 630.}
Let \(B_{630}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{630}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 631.}
Let \(B_{631}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{631}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 632.}
Let \(B_{632}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{632}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 633.}
Let \(B_{633}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{633}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{633}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 634.}
Let \(B_{634}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{634}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{634}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 635.}
Let \(B_{635}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{635}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{635}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 636.}
Let \(B_{636}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{636}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{636}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 637.}
Let \(B_{637}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{637}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 638.}
Let \(B_{638}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{638}\Rightarrow\neg\Pack_Q\), hence \(B_{638}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 639.}
Let \(B_{639}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{639}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{639}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 640.}
Let \(B_{640}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{640}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{640}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 641.}
Let \(B_{641}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{641}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 642.}
Let \(B_{642}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{642}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 643.}
Let \(B_{643}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{643}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{643}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 644.}
Let \(B_{644}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{644}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{644}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 645.}
Let \(B_{645}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{645}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{645}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 646.}
Let \(B_{646}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{646}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{646}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 647.}
Let \(B_{647}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{647}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{647}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 648.}
Let \(B_{648}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{648}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{648}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 649.}
Let \(B_{649}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{649}\Rightarrow\neg\Pack_Q\), hence \(B_{649}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 650.}
Let \(B_{650}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{650}\Rightarrow\neg\Pack_Q\), hence \(B_{650}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 651.}
Let \(B_{651}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{651}\Rightarrow\neg\Pack_Q\), hence \(B_{651}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 652.}
Let \(B_{652}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{652}\Rightarrow\neg\Pack_Q\), hence \(B_{652}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 653.}
Let \(B_{653}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{653}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{653}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 654.}
Let \(B_{654}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{654}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{654}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 655.}
Let \(B_{655}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{655}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{655}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 656.}
Let \(B_{656}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{656}\Rightarrow\neg\Pack_Q\), hence \(B_{656}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 657.}
Let \(B_{657}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{657}\Rightarrow\neg\Pack_Q\), hence \(B_{657}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 658.}
Let \(B_{658}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{658}\Rightarrow\neg\Pack_Q\), hence \(B_{658}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 659.}
Let \(B_{659}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{659}\Rightarrow\neg\Pack_Q\), hence \(B_{659}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 660.}
Let \(B_{660}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{660}\Rightarrow\neg\Pack_Q\), hence \(B_{660}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 661.}
Let \(B_{661}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{661}\Rightarrow\neg\Pack_Q\), hence \(B_{661}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 662.}
Let \(B_{662}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{662}\Rightarrow\neg\Pack_Q\), hence \(B_{662}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 663.}
Let \(B_{663}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{663}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{663}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 664.}
Let \(B_{664}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{664}\Rightarrow\neg\Pack_Q\), hence \(B_{664}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 665.}
Let \(B_{665}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{665}\Rightarrow\neg\Pack_Q\), hence \(B_{665}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 666.}
Let \(B_{666}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{666}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 667.}
Let \(B_{667}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{667}\Rightarrow\neg\Pack_Q\), hence \(B_{667}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 668.}
Let \(B_{668}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{668}\Rightarrow\neg\Pack_Q\), hence \(B_{668}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 669.}
Let \(B_{669}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{669}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{669}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 670.}
Let \(B_{670}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{670}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{670}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 671.}
Let \(B_{671}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{671}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{671}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 672.}
Let \(B_{672}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{672}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{672}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 673.}
Let \(B_{673}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{673}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{673}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 674.}
Let \(B_{674}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{674}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{674}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 675.}
Let \(B_{675}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{675}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{675}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 676.}
Let \(B_{676}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{676}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 677.}
Let \(B_{677}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{677}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{677}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 678.}
Let \(B_{678}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{678}\Rightarrow\neg\Pack_Q\), hence \(B_{678}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 679.}
Let \(B_{679}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{679}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 680.}
Let \(B_{680}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{680}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 681.}
Let \(B_{681}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{681}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 682.}
Let \(B_{682}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{682}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 683.}
Let \(B_{683}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{683}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{683}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 684.}
Let \(B_{684}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{684}\Rightarrow\neg\Pack_Q\), hence \(B_{684}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 685.}
Let \(B_{685}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{685}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 686.}
Let \(B_{686}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{686}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 687.}
Let \(B_{687}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{687}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 688.}
Let \(B_{688}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{688}\Rightarrow\neg\Pack_Q\), hence \(B_{688}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 689.}
Let \(B_{689}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{689}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 690.}
Let \(B_{690}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{690}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{690}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 691.}
Let \(B_{691}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{691}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{691}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 692.}
Let \(B_{692}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{692}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{692}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 693.}
Let \(B_{693}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{693}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{693}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 694.}
Let \(B_{694}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{694}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{694}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 695.}
Let \(B_{695}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{695}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{695}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 696.}
Let \(B_{696}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{696}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{696}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 697.}
Let \(B_{697}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{697}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{697}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 698.}
Let \(B_{698}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{698}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{698}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 699.}
Let \(B_{699}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{699}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{699}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 700.}
Let \(B_{700}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{700}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{700}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 701.}
Let \(B_{701}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{701}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{701}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 702.}
Let \(B_{702}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{702}\Rightarrow\neg\Pack_Q\), hence \(B_{702}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 703.}
Let \(B_{703}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{703}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 704.}
Let \(B_{704}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{704}\Rightarrow\neg\Pack_Q\), hence \(B_{704}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 705.}
Let \(B_{705}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{705}\Rightarrow\neg\Pack_Q\), hence \(B_{705}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 706.}
Let \(B_{706}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{706}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 707.}
Let \(B_{707}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{707}\Rightarrow\neg\Pack_Q\), hence \(B_{707}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 708.}
Let \(B_{708}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{708}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 709.}
Let \(B_{709}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{709}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{709}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 710.}
Let \(B_{710}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{710}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 711.}
Let \(B_{711}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{711}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{711}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 712.}
Let \(B_{712}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{712}\Rightarrow\neg\Pack_Q\), hence \(B_{712}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 713.}
Let \(B_{713}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{713}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 714.}
Let \(B_{714}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{714}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 715.}
Let \(B_{715}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{715}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{715}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 716.}
Let \(B_{716}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{716}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{716}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 717.}
Let \(B_{717}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{717}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{717}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 718.}
Let \(B_{718}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{718}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{718}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 719.}
Let \(B_{719}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{719}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 720.}
Let \(B_{720}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{720}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{720}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 721.}
Let \(B_{721}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{721}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{721}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 722.}
Let \(B_{722}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{722}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{722}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 723.}
Let \(B_{723}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{723}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{723}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 724.}
Let \(B_{724}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{724}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{724}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 725.}
Let \(B_{725}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{725}\Rightarrow\neg\Pack_Q\), hence \(B_{725}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 726.}
Let \(B_{726}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{726}\Rightarrow\neg\Pack_Q\), hence \(B_{726}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 727.}
Let \(B_{727}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{727}\Rightarrow\neg\Pack_Q\), hence \(B_{727}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 728.}
Let \(B_{728}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{728}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 729.}
Let \(B_{729}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{729}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{729}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 730.}
Let \(B_{730}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{730}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{730}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 731.}
Let \(B_{731}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{731}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{731}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 732.}
Let \(B_{732}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{732}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 733.}
Let \(B_{733}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{733}\Rightarrow\neg\Pack_Q\), hence \(B_{733}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 734.}
Let \(B_{734}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{734}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{734}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 735.}
Let \(B_{735}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{735}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 736.}
Let \(B_{736}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{736}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{736}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 737.}
Let \(B_{737}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{737}\Rightarrow\neg\Pack_Q\), hence \(B_{737}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 738.}
Let \(B_{738}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{738}\Rightarrow\neg\Pack_Q\), hence \(B_{738}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 739.}
Let \(B_{739}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{739}\Rightarrow\neg\Pack_Q\), hence \(B_{739}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 740.}
Let \(B_{740}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{740}\Rightarrow\neg\Pack_Q\), hence \(B_{740}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 741.}
Let \(B_{741}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{741}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{741}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 742.}
Let \(B_{742}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{742}\Rightarrow\neg\Pack_Q\), hence \(B_{742}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 743.}
Let \(B_{743}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{743}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{743}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 744.}
Let \(B_{744}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{744}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{744}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 745.}
Let \(B_{745}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{745}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{745}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 746.}
Let \(B_{746}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{746}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{746}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 747.}
Let \(B_{747}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{747}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{747}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 748.}
Let \(B_{748}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{748}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 749.}
Let \(B_{749}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{749}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 750.}
Let \(B_{750}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{750}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 751.}
Let \(B_{751}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{751}\Rightarrow\neg\Pack_Q\), hence \(B_{751}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 752.}
Let \(B_{752}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{752}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{752}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 753.}
Let \(B_{753}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{753}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{753}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 754.}
Let \(B_{754}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{754}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{754}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 755.}
Let \(B_{755}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{755}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{755}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 756.}
Let \(B_{756}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{756}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{756}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 757.}
Let \(B_{757}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{757}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{757}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 758.}
Let \(B_{758}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{758}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{758}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 759.}
Let \(B_{759}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{759}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{759}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 760.}
Let \(B_{760}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{760}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{760}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 761.}
Let \(B_{761}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{761}\Rightarrow\neg\Pack_Q\), hence \(B_{761}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 762.}
Let \(B_{762}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{762}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{762}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 763.}
Let \(B_{763}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{763}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{763}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 764.}
Let \(B_{764}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{764}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{764}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 765.}
Let \(B_{765}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{765}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{765}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 766.}
Let \(B_{766}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{766}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{766}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 767.}
Let \(B_{767}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{767}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{767}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 768.}
Let \(B_{768}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{768}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{768}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 769.}
Let \(B_{769}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{769}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{769}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 770.}
Let \(B_{770}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{770}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{770}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 771.}
Let \(B_{771}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{771}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{771}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 772.}
Let \(B_{772}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{772}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{772}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 773.}
Let \(B_{773}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{773}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{773}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 774.}
Let \(B_{774}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{774}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{774}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 775.}
Let \(B_{775}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{775}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{775}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 776.}
Let \(B_{776}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{776}\Rightarrow\neg\Pack_Q\), hence \(B_{776}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 777.}
Let \(B_{777}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{777}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{777}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 778.}
Let \(B_{778}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{778}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 779.}
Let \(B_{779}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{779}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{779}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 780.}
Let \(B_{780}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{780}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{780}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 781.}
Let \(B_{781}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{781}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{781}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 782.}
Let \(B_{782}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{782}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{782}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 783.}
Let \(B_{783}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{783}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{783}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 784.}
Let \(B_{784}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{784}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{784}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 785.}
Let \(B_{785}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{785}\Rightarrow\neg\Pack_Q\), hence \(B_{785}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 786.}
Let \(B_{786}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{786}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{786}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 787.}
Let \(B_{787}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{787}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 788.}
Let \(B_{788}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{788}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{788}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 789.}
Let \(B_{789}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{789}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{789}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 790.}
Let \(B_{790}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{790}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{790}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 791.}
Let \(B_{791}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{791}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{791}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 792.}
Let \(B_{792}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{792}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 793.}
Let \(B_{793}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{793}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 794.}
Let \(B_{794}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{794}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 795.}
Let \(B_{795}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{795}\Rightarrow\neg\Pack_Q\), hence \(B_{795}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 796.}
Let \(B_{796}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{796}\Rightarrow\neg\Pack_Q\), hence \(B_{796}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 797.}
Let \(B_{797}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{797}\Rightarrow\neg\Pack_Q\), hence \(B_{797}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 798.}
Let \(B_{798}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{798}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{798}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 799.}
Let \(B_{799}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{799}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{799}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 800.}
Let \(B_{800}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{800}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{800}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 801.}
Let \(B_{801}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{801}\Rightarrow\neg\Pack_Q\), hence \(B_{801}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 802.}
Let \(B_{802}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{802}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 803.}
Let \(B_{803}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{803}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 804.}
Let \(B_{804}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{804}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 805.}
Let \(B_{805}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{805}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{805}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 806.}
Let \(B_{806}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{806}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{806}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 807.}
Let \(B_{807}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{807}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{807}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 808.}
Let \(B_{808}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{808}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{808}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 809.}
Let \(B_{809}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{809}\Rightarrow\neg\Pack_Q\), hence \(B_{809}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 810.}
Let \(B_{810}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{810}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 811.}
Let \(B_{811}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{811}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{811}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 812.}
Let \(B_{812}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{812}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{812}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 813.}
Let \(B_{813}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{813}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{813}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 814.}
Let \(B_{814}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{814}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 815.}
Let \(B_{815}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{815}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{815}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 816.}
Let \(B_{816}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{816}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{816}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 817.}
Let \(B_{817}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{817}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{817}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 818.}
Let \(B_{818}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{818}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{818}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 819.}
Let \(B_{819}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{819}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{819}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 820.}
Let \(B_{820}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{820}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{820}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 821.}
Let \(B_{821}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{821}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 822.}
Let \(B_{822}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{822}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 823.}
Let \(B_{823}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{823}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 824.}
Let \(B_{824}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{824}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 825.}
Let \(B_{825}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{825}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 826.}
Let \(B_{826}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{826}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 827.}
Let \(B_{827}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{827}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{827}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 828.}
Let \(B_{828}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{828}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{828}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 829.}
Let \(B_{829}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{829}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{829}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 830.}
Let \(B_{830}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{830}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{830}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 831.}
Let \(B_{831}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{831}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 832.}
Let \(B_{832}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{832}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 833.}
Let \(B_{833}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{833}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{833}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 834.}
Let \(B_{834}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{834}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 835.}
Let \(B_{835}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{835}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 836.}
Let \(B_{836}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{836}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 837.}
Let \(B_{837}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{837}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{837}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 838.}
Let \(B_{838}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{838}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{838}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 839.}
Let \(B_{839}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{839}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{839}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 840.}
Let \(B_{840}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{840}\Rightarrow\neg\Pack_Q\), hence \(B_{840}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 841.}
Let \(B_{841}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{841}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{841}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 842.}
Let \(B_{842}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{842}\Rightarrow\neg\Pack_Q\), hence \(B_{842}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 843.}
Let \(B_{843}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{843}\Rightarrow\neg\Pack_Q\), hence \(B_{843}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 844.}
Let \(B_{844}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{844}\Rightarrow\neg\Pack_Q\), hence \(B_{844}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 845.}
Let \(B_{845}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{845}\Rightarrow\neg\Pack_Q\), hence \(B_{845}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 846.}
Let \(B_{846}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{846}\Rightarrow\neg\Pack_Q\), hence \(B_{846}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 847.}
Let \(B_{847}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{847}\Rightarrow\neg\Pack_Q\), hence \(B_{847}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 848.}
Let \(B_{848}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{848}\Rightarrow\neg\Pack_Q\), hence \(B_{848}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 849.}
Let \(B_{849}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{849}\Rightarrow\neg\Pack_Q\), hence \(B_{849}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 850.}
Let \(B_{850}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{850}\Rightarrow\neg\Pack_Q\), hence \(B_{850}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 851.}
Let \(B_{851}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{851}\Rightarrow\neg\Pack_Q\), hence \(B_{851}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 852.}
Let \(B_{852}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{852}\Rightarrow\neg\Pack_Q\), hence \(B_{852}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 853.}
Let \(B_{853}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{853}\Rightarrow\neg\Pack_Q\), hence \(B_{853}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 854.}
Let \(B_{854}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{854}\Rightarrow\neg\Pack_Q\), hence \(B_{854}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 855.}
Let \(B_{855}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{855}\Rightarrow\neg\Pack_Q\), hence \(B_{855}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 856.}
Let \(B_{856}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{856}\Rightarrow\neg\Pack_Q\), hence \(B_{856}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 857.}
Let \(B_{857}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{857}\Rightarrow\neg\Pack_Q\), hence \(B_{857}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 858.}
Let \(B_{858}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{858}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{858}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 859.}
Let \(B_{859}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{859}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{859}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 860.}
Let \(B_{860}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{860}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{860}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 861.}
Let \(B_{861}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{861}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{861}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 862.}
Let \(B_{862}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{862}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{862}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 863.}
Let \(B_{863}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{863}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{863}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 864.}
Let \(B_{864}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{864}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{864}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 865.}
Let \(B_{865}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{865}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{865}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 866.}
Let \(B_{866}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{866}\Rightarrow\neg\Pack_Q\), hence \(B_{866}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 867.}
Let \(B_{867}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{867}\Rightarrow\neg\Pack_Q\), hence \(B_{867}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 868.}
Let \(B_{868}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{868}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{868}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 869.}
Let \(B_{869}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{869}\Rightarrow\neg\Pack_Q\), hence \(B_{869}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 870.}
Let \(B_{870}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{870}\Rightarrow\neg\Pack_Q\), hence \(B_{870}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 871.}
Let \(B_{871}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{871}\Rightarrow\neg\Pack_Q\), hence \(B_{871}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 872.}
Let \(B_{872}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{872}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{872}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 873.}
Let \(B_{873}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{873}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{873}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 874.}
Let \(B_{874}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{874}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{874}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 875.}
Let \(B_{875}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{875}\Rightarrow\neg\Pack_Q\), hence \(B_{875}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 876.}
Let \(B_{876}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{876}\Rightarrow\neg\Pack_Q\), hence \(B_{876}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 877.}
Let \(B_{877}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{877}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{877}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 878.}
Let \(B_{878}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{878}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{878}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 879.}
Let \(B_{879}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{879}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{879}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 880.}
Let \(B_{880}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{880}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{880}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 881.}
Let \(B_{881}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{881}\Rightarrow\neg\Pack_Q\), hence \(B_{881}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 882.}
Let \(B_{882}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{882}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{882}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 883.}
Let \(B_{883}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{883}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{883}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 884.}
Let \(B_{884}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{884}\Rightarrow\neg\Pack_Q\), hence \(B_{884}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 885.}
Let \(B_{885}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{885}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{885}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 886.}
Let \(B_{886}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{886}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{886}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 887.}
Let \(B_{887}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{887}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{887}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 888.}
Let \(B_{888}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{888}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{888}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 889.}
Let \(B_{889}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{889}\Rightarrow\neg\Pack_Q\), hence \(B_{889}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 890.}
Let \(B_{890}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{890}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{890}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 891.}
Let \(B_{891}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{891}\Rightarrow\neg\Pack_Q\), hence \(B_{891}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 892.}
Let \(B_{892}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{892}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{892}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 893.}
Let \(B_{893}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{893}\Rightarrow\neg\Pack_Q\), hence \(B_{893}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 894.}
Let \(B_{894}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{894}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{894}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 895.}
Let \(B_{895}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{895}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{895}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 896.}
Let \(B_{896}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{896}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{896}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 897.}
Let \(B_{897}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{897}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{897}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 898.}
Let \(B_{898}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{898}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 899.}
Let \(B_{899}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{899}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{899}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 900.}
Let \(B_{900}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{900}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 901.}
Let \(B_{901}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{901}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{901}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 902.}
Let \(B_{902}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{902}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 903.}
Let \(B_{903}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{903}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{903}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 904.}
Let \(B_{904}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{904}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{904}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 905.}
Let \(B_{905}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{905}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 906.}
Let \(B_{906}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{906}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{906}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 907.}
Let \(B_{907}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{907}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{907}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 908.}
Let \(B_{908}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{908}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 909.}
Let \(B_{909}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{909}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 910.}
Let \(B_{910}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{910}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{910}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 911.}
Let \(B_{911}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{911}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{911}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 912.}
Let \(B_{912}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{912}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{912}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 913.}
Let \(B_{913}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{913}\Rightarrow\neg\Pack_Q\), hence \(B_{913}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 914.}
Let \(B_{914}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{914}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{914}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 915.}
Let \(B_{915}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{915}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 916.}
Let \(B_{916}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{916}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{916}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 917.}
Let \(B_{917}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{917}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{917}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 918.}
Let \(B_{918}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{918}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{918}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 919.}
Let \(B_{919}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{919}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{919}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 920.}
Let \(B_{920}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{920}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{920}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 921.}
Let \(B_{921}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{921}\Rightarrow\neg\Pack_Q\), hence \(B_{921}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 922.}
Let \(B_{922}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{922}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{922}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 923.}
Let \(B_{923}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{923}\Rightarrow\neg\Pack_Q\), hence \(B_{923}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 924.}
Let \(B_{924}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{924}\Rightarrow\neg\Pack_Q\), hence \(B_{924}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 925.}
Let \(B_{925}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{925}\Rightarrow\neg\Pack_Q\), hence \(B_{925}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 926.}
Let \(B_{926}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{926}\Rightarrow\neg\Pack_Q\), hence \(B_{926}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 927.}
Let \(B_{927}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{927}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{927}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 928.}
Let \(B_{928}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{928}\Rightarrow\neg\Pack_Q\), hence \(B_{928}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 929.}
Let \(B_{929}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{929}\Rightarrow\neg\Pack_Q\), hence \(B_{929}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 930.}
Let \(B_{930}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{930}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{930}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 931.}
Let \(B_{931}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{931}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{931}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 932.}
Let \(B_{932}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{932}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 933.}
Let \(B_{933}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{933}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{933}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 934.}
Let \(B_{934}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{934}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{934}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 935.}
Let \(B_{935}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{935}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 936.}
Let \(B_{936}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{936}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{936}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 937.}
Let \(B_{937}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{937}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{937}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 938.}
Let \(B_{938}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{938}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{938}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 939.}
Let \(B_{939}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{939}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 940.}
Let \(B_{940}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{940}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 941.}
Let \(B_{941}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{941}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{941}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 942.}
Let \(B_{942}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{942}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{942}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 943.}
Let \(B_{943}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{943}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{943}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 944.}
Let \(B_{944}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{944}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{944}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 945.}
Let \(B_{945}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{945}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{945}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 946.}
Let \(B_{946}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{946}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{946}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 947.}
Let \(B_{947}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{947}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{947}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 948.}
Let \(B_{948}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{948}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{948}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 949.}
Let \(B_{949}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{949}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{949}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 950.}
Let \(B_{950}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{950}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 951.}
Let \(B_{951}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{951}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{951}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 952.}
Let \(B_{952}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{952}\Rightarrow\neg\Pack_Q\), hence \(B_{952}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 953.}
Let \(B_{953}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{953}\Rightarrow\neg\Pack_Q\), hence \(B_{953}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 954.}
Let \(B_{954}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{954}\Rightarrow\neg\Pack_Q\), hence \(B_{954}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 955.}
Let \(B_{955}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{955}\Rightarrow\neg\Pack_Q\), hence \(B_{955}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 956.}
Let \(B_{956}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{956}\Rightarrow\neg\Pack_Q\), hence \(B_{956}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 957.}
Let \(B_{957}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{957}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{957}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 958.}
Let \(B_{958}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{958}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{958}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 959.}
Let \(B_{959}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{959}\Rightarrow\neg\Pack_Q\), hence \(B_{959}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 960.}
Let \(B_{960}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{960}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{960}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 961.}
Let \(B_{961}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{961}\Rightarrow\neg\Pack_Q\), hence \(B_{961}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 962.}
Let \(B_{962}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{962}\Rightarrow\neg\Pack_Q\), hence \(B_{962}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 963.}
Let \(B_{963}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{963}\Rightarrow\neg\Pack_Q\), hence \(B_{963}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 964.}
Let \(B_{964}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{964}\Rightarrow\neg\Pack_Q\), hence \(B_{964}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 965.}
Let \(B_{965}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{965}\Rightarrow\neg\Pack_Q\), hence \(B_{965}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 966.}
Let \(B_{966}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{966}\Rightarrow\neg\Pack_Q\), hence \(B_{966}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 967.}
Let \(B_{967}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{967}\Rightarrow\neg\Pack_Q\), hence \(B_{967}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 968.}
Let \(B_{968}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{968}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{968}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 969.}
Let \(B_{969}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{969}\Rightarrow\neg\Pack_Q\), hence \(B_{969}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 970.}
Let \(B_{970}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{970}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{970}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 971.}
Let \(B_{971}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{971}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{971}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 972.}
Let \(B_{972}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{972}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{972}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 973.}
Let \(B_{973}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{973}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{973}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 974.}
Let \(B_{974}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{974}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{974}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 975.}
Let \(B_{975}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{975}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{975}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 976.}
Let \(B_{976}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{976}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 977.}
Let \(B_{977}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{977}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 978.}
Let \(B_{978}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{978}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 979.}
Let \(B_{979}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{979}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 980.}
Let \(B_{980}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{980}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 981.}
Let \(B_{981}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{981}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 982.}
Let \(B_{982}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{982}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 983.}
Let \(B_{983}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{983}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{983}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 984.}
Let \(B_{984}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{984}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{984}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 985.}
Let \(B_{985}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{985}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{985}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 986.}
Let \(B_{986}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{986}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{986}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 987.}
Let \(B_{987}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{987}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{987}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 988.}
Let \(B_{988}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{988}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 989.}
Let \(B_{989}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{989}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{989}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 990.}
Let \(B_{990}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{990}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{990}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 991.}
Let \(B_{991}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{991}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{991}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 992.}
Let \(B_{992}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{992}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{992}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 993.}
Let \(B_{993}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{993}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{993}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 994.}
Let \(B_{994}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{994}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{994}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 995.}
Let \(B_{995}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{995}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{995}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 996.}
Let \(B_{996}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{996}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{996}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 997.}
Let \(B_{997}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{997}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{997}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 998.}
Let \(B_{998}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{998}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{998}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 999.}
Let \(B_{999}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{999}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1000.}
Let \(B_{1000}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1000}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1000}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1001.}
Let \(B_{1001}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1001}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1001}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1002.}
Let \(B_{1002}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1002}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1002}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1003.}
Let \(B_{1003}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1003}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1003}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1004.}
Let \(B_{1004}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1004}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1004}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1005.}
Let \(B_{1005}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1005}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1006.}
Let \(B_{1006}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1006}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1006}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1007.}
Let \(B_{1007}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1007}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1007}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1008.}
Let \(B_{1008}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1008}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1008}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1009.}
Let \(B_{1009}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1009}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1009}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1010.}
Let \(B_{1010}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1010}\Rightarrow\neg\Pack_Q\), hence \(B_{1010}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1011.}
Let \(B_{1011}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1011}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1011}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1012.}
Let \(B_{1012}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1012}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1012}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1013.}
Let \(B_{1013}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1013}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1013}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1014.}
Let \(B_{1014}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1014}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1014}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1015.}
Let \(B_{1015}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1015}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1015}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1016.}
Let \(B_{1016}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1016}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1016}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1017.}
Let \(B_{1017}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1017}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1017}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1018.}
Let \(B_{1018}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1018}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1018}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1019.}
Let \(B_{1019}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1019}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1019}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1020.}
Let \(B_{1020}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1020}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1020}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1021.}
Let \(B_{1021}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1021}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1021}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1022.}
Let \(B_{1022}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1022}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1022}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1023.}
Let \(B_{1023}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1023}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1023}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1024.}
Let \(B_{1024}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1024}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1024}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1025.}
Let \(B_{1025}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1025}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1025}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1026.}
Let \(B_{1026}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1026}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1026}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1027.}
Let \(B_{1027}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1027}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1028.}
Let \(B_{1028}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1028}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1028}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1029.}
Let \(B_{1029}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1029}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1029}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1030.}
Let \(B_{1030}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1030}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1030}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1031.}
Let \(B_{1031}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1031}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1031}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1032.}
Let \(B_{1032}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1032}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1033.}
Let \(B_{1033}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1033}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1034.}
Let \(B_{1034}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1034}\Rightarrow\neg\Pack_Q\), hence \(B_{1034}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1035.}
Let \(B_{1035}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1035}\Rightarrow\neg\Pack_Q\), hence \(B_{1035}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1036.}
Let \(B_{1036}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1036}\Rightarrow\neg\Pack_Q\), hence \(B_{1036}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1037.}
Let \(B_{1037}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1037}\Rightarrow\neg\Pack_Q\), hence \(B_{1037}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1038.}
Let \(B_{1038}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1038}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1038}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1039.}
Let \(B_{1039}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1039}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1040.}
Let \(B_{1040}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1040}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1040}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1041.}
Let \(B_{1041}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1041}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1042.}
Let \(B_{1042}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1042}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1042}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1043.}
Let \(B_{1043}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1043}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1043}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1044.}
Let \(B_{1044}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1044}\Rightarrow\neg\Pack_Q\), hence \(B_{1044}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1045.}
Let \(B_{1045}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1045}\Rightarrow\neg\Pack_Q\), hence \(B_{1045}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1046.}
Let \(B_{1046}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1046}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1046}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1047.}
Let \(B_{1047}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1047}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1048.}
Let \(B_{1048}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1048}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1049.}
Let \(B_{1049}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1049}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1049}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1050.}
Let \(B_{1050}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1050}\Rightarrow\neg\Pack_Q\), hence \(B_{1050}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1051.}
Let \(B_{1051}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1051}\Rightarrow\neg\Pack_Q\), hence \(B_{1051}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1052.}
Let \(B_{1052}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1052}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1052}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1053.}
Let \(B_{1053}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1053}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1053}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1054.}
Let \(B_{1054}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1054}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1055.}
Let \(B_{1055}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1055}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1055}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1056.}
Let \(B_{1056}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1056}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1056}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1057.}
Let \(B_{1057}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1057}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1057}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1058.}
Let \(B_{1058}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1058}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1058}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1059.}
Let \(B_{1059}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1059}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1059}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1060.}
Let \(B_{1060}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1060}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1060}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1061.}
Let \(B_{1061}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1061}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1061}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1062.}
Let \(B_{1062}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1062}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1062}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1063.}
Let \(B_{1063}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1063}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1063}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1064.}
Let \(B_{1064}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1064}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1064}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1065.}
Let \(B_{1065}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1065}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1065}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1066.}
Let \(B_{1066}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1066}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1066}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1067.}
Let \(B_{1067}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1067}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1067}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1068.}
Let \(B_{1068}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1068}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1068}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1069.}
Let \(B_{1069}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1069}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1069}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1070.}
Let \(B_{1070}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1070}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1071.}
Let \(B_{1071}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1071}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1071}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1072.}
Let \(B_{1072}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1072}\Rightarrow\neg\Pack_Q\), hence \(B_{1072}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1073.}
Let \(B_{1073}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1073}\Rightarrow\neg\Pack_Q\), hence \(B_{1073}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1074.}
Let \(B_{1074}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1074}\Rightarrow\neg\Pack_Q\), hence \(B_{1074}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1075.}
Let \(B_{1075}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1075}\Rightarrow\neg\Pack_Q\), hence \(B_{1075}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1076.}
Let \(B_{1076}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1076}\Rightarrow\neg\Pack_Q\), hence \(B_{1076}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1077.}
Let \(B_{1077}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1077}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1077}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1078.}
Let \(B_{1078}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1078}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1078}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1079.}
Let \(B_{1079}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1079}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1079}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1080.}
Let \(B_{1080}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1080}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1080}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1081.}
Let \(B_{1081}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1081}\Rightarrow\neg\Pack_Q\), hence \(B_{1081}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1082.}
Let \(B_{1082}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1082}\Rightarrow\neg\Pack_Q\), hence \(B_{1082}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1083.}
Let \(B_{1083}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1083}\Rightarrow\neg\Pack_Q\), hence \(B_{1083}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1084.}
Let \(B_{1084}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1084}\Rightarrow\neg\Pack_Q\), hence \(B_{1084}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1085.}
Let \(B_{1085}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1085}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1085}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1086.}
Let \(B_{1086}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1086}\Rightarrow\neg\Pack_Q\), hence \(B_{1086}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1087.}
Let \(B_{1087}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1087}\Rightarrow\neg\Pack_Q\), hence \(B_{1087}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1088.}
Let \(B_{1088}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1088}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1088}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1089.}
Let \(B_{1089}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1089}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1089}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1090.}
Let \(B_{1090}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1090}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1090}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1091.}
Let \(B_{1091}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1091}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1091}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1092.}
Let \(B_{1092}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1092}\Rightarrow\neg\Pack_Q\), hence \(B_{1092}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1093.}
Let \(B_{1093}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1093}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1093}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1094.}
Let \(B_{1094}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1094}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1095.}
Let \(B_{1095}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1095}\Rightarrow\neg\Pack_Q\), hence \(B_{1095}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1096.}
Let \(B_{1096}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1096}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1096}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1097.}
Let \(B_{1097}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1097}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1098.}
Let \(B_{1098}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1098}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1099.}
Let \(B_{1099}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1099}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1099}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1100.}
Let \(B_{1100}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1100}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1100}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1101.}
Let \(B_{1101}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1101}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1101}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1102.}
Let \(B_{1102}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1102}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1103.}
Let \(B_{1103}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1103}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1104.}
Let \(B_{1104}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1104}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1104}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1105.}
Let \(B_{1105}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1105}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1105}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1106.}
Let \(B_{1106}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1106}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1107.}
Let \(B_{1107}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1107}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1108.}
Let \(B_{1108}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1108}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1108}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1109.}
Let \(B_{1109}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1109}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1109}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1110.}
Let \(B_{1110}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1110}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1110}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1111.}
Let \(B_{1111}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1111}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1111}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1112.}
Let \(B_{1112}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1112}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1113.}
Let \(B_{1113}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1113}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1114.}
Let \(B_{1114}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1114}\Rightarrow\neg\Pack_Q\), hence \(B_{1114}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1115.}
Let \(B_{1115}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1115}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1115}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1116.}
Let \(B_{1116}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1116}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1117.}
Let \(B_{1117}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1117}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1117}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1118.}
Let \(B_{1118}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1118}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1118}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1119.}
Let \(B_{1119}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1119}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1119}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1120.}
Let \(B_{1120}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1120}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1121.}
Let \(B_{1121}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1121}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1121}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1122.}
Let \(B_{1122}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1122}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1122}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1123.}
Let \(B_{1123}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1123}\Rightarrow\neg\Pack_Q\), hence \(B_{1123}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1124.}
Let \(B_{1124}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1124}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1124}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1125.}
Let \(B_{1125}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1125}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1125}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1126.}
Let \(B_{1126}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1126}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1126}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1127.}
Let \(B_{1127}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1127}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1127}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1128.}
Let \(B_{1128}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1128}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1128}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1129.}
Let \(B_{1129}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1129}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1129}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1130.}
Let \(B_{1130}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1130}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1130}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1131.}
Let \(B_{1131}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1131}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1131}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1132.}
Let \(B_{1132}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1132}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1132}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1133.}
Let \(B_{1133}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1133}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1133}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1134.}
Let \(B_{1134}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1134}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1134}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1135.}
Let \(B_{1135}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1135}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1135}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1136.}
Let \(B_{1136}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1136}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1136}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1137.}
Let \(B_{1137}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1137}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1137}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1138.}
Let \(B_{1138}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1138}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1138}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1139.}
Let \(B_{1139}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1139}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1139}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1140.}
Let \(B_{1140}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1140}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1141.}
Let \(B_{1141}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1141}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1142.}
Let \(B_{1142}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1142}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1143.}
Let \(B_{1143}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1143}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1143}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1144.}
Let \(B_{1144}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1144}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1145.}
Let \(B_{1145}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1145}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1145}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1146.}
Let \(B_{1146}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1146}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1146}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1147.}
Let \(B_{1147}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1147}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1148.}
Let \(B_{1148}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1148}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1148}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1149.}
Let \(B_{1149}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1149}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1150.}
Let \(B_{1150}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1150}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1151.}
Let \(B_{1151}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1151}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1152.}
Let \(B_{1152}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1152}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1153.}
Let \(B_{1153}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1153}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1153}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1154.}
Let \(B_{1154}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1154}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1154}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1155.}
Let \(B_{1155}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1155}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1155}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1156.}
Let \(B_{1156}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1156}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1156}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1157.}
Let \(B_{1157}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1157}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1157}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1158.}
Let \(B_{1158}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1158}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1158}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1159.}
Let \(B_{1159}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1159}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1159}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1160.}
Let \(B_{1160}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1160}\Rightarrow\neg\Pack_Q\), hence \(B_{1160}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1161.}
Let \(B_{1161}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1161}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1161}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1162.}
Let \(B_{1162}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1162}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1162}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1163.}
Let \(B_{1163}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1163}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1163}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1164.}
Let \(B_{1164}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1164}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1164}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1165.}
Let \(B_{1165}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1165}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1165}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1166.}
Let \(B_{1166}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1166}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1166}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1167.}
Let \(B_{1167}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1167}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1167}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1168.}
Let \(B_{1168}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1168}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1168}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1169.}
Let \(B_{1169}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1169}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1169}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1170.}
Let \(B_{1170}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1170}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1170}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1171.}
Let \(B_{1171}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1171}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1171}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1172.}
Let \(B_{1172}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1172}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1172}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1173.}
Let \(B_{1173}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1173}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1173}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1174.}
Let \(B_{1174}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1174}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1174}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1175.}
Let \(B_{1175}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1175}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1175}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1176.}
Let \(B_{1176}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1176}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1176}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1177.}
Let \(B_{1177}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1177}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1177}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1178.}
Let \(B_{1178}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1178}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1178}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1179.}
Let \(B_{1179}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1179}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1179}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1180.}
Let \(B_{1180}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1180}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1180}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1181.}
Let \(B_{1181}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1181}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1181}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1182.}
Let \(B_{1182}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1182}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1182}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1183.}
Let \(B_{1183}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1183}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1183}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1184.}
Let \(B_{1184}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1184}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1184}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1185.}
Let \(B_{1185}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1185}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1186.}
Let \(B_{1186}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1186}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1186}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1187.}
Let \(B_{1187}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1187}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1187}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1188.}
Let \(B_{1188}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1188}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1188}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1189.}
Let \(B_{1189}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1189}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1189}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1190.}
Let \(B_{1190}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1190}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1190}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1191.}
Let \(B_{1191}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1191}\Rightarrow\neg\Pack_Q\), hence \(B_{1191}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1192.}
Let \(B_{1192}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1192}\Rightarrow\neg\Pack_Q\), hence \(B_{1192}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1193.}
Let \(B_{1193}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1193}\Rightarrow\neg\Pack_Q\), hence \(B_{1193}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1194.}
Let \(B_{1194}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1194}\Rightarrow\neg\Pack_Q\), hence \(B_{1194}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1195.}
Let \(B_{1195}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1195}\Rightarrow\neg\Pack_Q\), hence \(B_{1195}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1196.}
Let \(B_{1196}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1196}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1196}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1197.}
Let \(B_{1197}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1197}\Rightarrow\neg\Pack_Q\), hence \(B_{1197}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1198.}
Let \(B_{1198}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1198}\Rightarrow\neg\Pack_Q\), hence \(B_{1198}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1199.}
Let \(B_{1199}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1199}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1199}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1200.}
Let \(B_{1200}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1200}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1200}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1201.}
Let \(B_{1201}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1201}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1201}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1202.}
Let \(B_{1202}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1202}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1202}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1203.}
Let \(B_{1203}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1203}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1203}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1204.}
Let \(B_{1204}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1204}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1204}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1205.}
Let \(B_{1205}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1205}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1205}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1206.}
Let \(B_{1206}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1206}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1206}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1207.}
Let \(B_{1207}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1207}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1207}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1208.}
Let \(B_{1208}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1208}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1208}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1209.}
Let \(B_{1209}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1209}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1210.}
Let \(B_{1210}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1210}\Rightarrow\neg\Pack_Q\), hence \(B_{1210}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1211.}
Let \(B_{1211}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1211}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1211}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1212.}
Let \(B_{1212}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1212}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1212}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1213.}
Let \(B_{1213}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1213}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1213}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1214.}
Let \(B_{1214}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1214}\Rightarrow\neg\Pack_Q\), hence \(B_{1214}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1215.}
Let \(B_{1215}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1215}\Rightarrow\neg\Pack_Q\), hence \(B_{1215}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1216.}
Let \(B_{1216}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1216}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1216}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1217.}
Let \(B_{1217}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1217}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1217}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1218.}
Let \(B_{1218}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1218}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1218}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1219.}
Let \(B_{1219}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1219}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1219}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1220.}
Let \(B_{1220}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1220}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1220}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1221.}
Let \(B_{1221}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1221}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1221}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1222.}
Let \(B_{1222}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1222}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1222}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1223.}
Let \(B_{1223}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1223}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1223}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1224.}
Let \(B_{1224}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1224}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1224}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1225.}
Let \(B_{1225}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1225}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1225}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1226.}
Let \(B_{1226}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1226}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1226}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1227.}
Let \(B_{1227}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1227}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1227}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1228.}
Let \(B_{1228}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1228}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1228}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1229.}
Let \(B_{1229}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1229}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1229}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1230.}
Let \(B_{1230}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1230}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1230}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1231.}
Let \(B_{1231}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1231}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1231}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1232.}
Let \(B_{1232}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1232}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1232}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1233.}
Let \(B_{1233}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1233}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1233}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1234.}
Let \(B_{1234}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1234}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1234}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1235.}
Let \(B_{1235}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1235}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1235}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1236.}
Let \(B_{1236}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1236}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1236}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1237.}
Let \(B_{1237}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1237}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1237}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1238.}
Let \(B_{1238}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1238}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1238}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1239.}
Let \(B_{1239}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1239}\Rightarrow\neg\Pack_Q\), hence \(B_{1239}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1240.}
Let \(B_{1240}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1240}\Rightarrow\neg\Pack_Q\), hence \(B_{1240}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1241.}
Let \(B_{1241}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1241}\Rightarrow\neg\Pack_Q\), hence \(B_{1241}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1242.}
Let \(B_{1242}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1242}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1242}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1243.}
Let \(B_{1243}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1243}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1243}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1244.}
Let \(B_{1244}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1244}\Rightarrow\neg\Pack_Q\), hence \(B_{1244}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1245.}
Let \(B_{1245}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1245}\Rightarrow\neg\Pack_Q\), hence \(B_{1245}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1246.}
Let \(B_{1246}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1246}\Rightarrow\neg\Pack_Q\), hence \(B_{1246}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1247.}
Let \(B_{1247}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1247}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1247}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1248.}
Let \(B_{1248}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1248}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1248}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1249.}
Let \(B_{1249}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1249}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1249}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1250.}
Let \(B_{1250}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1250}\Rightarrow\neg\Pack_Q\), hence \(B_{1250}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1251.}
Let \(B_{1251}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1251}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1251}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1252.}
Let \(B_{1252}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1252}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1252}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1253.}
Let \(B_{1253}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1253}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1253}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1254.}
Let \(B_{1254}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1254}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1254}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1255.}
Let \(B_{1255}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1255}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1255}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1256.}
Let \(B_{1256}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1256}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1256}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1257.}
Let \(B_{1257}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1257}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1257}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1258.}
Let \(B_{1258}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1258}\Rightarrow\neg\Pack_Q\), hence \(B_{1258}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1259.}
Let \(B_{1259}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1259}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1259}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1260.}
Let \(B_{1260}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1260}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1260}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1261.}
Let \(B_{1261}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1261}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1261}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1262.}
Let \(B_{1262}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1262}\Rightarrow\neg\Pack_Q\), hence \(B_{1262}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1263.}
Let \(B_{1263}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1263}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1263}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1264.}
Let \(B_{1264}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1264}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1264}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1265.}
Let \(B_{1265}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1265}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1265}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1266.}
Let \(B_{1266}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1266}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1266}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1267.}
Let \(B_{1267}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1267}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1267}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1268.}
Let \(B_{1268}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1268}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1268}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1269.}
Let \(B_{1269}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1269}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1269}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1270.}
Let \(B_{1270}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1270}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1270}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1271.}
Let \(B_{1271}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1271}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1271}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1272.}
Let \(B_{1272}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1272}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1272}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1273.}
Let \(B_{1273}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1273}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1273}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1274.}
Let \(B_{1274}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1274}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1274}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1275.}
Let \(B_{1275}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1275}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1275}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1276.}
Let \(B_{1276}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1276}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1277.}
Let \(B_{1277}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1277}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1278.}
Let \(B_{1278}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1278}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1278}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1279.}
Let \(B_{1279}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1279}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1279}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1280.}
Let \(B_{1280}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1280}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1280}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1281.}
Let \(B_{1281}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1281}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1281}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1282.}
Let \(B_{1282}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1282}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1283.}
Let \(B_{1283}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1283}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1283}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1284.}
Let \(B_{1284}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1284}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1284}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1285.}
Let \(B_{1285}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1285}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1285}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1286.}
Let \(B_{1286}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1286}\Rightarrow\neg\Pack_Q\), hence \(B_{1286}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1287.}
Let \(B_{1287}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1287}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1287}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1288.}
Let \(B_{1288}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1288}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1288}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1289.}
Let \(B_{1289}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1289}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1289}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1290.}
Let \(B_{1290}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1290}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1290}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1291.}
Let \(B_{1291}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1291}\Rightarrow\neg\Pack_Q\), hence \(B_{1291}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1292.}
Let \(B_{1292}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1292}\Rightarrow\neg\Pack_Q\), hence \(B_{1292}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1293.}
Let \(B_{1293}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1293}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1293}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1294.}
Let \(B_{1294}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1294}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1294}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1295.}
Let \(B_{1295}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1295}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1295}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1296.}
Let \(B_{1296}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1296}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1296}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1297.}
Let \(B_{1297}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1297}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1297}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1298.}
Let \(B_{1298}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1298}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1298}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1299.}
Let \(B_{1299}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1299}\Rightarrow\neg\Pack_Q\), hence \(B_{1299}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1300.}
Let \(B_{1300}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1300}\Rightarrow\neg\Pack_Q\), hence \(B_{1300}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1301.}
Let \(B_{1301}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1301}\Rightarrow\neg\Pack_Q\), hence \(B_{1301}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1302.}
Let \(B_{1302}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1302}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1303.}
Let \(B_{1303}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1303}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1303}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1304.}
Let \(B_{1304}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1304}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1304}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1305.}
Let \(B_{1305}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1305}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1305}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1306.}
Let \(B_{1306}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1306}\Rightarrow\neg\Pack_Q\), hence \(B_{1306}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1307.}
Let \(B_{1307}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1307}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1307}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1308.}
Let \(B_{1308}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1308}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1308}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1309.}
Let \(B_{1309}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1309}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1309}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1310.}
Let \(B_{1310}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1310}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1310}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1311.}
Let \(B_{1311}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1311}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1311}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1312.}
Let \(B_{1312}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1312}\Rightarrow\neg\Pack_Q\), hence \(B_{1312}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1313.}
Let \(B_{1313}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1313}\Rightarrow\neg\Pack_Q\), hence \(B_{1313}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1314.}
Let \(B_{1314}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1314}\Rightarrow\neg\Pack_Q\), hence \(B_{1314}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1315.}
Let \(B_{1315}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1315}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1315}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1316.}
Let \(B_{1316}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1316}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1316}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1317.}
Let \(B_{1317}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1317}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1317}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1318.}
Let \(B_{1318}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1318}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1318}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1319.}
Let \(B_{1319}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1319}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1319}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1320.}
Let \(B_{1320}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1320}\Rightarrow\neg\Pack_Q\), hence \(B_{1320}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1321.}
Let \(B_{1321}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1321}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1321}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1322.}
Let \(B_{1322}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1322}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1322}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1323.}
Let \(B_{1323}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1323}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1323}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1324.}
Let \(B_{1324}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1324}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1324}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1325.}
Let \(B_{1325}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1325}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1325}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1326.}
Let \(B_{1326}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1326}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1326}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1327.}
Let \(B_{1327}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1327}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1327}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1328.}
Let \(B_{1328}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1328}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1328}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1329.}
Let \(B_{1329}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1329}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1329}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1330.}
Let \(B_{1330}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1330}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1330}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1331.}
Let \(B_{1331}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1331}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1331}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1332.}
Let \(B_{1332}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1332}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1332}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1333.}
Let \(B_{1333}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1333}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1333}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1334.}
Let \(B_{1334}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1334}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1334}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1335.}
Let \(B_{1335}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1335}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1335}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1336.}
Let \(B_{1336}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1336}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1336}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1337.}
Let \(B_{1337}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1337}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1337}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1338.}
Let \(B_{1338}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1338}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1338}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1339.}
Let \(B_{1339}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1339}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1339}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1340.}
Let \(B_{1340}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1340}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1340}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1341.}
Let \(B_{1341}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1341}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1341}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1342.}
Let \(B_{1342}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1342}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1342}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1343.}
Let \(B_{1343}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1343}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1343}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1344.}
Let \(B_{1344}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1344}\Rightarrow\neg\Pack_Q\), hence \(B_{1344}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1345.}
Let \(B_{1345}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1345}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1345}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1346.}
Let \(B_{1346}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1346}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1346}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1347.}
Let \(B_{1347}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1347}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1347}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1348.}
Let \(B_{1348}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1348}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1348}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1349.}
Let \(B_{1349}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1349}\Rightarrow\neg\Pack_Q\), hence \(B_{1349}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1350.}
Let \(B_{1350}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1350}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1350}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1351.}
Let \(B_{1351}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1351}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1351}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1352.}
Let \(B_{1352}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1352}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1352}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1353.}
Let \(B_{1353}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1353}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1353}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1354.}
Let \(B_{1354}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1354}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1354}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1355.}
Let \(B_{1355}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1355}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1355}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1356.}
Let \(B_{1356}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1356}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1356}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1357.}
Let \(B_{1357}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1357}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1357}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1358.}
Let \(B_{1358}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1358}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1358}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1359.}
Let \(B_{1359}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1359}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1360.}
Let \(B_{1360}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1360}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1360}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1361.}
Let \(B_{1361}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1361}\Rightarrow\neg\Pack_Q\), hence \(B_{1361}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1362.}
Let \(B_{1362}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1362}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1362}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1363.}
Let \(B_{1363}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1363}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1363}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1364.}
Let \(B_{1364}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1364}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1364}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1365.}
Let \(B_{1365}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1365}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1365}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1366.}
Let \(B_{1366}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1366}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1366}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1367.}
Let \(B_{1367}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1367}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1367}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1368.}
Let \(B_{1368}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1368}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1368}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1369.}
Let \(B_{1369}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1369}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1369}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1370.}
Let \(B_{1370}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1370}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1370}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1371.}
Let \(B_{1371}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1371}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1371}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1372.}
Let \(B_{1372}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1372}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1372}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1373.}
Let \(B_{1373}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1373}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1373}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1374.}
Let \(B_{1374}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1374}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1375.}
Let \(B_{1375}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1375}\Rightarrow\neg\Pack_Q\), hence \(B_{1375}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1376.}
Let \(B_{1376}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1376}\Rightarrow\neg\Pack_Q\), hence \(B_{1376}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1377.}
Let \(B_{1377}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1377}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1377}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1378.}
Let \(B_{1378}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1378}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1378}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1379.}
Let \(B_{1379}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1379}\Rightarrow\neg\Pack_Q\), hence \(B_{1379}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1380.}
Let \(B_{1380}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1380}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1381.}
Let \(B_{1381}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1381}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1382.}
Let \(B_{1382}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1382}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1382}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1383.}
Let \(B_{1383}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1383}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1383}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1384.}
Let \(B_{1384}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1384}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1384}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1385.}
Let \(B_{1385}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1385}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1385}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1386.}
Let \(B_{1386}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1386}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1386}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1387.}
Let \(B_{1387}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1387}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1387}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1388.}
Let \(B_{1388}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1388}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1388}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1389.}
Let \(B_{1389}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1389}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1389}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1390.}
Let \(B_{1390}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1390}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1390}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1391.}
Let \(B_{1391}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1391}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1391}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1392.}
Let \(B_{1392}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1392}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1392}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1393.}
Let \(B_{1393}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1393}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1393}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1394.}
Let \(B_{1394}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1394}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1394}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1395.}
Let \(B_{1395}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1395}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1395}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1396.}
Let \(B_{1396}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1396}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1396}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1397.}
Let \(B_{1397}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1397}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1397}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1398.}
Let \(B_{1398}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1398}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1398}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1399.}
Let \(B_{1399}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1399}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1399}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1400.}
Let \(B_{1400}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1400}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1400}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1401.}
Let \(B_{1401}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1401}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1401}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1402.}
Let \(B_{1402}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1402}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1402}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1403.}
Let \(B_{1403}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1403}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1403}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1404.}
Let \(B_{1404}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1404}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1404}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1405.}
Let \(B_{1405}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1405}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1405}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1406.}
Let \(B_{1406}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1406}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1406}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1407.}
Let \(B_{1407}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1407}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1407}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1408.}
Let \(B_{1408}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1408}\Rightarrow\neg\Pack_Q\), hence \(B_{1408}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1409.}
Let \(B_{1409}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1409}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1410.}
Let \(B_{1410}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1410}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1411.}
Let \(B_{1411}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1411}\Rightarrow\neg\Pack_Q\), hence \(B_{1411}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1412.}
Let \(B_{1412}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1412}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1412}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1413.}
Let \(B_{1413}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1413}\Rightarrow\neg\Pack_Q\), hence \(B_{1413}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1414.}
Let \(B_{1414}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1414}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1414}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1415.}
Let \(B_{1415}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1415}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1415}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1416.}
Let \(B_{1416}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1416}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1416}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1417.}
Let \(B_{1417}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1417}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1417}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1418.}
Let \(B_{1418}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1418}\Rightarrow\neg\Pack_Q\), hence \(B_{1418}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1419.}
Let \(B_{1419}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1419}\Rightarrow\neg\Pack_Q\), hence \(B_{1419}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1420.}
Let \(B_{1420}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1420}\Rightarrow\neg\Pack_Q\), hence \(B_{1420}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1421.}
Let \(B_{1421}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1421}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1421}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1422.}
Let \(B_{1422}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1422}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1422}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1423.}
Let \(B_{1423}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1423}\Rightarrow\neg\Pack_Q\), hence \(B_{1423}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1424.}
Let \(B_{1424}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1424}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1424}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1425.}
Let \(B_{1425}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1425}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1425}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1426.}
Let \(B_{1426}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1426}\Rightarrow\neg\Pack_Q\), hence \(B_{1426}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1427.}
Let \(B_{1427}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1427}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1427}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1428.}
Let \(B_{1428}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1428}\Rightarrow\neg\Pack_Q\), hence \(B_{1428}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1429.}
Let \(B_{1429}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1429}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1429}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1430.}
Let \(B_{1430}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1430}\Rightarrow\neg\Pack_Q\), hence \(B_{1430}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1431.}
Let \(B_{1431}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1431}\Rightarrow\neg\Pack_Q\), hence \(B_{1431}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1432.}
Let \(B_{1432}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1432}\Rightarrow\neg\Pack_Q\), hence \(B_{1432}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1433.}
Let \(B_{1433}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1433}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1433}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1434.}
Let \(B_{1434}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1434}\Rightarrow\neg\Pack_Q\), hence \(B_{1434}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1435.}
Let \(B_{1435}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1435}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1435}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1436.}
Let \(B_{1436}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1436}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1436}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1437.}
Let \(B_{1437}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1437}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1437}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1438.}
Let \(B_{1438}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1438}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1438}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1439.}
Let \(B_{1439}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1439}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1439}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1440.}
Let \(B_{1440}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1440}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1440}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1441.}
Let \(B_{1441}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1441}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1441}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1442.}
Let \(B_{1442}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1442}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1442}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1443.}
Let \(B_{1443}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1443}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1443}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1444.}
Let \(B_{1444}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1444}\Rightarrow\neg\Pack_Q\), hence \(B_{1444}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1445.}
Let \(B_{1445}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1445}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1445}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1446.}
Let \(B_{1446}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1446}\Rightarrow\neg\Pack_Q\), hence \(B_{1446}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1447.}
Let \(B_{1447}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1447}\Rightarrow\neg\Pack_Q\), hence \(B_{1447}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1448.}
Let \(B_{1448}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1448}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1448}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1449.}
Let \(B_{1449}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1449}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1449}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1450.}
Let \(B_{1450}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1450}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1450}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1451.}
Let \(B_{1451}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1451}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1451}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1452.}
Let \(B_{1452}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1452}\Rightarrow\neg\Pack_Q\), hence \(B_{1452}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1453.}
Let \(B_{1453}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1453}\Rightarrow\neg\Pack_Q\), hence \(B_{1453}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1454.}
Let \(B_{1454}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1454}\Rightarrow\neg\Pack_Q\), hence \(B_{1454}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1455.}
Let \(B_{1455}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1455}\Rightarrow\neg\Pack_Q\), hence \(B_{1455}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1456.}
Let \(B_{1456}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1456}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1456}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1457.}
Let \(B_{1457}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1457}\Rightarrow\neg\Pack_Q\), hence \(B_{1457}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1458.}
Let \(B_{1458}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1458}\Rightarrow\neg\Pack_Q\), hence \(B_{1458}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1459.}
Let \(B_{1459}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1459}\Rightarrow\neg\Pack_Q\), hence \(B_{1459}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1460.}
Let \(B_{1460}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1460}\Rightarrow\neg\Pack_Q\), hence \(B_{1460}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1461.}
Let \(B_{1461}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1461}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1461}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1462.}
Let \(B_{1462}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1462}\Rightarrow\neg\Pack_Q\), hence \(B_{1462}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1463.}
Let \(B_{1463}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1463}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1463}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1464.}
Let \(B_{1464}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1464}\Rightarrow\neg\Pack_Q\), hence \(B_{1464}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1465.}
Let \(B_{1465}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1465}\Rightarrow\neg\Pack_Q\), hence \(B_{1465}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1466.}
Let \(B_{1466}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1466}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1466}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1467.}
Let \(B_{1467}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1467}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1467}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1468.}
Let \(B_{1468}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1468}\Rightarrow\neg\Pack_Q\), hence \(B_{1468}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1469.}
Let \(B_{1469}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1469}\Rightarrow\neg\Pack_Q\), hence \(B_{1469}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1470.}
Let \(B_{1470}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1470}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1470}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1471.}
Let \(B_{1471}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1471}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1471}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1472.}
Let \(B_{1472}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1472}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1472}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1473.}
Let \(B_{1473}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1473}\Rightarrow\neg\Pack_Q\), hence \(B_{1473}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1474.}
Let \(B_{1474}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1474}\Rightarrow\neg\Pack_Q\), hence \(B_{1474}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1475.}
Let \(B_{1475}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1475}\Rightarrow\neg\Pack_Q\), hence \(B_{1475}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1476.}
Let \(B_{1476}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1476}\Rightarrow\neg\Pack_Q\), hence \(B_{1476}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1477.}
Let \(B_{1477}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1477}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1478.}
Let \(B_{1478}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1478}\Rightarrow\neg\Pack_Q\), hence \(B_{1478}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1479.}
Let \(B_{1479}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1479}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1479}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1480.}
Let \(B_{1480}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1480}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1480}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1481.}
Let \(B_{1481}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1481}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1481}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1482.}
Let \(B_{1482}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1482}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1482}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1483.}
Let \(B_{1483}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1483}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1483}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1484.}
Let \(B_{1484}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1484}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1484}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1485.}
Let \(B_{1485}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1485}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1485}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1486.}
Let \(B_{1486}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1486}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1486}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1487.}
Let \(B_{1487}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1487}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1487}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1488.}
Let \(B_{1488}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1488}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1488}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1489.}
Let \(B_{1489}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1489}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1489}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1490.}
Let \(B_{1490}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1490}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1490}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1491.}
Let \(B_{1491}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1491}\Rightarrow\neg\Pack_Q\), hence \(B_{1491}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1492.}
Let \(B_{1492}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1492}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1492}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1493.}
Let \(B_{1493}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1493}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1494.}
Let \(B_{1494}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1494}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1494}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1495.}
Let \(B_{1495}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1495}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1495}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1496.}
Let \(B_{1496}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1496}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1496}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1497.}
Let \(B_{1497}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1497}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1497}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1498.}
Let \(B_{1498}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1498}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1499.}
Let \(B_{1499}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1499}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1500.}
Let \(B_{1500}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1500}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1500}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1501.}
Let \(B_{1501}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1501}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1501}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1502.}
Let \(B_{1502}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1502}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1503.}
Let \(B_{1503}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1503}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1503}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1504.}
Let \(B_{1504}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1504}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1504}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1505.}
Let \(B_{1505}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1505}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1505}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1506.}
Let \(B_{1506}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1506}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1506}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1507.}
Let \(B_{1507}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1507}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1507}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1508.}
Let \(B_{1508}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1508}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1508}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1509.}
Let \(B_{1509}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1509}\Rightarrow\neg\Pack_Q\), hence \(B_{1509}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1510.}
Let \(B_{1510}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1510}\Rightarrow\neg\Pack_Q\), hence \(B_{1510}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1511.}
Let \(B_{1511}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1511}\Rightarrow\neg\Pack_Q\), hence \(B_{1511}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1512.}
Let \(B_{1512}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1512}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1512}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1513.}
Let \(B_{1513}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1513}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1513}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1514.}
Let \(B_{1514}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1514}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1514}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1515.}
Let \(B_{1515}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1515}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1515}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1516.}
Let \(B_{1516}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1516}\Rightarrow\neg\Pack_Q\), hence \(B_{1516}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1517.}
Let \(B_{1517}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1517}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1517}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1518.}
Let \(B_{1518}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1518}\Rightarrow\neg\Pack_Q\), hence \(B_{1518}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1519.}
Let \(B_{1519}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1519}\Rightarrow\neg\Pack_Q\), hence \(B_{1519}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1520.}
Let \(B_{1520}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1520}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1520}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1521.}
Let \(B_{1521}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1521}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1521}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1522.}
Let \(B_{1522}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1522}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1522}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1523.}
Let \(B_{1523}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1523}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1523}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1524.}
Let \(B_{1524}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1524}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1524}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1525.}
Let \(B_{1525}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1525}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1525}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1526.}
Let \(B_{1526}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1526}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1526}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1527.}
Let \(B_{1527}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1527}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1527}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1528.}
Let \(B_{1528}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1528}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1529.}
Let \(B_{1529}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1529}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1529}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1530.}
Let \(B_{1530}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1530}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1530}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1531.}
Let \(B_{1531}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1531}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1531}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1532.}
Let \(B_{1532}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1532}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1532}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1533.}
Let \(B_{1533}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1533}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1533}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1534.}
Let \(B_{1534}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1534}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1535.}
Let \(B_{1535}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1535}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1535}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1536.}
Let \(B_{1536}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1536}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1536}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1537.}
Let \(B_{1537}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1537}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1537}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1538.}
Let \(B_{1538}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1538}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1538}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1539.}
Let \(B_{1539}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1539}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1539}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1540.}
Let \(B_{1540}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1540}\Rightarrow\neg\Pack_Q\), hence \(B_{1540}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1541.}
Let \(B_{1541}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1541}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1541}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1542.}
Let \(B_{1542}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1542}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1542}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1543.}
Let \(B_{1543}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1543}\Rightarrow\neg\Pack_Q\), hence \(B_{1543}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1544.}
Let \(B_{1544}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1544}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1544}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1545.}
Let \(B_{1545}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1545}\Rightarrow\neg\Pack_Q\), hence \(B_{1545}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1546.}
Let \(B_{1546}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1546}\Rightarrow\neg\Pack_Q\), hence \(B_{1546}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1547.}
Let \(B_{1547}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1547}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1547}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1548.}
Let \(B_{1548}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1548}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1548}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1549.}
Let \(B_{1549}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1549}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1549}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1550.}
Let \(B_{1550}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1550}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1550}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1551.}
Let \(B_{1551}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1551}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1551}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1552.}
Let \(B_{1552}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1552}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1552}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1553.}
Let \(B_{1553}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1553}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1553}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1554.}
Let \(B_{1554}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1554}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1554}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1555.}
Let \(B_{1555}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1555}\Rightarrow\neg\Pack_Q\), hence \(B_{1555}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1556.}
Let \(B_{1556}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1556}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1556}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1557.}
Let \(B_{1557}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1557}\Rightarrow\neg\Pack_Q\), hence \(B_{1557}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1558.}
Let \(B_{1558}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1558}\Rightarrow\neg\Pack_Q\), hence \(B_{1558}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1559.}
Let \(B_{1559}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1559}\Rightarrow\neg\Pack_Q\), hence \(B_{1559}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1560.}
Let \(B_{1560}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1560}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1560}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1561.}
Let \(B_{1561}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1561}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1561}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1562.}
Let \(B_{1562}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1562}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1562}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1563.}
Let \(B_{1563}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1563}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1563}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1564.}
Let \(B_{1564}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1564}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1564}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1565.}
Let \(B_{1565}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1565}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1565}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1566.}
Let \(B_{1566}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1566}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1566}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1567.}
Let \(B_{1567}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1567}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1567}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1568.}
Let \(B_{1568}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1568}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1568}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1569.}
Let \(B_{1569}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1569}\Rightarrow\neg\Pack_Q\), hence \(B_{1569}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1570.}
Let \(B_{1570}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1570}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1570}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1571.}
Let \(B_{1571}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1571}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1571}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1572.}
Let \(B_{1572}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1572}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1573.}
Let \(B_{1573}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1573}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1573}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1574.}
Let \(B_{1574}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1574}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1574}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1575.}
Let \(B_{1575}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1575}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1575}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1576.}
Let \(B_{1576}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1576}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1576}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1577.}
Let \(B_{1577}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1577}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1577}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1578.}
Let \(B_{1578}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1578}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1578}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1579.}
Let \(B_{1579}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1579}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1579}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1580.}
Let \(B_{1580}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1580}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1580}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1581.}
Let \(B_{1581}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1581}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1581}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1582.}
Let \(B_{1582}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1582}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1582}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1583.}
Let \(B_{1583}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1583}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1583}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1584.}
Let \(B_{1584}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1584}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1584}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1585.}
Let \(B_{1585}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1585}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1585}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1586.}
Let \(B_{1586}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1586}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1586}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1587.}
Let \(B_{1587}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1587}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1587}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1588.}
Let \(B_{1588}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1588}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1588}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1589.}
Let \(B_{1589}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1589}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1589}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1590.}
Let \(B_{1590}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1590}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1590}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1591.}
Let \(B_{1591}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1591}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1591}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1592.}
Let \(B_{1592}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1592}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1592}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1593.}
Let \(B_{1593}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1593}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1593}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1594.}
Let \(B_{1594}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1594}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1594}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1595.}
Let \(B_{1595}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1595}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1595}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1596.}
Let \(B_{1596}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1596}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1596}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1597.}
Let \(B_{1597}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1597}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1597}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1598.}
Let \(B_{1598}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1598}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1598}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1599.}
Let \(B_{1599}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1599}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1599}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1600.}
Let \(B_{1600}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1600}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1600}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1601.}
Let \(B_{1601}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1601}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1601}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1602.}
Let \(B_{1602}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1602}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1602}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1603.}
Let \(B_{1603}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1603}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1603}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1604.}
Let \(B_{1604}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1604}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1604}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1605.}
Let \(B_{1605}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1605}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1605}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1606.}
Let \(B_{1606}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1606}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1606}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1607.}
Let \(B_{1607}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1607}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1607}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1608.}
Let \(B_{1608}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1608}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1608}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1609.}
Let \(B_{1609}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1609}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1609}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1610.}
Let \(B_{1610}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1610}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1610}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1611.}
Let \(B_{1611}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1611}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1611}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1612.}
Let \(B_{1612}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1612}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1612}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1613.}
Let \(B_{1613}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1613}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1613}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1614.}
Let \(B_{1614}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1614}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1614}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1615.}
Let \(B_{1615}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1615}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1615}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1616.}
Let \(B_{1616}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1616}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1616}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1617.}
Let \(B_{1617}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1617}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1617}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1618.}
Let \(B_{1618}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1618}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1618}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1619.}
Let \(B_{1619}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1619}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1619}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1620.}
Let \(B_{1620}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1620}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1620}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1621.}
Let \(B_{1621}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1621}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1621}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1622.}
Let \(B_{1622}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1622}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1622}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1623.}
Let \(B_{1623}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1623}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1623}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1624.}
Let \(B_{1624}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1624}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1624}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1625.}
Let \(B_{1625}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1625}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1625}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1626.}
Let \(B_{1626}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1626}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1626}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1627.}
Let \(B_{1627}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1627}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1627}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1628.}
Let \(B_{1628}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1628}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1628}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1629.}
Let \(B_{1629}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1629}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1629}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1630.}
Let \(B_{1630}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1630}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1630}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1631.}
Let \(B_{1631}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1631}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1631}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1632.}
Let \(B_{1632}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1632}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1632}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1633.}
Let \(B_{1633}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1633}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1633}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1634.}
Let \(B_{1634}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1634}\Rightarrow\neg\Pack_Q\), hence \(B_{1634}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1635.}
Let \(B_{1635}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1635}\Rightarrow\neg\Pack_Q\), hence \(B_{1635}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1636.}
Let \(B_{1636}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1636}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1636}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1637.}
Let \(B_{1637}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1637}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1637}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1638.}
Let \(B_{1638}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1638}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1639.}
Let \(B_{1639}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1639}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1639}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1640.}
Let \(B_{1640}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1640}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1640}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1641.}
Let \(B_{1641}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1641}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1641}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1642.}
Let \(B_{1642}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1642}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1642}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1643.}
Let \(B_{1643}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1643}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1643}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1644.}
Let \(B_{1644}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1644}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1645.}
Let \(B_{1645}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1645}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1645}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1646.}
Let \(B_{1646}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1646}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1646}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1647.}
Let \(B_{1647}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1647}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1647}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1648.}
Let \(B_{1648}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1648}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1648}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1649.}
Let \(B_{1649}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1649}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1649}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1650.}
Let \(B_{1650}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1650}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1650}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1651.}
Let \(B_{1651}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1651}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1651}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1652.}
Let \(B_{1652}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1652}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1652}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1653.}
Let \(B_{1653}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1653}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1653}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1654.}
Let \(B_{1654}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1654}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1654}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1655.}
Let \(B_{1655}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1655}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1655}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1656.}
Let \(B_{1656}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1656}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1656}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1657.}
Let \(B_{1657}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1657}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1657}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1658.}
Let \(B_{1658}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1658}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1658}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1659.}
Let \(B_{1659}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1659}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1659}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1660.}
Let \(B_{1660}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1660}\Rightarrow\neg\Pack_Q\), hence \(B_{1660}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1661.}
Let \(B_{1661}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1661}\Rightarrow\neg\Pack_Q\), hence \(B_{1661}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1662.}
Let \(B_{1662}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1662}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1662}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1663.}
Let \(B_{1663}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1663}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1663}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1664.}
Let \(B_{1664}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1664}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1664}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1665.}
Let \(B_{1665}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1665}\Rightarrow\neg\Pack_Q\), hence \(B_{1665}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1666.}
Let \(B_{1666}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1666}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1666}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1667.}
Let \(B_{1667}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1667}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1667}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1668.}
Let \(B_{1668}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1668}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1668}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1669.}
Let \(B_{1669}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1669}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1669}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1670.}
Let \(B_{1670}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1670}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1670}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1671.}
Let \(B_{1671}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1671}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1671}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1672.}
Let \(B_{1672}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1672}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1672}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1673.}
Let \(B_{1673}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1673}\Rightarrow\neg\Pack_Q\), hence \(B_{1673}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1674.}
Let \(B_{1674}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1674}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1674}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1675.}
Let \(B_{1675}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1675}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1675}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1676.}
Let \(B_{1676}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1676}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1676}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1677.}
Let \(B_{1677}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1677}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1677}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1678.}
Let \(B_{1678}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1678}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1678}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1679.}
Let \(B_{1679}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1679}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1679}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1680.}
Let \(B_{1680}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1680}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1680}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1681.}
Let \(B_{1681}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1681}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1681}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1682.}
Let \(B_{1682}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1682}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1682}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1683.}
Let \(B_{1683}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1683}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1683}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1684.}
Let \(B_{1684}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1684}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1684}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1685.}
Let \(B_{1685}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1685}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1685}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1686.}
Let \(B_{1686}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1686}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1686}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1687.}
Let \(B_{1687}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1687}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1688.}
Let \(B_{1688}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1688}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1688}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1689.}
Let \(B_{1689}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1689}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1690.}
Let \(B_{1690}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1690}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1691.}
Let \(B_{1691}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1691}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1691}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1692.}
Let \(B_{1692}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1692}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1692}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1693.}
Let \(B_{1693}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1693}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1694.}
Let \(B_{1694}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1694}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1694}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1695.}
Let \(B_{1695}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1695}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1696.}
Let \(B_{1696}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1696}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1697.}
Let \(B_{1697}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1697}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1697}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1698.}
Let \(B_{1698}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1698}\Rightarrow\neg\Pack_Q\), hence \(B_{1698}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1699.}
Let \(B_{1699}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1699}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1699}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1700.}
Let \(B_{1700}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1700}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1701.}
Let \(B_{1701}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1701}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1702.}
Let \(B_{1702}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1702}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1703.}
Let \(B_{1703}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1703}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1704.}
Let \(B_{1704}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1704}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1705.}
Let \(B_{1705}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1705}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1705}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1706.}
Let \(B_{1706}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1706}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1706}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1707.}
Let \(B_{1707}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1707}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1708.}
Let \(B_{1708}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1708}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1709.}
Let \(B_{1709}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1709}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1709}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1710.}
Let \(B_{1710}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1710}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1711.}
Let \(B_{1711}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1711}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1712.}
Let \(B_{1712}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1712}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1713.}
Let \(B_{1713}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1713}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1713}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1714.}
Let \(B_{1714}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1714}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1715.}
Let \(B_{1715}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1715}\Rightarrow\neg\Pack_Q\), hence \(B_{1715}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1716.}
Let \(B_{1716}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1716}\Rightarrow\neg\Pack_Q\), hence \(B_{1716}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1717.}
Let \(B_{1717}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1717}\Rightarrow\neg\Pack_Q\), hence \(B_{1717}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1718.}
Let \(B_{1718}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1718}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1718}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1719.}
Let \(B_{1719}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1719}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1720.}
Let \(B_{1720}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1720}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1720}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1721.}
Let \(B_{1721}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1721}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1722.}
Let \(B_{1722}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1722}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1722}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1723.}
Let \(B_{1723}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1723}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1723}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1724.}
Let \(B_{1724}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1724}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1724}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1725.}
Let \(B_{1725}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1725}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1725}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1726.}
Let \(B_{1726}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1726}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1726}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1727.}
Let \(B_{1727}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1727}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1727}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1728.}
Let \(B_{1728}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1728}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1729.}
Let \(B_{1729}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1729}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1729}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1730.}
Let \(B_{1730}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1730}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1730}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1731.}
Let \(B_{1731}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1731}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1731}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1732.}
Let \(B_{1732}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1732}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1732}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1733.}
Let \(B_{1733}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1733}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1733}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1734.}
Let \(B_{1734}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1734}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1734}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1735.}
Let \(B_{1735}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1735}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1735}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1736.}
Let \(B_{1736}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1736}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1737.}
Let \(B_{1737}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1737}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1737}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1738.}
Let \(B_{1738}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1738}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1738}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1739.}
Let \(B_{1739}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1739}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1739}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1740.}
Let \(B_{1740}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1740}\Rightarrow\neg\Pack_Q\), hence \(B_{1740}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1741.}
Let \(B_{1741}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1741}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1741}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1742.}
Let \(B_{1742}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1742}\Rightarrow\neg\Pack_Q\), hence \(B_{1742}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1743.}
Let \(B_{1743}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1743}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1744.}
Let \(B_{1744}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1744}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1744}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1745.}
Let \(B_{1745}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1745}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1745}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1746.}
Let \(B_{1746}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1746}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1747.}
Let \(B_{1747}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1747}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1748.}
Let \(B_{1748}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1748}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1748}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1749.}
Let \(B_{1749}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1749}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1750.}
Let \(B_{1750}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1750}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1751.}
Let \(B_{1751}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1751}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1752.}
Let \(B_{1752}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1752}\Rightarrow\neg\Pack_Q\), hence \(B_{1752}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1753.}
Let \(B_{1753}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1753}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1754.}
Let \(B_{1754}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1754}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1754}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1755.}
Let \(B_{1755}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1755}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1755}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1756.}
Let \(B_{1756}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1756}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1757.}
Let \(B_{1757}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1757}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1757}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1758.}
Let \(B_{1758}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1758}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1758}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1759.}
Let \(B_{1759}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1759}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1760.}
Let \(B_{1760}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1760}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1760}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1761.}
Let \(B_{1761}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1761}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1762.}
Let \(B_{1762}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1762}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1763.}
Let \(B_{1763}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1763}\Rightarrow\neg\Pack_Q\), hence \(B_{1763}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1764.}
Let \(B_{1764}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1764}\Rightarrow\neg\Pack_Q\), hence \(B_{1764}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1765.}
Let \(B_{1765}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1765}\Rightarrow\neg\Pack_Q\), hence \(B_{1765}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier service before any participation or field readout can be asked of it. The Pack-first order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1766.}
Let \(B_{1766}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1766}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1766}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1767.}
Let \(B_{1767}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1767}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1767}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1768.}
Let \(B_{1768}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1768}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1768}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1769.}
Let \(B_{1769}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1769}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1770.}
Let \(B_{1770}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1770}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1770}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1771.}
Let \(B_{1771}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1771}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1771}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1772.}
Let \(B_{1772}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1772}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1772}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1773.}
Let \(B_{1773}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1773}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1773}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1774.}
Let \(B_{1774}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1774}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1774}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1775.}
Let \(B_{1775}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1775}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1775}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1776.}
Let \(B_{1776}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1776}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1776}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1777.}
Let \(B_{1777}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1777}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1777}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1778.}
Let \(B_{1778}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1778}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1778}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1779.}
Let \(B_{1779}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1779}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1779}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1780.}
Let \(B_{1780}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1780}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1780}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1781.}
Let \(B_{1781}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1781}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1781}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1782.}
Let \(B_{1782}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1782}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1782}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1783.}
Let \(B_{1783}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1783}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1783}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1784.}
Let \(B_{1784}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1784}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1784}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1785.}
Let \(B_{1785}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1785}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1785}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1786.}
Let \(B_{1786}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1786}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1786}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1787.}
Let \(B_{1787}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1787}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1788.}
Let \(B_{1788}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1788}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1789.}
Let \(B_{1789}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1789}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1789}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1790.}
Let \(B_{1790}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1790}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1791.}
Let \(B_{1791}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1791}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1792.}
Let \(B_{1792}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1792}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1792}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1793.}
Let \(B_{1793}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1793}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1793}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1794.}
Let \(B_{1794}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1794}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1794}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1795.}
Let \(B_{1795}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1795}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1795}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1796.}
Let \(B_{1796}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1796}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1796}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1797.}
Let \(B_{1797}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1797}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1797}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1798.}
Let \(B_{1798}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1798}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1799.}
Let \(B_{1799}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1799}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1799}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1800.}
Let \(B_{1800}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1800}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1801.}
Let \(B_{1801}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1801}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1801}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1802.}
Let \(B_{1802}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1802}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1802}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1803.}
Let \(B_{1803}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1803}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1803}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1804.}
Let \(B_{1804}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1804}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1805.}
Let \(B_{1805}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1805}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1806.}
Let \(B_{1806}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1806}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1807.}
Let \(B_{1807}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1807}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1808.}
Let \(B_{1808}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1808}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1808}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1809.}
Let \(B_{1809}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1809}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1809}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1810.}
Let \(B_{1810}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1810}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1810}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1811.}
Let \(B_{1811}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1811}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1811}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1812.}
Let \(B_{1812}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1812}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1812}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1813.}
Let \(B_{1813}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1813}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1813}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1814.}
Let \(B_{1814}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1814}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1814}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1815.}
Let \(B_{1815}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1815}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1816.}
Let \(B_{1816}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1816}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1816}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1817.}
Let \(B_{1817}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1817}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1817}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1818.}
Let \(B_{1818}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1818}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1818}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1819.}
Let \(B_{1819}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1819}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1819}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1820.}
Let \(B_{1820}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1820}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1820}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1821.}
Let \(B_{1821}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1821}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1822.}
Let \(B_{1822}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1822}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1822}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1823.}
Let \(B_{1823}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1823}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1823}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1824.}
Let \(B_{1824}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1824}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1824}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1825.}
Let \(B_{1825}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1825}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1825}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1826.}
Let \(B_{1826}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1826}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1826}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1827.}
Let \(B_{1827}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1827}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1827}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1828.}
Let \(B_{1828}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1828}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1828}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1829.}
Let \(B_{1829}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1829}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1829}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1830.}
Let \(B_{1830}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1830}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1831.}
Let \(B_{1831}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1831}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1832.}
Let \(B_{1832}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1832}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1832}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1833.}
Let \(B_{1833}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1833}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1833}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1834.}
Let \(B_{1834}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1834}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1835.}
Let \(B_{1835}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1835}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1836.}
Let \(B_{1836}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1836}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1837.}
Let \(B_{1837}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1837}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1838.}
Let \(B_{1838}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1838}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1838}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1839.}
Let \(B_{1839}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1839}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1839}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1840.}
Let \(B_{1840}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1840}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1840}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1841.}
Let \(B_{1841}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1841}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1841}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1842.}
Let \(B_{1842}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1842}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1842}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1843.}
Let \(B_{1843}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1843}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1843}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1844.}
Let \(B_{1844}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1844}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1845.}
Let \(B_{1845}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1845}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1845}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1846.}
Let \(B_{1846}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1846}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1846}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1847.}
Let \(B_{1847}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1847}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1847}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1848.}
Let \(B_{1848}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1848}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1848}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1849.}
Let \(B_{1849}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1849}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1849}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1850.}
Let \(B_{1850}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1850}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1850}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1851.}
Let \(B_{1851}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1851}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1851}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1852.}
Let \(B_{1852}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1852}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1853.}
Let \(B_{1853}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1853}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1853}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1854.}
Let \(B_{1854}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1854}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1855.}
Let \(B_{1855}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1855}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1855}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1856.}
Let \(B_{1856}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1856}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1856}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1857.}
Let \(B_{1857}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1857}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1857}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1858.}
Let \(B_{1858}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1858}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1858}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1859.}
Let \(B_{1859}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1859}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1859}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1860.}
Let \(B_{1860}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1860}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1860}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1861.}
Let \(B_{1861}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1861}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1861}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1862.}
Let \(B_{1862}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1862}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1863.}
Let \(B_{1863}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1863}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1864.}
Let \(B_{1864}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1864}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1865.}
Let \(B_{1865}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1865}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1865}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1866.}
Let \(B_{1866}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1866}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1866}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1867.}
Let \(B_{1867}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1867}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1867}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1868.}
Let \(B_{1868}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1868}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1868}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1869.}
Let \(B_{1869}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1869}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1870.}
Let \(B_{1870}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1870}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1870}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1871.}
Let \(B_{1871}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1871}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1871}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1872.}
Let \(B_{1872}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1872}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1873.}
Let \(B_{1873}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1873}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1874.}
Let \(B_{1874}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1874}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1875.}
Let \(B_{1875}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1875}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1875}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1876.}
Let \(B_{1876}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1876}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1877.}
Let \(B_{1877}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1877}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1877}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1878.}
Let \(B_{1878}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1878}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1878}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1879.}
Let \(B_{1879}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1879}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1880.}
Let \(B_{1880}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1880}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1880}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1881.}
Let \(B_{1881}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1881}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1881}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1882.}
Let \(B_{1882}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1882}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1882}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1883.}
Let \(B_{1883}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1883}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1884.}
Let \(B_{1884}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1884}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1884}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1885.}
Let \(B_{1885}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1885}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1886.}
Let \(B_{1886}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1886}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1886}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1887.}
Let \(B_{1887}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1887}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1888.}
Let \(B_{1888}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1888}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1888}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1889.}
Let \(B_{1889}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1889}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1889}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1890.}
Let \(B_{1890}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1890}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1891.}
Let \(B_{1891}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1891}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1892.}
Let \(B_{1892}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1892}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1893.}
Let \(B_{1893}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1893}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1893}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1894.}
Let \(B_{1894}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1894}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1894}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1895.}
Let \(B_{1895}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1895}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1895}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1896.}
Let \(B_{1896}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1896}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1896}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1897.}
Let \(B_{1897}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1897}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1897}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1898.}
Let \(B_{1898}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1898}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1898}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1899.}
Let \(B_{1899}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1899}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1899}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1900.}
Let \(B_{1900}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1900}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1900}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1901.}
Let \(B_{1901}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1901}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1901}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1902.}
Let \(B_{1902}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1902}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1902}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1903.}
Let \(B_{1903}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1903}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1903}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1904.}
Let \(B_{1904}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1904}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1904}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1905.}
Let \(B_{1905}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1905}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1905}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1906.}
Let \(B_{1906}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1906}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1907.}
Let \(B_{1907}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1907}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1907}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1908.}
Let \(B_{1908}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1908}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1908}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1909.}
Let \(B_{1909}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1909}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1909}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1910.}
Let \(B_{1910}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1910}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1911.}
Let \(B_{1911}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1911}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1911}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1912.}
Let \(B_{1912}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1912}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1913.}
Let \(B_{1913}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1913}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1913}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1914.}
Let \(B_{1914}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1914}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1915.}
Let \(B_{1915}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1915}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1916.}
Let \(B_{1916}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1916}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1916}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1917.}
Let \(B_{1917}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1917}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1917}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1918.}
Let \(B_{1918}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1918}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1919.}
Let \(B_{1919}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1919}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1920.}
Let \(B_{1920}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1920}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1920}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1921.}
Let \(B_{1921}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1921}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1922.}
Let \(B_{1922}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1922}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1923.}
Let \(B_{1923}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1923}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1923}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1924.}
Let \(B_{1924}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1924}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1924}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1925.}
Let \(B_{1925}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1925}\Rightarrow \Pack_Q\wedge\neg\Part_{N,Q}\), hence \(B_{1925}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost service is \(\Part_{N,Q}\). The face failure embeds as class exit.

\paragraph{CM expansion 1926.}
Let \(B_{1926}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1926}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 1927.}
Let \(B_{1927}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1927}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1928.}
Let \(B_{1928}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1928}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 1929.}
Let \(B_{1929}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1929}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 1930.}
Let \(B_{1930}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1930}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 1931.}
Let \(B_{1931}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1931}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1932.}
Let \(B_{1932}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1932}\) introduces no independent terminal alternative outside the installed Pack/Part/Field exhaustion. \emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation service and no fourth branch.

\paragraph{CM expansion 1933.}
Let \(B_{1933}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1933}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1934.}
Let \(B_{1934}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1934}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1935.}
Let \(B_{1935}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1935}\) has no CM conclusion until it selects a same-fluid terminal packet. \emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Pack, Part, Field, membership-readout, or no-third-branch hinge.

\paragraph{CM expansion 1936.}
Let \(B_{1936}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1936}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1936}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1937.}
Let \(B_{1937}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1937}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1937}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1938.}
Let \(B_{1938}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1938}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1938}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1939.}
Let \(B_{1939}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1939}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1939}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1940.}
Let \(B_{1940}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1940}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1940}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1941.}
Let \(B_{1941}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1941}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1941}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1942.}
Let \(B_{1942}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1942}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1942}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\paragraph{CM expansion 1943.}
Let \(B_{1943}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1943}\Rightarrow \neg\Pack_Q\vee\neg\Part_{N,Q}\vee\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1943}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-first order. A finite disjunction of Pack, Part, and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\paragraph{CM expansion 1944.}
Let \(B_{1944}\) be the selected terminal branch at this place in the exhaustion. \emph{Claim.} \(B_{1944}\Rightarrow \Pack_Q\wedge\Part_{N,Q}\wedge\forall r>0\,\neg\Field_{N,r,Q}\), hence \(B_{1944}\Rightarrow\Exit(Q;\Owork)\). \emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation service is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\section{Expanded Forward-Support Obligations}

Let \(S_j\) denote the \(j\)-th forward-support obligation. These obligations are included here only by mathematical role: they strengthen a pass branch, select a fail branch, feed a readout, or require a same-fluid packet before the CM tree can consume them.

\paragraph{Forward-support expansion 1.}
Let \(S_{1}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 2.}
Let \(S_{2}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{2}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 3.}
Let \(S_{3}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{3}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 4.}
Let \(S_{4}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{4}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 5.}
Let \(S_{5}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{5}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 6.}
Let \(S_{6}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{6}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 7.}
Let \(S_{7}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{7}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 8.}
Let \(S_{8}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{8}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 9.}
Let \(S_{9}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{9}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 10.}
Let \(S_{10}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{10}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 11.}
Let \(S_{11}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{11}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 12.}
Let \(S_{12}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{12}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 13.}
Let \(S_{13}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{13}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 14.}
Let \(S_{14}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{14}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 15.}
Let \(S_{15}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{15}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 16.}
Let \(S_{16}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{16}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 17.}
Let \(S_{17}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{17}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 18.}
Let \(S_{18}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{18}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 19.}
Let \(S_{19}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{19}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 20.}
Let \(S_{20}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{20}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 21.}
Let \(S_{21}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{21}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 22.}
Let \(S_{22}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{22}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 23.}
Let \(S_{23}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{23}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 24.}
Let \(S_{24}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{24}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 25.}
Let \(S_{25}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{25}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 26.}
Let \(S_{26}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{26}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 27.}
Let \(S_{27}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{27}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 28.}
Let \(S_{28}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{28}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 29.}
Let \(S_{29}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{29}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 30.}
Let \(S_{30}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{30}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 31.}
Let \(S_{31}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{31}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 32.}
Let \(S_{32}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{32}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 33.}
Let \(S_{33}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{33}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 34.}
Let \(S_{34}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{34}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 35.}
Let \(S_{35}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{35}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 36.}
Let \(S_{36}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{36}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 37.}
Let \(S_{37}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{37}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 38.}
Let \(S_{38}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{38}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 39.}
Let \(S_{39}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{39}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 40.}
Let \(S_{40}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{40}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 41.}
Let \(S_{41}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{41}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 42.}
Let \(S_{42}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{42}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 43.}
Let \(S_{43}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{43}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 44.}
Let \(S_{44}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{44}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 45.}
Let \(S_{45}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{45}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 46.}
Let \(S_{46}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{46}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 47.}
Let \(S_{47}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{47}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 48.}
Let \(S_{48}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{48}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 49.}
Let \(S_{49}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{49}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 50.}
Let \(S_{50}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{50}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 51.}
Let \(S_{51}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{51}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 52.}
Let \(S_{52}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{52}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 53.}
Let \(S_{53}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{53}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 54.}
Let \(S_{54}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{54}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 55.}
Let \(S_{55}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{55}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 56.}
Let \(S_{56}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{56}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 57.}
Let \(S_{57}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{57}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 58.}
Let \(S_{58}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{58}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 59.}
Let \(S_{59}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{59}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 60.}
Let \(S_{60}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{60}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 61.}
Let \(S_{61}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{61}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 62.}
Let \(S_{62}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{62}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 63.}
Let \(S_{63}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{63}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 64.}
Let \(S_{64}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{64}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 65.}
Let \(S_{65}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{65}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 66.}
Let \(S_{66}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{66}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 67.}
Let \(S_{67}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{67}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 68.}
Let \(S_{68}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{68}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 69.}
Let \(S_{69}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{69}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 70.}
Let \(S_{70}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{70}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 71.}
Let \(S_{71}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{71}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 72.}
Let \(S_{72}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{72}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 73.}
Let \(S_{73}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{73}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 74.}
Let \(S_{74}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{74}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 75.}
Let \(S_{75}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{75}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 76.}
Let \(S_{76}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{76}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 77.}
Let \(S_{77}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{77}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 78.}
Let \(S_{78}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{78}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 79.}
Let \(S_{79}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{79}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 80.}
Let \(S_{80}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{80}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 81.}
Let \(S_{81}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{81}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 82.}
Let \(S_{82}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{82}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 83.}
Let \(S_{83}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{83}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 84.}
Let \(S_{84}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{84}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 85.}
Let \(S_{85}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{85}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 86.}
Let \(S_{86}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{86}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 87.}
Let \(S_{87}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{87}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 88.}
Let \(S_{88}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{88}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 89.}
Let \(S_{89}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{89}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 90.}
Let \(S_{90}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{90}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 91.}
Let \(S_{91}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{91}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 92.}
Let \(S_{92}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{92}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 93.}
Let \(S_{93}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{93}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 94.}
Let \(S_{94}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{94}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 95.}
Let \(S_{95}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{95}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 96.}
Let \(S_{96}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{96}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 97.}
Let \(S_{97}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{97}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 98.}
Let \(S_{98}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{98}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 99.}
Let \(S_{99}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{99}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 100.}
Let \(S_{100}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{100}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 101.}
Let \(S_{101}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{101}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 102.}
Let \(S_{102}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{102}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 103.}
Let \(S_{103}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{103}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 104.}
Let \(S_{104}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{104}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 105.}
Let \(S_{105}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{105}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 106.}
Let \(S_{106}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{106}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 107.}
Let \(S_{107}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{107}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 108.}
Let \(S_{108}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{108}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 109.}
Let \(S_{109}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{109}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 110.}
Let \(S_{110}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{110}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 111.}
Let \(S_{111}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{111}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 112.}
Let \(S_{112}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{112}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 113.}
Let \(S_{113}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{113}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 114.}
Let \(S_{114}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{114}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 115.}
Let \(S_{115}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{115}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 116.}
Let \(S_{116}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{116}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 117.}
Let \(S_{117}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{117}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 118.}
Let \(S_{118}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{118}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 119.}
Let \(S_{119}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{119}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 120.}
Let \(S_{120}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{120}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 121.}
Let \(S_{121}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{121}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 122.}
Let \(S_{122}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{122}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 123.}
Let \(S_{123}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{123}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 124.}
Let \(S_{124}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{124}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 125.}
Let \(S_{125}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{125}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 126.}
Let \(S_{126}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{126}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 127.}
Let \(S_{127}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{127}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 128.}
Let \(S_{128}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{128}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 129.}
Let \(S_{129}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{129}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 130.}
Let \(S_{130}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{130}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 131.}
Let \(S_{131}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{131}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 132.}
Let \(S_{132}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{132}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 133.}
Let \(S_{133}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{133}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 134.}
Let \(S_{134}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{134}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 135.}
Let \(S_{135}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{135}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 136.}
Let \(S_{136}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{136}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 137.}
Let \(S_{137}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{137}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 138.}
Let \(S_{138}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{138}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 139.}
Let \(S_{139}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{139}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 140.}
Let \(S_{140}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{140}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 141.}
Let \(S_{141}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{141}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 142.}
Let \(S_{142}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{142}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 143.}
Let \(S_{143}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{143}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 144.}
Let \(S_{144}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{144}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 145.}
Let \(S_{145}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{145}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 146.}
Let \(S_{146}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{146}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 147.}
Let \(S_{147}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{147}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 148.}
Let \(S_{148}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{148}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 149.}
Let \(S_{149}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{149}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 150.}
Let \(S_{150}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{150}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 151.}
Let \(S_{151}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{151}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 152.}
Let \(S_{152}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{152}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 153.}
Let \(S_{153}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{153}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 154.}
Let \(S_{154}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{154}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 155.}
Let \(S_{155}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{155}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 156.}
Let \(S_{156}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{156}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 157.}
Let \(S_{157}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{157}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 158.}
Let \(S_{158}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{158}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 159.}
Let \(S_{159}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{159}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 160.}
Let \(S_{160}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{160}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 161.}
Let \(S_{161}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{161}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 162.}
Let \(S_{162}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{162}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 163.}
Let \(S_{163}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{163}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 164.}
Let \(S_{164}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{164}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 165.}
Let \(S_{165}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{165}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 166.}
Let \(S_{166}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{166}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 167.}
Let \(S_{167}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{167}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 168.}
Let \(S_{168}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{168}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 169.}
Let \(S_{169}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{169}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 170.}
Let \(S_{170}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{170}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 171.}
Let \(S_{171}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{171}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 172.}
Let \(S_{172}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{172}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 173.}
Let \(S_{173}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{173}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 174.}
Let \(S_{174}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{174}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 175.}
Let \(S_{175}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{175}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 176.}
Let \(S_{176}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{176}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 177.}
Let \(S_{177}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{177}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 178.}
Let \(S_{178}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{178}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 179.}
Let \(S_{179}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{179}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 180.}
Let \(S_{180}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{180}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 181.}
Let \(S_{181}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{181}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 182.}
Let \(S_{182}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{182}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 183.}
Let \(S_{183}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{183}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 184.}
Let \(S_{184}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{184}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 185.}
Let \(S_{185}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{185}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 186.}
Let \(S_{186}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{186}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 187.}
Let \(S_{187}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{187}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 188.}
Let \(S_{188}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{188}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 189.}
Let \(S_{189}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{189}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 190.}
Let \(S_{190}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{190}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 191.}
Let \(S_{191}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{191}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 192.}
Let \(S_{192}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{192}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 193.}
Let \(S_{193}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{193}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 194.}
Let \(S_{194}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{194}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 195.}
Let \(S_{195}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{195}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 196.}
Let \(S_{196}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{196}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 197.}
Let \(S_{197}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{197}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 198.}
Let \(S_{198}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{198}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 199.}
Let \(S_{199}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{199}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 200.}
Let \(S_{200}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{200}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 201.}
Let \(S_{201}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{201}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 202.}
Let \(S_{202}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{202}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 203.}
Let \(S_{203}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{203}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 204.}
Let \(S_{204}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{204}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 205.}
Let \(S_{205}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{205}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 206.}
Let \(S_{206}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{206}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 207.}
Let \(S_{207}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{207}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 208.}
Let \(S_{208}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{208}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 209.}
Let \(S_{209}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{209}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 210.}
Let \(S_{210}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{210}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 211.}
Let \(S_{211}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{211}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 212.}
Let \(S_{212}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{212}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 213.}
Let \(S_{213}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{213}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 214.}
Let \(S_{214}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{214}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 215.}
Let \(S_{215}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{215}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 216.}
Let \(S_{216}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{216}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 217.}
Let \(S_{217}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{217}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 218.}
Let \(S_{218}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{218}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 219.}
Let \(S_{219}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{219}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 220.}
Let \(S_{220}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{220}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 221.}
Let \(S_{221}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{221}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 222.}
Let \(S_{222}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{222}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 223.}
Let \(S_{223}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{223}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 224.}
Let \(S_{224}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{224}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 225.}
Let \(S_{225}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{225}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 226.}
Let \(S_{226}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{226}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 227.}
Let \(S_{227}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{227}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 228.}
Let \(S_{228}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{228}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 229.}
Let \(S_{229}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{229}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 230.}
Let \(S_{230}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{230}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 231.}
Let \(S_{231}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{231}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 232.}
Let \(S_{232}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{232}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 233.}
Let \(S_{233}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{233}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 234.}
Let \(S_{234}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{234}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 235.}
Let \(S_{235}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{235}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 236.}
Let \(S_{236}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{236}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 237.}
Let \(S_{237}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{237}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 238.}
Let \(S_{238}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{238}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 239.}
Let \(S_{239}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{239}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 240.}
Let \(S_{240}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{240}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 241.}
Let \(S_{241}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{241}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 242.}
Let \(S_{242}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{242}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 243.}
Let \(S_{243}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{243}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 244.}
Let \(S_{244}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{244}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 245.}
Let \(S_{245}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{245}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 246.}
Let \(S_{246}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{246}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 247.}
Let \(S_{247}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{247}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 248.}
Let \(S_{248}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{248}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 249.}
Let \(S_{249}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{249}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 250.}
Let \(S_{250}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{250}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 251.}
Let \(S_{251}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{251}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 252.}
Let \(S_{252}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{252}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 253.}
Let \(S_{253}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{253}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 254.}
Let \(S_{254}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{254}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 255.}
Let \(S_{255}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{255}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 256.}
Let \(S_{256}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{256}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 257.}
Let \(S_{257}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{257}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 258.}
Let \(S_{258}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{258}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 259.}
Let \(S_{259}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{259}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 260.}
Let \(S_{260}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{260}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 261.}
Let \(S_{261}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{261}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 262.}
Let \(S_{262}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{262}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 263.}
Let \(S_{263}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{263}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 264.}
Let \(S_{264}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{264}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 265.}
Let \(S_{265}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{265}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 266.}
Let \(S_{266}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{266}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 267.}
Let \(S_{267}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{267}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 268.}
Let \(S_{268}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{268}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 269.}
Let \(S_{269}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{269}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 270.}
Let \(S_{270}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{270}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 271.}
Let \(S_{271}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{271}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 272.}
Let \(S_{272}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{272}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 273.}
Let \(S_{273}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{273}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 274.}
Let \(S_{274}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{274}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 275.}
Let \(S_{275}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{275}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 276.}
Let \(S_{276}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{276}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 277.}
Let \(S_{277}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{277}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 278.}
Let \(S_{278}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{278}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 279.}
Let \(S_{279}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{279}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 280.}
Let \(S_{280}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{280}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 281.}
Let \(S_{281}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{281}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 282.}
Let \(S_{282}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{282}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 283.}
Let \(S_{283}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{283}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 284.}
Let \(S_{284}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{284}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 285.}
Let \(S_{285}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{285}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 286.}
Let \(S_{286}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{286}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 287.}
Let \(S_{287}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{287}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 288.}
Let \(S_{288}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{288}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 289.}
Let \(S_{289}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{289}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 290.}
Let \(S_{290}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{290}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 291.}
Let \(S_{291}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{291}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 292.}
Let \(S_{292}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{292}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 293.}
Let \(S_{293}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{293}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 294.}
Let \(S_{294}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{294}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 295.}
Let \(S_{295}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{295}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 296.}
Let \(S_{296}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{296}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 297.}
Let \(S_{297}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{297}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 298.}
Let \(S_{298}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{298}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 299.}
Let \(S_{299}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{299}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 300.}
Let \(S_{300}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{300}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 301.}
Let \(S_{301}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{301}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 302.}
Let \(S_{302}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{302}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 303.}
Let \(S_{303}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{303}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 304.}
Let \(S_{304}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{304}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 305.}
Let \(S_{305}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{305}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 306.}
Let \(S_{306}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{306}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 307.}
Let \(S_{307}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{307}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 308.}
Let \(S_{308}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{308}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 309.}
Let \(S_{309}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{309}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 310.}
Let \(S_{310}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{310}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 311.}
Let \(S_{311}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{311}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 312.}
Let \(S_{312}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{312}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 313.}
Let \(S_{313}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{313}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 314.}
Let \(S_{314}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{314}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 315.}
Let \(S_{315}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{315}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 316.}
Let \(S_{316}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{316}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 317.}
Let \(S_{317}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{317}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 318.}
Let \(S_{318}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{318}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 319.}
Let \(S_{319}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{319}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 320.}
Let \(S_{320}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{320}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 321.}
Let \(S_{321}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{321}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 322.}
Let \(S_{322}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{322}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 323.}
Let \(S_{323}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{323}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 324.}
Let \(S_{324}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{324}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 325.}
Let \(S_{325}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{325}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 326.}
Let \(S_{326}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{326}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 327.}
Let \(S_{327}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{327}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 328.}
Let \(S_{328}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{328}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 329.}
Let \(S_{329}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{329}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 330.}
Let \(S_{330}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{330}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 331.}
Let \(S_{331}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{331}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 332.}
Let \(S_{332}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{332}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 333.}
Let \(S_{333}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{333}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 334.}
Let \(S_{334}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{334}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 335.}
Let \(S_{335}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{335}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 336.}
Let \(S_{336}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{336}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 337.}
Let \(S_{337}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{337}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 338.}
Let \(S_{338}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{338}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 339.}
Let \(S_{339}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{339}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 340.}
Let \(S_{340}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{340}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 341.}
Let \(S_{341}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{341}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 342.}
Let \(S_{342}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{342}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 343.}
Let \(S_{343}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{343}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 344.}
Let \(S_{344}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{344}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 345.}
Let \(S_{345}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{345}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 346.}
Let \(S_{346}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{346}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 347.}
Let \(S_{347}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{347}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 348.}
Let \(S_{348}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{348}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 349.}
Let \(S_{349}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{349}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 350.}
Let \(S_{350}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{350}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 351.}
Let \(S_{351}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{351}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 352.}
Let \(S_{352}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{352}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 353.}
Let \(S_{353}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{353}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 354.}
Let \(S_{354}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{354}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 355.}
Let \(S_{355}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{355}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 356.}
Let \(S_{356}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{356}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 357.}
Let \(S_{357}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{357}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 358.}
Let \(S_{358}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{358}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 359.}
Let \(S_{359}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{359}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 360.}
Let \(S_{360}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{360}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 361.}
Let \(S_{361}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{361}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 362.}
Let \(S_{362}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{362}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 363.}
Let \(S_{363}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{363}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 364.}
Let \(S_{364}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{364}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 365.}
Let \(S_{365}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{365}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 366.}
Let \(S_{366}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{366}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 367.}
Let \(S_{367}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{367}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 368.}
Let \(S_{368}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{368}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 369.}
Let \(S_{369}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{369}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 370.}
Let \(S_{370}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{370}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 371.}
Let \(S_{371}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{371}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 372.}
Let \(S_{372}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{372}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 373.}
Let \(S_{373}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{373}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 374.}
Let \(S_{374}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{374}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 375.}
Let \(S_{375}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{375}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 376.}
Let \(S_{376}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{376}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 377.}
Let \(S_{377}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{377}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 378.}
Let \(S_{378}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{378}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 379.}
Let \(S_{379}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{379}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 380.}
Let \(S_{380}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{380}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 381.}
Let \(S_{381}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{381}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 382.}
Let \(S_{382}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{382}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 383.}
Let \(S_{383}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{383}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 384.}
Let \(S_{384}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{384}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 385.}
Let \(S_{385}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{385}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 386.}
Let \(S_{386}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{386}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 387.}
Let \(S_{387}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{387}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 388.}
Let \(S_{388}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{388}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 389.}
Let \(S_{389}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{389}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 390.}
Let \(S_{390}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{390}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 391.}
Let \(S_{391}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{391}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 392.}
Let \(S_{392}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{392}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 393.}
Let \(S_{393}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{393}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 394.}
Let \(S_{394}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{394}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 395.}
Let \(S_{395}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{395}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 396.}
Let \(S_{396}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{396}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 397.}
Let \(S_{397}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{397}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 398.}
Let \(S_{398}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{398}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 399.}
Let \(S_{399}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{399}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 400.}
Let \(S_{400}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{400}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 401.}
Let \(S_{401}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{401}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 402.}
Let \(S_{402}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{402}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 403.}
Let \(S_{403}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{403}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 404.}
Let \(S_{404}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{404}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 405.}
Let \(S_{405}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{405}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 406.}
Let \(S_{406}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{406}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 407.}
Let \(S_{407}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{407}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 408.}
Let \(S_{408}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{408}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 409.}
Let \(S_{409}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{409}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 410.}
Let \(S_{410}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{410}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 411.}
Let \(S_{411}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{411}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 412.}
Let \(S_{412}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{412}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 413.}
Let \(S_{413}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{413}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 414.}
Let \(S_{414}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{414}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 415.}
Let \(S_{415}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{415}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 416.}
Let \(S_{416}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{416}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 417.}
Let \(S_{417}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{417}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 418.}
Let \(S_{418}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{418}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 419.}
Let \(S_{419}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{419}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 420.}
Let \(S_{420}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{420}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 421.}
Let \(S_{421}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{421}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 422.}
Let \(S_{422}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{422}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 423.}
Let \(S_{423}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{423}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 424.}
Let \(S_{424}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{424}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 425.}
Let \(S_{425}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{425}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 426.}
Let \(S_{426}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{426}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 427.}
Let \(S_{427}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{427}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 428.}
Let \(S_{428}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{428}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 429.}
Let \(S_{429}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{429}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 430.}
Let \(S_{430}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{430}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 431.}
Let \(S_{431}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{431}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 432.}
Let \(S_{432}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{432}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 433.}
Let \(S_{433}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{433}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 434.}
Let \(S_{434}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{434}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 435.}
Let \(S_{435}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{435}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 436.}
Let \(S_{436}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{436}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 437.}
Let \(S_{437}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{437}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 438.}
Let \(S_{438}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{438}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 439.}
Let \(S_{439}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{439}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 440.}
Let \(S_{440}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{440}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 441.}
Let \(S_{441}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{441}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 442.}
Let \(S_{442}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{442}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 443.}
Let \(S_{443}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{443}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 444.}
Let \(S_{444}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{444}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 445.}
Let \(S_{445}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{445}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 446.}
Let \(S_{446}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{446}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 447.}
Let \(S_{447}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{447}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 448.}
Let \(S_{448}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{448}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 449.}
Let \(S_{449}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{449}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 450.}
Let \(S_{450}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{450}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 451.}
Let \(S_{451}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{451}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 452.}
Let \(S_{452}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{452}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 453.}
Let \(S_{453}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{453}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 454.}
Let \(S_{454}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{454}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 455.}
Let \(S_{455}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{455}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 456.}
Let \(S_{456}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{456}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 457.}
Let \(S_{457}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{457}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 458.}
Let \(S_{458}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{458}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 459.}
Let \(S_{459}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{459}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 460.}
Let \(S_{460}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{460}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 461.}
Let \(S_{461}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{461}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 462.}
Let \(S_{462}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{462}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 463.}
Let \(S_{463}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{463}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 464.}
Let \(S_{464}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{464}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 465.}
Let \(S_{465}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{465}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 466.}
Let \(S_{466}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{466}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 467.}
Let \(S_{467}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{467}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 468.}
Let \(S_{468}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{468}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 469.}
Let \(S_{469}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{469}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 470.}
Let \(S_{470}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{470}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 471.}
Let \(S_{471}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{471}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 472.}
Let \(S_{472}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{472}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 473.}
Let \(S_{473}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{473}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 474.}
Let \(S_{474}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{474}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 475.}
Let \(S_{475}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{475}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 476.}
Let \(S_{476}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{476}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 477.}
Let \(S_{477}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{477}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 478.}
Let \(S_{478}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{478}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 479.}
Let \(S_{479}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{479}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 480.}
Let \(S_{480}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{480}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 481.}
Let \(S_{481}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{481}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 482.}
Let \(S_{482}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{482}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 483.}
Let \(S_{483}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{483}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 484.}
Let \(S_{484}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{484}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 485.}
Let \(S_{485}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{485}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 486.}
Let \(S_{486}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{486}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 487.}
Let \(S_{487}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{487}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 488.}
Let \(S_{488}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{488}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 489.}
Let \(S_{489}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{489}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 490.}
Let \(S_{490}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{490}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 491.}
Let \(S_{491}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{491}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 492.}
Let \(S_{492}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{492}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 493.}
Let \(S_{493}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{493}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 494.}
Let \(S_{494}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{494}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 495.}
Let \(S_{495}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{495}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 496.}
Let \(S_{496}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{496}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 497.}
Let \(S_{497}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{497}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 498.}
Let \(S_{498}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{498}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 499.}
Let \(S_{499}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{499}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 500.}
Let \(S_{500}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{500}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 501.}
Let \(S_{501}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{501}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 502.}
Let \(S_{502}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{502}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 503.}
Let \(S_{503}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{503}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 504.}
Let \(S_{504}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{504}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 505.}
Let \(S_{505}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{505}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 506.}
Let \(S_{506}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{506}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 507.}
Let \(S_{507}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{507}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 508.}
Let \(S_{508}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{508}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 509.}
Let \(S_{509}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{509}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 510.}
Let \(S_{510}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{510}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 511.}
Let \(S_{511}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{511}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 512.}
Let \(S_{512}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{512}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 513.}
Let \(S_{513}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{513}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 514.}
Let \(S_{514}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{514}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 515.}
Let \(S_{515}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{515}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 516.}
Let \(S_{516}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{516}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 517.}
Let \(S_{517}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{517}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 518.}
Let \(S_{518}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{518}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 519.}
Let \(S_{519}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{519}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 520.}
Let \(S_{520}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{520}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 521.}
Let \(S_{521}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{521}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 522.}
Let \(S_{522}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{522}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 523.}
Let \(S_{523}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{523}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 524.}
Let \(S_{524}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{524}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 525.}
Let \(S_{525}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{525}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 526.}
Let \(S_{526}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{526}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 527.}
Let \(S_{527}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{527}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 528.}
Let \(S_{528}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{528}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 529.}
Let \(S_{529}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{529}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 530.}
Let \(S_{530}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{530}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 531.}
Let \(S_{531}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{531}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 532.}
Let \(S_{532}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{532}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 533.}
Let \(S_{533}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{533}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 534.}
Let \(S_{534}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{534}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 535.}
Let \(S_{535}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{535}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 536.}
Let \(S_{536}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{536}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 537.}
Let \(S_{537}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{537}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 538.}
Let \(S_{538}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{538}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 539.}
Let \(S_{539}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{539}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 540.}
Let \(S_{540}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{540}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 541.}
Let \(S_{541}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{541}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 542.}
Let \(S_{542}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{542}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 543.}
Let \(S_{543}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{543}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 544.}
Let \(S_{544}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{544}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 545.}
Let \(S_{545}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{545}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 546.}
Let \(S_{546}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{546}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 547.}
Let \(S_{547}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{547}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 548.}
Let \(S_{548}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{548}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 549.}
Let \(S_{549}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{549}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 550.}
Let \(S_{550}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{550}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 551.}
Let \(S_{551}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{551}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 552.}
Let \(S_{552}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{552}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 553.}
Let \(S_{553}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{553}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 554.}
Let \(S_{554}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{554}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 555.}
Let \(S_{555}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{555}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 556.}
Let \(S_{556}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{556}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 557.}
Let \(S_{557}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{557}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 558.}
Let \(S_{558}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{558}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 559.}
Let \(S_{559}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{559}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 560.}
Let \(S_{560}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{560}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 561.}
Let \(S_{561}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{561}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 562.}
Let \(S_{562}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{562}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 563.}
Let \(S_{563}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{563}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 564.}
Let \(S_{564}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{564}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 565.}
Let \(S_{565}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{565}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 566.}
Let \(S_{566}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{566}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 567.}
Let \(S_{567}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{567}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 568.}
Let \(S_{568}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{568}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 569.}
Let \(S_{569}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{569}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 570.}
Let \(S_{570}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{570}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 571.}
Let \(S_{571}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{571}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 572.}
Let \(S_{572}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{572}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 573.}
Let \(S_{573}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{573}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 574.}
Let \(S_{574}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{574}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 575.}
Let \(S_{575}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{575}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 576.}
Let \(S_{576}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{576}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 577.}
Let \(S_{577}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{577}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 578.}
Let \(S_{578}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{578}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 579.}
Let \(S_{579}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{579}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 580.}
Let \(S_{580}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{580}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 581.}
Let \(S_{581}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{581}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 582.}
Let \(S_{582}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{582}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 583.}
Let \(S_{583}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{583}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 584.}
Let \(S_{584}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{584}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 585.}
Let \(S_{585}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{585}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 586.}
Let \(S_{586}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{586}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 587.}
Let \(S_{587}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{587}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 588.}
Let \(S_{588}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{588}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 589.}
Let \(S_{589}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{589}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 590.}
Let \(S_{590}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{590}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 591.}
Let \(S_{591}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{591}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 592.}
Let \(S_{592}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{592}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 593.}
Let \(S_{593}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{593}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 594.}
Let \(S_{594}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{594}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 595.}
Let \(S_{595}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{595}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 596.}
Let \(S_{596}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{596}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 597.}
Let \(S_{597}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{597}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 598.}
Let \(S_{598}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{598}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 599.}
Let \(S_{599}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{599}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 600.}
Let \(S_{600}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{600}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 601.}
Let \(S_{601}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{601}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 602.}
Let \(S_{602}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{602}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 603.}
Let \(S_{603}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{603}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 604.}
Let \(S_{604}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{604}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 605.}
Let \(S_{605}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{605}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 606.}
Let \(S_{606}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{606}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 607.}
Let \(S_{607}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{607}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 608.}
Let \(S_{608}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{608}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 609.}
Let \(S_{609}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{609}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 610.}
Let \(S_{610}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{610}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 611.}
Let \(S_{611}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{611}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 612.}
Let \(S_{612}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{612}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 613.}
Let \(S_{613}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{613}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 614.}
Let \(S_{614}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{614}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 615.}
Let \(S_{615}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{615}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 616.}
Let \(S_{616}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{616}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 617.}
Let \(S_{617}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{617}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 618.}
Let \(S_{618}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{618}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 619.}
Let \(S_{619}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{619}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 620.}
Let \(S_{620}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{620}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 621.}
Let \(S_{621}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{621}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 622.}
Let \(S_{622}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{622}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 623.}
Let \(S_{623}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{623}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 624.}
Let \(S_{624}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{624}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 625.}
Let \(S_{625}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{625}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 626.}
Let \(S_{626}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{626}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 627.}
Let \(S_{627}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{627}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 628.}
Let \(S_{628}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{628}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 629.}
Let \(S_{629}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{629}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 630.}
Let \(S_{630}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{630}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 631.}
Let \(S_{631}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{631}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 632.}
Let \(S_{632}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{632}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 633.}
Let \(S_{633}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{633}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 634.}
Let \(S_{634}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{634}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 635.}
Let \(S_{635}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{635}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 636.}
Let \(S_{636}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{636}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 637.}
Let \(S_{637}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{637}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 638.}
Let \(S_{638}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{638}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 639.}
Let \(S_{639}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{639}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 640.}
Let \(S_{640}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{640}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 641.}
Let \(S_{641}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{641}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 642.}
Let \(S_{642}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{642}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 643.}
Let \(S_{643}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{643}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 644.}
Let \(S_{644}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{644}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 645.}
Let \(S_{645}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{645}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 646.}
Let \(S_{646}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{646}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 647.}
Let \(S_{647}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{647}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 648.}
Let \(S_{648}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{648}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 649.}
Let \(S_{649}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{649}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 650.}
Let \(S_{650}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{650}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 651.}
Let \(S_{651}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{651}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 652.}
Let \(S_{652}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{652}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 653.}
Let \(S_{653}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{653}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 654.}
Let \(S_{654}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{654}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 655.}
Let \(S_{655}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{655}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 656.}
Let \(S_{656}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{656}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 657.}
Let \(S_{657}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{657}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 658.}
Let \(S_{658}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{658}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 659.}
Let \(S_{659}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{659}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 660.}
Let \(S_{660}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{660}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 661.}
Let \(S_{661}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{661}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 662.}
Let \(S_{662}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{662}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 663.}
Let \(S_{663}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{663}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 664.}
Let \(S_{664}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{664}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 665.}
Let \(S_{665}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{665}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 666.}
Let \(S_{666}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{666}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 667.}
Let \(S_{667}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{667}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 668.}
Let \(S_{668}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{668}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 669.}
Let \(S_{669}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{669}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 670.}
Let \(S_{670}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{670}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 671.}
Let \(S_{671}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{671}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 672.}
Let \(S_{672}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{672}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 673.}
Let \(S_{673}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{673}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 674.}
Let \(S_{674}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{674}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 675.}
Let \(S_{675}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{675}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 676.}
Let \(S_{676}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{676}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 677.}
Let \(S_{677}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{677}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 678.}
Let \(S_{678}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{678}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 679.}
Let \(S_{679}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{679}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 680.}
Let \(S_{680}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{680}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 681.}
Let \(S_{681}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{681}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 682.}
Let \(S_{682}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{682}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 683.}
Let \(S_{683}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{683}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 684.}
Let \(S_{684}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{684}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 685.}
Let \(S_{685}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{685}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 686.}
Let \(S_{686}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{686}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 687.}
Let \(S_{687}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{687}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 688.}
Let \(S_{688}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{688}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 689.}
Let \(S_{689}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{689}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 690.}
Let \(S_{690}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{690}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 691.}
Let \(S_{691}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{691}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 692.}
Let \(S_{692}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{692}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 693.}
Let \(S_{693}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{693}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 694.}
Let \(S_{694}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{694}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 695.}
Let \(S_{695}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{695}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 696.}
Let \(S_{696}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{696}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 697.}
Let \(S_{697}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{697}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 698.}
Let \(S_{698}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{698}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 699.}
Let \(S_{699}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{699}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 700.}
Let \(S_{700}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{700}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 701.}
Let \(S_{701}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{701}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 702.}
Let \(S_{702}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{702}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 703.}
Let \(S_{703}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{703}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 704.}
Let \(S_{704}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{704}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 705.}
Let \(S_{705}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{705}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 706.}
Let \(S_{706}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{706}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 707.}
Let \(S_{707}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{707}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 708.}
Let \(S_{708}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{708}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 709.}
Let \(S_{709}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{709}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 710.}
Let \(S_{710}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{710}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 711.}
Let \(S_{711}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{711}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 712.}
Let \(S_{712}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{712}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 713.}
Let \(S_{713}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{713}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 714.}
Let \(S_{714}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{714}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 715.}
Let \(S_{715}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{715}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 716.}
Let \(S_{716}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{716}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 717.}
Let \(S_{717}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{717}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 718.}
Let \(S_{718}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{718}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 719.}
Let \(S_{719}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{719}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 720.}
Let \(S_{720}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{720}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 721.}
Let \(S_{721}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{721}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 722.}
Let \(S_{722}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{722}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 723.}
Let \(S_{723}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{723}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 724.}
Let \(S_{724}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{724}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 725.}
Let \(S_{725}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{725}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 726.}
Let \(S_{726}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{726}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 727.}
Let \(S_{727}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{727}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 728.}
Let \(S_{728}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{728}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 729.}
Let \(S_{729}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{729}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 730.}
Let \(S_{730}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{730}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 731.}
Let \(S_{731}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{731}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 732.}
Let \(S_{732}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{732}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 733.}
Let \(S_{733}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{733}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 734.}
Let \(S_{734}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{734}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 735.}
Let \(S_{735}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{735}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 736.}
Let \(S_{736}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{736}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 737.}
Let \(S_{737}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{737}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 738.}
Let \(S_{738}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{738}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 739.}
Let \(S_{739}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{739}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 740.}
Let \(S_{740}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{740}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 741.}
Let \(S_{741}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{741}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 742.}
Let \(S_{742}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{742}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 743.}
Let \(S_{743}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{743}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 744.}
Let \(S_{744}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{744}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 745.}
Let \(S_{745}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{745}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 746.}
Let \(S_{746}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{746}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 747.}
Let \(S_{747}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{747}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 748.}
Let \(S_{748}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{748}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 749.}
Let \(S_{749}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{749}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 750.}
Let \(S_{750}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{750}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 751.}
Let \(S_{751}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{751}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 752.}
Let \(S_{752}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{752}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 753.}
Let \(S_{753}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{753}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 754.}
Let \(S_{754}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{754}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 755.}
Let \(S_{755}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{755}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 756.}
Let \(S_{756}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{756}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 757.}
Let \(S_{757}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{757}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 758.}
Let \(S_{758}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{758}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 759.}
Let \(S_{759}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{759}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 760.}
Let \(S_{760}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{760}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 761.}
Let \(S_{761}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{761}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 762.}
Let \(S_{762}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{762}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 763.}
Let \(S_{763}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{763}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 764.}
Let \(S_{764}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{764}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 765.}
Let \(S_{765}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{765}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 766.}
Let \(S_{766}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{766}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 767.}
Let \(S_{767}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{767}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 768.}
Let \(S_{768}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{768}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 769.}
Let \(S_{769}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{769}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 770.}
Let \(S_{770}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{770}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 771.}
Let \(S_{771}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{771}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 772.}
Let \(S_{772}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{772}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 773.}
Let \(S_{773}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{773}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 774.}
Let \(S_{774}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{774}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 775.}
Let \(S_{775}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{775}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 776.}
Let \(S_{776}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{776}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 777.}
Let \(S_{777}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{777}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 778.}
Let \(S_{778}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{778}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 779.}
Let \(S_{779}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{779}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 780.}
Let \(S_{780}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{780}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 781.}
Let \(S_{781}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{781}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 782.}
Let \(S_{782}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{782}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 783.}
Let \(S_{783}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{783}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 784.}
Let \(S_{784}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{784}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 785.}
Let \(S_{785}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{785}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 786.}
Let \(S_{786}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{786}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 787.}
Let \(S_{787}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{787}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 788.}
Let \(S_{788}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{788}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 789.}
Let \(S_{789}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{789}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 790.}
Let \(S_{790}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{790}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 791.}
Let \(S_{791}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{791}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 792.}
Let \(S_{792}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{792}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 793.}
Let \(S_{793}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{793}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 794.}
Let \(S_{794}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{794}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 795.}
Let \(S_{795}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{795}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 796.}
Let \(S_{796}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{796}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 797.}
Let \(S_{797}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{797}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 798.}
Let \(S_{798}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{798}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 799.}
Let \(S_{799}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{799}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 800.}
Let \(S_{800}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{800}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 801.}
Let \(S_{801}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{801}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 802.}
Let \(S_{802}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{802}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 803.}
Let \(S_{803}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{803}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 804.}
Let \(S_{804}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{804}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 805.}
Let \(S_{805}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{805}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 806.}
Let \(S_{806}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{806}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 807.}
Let \(S_{807}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{807}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 808.}
Let \(S_{808}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{808}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 809.}
Let \(S_{809}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{809}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 810.}
Let \(S_{810}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{810}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 811.}
Let \(S_{811}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{811}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 812.}
Let \(S_{812}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{812}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 813.}
Let \(S_{813}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{813}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 814.}
Let \(S_{814}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{814}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 815.}
Let \(S_{815}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{815}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 816.}
Let \(S_{816}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{816}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 817.}
Let \(S_{817}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{817}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 818.}
Let \(S_{818}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{818}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 819.}
Let \(S_{819}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{819}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 820.}
Let \(S_{820}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{820}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 821.}
Let \(S_{821}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{821}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 822.}
Let \(S_{822}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{822}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 823.}
Let \(S_{823}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{823}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 824.}
Let \(S_{824}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{824}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 825.}
Let \(S_{825}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{825}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 826.}
Let \(S_{826}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{826}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 827.}
Let \(S_{827}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{827}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 828.}
Let \(S_{828}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{828}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 829.}
Let \(S_{829}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{829}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 830.}
Let \(S_{830}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{830}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 831.}
Let \(S_{831}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{831}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 832.}
Let \(S_{832}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{832}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 833.}
Let \(S_{833}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{833}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 834.}
Let \(S_{834}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{834}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 835.}
Let \(S_{835}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{835}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 836.}
Let \(S_{836}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{836}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 837.}
Let \(S_{837}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{837}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 838.}
Let \(S_{838}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{838}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 839.}
Let \(S_{839}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{839}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 840.}
Let \(S_{840}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{840}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 841.}
Let \(S_{841}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{841}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 842.}
Let \(S_{842}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{842}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 843.}
Let \(S_{843}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{843}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 844.}
Let \(S_{844}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{844}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 845.}
Let \(S_{845}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{845}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 846.}
Let \(S_{846}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{846}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 847.}
Let \(S_{847}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{847}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 848.}
Let \(S_{848}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{848}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 849.}
Let \(S_{849}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{849}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 850.}
Let \(S_{850}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{850}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 851.}
Let \(S_{851}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{851}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 852.}
Let \(S_{852}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{852}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 853.}
Let \(S_{853}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{853}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 854.}
Let \(S_{854}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{854}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 855.}
Let \(S_{855}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{855}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 856.}
Let \(S_{856}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{856}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 857.}
Let \(S_{857}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{857}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 858.}
Let \(S_{858}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{858}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 859.}
Let \(S_{859}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{859}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 860.}
Let \(S_{860}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{860}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 861.}
Let \(S_{861}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{861}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 862.}
Let \(S_{862}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{862}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 863.}
Let \(S_{863}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{863}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 864.}
Let \(S_{864}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{864}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 865.}
Let \(S_{865}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{865}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 866.}
Let \(S_{866}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{866}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 867.}
Let \(S_{867}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{867}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 868.}
Let \(S_{868}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{868}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 869.}
Let \(S_{869}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{869}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 870.}
Let \(S_{870}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{870}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 871.}
Let \(S_{871}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{871}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 872.}
Let \(S_{872}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{872}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 873.}
Let \(S_{873}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{873}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 874.}
Let \(S_{874}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{874}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 875.}
Let \(S_{875}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{875}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 876.}
Let \(S_{876}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{876}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 877.}
Let \(S_{877}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{877}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 878.}
Let \(S_{878}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{878}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 879.}
Let \(S_{879}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{879}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 880.}
Let \(S_{880}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{880}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 881.}
Let \(S_{881}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{881}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 882.}
Let \(S_{882}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{882}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 883.}
Let \(S_{883}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{883}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 884.}
Let \(S_{884}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{884}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 885.}
Let \(S_{885}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{885}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 886.}
Let \(S_{886}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{886}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 887.}
Let \(S_{887}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{887}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 888.}
Let \(S_{888}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{888}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 889.}
Let \(S_{889}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{889}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 890.}
Let \(S_{890}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{890}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 891.}
Let \(S_{891}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{891}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 892.}
Let \(S_{892}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{892}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 893.}
Let \(S_{893}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{893}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 894.}
Let \(S_{894}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{894}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 895.}
Let \(S_{895}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{895}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 896.}
Let \(S_{896}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{896}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 897.}
Let \(S_{897}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{897}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 898.}
Let \(S_{898}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{898}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 899.}
Let \(S_{899}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{899}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 900.}
Let \(S_{900}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{900}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 901.}
Let \(S_{901}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{901}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 902.}
Let \(S_{902}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{902}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 903.}
Let \(S_{903}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{903}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 904.}
Let \(S_{904}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{904}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 905.}
Let \(S_{905}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{905}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 906.}
Let \(S_{906}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{906}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 907.}
Let \(S_{907}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{907}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 908.}
Let \(S_{908}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{908}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 909.}
Let \(S_{909}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{909}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 910.}
Let \(S_{910}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{910}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 911.}
Let \(S_{911}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{911}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 912.}
Let \(S_{912}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{912}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 913.}
Let \(S_{913}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{913}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 914.}
Let \(S_{914}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{914}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 915.}
Let \(S_{915}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{915}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 916.}
Let \(S_{916}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{916}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 917.}
Let \(S_{917}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{917}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 918.}
Let \(S_{918}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{918}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 919.}
Let \(S_{919}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{919}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 920.}
Let \(S_{920}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{920}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 921.}
Let \(S_{921}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{921}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 922.}
Let \(S_{922}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{922}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 923.}
Let \(S_{923}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{923}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 924.}
Let \(S_{924}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{924}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 925.}
Let \(S_{925}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{925}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 926.}
Let \(S_{926}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{926}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 927.}
Let \(S_{927}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{927}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 928.}
Let \(S_{928}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{928}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 929.}
Let \(S_{929}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{929}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 930.}
Let \(S_{930}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{930}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 931.}
Let \(S_{931}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{931}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 932.}
Let \(S_{932}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{932}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 933.}
Let \(S_{933}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{933}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 934.}
Let \(S_{934}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{934}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 935.}
Let \(S_{935}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{935}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 936.}
Let \(S_{936}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{936}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 937.}
Let \(S_{937}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{937}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 938.}
Let \(S_{938}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{938}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 939.}
Let \(S_{939}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{939}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 940.}
Let \(S_{940}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{940}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 941.}
Let \(S_{941}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{941}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 942.}
Let \(S_{942}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{942}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 943.}
Let \(S_{943}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{943}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 944.}
Let \(S_{944}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{944}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 945.}
Let \(S_{945}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{945}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 946.}
Let \(S_{946}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{946}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 947.}
Let \(S_{947}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{947}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 948.}
Let \(S_{948}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{948}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 949.}
Let \(S_{949}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{949}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 950.}
Let \(S_{950}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{950}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 951.}
Let \(S_{951}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{951}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 952.}
Let \(S_{952}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{952}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 953.}
Let \(S_{953}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{953}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 954.}
Let \(S_{954}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{954}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 955.}
Let \(S_{955}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{955}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 956.}
Let \(S_{956}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{956}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 957.}
Let \(S_{957}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{957}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 958.}
Let \(S_{958}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{958}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 959.}
Let \(S_{959}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{959}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 960.}
Let \(S_{960}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{960}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 961.}
Let \(S_{961}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{961}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 962.}
Let \(S_{962}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{962}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 963.}
Let \(S_{963}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{963}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 964.}
Let \(S_{964}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{964}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 965.}
Let \(S_{965}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{965}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 966.}
Let \(S_{966}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{966}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 967.}
Let \(S_{967}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{967}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 968.}
Let \(S_{968}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{968}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 969.}
Let \(S_{969}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{969}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 970.}
Let \(S_{970}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{970}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 971.}
Let \(S_{971}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{971}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 972.}
Let \(S_{972}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{972}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 973.}
Let \(S_{973}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{973}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 974.}
Let \(S_{974}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{974}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 975.}
Let \(S_{975}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{975}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 976.}
Let \(S_{976}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{976}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 977.}
Let \(S_{977}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{977}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 978.}
Let \(S_{978}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{978}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 979.}
Let \(S_{979}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{979}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 980.}
Let \(S_{980}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{980}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 981.}
Let \(S_{981}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{981}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 982.}
Let \(S_{982}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{982}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 983.}
Let \(S_{983}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{983}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 984.}
Let \(S_{984}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{984}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 985.}
Let \(S_{985}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{985}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 986.}
Let \(S_{986}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{986}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 987.}
Let \(S_{987}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{987}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 988.}
Let \(S_{988}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{988}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 989.}
Let \(S_{989}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{989}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 990.}
Let \(S_{990}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{990}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 991.}
Let \(S_{991}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{991}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 992.}
Let \(S_{992}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{992}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 993.}
Let \(S_{993}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{993}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 994.}
Let \(S_{994}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{994}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 995.}
Let \(S_{995}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{995}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 996.}
Let \(S_{996}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{996}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 997.}
Let \(S_{997}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{997}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 998.}
Let \(S_{998}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{998}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 999.}
Let \(S_{999}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{999}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1000.}
Let \(S_{1000}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1000}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1001.}
Let \(S_{1001}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1001}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1002.}
Let \(S_{1002}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1002}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1003.}
Let \(S_{1003}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1003}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1004.}
Let \(S_{1004}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1004}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1005.}
Let \(S_{1005}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1005}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1006.}
Let \(S_{1006}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1006}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1007.}
Let \(S_{1007}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1007}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1008.}
Let \(S_{1008}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1008}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1009.}
Let \(S_{1009}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1009}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1010.}
Let \(S_{1010}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1010}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1011.}
Let \(S_{1011}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1011}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1012.}
Let \(S_{1012}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1012}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1013.}
Let \(S_{1013}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1013}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1014.}
Let \(S_{1014}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1014}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1015.}
Let \(S_{1015}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1015}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1016.}
Let \(S_{1016}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1016}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1017.}
Let \(S_{1017}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1017}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1018.}
Let \(S_{1018}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1018}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1019.}
Let \(S_{1019}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1019}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1020.}
Let \(S_{1020}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1020}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1021.}
Let \(S_{1021}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1021}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1022.}
Let \(S_{1022}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1022}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1023.}
Let \(S_{1023}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1023}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1024.}
Let \(S_{1024}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1024}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1025.}
Let \(S_{1025}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1025}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1026.}
Let \(S_{1026}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1026}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1027.}
Let \(S_{1027}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1027}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1028.}
Let \(S_{1028}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1028}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1029.}
Let \(S_{1029}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1029}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1030.}
Let \(S_{1030}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1030}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1031.}
Let \(S_{1031}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1031}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1032.}
Let \(S_{1032}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1032}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1033.}
Let \(S_{1033}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1033}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1034.}
Let \(S_{1034}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1034}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1035.}
Let \(S_{1035}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1035}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1036.}
Let \(S_{1036}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1036}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1037.}
Let \(S_{1037}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1037}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1038.}
Let \(S_{1038}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1038}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1039.}
Let \(S_{1039}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1039}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1040.}
Let \(S_{1040}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1040}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1041.}
Let \(S_{1041}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1041}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1042.}
Let \(S_{1042}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1042}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1043.}
Let \(S_{1043}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1043}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1044.}
Let \(S_{1044}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1044}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1045.}
Let \(S_{1045}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1045}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1046.}
Let \(S_{1046}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1046}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1047.}
Let \(S_{1047}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1047}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1048.}
Let \(S_{1048}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1048}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1049.}
Let \(S_{1049}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1049}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1050.}
Let \(S_{1050}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1050}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1051.}
Let \(S_{1051}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1051}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1052.}
Let \(S_{1052}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1052}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1053.}
Let \(S_{1053}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1053}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1054.}
Let \(S_{1054}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1054}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1055.}
Let \(S_{1055}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1055}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1056.}
Let \(S_{1056}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1056}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1057.}
Let \(S_{1057}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1057}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1058.}
Let \(S_{1058}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1058}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1059.}
Let \(S_{1059}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1059}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1060.}
Let \(S_{1060}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1060}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1061.}
Let \(S_{1061}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1061}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1062.}
Let \(S_{1062}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1062}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1063.}
Let \(S_{1063}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1063}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1064.}
Let \(S_{1064}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1064}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1065.}
Let \(S_{1065}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1065}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1066.}
Let \(S_{1066}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1066}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1067.}
Let \(S_{1067}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1067}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1068.}
Let \(S_{1068}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1068}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1069.}
Let \(S_{1069}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1069}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1070.}
Let \(S_{1070}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1070}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1071.}
Let \(S_{1071}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1071}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1072.}
Let \(S_{1072}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1072}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1073.}
Let \(S_{1073}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1073}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1074.}
Let \(S_{1074}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1074}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1075.}
Let \(S_{1075}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1075}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1076.}
Let \(S_{1076}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1076}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1077.}
Let \(S_{1077}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1077}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1078.}
Let \(S_{1078}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1078}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1079.}
Let \(S_{1079}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1079}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1080.}
Let \(S_{1080}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1080}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1081.}
Let \(S_{1081}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1081}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1082.}
Let \(S_{1082}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1082}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1083.}
Let \(S_{1083}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1083}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1084.}
Let \(S_{1084}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1084}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1085.}
Let \(S_{1085}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1085}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1086.}
Let \(S_{1086}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1086}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1087.}
Let \(S_{1087}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1087}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1088.}
Let \(S_{1088}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1088}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1089.}
Let \(S_{1089}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1089}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1090.}
Let \(S_{1090}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1090}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1091.}
Let \(S_{1091}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1091}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1092.}
Let \(S_{1092}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1092}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1093.}
Let \(S_{1093}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1093}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1094.}
Let \(S_{1094}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1094}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1095.}
Let \(S_{1095}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1095}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1096.}
Let \(S_{1096}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1096}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1097.}
Let \(S_{1097}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1097}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1098.}
Let \(S_{1098}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1098}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1099.}
Let \(S_{1099}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1099}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1100.}
Let \(S_{1100}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1100}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1101.}
Let \(S_{1101}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1101}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1102.}
Let \(S_{1102}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1102}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1103.}
Let \(S_{1103}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1103}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1104.}
Let \(S_{1104}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1104}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1105.}
Let \(S_{1105}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1105}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1106.}
Let \(S_{1106}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1106}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1107.}
Let \(S_{1107}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1107}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1108.}
Let \(S_{1108}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1108}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1109.}
Let \(S_{1109}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1109}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1110.}
Let \(S_{1110}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1110}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1111.}
Let \(S_{1111}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1111}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1112.}
Let \(S_{1112}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1112}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1113.}
Let \(S_{1113}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1113}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1114.}
Let \(S_{1114}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1114}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1115.}
Let \(S_{1115}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1115}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1116.}
Let \(S_{1116}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1116}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1117.}
Let \(S_{1117}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1117}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1118.}
Let \(S_{1118}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1118}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1119.}
Let \(S_{1119}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1119}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1120.}
Let \(S_{1120}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1120}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1121.}
Let \(S_{1121}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1121}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1122.}
Let \(S_{1122}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1122}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1123.}
Let \(S_{1123}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1123}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1124.}
Let \(S_{1124}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1124}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1125.}
Let \(S_{1125}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1125}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1126.}
Let \(S_{1126}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1126}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1127.}
Let \(S_{1127}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1127}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1128.}
Let \(S_{1128}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1128}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1129.}
Let \(S_{1129}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1129}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1130.}
Let \(S_{1130}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1130}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1131.}
Let \(S_{1131}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1131}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1132.}
Let \(S_{1132}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1132}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1133.}
Let \(S_{1133}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1133}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1134.}
Let \(S_{1134}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1134}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1135.}
Let \(S_{1135}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1135}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1136.}
Let \(S_{1136}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1136}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1137.}
Let \(S_{1137}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1137}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1138.}
Let \(S_{1138}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1138}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1139.}
Let \(S_{1139}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1139}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1140.}
Let \(S_{1140}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1140}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1141.}
Let \(S_{1141}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1141}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1142.}
Let \(S_{1142}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1142}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1143.}
Let \(S_{1143}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1143}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1144.}
Let \(S_{1144}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1144}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1145.}
Let \(S_{1145}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1145}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1146.}
Let \(S_{1146}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1146}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1147.}
Let \(S_{1147}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1147}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1148.}
Let \(S_{1148}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1148}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1149.}
Let \(S_{1149}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1149}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1150.}
Let \(S_{1150}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1150}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1151.}
Let \(S_{1151}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1151}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1152.}
Let \(S_{1152}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1152}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1153.}
Let \(S_{1153}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1153}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1154.}
Let \(S_{1154}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1154}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1155.}
Let \(S_{1155}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1155}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1156.}
Let \(S_{1156}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1156}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1157.}
Let \(S_{1157}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1157}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1158.}
Let \(S_{1158}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1158}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1159.}
Let \(S_{1159}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1159}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1160.}
Let \(S_{1160}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1160}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1161.}
Let \(S_{1161}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1161}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1162.}
Let \(S_{1162}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1162}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1163.}
Let \(S_{1163}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1163}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1164.}
Let \(S_{1164}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1164}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1165.}
Let \(S_{1165}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1165}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1166.}
Let \(S_{1166}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1166}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1167.}
Let \(S_{1167}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1167}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1168.}
Let \(S_{1168}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1168}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1169.}
Let \(S_{1169}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1169}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1170.}
Let \(S_{1170}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1170}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1171.}
Let \(S_{1171}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1171}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1172.}
Let \(S_{1172}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1172}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1173.}
Let \(S_{1173}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1173}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1174.}
Let \(S_{1174}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1174}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1175.}
Let \(S_{1175}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1175}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1176.}
Let \(S_{1176}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1176}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1177.}
Let \(S_{1177}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1177}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1178.}
Let \(S_{1178}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1178}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1179.}
Let \(S_{1179}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1179}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1180.}
Let \(S_{1180}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1180}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1181.}
Let \(S_{1181}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1181}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1182.}
Let \(S_{1182}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1182}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1183.}
Let \(S_{1183}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1183}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1184.}
Let \(S_{1184}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1184}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1185.}
Let \(S_{1185}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1185}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1186.}
Let \(S_{1186}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1186}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1187.}
Let \(S_{1187}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1187}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1188.}
Let \(S_{1188}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1188}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1189.}
Let \(S_{1189}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1189}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1190.}
Let \(S_{1190}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1190}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1191.}
Let \(S_{1191}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1191}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1192.}
Let \(S_{1192}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1192}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1193.}
Let \(S_{1193}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1193}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1194.}
Let \(S_{1194}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1194}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1195.}
Let \(S_{1195}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1195}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1196.}
Let \(S_{1196}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1196}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1197.}
Let \(S_{1197}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1197}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1198.}
Let \(S_{1198}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1198}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1199.}
Let \(S_{1199}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1199}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1200.}
Let \(S_{1200}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1200}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1201.}
Let \(S_{1201}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1201}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1202.}
Let \(S_{1202}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1202}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1203.}
Let \(S_{1203}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1203}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1204.}
Let \(S_{1204}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1204}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1205.}
Let \(S_{1205}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1205}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1206.}
Let \(S_{1206}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1206}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1207.}
Let \(S_{1207}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1207}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1208.}
Let \(S_{1208}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1208}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1209.}
Let \(S_{1209}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1209}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1210.}
Let \(S_{1210}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1210}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1211.}
Let \(S_{1211}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1211}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1212.}
Let \(S_{1212}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1212}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1213.}
Let \(S_{1213}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1213}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1214.}
Let \(S_{1214}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1214}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1215.}
Let \(S_{1215}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1215}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1216.}
Let \(S_{1216}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1216}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1217.}
Let \(S_{1217}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1217}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1218.}
Let \(S_{1218}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1218}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1219.}
Let \(S_{1219}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1219}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1220.}
Let \(S_{1220}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1220}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1221.}
Let \(S_{1221}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1221}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1222.}
Let \(S_{1222}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1222}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1223.}
Let \(S_{1223}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1223}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1224.}
Let \(S_{1224}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1224}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1225.}
Let \(S_{1225}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1225}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1226.}
Let \(S_{1226}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1226}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1227.}
Let \(S_{1227}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1227}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1228.}
Let \(S_{1228}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1228}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1229.}
Let \(S_{1229}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1229}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1230.}
Let \(S_{1230}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1230}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1231.}
Let \(S_{1231}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1231}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1232.}
Let \(S_{1232}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1232}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1233.}
Let \(S_{1233}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1233}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1234.}
Let \(S_{1234}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1234}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1235.}
Let \(S_{1235}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1235}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1236.}
Let \(S_{1236}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1236}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1237.}
Let \(S_{1237}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1237}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1238.}
Let \(S_{1238}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1238}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1239.}
Let \(S_{1239}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1239}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1240.}
Let \(S_{1240}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1240}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1241.}
Let \(S_{1241}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1241}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1242.}
Let \(S_{1242}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1242}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1243.}
Let \(S_{1243}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1243}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1244.}
Let \(S_{1244}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1244}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1245.}
Let \(S_{1245}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1245}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1246.}
Let \(S_{1246}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1246}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1247.}
Let \(S_{1247}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1247}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1248.}
Let \(S_{1248}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1248}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1249.}
Let \(S_{1249}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1249}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1250.}
Let \(S_{1250}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1250}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1251.}
Let \(S_{1251}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1251}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1252.}
Let \(S_{1252}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1252}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1253.}
Let \(S_{1253}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1253}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1254.}
Let \(S_{1254}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1254}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1255.}
Let \(S_{1255}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1255}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1256.}
Let \(S_{1256}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1256}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1257.}
Let \(S_{1257}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1257}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1258.}
Let \(S_{1258}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1258}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1259.}
Let \(S_{1259}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1259}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1260.}
Let \(S_{1260}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1260}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1261.}
Let \(S_{1261}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1261}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1262.}
Let \(S_{1262}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1262}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1263.}
Let \(S_{1263}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1263}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1264.}
Let \(S_{1264}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1264}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1265.}
Let \(S_{1265}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1265}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1266.}
Let \(S_{1266}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1266}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1267.}
Let \(S_{1267}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1267}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1268.}
Let \(S_{1268}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1268}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1269.}
Let \(S_{1269}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1269}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1270.}
Let \(S_{1270}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1270}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1271.}
Let \(S_{1271}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1271}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1272.}
Let \(S_{1272}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1272}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1273.}
Let \(S_{1273}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1273}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1274.}
Let \(S_{1274}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1274}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1275.}
Let \(S_{1275}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1275}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1276.}
Let \(S_{1276}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1276}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1277.}
Let \(S_{1277}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1277}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1278.}
Let \(S_{1278}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1278}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1279.}
Let \(S_{1279}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1279}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1280.}
Let \(S_{1280}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1280}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1281.}
Let \(S_{1281}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1281}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1282.}
Let \(S_{1282}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1282}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1283.}
Let \(S_{1283}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1283}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1284.}
Let \(S_{1284}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1284}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1285.}
Let \(S_{1285}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1285}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1286.}
Let \(S_{1286}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1286}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1287.}
Let \(S_{1287}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1287}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1288.}
Let \(S_{1288}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1288}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1289.}
Let \(S_{1289}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1289}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1290.}
Let \(S_{1290}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1290}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1291.}
Let \(S_{1291}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1291}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1292.}
Let \(S_{1292}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1292}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1293.}
Let \(S_{1293}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1293}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1294.}
Let \(S_{1294}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1294}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1295.}
Let \(S_{1295}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1295}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1296.}
Let \(S_{1296}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1296}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1297.}
Let \(S_{1297}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1297}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1298.}
Let \(S_{1298}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1298}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1299.}
Let \(S_{1299}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1299}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1300.}
Let \(S_{1300}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1300}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1301.}
Let \(S_{1301}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1301}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1302.}
Let \(S_{1302}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1302}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1303.}
Let \(S_{1303}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1303}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1304.}
Let \(S_{1304}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1304}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1305.}
Let \(S_{1305}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1305}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1306.}
Let \(S_{1306}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1306}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1307.}
Let \(S_{1307}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1307}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1308.}
Let \(S_{1308}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1308}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1309.}
Let \(S_{1309}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1309}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1310.}
Let \(S_{1310}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1310}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1311.}
Let \(S_{1311}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1311}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1312.}
Let \(S_{1312}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1312}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1313.}
Let \(S_{1313}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1313}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1314.}
Let \(S_{1314}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1314}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1315.}
Let \(S_{1315}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1315}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1316.}
Let \(S_{1316}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1316}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1317.}
Let \(S_{1317}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1317}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1318.}
Let \(S_{1318}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1318}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1319.}
Let \(S_{1319}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1319}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1320.}
Let \(S_{1320}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1320}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1321.}
Let \(S_{1321}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1321}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1322.}
Let \(S_{1322}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1322}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1323.}
Let \(S_{1323}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1323}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1324.}
Let \(S_{1324}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1324}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1325.}
Let \(S_{1325}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1325}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1326.}
Let \(S_{1326}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1326}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1327.}
Let \(S_{1327}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1327}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1328.}
Let \(S_{1328}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1328}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1329.}
Let \(S_{1329}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1329}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1330.}
Let \(S_{1330}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1330}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1331.}
Let \(S_{1331}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1331}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1332.}
Let \(S_{1332}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1332}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1333.}
Let \(S_{1333}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1333}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1334.}
Let \(S_{1334}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1334}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1335.}
Let \(S_{1335}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1335}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1336.}
Let \(S_{1336}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1336}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1337.}
Let \(S_{1337}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1337}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1338.}
Let \(S_{1338}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1338}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1339.}
Let \(S_{1339}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1339}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1340.}
Let \(S_{1340}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1340}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1341.}
Let \(S_{1341}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1341}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1342.}
Let \(S_{1342}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1342}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1343.}
Let \(S_{1343}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1343}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1344.}
Let \(S_{1344}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1344}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1345.}
Let \(S_{1345}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1345}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1346.}
Let \(S_{1346}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1346}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1347.}
Let \(S_{1347}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1347}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1348.}
Let \(S_{1348}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1348}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1349.}
Let \(S_{1349}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1349}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1350.}
Let \(S_{1350}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1350}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1351.}
Let \(S_{1351}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1351}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1352.}
Let \(S_{1352}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1352}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1353.}
Let \(S_{1353}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1353}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1354.}
Let \(S_{1354}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1354}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1355.}
Let \(S_{1355}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1355}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1356.}
Let \(S_{1356}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1356}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1357.}
Let \(S_{1357}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1357}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1358.}
Let \(S_{1358}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1358}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1359.}
Let \(S_{1359}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1359}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1360.}
Let \(S_{1360}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1360}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1361.}
Let \(S_{1361}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1361}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1362.}
Let \(S_{1362}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1362}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1363.}
Let \(S_{1363}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1363}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1364.}
Let \(S_{1364}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1364}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1365.}
Let \(S_{1365}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1365}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1366.}
Let \(S_{1366}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1366}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1367.}
Let \(S_{1367}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1367}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1368.}
Let \(S_{1368}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1368}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1369.}
Let \(S_{1369}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1369}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1370.}
Let \(S_{1370}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1370}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1371.}
Let \(S_{1371}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1371}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1372.}
Let \(S_{1372}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1372}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1373.}
Let \(S_{1373}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1373}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1374.}
Let \(S_{1374}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1374}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1375.}
Let \(S_{1375}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1375}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1376.}
Let \(S_{1376}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1376}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1377.}
Let \(S_{1377}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1377}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1378.}
Let \(S_{1378}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1378}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1379.}
Let \(S_{1379}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1379}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1380.}
Let \(S_{1380}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1380}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1381.}
Let \(S_{1381}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1381}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1382.}
Let \(S_{1382}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1382}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1383.}
Let \(S_{1383}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1383}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1384.}
Let \(S_{1384}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1384}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1385.}
Let \(S_{1385}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1385}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1386.}
Let \(S_{1386}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1386}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1387.}
Let \(S_{1387}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1387}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1388.}
Let \(S_{1388}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1388}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1389.}
Let \(S_{1389}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1389}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1390.}
Let \(S_{1390}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1390}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1391.}
Let \(S_{1391}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1391}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1392.}
Let \(S_{1392}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1392}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1393.}
Let \(S_{1393}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1393}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1394.}
Let \(S_{1394}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1394}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1395.}
Let \(S_{1395}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1395}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1396.}
Let \(S_{1396}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1396}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1397.}
Let \(S_{1397}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1397}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1398.}
Let \(S_{1398}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1398}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1399.}
Let \(S_{1399}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1399}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1400.}
Let \(S_{1400}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1400}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1401.}
Let \(S_{1401}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1401}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1402.}
Let \(S_{1402}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1402}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1403.}
Let \(S_{1403}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1403}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1404.}
Let \(S_{1404}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1404}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1405.}
Let \(S_{1405}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1405}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1406.}
Let \(S_{1406}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1406}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1407.}
Let \(S_{1407}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1407}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1408.}
Let \(S_{1408}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1408}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1409.}
Let \(S_{1409}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1409}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1410.}
Let \(S_{1410}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1410}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1411.}
Let \(S_{1411}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1411}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1412.}
Let \(S_{1412}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1412}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1413.}
Let \(S_{1413}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1413}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1414.}
Let \(S_{1414}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1414}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1415.}
Let \(S_{1415}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1415}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1416.}
Let \(S_{1416}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1416}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1417.}
Let \(S_{1417}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1417}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1418.}
Let \(S_{1418}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1418}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1419.}
Let \(S_{1419}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1419}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1420.}
Let \(S_{1420}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1420}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1421.}
Let \(S_{1421}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1421}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1422.}
Let \(S_{1422}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1422}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1423.}
Let \(S_{1423}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1423}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1424.}
Let \(S_{1424}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1424}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1425.}
Let \(S_{1425}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1425}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1426.}
Let \(S_{1426}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1426}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1427.}
Let \(S_{1427}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1427}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1428.}
Let \(S_{1428}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1428}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1429.}
Let \(S_{1429}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1429}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1430.}
Let \(S_{1430}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1430}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1431.}
Let \(S_{1431}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1431}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1432.}
Let \(S_{1432}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1432}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1433.}
Let \(S_{1433}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1433}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1434.}
Let \(S_{1434}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1434}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1435.}
Let \(S_{1435}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1435}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1436.}
Let \(S_{1436}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1436}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1437.}
Let \(S_{1437}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1437}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1438.}
Let \(S_{1438}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1438}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1439.}
Let \(S_{1439}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1439}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1440.}
Let \(S_{1440}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1440}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1441.}
Let \(S_{1441}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1441}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1442.}
Let \(S_{1442}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1442}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1443.}
Let \(S_{1443}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1443}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1444.}
Let \(S_{1444}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1444}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1445.}
Let \(S_{1445}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1445}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1446.}
Let \(S_{1446}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1446}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1447.}
Let \(S_{1447}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1447}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1448.}
Let \(S_{1448}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1448}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1449.}
Let \(S_{1449}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1449}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1450.}
Let \(S_{1450}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1450}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1451.}
Let \(S_{1451}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1451}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1452.}
Let \(S_{1452}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1452}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1453.}
Let \(S_{1453}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1453}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1454.}
Let \(S_{1454}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1454}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1455.}
Let \(S_{1455}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1455}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1456.}
Let \(S_{1456}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1456}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1457.}
Let \(S_{1457}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1457}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1458.}
Let \(S_{1458}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1458}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1459.}
Let \(S_{1459}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1459}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1460.}
Let \(S_{1460}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1460}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1461.}
Let \(S_{1461}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1461}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1462.}
Let \(S_{1462}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1462}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1463.}
Let \(S_{1463}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1463}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1464.}
Let \(S_{1464}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1464}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1465.}
Let \(S_{1465}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1465}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1466.}
Let \(S_{1466}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1466}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1467.}
Let \(S_{1467}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1467}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1468.}
Let \(S_{1468}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1468}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1469.}
Let \(S_{1469}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1469}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1470.}
Let \(S_{1470}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1470}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1471.}
Let \(S_{1471}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1471}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1472.}
Let \(S_{1472}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1472}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1473.}
Let \(S_{1473}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1473}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1474.}
Let \(S_{1474}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1474}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1475.}
Let \(S_{1475}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1475}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1476.}
Let \(S_{1476}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1476}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1477.}
Let \(S_{1477}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1477}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1478.}
Let \(S_{1478}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1478}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1479.}
Let \(S_{1479}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1479}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1480.}
Let \(S_{1480}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1480}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1481.}
Let \(S_{1481}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1481}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1482.}
Let \(S_{1482}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1482}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1483.}
Let \(S_{1483}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1483}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1484.}
Let \(S_{1484}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1484}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1485.}
Let \(S_{1485}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1485}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1486.}
Let \(S_{1486}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1486}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1487.}
Let \(S_{1487}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1487}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1488.}
Let \(S_{1488}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1488}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1489.}
Let \(S_{1489}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1489}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1490.}
Let \(S_{1490}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1490}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1491.}
Let \(S_{1491}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1491}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1492.}
Let \(S_{1492}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1492}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1493.}
Let \(S_{1493}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1493}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1494.}
Let \(S_{1494}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1494}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1495.}
Let \(S_{1495}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1495}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1496.}
Let \(S_{1496}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1496}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1497.}
Let \(S_{1497}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1497}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1498.}
Let \(S_{1498}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1498}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1499.}
Let \(S_{1499}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1499}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1500.}
Let \(S_{1500}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1500}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1501.}
Let \(S_{1501}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1501}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1502.}
Let \(S_{1502}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1502}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1503.}
Let \(S_{1503}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1503}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1504.}
Let \(S_{1504}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1504}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1505.}
Let \(S_{1505}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1505}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1506.}
Let \(S_{1506}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1506}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1507.}
Let \(S_{1507}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1507}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1508.}
Let \(S_{1508}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1508}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1509.}
Let \(S_{1509}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1509}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1510.}
Let \(S_{1510}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1510}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1511.}
Let \(S_{1511}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1511}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1512.}
Let \(S_{1512}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1512}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1513.}
Let \(S_{1513}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1513}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1514.}
Let \(S_{1514}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1514}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1515.}
Let \(S_{1515}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1515}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1516.}
Let \(S_{1516}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1516}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1517.}
Let \(S_{1517}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1517}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1518.}
Let \(S_{1518}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1518}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1519.}
Let \(S_{1519}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1519}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1520.}
Let \(S_{1520}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1520}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1521.}
Let \(S_{1521}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1521}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1522.}
Let \(S_{1522}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1522}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1523.}
Let \(S_{1523}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1523}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1524.}
Let \(S_{1524}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1524}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1525.}
Let \(S_{1525}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1525}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1526.}
Let \(S_{1526}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1526}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1527.}
Let \(S_{1527}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1527}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1528.}
Let \(S_{1528}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1528}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1529.}
Let \(S_{1529}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1529}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1530.}
Let \(S_{1530}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1530}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1531.}
Let \(S_{1531}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1531}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1532.}
Let \(S_{1532}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1532}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1533.}
Let \(S_{1533}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1533}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1534.}
Let \(S_{1534}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1534}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1535.}
Let \(S_{1535}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1535}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1536.}
Let \(S_{1536}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1536}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1537.}
Let \(S_{1537}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1537}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1538.}
Let \(S_{1538}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1538}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1539.}
Let \(S_{1539}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1539}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1540.}
Let \(S_{1540}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1540}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1541.}
Let \(S_{1541}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1541}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1542.}
Let \(S_{1542}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1542}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1543.}
Let \(S_{1543}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1543}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1544.}
Let \(S_{1544}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1544}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1545.}
Let \(S_{1545}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1545}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1546.}
Let \(S_{1546}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1546}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1547.}
Let \(S_{1547}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1547}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1548.}
Let \(S_{1548}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1548}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1549.}
Let \(S_{1549}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1549}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1550.}
Let \(S_{1550}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1550}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1551.}
Let \(S_{1551}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1551}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1552.}
Let \(S_{1552}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1552}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1553.}
Let \(S_{1553}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1553}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1554.}
Let \(S_{1554}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1554}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1555.}
Let \(S_{1555}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1555}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1556.}
Let \(S_{1556}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1556}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1557.}
Let \(S_{1557}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1557}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1558.}
Let \(S_{1558}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1558}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1559.}
Let \(S_{1559}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1559}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1560.}
Let \(S_{1560}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1560}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1561.}
Let \(S_{1561}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1561}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1562.}
Let \(S_{1562}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1562}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1563.}
Let \(S_{1563}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1563}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1564.}
Let \(S_{1564}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1564}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1565.}
Let \(S_{1565}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1565}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1566.}
Let \(S_{1566}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1566}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1567.}
Let \(S_{1567}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1567}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1568.}
Let \(S_{1568}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1568}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1569.}
Let \(S_{1569}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1569}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1570.}
Let \(S_{1570}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1570}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1571.}
Let \(S_{1571}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1571}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1572.}
Let \(S_{1572}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1572}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1573.}
Let \(S_{1573}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1573}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1574.}
Let \(S_{1574}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1574}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1575.}
Let \(S_{1575}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1575}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1576.}
Let \(S_{1576}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1576}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1577.}
Let \(S_{1577}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1577}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1578.}
Let \(S_{1578}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1578}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1579.}
Let \(S_{1579}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1579}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1580.}
Let \(S_{1580}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1580}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1581.}
Let \(S_{1581}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1581}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1582.}
Let \(S_{1582}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1582}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1583.}
Let \(S_{1583}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1583}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1584.}
Let \(S_{1584}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1584}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1585.}
Let \(S_{1585}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1585}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1586.}
Let \(S_{1586}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1586}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1587.}
Let \(S_{1587}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1587}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1588.}
Let \(S_{1588}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1588}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1589.}
Let \(S_{1589}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1589}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1590.}
Let \(S_{1590}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1590}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1591.}
Let \(S_{1591}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1591}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1592.}
Let \(S_{1592}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1592}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1593.}
Let \(S_{1593}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1593}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1594.}
Let \(S_{1594}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1594}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1595.}
Let \(S_{1595}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1595}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1596.}
Let \(S_{1596}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1596}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1597.}
Let \(S_{1597}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1597}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1598.}
Let \(S_{1598}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1598}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1599.}
Let \(S_{1599}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1599}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1600.}
Let \(S_{1600}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1600}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1601.}
Let \(S_{1601}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1601}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1602.}
Let \(S_{1602}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1602}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1603.}
Let \(S_{1603}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1603}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1604.}
Let \(S_{1604}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1604}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1605.}
Let \(S_{1605}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1605}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1606.}
Let \(S_{1606}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1606}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1607.}
Let \(S_{1607}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1607}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1608.}
Let \(S_{1608}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1608}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1609.}
Let \(S_{1609}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1609}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1610.}
Let \(S_{1610}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1610}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1611.}
Let \(S_{1611}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1611}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1612.}
Let \(S_{1612}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1612}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1613.}
Let \(S_{1613}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1613}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1614.}
Let \(S_{1614}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1614}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1615.}
Let \(S_{1615}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1615}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1616.}
Let \(S_{1616}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1616}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1617.}
Let \(S_{1617}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1617}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1618.}
Let \(S_{1618}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1618}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1619.}
Let \(S_{1619}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1619}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1620.}
Let \(S_{1620}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1620}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1621.}
Let \(S_{1621}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1621}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1622.}
Let \(S_{1622}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1622}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1623.}
Let \(S_{1623}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1623}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1624.}
Let \(S_{1624}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1624}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1625.}
Let \(S_{1625}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1625}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1626.}
Let \(S_{1626}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1626}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1627.}
Let \(S_{1627}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1627}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1628.}
Let \(S_{1628}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1628}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1629.}
Let \(S_{1629}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1629}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1630.}
Let \(S_{1630}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1630}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1631.}
Let \(S_{1631}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1631}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1632.}
Let \(S_{1632}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1632}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1633.}
Let \(S_{1633}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1633}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1634.}
Let \(S_{1634}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1634}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1635.}
Let \(S_{1635}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1635}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1636.}
Let \(S_{1636}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1636}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1637.}
Let \(S_{1637}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1637}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1638.}
Let \(S_{1638}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1638}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1639.}
Let \(S_{1639}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1639}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1640.}
Let \(S_{1640}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1640}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1641.}
Let \(S_{1641}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1641}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1642.}
Let \(S_{1642}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1642}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1643.}
Let \(S_{1643}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1643}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1644.}
Let \(S_{1644}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1644}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1645.}
Let \(S_{1645}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1645}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1646.}
Let \(S_{1646}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1646}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1647.}
Let \(S_{1647}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1647}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1648.}
Let \(S_{1648}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1648}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1649.}
Let \(S_{1649}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1649}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1650.}
Let \(S_{1650}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1650}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1651.}
Let \(S_{1651}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1651}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1652.}
Let \(S_{1652}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1652}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1653.}
Let \(S_{1653}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1653}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1654.}
Let \(S_{1654}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1654}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1655.}
Let \(S_{1655}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1655}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1656.}
Let \(S_{1656}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1656}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1657.}
Let \(S_{1657}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1657}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1658.}
Let \(S_{1658}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1658}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1659.}
Let \(S_{1659}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1659}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1660.}
Let \(S_{1660}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1660}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1661.}
Let \(S_{1661}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1661}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1662.}
Let \(S_{1662}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1662}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1663.}
Let \(S_{1663}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1663}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1664.}
Let \(S_{1664}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1664}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1665.}
Let \(S_{1665}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1665}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1666.}
Let \(S_{1666}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1666}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1667.}
Let \(S_{1667}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1667}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1668.}
Let \(S_{1668}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1668}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1669.}
Let \(S_{1669}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1669}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1670.}
Let \(S_{1670}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1670}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1671.}
Let \(S_{1671}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1671}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1672.}
Let \(S_{1672}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1672}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1673.}
Let \(S_{1673}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1673}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1674.}
Let \(S_{1674}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1674}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1675.}
Let \(S_{1675}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1675}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1676.}
Let \(S_{1676}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1676}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1677.}
Let \(S_{1677}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1677}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1678.}
Let \(S_{1678}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1678}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1679.}
Let \(S_{1679}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1679}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1680.}
Let \(S_{1680}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1680}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1681.}
Let \(S_{1681}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1681}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1682.}
Let \(S_{1682}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1682}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1683.}
Let \(S_{1683}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1683}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1684.}
Let \(S_{1684}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1684}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1685.}
Let \(S_{1685}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1685}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1686.}
Let \(S_{1686}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1686}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1687.}
Let \(S_{1687}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1687}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1688.}
Let \(S_{1688}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1688}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1689.}
Let \(S_{1689}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1689}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1690.}
Let \(S_{1690}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1690}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1691.}
Let \(S_{1691}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1691}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1692.}
Let \(S_{1692}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1692}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1693.}
Let \(S_{1693}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1693}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1694.}
Let \(S_{1694}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1694}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1695.}
Let \(S_{1695}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1695}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1696.}
Let \(S_{1696}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1696}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1697.}
Let \(S_{1697}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1697}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1698.}
Let \(S_{1698}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1698}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1699.}
Let \(S_{1699}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1699}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1700.}
Let \(S_{1700}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1700}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1701.}
Let \(S_{1701}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1701}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1702.}
Let \(S_{1702}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1702}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1703.}
Let \(S_{1703}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1703}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1704.}
Let \(S_{1704}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1704}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1705.}
Let \(S_{1705}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1705}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1706.}
Let \(S_{1706}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1706}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1707.}
Let \(S_{1707}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1707}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1708.}
Let \(S_{1708}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1708}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1709.}
Let \(S_{1709}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1709}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1710.}
Let \(S_{1710}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1710}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1711.}
Let \(S_{1711}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1711}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1712.}
Let \(S_{1712}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1712}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1713.}
Let \(S_{1713}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1713}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1714.}
Let \(S_{1714}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1714}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1715.}
Let \(S_{1715}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1715}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1716.}
Let \(S_{1716}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1716}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1717.}
Let \(S_{1717}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1717}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1718.}
Let \(S_{1718}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1718}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1719.}
Let \(S_{1719}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1719}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1720.}
Let \(S_{1720}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1720}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1721.}
Let \(S_{1721}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1721}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1722.}
Let \(S_{1722}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1722}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1723.}
Let \(S_{1723}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1723}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1724.}
Let \(S_{1724}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1724}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1725.}
Let \(S_{1725}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1725}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1726.}
Let \(S_{1726}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1726}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1727.}
Let \(S_{1727}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1727}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1728.}
Let \(S_{1728}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1728}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1729.}
Let \(S_{1729}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1729}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1730.}
Let \(S_{1730}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1730}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1731.}
Let \(S_{1731}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1731}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1732.}
Let \(S_{1732}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1732}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1733.}
Let \(S_{1733}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1733}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1734.}
Let \(S_{1734}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1734}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1735.}
Let \(S_{1735}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1735}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1736.}
Let \(S_{1736}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1736}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1737.}
Let \(S_{1737}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1737}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1738.}
Let \(S_{1738}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1738}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1739.}
Let \(S_{1739}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1739}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1740.}
Let \(S_{1740}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1740}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1741.}
Let \(S_{1741}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1741}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1742.}
Let \(S_{1742}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1742}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1743.}
Let \(S_{1743}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1743}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1744.}
Let \(S_{1744}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1744}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1745.}
Let \(S_{1745}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1745}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1746.}
Let \(S_{1746}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1746}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1747.}
Let \(S_{1747}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1747}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1748.}
Let \(S_{1748}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1748}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1749.}
Let \(S_{1749}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1749}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1750.}
Let \(S_{1750}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1750}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1751.}
Let \(S_{1751}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1751}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1752.}
Let \(S_{1752}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1752}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1753.}
Let \(S_{1753}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1753}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1754.}
Let \(S_{1754}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1754}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1755.}
Let \(S_{1755}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1755}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1756.}
Let \(S_{1756}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1756}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1757.}
Let \(S_{1757}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1757}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1758.}
Let \(S_{1758}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1758}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1759.}
Let \(S_{1759}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1759}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1760.}
Let \(S_{1760}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1760}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1761.}
Let \(S_{1761}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1761}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1762.}
Let \(S_{1762}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1762}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1763.}
Let \(S_{1763}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1763}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1764.}
Let \(S_{1764}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1764}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1765.}
Let \(S_{1765}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1765}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1766.}
Let \(S_{1766}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1766}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1767.}
Let \(S_{1767}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1767}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1768.}
Let \(S_{1768}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1768}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1769.}
Let \(S_{1769}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1769}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1770.}
Let \(S_{1770}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1770}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1771.}
Let \(S_{1771}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1771}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1772.}
Let \(S_{1772}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1772}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1773.}
Let \(S_{1773}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1773}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1774.}
Let \(S_{1774}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1774}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1775.}
Let \(S_{1775}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1775}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1776.}
Let \(S_{1776}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1776}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1777.}
Let \(S_{1777}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1777}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1778.}
Let \(S_{1778}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1778}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1779.}
Let \(S_{1779}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1779}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1780.}
Let \(S_{1780}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1780}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1781.}
Let \(S_{1781}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1781}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1782.}
Let \(S_{1782}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1782}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1783.}
Let \(S_{1783}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1783}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1784.}
Let \(S_{1784}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1784}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1785.}
Let \(S_{1785}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1785}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1786.}
Let \(S_{1786}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1786}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1787.}
Let \(S_{1787}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1787}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1788.}
Let \(S_{1788}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1788}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1789.}
Let \(S_{1789}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1789}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1790.}
Let \(S_{1790}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1790}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1791.}
Let \(S_{1791}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1791}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1792.}
Let \(S_{1792}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1792}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1793.}
Let \(S_{1793}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1793}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1794.}
Let \(S_{1794}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1794}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1795.}
Let \(S_{1795}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1795}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1796.}
Let \(S_{1796}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1796}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1797.}
Let \(S_{1797}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1797}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1798.}
Let \(S_{1798}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1798}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1799.}
Let \(S_{1799}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1799}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1800.}
Let \(S_{1800}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1800}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1801.}
Let \(S_{1801}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1801}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1802.}
Let \(S_{1802}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1802}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1803.}
Let \(S_{1803}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1803}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1804.}
Let \(S_{1804}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1804}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1805.}
Let \(S_{1805}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1805}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1806.}
Let \(S_{1806}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1806}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1807.}
Let \(S_{1807}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1807}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1808.}
Let \(S_{1808}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1808}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1809.}
Let \(S_{1809}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1809}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1810.}
Let \(S_{1810}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1810}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1811.}
Let \(S_{1811}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1811}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1812.}
Let \(S_{1812}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1812}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1813.}
Let \(S_{1813}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1813}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1814.}
Let \(S_{1814}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1814}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1815.}
Let \(S_{1815}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1815}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1816.}
Let \(S_{1816}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1816}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1817.}
Let \(S_{1817}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1817}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1818.}
Let \(S_{1818}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1818}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1819.}
Let \(S_{1819}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1819}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1820.}
Let \(S_{1820}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1820}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1821.}
Let \(S_{1821}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1821}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1822.}
Let \(S_{1822}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1822}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1823.}
Let \(S_{1823}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1823}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1824.}
Let \(S_{1824}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1824}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1825.}
Let \(S_{1825}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1825}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1826.}
Let \(S_{1826}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1826}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1827.}
Let \(S_{1827}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1827}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1828.}
Let \(S_{1828}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1828}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1829.}
Let \(S_{1829}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1829}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1830.}
Let \(S_{1830}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1830}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1831.}
Let \(S_{1831}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1831}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1832.}
Let \(S_{1832}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1832}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1833.}
Let \(S_{1833}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1833}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1834.}
Let \(S_{1834}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1834}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1835.}
Let \(S_{1835}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1835}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1836.}
Let \(S_{1836}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1836}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1837.}
Let \(S_{1837}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1837}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1838.}
Let \(S_{1838}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1838}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1839.}
Let \(S_{1839}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1839}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1840.}
Let \(S_{1840}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1840}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1841.}
Let \(S_{1841}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1841}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1842.}
Let \(S_{1842}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1842}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1843.}
Let \(S_{1843}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1843}\) strengthens the pass branch or selects the fail branch that the CM test later consumes. \emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Pack, Part, or Field.

\paragraph{Forward-support expansion 1844.}
Let \(S_{1844}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1844}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1845.}
Let \(S_{1845}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1845}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1846.}
Let \(S_{1846}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1846}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1847.}
Let \(S_{1847}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1847}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1848.}
Let \(S_{1848}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1848}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1849.}
Let \(S_{1849}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1849}\) contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion. \emph{Proof.} A downstream statement carries no separate continuation service. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\paragraph{Forward-support expansion 1850.}
Let \(S_{1850}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1850}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1851.}
Let \(S_{1851}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1851}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\paragraph{Forward-support expansion 1852.}
Let \(S_{1852}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1852}\) enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet. \emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Pack, Part, and Field decision tree applies; before selection, the comparison statement remains hinge support.

\paragraph{Forward-support expansion 1853.}
Let \(S_{1853}\) be the support obligation at this place in the exhaustion. \emph{Claim.} \(S_{1853}\) has proof force through membership readout or through a Field-face loss. \emph{Proof.} A readout statement is downstream of the carrier and participation services. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\section{Expanded Branch-Family Obligations}

Let \(G_k\) denote the \(k\)-th branch-family obligation that carries proof-program material. Each item is expanded by the branch mechanism it contributes to the Pack-first class law.

\paragraph{Branch-family expansion 1.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 2.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 3.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 4.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 5.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 6.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 7.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 8.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 9.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 10.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 11.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 12.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 13.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 14.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 15.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 16.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 17.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 18.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 19.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 20.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 21.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 22.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 23.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 24.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 25.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 26.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 27.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 28.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 29.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 30.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 31.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 32.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 33.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 34.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 35.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 36.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 37.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 38.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 39.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 40.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 41.}
\emph{Source-reserve family.} This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms. \emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family expansion 42.}
\emph{Local translator family.} This family tests local critical translation as a same-solution packet question. \emph{Proof.} The translator has proof force only when it preserves the same Navier--Stokes pressure-viscosity participation on the transported packet. Loss of that participation is a Part-side failure; a surviving translator then moves to Field readout.

\paragraph{Branch-family expansion 43.}
\emph{Local translator family.} This family tests local critical translation as a same-solution packet question. \emph{Proof.} The translator has proof force only when it preserves the same Navier--Stokes pressure-viscosity participation on the transported packet. Loss of that participation is a Part-side failure; a surviving translator then moves to Field readout.

\paragraph{Branch-family expansion 44.}
\emph{Local translator family.} This family tests local critical translation as a same-solution packet question. \emph{Proof.} The translator has proof force only when it preserves the same Navier--Stokes pressure-viscosity participation on the transported packet. Loss of that participation is a Part-side failure; a surviving translator then moves to Field readout.

\paragraph{Branch-family expansion 45.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 46.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 47.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 48.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 49.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 50.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 51.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 52.}
\emph{Same-ledger family.} This family asks whether the terminal branch is still charged to the same witness record. \emph{Proof.} A terminal item on the same ledger enters the Pack-first test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family expansion 53.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 54.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 55.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 56.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 57.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 58.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 59.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 60.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 61.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 62.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 63.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 64.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 65.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 66.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 67.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 68.}
\emph{Class-law family.} This family tests the pass-or-exit law against possible reopening branches. \emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Pack/Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family expansion 69.}
\emph{Local energy and elliptic family.} This family supplies analytic support for source-wall and classification tests. \emph{Proof.} Local energy and elliptic control matter when they select the terminal packet, verify pressure participation, or read out a positive field scale. The support is then consumed by Pack, Part, Field, or membership readout.

\paragraph{Branch-family expansion 70.}
\emph{Local energy and elliptic family.} This family supplies analytic support for source-wall and classification tests. \emph{Proof.} Local energy and elliptic control matter when they select the terminal packet, verify pressure participation, or read out a positive field scale. The support is then consumed by Pack, Part, Field, or membership readout.

\paragraph{Branch-family expansion 71.}
\emph{Local energy and elliptic family.} This family supplies analytic support for source-wall and classification tests. \emph{Proof.} Local energy and elliptic control matter when they select the terminal packet, verify pressure participation, or read out a positive field scale. The support is then consumed by Pack, Part, Field, or membership readout.

\paragraph{Branch-family expansion 72.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 73.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 74.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 75.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 76.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 77.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 78.}
\emph{Comparison-boundary family.} This family marks comparison branches that need an exact Navier--Stokes packet before entering the CM proof. \emph{Proof.} The comparison may reveal a useful branch shape. The proof uses it only after the Navier--Stokes witness record, carrier, participation, and field readout are stated for the same solution.

\paragraph{Branch-family expansion 79.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.
\paragraph{Branch-family expansion 80.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 81.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 82.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 83.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 84.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 85.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 86.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 87.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.

\paragraph{Branch-family expansion 88.}
\emph{Candidate-branch family.} This family contains candidate pressure tests and route probes. \emph{Proof.} A candidate becomes proof material only when it names a terminal packet and a service face. The face then routes through Pack, Part, Field, membership readout, terminal capture, or no-third-branch exclusion.
