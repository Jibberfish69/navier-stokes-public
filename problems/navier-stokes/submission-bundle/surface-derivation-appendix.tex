% Generated by problems/navier-stokes/tools/build_surface_derivation_appendix.rb
% Internal source names stay in surface-derivation-inventory.yaml.
% The visible content below is mathematical branch expansion only.

\clearpage
\section{Silver VPI-Complement Exhaustion}

Fix one smooth-data, fixed-positive-viscosity, no-reset original Navier--Stokes history with full VPI participation. The entire Silver proof is the exhaustive negative implication and its direct contrapositive: \(\neg\operatorname{Smooth}(Q)\Rightarrow\neg\operatorname{Member}(Q)\Rightarrow\neg\operatorname{VPIParticipation}(Q)\), hence \(\operatorname{VPIParticipation}(Q)\Rightarrow\operatorname{Member}(Q)\Rightarrow\operatorname{Smooth}(Q)\). The grouped rows below only show how departure presentations reduce to that one loss; they do not add a forward terminal theorem or a Gold premise.

\subsection{Negative-side presentation 1: Field-face terminal obligations}
This group accounts for 769 classified surfaces. The rules represented here are retained field failure after the other faces have been removed (417); direct Part/Field failure (352). The Part/Field localizations are \(\Field_{N,r,Q}\) (769). The departure mechanisms represented here are terminal source wall, source reserve, source pulse, square source, or active window failure (285); one field coherence, receiver, finite difference, Field readout, or endpoint Field failure (266); carrier, cover, or Pack gauge failure routed by the row's face sort rule (96); source wall / Carleson type retained terminal source failure routed by the row's face sort rule (69); Zeno or zero heat time source residue failure routed by the row's face sort rule (33); one field coherence or Field readout failure routed by the row's face sort rule (14); Zeno terminal atom, zero heat time residue, or terminal source residue failure (6).

\paragraph{Family inventory role.}
\emph{Inventory statement.} Negative-side presentation 1 gives terminal Field failure, hence \(\neg\operatorname{Member}(Q)\Rightarrow\neg\operatorname{VPIParticipation}(Q)\).

\emph{Support explanation.} A terminal loss of coherent surrounding-field participation is itself loss of full VPI participation by the same original history. Field is the local presentation of that loss, not a retained singular participant and not an additional PDE solution.

\emph{Reader-facing role.} The group belongs to the exhaustive negative inventory used by direct contraposition.

\subsection{Negative-side presentation 2: finite Part/Field disjunction obligations}
This group accounts for 96 classified surfaces. The rules represented here are finite face disjunction (96). The Part/Field localizations are \(\Field_{N,r,Q}\), \(\Part_{N,Q}\) (96). The departure mechanisms represented here are branch local first failure routed by the surface's trichotomy or boundary lemma (80); carrier, cover, or Pack gauge failure routed by the row's face sort rule (7); source wall / Carleson type retained terminal source failure routed by the row's face sort rule (5); Zeno or zero heat time source residue failure routed by the row's face sort rule (2); one field coherence or Field readout failure routed by the row's face sort rule (2).

\paragraph{Family inventory role.}
\emph{Inventory statement.} Negative-side presentation 2 gives a finite disjunction of negative-side presentations, each ending in loss of full VPI participation.

\emph{Support explanation.} Part and Field localize \(\neg\operatorname{Member}(Q)\); Pack remains outside CM as Field-window evidence. Dead, Blown, Jump, and any conditional limiting-PDE label name mechanisms for the same loss rather than extra terminal branches.

\emph{Reader-facing role.} Exhausting the listed presentations supplies \(\neg\operatorname{Smooth}(Q)\Rightarrow\neg\operatorname{Member}(Q)\Rightarrow\neg\operatorname{VPIParticipation}(Q)\).

\subsection{Negative-side presentation 3: inherited-authority obligations}
This group accounts for 44 classified surfaces. The rules represented here are authority inheritance with no new terminal face (44). The Part/Field localizations are no independent Part/Field face (44). The departure mechanisms represented here are authority or generated carrier for the installed face sweep boundary (24); copy of an authority or export support surface (17); carrier, cover, or Pack gauge failure routed by the row's face sort rule (3).

\paragraph{Family inventory role.}
\emph{Inventory statement.} Negative-side presentation 3 inherits the installed VPI-complement classification and adds no new departure mechanism.

\emph{Support explanation.} The branch is a carrier of already established negative-side content. It does not create a new participant, a forward terminal burden, or a Gold dependency.

\emph{Reader-facing role.} Its role is provenance only; the one-line Silver contrapositive remains the proof authority.

\subsection{Negative-side presentation 4: outside-CM carrier-window participation-loss presentations}
This group accounts for 244 classified surfaces. The rules represented here are direct Part/Field failure (244). The Part/Field localizations are outside-CM Field-window evidence (244). The departure mechanisms represented here are carrier, cover, packing, retained packet admission, or Pack bridge failure (164); carrier, cover, or Pack gauge failure routed by the row's face sort rule (71); one field coherence or Field readout failure routed by the row's face sort rule (7); participation, no drop, or Part envelope failure routed by the row's face sort rule (2).

\paragraph{Family inventory role.}
\emph{Inventory statement.} Negative-side presentation 4 presents loss of full VPI participation through carrier or Field-window failure.

\emph{Support explanation.} The original smooth-data, fixed-viscosity Navier--Stokes history remains the object. If its carrier, ancestry, or coherent field window fails, that same history no longer has full VPI participation. Pack records this outside CM; it is not a third CM face and does not by itself assert \(\neg\operatorname{Member}(Q)\).

\emph{Reader-facing role.} This group contributes to the exhaustive implication \(\neg\operatorname{Smooth}(Q)\Rightarrow\neg\operatorname{VPIParticipation}(Q)\). Any class-exit wording still has to be localized through Part or Field.

\subsection{Negative-side presentation 5: Part-face terminal obligations}
This group accounts for 250 classified surfaces. The rules represented here are direct Part/Field failure (250). The Part/Field localizations are \(\Part_{N,Q}\) (250). The departure mechanisms represented here are same PDE participation, no drop, pressure viscosity tether, envelope, or Part bridge failure (195); carrier, cover, or Pack gauge failure routed by the row's face sort rule (42); one field coherence or Field readout failure routed by the row's face sort rule (7); participation, no drop, or Part envelope failure routed by the row's face sort rule (6).

\paragraph{Family inventory role.}
\emph{Inventory statement.} Negative-side presentation 5 gives \(\neg\Part_{N,Q}\), hence \(\neg\operatorname{Member}(Q)\Rightarrow\neg\operatorname{VPIParticipation}(Q)\).

\emph{Support explanation.} This is a failure of transport, simultaneous nonlocal pressure, viscosity, incompressibility, or material ancestry in the same original Navier--Stokes history. Part merely localizes that one loss; it does not create a separate fluid object.

\emph{Reader-facing role.} The group is one presentation on the negative side of the Silver implication, not a positive terminal-exclusion theorem.

\subsection{Negative-side presentation 6: support-boundary obligations}
This group accounts for 541 classified surfaces. The rules represented here are support retained outside the CM conclusion until a bridge supplies a face (541). The Part/Field localizations are no independent Part/Field face (541). The departure mechanisms represented here are history, audit, or context support (164); downstream export or readout consumer (94); root support or context surface (91); carrier, cover, or Pack gauge failure routed by the row's face sort rule (71); supplier/readout claim without independent CM steering authority (42); runtime or governance surface (31); manuscript or export presentation (23); source recovery or history context (22); Zeno or zero heat time source residue failure routed by the row's face sort rule (2); participation, no drop, or Part envelope failure routed by the row's face sort rule (1).

\paragraph{Family inventory role.}
\emph{Inventory statement.} Negative-side presentation 6 has no Silver consequence until it identifies a departure of the same original Navier--Stokes history.

\emph{Support explanation.} Support cannot assert class exit or smoothness by itself. It enters only by proving loss of full VPI participation and, where \(\neg\operatorname{Member}(Q)\) is claimed, by localizing that loss through Part or Field.

\emph{Reader-facing role.} This boundary prevents estimates, labels, and generated rows from replacing the exhaustive negative-side proof.


\section{Expanded Forward-Support Obligations}

Forward-positive material is not a premise of Silver. Each group below remains in the independent nonblocking Gold lane unless it proves a same-history departure and loss of full VPI participation.

\subsection{Support proof role 1: classified positive / export/legacy / supplier / readout / positive smoothness / transfer/Euler}
This proof role accounts for 263 support obligations with one mathematical support job. The support classes represented here are separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support (45); separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (34); separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support (31); forward supplier support; transfer or Euler comparison support (20); transfer or Euler comparison support (20); transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support (19); transfer or Euler comparison support; downstream export or legacy engine support (14); separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support; previously classified positive support language (10); forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support (7); transfer or Euler comparison support; separate positive smoothness support (7); transfer or Euler comparison support; separate positive smoothness support; receiver, readout, or endpoint support; downstream export or legacy engine support (7); forward supplier support; separate positive smoothness support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (6); separate positive smoothness support; transfer or Euler comparison support; receiver, readout, or endpoint support (6); separate positive smoothness support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support (4); 19 further mechanisms. The support may enter Silver only through an exact same-history participation-loss bridge; Part or Field is additionally required when it asserts class exit.

\paragraph{Family support role.}
\emph{Support statement.} Support proof role 1 may classify a departure only after it is derived forward from the same original Navier--Stokes history.

\emph{Support explanation.} Euler is permitted only when effective viscosity and every Reynolds, forcing, pressure, incompressibility, and ancestry defect vanish. The limiting PDE then names the same departure presentation; the proof never starts from Euler and transfers smoothness backward.

\emph{Reader-facing role.} The comparison has no authority unless it preserves the original history and proves loss of its full VPI participation.

\subsection{Support proof role 2: export/legacy}
This proof role accounts for 283 support obligations with one mathematical support job. The support classes represented here are downstream export or legacy engine support (283). The support may enter Silver only through an exact same-history participation-loss bridge; Part or Field is additionally required when it asserts class exit.

\paragraph{Family support role.}
\emph{Support statement.} Support proof role 2 contributes only through a named same-history participation-loss classification.

\emph{Support explanation.} A downstream label has no independent theorem force. It must identify which element of full VPI participation fails and, for a class-exit assertion, how Part or Field localizes \(\neg\operatorname{Member}(Q)\).

\emph{Reader-facing role.} Anything else remains provenance outside the Silver proof.

\subsection{Support proof role 3: classified positive / export/legacy / supplier / positive smoothness}
This proof role accounts for 810 support obligations with one mathematical support job. The support classes represented here are forward supplier support (493); separate positive smoothness support (113); separate positive smoothness support; downstream export or legacy engine support (63); separate positive smoothness support; forward supplier support (50); forward supplier support; downstream export or legacy engine support (36); forward supplier support; previously classified positive support language (21); separate positive smoothness support; forward supplier support; downstream export or legacy engine support (10); downstream export or legacy engine support; separate positive smoothness support (7); forward supplier support; separate positive smoothness support (6); previously classified positive support language (3); separate positive smoothness support; forward supplier support; downstream export or legacy engine support; previously classified positive support language (3); separate positive smoothness support; previously classified positive support language (2); downstream export or legacy engine support; previously classified positive support language (1); downstream export or legacy engine support; separate positive smoothness support; previously classified positive support language (1); 1 further mechanism. The support may enter Silver only through an exact same-history participation-loss bridge; Part or Field is additionally required when it asserts class exit.

\paragraph{Family support role.}
\emph{Support statement.} Support proof role 3 belongs to the independent nonblocking Gold lane unless it classifies a same-history VPI participation loss.

\emph{Support explanation.} Positive estimates are not premises of Silver and cannot replace its contrapositive with a forward terminal proof. Only their exact negative-side departure consequence, if proved, enters this inventory.

\emph{Reader-facing role.} Gold may remain open without blocking the accepted Silver proof.

\subsection{Support proof role 4: classified positive / export/legacy / supplier / readout / positive smoothness}
This proof role accounts for 1949 support obligations with one mathematical support job. The support classes represented here are receiver, readout, or endpoint support (781); forward supplier support; receiver, readout, or endpoint support (437); receiver, readout, or endpoint support; downstream export or legacy engine support (152); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support (106); separate positive smoothness support; receiver, readout, or endpoint support (101); separate positive smoothness support; receiver, readout, or endpoint support; downstream export or legacy engine support (84); forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support (62); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support (33); forward supplier support; receiver, readout, or endpoint support; previously classified positive support language (29); receiver, readout, or endpoint support; separate positive smoothness support (24); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (24); forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (22); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support; previously classified positive support language (17); forward supplier support; separate positive smoothness support; receiver, readout, or endpoint support (16); 15 further mechanisms. The support may enter Silver only through an exact same-history participation-loss bridge; Part or Field is additionally required when it asserts class exit.

\paragraph{Family support role.}
\emph{Support statement.} Support proof role 4 has Silver force only when it records Field failure or another loss of full VPI participation in the same history.

\emph{Support explanation.} A bounded readout is not a separate Silver premise. A terminal failed readout may localize Field loss, and that Field loss is loss of full VPI participation rather than a surviving singular participant.

\emph{Reader-facing role.} The readout therefore classifies the negative side; it does not replace the one-line contrapositive.


\section{Expanded Branch-Family Obligations}

The current-material coverage families are represented by mathematical proof roles rather than file names. Repetition is by branch family, so the visible appendix shows the obligation being carried instead of repeating the same anonymous sentence for every source item.

\paragraph{Branch-family obligation 1.}
\emph{Source-reserve family.} This family records 41 current support or inventory obligations.

This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms.

\emph{Support explanation.} Every terminal mechanism is applied to the same original Navier--Stokes history. Loss of the coupled pressure-viscosity participation gives Part failure; loss of coherent surrounding-field participation gives Field failure; carrier/window loss is outside-CM Field-window evidence. Each realized departure loses full VPI participation.

\paragraph{Branch-family obligation 2.}
\emph{Local translator family.} This family records 3 current support or inventory obligations.

This family tests local critical translation as a same-solution packet question.

\emph{Support explanation.} The translator has proof force only on the same Navier--Stokes history. Loss of its pressure-viscosity participation is Part-side; loss of surrounding-field coherence is Field-side; either loses full VPI participation.

\paragraph{Branch-family obligation 3.}
\emph{Same-ledger family.} This family records 4 current support or inventory obligations.

This family asks whether the terminal branch is still charged to the same witness record.

\emph{Support explanation.} A terminal item must remain the same material/all-field record. An imported or reset branch has no Silver force because it is not the original no-reset history.

\paragraph{Branch-family obligation 4.}
\emph{Class-law family.} This family records 2 current support or inventory obligations.

This family checks the exhaustive negative-side inventory.

\emph{Support explanation.} A claimed extra departure must preserve full VPI participation while being nonsmooth to escape the contrapositive. Dead, Blown, Jump, Part, Field, and conditional limiting-PDE presentations instead reduce to lost participation.

\paragraph{Branch-family obligation 5.}
\emph{Local energy and elliptic family.} This family records 3 current support or inventory obligations.

This family supplies analytic support for source-wall and classification tests.

\emph{Support explanation.} Local energy and elliptic control matter only when they classify which component of the same history's full VPI participation fails. Positive estimates remain Gold support.

\paragraph{Branch-family obligation 6.}
\emph{Candidate-branch family.} This family records 34 current support or inventory obligations.

This family contains candidate pressure tests and route probes.

\emph{Support explanation.} A candidate becomes proof material only when it names the same original history and proves which aspect of full VPI participation fails. Part and Field may localize class exit; Pack cannot become a CM face.
