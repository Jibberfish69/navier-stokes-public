% Generated by problems/navier-stokes/tools/build_surface_derivation_appendix.rb
% Internal source names stay in surface-derivation-inventory.yaml.
% The visible content below is mathematical branch expansion only.

\clearpage
\section{Expanded CM Branch Obligations}

This appendix expands the source field into class-membership obligations. Each numbered obligation names the terminal mechanism, the CM requirement being tested, and the proof role it plays in the pass-or-exit engine. The full source inventory is kept outside the reader-facing paper.

\subsection{CM proof role 1: Field-face terminal obligations}
This proof role accounts for 769 branch obligations with one mathematical CM job. The proof rules represented here are retained field failure after the other faces have been removed (417); direct Part/Field failure (352). The tested requirements are Field \{N,r,Q\} (769). The terminal mechanisms represented here are terminal source wall, source reserve, source pulse, square source, or active window failure (285); one field coherence, receiver, finite difference, Field readout, or endpoint Field failure (266); carrier, cover, or Pack gauge failure routed by the row's face sort rule (96); source wall / Carleson type retained terminal source failure routed by the row's face sort rule (69); Zeno or zero heat time source residue failure routed by the row's face sort rule (33); one field coherence or Field readout failure routed by the row's face sort rule (14); Zeno terminal atom, zero heat time residue, or terminal source residue failure (6).

\paragraph{Family proof role.}
\emph{Claim.} CM proof role 1 keeps carrier and participation but gives \(\forall r>0\,\neg\Field_{N,r,Q}\), hence \(\Exit(Q;\Owork)\).

\emph{Proof.} This branch is read after carrier and participation have survived for the case split. The remaining continuation requirement is a positive-scale coherent field readout. The terminal branch has no such scale at any \(r>0\), so the Field face fails and the failure embeds as \(\Exit(Q;\Owork)\).

\emph{Reader-facing role.} This is the last face of the witness tree, so the page has to make the order visible. The branch has already survived the packet and participation questions; what remains is the absence of any positive scale on which the field can be read coherently. The conclusion is Field-face exit, not a generic statement that the solution looks bad.

\subsection{CM proof role 2: finite Part/Field disjunction obligations}
This proof role accounts for 96 branch obligations with one mathematical CM job. The proof rules represented here are finite face disjunction (96). The tested requirements are Field \{N,r,Q\} and Pack Q and Part \{N,Q\} (96). The terminal mechanisms represented here are branch local first failure routed by the surface's trichotomy or boundary lemma (80); carrier, cover, or Pack gauge failure routed by the row's face sort rule (7); source wall / Carleson type retained terminal source failure routed by the row's face sort rule (5); Zeno or zero heat time source residue failure routed by the row's face sort rule (2); one field coherence or Field readout failure routed by the row's face sort rule (2).

\paragraph{Family proof role.}
\emph{Claim.} CM proof role 2 gives a finite disjunction of Part and Field losses, hence \(\Exit(Q;\Owork)\).

\emph{Proof.} The terminal branch presents finitely many same-solution alternatives. Each alternative is tested in Pack-out-of-CM audited order. A finite disjunction of Part and Field losses is still exhausted by the CM witness tree, so every alternative gives the same class-exit conclusion.

\emph{Reader-facing role.} This keeps the reader from seeing the alternatives as an open cloud of cases. There are only finitely many branch options here, and each option pays one of the same three witness costs. Once the finite list is exhausted, there is no remaining terminal branch outside the pass-or-exit law.

\subsection{CM proof role 3: inherited-authority obligations}
This proof role accounts for 44 branch obligations with one mathematical CM job. The proof rules represented here are authority inheritance with no new terminal face (44). The tested requirements are no independent Part/Field face (44). The terminal mechanisms represented here are authority or generated carrier for the installed face sweep boundary (24); copy of an authority or export support surface (17); carrier, cover, or Pack gauge failure routed by the row's face sort rule (3).

\paragraph{Family proof role.}
\emph{Claim.} CM proof role 3 introduces no independent terminal alternative outside the installed Part/Field exhaustion.

\emph{Proof.} The branch carries already established CM content. Its mathematical force is inherited from the terminal packet capture and face-exhaustion theorems. Since it supplies no new selected nonsmooth packet, it contributes no extra continuation requirement and no fourth branch.

\emph{Reader-facing role.} For the reader, the point is restraint. This entry explains why already-paid proof work is not being reread as a new terminal branch. It belongs in the manuscript because it preserves the closed branch count and prevents a hidden fourth alternative from being smuggled back into the argument.

\subsection{CM proof role 4: Field-window evidence terminal obligations}
This proof role accounts for 244 branch obligations with one mathematical CM job. The proof rules represented here are direct Part/Field failure (244). The tested requirements are Pack Q (244). The terminal mechanisms represented here are carrier, cover, packing, retained packet admission, or Pack bridge failure (164); carrier, cover, or Pack gauge failure routed by the row's face sort rule (71); one field coherence or Field readout failure routed by the row's face sort rule (7); participation, no drop, or Part envelope failure routed by the row's face sort rule (2).

\paragraph{Family proof role.}
\emph{Claim.} CM proof role 4 gives \(\neg\Pack_Q\), hence \(\Exit(Q;\Owork)\).

\emph{Proof.} A member branch must first carry \(Q\) by a positive same-fluid packet. The selected terminal branch loses that carrier requirement before any participation or field readout can be asked of it. The Pack-out-of-CM audited order therefore gives \(\neg\Pack_Q\), and the class-exit embedding gives \(\Exit(Q;\Owork)\).

\emph{Reader-facing role.} On the page, this tells the reader that the branch never reaches the later questions. The proof is not claiming a new smooth solution; it is recording that the alleged terminal branch cannot even supply the packet that would let membership be tested. That is why the branch belongs on the fail side of the pass-or-exit split.

\subsection{CM proof role 5: Part-face terminal obligations}
This proof role accounts for 250 branch obligations with one mathematical CM job. The proof rules represented here are direct Part/Field failure (250). The tested requirements are Part \{N,Q\} (250). The terminal mechanisms represented here are same PDE participation, no drop, pressure viscosity tether, envelope, or Part bridge failure (195); carrier, cover, or Pack gauge failure routed by the row's face sort rule (42); one field coherence or Field readout failure routed by the row's face sort rule (7); participation, no drop, or Part envelope failure routed by the row's face sort rule (6).

\paragraph{Family proof role.}
\emph{Claim.} CM proof role 5 keeps the carrier but gives \(\neg\Part_{N,Q}\), hence \(\Exit(Q;\Owork)\).

\emph{Proof.} Here the carrier question has already passed for the purpose of the split. The branch still has to participate in the same pressure-viscosity tower as the original Navier--Stokes solution. The selected failure breaks that tower, so the first surviving lost requirement is \(\Part_{N,Q}\). The Part/Field failure embeds as class exit.

\emph{Reader-facing role.} The reader-facing point is that this is a same-solution participation test. The same-fluid carrier is still present. The branch no longer participates in the Navier--Stokes pressure-viscosity tower that the member branch needs. The obligation therefore closes by loss of participation.

\subsection{CM proof role 6: support-boundary obligations}
This proof role accounts for 541 branch obligations with one mathematical CM job. The proof rules represented here are support retained outside the CM conclusion until a bridge supplies a face (541). The tested requirements are no independent Part/Field face (541). The terminal mechanisms represented here are history, audit, or context support (164); downstream export or readout consumer (94); root support or context surface (91); carrier, cover, or Pack gauge failure routed by the row's face sort rule (71); supplier/readout claim without independent CM steering authority (42); runtime or governance surface (31); manuscript or export presentation (23); source recovery or history context (22); Zeno or zero heat time source residue failure routed by the row's face sort rule (2); participation, no drop, or Part envelope failure routed by the row's face sort rule (1).

\paragraph{Family proof role.}
\emph{Claim.} CM proof role 6 has no CM conclusion until it selects a same-fluid terminal packet.

\emph{Proof.} A support branch can strengthen the positive pass side, sharpen a pressure test, or prepare a later terminal witness. It cannot assert \(\Member(Q;\Owork)\) or \(\Exit(Q;\Owork)\) by itself. Its proof role is to feed a later Part and Field, membership-readout, or no-third-branch hinge.

\emph{Reader-facing role.} This is included so the reader can see the boundary of the proof claim. The material may be useful, but it has not yet selected the same-fluid terminal packet that the CM test consumes. Until that selection is made, the branch remains support for the argument rather than an independent class conclusion.


\section{Expanded Forward-Support Obligations}

Forward-positive material enters the paper only after it is rewritten as support for a class-membership hinge. Each obligation below records the support role without letting forward progress substitute for the CM conclusion.

\subsection{Support proof role 1: classified positive / export/legacy / supplier / readout / positive smoothness / transfer/Euler}
This proof role accounts for 263 support obligations with one mathematical support job. The support classes represented here are separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support (45); separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (34); separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support (31); forward supplier support; transfer or Euler comparison support (20); transfer or Euler comparison support (20); transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support (19); transfer or Euler comparison support; downstream export or legacy engine support (14); separate positive smoothness support; forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support; previously classified positive support language (10); forward supplier support; transfer or Euler comparison support; receiver, readout, or endpoint support (7); transfer or Euler comparison support; separate positive smoothness support (7); transfer or Euler comparison support; separate positive smoothness support; receiver, readout, or endpoint support; downstream export or legacy engine support (7); forward supplier support; separate positive smoothness support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (6); separate positive smoothness support; transfer or Euler comparison support; receiver, readout, or endpoint support (6); separate positive smoothness support; transfer or Euler comparison support; receiver, readout, or endpoint support; downstream export or legacy engine support (4); 19 further mechanisms. The support may enter a CM hinge only through a Field-certification admission, a receiver/readout bridge, or a same-branch transfer license.

\paragraph{Family proof role.}
\emph{Claim.} Support proof role 1 enters the Navier--Stokes class proof only after it selects an exact same-fluid Navier--Stokes packet.

\emph{Proof.} Comparison information may locate an analogous obstruction or exclude a misleading branch shape. The CM proof still acts on the Navier--Stokes packet itself. Once that packet is selected, the same Part and Field decision tree applies; before selection, the comparison statement remains hinge support.

\emph{Reader-facing role.} The reader-facing role is to keep comparison from becoming authority by accident. The comparison can show why a branch is worth testing, but the Clay-facing proof is charged to the Navier--Stokes packet and its own witnesses. This paragraph therefore marks the exact point where comparison stops and same-fluid proof begins.

\subsection{Support proof role 2: export/legacy}
This proof role accounts for 283 support obligations with one mathematical support job. The support classes represented here are downstream export or legacy engine support (283). The support may enter a CM hinge only through a Field-certification admission, a receiver/readout bridge, or a same-branch transfer license.

\paragraph{Family proof role.}
\emph{Claim.} Support proof role 2 contributes only through a named proof hinge: terminal packet capture, membership readout, class exit, or no-third-branch exclusion.

\emph{Proof.} A downstream statement carries no separate continuation requirement. Its mathematical value is measured by whether it verifies one of the hinges already used by the proof spine. If it does, the hinge supplies the proof force; if it does not, it remains outside the terminal branch law.

\emph{Reader-facing role.} This keeps the manuscript from making the reader trust a downstream label as though it were a theorem. The page has to show which hinge is being paid and what that hinge already proves. Anything that does not pay one of those hinges remains outside the terminal proof claim.

\subsection{Support proof role 3: classified positive / export/legacy / supplier / positive smoothness}
This proof role accounts for 810 support obligations with one mathematical support job. The support classes represented here are forward supplier support (493); separate positive smoothness support (113); separate positive smoothness support; downstream export or legacy engine support (63); separate positive smoothness support; forward supplier support (50); forward supplier support; downstream export or legacy engine support (36); forward supplier support; previously classified positive support language (21); separate positive smoothness support; forward supplier support; downstream export or legacy engine support (10); downstream export or legacy engine support; separate positive smoothness support (7); forward supplier support; separate positive smoothness support (6); previously classified positive support language (3); separate positive smoothness support; forward supplier support; downstream export or legacy engine support; previously classified positive support language (3); separate positive smoothness support; previously classified positive support language (2); downstream export or legacy engine support; previously classified positive support language (1); downstream export or legacy engine support; separate positive smoothness support; previously classified positive support language (1); 1 further mechanism. The support may enter a CM hinge only through a Field-certification admission, a receiver/readout bridge, or a same-branch transfer license.

\paragraph{Family proof role.}
\emph{Claim.} Support proof role 3 strengthens the pass branch or selects the fail branch that the CM test later consumes.

\emph{Proof.} A positive supplier estimate can pay a source term, preserve a packet, or narrow the terminal pressure. The CM proof uses that work at the hinge where the branch becomes a selected same-solution packet. After selection, failure is decided by Part or Field.

\emph{Reader-facing role.} The reader-facing role is to show why forward estimates are present without letting them replace the contrapositive proof. They can pay costs on the pass side or select the branch that the fail side tests. Once the selected packet is on the page, the CM witness tree carries the final burden.

\subsection{Support proof role 4: classified positive / export/legacy / supplier / readout / positive smoothness}
This proof role accounts for 1949 support obligations with one mathematical support job. The support classes represented here are receiver, readout, or endpoint support (781); forward supplier support; receiver, readout, or endpoint support (437); receiver, readout, or endpoint support; downstream export or legacy engine support (152); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support (106); separate positive smoothness support; receiver, readout, or endpoint support (101); separate positive smoothness support; receiver, readout, or endpoint support; downstream export or legacy engine support (84); forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support (62); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support (33); forward supplier support; receiver, readout, or endpoint support; previously classified positive support language (29); receiver, readout, or endpoint support; separate positive smoothness support (24); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (24); forward supplier support; receiver, readout, or endpoint support; downstream export or legacy engine support; previously classified positive support language (22); separate positive smoothness support; forward supplier support; receiver, readout, or endpoint support; previously classified positive support language (17); forward supplier support; separate positive smoothness support; receiver, readout, or endpoint support (16); 15 further mechanisms. The support may enter a CM hinge only through a Field-certification admission, a receiver/readout bridge, or a same-branch transfer license.

\paragraph{Family proof role.}
\emph{Claim.} Support proof role 4 has proof force through membership readout or through a Field-face loss.

\emph{Proof.} A readout statement is downstream of the carrier and participation requirements. When the readout remains bounded on a positive scale, it supports the pass-side continuation norm. When every positive-scale readout fails, it supplies \(\forall r>0\,\neg\Field_{N,r,Q}\) and the fail side exits the class.

\emph{Reader-facing role.} This tells the reader how to read endpoint material without turning it into a separate proof program. A good readout supports the member branch; a failed readout, after carrier and participation have survived, identifies the Field face. The obligation is therefore placed where the paper decides between positive-scale readability and field exit.


\section{Expanded Branch-Family Obligations}

The current-material coverage families are represented by mathematical proof roles rather than file names. Repetition is by branch family, so the visible appendix shows the obligation being carried instead of repeating the same anonymous sentence for every source item.

\paragraph{Branch-family obligation 1.}
\emph{Source-reserve family.} This family accounts for 40 current obligations.

This branch family is tested by source survival, zero-radius refinement, signed-current/no-free-sink pressure, retained amplitude, and terminal atom mechanisms.

\emph{Proof.} The proof first asks whether a positive same-fluid packet carries the source residue. Carrier loss gives \(\neg\Pack_Q\). A surviving carrier then asks whether the pressure-viscosity tower remains the same; failure gives \(\neg\Part_{N,Q}\). Surviving participation finally asks for a positive-scale coherent field readout; failure at every scale gives the Field face.

\paragraph{Branch-family obligation 2.}
\emph{Local translator family.} This family accounts for 3 current obligations.

This family tests local critical translation as a same-solution packet question.

\emph{Proof.} The translator has proof force only when it preserves the same Navier--Stokes pressure-viscosity participation on the transported packet. Loss of that participation is a Part-side failure; a surviving translator then moves to Field readout.

\paragraph{Branch-family obligation 3.}
\emph{Same-ledger family.} This family accounts for 4 current obligations.

This family asks whether the terminal branch is still charged to the same witness record.

\emph{Proof.} A terminal item on the same ledger enters the Field-certification test. A preterminal reflection or unpaid imported branch has no member conclusion until it supplies the same witness record, the same carrier, and the same participation tower.

\paragraph{Branch-family obligation 4.}
\emph{Class-law family.} This family accounts for 3 current obligations.

This family tests the pass-or-exit law against possible reopening branches.

\emph{Proof.} A reopening branch must either be an in-class nonsmooth third branch or break terminal CM entry, finite Part/Field exhaustion, or class-exit embedding. Every other challenge is absorbed by the installed pass branch, fail branch, or no-third-branch law.

\paragraph{Branch-family obligation 5.}
\emph{Local energy and elliptic family.} This family accounts for 3 current obligations.

This family supplies analytic support for source-wall and classification tests.

\emph{Proof.} Local energy and elliptic control matter when they select the terminal packet, verify pressure participation, or read out a positive field scale. The support is then consumed by Part and Field, or membership readout.
