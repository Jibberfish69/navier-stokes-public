# Active-Square Upper-Tail Carleson Blocked By Square-Source

## Target

The active-square upper-tail Carleson theorem is

```math
\int_I
\sum_{j\ge\max(N,k+C_1)}2^{-j}D_j(t)^2dt
\le
\varepsilon\nu2^{2k}|I|+C2^{-2\delta\max(N,k)}.
```

## Dependency

This follows by summing single-shell active-square estimates of the form

```math
\int_I2^{-j}D_j(t)^2dt
\le
\varepsilon\nu2^{2j}|I|+C2^{-2\delta j}.
```

The entrance-tail reserve is installed, while the required square-source estimate for `F_j` failed as a universal theorem by fixed-shell cubic amplitude scaling.

## Result

Active-square upper-tail Carleson remains blocked by the missing square-source theorem, or by an equivalent replacement such as active-shell amplitude gain or direct active-square control.