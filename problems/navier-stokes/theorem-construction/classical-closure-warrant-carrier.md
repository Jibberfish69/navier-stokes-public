# Mediating Object: `classical_closure_warrant_carrier`

## Object Identifier

- `object_id`: `classical_closure_warrant_carrier`
- `canonical_alias`: `same_surface_closure_warrant`
- `object_kind`: final theorem-warrant carrier
- `frontier_target`: `warrant-theorem-2.1`

## Purpose

This file isolates the exact local carrier needed to promote `Theorem 2.1
(Classical Closure)` on one theorem surface.

Its job is not to re-prove the already localized bridge packages. Its job is to
package them into one same-surface warrant object so the final theorem no longer
depends on a narrative jump from "all the bridge notes exist" to "therefore the
theorem is discharged."

## Domain

For fixed horizon `T > 0`, viscosity `\nu > 0`, and admissible smooth
divergence-free Euclidean data `u_0`, the domain consists of classical
Navier-Stokes solution carriers `u` on `[0,T) x \mathbb{R}^3` for which the
following local packets are all already available on the same theorem surface:

1. `S1` and `S2`: the scale-barrier package;
2. `M1` and `M2`: the named `Q(t)` bridge and post-monotone cascade carrier;
3. `K1`, `K2`, and `K3`: the compactness and nonlinear closure package;
4. `G1` and `G2`: the direct Euclidean fourth bridge and its tail-energy
   recertification arrow;
5. `C1` and `C2`: the same-surface and provenance audits.

## Codomain

The codomain is the warrant-level closure assertion:

```math
\mathcal{W}[u]
\Longrightarrow
\text{the Clay whole-space existence-and-smoothness statement with } f=0 \text{ follows.}
```

Here `\mathcal{W}[u]` means that all bridge outputs above belong to one fixed
classical theorem surface with no hidden branch switch and no theorem-critical
import from modified dynamics.

## Theorem Interface

The carrier discharges `warrant-theorem-2.1` precisely when it establishes the
following exact statement:

> If the scale-barrier, monotone-functional, compactness, and
> Euclidean fourth-bridge packages all hold on the active whole-space branch and the
> provenance audit excludes hidden modified-equation work or silent branch
> substitution, then Theorem 2.1 is promoted from a route theorem to a
> theorem-warrant candidate on that same surface.

This is the local carrier version of the combined closure sufficiency step.

## Theorem W

Assume:

1. `S1` and `S2` hold on the fixed Euclidean approximation surface;
2. `M1` and `M2` keep the named `Q(t)` bridge and its cascade handoff explicit on
   that same surface;
3. `K1`, `K2`, and `K3` close the nonlinear approximation/compactness package on
   that same surface;
4. `G1` and `G2` provide the direct Euclidean fourth bridge and the exact
   `4 -> 1` recertification arrow on that same surface;
5. `C1` and `C2` exclude hidden branch-switching, modified-equation imports, and
   silent hypothesis strengthening.

Then

```math
(S1 \wedge S2 \wedge M1 \wedge M2 \wedge K1 \wedge K2 \wedge K3 \wedge G1 \wedge G2 \wedge C1 \wedge C2)
\Longrightarrow
\text{the Clay whole-space existence-and-smoothness statement with } f=0 \text{ follows.}
```

This is the exact same-surface warrant promotion theorem that turns the route
theorem into the final classical closure warrant on the declared Euclidean
surface.

## Pullback Safeguard

The fourth bridge is now the direct Euclidean Route B package. So the warrant
carrier must make one additional promise explicit:

1. the Euclidean fourth bridge is consumed as the primary theorem bridge;
2. the tail-energy recertification arrow survives on the same theorem surface;
3. no extra coercive geometric structure is smuggled into the theorem claim;
4. any curvature-first language is provenance only unless explicitly revived.

The old pullback target remains a historical audit checkpoint only. It no
longer governs the live warrant carrier.

## Closure Mechanism

The warrant carrier closes the theorem in five steps:

1. `S1/S2` suppress dangerous-scale transfer.
2. `M1/M2` keep the `Q(t)` bridge and its cascade handoff explicit on the same
   surface.
3. `K1/K2/K3` preserve the nonlinear term on that carrier.
4. `G1/G2` force continuation control and feed back into the scale side on the
   same Euclidean surface.
5. `C1/C2` certify that the previous four steps belong to one theorem and not to
   a stitched family of near-miss arguments.

Only then may the theorem warrant say that the active whole-space Clay branch
has been proved on the declared classical surface.

## Local Source Anchors

- `theorem-construction/ontic-to-ns-sterile-theorem-map.md`
- `theorem-construction/combined-closure-sufficiency-lemma.md`
- `theorem-construction/main-theorem-draft.md`
- `theorem-construction/route-b-euclidean-closure-theorem.md`
- `theorem-construction/euclidean-scale-barrier-recertification-lemma.md`
- `theorem-to-warrant.yaml`

## Honest Status

`exact warrant carrier available; promotion pending`

This file now carries the correct same-surface final warrant object for Route B.
The remaining work is not inventing the warrant. It is promoting this warrant
through the theorem packet, review stack, and export manuscript without letting
stale control surfaces reintroduce old branch baggage.
