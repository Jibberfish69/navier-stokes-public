# Frontier-dissolution loop - Cycle 5 W12/W13 common reduction candidate (2026-07-08)

Status: pending partner refutation. This cycle opens from the confirmed
ten-wall frontier:

```text
W2, W3, W4, W5, W6, W7, W8, W9, W12, W13.
```

## Cross-wall attempt first

The required first cross-wall attempt is W12 == W13 as one tail/annular
whole-space boundary wall. This fails. W12 is the exterior dyadic tail /
whole-space \(H^s\) continuation bridge, while W13 is the residual annular
same-solution witness / Part-Field readout boundary.

## Candidate: W12 and W13 reduce to the W10/signed-height survivor

Installed W12:

```text
R3ExteriorDyadicSurvivorMaterialClockExclusion.A /
R3.WholeSpaceHsTailCompletion.A
```

and installed W13:

```text
ScaleUniformAnnularSupplierGoalResolution.A / AnnularLimitCMFaceLanding.A
```

are not identical local claims, but their current source status is the same
downstream conditional form:

```text
kind: silver-support-and-gold-counterpart
status: support-not-silver-blocker
```

The exact shared certificate is the material-record/full-action dependency.
W12 says the exterior survivor is excluded once finite positive variation of
the continuation-depth material record supplies finite total full-action mass.
W13 says finite rectified annular force-action variation is conditional on the
same finite positive variation of the continuation-depth material record, or an
equivalent bounded-below global same-material storage. W10 has already been
confirmed as reduced to the surviving Gold signed-height object, and the
live-edge W10 row says the finite full material-history action bounds that
continuation-depth material record.

So W12 and W13 are downstream support/gold-counterpart consumers of the same
surviving written object:

```text
SignedCriticalHeightSignPersistenceOrPeakBound.A
via the confirmed W10 full-material-clock reduction.
```

Certificate to attack. Refute by exhibiting residual W12 or W13 content not
discharged by finite full material-history action / bounded continuation-depth
material record after W10 is reduced to the signed-height survivor. An attack
must name a tail-specific or annular-specific clause that remains independent
after the full-action material record is supplied.

DELETED: none; W12/W13 common reduction is pending partner refutation.
CERTIFICATE: W12 and W13 are both `support-not-silver-blocker` / `silver-support-and-gold-counterpart` obligations whose live statements are conditional on finite positive variation of the continuation-depth material record or equivalent global same-material storage, while W10's confirmed survivor supplies the full material-history action that bounds that record.
REMAINING: W2, W3, W4, W5, W6, W7, W8, W9, W12, W13.
