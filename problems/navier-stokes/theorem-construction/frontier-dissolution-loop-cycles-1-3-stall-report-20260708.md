# Frontier-dissolution loop — stall report after provisional Cycles 1–3 (2026-07-08)

**Status: STALL REPORT, mandated by cycle-law 6 (three cycles, zero CONFIRMED deletions). Nothing paid; nothing closed. All cycle work below is provisional because the install field was never owner-confirmed.**

## The blocking condition (procedural, first)

The `Frontier at install (owner-confirmed)` field is unfilled. A 14-wall list (W1–W14, drawn from the spine's retained-branch burdens, the frozen-core source frontier, and the 07-08 census) was proposed to the owner and to the partner lane; the partner correctly holds it as proposed-only. No deletion is payable until the owner returns `confirm as-is` or an edited list. This blocks payment on every result below.

## The sticking wall, physical sentence first

W1 (`AlignedRowShedsRecordOneSense.A`). What the fluid does: the field's instantaneous answer to a unit of m=0 meridional current at the imprint front is one exactly aligned Hessian at the core, and its pairing with the record is a single nonzero constant — the row is never silent. Computed both ways: the **gathering** current (caps→belt, downscale transfer — what a climbing record must run) forces the row that **eats** the record; the **localization/shedding** current (belt→caps, the residual of an outward-decaying imprint) forces the row that **feeds** it. What is stuck is not physics but adjudication: which orientation the gated log-chain normal form assigns to the climbing history — that single convention check decides whether W1 as worded is refuted or inverted-and-confirmed (with the brake landing on the climb, the stronger outcome).

## Attempts made (provisional Cycles 1–3)

1. **Cycle 1 — P1 sharpening.** Center-Hessian channel selection: the record-coupled shedding channel is ℓ=2 alone; the two-ratio proportionality certificate collapsed to one exact sign; the ℓ=4 wedge decoupled at leading order. Filed (§3a of the two-functional note); sent to partner with four attack surfaces.
2. **Cycle 2 — the ℓ=2 constant executed** (exact fractions, thin-shell model): H₁(0) = A·diag(−1,−1,2), A = +(24/35)·a v₂/R², angular integral −16/105 ≠ 0, row–record pairing 3tr(S²H₁) = +(432/35)a³v₂/R². Non-degeneracy proved in-channel (thin-shell model); sense inverted vs W1's wording. Filed (§3b); sent to partner with the three-line check chain; partner acknowledged "refuted-or-reworded W1, not a confirmed deletion" and is posting its gate result.
3. **Cycle 3 — P2 checked against the gated text and CORRECTED.** ZPF.21a as gated is `TotalStrainVorticityExchangeRetention.A` — a same-packet, rung-local no-waste conversion of the signed exchange ledger (ZPF.21). It does NOT state limit-passage. Therefore P2's consumer chain as first sent (W6 consumed by W1+W2) is wrong; the corrected chain is **W6 consumed by W1(adjudicated) + W2 + W7** (`RecordAdmissionClosedness.A` supplies the passage of row-membership through the self-similar limit). The remaining analytic content of the relay-endpoint Liouville localizes to the closedness face.

## The missing exact facts (what would unstick each item)

1. **Owner confirmation** of the install list (`confirm as-is` or edited). Procedural; blocks all payment.
2. **The normal-form sense adjudication for v₂** against the gated cubic/log-chain conventions: which orientation is the climb. One convention check; decides W1 refuted vs inverted-confirmed.
3. **Partner verdict on the ℓ=2 integral chain** (R1 of the sent refutation targets) and on thin-shell fidelity (R2).
4. **The closedness face** (W7) stated as the limit-passage clause the corrected P2 consumes — currently gated inside the compactness/no-arbitrage package; needs its row-membership form made explicit.

## Standing per the goal law

DELETED: none. CERTIFICATE: exact ℓ=2 row–current constant (A = +(24/35)av₂/R², integral −16/105) + the P2 correction (consumer chain requires W7). REMAINING: 14 proposed walls, unconfirmed. Loop STOPPED per cycle-law 6 pending the owner's install confirmation and the partner's refutation verdicts.
