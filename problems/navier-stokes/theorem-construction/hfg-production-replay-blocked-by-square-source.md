# HFG Production Replay Blocked By Square-Source

## Target

The intended replay is

```math
HFG
\Rightarrow Part+Field+\mathfrak A_{N,Q}
\Rightarrow H1+H2+H6+ORIGIN.Retain
\Rightarrow LCI.A+CJ.A_{low}.7+FCI.5f+OFP.A+CFI.A+End_{NS}.
```

## Current status

The HFG rerun requires weighted carrier closure. Weighted carrier closure remains blocked by LPAS / active-square Carleson. The active-square route through square-source control is blocked because the universal square-source estimate for `F_j` fails by fixed-shell cubic amplitude scaling.

## Result

HFG and the production/six-burden replay cannot be promoted from this branch at the current state.

The exact live replacement input is one of:

```math
\text{square-source estimate for }F_j\text{ under active-interval normalization,}
```

```math
\text{active-shell amplitude theorem,}
```

```math
\text{direct active-square Carleson theorem,}
```

or

```math
\text{direct LPAS theorem.}
```