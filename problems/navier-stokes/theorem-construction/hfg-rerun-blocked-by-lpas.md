# HFG Rerun Blocked By LPAS

## Rerun target

The HFG target is

```math
\sum_{j\ge0}2^{3j/2}\|\Delta_j\omega\|_{L_t^1L_x^2}<\infty.
```

## Precondition

The repaired cascade needs the weighted carrier closure

```math
\int_0^T(\Lambda_N+\Theta_N^\sharp)E_Ndt
\le
\varepsilon\nu\int_0^TD_Ndt+C2^{-2\delta N}.
```

## Current status

The weighted carrier closure is blocked by the active-square far-corona theorem

```math
\int_0^TLPAS_N(t)dt
\le
\varepsilon\nu\int_0^TD_N(t)dt+C2^{-2\delta N}.
```

The direct LPAS attempt left this as an open lower-prefix active-square theorem.

## Result

HFG cannot be rerun from this route until LPAS/FC or an equivalent active-square far-corona closure is installed.