# HFG Rerun Blocked By Shell Amplitude

## Target

The HFG rerun target is

```math
\sum_{j\ge0}2^{3j/2}\|\Delta_j\omega\|_{L_t^1L_x^2}<\infty.
```

## Precondition

The repaired cascade requires the weighted carrier closure

```math
\int_0^T(\Lambda_N+\Theta_N^\sharp)(t)E_N(t)dt
\le
\varepsilon\nu\int_0^TD_N(t)dt+C2^{-2\delta N}.
```

## Current status

Weighted carrier closure remains blocked by active-square Carleson or direct LPAS. The shell-amplitude gain route gives only a conditional implication and the gain itself is not installed.

## Result

HFG cannot be rerun from this branch until active-square Carleson, shell-amplitude gain, or direct LPAS is proved.