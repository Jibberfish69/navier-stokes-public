# Localized Projector Coarse Route Reduces to Force-Ceiling Plus Familywise Summation

## Status

Active theorem-facing reduction note for the localized-projector exact-potential
branch.

Role: identify the sharp remaining wall on the coarse localized-projector route
after overlap has been absorbed into the same familywise source-summation object
already isolated in the older exact-potential synchronization packet.

## Purpose

Three reductions are now on disk:

1. normalized localized kernels plus subordination price coarse overlap by the
   retained-cover counting term

```math
\sum_k |I_k|\,\mu_J^{pair}(\mathcal J_k);
\tag{LCR.0}
```

2. that same counting term feeds the same familywise summability theorem already
   isolated in `EP.10aj`;
3. the entrance side is already reduced on the source packet to the local
   projective entrance ledger by `EP.10ab`.

So the coarse localized-projector route should now collapse to the same two
remaining theorem packets already named in `EP.10af`.

## Source Match

In
[hwy-instability-attack-packet.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/hwy-instability-attack-packet.md),
the source reductions proceed as follows:

1. `EP.10ab4` reduces the entrance side to the installed local projective
   entrance ledger:

```math
\sum_\ell
\bigl(\mathbf B_{\ell,\mathrm{proj}}^{ent}+\kappa_\ell\bigr)\,
\nu_J(\Lambda_\ell)
+
\frac{1}{\gamma_\ast^2}
\sum_{(\ell,\ell')\in\mathcal O_\Lambda}
\Bigl(
\operatorname*{ess\,sup}_{\Lambda_\ell\cap\Lambda_{\ell'}}
\varepsilon_{\ell\ell'}^{sur}
\Bigr)^2
\nu_J(\Lambda_\ell\cap\Lambda_{\ell'})
\le
C\bigl(\mathcal E_J^{obs}(I)+\mathrm{Err}_{J,\mathrm{pair}}(I)\bigr);
\tag{LCR.1}
```

2. `EP.10ac` says the first truly new debts are now:

```math
1.\ \texttt{windowwise drift pricing law};
\qquad
2.\ \texttt{overlap-surrogate family sum};
\qquad
3.\ \texttt{familywise summability theorem}.
\tag{LCR.2}
```

3. the coarse overlap reductions `EP.10ai`-`EP.10aj` absorb the second item into
   the same familywise summability object.

4. `EP.10af` then records the remaining exact debts as:

```math
1.\ \texttt{one local force-ceiling theorem};
\qquad
2.\ \texttt{one familywise observation/error summability theorem}.
\tag{LCR.3}
```

So the coarse localized-projector route is not merely “overlap is cheaper.” It
lands exactly on the `EP.10af` two-packet wall.

## Corollary `LCR.A` (coarse localized-projector route lands on the `EP.10af` wall)

Assume:

1. the localized-projector coarse overlap reductions of
   [localized-projector-coarse-overlap-is-familywise-summability-consumer.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/localized-projector-coarse-overlap-is-familywise-summability-consumer.md);
2. the installed localized entrance reduction to explicit entrance scalars /
   local projective entrance ledgers;
3. the same source-side projective synchronization framework as in
   `EP.10ab`-`EP.10af`.

Then the remaining genuinely new coarse localized-projector debts are exactly:

```math
\boxed{\texttt{one local force-ceiling theorem}}
\quad+\quad
\boxed{\texttt{one familywise source-summation theorem}}.
\tag{LCR.4}
```

### Proof

By the cited coarse overlap notes, the overlap contribution is absorbed into the
same familywise summability theorem. By `EP.10ab`, the entrance side is already
reduced to the installed local projective entrance ledger. The only unreduced
new term is the local drift price `\kappa_\ell`, i.e. the local force-ceiling
theorem, together with the familywise summability theorem pricing the resulting
windowwise source sum. This is exactly the content of `EP.10af`. ∎

## Interpretation

The coarse localized-projector route is now pinned to the sharpest source-faithful
wall visible on disk:

```math
\boxed{
\texttt{coarse overlap}
}
\Longrightarrow
\boxed{
\texttt{same familywise source-summation object}
}
\Longrightarrow
\boxed{
\texttt{local force-ceiling theorem}
+
\texttt{familywise source-summation theorem}
}.
\tag{LCR.5}
```

So on the coarse route, overlap no longer survives even as an independent scalar
geometry burden. The real remaining theorem content is exactly the same two-part
wall already named in the older exact-potential synchronization packet.

The sharper current-packet refinement is recorded separately in
[localized-projector-coarse-route-bypasses-force-ceiling-via-angular-forcing-ledger.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/localized-projector-coarse-route-bypasses-force-ceiling-via-angular-forcing-ledger.md):
on the installed localized-projector packet, `AFR.A/B/G` already bypass the
separate `EP.10ae` force-ceiling theorem, so only the familywise source-
summation theorem remains genuinely new.

This note does **not** claim that those two packets are already closed on the
current localized-projector branch. It only records that the coarse route has
now been reduced to them.
