# ActiveDirectionEquipartition.SelectedAngularMixingAttempt

## Status

Failed direct discharge.  This note tests source-weighted angular mixing as the next route after selected-window sign comparison.

## Target

A useful theorem would state that selected active directions cannot concentrate only on expanding eigendirections:

```math
\sum_{P\in\mathcal F_N}\int_{Q(P)}
|w_{j_P}|^2[e_{j_P}\cdot S^{loc}_{<j_P}e_{j_P}]_+dxdt
\le
Drain_{parent}+Loss_{legal}+o_N(1).
```

This is `SourceWeightedTerminalAngularDepletion.A` in angular-mixing form.

## Tests

### Eigenframe rotation

If the expanding eigenspace rotated fast relative to the active direction, then positive alignment would lose coherence.  Current inputs do not provide source-weighted eigenframe BV control on the selected terminal packet family.

### Vorticity-direction coherence

Coherence can prevent geometric concentration in some regimes.  The current route does not generate coherence strong enough to force sampling of contracting strain directions on the selected positive packets.

### Beltrami / anti-Beltrami depletion

Beltrami-style structure can deplete nonlinear stretching if the selected terminal packets enter that class.  No installed theorem produces that class from original smooth data or from `B_ASAC` on the terminal layer.

### ASAC / B_ASAC

ASAC pays active alignment in the signed/current presentation.  `B_ASAC` gives terminal equality/tangent organization.  Neither gives source-weighted angular equipartition for the one-sided positive carrier.

## Result

The route requires a new theorem:

```math
\boxed{SourceWeightedEigenframeMixing.A}
```

or equivalently

```math
\boxed{TerminalActiveDirectionAntiAlignment.A.}
```

## Verdict

Angular mixing remains open.  The next alternative should target a dynamical reason why persistent expansion alignment must create measurable amplitude gain or temporal thickness.