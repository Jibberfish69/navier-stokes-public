# ActiveShellSourceNormalize.OnwardCommutatorTailSplit

## Status

Continuation from `ActiveShellSourceNormalize.A`.  The branch reduces to a two-part source-wall split and remains open.

## Weighted commutator side

The bare separated commutator vanishes in the declared frequency geometry.  The live terminal record is the true lifted weighted remainder after shell weights, cutoffs, projections, and lifted summation:

```math
G_N^{lift,\sigma}=\sum W_{j,\ell}^{\sigma}R_{j,k,\ell}^{lift}.
```

Its leading packet form contains the weighted strain interaction

```math
2^{2j}\sum_P\langle S_k(x_P,t)e_P,e_P\rangle |c_P(t)|^2.
```

After ASAC, the active-alignment and positive receiver pair-weight parts are paid.  The remaining signed-current item is:

```math
\boxed{TerminalSignedSaturation.A.}
```

This is the signed partner / donor-sink depletion theorem for terminal source-current edges.

## Residual-tail side

Taking absolute values gives the positive carrier and a residual tail.  The required residual-tail estimate has active-square strength:

```math
\int_I\sum_{j\ge k}2^{-j}D_j(t)^2dt
```

on active terminal windows.  The installed full tail-square reserve controls cumulative tail energy; it does not control shellwise active-square density at this strength.

The residual-tail side therefore reduces to:

```math
\boxed{ActiveSquareUpperTailCarleson.A}
```

or equivalently:

```math
\boxed{ActiveShellAmplitudeGain.A.}
```

A sufficient shell form is

```math
2^{-j}D_j(t)^2\le \varepsilon\nu D_j(t)+r_j(t)
```

with summable reserve on the terminal active family.

## Current split

The source-normalization frontier is now:

```math
\boxed{
TerminalSignedSaturation.A
\quad\text{and}\quad
ActiveShellAmplitudeGain.A.
}
```

The first pays signed partner loss.  The second pays shellwise active-square residual tail.

## Conditional chain

If both are supplied, then

```math
TerminalSignedSaturation.A+ActiveShellAmplitudeGain.A
\Longrightarrow
WeightedCommutatorDefect.A+ResidualTailControl.A
\Longrightarrow
ActiveShellSourceNormalize.A
\Longrightarrow
UniformTemporalSourceIntegrability_p.A
\Longrightarrow
SourcePulseExclusion.A.
```

## Verdict

The branch remains open.  The next visible atoms are terminal signed-partner saturation and active-shell amplitude gain.