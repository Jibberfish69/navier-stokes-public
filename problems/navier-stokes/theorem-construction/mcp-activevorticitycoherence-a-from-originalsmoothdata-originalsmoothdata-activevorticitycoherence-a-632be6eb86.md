# ActiveVorticityCoherence.A from OriginalSmoothData

## Target

Prove

```math
OriginalSmoothData\Longrightarrow ActiveVorticityCoherence.A.
```

The desired theorem says terminal bad active windows carry enough vorticity-direction coherence to deplete their positive active source.

## Direct test

The relevant stretching carrier is

```math
\omega\cdot S\omega.
```

A Constantin-Fefferman style mechanism can deplete stretching when a suitable vorticity-direction modulus is available. Current original-data inputs give smoothness on compact preterminal intervals, while they do not supply a uniform terminal direction modulus on the shrinking active windows selected by the source-pulse branch.

Even under a direction-coherence hypothesis, the bridge must control the exact route carrier. The active source packet also includes transverse strain eigenvalues, low-vorticity/high-strain portions, shellwise mixed bilinear dissipation, and weighted lifted remainders after cutoffs and one-sided weights. Direction coherence of `\omega/|\omega|` alone does not cover all these carriers.

## Result

`ActiveVorticityCoherence.A` is a plausible analytic mechanism, with two missing pieces:

```math
OriginalSmoothData\Longrightarrow \text{terminal vorticity-direction modulus},
```

and

```math
\text{direction modulus}\Longrightarrow\text{exact active source-packet depletion}.
```

## Verdict

`ActiveVorticityCoherence.A` remains open and route-equivalent to the source-pulse wall at current resolution.