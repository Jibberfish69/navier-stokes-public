# AntiBeltramiCarleson.A from OriginalSmoothData

## Target

Prove

```math
OriginalSmoothData\Longrightarrow AntiBeltramiCarleson.A.
```

Equivalent helical language:

```math
HelicalCoherenceDeplete.A:
\quad
\text{terminal active windows become Beltrami/helical enough to kill active source.}
```

## Direct test

A pure Beltrami or single-helicity packet can have strong cancellation. Its projected self-interaction can collapse to a pressure gradient or to a non-active contribution.

That cancellation example does not give a theorem for arbitrary terminal active windows. The live branch must rule out active packets that remain non-Beltrami enough to feed source while evading signed reserve and active-square ledgers.

OriginalSmoothData gives no theorem forcing selected same-fluid terminal active packets into Beltrami or single-helicity alignment.

Helical decomposition alone also fails as a sign theorem. Mixed helical interactions can transfer energy between shells, and current route inputs have no quantitative bridge making the anti-Beltrami defect Carleson-small on `Jump_avg` bad windows.

## Result

`AntiBeltramiCarleson.A` remains a possible proof language only. Its missing theorem is

```math
OriginalSmoothData\Longrightarrow
\text{terminal anti-Beltrami defect is small on bad active windows.}
```

## Verdict

The Beltrami/helical branch remains open and route-equivalent to `SourcePulseExclusion.A` at current resolution.