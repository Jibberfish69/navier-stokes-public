# BASACNewAlternativeMechanismExhaustion.A

## Status

Failed to find an additional installed production mechanism.  The branch is completed at current resolution by obstruction placement: `B_ASAC^closed` lands as a selected Zeno terminal atom Field-face diagnostic.

## Starting point

The current survivor is

```math
B_{ASAC}^{closed}.
```

It has finite local `L^1_s` native positive source mass and remains compatible with the terminal marginal model

```math
g_m(s)=m\mathbf 1_{(-1/m,0]}(s)
\rightharpoonup\delta_{s=0}.
```

The audited doors already include temporal anti-concentration, branch entropy, transported no-flux, rigid subclass production, pressure/Leray tether, and localized trace-jump exclusion.

## Candidate search

### Local energy trace

Candidate:

```math
BASACLocalEnergyTraceNoPositiveJump.A.
```

Collapse: finite `L^1_s` source mass gives a bounded-variation primitive.  Bounded variation allows terminal jumps.  This route returns to terminal anti-atom / super-`L^1_s` residence.

### Pressure carrier trace / elliptic time smearing

Candidates:

```math
PressureCarrierTrace.A,
\qquad
EllipticTimeSmearing.A.
```

Collapse: pressure recovery is elliptic in space at each time slice.  It gives spatial partner structure and no time smoothing across the terminal face.  This route returns to `PressureTimeFaceSpread.A`.

### Pure pressure-source singular residue

Candidate:

```math
PurePressureSourceSingularResidueLiouville.A.
```

Collapse: classification gives a label for a residue class.  A closure still needs a production theorem into that class and a Liouville theorem excluding terminal time-face concentration.

### Pressure adjoint fork

Candidate:

```math
BASACNewAlternativeCascadePressureAdjointFork.A.
```

Collapse: the adjoint identity contains the terminal source pairing as the live term.  Pressure partner cancellation survives at the signed graph level, while one-sided terminal positive selection still needs partner retention, legal/collar exit, donor depletion, or time-face spread.

## Mechanism audit table

```text
local energy trace -> terminal anti-atom / super-L1 residence
pressure carrier trace -> pressure time-face spread
pure pressure singular class -> generated rigid class plus Liouville
pressure adjoint fork -> signed partner / donor / legal / time-face spread
pressure/Leray tether -> pressure time-face spread
transported no-flux -> time-face anti-atom
branch entropy -> source-parent reserve
rigid subclass -> production into anti-atom subclass
temporal route -> super-L1 residence
```

Every candidate returns to one of the already isolated production atoms.

## Completed state

The branch is completed at current theorem-surface resolution as

```math
B_{ASAC}^{closed}
\leadsto
Field_{N,r,Q}\text{-face selected Zeno terminal atom diagnostic.}
```

The forward positive-supplier theorem remains open at the production level.  A future proof must add a genuinely new theorem of one of these exact types:

```math
PressureTimeFaceSpread.A,
\quad
BASACLocalEnergyTraceNoPositiveJump.A,
\quad
PurePressureSourceSingularResidueLiouville.A,
\quad
ScaleNormalizedBranchEntropyReserve.A,
\quad
UniformTemporalSourceIntegrability_{p,B_ASAC}.A,
\quad
ProductionIntoRigidBASACTimeFaceSubclass.A.
```

## Verdict

No additional installed mechanism closes the source-wall supplier.  The run is completed as a hard obstruction placement: `B_ASAC^closed` is the canonical Field-face obstruction class for the current route, and all searched alternatives return to named production atoms.