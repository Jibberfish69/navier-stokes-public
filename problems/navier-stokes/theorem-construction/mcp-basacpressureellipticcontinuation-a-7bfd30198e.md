# BASACPressureEllipticContinuation.A

## Status

Failed target-local closure from installed inputs.  The pressure/elliptic continuation sharpens the next possible pressure theorems, while the `B_ASAC` terminal atom remains the canonical target-local obstruction.

## Starting point

The survivor is

```math
B_{ASAC}^{closed},
```

already identified as a selected zero-radius terminal Zeno residue, hence
Part-side when the same original-NS participation record remains and
true terminal packet/window evidence only when that record is absent, rather than a
retained `Field_{N,r,Q}` packet.

The pressure fork seeks a production theorem of one of two kinds:

```math
PressureSourceAC.A / LocalizedLeraySourceTether.A_{ind},
```

or

```math
PurePressureSustainResidueLiouville.A.
```

## Pressure/source absolute continuity

The desired carrier relation is

```math
\mu_*^{press-sus}\ll \mu_*^{src}+\mu_*^{legal,src}.
```

Pressure Poisson structure gives

```math
-\Delta p=\partial_i\partial_j(u_i u_j),
\qquad
\nabla^2p=\mathcal R\mathcal R(\nabla u\otimes\nabla u).
```

This supplies spatial partner structure and Calderon-Zygmund bounds.  It does not dominate the pressure quadratic-gradient carrier by the native same-fluid source-current carrier after localization, one-sided terminal weights, and positive selection.

The missing theorem is

```math
QuadraticDefectSourceDomination.A.
```

## Localized Leray tether

A finite-packet Leray tether would anchor every retained pressure lobe to a same-fluid native source edge.  After terminal positive selection, this requires three independent gates:

```math
TerminalLerayOrientation_{ind}.A,
\quad
TerminalLerayCommutatorLegal_{ind}.A,
\quad
TerminalLeraySaturation_{ind}.A.
```

The installed Leray identity is algebraic before selection.  The one-sided terminal packet ledger needs these gates.  Importing the existing terminal skew/source-current ledger would make the fork circular.

Thus finite-packet Leray anchoring remains open.

## Pressure time-face spread and elliptic smearing

Pressure recovery is elliptic and time-slice based.  It can spread a pressure lobe across space at a fixed time.  The `B_ASAC` obstruction is concentration in time.  Therefore elliptic recovery supplies no terminal time-face anti-atom estimate.

The pressure spread route returns to temporal residence or pressure-source absolute continuity plus an independent spread estimate:

```math
PressureTimeFaceSpread.A.
```

## Pure pressure-sustain residue

The residual singular pressure carrier is

```math
\mu_*^{press\perp src}\perp(\mu_*^{src}+\mu_*^{legal,src}).
```

Existing pressure legal ledgers cover cutoff, projector, harmonic collar, off-family tail, geometric collar, and boundary pressure work.  The retained pure pressure-sustain residue is interior after those exits are removed.

Two possible theorems would close this fork:

```math
TerminalPressurePureStrainLegal.A,
```

or

```math
PurePressureSourceSingularLiouville.A:
\mu_*^{press\perp src}=0.
```

Neither theorem is installed by the current route inputs.

## Verdict

The pressure/elliptic continuation is exhausted at the following exact future targets:

```math
TerminalLerayOrientation_{ind}.A
+TerminalLerayCommutatorLegal_{ind}.A
+TerminalLeraySaturation_{ind}.A,
```

```math
TerminalPressurePureStrainLegal.A,
```

or

```math
PurePressureSourceSingularLiouville.A.
```

Absent one of these, `B_ASAC^closed` remains the canonical terminal Pack-failure / source-accounting obstruction.
