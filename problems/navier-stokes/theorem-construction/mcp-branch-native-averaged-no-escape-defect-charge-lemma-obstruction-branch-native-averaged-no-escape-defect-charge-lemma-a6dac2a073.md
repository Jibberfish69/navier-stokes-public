# Branch-native averaged no-escape defect-charge lemma — failed attempt

Target: prove a direct contradiction from `Jump_avg` without using `Field_avg`.

A direct lemma would take `Jump_avg` to a persistent averaged bad set `B_epsilon^Phi`, attach a nonnegative defect charge `D_k` to each terminal scale or scheduler cell, and prove a lower bound whose scale-sum contradicts finite energy or the averaged source budget.

The available formal skeleton is:

`Jump_avg -> B_epsilon^Phi persists`,
`B_epsilon^Phi persists -> D_k >= d0` on a terminal subtail,
`sum_k D_k <= C(E0 + source)` from energy, incompressibility, and pressure/source estimates.

The contradiction follows only if the lower-bound cells are quantitatively separated or summable with a divergent lower total. Current data supply persistence language, while the needed orthogonality/separation or flux lower-bound theorem is absent.

Result: the branch-native no-escape lemma remains open. The exact missing input is a quantitative defect-charge lower bound plus a finite-energy upper bound on the same charge.