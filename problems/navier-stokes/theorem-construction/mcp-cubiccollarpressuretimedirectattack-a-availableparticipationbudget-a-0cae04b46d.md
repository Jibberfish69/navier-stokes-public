# CubicCollarPressureTimeDirectAttack.A

Status: direct attack complete; no unconditional closure from installed inputs.  The participation law is respected throughout: the cubic collar term and pressure-time term are treated as coordinates of the same transported pressure-viscosity-incompressibility-velocity material packet, not as detached scalar errors.

## Target terms

For the gauge-reduced transported annular packet,

```math
D_tU=\nabla\cdot T^\sharp,
\qquad
T^\sharp=-PI+2\nu S(U),
\qquad
D_t\phi=0,
```

the active terms are

```math
S_5(t)=\int |U|\,|\nabla U|^2|\nabla\phi|\,dx
```

and

```math
S_2(t)=\int |U|\,|\nabla(-\Delta)^{-1}\mathcal E_P|\,|\nabla\phi|\,dx,
```

where \(\mathcal E_P\) is generated by material differentiation of

```math
-\Delta P=\partial_iU_j\partial_jU_i.
```

These are not external sources.  They are the collar/pressure-time coordinates of the same PLS transaction.

## 1. Interpolation attack on the cubic collar term

Ignoring the bounded collar coefficient first,

```math
\int |U|\,|\nabla U|^2
\le
\|U\|_{L^6}\|\nabla U\|_{L^2}\|\nabla U\|_{L^3}.
```

Sobolev gives

```math
\|U\|_{L^6}\lesssim \|\nabla U\|_{L^2}.
```

Thus

```math
\int |U|\,|\nabla U|^2
\lesssim
\|\nabla U\|_{L^2}^2\|\nabla U\|_{L^3}.
```

Interpolating

```math
\|\nabla U\|_{L^3}
\lesssim
\|\nabla U\|_{L^2}^{1/2}\|\nabla^2U\|_{L^2}^{1/2}
```

gives

```math
S_5(t)\lesssim D(t)^{5/4}H(t)^{1/4}
```

with

```math
D(t)=\|\nabla U(t)\|_2^2,
\qquad
H(t)=\|\nabla^2U(t)\|_2^2.
```

Young's inequality yields

```math
S_5(t)\le \varepsilon H(t)+C_\varepsilon D(t)^{5/3}.
```

This is worse than the desired energy-class bound.  Energy supplies \(\int D(t)dt<\infty\), not \(\int D(t)^{5/3}dt<\infty\).  The interpolation route therefore requires a continuation-strength higher-gradient clock.

The local-energy spacetime exponents do not fix this.  \(U\in L^{10/3}_{t,x}\) and \(\nabla U\in L^2_{t,x}\) do not imply \( |U||\nabla U|^2\in L^1_{t,x}\) with scale-uniform terminal annular control.  The missing factor is exactly higher integrability of the strain/gradient clock.

Verdict: no interpolation improvement beats the \(D^{5/4}\)-type obstruction from installed inputs.

## 2. Local depletion attack

The transported local-energy identity gives

```math
{d\over dt}M_\phi+2\nu\int\phi|S|^2dx=-\int U\cdot T^\sharp\nabla\phi\,dx.
```

This pays interior strain and attaches center storage to annular Cauchy stress-work.  It does not control the cubic collar term because \(S_5\) contains an additional factor of \(|U||\nabla\phi|\) multiplying \( |\nabla U|^2\).  Local depletion gives

```math
\int\phi|S|^2
```

and signed annular work, not

```math
\int |U||\nabla U|^2|\nabla\phi|.
```

To turn depletion into \(S_5\)-control, one needs either a collar \(L^\infty\) or scale-critical \(L^3\)-type bound for \(U\), or a signed cancellation.  Both are continuation/full-clock strength in the current packet.

Verdict: local depletion attaches the term but does not dominate it positively.

## 3. Annular payback attack

The annular flux

```math
F_\phi^\sharp=\int U_iT^\sharp_{ij}\partial_j\phi\,dx
```

has material derivative

```math
{d\over dt}F_\phi^\sharp
=
\int (\nabla\cdot T^\sharp)_iT^\sharp_{ij}\partial_j\phi
+
\int U_i(D_tT^\sharp_{ij})\partial_j\phi
-
\int U_iT^\sharp_{ij}(\partial_jU_k)(\partial_k\phi).
```

The cubic collar and pressure-time terms are inside this same signed derivative.  Therefore annular payback is real: the active terms are not independent future entropy sources.

However, \(F_\phi^\sharp\) is signed.  Endpoint control gives net payback, not total variation.  The terminal clock requires rectified action.  Bounding

```math
\int |(F_\phi^\sharp)'|dt
```

from endpoint values would require a no-recirculation/finite-variation theorem.  That theorem is equivalent to the missing full PLS action estimate unless a new sign mechanism is supplied.

Verdict: annular payback gives attachment and signed storage, but not rectified full-clock control.

## 4. Pressure-time elliptic service attack

Material differentiation of the pressure law produces

```math
\mathcal E_P
=
\partial^2T^\sharp\,\partial U
+(\partial U)^3
+\partial U\,\partial^2P
+\Delta U\,\nabla P
```

schematically, with the precise expression inside the pressure-time service surface.  Calderon-Zygmund estimates keep \(\nabla(-\Delta)^{-1}\mathcal E_P\) in the pressure tower, but they do not lower the order to energy.  The source contains the same cubic-gradient and pressure-Hessian service that controls selected strain growth.

The pressure term must therefore be paired with strain/vorticity/collar partners before rectification.  In the strain equation,

```math
D_tS+S^2+\Omega^2+\nabla^2P=\nu\Delta S,
```

selected positive strain growth is governed by the same packet quantity

```math
|\Omega e|^2-\langle e,\nabla^2P e\rangle
```

plus damping and viscosity.  This shows the correct PLS ontology: pressure-time service is the internal incompressibility response to strain/vorticity, not a separate forcing.  It does not by itself produce a positive bound after absolute values.

Verdict: pressure-time service requires signed partner retention or a full material pressure clock.  Absolute elliptic estimates alone are continuation-strength.

## 5. Participation-law conclusion

Respecting the participation law improves the ontology but not the estimate by itself.  It proves:

```math
S_2+S_5\quad\text{are same-material PLS action coordinates.}
```

It does not prove:

```math
\int_0^{T^*}(S_2(t)+S_5(t))dt<\infty
```

from energy, initial smooth entrance, linear viscous damping, fixed-threshold cascade, or scalar shell budgets.

The only noncircular route left by this direct attack is signed same-packet cancellation before absolute values.  The precise target is:

```math
\boxed{
\text{construct a signed PLS cross-storage }X_{cubic/press,N}
\text{ such that}
}
```

```math
S_2(t)+S_5(t)+dX_{cubic/press,N}(t)
\le
\varepsilon dD_N^{vis}+\varepsilon dD_N^{rad}+dR_N^{legal}(t)
```

as part of the full \(d\mathcal B_N+c_Nd\Omega_N^{PLS}\le dR_N^{legal}\) budget law.

## Final verdict

No cancellation, interpolation improvement, local depletion estimate, or annular payback mechanism currently beats the \(D^{5/4}\)-type obstruction after rectification.  The full PLS structure is necessary because it keeps the cubic and pressure-time terms in one same-material transaction.  But the present installed inputs still need a signed pre-rectification cross-storage/no-recirculation theorem to turn that transaction into a finite positive clock.