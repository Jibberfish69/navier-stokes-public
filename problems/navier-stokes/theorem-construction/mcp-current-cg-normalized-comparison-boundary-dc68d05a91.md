# Current-CG Normalized Comparison Boundary

## Status

Failed as an unconditional proof from current canonical surfaces.

## Current-CG comparison target

For the current-CG branch,

```math
Err_{cmp}^{frame}
=
\left\|
W_J^{coc}\Pi_{N_a}(W_J^{coc})^*
-
\mathcal C_J^{CG}
\right\|_{op}.
```

The required normalized estimate is

```math
Err_{cmp}^{frame}/\gamma_J\to0.
```

## Available implication

With the retained simple-top gap floor `gamma_J`, Davis-Kahan gives

```math
\|P_{al}-P_J^{CG}\|_F
\le C_{DK}
Err_{cmp}^{frame}/\gamma_J.
```

Therefore the normalized comparison implies BridgeA.

## Boundary

The aligned-reference convention gives zero tensor defect. The current-CG convention requires the reference-identification source theorem

```text
Err_cmp^frame/gamma_J -> 0.
```

Current canonical surfaces name this source theorem and supply the downstream projector conversion. They leave the normalized current-CG comparison estimate as live source work.

## Next queue item

```text
in-plane anisotropy drain
```
