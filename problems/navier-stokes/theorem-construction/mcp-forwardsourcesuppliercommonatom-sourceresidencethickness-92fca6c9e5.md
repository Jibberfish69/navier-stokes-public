# ForwardSourceSupplierCommonAtom.SourceResidenceThickness

## Status

Failed direct closure attempt.  This note isolates the common atom that would close both stronger forward-supplier branches:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

The two available forward presentations are:

```math
ScaleCriticalTreeCarleson.A
```

and

```math
TemporalNonAtomicSource.A
```

by a genuinely non-Carleson proof.

## Common atom

The common closure theorem is:

```math
\boxed{
PositiveViscositySourceThickness.A:
\quad
\mu_m^{src}(B_R\times[-\varepsilon,0])
\le C_R\varepsilon^{\theta}+o_m(1)
\quad(\theta>0)
}
\tag{PVST}
```

for every same-fluid terminal Zeno source-residue rescaling produced by the source-wall failure sequence.

Equivalently, the same atom can be stated as:

```math
\boxed{
TerminalResidueAtomExclusion.A:
\quad
\mu_*^{src}(B_R\times\{0\})=0
\quad\forall R<\infty.
}
\tag{TRA}
```

## Why this closes the Temporal branch

If `(PVST)` holds, then for every fixed `R`,

```math
\mu_*^{src}(B_R\times\{0\})
\le
\liminf_{\varepsilon\downarrow0}
\liminf_m
\mu_m^{src}(B_R\times[-\varepsilon,0])=0.
```

Thus:

```math
PositiveViscositySourceThickness.A
\Longrightarrow
TemporalNonAtomicSource.A.
```

With `NoEarlierSelectedSourceSlice.A`, the limit belongs to the temporal class `B_time`, and the residue Liouville step gives:

```math
TemporalNonAtomicSource.A
\Longrightarrow
ZenoSourceResidueRigidity.A.
```

## Why this closes the Carleson branch

If `ScaleCriticalTreeCarleson.A` fails after the ASAC and no-free-sink reductions, the only surviving failure is a same-fluid Zeno source-residue branch.  Zeno compactness gives:

```math
\mu_*^{src}(Q_1^-)\ge c_0>0.
```

The first-pulse reduction gives no earlier selected source slice.  `(PVST)` gives terminal non-atomicity.  These two together force the temporal residue to vanish in the `B_time` class, contradicting the inherited lower bound.

Therefore:

```math
PositiveViscositySourceThickness.A
\Longrightarrow
ScaleCriticalTreeCarleson.A.
```

Then the direct source-payment chain gives:

```math
ScaleCriticalTreeCarleson.A
\Longrightarrow
SourcePulseExclusion.A.
```

Hence the desired common closure would be:

```math
\boxed{
PositiveViscositySourceThickness.A
\Longrightarrow
TemporalNonAtomicSource.A
\wedge
ScaleCriticalTreeCarleson.A
\Longrightarrow
SourcePulseExclusion.A.
}
```

## Direct proof attempt for the common atom

The failed shortcut is the implication from fixed positive viscosity to nonzero
heat-time participation. The required theorem is an explicit positive residence
estimate: nonzero native source residue must occupy heat-scale windows for a
positive amount of parabolic time.

The direct test fails.  A scalar time marginal

```math
g_m(s)=m\mathbf 1_{(-1/m,0]}(s)
```

has bounded `L^1_s` mass, converges weakly to a terminal atom, and has vanishing heat-time moment:

```math
\int_{-1}^{0}(-s)g_m(s)\,ds={1\over 2m}\to0.
```

The terminal Duhamel response

```math
\int_{-1/m}^{0}e^{(0-s)\nu\Delta}F_m(s)\,ds
```

can remain order-size because the heat semigroup is close to the identity for `|s|\ll1` in rescaled heat units.

Thus fixed positive viscosity plus `L^1` source mass gives no `(PVST)` estimate.

## Exact remaining requirement

A successful non-Carleson proof must use the fact that the source is generated by the same-fluid Navier-Stokes nonlinearity, rather than an arbitrary external forcing profile.  The missing theorem is one of the following equivalent forms:

```math
SameFluidSourceResidence.A
```

```math
TerminalSourceReverseHolder.A
```

```math
NoZeroHeatTimeResidueInsideNSClass.A
```

Each must prove temporal spread, reverse Holder control, or heat-time participation for the native positive source carrier without using `HeatScaleSquareSource.A`, `LocalPositiveSourceCarleson.A`, or `ScaleCriticalTreeCarleson.A`.

## Verdict

The common route is found:

```math
PositiveViscositySourceThickness.A
```

would close both `TemporalNonAtomicSource.A` and `ScaleCriticalTreeCarleson.A`, hence close the stronger supplier `SourcePulseExclusion.A`.

The route remains unproved.  The obstruction is terminal-layer concentration of same-fluid source mass.  Current installed inputs classify that concentration as a Field-side CM-exit witness, while the stronger forward supplier still requires a new non-Carleson temporal thickness theorem.
