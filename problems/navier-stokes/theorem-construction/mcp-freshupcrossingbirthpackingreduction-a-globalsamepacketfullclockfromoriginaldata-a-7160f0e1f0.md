# FreshUpcrossingBirthPackingReduction.A

Status: failed direct discharge; fresh free-material upcrossing packing reduces to the rigid terminal strain-aligned anti-atom / time-thickness theorem.

## 0. Target

The transaction-age split reduced free-material no-free-upcrossing to a native birth-packing/no-reuse theorem:

```math
\iota:\Gamma_N^{fresh}\to\mathcal R_N^{free}(u_0),
\qquad
\sum_{\gamma\in\Gamma_N^{fresh}}\omega_\gamma
\le
C\sum_{\rho\in\mathcal R_N^{free}(u_0)}\rho
+\text{visc/top-strain/annular/legal}.
\tag{FBR.1}
```

This note tests whether the existing retained positive-scale native reserve birth-charge machinery supplies `(FBR.1)`.

## 1. Paid branches

The installed birth-charge reductions remove the following cases:

```text
inherited past reserve,
ledger-visible residual reserve,
finite donor trees,
non-Zeno refill,
legal/projected/cutoff/boundary/off-family loss,
paid ASAC / pair-weight mismatch,
angular turnover,
recirculatory return already covered by the PLS recirculation closure.
```

For free-material upcrossings, these are exactly the branches in which a fresh event is not a genuinely uncharged new birth.  They are paid by existing ledgers or by the graph-compatible PLS clock.

## 2. Remaining case

After those removals, the retained case has the same structure as the previously isolated `B_ASAC` strain-aligned obstruction:

```text
same selected packet family,
positive expanding strain readout,
zero angular / ASAC defect,
no earlier selected source slice,
finite local source mass,
terminal time-face support,
no legal exit,
no non-Zeno refill,
no finite donor tree.
```

In free-material upcrossing language, this is a terminal sequence of fresh positive shell births whose parents are not visible earlier in the selected material ledger and whose time support collapses onto the terminal face.

A scalar model is

```math
g_m(s)=m\mathbf 1_{(-1/m,0]}(s),
```

with a fixed expanding strain direction and no angular turnover.  It can carry finite local mass while defeating a finite birth-count ledger unless a time-thickness / reverse-Holder / anti-time-face-atom theorem is available.

## 3. Consequence

The native birth-packing theorem `(FBR.1)` is therefore not proved by current installed inputs.  It reduces to the terminal anti-atom theorem:

```math
\boxed{
\text{BASACStrainAlignedTemporalAntiAtom.A}
}
```

or an equivalent statement:

```math
\text{UniformTemporalSourceIntegrability}_{p,B_ASAC-strain}.A,
\quad
\text{TerminalSourceReverseHolder}_{B_ASAC-strain}.A,
\quad
\text{StrainAlignedNoTerminalTimeFaceAtom}_{B_ASAC}.A,
```

or a class-specific backward-uniqueness/no-incoming theorem for the rigid strain-aligned terminal source residue.

## Verdict

Fresh free-material birth packing is not an independent new mystery.  It is the same terminal time-face anti-atom obstruction already isolated by the retained positive-scale native reserve birth-charge attempt.  The live frontier is sharpened to the rigid strain-aligned terminal anti-atom / reverse-Holder theorem.