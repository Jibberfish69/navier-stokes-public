# GraphCompatiblePressureClockReplacementAttempt.A

Status: graph-compatible pressure/collar entropy closes conditionally; equivalence to the old pointwise positive-lobe pressure clock is not proved.

## 0. Problem

The previous full-entropy attempt failed at one-sided positive pressure/Helmholtz--Leray/collar graph-parallel growth after terminal selection.  The obstruction belongs to a pointwise pressure-lobe clock: after Calderon-Zygmund pressure cancellation, terminal positive-part extraction can discard the negative pressure partners.

The Schur-complement entropy is not naturally paired to that lobe clock.  It is naturally paired to a graph-compatible pressure/collar clock.

## 1. Graph-compatible clock

With

```math
Z_N=(q,C_N),
\qquad
Z_N=\Gamma_N(Y_N)+W_N,
```

define

```math
d\Omega_N^{press/collar,graph}
:=
d\Omega_N^{rel.defect}
+d\Omega_N^{graph.driver},
```

where

```math
d\Omega_N^{rel.defect}
```

is generated by the positive orientation of

```math
-{1\over2}\|W_N\|_{pc,N}^2,
```

and

```math
d\Omega_N^{graph.driver}
```

is the same-material service required to move the forcing graph

```math
\Gamma_N(Y_N)=\left(L_G^{-1}R(Y_N),\mathcal C_N(A,G,\phi)\right).
```

The graph-driver clock is not a new pressure account.  It is composed of velocity tower, coefficient/frame, material Helmholtz--Leray/Stokes projection, transported-collar, strain, viscosity/tower, and annular stress-work service already present in the free material PLS clock.

## 2. Entropy line for graph-compatible clock

The Schur-complement storage is

```math
\widetilde{\mathscr A}_N^{press/op+collar}
=-{1\over2}\|W_N\|_{pc,N}^2.
```

Its derivative pays relative defect growth.  Graph-parallel pressure/collar motion is not charged here; by definition it is charged to \(d\Omega_N^{graph.driver}\), hence to the free same-material PLS budget.

Thus the pressure/collar entropy law closes in the graph-compatible form:

```math
d\widetilde{\mathscr A}_N^{press/op+collar}
+c_N d\Omega_N^{rel.defect}
\le
dR_N^{legal}
```

provided the relative defect lower-rank estimate holds:

```math
\|W_N\|_{pc,N}^2
\le
C_N(u_0)+\text{paid lower-rank/top-strain/viscous/annular/legal terms}.
```

Then the full graph-compatible clock satisfies

```math
d\mathscr B_N
+c_N\left(
d\Omega_N^{free\ material}
+d\Omega_N^{rel.defect}
\right)
\le dR_N^{legal},
```

where \(d\Omega_N^{graph.driver}\subset d\Omega_N^{free\ material}\).

## 3. Remaining bridge

This does not prove finiteness of the old pointwise pressure-lobe clock.  The missing bridge is

```math
\boxed{
 d\Omega_N^{press/collar,lobe}
 \le
 C\,d\Omega_N^{press/collar,graph}
 +dR_N^{legal}.
}
\tag{GPC.1}
```

The earlier pressure-partner obstruction is exactly failure to prove `(GPC.1)` after terminal positive selection.

## Verdict

The Schur-complement entropy should be paired with the graph-compatible pressure/collar clock, not the pointwise positive pressure-lobe clock.  In that native formulation, the remaining pressure-positive-part obstruction is moved into a clock-equivalence theorem `(GPC.1)`, not into the entropy law itself.

So the next exact task is not another entropy construction.  It is:

```math
\boxed{
\text{prove or replace the bridge from pointwise pressure-lobe PLS action to graph-compatible pressure/collar action.}
}
```