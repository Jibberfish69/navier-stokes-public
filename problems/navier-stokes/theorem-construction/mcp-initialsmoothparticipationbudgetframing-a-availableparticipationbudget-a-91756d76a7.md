# InitialSmoothParticipationBudgetFraming.A

Status: corrected framing installed; this is not a discharge of the live budget theorem.

## Correction

The failed construction attempts do not disprove the payment route.  They only eliminate shallow representations of the available-participation budget:

```math
\text{ordinary energy},\quad
\text{scalar shell reservoir},\quad
\text{negative weighted energy},\quad
\text{signed annular net work},\quad
\text{future-action Gramian}.
```

Those are not the intended object.

The intended object is the finite initial smooth-state same-material participation endowment: a present-time budget inherited from the original smooth field and spent by every later pressure/metric/collar/annular/strain/viscous rearrangement generated by the same Navier--Stokes participation law.

## Correct theorem target

Construct a budget functional or measure-valued budget

```math
\mathcal B_N(t)
```

attached to the original transported material history, with

```math
\mathcal B_N(0)\le C_N(u_0),
\qquad
\inf_{t<T^*}\mathcal B_N(t)>-C_N(u_0),
```

and the participation-tax inequality

```math
\mathcal B_N(t_1)-\mathcal B_N(t_2)
\ge
c_N\,\Omega_N^{PLS}([t_1,t_2])
-
R_N^{legal}([t_1,t_2]),
\tag{BUD.1}
```

with

```math
R_N^{legal}([0,T^*])<\infty .
```

Equivalently,

```math
d\mathcal B_N+c_N\,d\Omega_N^{PLS}\le dR_N^{legal}.
\tag{BUD.2}
```

## Meaning

The budget is not merely viscous energy.  Viscosity is one visible tax channel.  The full tax includes pressure constraint, material metric motion, Hodge/Stokes motion, transported collar motion, annular Cauchy stress-work, strain, and viscous/tower loss on the same material packet.

Terminal normalized PLS action is not an unlimited future entropy source.  Every unit of terminal PLS action must be generated from the initially smooth finite field and charged through the same-fluid participation law while usable motion is being drained.

## Closure if installed

If `(BUD.1)` is proved, then

```math
\int_0^{T^*}d\Omega_N^{PLS}<\infty,
```

hence finite positive material-record variation, bounded material restart package, fixed \(H^s\) continuation, and no finite smooth breakdown.

## Remaining proof obligation

The live theorem is therefore not "find a trick symmetrizer" and not "viscosity alone smooths everything."  It is:

```math
\boxed{
\text{finite initial smooth-state participation endowment taxes every later full-PLS rearrangement.}
}
```

The previous failed candidates do not refute this theorem; they only show the budget cannot be represented by a scalar frequency reservoir, ordinary energy, signed net annular work, or a future-action definition.