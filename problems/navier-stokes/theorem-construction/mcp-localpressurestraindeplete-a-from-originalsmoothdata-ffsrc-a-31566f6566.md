# LocalPressureStrainDeplete.A from OriginalSmoothData

## Target

Prove

```math
OriginalSmoothData\Longrightarrow LocalPressureStrainDeplete.A.
```

A concrete pressure-vortex form is

```math
\int_{Bad_N(\eta)}(\omega_j\cdot S_{<j}\omega_j)_+(t)\,dt
\le \text{viscous drain}+	ext{legal losses}+o_N(1).
```

## Direct test

The vorticity equation is

```math
\partial_t\omega+u\cdot\nabla\omega-\nu\Delta\omega=S\omega.
```

The active stretching carrier is

```math
\omega\cdot S\omega.
```

Pressure disappears after curl. Pressure Poisson recovery gives elliptic and Calderon-Zygmund control of pressure/strain response. It supplies routing and integrability estimates, while it gives no sign for the local stretching term.

The sign obstruction is:

```math
S=\mathrm{diag}(2,-1,-1),\qquad \omega=(1,0,0),
```

one has

```math
\omega\cdot S\omega=2>0
```

and `tr S=0`. Thus incompressibility permits positive stretching.

The strengthened source supplier

```math
FFSRC.A\Longrightarrow FCI.5f
```

is a downstream integrability supplier. It does not eliminate positive source production on the terminal bad windows.

## Result

`LocalPressureStrainDeplete.A` is absent from current pressure, vorticity, and source-supplier inputs. It remains a possible new proof mechanism equivalent to branch-native positive source depletion.

## Verdict

The pressure-strain branch remains open.  The remaining Active Vorticity
Coherence.A branch must prove a source-weighted coherence or anti-alignment law
on the same terminal packet family; no such theorem is installed here.
