# NS.ExtensionRouteSpineQuarantine.A

## Claim type
Package-local theorem-spine quarantine note.

## Spine under review
The current Navier-Stokes theorem-facing spine is the class-membership forward-invariance / endpoint-exclusion packet:

- receiver/source assembly through `LCI.A`, `FCI.5f`, `CSP.A`, `OFP.A`, and `CFI.A`;
- endpoint assembly through `End_NS` and the retained or averaged endpoint readout maps;
- CM witness landing through synchronized `Pack_Q`, `Part_{N,Q}`, or `Field_{N,r,Q}`;
- terminal packet closure under the retained H1-H6 conditions.

An object belongs to the theorem spine only when it appears as a live-edge theorem object, a theorem-packet dependency, a named bridge into `Member(Q; O_NS^work)`, a synchronized Part/Field witness landing, or an endpoint certificate map.

## Quarantine theorem
The following families have support, readout, extension, comparison, source-recovery, or runtime status unless a later authority surface installs a named bridge into the spine:

1. TPS / `SG.4` and exact-potential / `PSJ+` readouts.
2. Hodge / normal-covector readouts.
3. Euclidean strict-shadow and downstream export branches.
4. Fixed-nu Euler-to-NS transfer and Euler-mirror comparison branches.
5. Positive-smoothness or source-wall supplier work outside a Part/Field landing.
6. Receiver/source/readout support, PCTP, SOURCE.NO-PULSE, AWG, TGC, no-`Jump_avg`, signed-current, pressure-strain, compactness, ancestry, and donor-depletion objects.
7. Source recovery, new-idea generation, and automation/runtime repair surfaces.

## Proof audit
Step 1. The live edge names the class-membership forward-invariance / endpoint-exclusion route as the theorem-facing edge and records downstream readout families as preserved context.

Step 2. The review verdict requires unresolved or extension-side routes to stay outside the theorem spine. This is a package-honesty rule, not an analytic closure theorem.

Step 3. The target-operating contract separates proof modes: positive smoothness, CM class-exit, fixed-nu transfer, Euler mirror, source-wall supplier, receiver/readout support, downstream export/readout, source recovery, new ideas, and runtime repair.

Step 4. A route family outside the spine can become theorem-spine material only through a named bridge into `Member(Q; O_NS^work)`, synchronized Part/Field landing, or an endpoint certificate map.

Step 5. The unresolved source-wall-root group remains the terminal release blocker. Its supplier presentations remain separate from theorem-spine closure until they land in a witness or prove the required positive supplier theorem.

## Result
This note localizes the quarantine handoff. The local package can cite extension-side routes as support, readout, comparison, recovery, or runtime context while keeping the theorem spine restricted to the live class-membership and endpoint-exclusion packet. Terminal release still depends on `source-wall-root-after-reconcile`.

## Remaining gap
The review hygiene handoff is localized. The mathematical live blocker remains the source-wall-root theorem burden.