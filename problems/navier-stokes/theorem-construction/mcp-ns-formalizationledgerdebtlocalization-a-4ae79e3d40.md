# NS.FormalizationLedgerDebtLocalization.A

## Claim type
Package-local proof-debt localization note.

## Ledger object
The current formalization ledger records one explicit proof-debt row:

`Keep theorem scope below the first unsafe overclaim`

The stand-alone need is recorded as `no`, and the existing local anchors are:

- claim-ladder: Lowest Safe Claim
- claim-ladder: First Unsafe Overclaim
- `theorem-construction/mpp-status-sync-proof-dependency-and-route-authority-closure-note.md`

The promotion rule says that promotion beyond `revise` requires every live theorem burden to be localized into the local package or explicitly parked below the theorem surface.

## Localized debt theorem
The ledger debt is package-localized as a claim-boundary obligation:

The Navier-Stokes package may present the current class-membership forward-invariance / endpoint-exclusion packet as a route-aligned local theorem package. It must keep stand-alone global smoothness export below the first unsafe overclaim until `source-wall-root-after-reconcile` is proved or parked beneath the theorem surface with an explicit review verdict.

## Proof audit
Step 1. The ledger row identifies scope control as the only explicit local proof-debt row. The stand-alone need field says `no`, which means the row is currently prose/status hygiene rather than an added mathematical theorem burden.

Step 2. The review verdict identifies the active theorem status as `theorem-open-at-source-wall-root`. Therefore the local package can contain a coherent theorem packet while stand-alone release stays in revise posture.

Step 3. The claim ladder supplies the two required boundaries: lowest safe claim and first unsafe overclaim. These boundaries localize the ledger row inside package prose and review metadata.

Step 4. The source-wall-root burden supplies the mathematical blocker. The ledger row does not create a new analytic proof program. It requires the package to expose the blocker and prevent speculative recovered support from being presented as an installed bridge.

Step 5. The localized package treatment is: keep the route-aligned packet visible, mark `source-wall-root-after-reconcile` as live theorem burden, and keep terminal stand-alone export in revise posture.

## Result
The formalization-ledger debt is localized into the local package as `NS.FormalizationLedgerDebtLocalization.A`. It is a scope and claim-ladder obligation tied to the source-wall-root blocker. The mathematical burden remains the source-wall-root theorem work rather than a separate ledger-only theorem.

## Remaining local gap
The package still needs the unresolved or extension-side routes placed outside the theorem spine with explicit quarantine language.