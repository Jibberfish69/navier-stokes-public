# OriginalSmoothData to SourcePulseExclusion.A — direct frontier attempt

## Target

Prove

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

This is the active strong supplier target under the route-governing primitive `SourcePulseCMExit.A`. It is route-equivalent to the signed local source presentation

```math
OriginalSmoothData\Longrightarrow SignedLocalSource.A
```

and the sharp mixed-shell formulation

```math
OriginalSmoothData\Longrightarrow MSC.BadAC.
```

## Current route state

The downstream consequences are already organized:

```math
SourcePulseExclusion.A\Longrightarrow
\text{full-SCF upper control}\Longrightarrow
Jump_{avg}\text{ no-escape}.
```

Together with the installed full-SCF lower accumulation theorem, this gives the conditional branch contradiction

```math
Jump_{avg}+SourcePulseExclusion.A\Longrightarrow\bot.
```

Thus the only missing promotion input for unconditional `Jump_avg` elimination is the present theorem.

## Direct attempt

The installed original-data package supplies smooth preterminal evolution, finite raw energy, finite dissipation, pressure/source routing, legal-loss ledgers, same-fluid localization, and the already-recorded terminal normal forms.

After those reserves are spent, the surviving object is a native positive active source measure on terminal active windows. The source-pulse ledger records that the package must do one of three jobs:

```math
\text{remove the source pulse},
\qquad
\text{charge it to a finite reserve},
\qquad
\text{rigidify it into a class with a Liouville or exit theorem}.
```

The current route inputs perform none of these three jobs for the singular native positive active source measure.

## Exact exposed exits

The source-theorem side has two live presentations:

```math
QDrain.A,
\qquad
MSC.BadAC.
```

The compactness/localized-core side has three live presentations:

```math
LocalSource.NoPulse.A,
```

```math
ParentConcentrationOrDiffuseCharge.A+BackwardDrainFunctional.A,
```

```math
ParentConcentrationOrDiffuseCharge.A+BoundedAncestryClass.A+SourceResidueLiouville.A.
```

Each presentation must eliminate the same terminal native positive active source-pulse survivor.

## Audit result

The current surfaces have already audited the common candidate mechanisms: pressure-vortex, helical/Beltrami, vorticity-direction coherence, localized drain, local-energy / CKN, compactness / Liouville, viscous-Duhamel, no-hop, active-square, square-source, residual-tail, active-shell amplitude gain, terminal source coherence, reverse-Holder parent concentration, and Zeno rigid-class production.

Each route reduces to the same native positive source-pulse primitive or one of the five exposed exits above.

## Verdict

The theorem

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A
```

is currently open. The exact survivor is the singular native positive active source measure relative to all installed route ledgers.

A successful closure must prove one of the exposed exits:

```math
QDrain.A,
\quad
MSC.BadAC,
\quad
LocalSource.NoPulse.A,
\quad
ParentConcentrationOrDiffuseCharge.A+BackwardDrainFunctional.A,
\quad
ParentConcentrationOrDiffuseCharge.A+BoundedAncestryClass.A+SourceResidueLiouville.A.
```

Claimed status: failed direct frontier attempt with exact survivor exposed.