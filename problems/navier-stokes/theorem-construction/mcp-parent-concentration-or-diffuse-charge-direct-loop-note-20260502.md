# ParentConcentrationOrDiffuseCharge.A — direct loop note

## Target

For a localized source-balanced child pulse `P` with positive same-fluid source-parent measure

```math
d\pi_P(P^-),
```

prove the disjunction

```math
ParentConcentration.A
\quad\vee\quad
DiffuseParentCharge.A.
```

The concentration branch asks for a bounded legal predecessor family

```math
\mathcal C(P)\subset\mathsf{Pred}(P),\qquad
|\mathcal C(P)|\le B,\qquad
\pi_P(\mathcal C(P))\ge c\pi_P(\mathsf{Pred}(P)).
```

The diffuse-charge branch asks for the complement to pay a globally summable finite route reserve.

## Attempt

The edge-disintegration layer is available once the localized pre-Cauchy source is retained:

```math
EdgeDisintegrate.A:
\quad
\text{source recovery plus same-fluid edge fidelity.}
```

Thus the remaining work is entirely anti-diffuse: either force bounded parent concentration or charge broad legal parent diffusion.

Finite dyadic banding gives only

```math
j_{P^-}\in[j_P-M,j_P+M]
```

up to licensed lower-feed bands. This bounds shell indices and interaction types, but it does not bound physical parent multiplicity or triadic packet multiplicity inside the same-fluid predecessor region.

The obstruction model is the diffuse legal parent measure

```math
\pi_P={1\over M}\sum_{\alpha=1}^{M}\delta_{P^-_\alpha},
\qquad
P^-_\alpha\in\mathsf{Pred}(P),
\qquad
M\to\infty.
```

For every fixed route constant `B`,

```math
\sup_{|\mathcal C|\le B}\pi_P(\mathcal C)\le {B\over M}\to0.
```

Therefore finite banding and same-fluid edge fidelity do not prove `ParentConcentration.A`.

For the diffuse-charge branch, the installed finite ledgers price raw energy,
local-energy balance, pressure/cutoff losses, and illegal source-localization
loss. They do not price legal parent branching entropy inside
`\mathsf{Pred}(P)`. The active-square reserve prices this only after a
square-source / active-window Carleson theorem is already installed, and that
theorem is the same source-pulse wall in another form.

Thus the direct route does not prove `DiffuseParentCharge.A` either.

## Result

The disjunction remains open under installed inputs:

```math
ParentConcentration.A \text{ open},\qquad
DiffuseParentCharge.A \text{ open}.
```

The sharp next sufficient target is

```math
TwoTowerDonorDepletion.A,
```

because it would construct a scale-normalized donor reserve for positive source-parent edges and imply the diffuse-charge branch.

## Loop status

This item is a hard local obstruction for the direct anti-diffuse parent step. Continue to `TwoTowerDonorDepletion.A`.
