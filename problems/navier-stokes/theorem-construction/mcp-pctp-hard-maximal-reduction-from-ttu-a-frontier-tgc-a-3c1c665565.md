# PCTP.hard maximal reduction from the TTU.A frontier

## Purpose

Push the `TTU.A` target as far as the current theorem surfaces support.

Target:

```math
OriginalSmoothData
\Longrightarrow
\text{uniform terminal-tail retained }Part/Field/\text{tower readout}.
```

## Maximal supported compression

For the selected strengthened-source averaged route, the five-burden frontier compresses to two primitive analytic kernels plus one exact-source optional branch.

The strongest supported conditional theorem is:

```math
Pack.TTU+GoodScale.TTU+JumpExclusion.Upstream
\Longrightarrow
TTU.A
```

provided the selected strengthened source authority `Hyp(FFSRC.A)=>FCI.5f`,
including `FCC.C1a`, the local `AACT.KX` repair, and the bridge-license map
below are used.

More explicitly:

```math
Pack.TTU+GoodScale.TTU
\Longrightarrow
READ.COVER+Field.TTU+TowerCover.TTU.
```

Then, with the averaged no-jump scheduler repair,

```math
Field_{avg}+ATD_m^\varepsilon+AACT.KX.local
\Longrightarrow
AACT.Global.noJump
\Longrightarrow
DTC.A_{avg}.
```

The selected source branch gives

```math
\operatorname{Hyp}(FFSRC.A)\Longrightarrow FCI.5f.
```

Hence

```math
DTC.A_{avg}+FCI.5f
\Longrightarrow
LCI.A_{avg}\Longrightarrow CSP.A_{avg}\Longrightarrow OFP.A_{avg}\Longrightarrow CFI.A_{avg}.
```

The averaged endpoint and readout chain gives

```math
CFI.A_{avg}+AVG.END.Cert
\Longrightarrow
End_{NS,avg}
\Longrightarrow
READ.COVER+Field.Read+DTC.Read
\Longrightarrow
End_{NS}.
```

Finally, endpoint tower control gives the `H^s`, `s>5/2`, continuation
contradiction: a common preterminal relaunch lifespan and uniqueness overlap
extend the same classical solution past `T_*`.

## Discharged or compressed cells

### Source cell for the selected route

The selected strengthened branch is closed at the level needed by the averaged route:

```math
\operatorname{Hyp}(FFSRC.A)\Longrightarrow FCI.5f.
```

The exact same-depth route remains an optional stricter reconstruction requiring
`FIRP.A0` and the same cutoff-defect input `FCC.C1a`.

### AACT.Global.noJump

The local absorption problem has a valid replacement shape once the direct-variable term audit is accepted:

```math
D^+X_R+cN_R\le B_RX_R+C X_R^{1/2}N_R+F_R.
```

Globalization is supported on the no-`Jump_avg` branch by reanchoring each restart to a fresh SCF-good cylinder, using `ATD_m^\varepsilon` to restore the small seed, and choosing a common scheduler from finite `L^1` majorants. Thus:

```math
\neg Jump_{avg}+TGC.A.cover+ATD_m^\varepsilon+AACT.KX.local
\Longrightarrow
DTC.A_{avg}.
```

The remaining dependence is exactly terminal cover production, not the local scheduler mechanism.

### Bridge-license into `CM=Pack+Part+Field`

For the selected route, the explicit bridge map is:

```math
Pack.TTU\Longrightarrow Pack.
```

```math
GoodScale.TTU+READ.COVER+Field.Read\Longrightarrow Field.
```

```math
Field+Tower.TTU+Pack\Longrightarrow Part\text{ persistence on the terminal tail}.
```

Tower amplitude is a readout/control object feeding endpoint exclusion, while `CM` consumes only `Pack`, `Part`, and `Field`. TPS, exact-potential, Hodge, and Euclidean strict-shadow branches remain downstream/context branches unless they declare one of the three displayed bridges.

## Remaining primitive kernels

### Kernel 1: `Pack.TTU`

Current proved statement:

```math
S_{pack,Q}\in L^1([t_0,T_*))
\Longrightarrow
\Gamma_{pack,Q}\in L^\infty([t_0,T_*)).
```

Open production theorem:

```math
OriginalSmoothData\Longrightarrow S_{pack,Q}\in L^1([t_0,T_*)).
```

Energy and incompressibility do not supply this supremal material strain ledger.

### Kernel 2: `GoodScale.TTU` / branch-native `Jump_avg` elimination

Current proved statement:

```math
Field_{avg}\Longleftrightarrow
\text{terminal finite positive-scale SCF-good same-fluid cover}.
```

Open production theorem:

```math
OriginalSmoothData+Pack.TTU
\Longrightarrow
GoodScale.TTU.
```

Equivalent branch form:

```math
Jump_{avg}\Longrightarrow\bot.
```

The current `nu_SCF` route supplies a raw finite upper mass. It still lacks:

```math
SCF_{base}\text{ terminal threshold defect}
\Longrightarrow
\nu_{SCF}\text{ lower mass on a separated recurring schedule},
```

and a matching scale-normalized Carleson upper bound for the same charge.

## Final maximal route

The most compressed honest route is:

```math
OriginalSmoothData
\Longrightarrow Pack.TTU
\Longrightarrow GoodScale.TTU
\Longrightarrow Field_{avg}+READ.COVER
\Longrightarrow AACT.Global.noJump
\Longrightarrow DTC.A_{avg}
\Longrightarrow CFI.A_{avg}
\Longrightarrow End_{NS,avg}
\Longrightarrow End_{NS}
\Longrightarrow H^s\text{ continuation.}
```

with the complementary branch discharged by

```math
Jump_{avg}\Longrightarrow\bot.
```

## Verdict

The maximal progress is a conditional closure theorem plus a compression of the live analytic work to:

```math
Pack.TTU
\quad\text{and}\quad
GoodScale.TTU/JumpExclusion.Upstream.
```

The selected strengthened source supplier and the no-jump AACT globalization are no longer first-line analytic blockers once `Field_avg` / terminal good cover is supplied. The exact same-depth source reconstruction remains open only if the route insists on avoiding `FFSRC.A`.
