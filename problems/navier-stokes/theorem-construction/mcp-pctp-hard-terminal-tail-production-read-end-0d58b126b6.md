# PCTP.hard terminal-tail production — conditional reduction attempt

## Target
Prove that `OriginalSmoothData` implies uniform terminal-tail retained `Part/Field/tower` readout.

## Attempt
Start with original smooth divergence-free zero-mean periodic data and a first finite classical endpoint candidate. On every compact retained classical subinterval before the endpoint, the existing route supplies the retained-window hypotheses and the accepted endpoint certificate maps. The terminal-tail production claim requires a uniform passage from those compact retained readouts to endpoint-adjacent retained `Pack`, retained `Part`, retained `Field`, and tower amplitude control.

The available route factors through the averaged terminal-tail spine:

`OriginalSmoothData -> SCF_base + SGC.A_a.e + ATD_m^epsilon -> SCF_avg^m -> AACT.KX -> DTC.A_avg -> AVG.RCV.A -> LCI.A_avg -> CSP.A_avg -> OFP.A_avg -> CFI.A_avg -> End_NS_avg -> READ.END -> End_NS`.

This proves the desired terminal-tail production under three additional local facts:

1. local `AACT.KX` absorption supplies the averaged ACT budget with a valid coefficient absorption mechanism;
2. `Jump_avg` is eliminated on its own branch, either by constructing `CFI.A_avg` there or by deriving a direct contradiction from original dynamics;
3. pressure/source suppliers are expanded into verified estimates feeding the pressure/source closure cells.

Under those three facts, the first endpoint alternative is exhausted. The retained `Pack` readout follows from the finite-cover terminal packet, retained `Part` follows from synchronized same-fluid participation, retained `Field` follows from `OFP.A_avg -> CFI.A_avg` plus the readout layer, and tower control follows from `DTC.A_avg` plus terminal tower readout. The endpoint certificate then returns `End_NS`, contradicting the first finite endpoint assumption.

## Result
This attempt establishes a conditional theorem:

`AACT.KX repaired + Jump_avg eliminated + pressure/source suppliers verified` implies `PCTP.hard` terminal-tail production.

It does not discharge `PCTP.hard` unconditionally, because the proof consumes exactly the three open blockers recorded by the independent audit.

## Remaining mathematical burden
The route must next repair `AACT.KX`, eliminate `Jump_avg`, and verify pressure/source suppliers line by line. After those three are installed, this conditional reduction can be promoted into the terminal-tail production theorem.