# Pressure-Partner Gate Demoted By Full PLS Strain Placement

Date: 2026-06-25

Status: correction installed; pressure-partner package is not an independent entropy-closing gate after pressure is kept inside the full participation-law--strain packet.

## 0. Target

The failed entropy-law pressure-partner closure attempt names this remaining
package:

```math
TerminalCZPressureCancellation.A
+
TerminalPressureCollarLegal.A
+
PressureDonorNoFreeSink.A.
\tag{PPD.1}
```

Equivalently, it names:

```math
SelectedPressurePositivePartComparison.A.
\tag{PPD.2}
```

This note checks whether `(PPD.1)` is actually an independent gate for the
gold route once pressure is held inside the full
pressure-viscosity-incompressibility-velocity participation law.

## 1. The pressure-only comparison is false as a standalone theorem

Calderon-Zygmund pressure cancellation is signed and spatial.  It acts before
terminal selection and before positive-part extraction:

```math
\nabla^2p
=
\mathcal R\mathcal R(\nabla u\otimes\nabla u).
\tag{PPD.3}
```

After selecting only the positive pressure lobe, there is no positivity theorem
of the form

```math
[\mathcal R\mathcal R f]_+
\le
C[f]_+
+\text{legal}
\tag{PPD.4}
```

for a general singular integral.  The old pressure-partner notes correctly
found this: a positive angular lobe can be retained while the negative
Calderon-Zygmund partner is diffuse, unselected, or weight-mismatched.

Therefore `SelectedPressurePositivePartComparison.A` is not the right
standalone theorem.  It is false without extra same-packet structure.

## 2. Full PLS placement rewrites the pressure lobe

The full same-material packet contains the strain equation:

```math
D_tS+S^2+\Omega^2+\nabla^2p
=
\nu\Delta S.
\tag{PPD.5}
```

For a strain eigen-direction \(e\),

```math
-e\cdot\nabla^2p\,e
=
D_t\lambda
+\lambda^2
-|\Omega e|^2
-\nu e\cdot\Delta S\,e.
\tag{PPD.6}
```

Thus a selected positive pressure-Hessian lobe satisfies

```math
\bigl[-e\cdot\nabla^2p\,e\bigr]_+
\le
[D_t\lambda]_+
+
[\lambda^2-|\Omega e|^2]_+
+
\nu|e\cdot\Delta S\,e|
\tag{PPD.7}
```

up to the transported collar/eigenframe terms already typed inside the same
packet.

This is the key point: the positive pressure lobe is not paid by a pressure-only
Calderon-Zygmund partner.  It is admitted into the full PLS clock as material
strain growth, strain-vorticity imbalance, viscous strain tower service, and
collar/eigenframe service.

## 3. Collar/Helmholtz--Leray graph-parallel growth has the same placement

The Helmholtz--Leray/Stokes and collar graph terms are generated by the same material
variables:

```math
A,\quad G,\quad \mathbb P_A,\quad \phi,\quad S,\quad q.
\tag{PPD.8}
```

At top rank,

```math
\partial_tD_a^\beta A
=
-A\,D_a^\beta B
+\text{lower products},
\tag{PPD.9}
```

and the pressure graph satisfies

```math
q=L_G^{-1}R(v,A,G).
\tag{PPD.10}
```

Therefore positive Helmholtz--Leray/collar graph-parallel growth is also not a
pressure-only donor problem.  It is material coefficient/frame growth plus the
same pressure/strain graph-driver.  Its positive part belongs to

```math
d[\log(1+\mathcal P_N^{mat})]_+
+
d\Omega_N^{strain/vorticity}
+
dD_N^{vis/tower}
+
dR_N^{legal}.
\tag{PPD.11}
```

## 4. Correct role of the old pressure-partner package

The old package `(PPD.1)` remains useful only in a pressure-only projection of
the argument.  In the full PLS packet it is superseded by the exact strain
placement `(PPD.5)`--`(PPD.7)`.

So the entropy law should not require:

```math
\text{one-sided pressure lobe}
\quad\text{is paid by retained CZ pressure partner.}
\tag{PPD.12}
```

It should require:

```math
\text{one-sided pressure/Helmholtz--Leray/collar graph growth}
\quad\text{is admitted into the full material PLS clock.}
\tag{PPD.13}
```

That admission is exactly `(PPD.7)` plus the material coefficient/collar tower
identities.

## 5. What remains

This demotes the pressure-partner package from an independent obstruction.
It does not prove finite total clock.  The remaining theorem is the same
available-participation entropy for the full PLS clock:

```math
\exists\,\mathscr A_N(t)\ge -C_N(u_0)
\quad\text{such that}\quad
d\mathscr A_N
+c_N\,d\Omega_N^{PLS}
\le
dR_N^{legal}.
\tag{PPD.14}
```

Here \(d\Omega_N^{PLS}\) already includes the one-sided pressure/Helmholtz--Leray/collar
graph-parallel growth through `(PPD.7)` and `(PPD.11)`.

Thus the failed pressure-partner theorem does not create a new external gate.
It confirms that pressure-only routing is the wrong projection.  The live
gold-route object remains one full participation-law--strain entropy.

## Verdict

`SelectedPressurePositivePartComparison.A` should not be pursued as a
pressure-only theorem.  The pressure positive part is absorbed into the full
material strain/record clock by the exact strain equation.  The still-unproved
mathematical burden is the bounded-below available-participation entropy for
that full clock, not a separate terminal Calderon-Zygmund pressure-partner
closure.
