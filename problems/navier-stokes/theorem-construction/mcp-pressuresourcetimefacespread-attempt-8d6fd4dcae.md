# PressureSourceTimeFaceSpread.Attempt

## Status

Failed direct discharge.  This note tests whether pressure/eigenframe sustain forces time-face spreading of the terminal source residue.

## Target

A useful theorem would be

```math
PressureSourceTimeFaceSpread.A:
\quad
PressureEigenframeSustain_{term}>0
\Longrightarrow
\mu_m^{src}(B_R\times[-\varepsilon,0])
\le C_R\varepsilon^\theta+o_m(1)
```

for some `theta>0`, or an equivalent pressure-carrier trace theorem:

```math
PressureCarrierTrace.A.
```

This would remove terminal time atoms and feed

```math
TemporalNonAtomicSource.A.
```

## Test

Pressure recovery is elliptic at each time.  It can see the instantaneous terminal source geometry, while carrying no automatic time-spreading information.  A shrinking terminal layer can produce a nontrivial pressure/eigenframe trace while its time marginal converges to a terminal atom.

The installed pressure-Hessian and eigenframe tools control signed near-band sustain.  They do not imply super-`L^1` time integrability of the native positive source measure.

## Exact missing theorem

The pressure route would need a parabolic memory theorem:

```math
\boxed{PressureSourceParabolicMemory.A}
```

stating that terminal pressure/eigenframe sustain generated by native source cannot arise from an arbitrarily thin terminal time layer.

## Verdict

Pressure-source time-face spreading remains open.  The next alternate should target same-fluid ancestry/branch complexity rather than pressure trace alone.