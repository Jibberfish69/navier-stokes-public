# Quantified defect-charge / flux machinery — hard obstruction

## Target

Install the missing machinery required by branch-native `Jump_avg` elimination:

```math
Jump_{avg}\Longrightarrow
\{D_k\}\text{ or }\{Flux_k\}\text{ with terminal lower accumulation,}
```

and

```math
\sum_k D_k\le C(E_0,source)
\quad\text{or}\quad
\sum_k Flux_k\le C(E_0,source),
```

so that the lower accumulation contradicts the finite energy / source budget.

## Surfaces checked

The canonical hard-obstruction surface for quantitative defect charge states that persistent `B_{\varepsilon}^{\Phi}` cells do not yet supply the required quantitative separation, quasi-orthogonality, or lower-flux inequality.

The dyadic flux antisymmetry candidate proves exact unweighted exchange identities and a conditional weighted-defect endpoint. Its own boundary records that the tail-weight mismatch is order-one in the lifted regime, and the surviving route requires a stronger signed commutator-defect theorem. Even granting that conditional endpoint, the residual term is

```math
\int_0^T\sum_{k>N+M}D_k(t)\,dt,
```

which is stronger than the currently available first-moment surfaces.

## Attempt

A valid discharge would need a canonical nonnegative charge attached to terminal bad cells:

```math
D_k(B_{\varepsilon}^{\Phi})\ge d_0>0
```

on sufficiently many terminal scales, together with separation or quasi-orthogonality giving divergent or excessive total lower mass. It would also need the same charge to satisfy a finite upper estimate from energy, incompressibility, and pressure/source bounds.

The current surfaces provide neither side as an installed theorem. They provide only:

1. bad-set or terminal radius-collapse typing into `Jump_avg`;
2. exploratory flux identities whose residual high-frequency tail remains uncontrolled;
3. a recorded hard obstruction identifying the missing lower-bound/separation theorem.

## Result

The quantified defect-charge / flux machinery is not installed. The branch-native `Jump_avg` contradiction cannot be proved from the current repo state.

This is a hard mathematical obstruction for the requested discharge in the current MCP-visible surfaces. The obligation remains open. A legitimate future discharge must first prove one of these exact replacements:

```math
Jump_{avg}\Longrightarrow\sum_k D_k=\infty
\quad\text{and}\quad
\sum_kD_k\le C(E_0,source),
```

or a flux-equivalent theorem with the same lower/upper target quantity.

## Boundary

This note records a terminal failed attempt for the requested loop item. It does not promote `jump-avg-branch-elimination` and does not update the proof-obligation matrix to grounded.