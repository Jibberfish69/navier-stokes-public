# Recovered End_NS To PCTP.hard Bridge

Status: conditional theorem bridge.

## Statement

```math
End_{NS}+CFI.A
\Longrightarrow
PCTP.hard.
```

Equivalently,

```math
End_{NS}+CFI.A
\Longrightarrow
TTU.A.
```

## Proof

After `READ.END`, the old pointwise endpoint package is available:

```math
End_{NS}.
```

The pointwise class-invariance theorem is

```math
CFI.A.
```

The retained pointwise class witness is

```math
CM=Pack\wedge Part\wedge Field.
```

`CFI.A` propagates `CM` on retained same-fluid approach windows. If a first finite class exit occurred, the endpoint exhaustion row would force one old pointwise endpoint face:

```math
Dead\vee response\text{-}margin\text{-}collapse
\vee packing\text{-}detached\vee tower\text{-}blown\vee Jump.
```

But `End_NS` removes all of these old endpoint faces. Therefore no finite class exit occurs on the retained terminal branch.

By the installed class-membership readout, absence of finite class exit supplies the uniform terminal-tail retained class-membership readout required by `PCTP.hard`:

```math
OriginalSmoothData
\Longrightarrow
\text{uniform terminal-tail retained class-membership readout}.
```

Thus

```math
PCTP.hard
```

holds conditionally on the recovered endpoint package and `CFI.A`.

## Boundary

The endpoint consumed here is old pointwise `End_NS`, not `End_NS_avg`. The conversion from averaged to pointwise endpoint has already occurred through

```math
READ.END.
```