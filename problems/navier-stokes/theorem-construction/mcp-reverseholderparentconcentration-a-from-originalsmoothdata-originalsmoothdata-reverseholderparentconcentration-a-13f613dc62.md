# ReverseHolderParentConcentration.A from OriginalSmoothData

## Target

Prove

```math
OriginalSmoothData\Longrightarrow ReverseHolderParentConcentration.A.
```

For every localized positive source-parent measure `\pi_P`, this asks for a bounded legal parent family

```math
\exists\mathcal C(P)\subset\mathsf{Pred}(P),
\qquad
|\mathcal C(P)|\le B,
\qquad
\pi_P(\mathcal C(P))\ge c\pi_P(\mathsf{Pred}(P)).
```

or an equivalent diffuse charge statement.

## Direct tests

Finite frequency-band complexity bounds shell type. It does not bound physical packet multiplicity or triadic phase multiplicity. For each `M`, the diffuse parent countermodel can be taken as the probability measure

```math
\pi_P^{(M)}:= M^{-1}\sum_{\alpha=1}^M\delta_{P^-_\alpha},
\qquad M\to\infty,
```

so every bounded subfamily carries vanishing mass.

Raw orthogonality prices donor energy at the wrong normalization. A diffuse donor family can have small raw quadratic cost while maintaining order-one normalized child-feeding source mass.

Same-fluid transport preserves legal ancestry after a parent is chosen. It does not select a concentrated parent or price parent entropy.

Pressure/cutoff routing removes legal losses. It does not charge legal diffuse parentage.

## Result

The exact strengthening is

```math
ScaleNormalizedParentInverse.A:
\quad
\text{diffuse positive parentage carries a finite scale-normalized donor reserve.}
```

At route resolution,

```math
ScaleNormalizedParentInverse.A
\equiv_{route}
ScaleCriticalTreeCarleson.A.
```

## Verdict

`ReverseHolderParentConcentration.A` remains open as an independent supplier and reduces to the scale-critical tree-Carleson reserve.
