# ScaleNormalizedParentInverse.A / donor reserve from OriginalSmoothData

## Target

Prove

```math
OriginalSmoothData\Longrightarrow ScaleNormalizedParentInverse.A.
```

The desired statement charges diffuse positive parentage by a finite scale-normalized donor reserve. In parent-measure form, broad legal parent diffusion should satisfy

```math
\pi_P(\mathsf{Pred}(P)\setminus\mathcal C(P))
\le
Charge_P+Reserve_P+o(1),
```

with a globally summable reserve over terminal source-pulse packets.

## Direct tests

Finite dyadic banding bounds shell type. It leaves physical packet multiplicity and triadic phase multiplicity open. For each `M`, a legal diffuse parent countermodel can be the probability measure

```math
\pi_P^{(M)}:= M^{-1}\sum_{\alpha=1}^M\delta_{P^-_\alpha},
\qquad M\to\infty.
```

Raw orthogonality has the wrong scale. Donor amplitudes can spread across many
legal predecessors with raw quadratic cost
\(\sum_{\alpha=1}^M M^{-2}=M^{-1}\to0\), while the normalized child-feeding
source mass is \(\sum_{\alpha=1}^M M^{-1}=1\).

Same-fluid transport preserves legal labels after parent selection. It gives no inverse theorem selecting a bounded parent set and gives no entropy charge for diffuse parentage.

Pressure and cutoff routing allocate legal losses. They do not price legal interior parent diffusion.

## Result

The required strengthening is exactly the scale-critical donor reserve:

```math
ScaleNormalizedParentInverse.A
\equiv_{route}
ScaleCriticalTreeCarleson.A.
```

The donor-reserve branch therefore reaches the same source-control wall as the active-square and local positive-source Carleson branches.

## Verdict

`ScaleNormalizedParentInverse.A` remains open from current original-data inputs. A proof requires a new scale-critical donor reserve or tree-Carleson estimate for positive source-parent edges.
