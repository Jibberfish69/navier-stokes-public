# SCF-Bad Upgrade Remaining Obstruction

## Status

Conditional obstruction update.

## Upgrade result

The SCF-bad concentration upgrade gives

```math
(a,T_*)\in\mathcal B_{\varepsilon_m}^{\Phi}
\Longrightarrow
tower\text{-}blown
```

under the tower-depth and pressure-readout compatibility assumptions recorded in `GoodScale.SCFBad.ConcentrationUpgrade`.

The older grammar-level typing also remains valid:

```math
(a,T_*)\in\mathcal B_{\varepsilon_m}^{\Phi}
\Longrightarrow
Jump_{avg}.
```

The stronger route is the tower face.

## Remaining obstruction

The upgrade supplies endpoint typing. It supplies no tower-bound proof.

To use it for `GoodScale.TTU`, one still needs a noncircular exclusion of

```math
tower\text{-}blown
```

from sources independent of the `READ.COVER` generated by `GoodScale.TTU` itself.

The downstream tower readout route is

```math
GoodScale.TTU+Pack.TTU
\Longrightarrow
READ.COVER+ATD_m^\varepsilon
\Longrightarrow
DTC.Read
\Longrightarrow
END.TowerBound.
```

This route is valid after `GoodScale.TTU`; it cannot serve as the upstream tower-bound source for proving `GoodScale.TTU`.

## Role of SCFBase.FiniteComplexity

`SCFBase.FiniteComplexity` remains a useful side target for the Jump-only route:

```math
SCFBase.FiniteComplexity
\Longrightarrow
\text{moving SCF-bad packets cannot chase fresh labels forever.}
```

With the stronger tower-blown typing available, the leading target becomes

```math
TowerBound.Upstream:
OriginalSmoothData+Pack.TTU
\Longrightarrow
\neg tower\text{-}blown
```

without using `READ.COVER` produced by `GoodScale.TTU`.

## Updated target split

Primary route:

```math
SCF\text{-}bad
\Longrightarrow
tower\text{-}blown,
\qquad
TowerBound.Upstream
\Longrightarrow
GoodScale.TTU.
```

Secondary route:

```math
SCF\text{-}bad
\Longrightarrow
Jump_{avg},
\qquad
SCFBase.FiniteComplexity\text{ or }JumpExclusion.Upstream
\Longrightarrow
GoodScale.TTU.
```

## Verdict

The SCF-bad upgrade succeeded conditionally. The next leading theorem is `TowerBound.Upstream`; `SCFBase.FiniteComplexity` remains the fallback route for direct bad-set exclusion.