# SG.4 / TPS completion attempt

## Status

Conditional assembly. The terminal SG.4/TPS chain is explicit once `D.7mr2` is available. The upstream supplier layer remains open.

## Request

Complete `SG.4/TPS`.

## Result

The exact terminal chain is:

```text
D.7mq + (D.7mp + D.7mo + D.7mm)
=> D.7mr2
=> RPC.3
=> SG.4B
=> BR.lambda2
=> continuation
```

The decisive conversion object is `D.7mr2`. It turns exact-potential angular ledger control into the residual pairwise closure `RPC.3`, and the installed handoff converts `RPC.3` into `SG.4B`.

A stronger descendant route is also explicit:

```text
D.7mr2 + EP.11d + V1 + repaired direct V2 through LL + WZ + EP.8c
=> PD.70z
=> SG.4B
```

## Shortest completion path

1. Prove `PTC-Lin` on the active exact-potential strip.
2. Prove `PTC-Shape` on the same strip.
3. Apply `EP.Frame.Closure` to obtain `D.7mq`.
4. Prove the selector-strain packet `D.7mp + D.7mo + D.7mm` on the same strip.
5. Apply `SSD.A` to obtain `SSC-EP.A`.
6. Apply the angular ledger discharge to obtain `D.7mr2`.
7. Apply `RPC.A` and `SGH.B` / `SGH.C` to obtain `SG.4B` and the `BR.lambda2` continuation handoff.

## Open supplier layer

The live `PTC-Shape` stop line is:

```text
sectorwise relative oscillation control of the diagonal density around the parent-cell average
+ finite shell comparability
+ sup |grad v_J| on the active segments
=> balanced-frame shape descendants
```

A stronger selector-native sufficient route is:

```text
terminal sector-center lower density relative to the parent-cell average
+ local Holder oscillation on terminal entrance-sector cells
+ finite shell comparability
+ sup |grad v_J| on the active segments
=> balanced-frame shape descendants
```

The shell side is isolated as finite shell comparability on the same core-cap family. The selector side is isolated as sectorwise relative oscillation of the diagonal density.

## Honest conclusion

This attempt completes the terminal composition of `SG.4/TPS` from `D.7mr2` forward. The full branch discharge is blocked at the upstream supplier layer: `PTC-Lin`, `PTC-Shape`, and same-strip selector-strain sufficiency.

The branch should be pursued by proving the `PTC-Shape` stop line first, because that is the narrowest visible obstruction feeding `D.7mq` and therefore the entire `SG.4B` handoff.