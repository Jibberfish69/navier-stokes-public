# SourcePulseExclusion.A / EigenDecorrel.A — loop continuation

## Target

Proceed from active-window amplitude gain into the concrete source-control wall:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

The exposed concrete supplier is

```math
OriginalSmoothData\Longrightarrow EigenDecorrel.A.
```

Parallel suppliers are `SignedPositiveBalance.A`, `LocalPressureStrainDeplete.A`, `ActiveVorticityCoherence.A`, and `AntiBeltramiCarleson.A`.

## Installed ladder

The valid downstream ladder is

```math
TerminalSourceCoherence.A
\Longrightarrow
PositiveSourceDepletion.A
\Longrightarrow
ScaleCriticalTreeCarleson.A
\Longrightarrow
SourcePulseExclusion.A.
```

A donor-side route is

```math
ScaleCriticalTreeCarleson.A
\Longrightarrow
TwoTowerDonorDepletion.A
\Longrightarrow
SourcePulseExclusion.A.
```

## Attempt

The native source object is the localized positive active source packet after same-fluid localization, terminal active-window selection, cutoffs, shell weights, and positive-part extraction.

Global unweighted dyadic exchange cancels before these operations. After localization and positive-part extraction, the live object is a one-sided weighted lifted remainder. The remaining positive source can align with expanding strain directions, so incompressibility and trace-free strain do not give `EigenDecorrel.A`.

Reverse-Holder parent concentration also remains open. A diffuse legal parent measure can distribute mass over unboundedly many legal predecessors so that every bounded parent subfamily carries vanishing mass. Same-fluid ancestry preserves legal parentage once chosen; it does not select a concentrated parent or price parent entropy.

Scale-critical tree Carleson is equivalent at route resolution to the desired active-square / positive pre-Cauchy source control. Raw energy and first-moment dissipation do not price scale-critical positive source mass on shrinking heat-scale active windows.

## Verdict

`SourcePulseExclusion.A` remains open. The exact atoms are:

```math
EigenDecorrel.A,
SignedPositiveBalance.A,
LocalPressureStrainDeplete.A,
ActiveVorticityCoherence.A,
AntiBeltramiCarleson.A,
ScaleNormalizedParentInverse.A.
```

A successful closure must prove a native one-sided weighted positive-source depletion theorem after localization and active-window selection.

Claimed status: failed. Loop proceeds to the full-SCF upper side and `Jump_avg` contradiction rerun.