# SourcePulseExclusion.A frontier consolidation after direct loop

## Status

The direct loop worked the next targets downstream of the current Navier-Stokes frontier packet. The loop reached one exact source-pulse wall:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

The downstream implication remains valid:

```math
SourcePulseExclusion.A
\Longrightarrow AWG.A
\Longrightarrow JAVG.1+JAVG.2
\Longrightarrow (Jump_{avg}\Rightarrow\bot)
\Longrightarrow PCTP.hard/TTU.A
```

through the recorded TGC / AACT / AVG / READ assembly. The missing theorem is the first implication from original data to source-pulse exclusion.

## Worked targets

### ParentConcentrationOrDiffuseCharge.A

Direct parent concentration fails under installed data by the diffuse legal parent measure model

```math
\pi_P=M^{-1}\sum_{\alpha=1}^{M}\delta_{P^-_\alpha},\qquad M\to\infty.
```

Diffuse charge also lacks a finite route reserve for legal parent branching inside `Pred(P)`.

### TwoTowerDonorDepletion.A

The theorem remains a valid sufficient route, but installed reserves give raw mass, local balances, and localization losses rather than a scale-normalized edge-cost measure for every positive source-parent edge.

### AncestryDrain.A

The drain route reduces to `BackwardDrainFunctional.A`: a bounded ancestry rank or quantized finite reserve spend on every legal source-parent edge. Current route objects preserve ancestry and price first moments; they do not drain every source-balanced edge.

### AncestryCompactBound.A + SourceResidueLiouville.A

Compactness produces a local suitable ancient source-residue object. The current surfaces provide no rigid ancient class and no source-residue Liouville theorem killing the inherited residue.

### RefillTreeWellFounded.A

`LocalDonorBalance.A` is installed. Entrance leaves are paid by `EntranceLeafDecay.A`:
`sum_{v in partial T_N} E_N(v;t_v^-) <= C_leaf C_ov sup_{t<=T_*-tau} ||P_{>=N-C}u(t)||_2^2 -> 0`
for fixed preterminal or positive-scale entrance leaves. The terminal Zeno refill branch survives and reduces to either a scale-critical tree Carleson theorem or a Zeno source-residue rigidity theorem.

### ScaleCriticalTreeCarleson.A

The scale-critical tree Carleson theorem closes the Zeno charge branch once
supplied. Existing active-window Carleson and square-source presentations are
route-equivalent to the source-pulse theorem itself and cannot serve as
independent installed reserves.

### ZenoSourceResidueRigidity.A

Compactness reaches a local suitable ancient source-residue object. A rigid ancient class and a source-residue Liouville theorem for that class remain absent.

## Exact remaining theorem atom

The current irreducible atom is:

```math
\boxed{
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
}
```

Equivalent route presentations already audited into this atom include active-window Carleson, square-source control, signed local source cancellation, source-parent finite reserve, donor depletion, backward ancestry drain, and Zeno source-residue rigidity.

## Verdict

The requested direct loop did not close the frontier theorem. It compressed the target stack to a single precise source-pulse theorem and wrote the failed/direct notes for each next target. Further progress requires a new proof of `SourcePulseExclusion.A` from `OriginalSmoothData`, specifically a structural PDE estimate excluding heat-scale, scale-critical nonlinear source pulses carrying mixed bilinear dissipation on the active bad windows.
