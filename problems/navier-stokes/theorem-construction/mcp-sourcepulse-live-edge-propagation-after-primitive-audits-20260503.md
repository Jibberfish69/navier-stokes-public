# SourcePulse Live-Edge Propagation After Primitive Audits

Status: failed propagation; live branch-native target remains open.

## Target

Propagate current primitive source-control audits toward

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A
\Longrightarrow Jump_{avg}\Rightarrow\bot.
```

This is the branch-native no-escape route for `Jump_avg` and cannot assume `Field_avg` on that branch.

## Persisted primitive audits

`LocalPositiveSourceCarleson.A` was audited in

```text
problems/navier-stokes/theorem-construction/mcp-local-positive-source-carleson-loop-audit-20260503.md
```

and remains route-equivalent to the source-pulse atom itself:

```math
LocalPositiveSourceCarleson.A
\equiv_{route}
SOURCE.NO\text{-}PULSE.A
\equiv_{route}
SourcePulseExclusion.A.
```

Heat-scale square-source control was audited in

```text
problems/navier-stokes/theorem-construction/mcp-heat-scale-square-source-loop-audit-20260503.md
```

and remains open from `OriginalSmoothData` because source-balanced pulses at shrinking terminal scales are compatible with raw finite energy and dissipation.

Active-shell amplitude gain was audited in

```text
problems/navier-stokes/theorem-construction/mcp-active-shell-amplitude-gain-loop-audit-20260503.md
```

and remains open from `OriginalSmoothData`; it reduces to active-shell source normalization or an equivalent nonlinear flux theorem.

Scale-normalized parent inverse remains equivalent to the tree-Carleson donor-reserve wall already audited at

```text
problems/navier-stokes/theorem-construction/mcp-scalecriticaltreecarleson-child-loop-audit-20260503.md
```

and the reverse-Holder parent concentration surface records the same diffuse-parent obstruction.

The Zeno branch was audited at

```text
problems/navier-stokes/theorem-construction/mcp-zeno-rigidclass-child-loop-audit-20260503.md
```

and no current surface produces a rigid ancient class `B` with a matching residue-Liouville theorem.

## Propagation decision

No audited primitive currently supplies

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

The live target therefore remains:

```math
\boxed{
\text{prove native one-sided weighted positive-source depletion after localization and active-window selection,}
}
```

or provide an equivalent nonlinear source-control mechanism: scale-normalized parent inverse, heat-scale square-source control, active-shell amplitude gain, or a produced rigid Zeno class.

## Boundary

This audit preserves the stricter branch-native boundary. The averaged endpoint-face `Jump_avg` discharge remains a separate persisted route and does not discharge this branch-native no-escape theorem.