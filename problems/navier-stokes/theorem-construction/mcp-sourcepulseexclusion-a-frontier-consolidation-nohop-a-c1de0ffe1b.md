# SourcePulseExclusion.A frontier consolidation

## Target chain

The current terminal branch target is

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

Once this theorem is installed, the branch-native implication is

```math
SourcePulseExclusion.A\Longrightarrow Jump_{avg}\Rightarrow\bot.
```

Then the no-`Jump_avg` averaged terminal-tail machinery gives

```math
Jump_{avg}\Rightarrow\bot
\Longrightarrow
PCTP.hard/TTU.A.
```

## Irreducible terminal packet

Failure of `SourcePulseExclusion.A` is represented by a same-fluid terminal source-pulse packet

```math
\mathcal P_n=(Q_n^\Phi,j_n,I_n),
\qquad
j_n\to\infty,
\qquad
c_I\,2^{-2j_n}\le |I_n|\le C_I\,2^{-2j_n},
\qquad
t_n\uparrow T_*.
```

On the same selected heat-scale windows,

```math
\sup_{t\in I_n}\gamma_{j_n}(t)\ge\eta>0,
```

and the localized active nonlinear source / mixed bilinear packet carries nonvanishing normalized mass:

```math
\liminf_{n\to\infty}
\int_{I_n}\mathfrak S_{j_n,loc}^{active}(t)\,dt>0.
```

The installed reserves and cancellation ledgers fail to pay for that mass on the same selected windows.

## Route-equivalent presentations

The current surfaces identify the following as route-equivalent presentations of the same theorem:

```math
SignedLocalSource.A,
\quad LocalSource.NoPulse.A,
\quad SOURCE.NO\text{-}PULSE.A,
\quad MSC.BadAC,
\quad ActiveShellSourceNormalize.A,
```

```math
PositiveResidualReserve.A,
\quad ActiveSquareResidualTail.A,
\quad ActiveSquareCarleson.A,
\quad SquareSource.A,
\quad ActiveShellAmplitudeGain.A,
```

```math
QDrain.A,
\quad NOHOP.A,
\quad PressureVortexDeplete.A,
\quad HelicalCoherenceDeplete.A,
\quad AntiBeltramiCarleson.A,
\quad ActiveVorticityCoherence.A,
```

```math
LocalizedDrain.A,
\quad PackStrainDeplete.A,
\quad ParabolicNoPulse.A,
\quad ViscousNoRefill.A,
\quad MSC.Localize^{src}+Ancient.NoPulse^{src},
```

and the ancestry spelling

```math
SourceParent.Measure+
(AncestryDrain.A\vee AncestryCompactBound.A+SourceResidueLiouville.A).
```

These aliases attack one of three slots:

1. remove active mass;
2. charge active mass;
3. rigidify active mass.

## Remove route

A remove proof must show terminal positive-part source / strain depletion on the active bad windows, for example

```math
\int_{Bad_N(\eta)}\mathfrak S_{N,loc}^{active,+}(t)\,dt=o_N(1),
```

or a directional strain/vortex source sign theorem on the same heat-scale windows.

The installed original-data package gives energy, local energy, pressure Poisson recovery, preterminal smoothness, and dyadic exchange cancellation before bad-window selection. These inputs do not supply a sign for the active positive part after weights, cutoffs, and terminal selection.

## Charge route

A charge proof must force every source-balanced active pulse to spend a finite installed reserve:

```math
\mathcal P_n\Longrightarrow Reserve(\mathcal P_n)\ge c_0>0,
\qquad
\sum_n Reserve(\mathcal P_n)<\infty.
```

The current raw reserves include energy, dissipation, pressure/cutoff ledgers, signed reserves, and cumulative tail reserves. They do not price the scale-normalized active packet. The heat-scale model permits raw finite dissipation with order-one normalized active-square/source mass.

Thus active-window Carleson, square-source, active-shell amplitude gain, and source-parent finite reserve are all equivalent charge-route presentations of `SourcePulseExclusion.A`.

## Rigidify route

A rigidify proof must localize and extract the source residue, then forbid the extracted object by an ancient or ancestral theorem.

The older global `MSC.BadAC` presentation requires

```math
MSC.Localize^{src}\quad\text{or}\quad MSC.DiagTether^{src}.
```

The localized-core bypass improves the route: failure of `SourcePulseExclusion.A` already supplies the localized child pulse. Therefore ancestry can start from the localized source-pulse packet and proceed through

```math
SourceParent.Measure+
(AncestryDrain.A\vee AncestryCompactBound.A+SourceResidueLiouville.A).
```

The remaining parent-measure axioms, drain alternative, compact-bound alternative, and source-residue Liouville theorem are open. A broad ancient no-pulse theorem is unavailable because nonzero smooth ancient source-compatible examples exist outside the source-residue-specific class.

## Downstream theorem chain

The downstream dependency order is:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A
\Longrightarrow Jump_{avg}\Rightarrow\bot
\Longrightarrow PCTP.hard/TTU.A.
```

This is the live route to terminal positive-scale cover, no-jump averaged production, endpoint readout, and final continuation.

## Verdict

The target remains open. The current materials prove route equivalence and isolate the exact theorem, but they do not prove

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

A successful next proof must introduce one genuinely new nonlinear source-control mechanism on the selected terminal heat-scale active windows:

```math
\text{remove: positive-part source/strain depletion,}
```

```math
\text{charge: active-window Carleson / square-source / source-parent finite reserve,}
```

or

```math
\text{rigidify: localized source-residue ancestry or ancient exclusion.}
```

No installed energy, pressure, local-energy, compactness, dyadic cancellation, monotone-source, active-square, square-source, no-hop, viscous-Duhamel, or source-residue route performs one of these jobs yet.
