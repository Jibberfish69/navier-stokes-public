# SourceWall.CMExitLandingDiscipline.A

## Claim type
Local discipline / failed-discharge note for the `source-wall-root-after-reconcile` handoff.

## Route position
The controlling Navier-Stokes live edge keeps class-membership forward-invariance and endpoint exclusion as the theorem-facing route. The CM witness envelope is

`CM_{N,r,Q}:=Part_{N,Q}+Field_{N,r,Q}`.

The installed theorem-grade direction is

`forall N exists r_N: CM_{N,r_N,Q} => Member(Q; O_NS^work)`.

The class-exit primitive is

`Exit(Q) := ¬Member(Q; O_NS^work)`.

Source-wall-root-after-reconcile therefore has a narrow admissible role. A residual source-wall presentation can enter the live class-membership route only through a synchronized landing in one of the witnesss `Pack_Q`, `Part_{N,Q}`, or `Field_{N,r,Q}`, with the same `Q`, `N`, `r`, same-fluid carrier, and still-live window. A presentation outside that landing remains supplier/readout support.

## Recentered handoff theorem
For any source-wall residual presentation `S` arising after `SourceWall.Reconcile.A`, the live-route license is:

1. `S` may be used as CM-exit work when it supplies a face certificate for `Pack_Q`, `Part_{N,Q}`, or `Field_{N,r,Q}` on the same synchronized carrier.
2. `S` may be used as supplier/readout support when it contributes to PCTP / SOURCE.NO-PULSE / AWG / TGC / source-drain / pressure-Hessian / signed-current families while leaving the CM primitive unchanged.
3. `S` earns promotion only after a bridge-license landing into the witness envelope. Promotion without that landing would bypass the live-edge route discipline.

## Proof audit
The proof is a route-typing audit.

Step 1. The live edge fixes the admissible class-membership spine through `CFI.A`, `OFP.A`, `LCI.A`, and the endpoint packet. This makes `CM_{N,r,Q}` the route-local witness envelope.

Step 2. `SourceWall.Reconcile.A` demotes normalized-adjoint source-drain to conditional support. Hence source-drain cannot act as an unconditional replacement for SOURCE.NO-PULSE.A or for the CM primitive.

Step 3. A first-exit analysis can activate a failure surface only through a witness break. The accepted face channels are `Pack_Q`, `Part_{N,Q}`, and `Field_{N,r,Q}`.

Step 4. The existing diagnostic ledger sorts major non-Euler source-wall surfaces into Part and Field, terminal-Zeno Pack-side/readout-quarantined subcases, or supplier/readout quarantine. This supplies classification discipline. It supplies row-level diagnostic coverage. Object-level positive supplier theorems remain separate burdens.

Step 5. The current positive source-control atom remains `PositiveRemainderDepletion.A`. The May 17 tests show that PRD needs `SourceWeightedTerminalAngularDepletion.A`; LPSC returns to PRD; SquareSourceEstimate.A and ActiveShellAmplitudeGain.A return to ActiveShellSourceNormalize.A / active-square reserve; the non-Carleson Zeno route needs TerminalSourceReverseHolder.A / UniformTemporalSourceIntegrability_p.A or a rigid residue class with Liouville.

Step 6. The equality route gives `ASACDefectMeasure.A` and `TerminalNoFreeSinkEqualityClassProduction.A`, producing `B_ASAC`. This object still permits a zero-thickness terminal source atom and lacks the global no-incoming-flux upgrade required for TerminalSourceReverseHolder.

Step 7. The eigenframe / pressure-Hessian notes add conditional near-band source-current obstruction. They do not create a new CM Part/Field.

## Result
This note discharges the wording alignment for the handoff: source-wall-root-after-reconcile is recentered as CM-exit landing discipline plus separate supplier work. It does not discharge the source-wall root itself. The open mathematical burden is a promoted Part/Field landing or a positive supplier theorem strong enough to feed such a landing.

## Remaining local gap
Produce one of the following:

- a Part/Field landing certificate for a selected source-wall residual presentation;
- `SourceWeightedTerminalAngularDepletion.A` strong enough to feed PRD;
- a rigid `B_ASAC` residue class with Liouville and TerminalSourceReverseHolder;
- a global no-incoming-flux upgrade for the equality class.
