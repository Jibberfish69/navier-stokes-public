# SourceWall.Reconcile.A — normalized-adjoint drain status

## Status

Demotion / reconciliation note.

The normalized-adjoint package is accepted as a definition-aligned analytic certificate for the parent-or-charge mechanism, but it is not accepted as a full unconditional proof of

```math
LemmaB.SourceDrain\Longrightarrow SOURCE.NO\text{-}PULSE.A
```

until the source-parent compactness / first-pulse ancestry wall is closed.

## Reason for reconciliation

Two surfaces currently point in different directions.

One surface, `mpp-source-drain-definition-alignment-and-discharge-20260504.md`, locks definitions and records the analytic certificate chain

```math
CoeffStop.A
\Longrightarrow WeightedAdjBessel.A
\Longrightarrow WeightedAdjRes.A
\Longrightarrow GainStop.A
\Longrightarrow ParentKernel.A
\Longrightarrow FirstPulseKill.A
\Longrightarrow LemmaB.SourceDrain.
```

A later source-wall audit, `mcp-unconditional-chain-next-targets-and-source-wall-20260504.md`, records that unconditional `FirstPulseKill.A` and unconditional `LemmaB.SourceDrain` remain blocked by the source-parent compactness wall.

The later audit is more specific about unconditional status. It identifies the missing bridge as source-parent compactness / drain closure, not packet-calculus bookkeeping.

## Accepted theorem-grade content

The following pieces remain accepted:

```math
WeightedAdjBessel.A+ResidualPair.A\Longrightarrow WeightedAdjRes.A,
```

```math
GainStop.A+WeightedAdjRes.A\Longrightarrow\text{high-gain edges are charge or ancestry},
```

and the normalized child-mass algebra for parent-or-charge iteration.

These are analytic-certificate results inside the normalized-adjoint package.

## Demoted content

The following inference is not theorem-grade unconditionally:

```math
FirstPulseKill.A\Longrightarrow LemmaB.SourceDrain
```

unless the first-pulse/no-parent condition is shown to exclude diffuse legal parent measures in the terminal ancient limit.

The obstruction is the legal diffuse parent model

```math
\pi_P={1\over M}\sum_{\alpha=1}^{M}\delta_{P^-_\alpha},
\qquad M\to\infty,
```

which can carry same-fluid source-parent mass while defeating every fixed bounded parent selector.

Thus the normalized-adjoint package proves a conditional drain theorem:

```math
\boxed{
\text{normalized-adjoint packet chain}
+SourceParentCompactness.A
\Longrightarrow LemmaB.SourceDrain.
}
```

It does not by itself prove `SourceParentCompactness.A`.

## Reopened source wall

The source wall is reopened at the primitive source-parent level. The exact open alternatives are:

```math
LocalPositiveSourceCarleson.A,
```

or

```math
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A,
```

or an equivalent theorem proving finite pricing / rigidity for diffuse legal source-parent ancestry.

The supporting open targets are:

```math
TerminalSourceCoherence.A,
ReverseHolderParentConcentration.A,
ScaleCriticalTreeCarleson.A,
TwoTowerDonorDepletion.A,
RefillTreeWellFounded.A,
FirstPulseKill.A,
LemmaB.SourceDrain.
```

## Verdict

```math
\boxed{
SourceWall.Reconcile.A:
\text{normalized-adjoint drain is conditional, and the source wall remains open.}
}
```

The current theorem target is one root source-wall theorem:

```math
LocalPositiveSourceCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
```

## Claimed status

The unconditional source-drain discharge is conditional analytic support with no proof force for unconditional source-wall closure. The row bridge and CM assembly must treat the Field row as conditional on source-wall closure until one root source-wall theorem is proved.
