# SourceWallRootDichotomy.A

## Status

Route-relative theorem note.  This records the current source-wall grammar after the source-residence route and the post-ASAC reductions.

## Claim

Within the current HeightFluxControl source-wall route grammar, every remaining supplier route for the terminal source wall lands in one of two branches:

```math
\boxed{\text{native positive source reserve}}
\qquad\text{or}\qquad
\boxed{\text{terminal Zeno source-residue anti-concentration}}.
```

The claim is relative to the audited route surfaces and installed inputs.  The scope excludes future route grammars that introduce a new primitive outside the retained-face trichotomy and source-wall presentations.

## Branch 1: native positive source reserve

The native reserve branch contains the equivalent or route-equivalent supplier labels

```math
PositiveRemainderDepletion.A,
\quad
LocalPositiveSourceCarleson.A,
\quad
SquareSourceEstimate.A,
\quad
ActiveShellAmplitudeGain.A,
\quad
ScaleCriticalTreeCarleson.A.
```

Each presentation supplies a way to pay the native positive pre-Cauchy source by drain, legal parent drain, legal loss, or a scale-critical active-square reserve.

The direct tests show the same surviving analytic carrier:

```math
[\langle S_{<j}^{loc}w_j,w_j\rangle]_+
=|w_j|^2[e_j\cdot S_{<j}^{loc}e_j]_+.
```

Pressure, cutoff, commutator, and boundary terms route into legal ledgers.  The interior positive carrier requires a reserve theorem.  Cauchy/Young envelopes return to active-square control.  Bellman attempts with controlled endpoints pay first-moment tail quantities, while a Bellman functional paying the scale-critical square density already contains the reserve.

Thus all audited native-source presentations collapse to this branch.

## Branch 2: terminal Zeno source-residue anti-concentration

The Zeno branch contains the equivalent or route-equivalent supplier labels

```math
TemporalNonAtomicSource.A,
\quad
TerminalResidueAtomExclusion.A,
\quad
UniformTemporalSourceIntegrability.A,
\quad
TerminalSourceReverseHolder.A,
\quad
ZenoCancellationClassProduction.A+ZenoResidueLiouville_B.A.
```

These presentations evacuate the terminal Zeno source-residue atom without importing the square-source / local positive source Carleson theorem.

The compactness half gives an ancient local suitable source-residue object with inherited positive residue.  The missing supplier is temporal anti-concentration, super-`L^1` source residence, or production of an explicit rigid class with residue Liouville.

## Exhaustiveness within the current grammar

The retained-face trichotomy reduces the first large positive height-flux event to:

```text
Pack gain,
Part / heat-dwell failure,
legal Field source charge,
terminal Zeno source-residue atom.
```

The exact ASAC residual/no-incoming gate and the ledger surfaces pay the
alignment, pair-weight, legal boundary/cutoff, and non-Zeno donor/refill branches
only after their hypotheses are installed.  Under those hypotheses, the surviving
objects are:

```text
interior native positive source carrier,
terminal Zeno source-residue atom.
```

The first object is Branch 1.  The second object is Branch 2.

## Boundary of the theorem

This theorem is a route-grammar closure statement.  It does not assert global mathematical exclusivity across all possible future proof ideas.  A future proof can escape this dichotomy only by adding a new route grammar or a new retained-face primitive and then proving compatibility with the class-membership / CM witness discipline.

## Consequence

The sharp next non-Carleson target is

```math
TerminalSourceReverseHolder.A,
```

because it attacks Branch 2 directly by trying to produce super-`L^1` temporal residence for the same-fluid native positive source.
