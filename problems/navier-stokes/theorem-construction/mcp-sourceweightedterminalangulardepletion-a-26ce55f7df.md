# SourceWeightedTerminalAngularDepletion.A continuation

## Status

Failed unconditional discharge from installed inputs.  The source-weighted angular route advances to a precise signed no-free-sink subtheorem, and then to the terminal Zeno refill atom.

## Target

For a bounded-overlap retained same-fluid terminal packet family `\mathcal F_N`, prove

```math
\sum_{P\in\mathcal F_N}\int_{Q(P)}
\chi_P |w_{j_P}|^2
\left[e_{j_P}\cdot S_{<j_P}^{loc}e_{j_P}\right]_+dxdt
\le
\theta\sum_P\int_{Q(P)}\mathcal D_P
+Legal_N(\mathcal F_N)+o_N(1),
\quad 0<\theta<1.
```

This would imply `PositiveRemainderDepletion.A`.

## Route reduction

The material eigenframe split decomposes the coefficient into threshold-small, far-low turnover, near-band turnover, and source-residence components.

The threshold-small term is drain-absorbable.  Far-low turnover has parabolic
scale separation and routes into legal ledgers.  The ASAC assembly pays the
active-alignment and one-sided pair-weight defect component only after the exact
packet residual estimate and no-incoming/backward-uniqueness inputs for
`ASAC.A` are installed.

After projected-cutoff, boundary, pressure/collar/off-family ledgers, and finite donor-tree telescoping, the remaining near-band object is the selected terminal source-current

```math
\mathcal S_{near}
=
\sum_{P\in\mathcal F_N}\omega_P\tau_P
\int_{I_P}Source_P^{loc}(t)b_P^{near}(t)dt.
```

The needed independent theorem is

```math
TerminalWeightedNoFreeSink.A_{ind}:
\quad
|\mathcal S_{near}|
\le Legal^{signed}_N(\mathcal F_N)+o_N(1).
```

## Edge partition

Each selected positive terminal source-current edge has exactly one outcome in the audited graph partition:

```text
retained signed partner,
legal exit,
finite donor tree,
terminal Zeno refill.
```

ASAC pays retained partner pair-weight mismatch.  Legal ledgers pay legal exits.  LocalDonorBalance.A and EntranceLeafDecay.A pay finite donor trees.

The remaining case is

```math
TerminalZenoRefillNoFreeSink.A_{ind}.
```

## Obstruction

The surviving branch has shrinking heat scales and no fixed entrance leaf:

```math
r_m\downarrow0,
\qquad
\sum_m r_m^2<\infty.
```

Finite donor-balance identities pass the unpaid source-current into the terminal leaves.  Closing those leaves requires one of three genuine new inputs:

```math
TerminalSourceReverseHolder.A / UniformTemporalSourceIntegrability_p.A,
```

```math
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A,
```

or

```math
PositiveActiveCarlesonReserve.A.
```

The direct reverse-Holder attempt failed because current same-fluid ancestry supplies `L^1_t` source mass and route fidelity, while the terminal atom model keeps the source marginal within that information.

## Verdict

The valid conditional chain is

```math
TerminalZenoRefillNoFreeSink.A_{ind}
\Longrightarrow
TerminalWeightedNoFreeSink.A_{ind}
\Longrightarrow
OneSidedNearBandMaterialStrainBV.A
\Longrightarrow
SourceWeightedTerminalAngularDepletion.A
\Longrightarrow
PositiveRemainderDepletion.A.
```

The unconditional proof stops at `TerminalZenoRefillNoFreeSink.A_{ind}`.
