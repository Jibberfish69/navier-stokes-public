# SquareChargeEvolution.A Conditional Pass And Zeno Trigger Extraction

## Status

Claimed status: conditional.

This note handles the replacement target generated by the failed direct proof of `SquareReserveEvolution.A`.

The square reserve density is

```math
S_N(t):=\sum_{k>N}2^kT_k(t)^2,
\qquad
T_k(t):=\sum_{\ell>k+4}D_\ell(t),
```

and

```math
\mathcal R_N(I):=\int_I S_N(t)\,dt.
```

The first-moment tail ledger is

```math
M_N(I):=\int_I\sum_{\ell>N}D_\ell(t)\,dt.
```

The previous direct attempt showed that `M_N(I)=o_N(1)` alone cannot pay `\mathcal R_N(I)`. The missing ingredient is square-amplitude control.

## Conditional theorem: donor-tail amplitude gain implies square charge

Assume the following active donor-tail amplitude theorem.

```math
DonorTailAmplitudeGain.A:
\quad
S_N(t)
\le
\varepsilon\nu D_N(t)+r_N(t)
```

with

```math
\int_Ir_N(t)\,dt=o_N(1)+Loss_{legal}.
```

Then `SquareChargeEvolution.A` holds on `I` with the zero potential

```math
\Phi_N(t):=0,
```

charge density

```math
Ch_N(t):=r_N(t),
```

and legal dissipation absorption:

```math
S_N(t)
\le
\varepsilon\nu D_N(t)+Ch_N(t).
```

Integrating gives

```math
\mathcal R_N(I)
\le
\varepsilon\nu\int_ID_N(t)\,dt+o_N(1)+Loss_{legal}.
```

After choosing `\varepsilon` below the dissipation margin in the gradient-tail inequality, the square reserve is a legal Row 5 remainder.

Thus the direct source-wall route closes conditionally under `DonorTailAmplitudeGain.A`.

## Equivalent square-source form

A shell/source version of the same hypothesis is:

```math
SquareSourceControl.A:
\quad
\sum_{k>N}2^kT_k(t)^2
\le
\varepsilon\nu D_N(t)+r_N(t),
\qquad
\int r_N=o_N(1)+Loss_{legal}.
```

This is exactly the heat-scale square-source control appearing in the active-square source notes. The repo already records that active-shell amplitude normalization implies the corresponding scale-normalized Carleson estimate. The missing origin theorem is therefore:

```math
OriginalSmoothData
\Longrightarrow
\text{nonlinear shell-flux/source control ruling out source-balanced active pulses.}
```

## Zeno trigger extraction from failure of square charge

Assume `SquareChargeEvolution.A` fails along thresholds `N_m\to\infty`. Then there exist terminal windows `I_m` and a constant `\eta>0` such that

```math
\mathcal R_{N_m}(I_m)\ge\eta,
\qquad
M_{N_m}(I_m)\to0.
```

Define the square-reserve probability measure

```math
d\pi_m(t):={S_{N_m}(t)\over \mathcal R_{N_m}(I_m)}\,dt.
```

Then

```math
\int_{I_m}{\sum_{\ell>N_m}D_\ell(t)\over S_{N_m}(t)}\,d\pi_m(t)
=
{M_{N_m}(I_m)\over \mathcal R_{N_m}(I_m)}
\le
{M_{N_m}(I_m)\over\eta}	o0.
```

Hence for `\pi_m`-most square-reserve mass,

```math
S_{N_m}(t)
\gg
\sum_{\ell>N_m}D_\ell(t).
```

Refine the measure to space-scale atoms by

```math
d\Pi_m(t,k):={2^kT_k(t)^2\over \mathcal R_{N_m}(I_m)}\,dt,
\qquad k>N_m.
```

Then a subsequence of atoms `(t_m,k_m)` carries square-dominant terminal concentration:

```math
2^{k_m}T_{k_m}(t_m)^2
\gg
\sum_{\ell>N_m}D_\ell(t_m)
```

in the normalized reserve sense. Equivalently, the failure of square charge produces a terminal active pulse forest with vanishing first-moment cost and order-one square reserve.

This is the exact Zeno trigger generated by the source wall.

## Replacement Zeno theorem

The next theorem target is:

```math
ZenoSquarePulseRigidity.A:
\quad
\text{a terminal square-dominant pulse forest extracted above belongs to a rigid same-fluid class.}
```

The required residue theorem is:

```math
ZenoSquareResidueLiouville.A:
\quad
\text{every bounded rigid same-fluid square-pulse class has zero positive source residue.}
```

Together they imply the alternate source-wall close:

```math
ZenoSquarePulseRigidity.A+ZenoSquareResidueLiouville.A
\Longrightarrow
ScaleCriticalTreeCarleson.A.
```

## Outcome

The replacement target is solved conditionally:

```math
DonorTailAmplitudeGain.A\Longrightarrow SquareChargeEvolution.A.
```

Failure of this amplitude theorem produces the exact Zeno trigger above. The next executable frontier is proving either `DonorTailAmplitudeGain.A` from nonlinear shell-flux/source structure or proving the Zeno square-pulse rigidity and residue pair.