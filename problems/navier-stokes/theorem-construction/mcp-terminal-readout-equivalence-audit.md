# Terminal Readout Equivalence Audit

## Status

Closed terminal readout equivalence audit.

## Forward recovery

After the averaged endpoint package supplies a finite readout cover,

```math
End_{NS,avg}\Rightarrow READ.COVER.
```

On that cover:

```math
Field_{avg}\Rightarrow Field,
\qquad
DTC.A_{avg}\Rightarrow DTC.A.
```

The pointwise tower readout includes all forcing rungs

```math
K_q=-\nabla^{q+1}p+\nu\Delta U_q,
\qquad q\le N.
```

Pack and participation are shared primitive rows. Hence the pointwise endpoint package `End_NS` is recovered.

## Reverse compatibility

On the same cover, pointwise field and tower bounds average to the averaged package. The shared Pack and Part rows carry unchanged. Thus the readout bridge is exact at the endpoint-package level.

## Verdict

The terminal readout is exactly the averaged-to-pointwise endpoint equivalence used by the manuscript. It appears only after `End_NS_avg`.