# Terminal Route Compression — Retained Conditional Theorem

## Status

Conditional terminal theorem surface for the retained Navier-Stokes class-membership route.

Claim level: retained conditional theorem under H1--H6.

The theorem compresses the currently installed retained-route dependencies into one terminal chain and records the boundary against the averaged finite-energy route and terminal readout route.

## 1. Receiver and Source Compression

The retained receiver route supplies the lower-carrier theorem through the `ACT.KX` packet:

```math
RSCB.NKF
\Longrightarrow
NKF.Native
\Longrightarrow
ACT.KX
\Longrightarrow
ACT.X\text{-}Readout
\Longrightarrow
ACT.A
\Longrightarrow
RCF.A
\Longrightarrow
LCI.A.
```

The pressure-side returned receiver target is already compressed through the uniform lower-carrier finite-parameter theorem:

```math
ACT.KX
\Longrightarrow
LCI.A[N+2,N+2,\delta+5\Lambda]
\Longrightarrow
FFSRC.A
\Longrightarrow
FCI.5f.
```

Therefore the retained class-membership source/receiver chain is:

```math
ACT.KX
\Longrightarrow
LCI.A
\Longrightarrow
FFSRC.A
\Longrightarrow
FCI.5f
\Longrightarrow
CSP.A
\Longrightarrow
OFP.A
\Longrightarrow
CFI.A.
```

More explicitly:

```math
LCI.A+FCI.5f\Longrightarrow CSP.A,
```

```math
CSP.A\Longrightarrow OFP.A,
```

and

```math
OFP.A+CFI.B1+CFI.B2\Longrightarrow CFI.A.
```

The source side uses the strengthened branch:

```math
FFPB.A0+FFSC.C1+FCC.C1\Longrightarrow FFSRC.A\Longrightarrow FCI.5f.
```

The exact same-depth pressure branch remains separate from this strengthened source route.

## 2. Endpoint Side

The endpoint package is

```math
End_{NS}
:=
END.Pack\wedge END.Field\wedge END.TowerBound\wedge END.Cross\wedge END.Exh.
```

The endpoint side closes as

```math
DTC\text{-}to\text{-}TowerBound
+END.Exh
+END.Cross
+END.Pack
+END.Field
\Longrightarrow
End_{NS}.
```

`DTC-to-TowerBound` supplies the analytic tower-amplitude face:

```math
DTC.A\text{ through depth }N+2
\Longrightarrow
END.TowerAmp:
\mathfrak A_{N,Q}\in L^\infty(I),
```

and the definition-level cell gives

```math
END.TowerAmp\Longrightarrow END.TowerBound.
```

`END.Exh` is the accepted endpoint exhaustiveness theorem:

```math
\neg CM_{N,r,Q}\Longrightarrow Dead\vee response\text{-}margin\text{-}collapse
\vee packing\text{-}detached\vee tower\text{-}blown\vee Jump.
```

`END.Cross` reduces cross entries to the installed native endpoint rows. `END.Pack` spends bounded pack gauge. `END.Field` spends `OFP.A` and the positive field-scale persistence.

Therefore the endpoint block is independent of the source proof except through the required receiver/readout inputs and `OFP.A` for `END.Field`.

## 3. Terminal Class-Exit Exclusion

Combining the class-membership route and the endpoint package gives

```math
CFI.A+End_{NS}
\Longrightarrow
\text{no finite-time class exit}.
```

Substitution of the two compressed chains yields the terminal retained theorem:

```math
ACT.KX
+FFSRC.A
+DTC\text{-}to\text{-}TowerBound
+END.Exh+END.Cross+END.Pack+END.Field
\Longrightarrow
\text{no finite-time class exit},
```

with the understood route expansion

```math
ACT.KX\Rightarrow LCI.A,
\qquad
LCI.A\Rightarrow FFSRC.A\Rightarrow FCI.5f,
\qquad
LCI.A+FCI.5f\Rightarrow CFI.A.
```

At the current route level, the better explicit terminal statement is:

```math
RSCB.NKF+\Gamma_{pack}\in L^\infty+ACT.KX\text{ scheduler/scale seeds}+H5+H6
\Longrightarrow
\text{no finite-time class exit}.
```

## 4. ORIGIN.Retain

`ORIGIN.Retain` upgrades the conditional retained-window theorem to compact classical intervals of the original maximal solution setup.

For every compact classical interval

```math
I=[a,b]\Subset[0,T_*),
```

classical smoothness supplies retained center-ball regularity on a finite transported-center cover:

```math
\sum_j\|u\|_{L^1_t C^{m+2,\alpha}(B(c_j(t),2\rho_I))}
+
\sum_j\|p\|_{L^1_t C^{m+1,\alpha}(B(c_j(t),2\rho_I))}<\infty.
```

This gives `RSCB.NKF`, `ACT.X-Scale`, and native point recovery on the retained windows.

The classical flow also supplies the bounded pack gauge on compact intervals:

```math
\Gamma_{pack,Q}\in L^\infty(I).
```

Absolute continuity of the `L^1` coefficient ledgers gives a finite retained partition with scheduler budgets small enough for `ACT.KX`. Thus every compact classical interval admits the retained hypotheses H1--H4.

A finite endpoint failure must be typed through the endpoint vocabulary:

```math
\neg Part\Rightarrow Dead,
\qquad
\neg Pack\Rightarrow packing\text{-}detached\vee tower\text{-}blown,
\qquad
\neg Field\Rightarrow Jump,
```

or through tower-amplitude escape. The endpoint matrix excludes those faces once `End_NS` is installed. Therefore the retained-window theorem applies to the original maximal solution setup at the conditional route level.

## 5. Boundary: Retained, Averaged, Terminal Readout

### Retained conditional route

The retained route uses pointwise retained smooth center-ball input:

```math
RSCB.NKF\Longrightarrow NKF.Native\Longrightarrow ACT.KX\Longrightarrow LCI.A.
```

This route is conditional under H1--H6.

### Averaged finite-energy route

The finite-energy replacement route is separate:

```math
SCF_{base}+SGC.A_{a.e.}+ATD_m^\varepsilon
\Longrightarrow
SCF_{avg}^m
\Longrightarrow
AACT.KX
\Longrightarrow
DTC.A_{avg}
\Longrightarrow
AVG.RCV.A
\Longrightarrow
LCI.A_{avg}
\Longrightarrow
CSP.A_{avg}
\Longrightarrow
OFP.A_{avg}
\Longrightarrow
CFI.A_{avg}
\Longrightarrow
End_{NS,avg}.
```

Averaged objects remain averaged on this branch.

### Terminal readout route

Pointwise endpoint recovery from averaged data occurs only through terminal readout:

```math
DTC.Read:
DTC.A_{avg}+READ.COVER+ATD_m^\varepsilon\Longrightarrow DTC.A,
```

```math
Field.Read:
Field_{avg}+READ.COVER+ATD_m^\varepsilon\Longrightarrow Field,
```

and

```math
READ.END:
End_{NS,avg}+READ.COVER+Field.Read+DTC.Read\Longrightarrow End_{NS}.
```

No averaged object is used as its pointwise counterpart upstream of these readout theorems.

## 6. Remaining Hypothesis Status

Retained terminal theorem status: conditionally compressed under H1--H6.

Source status: strengthened branch closes through

```math
FFPB.A0+FFSC.C1+FCC.C1\Rightarrow FFSRC.A\Rightarrow FCI.5f.
```

Endpoint status: endpoint package closes through

```math
DTC\text{-}to\text{-}TowerBound+END.Exh+END.Cross+END.Pack+END.Field\Rightarrow End_{NS}.
```

Readout status: averaged-to-pointwise conversion remains terminal and typed through `DTC.Read`, `Field.Read`, and `READ.END`.

Promotion boundary: this file records a local retained conditional theorem compression. Full-MPP or Clay-level release requires the separate release/promotion packet surfaces.