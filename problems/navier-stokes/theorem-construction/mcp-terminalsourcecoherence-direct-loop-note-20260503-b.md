# TerminalSourceCoherence.A direct loop note

## Target

Prove from original smooth data that every bounded-overlap same-fluid terminal family has vanishing source-coherence defect:

```math
\sum_{P\in\mathcal F_N}\mathfrak C_{src}(P)=o_N(1),
```

with packet inequality

```math
\int_{Q(P)}[\mathcal N_{preCauchy}^{loc}(u;P)]_+\,dxdt
\le C\mathfrak C_{src}(P)+\ell(P).
```

## Installed bridge

The supplier bridge is valid:

```math
TerminalSourceCoherence.A\Longrightarrow PositiveSourceDepletion.A
\Longrightarrow ScaleCriticalTreeCarleson.A
\Longrightarrow SourcePulseExclusion.A.
```

## Attempt

Pressure Poisson recovery controls elliptic response. It produces no sign law or coherence decay for the positive active source on selected terminal packets.

Energy-class vorticity gives global finite quantities. It does not impose a DFD/CKN-type direction modulus on arbitrary terminal active windows.

Beltrami/helical coherent packets are cancellation geometries. Current original-data inputs do not force terminal bad active windows into anti-Beltrami or mixed-helicity defect decay.

Same-fluid transport preserves selected carrier labels and packet legality. It gives no decay of coherence defects.

The signed lifted-remainder route would supply a coherence component only after proving cancellation of the true weighted lifted remainder before absolute values and active-window selection. That theorem remains open.

## Result

`TerminalSourceCoherence.A from OriginalSmoothData` remains open.

The supplier bridge is conditional; the missing input is a concrete terminal coherence theorem such as vorticity-direction coherence, anti-Beltrami defect decay, positive strain-alignment depletion, or signed lifted-remainder cancellation.

## Next target

Proceed to `LocalPressureStrainDeplete.A` and `ActiveVorticityCoherence.A`.