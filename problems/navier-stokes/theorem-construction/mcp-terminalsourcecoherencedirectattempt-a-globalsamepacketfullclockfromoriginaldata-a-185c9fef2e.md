# TerminalSourceCoherenceDirectAttempt.A

Status: failed direct discharge from installed inputs.

## Target

Use terminal same-fluid coherence to prove the positive-source estimate needed by the graph-compatible full-clock route:

```math
\text{TerminalSourceCoherence.A}\Longrightarrow \text{LocalPositiveSourceCarleson.A}
```

and then use it to exclude the rigid strain-aligned terminal source residue.

## Direct test

The installed route gives same-fluid terminal carrier fidelity and coherence of the selected packet family in the following sense:

```text
same selected fluid history,
consistent terminal packet family,
legal exits removed,
finite donor-tree telescoping available,
ASAC / angular mismatch defects paid,
pressure/collar graph overcount removed.
```

These properties ensure that the source-current is attached to the original material history.  They also prevent the source from being an arbitrary external forcing term.

However, coherence of carrier identity is not temporal thickness.  The terminal source marginal can still concentrate on vanishing time strips while preserving same-fluid ancestry and packet coherence.  The model

```math
g_m(s)=m\mathbf 1_{(-1/m,0]}(s)
```

has finite \(L^1_s\) mass, a coherent terminal family, and no necessary angular turnover.  It violates every \(L^p_s\), \(p>1\), thickness estimate and converges to a terminal time-face atom.

## Consequence

Terminal coherence is a valid bridge only if it includes a quantitative spread property, such as a Carleson packing, reverse-Hölder temporal gain, active-square reserve, or source-normalization estimate.  Carrier coherence alone does not supply that exponent.

Thus the direct implication from installed coherence data to the required anti-atom statement fails.

## Verdict

`TerminalSourceCoherence.A` is not discharged from the currently installed same-fluid/coherence inputs.  It reduces back to one of:

```math
\text{TerminalSourceReverseHolder.A / UniformTemporalSourceIntegrability}_p.A,
```

```math
\text{PositiveActiveCarlesonReserve.A},
```

```math
\text{ReverseHolderParentConcentration.A},
```

or a class-specific

```math
\text{ZenoResidueLiouville}_B.A.
```

The terminal time-face atom remains admissible under carrier coherence plus \(L^1_s\) source mass alone.