# ThetaNActiveDensityToSignedCommutatorCoercivity.A

Status: failed as an unconditional finite-total estimate; reduced to signed same-packet commutator/coercivity.

## 0. Target

The finite service-coordinate theorem is

```math
\int_0^{T^*}\Theta_N(t)dt<\infty .
```

Read through the existing material-record growth law, this is the remaining Gold clock.

## 1. Paid pieces

The existing surfaces pay or localize the following pieces:

```text
initial high-frequency entrance,
linear viscous high-frequency tail,
fixed-threshold finite-collar support,
relative pressure/collar defect,
projected Hodge pressure overcount,
endpoint first-time-rung trace channel after inclusion in the full tower action.
```

These do not solve the moving full service coordinate, but they remove several false subfrontiers.

## 2. Active rectified densities

The frequency-controlled annular return leaves the active rectified service in two families:

```math
S_5(t)=\int_{collar}|U|\,|\nabla U|^2|\nabla\phi|\,dx
```

and

```math
S_2(t)=\int_{collar}|U|\,|\nabla(-\Delta)^{-1}\mathcal E_P|\,|\nabla\phi|\,dx.
```

The first is the cubic strain/gradient service.  The second is pressure-time elliptic service generated by differentiating the pressure equation along the material history.

## 3. Why energy does not pay them

For the cubic term,

```math
\int |U|\,|\nabla U|^2
\lesssim \|\nabla U\|_2^2\|\nabla U\|_3.
```

Energy gives \(\nabla U\in L_t^2L_x^2\), but does not give \(\nabla U\in L_t^1L_x^3\).  Interpolation requires \(\nabla^2U\), hence continuation-depth tower strength.

For the pressure-time term, \(\mathcal E_P\) contains second stress derivatives, pressure-Hessian service, and cubic gradient terms.  Calderon-Zygmund estimates keep it in the pressure tower but do not reduce it to energy.  Absolute control again requires continuation-strength gradient/pressure service.

Thus an unsigned proof of

```math
\int(S_2+S_5)dt<\infty
```

would assume the full material clock.

## 4. Correct noncircular primitive

The pressure/Hodge and commutator orientation notes show that these terms must be retained before rectification as a signed same-packet current.  The needed primitive is

```math
\boxed{\text{SignedSmoothSamePacketCommutatorCoercivity.A}.}
```

A representative prelimit inequality is

```math
\mathcal I_{N,m}
+dX_{comm,m}
+c\,d[\log(1+\mathcal P_{N,m}^{mat})]_+
\le
\varepsilon dD_{N,m}^{vis}
+\varepsilon dD_{N,m}^{rad}
+dR^0_{N,m},
```

with \(X_{comm,m}\) bounded below on the same retained material packet.

If this signed inequality is proved, the positive material-record log clock is paid before lower-semicontinuity and before the pressure/Hodge commutator is split into one-sided lobes.  The Body-II signed current can then pass to the adjacent four-body exchange and cancellation.

## 5. Result

`FiniteTotalThetaNServiceCoordinate.A` is not discharged by energy, pressure-only estimates, or local metric storage.  It is reduced to the signed same-packet commutator/coercivity theorem:

```math
\boxed{\text{SignedSmoothSamePacketCommutatorCoercivity.A}.}
```

That is the next theorem-grade target.  It is the only currently visible noncircular route because it keeps the pressure-Hodge/cubic-strain partner before rectification.