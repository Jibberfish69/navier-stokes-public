# MPP ActiveEigendirectionDecorrel.A Reentry

Date: 2026-06-09

Status: active-completion proof attempt. This note does not close public Clay
finality.

## Target

The signed native-source route now asks for:

```text
ActiveEigendirectionDecorrel.A.
```

Concrete form:

```text
sum_{P in F_N} int_{Q(P)}
[<S_<j_P^loc w_{j_P}, w_{j_P}>]_+ dxdt
<= o_N(1) + legal losses.
```

This would deplete the positive native source carrier, give temporal
thickness, and feed the first-exit Pack-survival route.

## Direct tests

Incompressibility gives `tr S = 0`. It does not make the selected strain
readout nonpositive. A trace-free symmetric strain in three dimensions may have
an expanding eigendirection, and the selected active shell can align with it.

Global dyadic skew symmetry is before one-sided weights, cutoffs, terminal
selection, lifted remainders, and positive-part extraction. It does not control
the positive part on the selected terminal family.

Pressure Poisson recovery pays elliptic/cutoff ledgers. It does not give a
sign rule for the interior active strain carrier.

Same-fluid transport preserves labels and carrier legality. It does not supply
an angular mixing rate or eigendirection decorrelation theorem.

Beltrami, helical, vorticity-direction, or anti-coherence classes would be
useful if produced. Current original-data inputs do not force arbitrary
terminal active packets into any of those classes.

## Result

`ActiveEigendirectionDecorrel.A` is not proved from the installed inputs.

The next target should not be the broad alias `SignedPositiveBalance.A`, because
that only renames the same native positive source-control wall at this depth.

The next concrete theorem is the positive fallback:

```text
ActiveSquareResidualTail.A
```

or direct active-square Carleson control for the selected terminal active-shell
source carrier.
