# MPP ActiveAmplitudeCarleson.A Direct Attempt Note

## Status

Direct attempt on the amplitude face isolated by
`mpp-native-trilinear-defect-alignment-split-note.md`.

The target is:

```math
\boxed{
ActiveAmplitudeCarleson.A.
}
```

It tests the active shell square/amplitude face in the native positive
trilinear source carrier

```math
\left[
\langle S_{<j}^{loc}w_j,w_j\rangle
\right]_+
=
|w_j|^2
\left[
e_j\cdot S_{<j}^{loc}e_j
\right]_+.
```

## Target

For every bounded-overlap same-fluid terminal active family `\mathcal F_N`,
one possible product-factor target is a same-carrier active amplitude reserve:

```math
\boxed{
\sum_{P\in\mathcal F_N}
\int_{Q(P)}
|w_{j_P}|^2\,dxdt
\le
C\rho_{inst,N}(\mathcal F_N)+o_N(1),
}
\tag{AAC.1}
```

By itself `(AAC.1)` is not theorem-grade unless the positive strain coefficient
is also bounded by an installed companion theorem.  As a single-face discharge,
`ActiveAmplitudeCarleson.A` must therefore mean a stronger active-square/source
estimate that charges the same selected terminal packets before the positive
source carrier is formed.

## Attempt 1: Energy And First-Moment Dissipation

The energy inequality and first-moment dissipation give finite global quadratic
cost, but `(AAC.1)` is a scale-normalized terminal active-window estimate.  A
terminal family can concentrate on shrinking heat-scale windows while keeping
the raw energy/dissipation ledger finite.

Thus raw energy and first-moment dissipation do not install
`ActiveAmplitudeCarleson.A`.

## Attempt 2: Active-Square Residual Tail

The active-square residual-tail branch would control the needed amplitude only
if one already had a scale-critical active-square estimate on the same terminal
bad windows.  The installed residual-tail audit records that this is not a
consequence of finite energy, cumulative tail-square reserves, square-source
notes, or downstream source integrability.

Thus active-square residual-tail control is not an independent supplier for
`ActiveAmplitudeCarleson.A`.

## Attempt 3: Square-Source / Amplitude Gain

The heat-scale square-source and active-shell amplitude-gain routes give valid
conditional implications.  Their missing input is precisely active-shell source
normalization: terminal active windows must not be source-balanced at
scale-critical strength.  That is the same source-pulse atom, not a discharged
amplitude estimate.

Thus square-source and amplitude-gain notes do not install
`ActiveAmplitudeCarleson.A` from `OriginalSmoothData`.

## Attempt 4: Frozen-Family Source Integrability

The downstream source supplier gives integrable source ledgers after the
receiver/source route is admitted.  It does not provide a uniform
branch-native active-window Carleson reserve over the same terminal bad packets
that define the no-pulse branch.

Thus `Hyp(FFSRC.A)=>FCI.5f` and its stronger source imports are consumers, not
suppliers, for `ActiveAmplitudeCarleson.A`.

## Result

No installed route input proves the active amplitude face:

```math
\boxed{
ActiveAmplitudeCarleson.A\ \text{is open.}
}
```

At current route resolution this face is the same analytic wall as
`PositiveActiveCarlesonReserve.A`, `ActiveSquareCarleson.A`,
`ActiveShellAmplitudeGain.A`, `SquareSource^{norm}.A`, and
`ScaleCriticalTreeCarleson.A`: a scale-critical reserve for native positive
source/amplitude on terminal same-fluid active windows.

## Consequence

`ActiveAmplitudeCarleson.A` is not an independent discharge of
`NativeTrilinearDefectDomination.A`.  It closes only if a genuine
active-window Carleson, active-square, or square-source theorem is proved on the
same terminal packet topology.
