# Actual Synthesis: HeightFluxControl and Class-Exit Architecture

Date: 2026-05-06

Status: audit-open research synthesis memo; not a theorem discharge or
promotion surface.

Post-audit correction: `FirstHeightCreationDichotomy.A` was pursued in
`mpp-firstheightcreation-dichotomy-terminal-attempt-and-collapse-20260506.md`
and collapsed as an independent target.  Read every occurrence below as
historical target selection unless explicitly restated by the live edge. The
direct route now needs `HeightFluxControl.A` with a real structural mechanism.

Exactness rework correction, 2026-06-18: the proof graph below is historical
route architecture, not a proof implication.  Its old mathematical claim was
that the Field/source-wall branch could be treated schematically as

```text
Pack + Part + not Field
=> source wall / Zeno residue
=> Field restored
=> Member restored
=> first class exit contradicted.
```

That claim is too fast.  Current live surfaces keep source-reserve,
height-flux, active-height, signed-current, and Zeno residue branches
support-level until a theorem supplies one of the exact interfaces:

```text
same-ledger payment
or
CM-test admission + first Pack/Part/Field face failure.
```

Zero-radius terminal residue or selected unpaid infinite donor-refill lands
first at `not Pack_Q`.  A retained positive-scale first-created native donor
reserve surplus remains supplier-quarantined until a same-witness charge,
anti-concentration, or non-source Pack-survival theorem is proved.  The live
frontier currently records that old ActiveHeightFlux / SourceReserve /
SignedPair / no-free-sink labels are consumed and cannot count as fresh
progress; the active replacement is the same-witness terminal
anti-concentration / source-Carleson / rigid-anti-atom / non-source
Pack-survival family.

Purpose: compress the Navier-Stokes lane into the current shortest proof
architecture and identify the theorem atoms that would actually move the
project.

## 1. Center of gravity

The repo has converged around a CM/class-exit proof grammar.

Smoothness is read as preservation of class membership:

```text
Original smooth data
=> still-live classical windows
=> Pack + Part + Field
=> CM
=> Member(Q; O_NS^work)
=> endpoint/readout continuation
=> smoothness.
```

Nonsmoothness is read contrapositive as class exit:

```text
finite-time nonsmoothness
=> class exit
=> first witness-face failure
=> Pack failure or Part failure or Field failure.
```

The witness faces are:

```text
Pack  = shared deformation / carrier geometry
Part  = shared participation / still-live tower participation
Field = one-field coherence / source-readout law
```

The current live work has routed enough Pack/Part structure that the active
mathematical burden sits on the Field face:

```text
Pack + Part + not Field
=> Field-face source wall.
```

## 2. Main hidden equivalence

Most source-wall branches are one obstruction in different coordinates:

```text
LocalPositiveSourceCarleson.A
SOURCE.NO-PULSE.A
SourcePulseExclusion.A
NativePositiveSourceControl.A
ScaleCriticalTreeCarleson.A
ActiveSquareCarleson.A
ActiveSquareResidualTail.A
SquareSource^norm.A
SignedLocalSource.A
CriticalFluxAmplitudeSmall.A
ParabolicEdgeResistance.A
TerminalAmplitudeGain.A
```

They all point at terminal positive source creation on the Field face.

Rule:

```text
Any source-wall theorem must say how it enters Field.
Any theorem that lands only in a source alias stays support-level.
```

The source wall becomes proof-relevant only through:

```text
FieldFaceResidueReduction.A.
```

Historical route sketch, not proof force:

```text
not Field
=> VisibleJump_Field
   or missing receiver supplier
   or missing read-cover/no-jump supplier
   or missing NativePositiveSourceControl
   or Zeno residue.
```

Exact current use: this display may only be cited as a list of candidate
Field-facing support branches.  To become CM proof material, the branch must be
admitted to the same terminal witness record and must derive the first failed
face

```text
not Pack_Q,
Pack_Q + not Part_{N,Q},
or
Pack_Q + Part_{N,Q} + forall r>0 not Field_{N,r,Q}.
```

Otherwise it remains supplier quarantine or downstream readout support.

After the non-source alternatives are routed, the live split is:

```text
not Field
=> missing ScaleCriticalTreeCarleson.A
   or
   ZenoBoundedClass_B.A + ZenoResidueLiouville_B.A.
```

## 3. Best direct target

Earlier framing put the target at:

```text
ReserveCreationCharge.A.
```

The sharper target is upstream:

```text
HeightFluxControl.A.
```

The donor square reserve is:

```math
R_N(W)
:=
\int_W
\sum_{k>N}
2^k
\left(
\sum_{\ell>k+4}D_\ell(t)
\right)^2
dt.
```

Define:

```math
T_k(t)=\sum_{\ell>k+4}D_\ell(t),
\qquad
H_k(t)=2^kT_k(t),
\qquad
H_N(W)=\sup_{t\in W}\sup_{k>N}H_k(t).
```

Then:

```math
R_N(W)\le H_N(W)\,M_N(W),
```

where `M_N(W)` is the first-moment donor tail.

Energy can make `M_N(W)` small.  A square pulse can still survive if
`H_N(W)` becomes huge.  Therefore first-time reserve creation requires large
donor height.  It is not automatically first-time donor-height creation; the
attempted first-height dichotomy is not independent and collapses back to the
structural `HeightFluxControl.A` problem.

The sharpened direct route is:

```text
HeightFluxControl.A
=> DonorHeightCreation.A
=> ReserveCreationCharge.A
=> SquareReserveEvolution.A
=> ScaleCriticalTreeCarleson.A.
```

`DonorHeightCreation.A` asks for:

```math
\left[
H_N(W)-H_N(Past(W))-Loss_{legal}^{height}(W)
\right]_+
\le
C\,Charge_N^{height}(W)+o_N(1).
```

The shell-balance attempt shows that the derivative grammar is straightforward.
The obstruction is the positive weighted nonlinear shell flux:

```math
\int_W
\sup_{k>N}
\left[
2^k\sum_{\ell>k+4}2^{2\ell}F_\ell(t)
\right]_+
dt.
```

So the actual missing theorem is:

```text
HeightFluxControl.A:
control the positive weighted shell flux by legal native charge.
```

This is the highest-value audit-open atom.

## 4. What HeightFluxControl.A probably needs

Scalar damping cannot prove it.  Energy area cannot prove it.  Absolute
commutator bounds loop back to the source wall.

The proof needs one of these structural mechanisms:

```text
signed cancellation that survives one-sided height weights
active strain decorrelation
pressure/viscous depletion of expanding directions
native height-charge monotonicity
terminal tangent rigidity
```

The dangerous flux should be read as the aligned positive strain carrier:

```math
|w_\ell|^2
\left[
e_\ell\cdot S_{<\ell}e_\ell
\right]_+.
```

The intermediate target once proposed was:

```text
FirstHeightCreationDichotomy.A
```

Statement shape:

```text
On a same-fluid terminal Field-face packet, first creation of large H_k either
descends from a parent packet, pays a local positive strain/source charge
visible to the legal ledger, violates active strain alignment, or violates
terminal packet rigidity.
```

The terminal attempt shows this does not sit independently between
`HeightFluxControl.A` and `DonorHeightCreation.A`; it is either tautological
positive shell-flux accounting or it spends the same active-alignment / TPNI /
retained-pack-gain / Zeno machinery. Move on from it as a headline target.

## 5. Zeno route

The Zeno branch is useful only if it escapes the direct source wall.

Current intended chain:

```text
TemporalNonAtomicSource.A
=> TerminalResidueAtomExclusion.A
=> ShrinkingWindowEvacuation.A
=> ZenoResidueLiouville_B.A.
```

The target is:

```text
terminal source-residue measure has no atom at terminal time.
```

Danger: temporal integrability routes usually return to:

```text
HeatScaleSquareSource.A / ScaleCriticalTreeCarleson.A.
```

So the independent Zeno target is:

```text
ResidueEvacuationWithoutSquareSource.A.
```

Meaning:

```text
A positive terminal residue atom forces backward-in-time source presence on
shrinking terminal intervals.  The accepted no-incoming / still-live class
excludes that backward presence.  Therefore the terminal atom evacuates.
```

This is the condition under which the Zeno branch becomes genuinely parallel.

## 6. Signed-current branch

The signed-current route is tertiary until it proves a real cancellation
principle.

The active hard gate is:

```text
WeightedLiftedSkewDefectLegal.A.
```

The active obstruction is:

```text
ActiveStrainAlignmentCost.A.
```

The likely geometric theorem is:

```text
TerminalTangentNoIncomingRigidity.A
```

or:

```text
ActiveStrainAlignmentCost.A
```

via:

```text
TPNI.A
+ PacketNormalForm.A
+ SourceTether.A
+ LimitPass.A.
```

This branch should receive fewer cycles than `HeightFluxControl.A` until it
produces a cancellation theorem that survives localization, saturation, lifted
commutators, one-sided weights, and positive-part extraction.

## 7. Endpoint matrix discipline

Endpoint is a consumer matrix:

```text
END.Pack
END.Field
END.TowerBound
END.Cross
END.Exh
=> End_NS.
```

Keep these three distinct:

```text
FieldFaceResidueReduction.A = branch reduction
FieldSourceResidueJump.A    = endpoint classification
END.Field                   = endpoint exclusion
```

Forbidden shortcut:

```text
source residue visible
=> jump label
=> contradiction.
```

The contradiction requires:

```text
END.Field:
Field + OFP.A => no Jump.
```

Thus the missing work remains inside Field preservation / source control.

## 8. Historical proof graph and exact replacement

```text
A. Classical local existence gives still-live windows.

B. Exact CM witness theorem gives:
   Pack + Part + Field => Member.

C. First finite-time nonsmoothness gives:
   class exit => Pack failure or Part failure or Field failure.

D. Pack and Part are routed by retained terminal packet machinery.

E. Remaining branch:
   Pack + Part + not Field.

F. FieldFaceResidueReduction routes not Field to:
   direct source wall or Zeno residue.

G1. Direct source wall closes by:
   HeightFluxControl
   => DonorHeightCreation
   => ReserveCreationCharge
   => SquareReserveEvolution
   => ScaleCriticalTreeCarleson.

G2. Zeno residue closes by:
   TerminalResidueAtomExclusion
   => TemporalNonAtomicSource
   => ZenoResidueLiouville.

H. Field restored.

I. CM restored.

J. Member restored.

K. First class exit contradicted.

L. Global smoothness on the torus-first target.
```

The graph above is not a clean proof graph under the current CM discipline.
Steps H--K promote a supplier route into positive no-exit closure.  The exact
replacement is the pass/fail obstruction engine:

```text
positive supplier branch reaches a real obstruction
=> pass branch: lawful in-class / Member(Q) continuation mechanism
=> fail branch: CM-test admission + first Pack/Part/Field face failure
=> Exit(Q):=not Member(Q) for that same witness.
```

The branch proves CM-facing work only through that exact face landing.  A
source-control theorem can still support the pass side, but a source alias,
height-flux label, or endpoint readout label does not by itself restore Field,
restore CM, restore Member, or contradict first exit.

## 9. Subagent lens allocation

### CM core lens

Maintain:

```text
CM = Pack + Part + Field
CM => Member
first-exit witness => face failure
```

Audit every theorem for its landing face.

### Source-wall lens

Historical primary, now consumed as an independent fresh-progress label:

```text
HeightFluxControl.A.
```

Secondary:

```text
FirstHeightCreationDichotomy.A
DonorHeightCreation.A
ReserveCreationCharge.A.
```

Every source theorem must reduce the positive weighted shell flux.

Current exact requirement: a source theorem must either pay the same-witness
branch in the installed ledger, or promote the admitted finite obstruction to a
specific Pack/Part/Field face failure.  Reducing a positive weighted shell flux
inside a supplier estimate is not enough unless that reduction supplies one of
those interfaces.

### Zeno lens

Primary:

```text
TerminalResidueAtomExclusion.A
ResidueEvacuationWithoutSquareSource.A.
```

Temporal non-atomicity must avoid hidden dependence on
`ScaleCriticalTreeCarleson.A`.

### Signed-current lens

Primary:

```text
WeightedLiftedSkewDefectLegal.A.
```

Every cancellation theorem must survive localization, saturation, and
one-sided weights.

### Endpoint/readout lens

Keep `END` rows and `READ.COVER` downstream.  Prevent endpoint classification
from acting as endpoint exclusion.

### Skeptic lens

Demote:

```text
source aliases presented as CM closure
signed-current estimates using absolute values only
Zeno integrability that routes back to square source
endpoint rows used as upstream suppliers
whole-space claims before torus-first closure
```

## 10. Next exact theorem attempts

### Attempt 1: historical `HeightFluxControl.A`

Concrete form:

```math
\int_W
\sup_{k>N}
\left[
2^k\sum_{\ell>k+4}2^{2\ell}F_\ell(t)
\right]_+
dt
\le
Charge_N^{height}(W)+o_N(1).
```

Expected proof components:

```text
dyadic shell balance
positive strain decomposition
parent/child packet ancestry
finite-overlap terminal packet selection
legal pressure/cutoff ledger
active strain decorrelation or depletion
```

Current status: this remains a supplier estimate template.  It is not an
installed CM theorem and it cannot be spent as a proof of Field restoration.
The live leaf has moved to same-witness terminal anti-concentration /
source-Carleson / rigid-anti-atom / non-source Pack-survival, or to a proved
same-ledger payment that makes this estimate enter the pass branch.

### Attempt 2: `FirstHeightCreationDichotomy.A`

Concrete form:

```text
first creation of large H_N(W)
=> inherited parent height
   or legal height charge
   or terminal tangent no-incoming violation.
```

### Attempt 3: `TerminalResidueAtomExclusion.A`

Concrete form:

```text
positive terminal source atom
=> fixed fraction persists on shrinking terminal intervals
=> evacuation through no-incoming/time-rigidity
=> contradiction.
```

### Attempt 4: `ActiveStrainAlignmentCost.A`

Concrete form:

```text
terminal high packets cannot maintain positive alignment with low-strain
expanding eigendirections without producing source ancestry / incoming pulse.
```

## 11. Honest verdict

The repo is coherent.  The CM architecture is real.  The contrapositive
class-exit route is the correct organizing principle.

The proof remains open at the same-witness terminal source/Pack-survival
frontier recorded by the live source-frontier surfaces.  The old
Field-face-source-wall language is retained only as historical support unless a
separate theorem places the branch in the current Pack/Part/Field witness tree.

The most valuable consolidation is:

```text
Source wall
= positive weighted shell-flux creation
= donor-height first creation
= terminal Field-face source concentration.
```

The old strongest next mathematical move was:

```text
prove HeightFluxControl.A.
```

The current next mathematical burden is sharper: prove the same-witness
terminal anti-concentration / source-Carleson / rigid-anti-atom /
non-source Pack-survival theorem, or prove an equivalent same-ledger payment
that promotes the retained positive-scale source branch without importing a
positive no-exit shortcut.

The most promising proof idea is:

```text
first-time creation of donor height must be charged by a native shell/strain
ledger, because uncharged first creation would create a terminal active packet
with no ancestry, which should violate same-fluid participation plus
no-incoming terminal rigidity.
```

That is historical proof-search context.  It is not the current theorem-crank
target unless the live frontier explicitly reopens this supplier estimate as the
same-witness anti-concentration or same-ledger payment mechanism.
