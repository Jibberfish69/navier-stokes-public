---
ns_viewer:
  theorem_id: annular-limit-witness-gap-goal-run-primer-20260611
  status: corrected-live-goal-run-primer-open
  proof_role: gold-forward-positive-and-silver-cm-annular-limit-reopen
  logical_landing_node: terminal_cone_annular_limit_witness
  edge_effect: "Reopens the overclaimed terminal-radius closure for the resumed goal-run. The zero-scale point-object lands Part-side when it still carries the same original-NS participation record, and packet/window evidence only when that record is absent; Thomas's objection is that the singularity is witnessed by the surrounding positive-radius fields as they approach the tip. The next run must either prove forward-positive annular/cone control in residual scale-critical currency, or admit the annular limiting behavior as the same-solution terminal witness and classify the admitted nonsmooth branch by Part or Field. A bare zero-radius Pack landing does not settle the positive-radius annular witness, and Pack is not the CM argument after annular admission."
  upstream_origin:
    - problems/navier-stokes/theorem-construction/mpp-residual-refill-ledger-and-spine-consolidation-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-active-square-obstruction-pass-or-pack-exit-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-terminal-zeno-pack-failure-correction-20260521.md
    - problems/navier-stokes/theorem-construction/mpp-any-finite-failure-witness-cm-exit-20260521.md
  downstream_consequence: "The resumed PDF/math goal-run must not claim the terminal-radius problem is solved merely by naming the endpoint not Pack_Q. The zero-scale endpoint itself must pass the Pack-out-of-CM audit, and the run must follow the approaching annular fields and either close them by the gold forward-positive route or, after annular admission, land the finite nonsmooth branch at Part or Field by the silver route."
---

# MPP Annular Limit Witness Gap Goal-Run Primer

Date: 2026-06-11

Status: live primer for the resumed Navier-Stokes goal-run; this is an open
proof/PDF burden, not a completed theorem note.

## 1. Why This Note Exists

The earlier audit overclaimed the terminal-radius closure.

The corrected Pack-out-of-CM grammar says

```math
r_*=0
\quad\hbox{lands Part-side when the same original-NS participation record remains,}
```

and becomes true packet/window evidence only when that original participation record is
absent. That point-object split still does not by itself handle Thomas's
objection:

```text
the singularity lives in the surrounding fields near the tip.
```

The actual blow-up claim is not only a naked point at radius zero.  It is a
limiting behavior of positive-radius fields, annuli, and cone slices that remain
smooth before the terminal time and may become unbounded as they approach the
tip.

So the resumed run must not stop at:

```text
zero radius => terminal closure.
```

It must pay the surrounding annular witness.

## 2. What Today Actually Established

The cone/tower work corrected the forward-positive target.

Raw LPAS is false by the smooth heat-shear counterexample in

```text
mpp-direct-lpas-raw-active-square-false-shear-counterexample-20260611.md
```

The live load is the residual nonlinear refill

```math
D_j^{res},
```

defined after heat descendants, through-flow, legal losses, finite payments,
and paid dissipation are removed.

The residual refill ledger says a failure of residual LPAS/CTS/FCTS selects one
object:

```text
terminal residual source-balanced active pulse.
```

The active-square pass-or-Pack note now reads the zero-radius endpoint of that
pulse through the Pack-out-of-CM audit:

same original-NS participation first, true packet/window evidence only when
that record is absent.

That sorts the endpoint. The missing payment is the limiting positive-radius
annular field that witnesses the approach to the endpoint.

## 3. The Live Gap

Let

```math
r_m \downarrow 0,\qquad t_m \uparrow T_*,
```

fix constants `0<\alpha<\beta` and `\theta>0`, and look at the backward
annular parabolic windows around the alleged tip:

```math
A_m
=
\{(x,t): \alpha r_m < |x-x_0| < \beta r_m,\ t_m-\theta r_m^2 < t < t_m\}.
```

Every `A_m` has positive radius.  The fields on `A_m` are still fields of the
same preterminal Navier-Stokes solution.  A singularity may be witnessed by the
behavior of these fields as `m -> infinity`.

The proof must decide what happens to that annular witness:

```text
Does the annular field become controlled?
Does it fail annular admission because no selected same-solution carrier is
available?
After admission, does it lose the pressure-viscosity participation tower?
After admission and participation, does it lose positive-scale one-field
coherence?
```

That is the missing bridge between the cone discussion and the silver CM
landing. Pack belongs to the pre-admission/no-carrier boundary in this annular
question. Once the annular witness is admitted, the silver landing is Part or
Field.

## 4. Gold Standard: Forward-Positive Annular Control

The gold route tries to prove control before using CM exit.

Working theorem target:

```text
AnnularLimitForwardControl.A
```

The target is a residual, scale-critical theorem.  It must use the corrected
currency:

```math
D_j^{res},
```

not raw shell size, raw wall flux, raw cone volume, first-moment global drain,
or bare derivative-tower amplitude.

The theorem target must prove one of the following forward closures, or a
checked implication from one of them to the same residual annular readout:

```text
ResidualSourceBalancedLPAS.A
ResidualDirectCTSOrFCTS.A
AnnularScaleCriticalConeBudget.A
AnnularLateSlabSmallness.A
```

A proof must account for:

1. the moving annular walls, including inner and outer boundary flux;
2. pressure work, viscous exchange, transport, and moving-boundary terms;
3. the derivative tower depth debt, including the `N+2` velocity depth and
   pressure depth;
4. donor drain and payment, measured in residual refill currency;
5. the rescaled annular profile on fixed annuli in cone variables;
6. the fact that finite physical energy is discounted and implies unweighted
   scale-critical control only after a discount-removal theorem.

Gold acceptance:

```text
The annular field cannot retain scale-critical residual load through infinitely
many terminal cone slabs.
```

Equivalently, the surviving late annular slabs must fall below a named
CKN/H^s continuation threshold with the required local energy, pressure,
velocity-depth, and readout hypotheses stated on the same annular record.

## 5. Silver Standard: CM Annular Face Landing

The silver route is used only for the residual obstruction that survives the
gold route.

Working theorem target:

```text
AnnularLimitCMFaceLanding.A
```

The CM classifier must follow the annular witness before naming the endpoint.

Pre-admission boundary: no selected annular carrier.

```text
The surrounding positive-radius fields do not retain a coherent same-fluid
carrier into the terminal witness.
```

Meaning:

```math
\text{the annular witness has not been admitted for this silver test.}
```

This may still be Field-window support for a point-object or generic no-carrier packet,
but it is not the CM argument for an admitted annular residual supplier.

Case 1 after admission: participation fails.

```text
The annular field has been selected as the same-solution terminal witness, but
the same pressure-viscosity, incompressibility, source, and derivative-tower
participation law cannot remain finite on that carrier.
```

Landing:

```math
\neg Part_{N,Q}.
```

Case 2 after admission: participation survives but one-field coherence fails.

```text
The annular witness keeps same-equation participation, but no positive coherent
field scale remains that supplies the continuation readout.
```

Landing:

```math
\forall r>0\,\neg Field_{N,r,Q}.
```

Silver acceptance:

```text
The admitted annular blow-up behavior is smooth/Member when Part and a positive
Field readout survive, and is finite nonsmooth/Exit only through Part or Field.
```

## 6. PDF Consequence

The PDF must not say the terminal-radius problem is solved in one sentence.

The paper must say, in ordinary referee-facing language:

```text
The terminal point is not treated as a packet.  The proof tests the fields on
shrinking positive-radius annuli around the alleged singular point.  If those
annuli retain the full continuation packet, the solution continues.  If the
annular witness is not admitted, the silver test has no annular object to spend.
Once it is admitted, a finite nonsmooth branch is recorded as Part or Field
failure.
```

That is the reader-facing payment for Thomas's objection that the singularity is
witnessed by the fields around the tip.

## 7. Instructions For The Resumed Goal-Run

The run is PDF-focused, but the math runner must carry this note as a live
frontier.

The gold route is the forward-positive annular route:

```text
AnnularLimitForwardControl.A
ResidualSourceBalancedLPAS.A
ResidualDirectCTSOrFCTS.A
AnnularScaleCriticalConeBudget.A
```

The silver route classifies only the surviving residual annular pulse:

```text
AnnularLimitCMFaceLanding.A
```

The PDF worker must integrate whichever result is actually proved.  It must not
present a bare endpoint Pack failure as a complete explanation of a blow-up
witnessed by surrounding positive-radius fields.

## 8. Non-Drift Rule

Do not restart the old 214-attempt theorem sprawl.

Every theorem attempt belongs to one of these roles:

```text
gold forward annular control,
silver annular CM Part/Field landing,
PDF explanation of the proved branch,
or support/quarantine.
```

Anything else is drift for this goal-run.
