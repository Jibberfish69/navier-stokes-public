# MPP AntiDiffuseParent.A Direct Attempt Note

## Status

Direct attempt on the live ancestry subtarget isolated by
`mpp-sourceparent-edge-disintegration-and-antidiffuse-split-note.md`:

```math
\boxed{
AntiDiffuseParent.A.
}
```

The target is not discharged by the installed estimates.  The attempt isolates
the exact replacement theorem.

## Target

Given the positive parent edge measure

```math
d\pi_P(P^-)
```

for a source-balanced child pulse `P`, prove that either:

1. **bounded parent concentration**

```math
\exists\ \mathcal C(P)\subset\mathsf{Pred}(P),
\qquad
|\mathcal C(P)|\le B,
\qquad
\pi_P(\mathcal C(P))\ge c\,\pi_P(\mathsf{Pred}(P));
```

or

2. **diffuse charge**

the complement is paid by a finite installed reserve:

```math
\pi_P(\mathsf{Pred}(P)\setminus\mathcal C(P))
\le
Charge_P+Reserve_P+o(1).
```

This is exactly what is needed to turn aggregate source recovery into a
comparable legal parent pulse and to make iterated ancestry lose only summably.

## Frequency Finite-Complexity Test

The dyadic interaction band is finite:

```math
j_{P^-}\in[j_P-M,j_P+M]
```

or in a licensed lower-feed band.  This gives finite frequency complexity, but
not finite physical parent complexity.

At heat scale `r_P`, the torus contains at most `C_{\mathbb T^3}r_P^{-3}`
possible spatial cells.
The positive source feeding one child can be distributed across many such cells
or across many triadic pieces while the total parent measure remains order one.
Frequency localization alone therefore does not give the required bounded
subcollection `\mathcal C(P)`.

## Local-Energy / Reserve Test

The installed finite ledgers control raw energy, dissipation, pressure/cutoff
terms, and selected source integrals.  They do not price the entropy or
branching number of the parent measure.

A diffuse model captures the gap:

```math
\pi_P=\frac1M\sum_{\alpha=1}^M\delta_{P^-_\alpha},
\qquad
M\to\infty.
```

Then

```math
\pi_P(\mathsf{Pred}(P))=1,
```

but every bounded subcollection carries mass at most `B/M\to0`.  The raw
energy and local-energy ledgers see the total mass; they do not force it to
concentrate on boundedly many parents or charge the diffuse distribution.

## Signed-Commutator Test

The signed lifted source is tested before the one-sided positive edge measure
is taken.  Its cancellation does not prevent many small positive parent edges
from feeding the same child after passing to

```math
d\pi_P=(d\mathcal S^{nl}(P^-\to P))_+.
```

The principal strain form can align positively on selected active windows.
Thus signed cancellation does not supply a parent lower-density theorem.

## Positive-Carrier Test

The positive-carrier estimates bound aggregate envelopes after absolute values
and Cauchy/Young:

```math
|\mathfrak G_N^{lift,\sigma}|
\le C_\sigma
\sum_{k>N}2^ka_kU_k.
```

Those estimates forget the child-to-parent edge structure.  They can upper
bound total source size, but they do not imply that a bounded subcollection of
parent edges carries a fixed fraction of the positive parent measure.  If the
positive carrier is used as the charge, the route returns to the active-square
/ square-source reserve already audited as another spelling of
`SourcePulseExclusion.A`.

## Pressure / Source-Supplier Test

Pressure and cutoff ledgers can route legal error terms once those terms are
separated.  The downstream frozen-family source supplier

```math
FFSRC.A\Longrightarrow FCI.5f
```

is an integrability supplier after the receiver/source route is admitted.  None
of these surfaces prices the diffuse part of a positive same-fluid parent
measure on the branch-native bad active windows.

## Same-Fluid Transport Test

Same-fluid transport preserves labels after a parent is selected.  It does not
select a concentrated parent from a diffuse positive source distribution.  A
broad low/intermediate strain field or a diffuse triadic source aggregate can
feed the child while no single predecessor packet has comparable normalized
mass.

Thus same-fluid fidelity does not imply anti-diffuse parent concentration.

## Exact Replacement Theorem

The missing theorem is:

```math
\boxed{
ParentConcentrationOrDiffuseCharge.A:
\quad
\text{every positive source-parent measure either has bounded-complexity
concentration or its diffuse part is charged to a finite route reserve.}
}
```

Equivalently:

```math
\boxed{
AntiDiffuseParent.A
\equiv_{\mathrm{route}}
ParentConcentration.A
\vee
DiffuseParentCharge.A.
}
```

where:

```math
ParentConcentration.A
```

is a reverse-holder / concentration theorem for `d\pi_P`, and

```math
DiffuseParentCharge.A
```

is a theorem pricing high-branching source diffusion by an installed finite
reserve.

The direct split audit is recorded in
`mpp-parent-concentration-or-diffuse-charge-direct-audit-note.md`: neither
bounded parent concentration nor diffuse-parent charge is installed by the
current finite-band, same-fluid, local-energy, pressure/cutoff,
positive-carrier, or active-square surfaces.

## Verdict

`AntiDiffuseParent.A` is not installed by current dyadic finite-band structure,
same-fluid transport, local energy, pressure/cutoff ledgers, or frozen source
suppliers.

The ancestry route has therefore been sharpened to:

```math
\boxed{
ParentConcentrationOrDiffuseCharge.A
+
\bigl(
AncestryDrain.A
\vee
AncestryCompactBound.A+SourceResidueLiouville.A
\bigr)
\Longrightarrow
SourcePulseExclusion.A.
}
```
