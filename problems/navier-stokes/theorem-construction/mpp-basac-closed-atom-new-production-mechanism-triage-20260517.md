# MPP B_ASAC^closed new production mechanism triage

Date: 2026-05-17

Status: bounded triage. No installed seventh production mechanism is identified. The only distinct candidate is a pressure/Leray tether, and current route state treats it as conditional near-band source-current support.

## Starting point

The canonical obstruction class is

```math
B_{ASAC}^{closed}.
```

It is already identified as a Zeno terminal zero-radius residue, hence terminal
`not Pack_Q` rather than a retained Field-face packet. The six audited production
doors are:

```text
temporal anti-concentration;
localized positive-source Carleson / PRD;
rigid residue class plus Liouville;
active-square reserve;
global transported no-incoming flux;
B_ASAC^flux spatial exhaustion.
```

A new mechanism must produce a positive quantitative property outside this list.

## Candidate: pressure-source absolute continuity

The only structurally different candidate is a pressure/Leray tether:

```math
PressureSourceAC.A
```

or finite-packet form

```math
LocalizedLeraySourceTether.A_{ind}.
```

The desired statement is that the pressure quadratic-gradient defect or Leray-projected near-band pressure channel is absolutely continuous with respect to the native source-current carrier in a way that forbids a pure terminal time-face atom.

A useful exact target is:

```math
\mu_*^{press}(E)=0
\quad\text{whenever}\quad
\mu_*^{src}(E)=0,
```

combined with a pressure-side estimate forcing spread away from `s=0` or forcing a finite packet source-current witness before terminal collapse.

## Test 1: residue-level pressure absolute continuity

The pressure solves an elliptic equation tied to velocity gradients. This creates nonlocal coupling. The available source-wall ledgers record pressure and near-band source-current terms as conditional supports.

They do not supply a theorem saying that pressure quadratic-gradient residue is absolutely continuous with respect to the native positive source marginal after localization, projection, terminal weighting, and positive selection.

Thus residue-level pressure absolute continuity remains a new theorem, not an installed mechanism.

## Test 2: finite-packet Leray-source tether

A finite-packet tether asserts that the localized Leray pressure/source channel must remain attached to the selected native source-current before terminal positive-part extraction.

The obstruction is the selection gap. Leray projection and pressure localization can move mass across packets and signs. After one-sided terminal selection, signed cancellation can disappear. A tether strong enough to survive this step becomes a signed no-free-sink or PRD-type theorem.

Hence the finite-packet tether currently returns to `TerminalWeightedNoFreeSink.A_ind` or `PositiveRemainderDepletion.A`.

## Test 3: pressure sustain residue forces source residue

If pressure sustain residue forced native source residue with temporal spread, it would open the temporal anti-concentration door. If it forced active-square reserve, it would open the active-reserve door. If it forced transported tail control, it would open the flux door.

Current route notes treat pressure/Hessian material as conditional near-band source-current support. That is useful support, while it does not yet produce a seventh production property.

## Verdict

The pressure/Leray tether is the only credible new mechanism outside the six audited doors.

At current route resolution it has this status:

```math
PressureSourceAC.A / LocalizedLeraySourceTether.A_{ind}
\quad\text{is conditional support, not an installed anti-atom production theorem.}
```

The next sharp target, if this direction continues, is:

```math
PressureSourceAC.A:
\text{pressure quadratic-gradient residue is absolutely continuous with respect to the native source-current residue, with a spread estimate strong enough to kill }\delta_{s=0}.
```

The present note records the triage result: no seventh production mechanism is installed, and pressure/Leray tether is the best candidate to isolate next.
