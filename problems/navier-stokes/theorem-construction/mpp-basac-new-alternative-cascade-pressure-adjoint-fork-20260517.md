# MPP B_ASAC New-Alternative Cascade: Pressure / Adjoint Fork

Date: 2026-05-17

## Status

Failed direct closure. This note persists the new-alternative search beyond the all-doors `B_ASAC` obstruction.

## Starting point

The target remains

```math
ZenoResidueLiouville_{B_{ASAC}}.A.
```

The survivor after the exhaustive all-doors pass is

```math
B_{ASAC}^{closed},
```

carrying a terminal native positive source atom compatible with the installed ledgers.

## Alternative 1: atom-escape dichotomy

The proposed route is

```math
BASACAtomEscapeDichotomy.A:
\mu_*^{src}(B_R\times\{0\})>0
\Longrightarrow
Flux\vee Residence\vee PressureEigenframeSustain.
```

The direct proof reduces to a sharper theorem:

```math
PositiveSourceAdjointEscapeIdentity.A.
```

The blocker is topology: installed adjoint identities control signed-current or near-band presentations, while the target requires the positive source-weighted carrier.

## Alternative 2: positive-source adjoint escape identity

The desired identity leaves an unpriced positive adjoint defect:

```math
AdjDefect_m^{+,src}
:=
\sum_P\int_{Q(P)}|w_{j_P}|^2[e_{j_P}\cdot S_{<j_P}^{loc}e_{j_P}]_+dxdt.
```

The sharper theorem is

```math
PositiveAdjointDefectDepletion.A.
```

Its direct attempt returns to the same source-weighted angular depletion carrier. Thus it gives a clean restatement, not a new discharge.

## Alternative 3: pressure/eigenframe to native source carrier

The next distinct route is

```math
PressureQuadraticDefectNativeCarrier.A
```

with equivalent supplier names

```math
PressureSourceAC.A,
\qquad
LocalizedLeraySourceTether.A_{ind}.
```

Direct tests through pressure Poisson, Calderon-Zygmund, Leray projection, local energy, and strain equation all fail to produce the native same-fluid source-current carrier after terminal positive selection.

The missing finite-packet gates are:

```math
TerminalLerayOrientation_{ind}.A,
\quad
TerminalLerayCommutatorLegal_{ind}.A,
\quad
TerminalLeraySaturation_{ind}.A.
```

The missing residue gate is:

```math
PressureSourceAC.A:
\mu_*^{press-sus}\ll \mu_*^{src}+\mu_*^{legal,src}.
```

## Alternative 4: pure pressure-sustain residue Liouville

The pressure carrier decomposition leaves a singular part

```math
\mu_*^{press\perp src}\perp(\mu_*^{src}+\mu_*^{legal,src}).
```

The source-wall machinery consumes native source residue. The singular pressure-sustain branch requires its own theorem:

```math
PurePressureSustainResidueLiouville.A:
\mu_*^{press\perp src}=0.
```

Current pressure legal ledgers cover cutoff, projector, harmonic collar, off-family tail, and boundary pressure work. The retained interior singular pressure-sustain residue sits beyond those ledgers.

## Result

The new-alternative cascade produces a genuine fork, still open from installed inputs:

```math
\boxed{PressureSourceAC.A / LocalizedLeraySourceTether.A_{ind}}
```

or

```math
\boxed{PurePressureSustainResidueLiouville.A.}
```

Either fork requires new theorem content beyond the current `B_ASAC` equality-class production.

## Verdict

The `B_ASAC` target remains unresolved. The alternative search has advanced the frontier from the original six doors to one additional pressure/adjoint fork. Closure requires one of the fork theorems or one of the earlier anti-atom/flux/entropy/rigid-production theorems.
