# MPP B_ASAC Source-Ancestry Compactness / Transported Flux Reentry

## Status

Active-completion reentry. This note does not close the public Clay bridge.

It attacks the current source-residue target:

```math
BASACSourceAncestryCompactness.A.
```

## Target role

The theorem would bridge:

```text
no fixed selected earlier source parent
```

to:

```text
no terminal source atom.
```

For the produced `B_ASAC` source-residue class, it would say that a nonzero
terminal source charge must create a fixed earlier selected source parent or a
legal/donor/ASAC exit.

## No-incoming flux attempt

The class-specific no-incoming target is:

```math
NoIncomingFlux_{B_{ASAC}}.A
```

or, in transported-cylinder form:

```math
TransportedCylinderNoFlux_{B_{ASAC}}.A.
```

Same-fluid transported cylinders are the right geometry for boundary inflow
bookkeeping. They remove fixed-frame carrier mismatch and isolate the physical
inflow/pressure/cutoff channels.

## What the current inputs give

The produced `B_ASAC` class has:

```text
same-fluid terminal carrier,
zero ASAC defect,
paid legal/projected/cutoff/boundary exits,
paid finite donor trees and non-Zeno refill,
no fixed earlier selected source slice,
finite local L^1_s source mass,
terminal Zeno source support.
```

These are strong classification clauses. They do not give global transported
boundary tightness or temporal thickness.

## Why lateral no-incoming does not eliminate the atom

A compact terminal source layer

```math
F_m^{src,+}(s,x)=m1_{(-1/m,0]}(s)\rho(x),
\qquad
\rho\in C_c(B_1),\quad \rho\ge0,
```

has bounded local `L^1_s` mass and converges to:

```math
\rho(x)\,dx\otimes\delta_{s=0}.
```

For transported cylinders containing the support of `rho`, lateral incoming
flux can vanish while the terminal time-face atom remains nonzero.

Thus boundary no-incoming and terminal time-face no-atom are separate burdens.

## Full transported no-incoming attempt

A full theorem would need both:

```text
boundary inflow vanishing
```

and:

```text
interior positive source production exclusion on shrinking terminal slabs.
```

The second clause is exactly the terminal time-face anti-atom theorem. Without
it, transported geometry leaves the interior source-production mechanism intact.

## Result

`BASACSourceAncestryCompactness.A` is not proved by current transported
no-incoming surfaces.

The branch reduces to one of:

```math
TerminalTimeFaceNoSourceAtom.A,
```

```math
TerminalSourceReverseHolder.A,
```

```math
ProductionIntoTransportedNoFluxClass.A
+
TransportedNoFluxLiouville.A,
```

or a new generated subclass with both spatial exhaustion and terminal
time-thickness built in.

## Verdict

The transported/no-incoming route is a real diagnostic and conditional route,
not a closure at current authority.

The current live target returns to:

```math
TerminalTimeFaceNoSourceAtom.A
\quad/\quad
TerminalSourceReverseHolder.A
```

for the produced `B_ASAC` terminal source-residue class.
