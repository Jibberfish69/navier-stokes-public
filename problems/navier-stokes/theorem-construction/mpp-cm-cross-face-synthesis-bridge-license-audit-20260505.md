# MPP CM Cross-Face Synthesis Bridge-License Audit

## Status

Theorem-facing bridge-license note for the aggregate class-membership witness
after Pack, Part, and Field supports have been localized on the same witness
ledger. Member is reached only by the installed membership implication after
that triadic witness exists.

This note installs only the cross-face synthesis rule.  It does not prove the
post-reconcile source wall:

```math
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
```

It also does not promote `PCTP.hard`, `SOURCE.NO-PULSE.A`, `AWG.A`, `TGC.A`,
`Jump_avg` elimination, or any averaged endpoint readout into the governing CM
primitive.

## Target

The aggregate bridge-license target is:

```math
\boxed{
CM.CrossFaceSynthesis.A
}
```

If the Pack, Part, and Field faces are all certified on the same carrier,
window, scale, and same-fluid family, then the triadic witness is certified:

```math
Pack_Q
\wedge
Part_{N,Q}
\wedge
Field_{N,r,Q}
\Longrightarrow
CM_{N,r,Q}.
```

Together with the installed CM witness-to-member direction,

```math
\forall N\ \exists r_N>0:\ CM_{N,r_N,Q}
\Longrightarrow
Member(Q;\mathfrak O_{NS}^{work}),
```

this licenses the Member row only after the exact triadic witness exists at all
required depths.

## Synchronized Witness Ledger

The cross-face conjunction is legal only when all localized supports share the
same witness ledger:

```math
\mathcal W
=
(O_{NS}^{work},Q,N,r,\Phi,\mathcal T).
```

Here `Q` is the same retained class region, `N` is the same tower depth, `r` is
the same positive field scale, `\Phi` is the same same-fluid family, and
`\mathcal T` is the same still-live terminal window.

The legal landing faces are exactly:

```math
Pack_Q(\mathcal W),
\qquad
Part_{N,Q}(\mathcal W),
\qquad
Field_{N,r,Q}(\mathcal W).
```

No supplier-only theorem is allowed to occupy one of these faces unless a
separate readout theorem lands it on that exact face.

## Theorem

```math
\boxed{
CM.CrossFaceSynthesis.A:
\left[
Pack_Q(\mathcal W)
\wedge
Part_{N,Q}(\mathcal W)
\wedge
Field_{N,r,Q}(\mathcal W)
\right]
\Longrightarrow
CM_{N,r,Q}(\mathcal W).
}
```

Moreover, if this holds for every required depth:

```math
\forall N\ \exists r_N>0:\
CM_{N,r_N,Q}(\mathcal W_N),
```

with the same `Q`, same still-live carrier, and compatible same-fluid windows,
then the installed CM witness-to-member direction gives

```math
Member(Q)
```

## Proof

By definition, the class-membership witness is triadic:

```math
CM_{N,r,Q}
=
Pack_Q
\wedge
Part_{N,Q}
\wedge
Field_{N,r,Q}.
```

If all three predicates are certified on the same witness ledger
`\mathcal W`, then the conjunction is exactly `CM_{N,r,Q}(\mathcal W)`.
There is no additional analytic estimate in this step; the content of the
bridge-license theorem is the synchronization rule that forbids mixing faces
from different carriers, windows, scales, or branches.

For all-depth membership, apply the same finite-depth synthesis at each `N`,
choosing the certified positive scale `r_N`.  The installed certificate theorem
then gives:

```math
\left[
\forall N\ \exists r_N>0:\ CM_{N,r_N,Q}
\right]
\Longrightarrow
Member(Q;\mathfrak O_{NS}^{work}).
```

This bridge does not install the blunt converse:

```math
\neg CM_{N,r,Q}
\Longrightarrow
\neg Member(Q;\mathfrak O_{NS}^{work}).
```

Thus the cross-face synthesis bridge is licensed exactly at the CM witness
object and nowhere else.

## Supplier Quarantine

The following objects remain supplier/readout objects unless a separate theorem
lands them on `Pack`, `Part`, or `Field` for the same witness ledger:

```math
PCTP.hard/TTU.A,\quad
SOURCE.NO\text{-}PULSE.A,\quad
AWG.A,\quad
TGC.A,\quad
AACT.KX,\quad
Jump_{avg}\Rightarrow\bot.
```

In particular:

```math
SOURCE.NO\text{-}PULSE.A
\not\equiv
Field_{N,r,Q},
```

```math
TGC.A.cover
\not\equiv
Pack_Q\wedge Part_{N,Q}\wedge Field_{N,r,Q},
```

and

```math
PCTP.hard/TTU.A
\not\equiv
Member(Q).
```

They may feed the Field/readout side only through the installed ordered route:

```math
DTC.A_{avg}
\Longrightarrow
AVG.RCV.A
\Longrightarrow
LCI.A_{avg}
\Longrightarrow
CSP.A_{avg}
\Longrightarrow
OFP.A_{avg}
\Longrightarrow
CFI.A_{avg}
\Longrightarrow
End_{NS,avg}
\Longrightarrow
READ.COVER
\Longrightarrow
Field.Read+DTC.Read.
```

## Boundary With The Source-Wall Root

`CM.CrossFaceSynthesis.A` closes the bookkeeping bridge from synchronized face
certificates to the CM witness.  It does not close the live source-wall root.

The source-pulse/no-`Jump_avg` supplier remains conditional until one of:

```math
ScaleCriticalTreeCarleson.A,
```

or

```math
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A
```

is proved.  Those roots are analytic inputs to the Field/source-readout side;
they are not replaced by the logical conjunction theorem above.

## Verdict

```math
\boxed{
CM.CrossFaceSynthesis.A\text{ is installed as a bridge-license theorem.}
}
```

The aggregate CM bridge may close only when Pack, Part, and Field are certified
on the same witness ledger.  The remaining mathematical frontier is still:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```
