# MPP CM Pass/Exit Positive-Existence Veto

Date: 2026-05-25
Status: theorem-facing branch-law correction
Proof mode: Navier-Stokes CM contrapositive class exit

## Claim

The CM pass-or-exit branch law is not a request to produce a smooth
class-member solution. The pass branch is the already lawful smooth
`Member(Q)` side, supplied by whichever valid mechanism witnesses smooth
continuation. The CM work is on the fail branch: admit the obstruction to the
CM terminal packet test and derive a concrete Pack/Part/Field face failure
supporting

```text
Exit(Q) := not Member(Q).
```

## Typed Roles

- Alleged finite Clay terminal breakdown: counterexample object under test.
- CM terminal packet admission: grammar-admissible test entry for that object.
- `Pack_Q`, `Part_{N,Q}`, `Field_{N,r,Q}`: witness faces.
- Face failure: the actual mathematical landing, one of `not Pack_Q`,
  `Pack_Q + not Part_{N,Q}`, or `Pack_Q + Part_{N,Q} + forall r>0 not Field_{N,r,Q}`.
- `Exit(Q):=not Member(Q)`: downstream class-exit conclusion supported by the
  face failure.
- Pass branch: legal smooth / `Member(Q)` side, not a new positive theorem
  demanding construction of a class-member solution.

## Consequence For Tonight's Work

The direct burden

```text
OriginalSmoothData => not GenuineCMExit(T_*)
```

is a positive no-exit route when used as a standalone theorem target. It may
be retained as a failed/direct attempt or as public-closure comparison context,
but it is not the CM branch-law burden for the nightly theorem extension.

Tonight's native theorem pressure must therefore do one of these jobs:

1. prove CM terminal packet admission for an obstruction already selected by
   the direct-live surfaces;
2. derive the first failed witness face in `Pack_Q`, `Part_{N,Q}`, or
   `Field_{N,r,Q}`;
3. quarantine the object as supplier/readout material when no bridge lands it
   inside the witness tree.

## Applied To The Source-Reserve Branch

The same-day source-reserve notes show the distinction. A forward
zero-moment active-height route exposed a positive-visibility gap, while
`mpp-source-reserve-birth-cm-face-consumption-20260525.md` records the CM use:
source-reserve failure is consumed as Pack/Part/Field face support instead of
being turned into a demand to control the reserve by a favorite positive
estimate.

For external threshold-packet claims, the same rule applies. Shahmurov-style
channel dictionaries and Ri-style scale/norm constructions may red-team the
native route, but they only affect the live proof after a lane-local promotion
audit rederives a CM-test admission or face failure inside the native
Pack/Part/Field grammar.

## Nightly Guard

Reject any next theorem target that says or implies:

- produce at least one smooth class-member solution;
- prove smooth data stays in CM as the branch-law burden;
- treat `CM-lawful` as `Member(Q)`;
- label a bad or nonsmooth-capable branch as `Exit(Q)` before deriving the
  Pack/Part/Field face failure;
- make a generated surface, direct attempt, or public-closure comparison
  override the direct-live CM invariant.

The live invariant for Navier-Stokes theorem work remains:

```text
finite terminal breakdown -> CM terminal packet test
first failed Pack/Part/Field face -> Exit(Q):=not Member(Q).
```
