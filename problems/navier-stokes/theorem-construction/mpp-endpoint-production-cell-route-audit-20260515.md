# MPP Endpoint Production Cell Route Audit

## Status

Theorem-facing audit after the retained Field endpoint reduction.

Purpose: decide which endpoint-production route gives the next noncircular
attack surface after
`mpp-retained-field-actkx-license-and-conditional-closure-20260515.md`.

## Candidate Routes

### Pointwise retained route

The pointwise same-family route is

```math
PreEnd.ACTKX_Q
+\text{same-cover pressure readout}
\Longrightarrow
DTC.A_{\mathfrak p}\text{ through depth }N+3
\Longrightarrow
\text{no retained Field exit}.
```

The installed pointwise packets show useful structure:

```math
ACT.KX+ACT.X\text{-}Scale+RWS.C_{\mathrm{scale}}
\Longrightarrow
ACT.X\text{-}Readout
\Longrightarrow
DTC.A_{\mathfrak p}.
```

But this remains a retained-conditional route.  Its native forcing input is
the retained center-ball certificate

```math
RSCB.NKF\Longrightarrow NKF.Native,
```

and therefore it is not an original-data production theorem for the endpoint
cell.  It is clean as conditional Field-row support, but it is not the first
unpaid original-data target.

### Averaged route

The finite-energy-native route is

```math
\neg Jump_{avg}
+TGC.A.cover
+ATD_m^\varepsilon
+AACT.KX.local
\Longrightarrow
DTC.A_{avg}.
```

This route is the better attack surface because the local analytic engine is
already organized without pointwise retained smoothness and without spending
`Field`, `OFP.A`, `End_NS`, `READ.COVER`, `DTC.Read`, or endpoint closure
upstream.

The remaining obstruction is not local `AACT.KX`.  The no-`Jump_avg` side is a
clean conditional production route.  The uncovered complementary branch is

```math
Jump_{avg}.
```

The installed branch combination theorem is

```math
Jump_{avg}+SourcePulseExclusion.A\Longrightarrow\bot.
```

Therefore the first unpaid input for the averaged endpoint-production route is

```math
\boxed{
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
}
```

## Boundary

`SourcePulseExclusion.A` is not the CM primitive and is not a fourth witness
face.  It is the original-data branch killer needed to remove the
`Jump_avg` complement so the averaged `ACT.KX/DTC.A_avg` route can cover the
terminal tail without reading endpoint conclusions upstream.

Equivalently, the same live atom appears under the active-window Carleson,
square-source, signed-local-source, source-parent reserve, and no-pulse names.
Those presentations should not be reopened as separate endpoint-production
targets unless one supplies a genuinely new theorem for
`OriginalSmoothData=>SourcePulseExclusion.A`.

## Resulting Frontier

The pointwise same-family `DTC.A` route stays retained-conditional support.
The averaged no-`Jump_avg` route is the preferred noncircular production route.
The honest next theorem burden is:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

No proof of this burden may use `Field`, `OFP.A`, `End_NS`, `READ.COVER`,
`DTC.Read`, `Field.Read`, or endpoint closure upstream.
