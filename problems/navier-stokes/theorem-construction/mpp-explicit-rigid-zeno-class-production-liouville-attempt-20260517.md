# MPP explicit rigid Zeno class production and Liouville attempt

Date: 2026-05-17

Status: failed unconditional discharge. The conditional rigid-class theorem is valid; the production half fails from installed Zeno ancestry.

## Target

The rigid-class door asks for an explicit ancient class `B` such that

```math
\boxed{
ZenoBoundedClass_B.A:
\text{every surviving same-fluid terminal Zeno branch extracts a limit in }B
}
\tag{B-prod}
```

and

```math
\boxed{
ZenoResidueLiouville_B.A:
(u_*,p_*,\mu_*^{src})\in B
\Longrightarrow
\mu_*^{src}=0.
}
\tag{B-Liouville}
```

Together these eliminate the terminal Zeno source residue and close the rigid branch of

```math
TerminalZenoRefillNoFreeSink.A_{ind}.
```

## Base extracted class

The extraction supplies an ancient local suitable source-residue object

```math
(u_*,p_*,\mu_*^{src})
```

with inherited residue

```math
\mu_*^{src}(Q_1^-)
\ge c_0>0.
```

The strongest class generated directly by the current branch is

```math
B_{sfZ,ASAC}:=
\left\{
\begin{array}{l}
\text{ancient local suitable source-residue limits generated by same-fluid Zeno ancestry;}\\
\text{ASAC-paid retained defects removed;}\\
\text{legal exits and finite donor truncations paid;}\\
\text{locally finite native positive source mass;}\\
\text{no fixed preterminal entrance leaf;}\\
\text{no earlier selected source slice, with possible terminal-layer support.}
\end{array}
\right\}.
```

This class is stronger than the bare compactness class `B_min`, because it records same-fluid ancestry, ASAC payment, and the terminal no-legal-exit branch.

## Liouville test for the generated class

`B_{sfZ,ASAC}` still admits terminal native source atoms. The witness source marginal

```math
d\mu_m^{src}(y,s)
=
\rho(y)m\mathbf 1_{(-1/m,0]}(s)\,dy\,ds
```

has locally bounded source mass and converges weak-star to

```math
\rho(y)dy\otimes\delta_{s=0}.
```

It has zero mass on every fixed earlier selected slab and positive terminal residue. This matches the measure-level data left by the branch. Same-fluid ancestry identifies the chain carrying the mass; ASAC records the paid pair-weight channel; finite truncations telescope. None of these properties removes the terminal atom.

Therefore

```math
B_{sfZ,ASAC}\Longrightarrow\mu_*^{src}=0
```

fails at the level of current extracted structure.

## Candidate rigid add-ons

### Finite global ancient energy

Under parabolic rescaling,

```math
u^{(m)}(y,s)=r_m u(x_m+r_my,t_m+r_m^2s),
```

the global energy scales as

```math
\|u^{(m)}(s)\|_{L^2_y}^2
=
r_m^{-1}\|u(t_m+r_m^2s)\|_{L^2_x}^2.
```

Since `r_m\downarrow0`, the original global energy bound yields local compactness and fails to yield finite global ancient energy.

### Type I

Type I requires a pointwise envelope such as

```math
|u_*(y,s)|
\le
C_{\mathrm{I}} |s|^{-1/2}.
```

The source-pulse branch gives a lower source-residue signal and local suitable compactness. It supplies no pointwise Type I upper envelope.

### Critical smallness

Critical smallness in `L^\infty_sL^3_y`, `L^5_{y,s}`, or an equivalent scale-critical norm is an epsilon-regularity input. The extracted branch carries nonzero terminal source residue on unit cylinders. The current hypotheses produce no smallness parameter.

### Bounded ancient vorticity

A uniform ancient vorticity bound places the ancient limit in a regular class. The compactness input gives weak local energy and pressure control. It supplies no such vorticity bound.

### Self-similarity

A terminal Zeno sequence gives shrinking scales

```math
r_m\downarrow0,
\qquad
\sum_m r_m^2<\infty.
```

The ratios, centers, orientations, and parent choices can vary. Compactness gives a subsequential ancient limit. It supplies no invariance equation under a fixed parabolic scaling.

### Two-dimensional or axisymmetric structure

Same-fluid carrier fidelity preserves route identity and local parent-child geometry. It supplies no invariant axis, no two-dimensional dependence, and no axisymmetric no-swirl structure.

### Source-cancellation class

Define

```math
B_{src}:=
\{\mathcal N_{preCauchy}^{loc}(u_*;\varphi)=0\text{ for every licensed source test }\varphi\}.
```

For this class, the Liouville half is exactly the definition of `B_src`.
Producing `B_src` from the surviving Zeno source-pulse branch is exactly the
native source-depletion theorem. Thus this class packages the desired
conclusion rather than generating a new rigid mechanism.

## Conditional theorem retained

The following conditional implication is valid:

```math
\boxed{
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A
\Longrightarrow
TerminalZenoRefillNoFreeSink.A_{ind}.
}
```

Proof: the failed no-free-sink branch extracts a nonzero terminal source-residue limit. `ZenoBoundedClass_B.A` places that limit in `B`. `ZenoResidueLiouville_B.A` forces its source residue to vanish. This contradicts the inherited lower bound. Hence the terminal Zeno branch is eliminated.

## Verdict

The rigid-class door remains open at the production half. The class naturally produced by same-fluid Zeno ancestry plus ASAC/legal routing is `B_{sfZ,ASAC}`, and it still admits terminal source atoms. Every audited useful rigid add-on requires an input absent from the extraction.

The route therefore returns to one of two remaining genuine mechanisms:

```text
1. temporal anti-concentration / source reverse Holder;
2. active-square or localized positive-source Carleson reserve.
```

This note discharges no unconditional rigid-class theorem. It records the strongest generated class and the exact reason its Liouville theorem fails.
