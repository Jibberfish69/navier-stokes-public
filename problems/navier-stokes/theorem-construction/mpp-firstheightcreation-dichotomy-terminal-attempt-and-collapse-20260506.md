# FirstHeightCreationDichotomy.A Terminal Attempt And Collapse

Date: 2026-05-06

Status: terminal direct attempt; not discharged; demote as independent target.

Exactness rework correction, 2026-06-18: the negative result below remains
valid, but the old "correct direct stack" toward `HeightFluxControl.A` is
historical supplier architecture.  Current live surfaces require a retained
source/height branch to be paid in the same ledger or admitted to the CM test
with a first Part/Field Part/Field failure before it has proof force.

Purpose: pursue `FirstHeightCreationDichotomy.A` to the end and decide whether
it is a real theorem atom below `HeightFluxControl.A`, or only a repackaging of
the same Field-face source wall.

## 1. Target

The intended statement was:

```text
FirstHeightCreationDichotomy.A

first creation of large H_N(W)
=> inherited parent height
   or legal native height charge
   or terminal tangent / no-incoming violation.
```

where:

```math
H_N(W):=\sup_{t\in W}\sup_{k>N}2^k\sum_{\ell>k+4}D_\ell(t).
```

It was inserted to repair the false implication:

```text
first R_N pulse => first H_N creation.
```

The corrected fact is only:

```text
first R_N pulse => large H_N on the active window.
```

## 2. Direct proof attempt

Select a terminal window `W=[s,t]` and an active index `k` such that:

```math
H_k(t)-H_N(Past(W))-Loss^{height}_{legal}(W)>0.
```

The native shell balance gives:

```math
\frac{d}{dt}H_k(t)
\le
\mathcal F_k^{height}(t)
-\mathcal V_k^{height}(t)
+\mathcal L_k^{height}(t),
```

with:

```math
\mathcal F_k^{height}(t)
:=
2^k\sum_{\ell>k+4}2^{2\ell}F_\ell(t).
```

After discarding the negative viscous term and absorbing legal losses:

```math
\left[
H_N(W)-H_N(Past(W))-Loss^{height}_{legal}(W)
\right]_+
\le
C_{\mathrm{height}}
\int_W\sup_{k>N}[\mathcal F_k^{height}(t)]_+dt.
```

Therefore the only available direct dichotomy is:

```text
first height creation
=> positive weighted shell-flux charge.
```

If that positive weighted shell flux is declared to be the legal native charge,
`FirstHeightCreationDichotomy.A` is tautological and proves nothing lower than
`HeightFluxControl.A`.

If it is not declared legal, the proof has reached exactly the open theorem:

```text
HeightFluxControl.A.
```

## 3. Parent-height alternative does not close it

One might try:

```text
large current H_N
=> inherited parent height
   or first height creation.
```

This is only a selection statement. It does not price the first-created height.

Worse, large past height need not have created a past square-reserve pulse.
The scalar high-shell pulse model permits:

```math
T_k(t)=A\,1_{[s,s+\tau]}(t),
\qquad
H_k=2^kA,
\qquad
R_k=2^kA^2\tau.
```

Taking the pulse extremely thin lets `H_k` be large while the past square
reserve remains small. At genuine dyadic level `\ell`, the heat time
`2^{-2\ell}` can be much shorter than the lower donor scale time indexed by
`k`, so thin inherited height is not excluded by first-pulse selection alone.

Thus parent-height inheritance is not a charge theorem. It only moves the same
question to the parent packet unless a well-founded parent/charge ledger is
already installed.

## 4. No-incoming alternative is not installed

A first terminal pulse supplies a local no-earlier-pulse property. It does not
give the global parabolic no-incoming boundary needed for terminal tangent
rigidity.

This is already consistent with the existing `TPNI.A` / no-incoming audits:

```text
first-pulse no-parent
!= global no-incoming ancient boundary.
```

So the terminal-rigidity branch of the dichotomy is not a lower theorem. It
requires the separate open package:

```text
NoIncomingLicense_global.A / TPNI.A
```

or an equivalent residue-evacuation theorem.

## 5. Alignment alternative is not installed

The remaining uninstalled structural implication is:

```text
first height creation
=> active strain alignment cost.
```

But the dangerous source is precisely:

```math
|w_\ell|^2
\left[
e_\ell\cdot S_{<\ell}^{loc}e_\ell
\right]_+.
```

Trace-free incompressibility does not remove a positive expanding eigendirection.
Finite `Pack_Q` controls net deformation, not accumulated positive low-strain
action. Existing Pack-gain audits already isolate this gap:

```text
Pack_Q does not imply PackGainLedger.A.
```

Thus the alignment branch needs a separate theorem such as:

```text
ActiveStrainAlignmentCost.A,
RetainedPackGainProduction.A,
PackGainLedger.A,
or TerminalTangentNoIncomingRigidity.A.
```

That is not `FirstHeightCreationDichotomy.A`; it is the hard source-wall
geometry theorem under another name.

## 6. Conclusion

`FirstHeightCreationDichotomy.A` is not an independent theorem atom.

It collapses to the following fork:

```text
either:
  tautological positive weighted shell-flux accounting

or:
  HeightFluxControl.A / ActiveStrainAlignmentCost.A / TPNI.A /
  RetainedPackGainProduction.A / Zeno residue evacuation.
```

Therefore it does not remain a live next target.

Historical direct supplier stack:

```text
HeightFluxControl.A
  with a genuine structural mechanism
=> DonorHeightCreation.A
=> ReserveCreationCharge.A
=> SquareReserveEvolution.A
=> ScaleCriticalTreeCarleson.A.
```

where the "genuine structural mechanism" must be one of:

```text
ActiveStrainAlignmentCost.A,
RetainedPackGainProduction.A / PackGainLedger.A,
TerminalTangentNoIncomingRigidity.A / TPNI.A,
or a truly independent signed/decorrelation/depletion theorem.
```

The route moves on from `FirstHeightCreationDichotomy.A` as a headline target.
Current live use is narrower: this note only proves that the
first-height dichotomy label cannot be spent as a proof step.  The next live
burden is a same-witness terminal anti-concentration / source-Carleson /
rigid-anti-atom / non-source Pack-survival theorem, or an equivalent same-ledger
payment that promotes the retained branch without a positive no-exit shortcut.
