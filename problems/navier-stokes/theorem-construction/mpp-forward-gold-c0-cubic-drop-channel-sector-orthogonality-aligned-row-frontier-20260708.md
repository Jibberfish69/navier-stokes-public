# Cubic-moment drop channel: sector orthogonality, and the aligned-row frontier (non-separable frame)

**Status: two exact facts PROVED at the strict same-affine atom (leading order); one named OPEN sign. No closure claimed.** Filed 2026-07-08. Runs census item B1 (`mpp-forward-gold-c0-attempt-census-20260707-undeveloped-frontier-20260708.md`) inside the non-separability frame (`mpp-forward-gold-c0-vpi-pressure-non-separability-reading-20260708.md`), under gate rule 14 (no separable coordinates). Consumes the residual-current normal form and rate-order result of `mpp-forward-gold-c0-cubic-radial-strain-moment-turnoff-20260708.md`.

## 0. The physical sentence first

The record's imprint on each material sphere is one ℓ=2 pattern of radial strain: two stretching lobes, one compressive belt. Shedding the imprint outward is an angular motion of the same fluid on that sphere, sliding the pattern's mass along the pattern's own slopes. A current that circles the axis (swirl) or waves around it (m≠0) moves mass ACROSS the pattern's contours: it re-phases the imprint but cannot lower its signed cubic content. Only the meridional current that drains the belt into the lobes lowers it. So **the only motion by which the fluid can shed the record is exactly the motion whose forced pressure row stays aligned with the record.** The shedding cannot be done off-frame. The off-frame (torque) sector and the shedding (drop) sector are different motions of the one record — and every payment attempt of 07-07 priced the first while the record was shedding through the second.

## 1. The two exact facts (translation of §0)

Normal form (already filed): on log-radius ρ with scale-normalized radial readout w and residual angular current j, incompressibility gives ∂_ρ w + div_{S²} j = 0, and the drop of the signed cubic moment M(ρ) = ∫_{S²} w³ obeys, exactly,

M'(ρ) = 3∫ w² ∂_ρ w = −3∫ w² div_{S²} j = 3∫ ∇_{S²}(w²)·j = 6∫ w (∇_{S²}w)·j .

On a strict same-affine atom, w = w₀(μ) ∝ θ·Sθ with S = a·diag(1,1,−2): axisymmetric, pure ℓ=2, m=0. Then:

- **Fact 1 (swirl is drop-null, pointwise).** An azimuthal current j = j_φ e_φ is everywhere perpendicular to ∇_{S²}w₀ (which is meridional), so its integrand vanishes pointwise: zero drop. Swirl re-phases the imprint around the axis; it does not shed it. (Swirl re-enters only at second order — by deforming w away from w₀, which is separately charged by the log-chain Hodge bill, and through its quadratic centrifugal pressure source, which is support. This *derives* the 07-08 owner demotion of swirl to support-only.)
- **Fact 2 (every m≠0 current is drop-null at the atom).** The weight w₀∇_{S²}w₀ is axisymmetric, so the φ-integral annihilates every e^{imφ}, m≠0 component of j: zero drop. Off-frame currents carry the eigenframe torque; they carry no shedding.

Hence, at the atom and at leading order in the (separately charged) deviation from same-affine:

**The drop channel is exactly the meridional m=0 current, and it reads exactly the ℓ∈{2,4}, m=0 gradient components of j** (since w₀∇w₀ = ½∇(w₀²) and w₀² carries ℓ∈{0,2,4}).

## 2. What this settles

1. **The invariant wall of 07-07 is now mechanical, not empirical.** Torque (off-diagonal row) lives in the m≠0 sector; the drop lives in the m=0 meridional sector; at the atom these are orthogonal. A transverse-torque bill can never meter the shedding — not because the right torque wasn't found, but because no torque is present in the shedding channel at leading order. Every payment attempt (boundary flux, Π₄Q, T_H, torque coercivity) was priced in a sector orthogonal to the motion that actually sheds the record.
2. **Census B1 dissolves at leading order.** The posed nullspace question — "is there an m≠0 current that drops the moment with zero odd transfer?" — has the answer: m≠0 currents produce **no drop at all** at the atom. They are drop-null, not zero-row. There is nothing in the off-frame sector to null-test.
3. **The Ladyzhenskaya import is demoted to support.** Cycle-45 called the aligned no-swirl null "benign" by importing global axisymmetric-no-swirl regularity. Per rule 14 the channel is a sector of one general record, not a globally axisymmetric flow; the import is a consistency check only. The channel's own row must be read.

## 3. The frontier, restated on the one record (the named open sign)

The only shedding motion forces a pressure row that is itself aligned m=0 (SO(2) symmetry of the mode; the linear-in-rate source Q₁ = 2S:∇v of the cubic note). The record's service growth reads that aligned row directly through the strain equation D_tS = −S² − Ω² − H + νΔS:

D_t(−tr S³) = 3tr(S⁴) + 3tr(S²Ω²) + **3tr(S²H)** − 3ν tr(S²ΔS),

and at the atom S² is aligned m=0, so tr(S²H) reads exactly the aligned m=0, ℓ∈{0,2,4} part of the row — the same sector the shedding lives in. Drop and row are bound in ONE sector of one record: nothing here is priced, everything is read.

**Open target — `AlignedRowShedsRecordOneSense.A`:** on the shedding channel of a retained same-affine atom, the aligned row forced by the shedding (H₁ from −Δp₁ = Q₁ = 2S:∇v) enters 3tr(S²H₁) with one material-time sense: shedding forces the row that eats the record; a record can only be fed by the row whose material-time reading is gathering — which is the reversed arrow, excluded on the forward history.

Parity bookkeeping (rule 13, stated exactly): H₁ is T-even as a configuration quantity (linear in S, linear in v; both flip). The odd content is the material-time direction of the shedding itself: the same sphere-pattern read backward is gathering, not shedding, and the pairing enters D_t(service), whose sense flips with the arrow. So the signed object in the allowed register is the material-time pairing ⟨shedding sense, aligned-row action on the record⟩ — a sense, not a magnitude, exactly the non-separable form of the T-odd lever. This is the per-atom content of the route-b material-time Liouville target; the infinite marginal relay remains the terminal object (Door 2), and Door-1 retention (no silent row-dropping between rungs, ZPF.21a) is unchanged.

## 4. Honest scope

- Facts 1–2 are exact at the strict same-affine atom; the deviation from same-affine is charged by the existing Hodge/viscous bill (log-chain note) — the classification governs the surviving class at leading order, and the second-order rearrangement pathway (swirl/m≠0 deforming w₀ first, then m=0 shedding) routes through that charged deviation.
- `AlignedRowShedsRecordOneSense.A` is OPEN. It is smaller than its predecessors: one sector (m=0, ℓ∈{2,4}), one forced row (Q₁ linear-in-rate, already verified), one material-time sense. Not closure; the next exact computation.
