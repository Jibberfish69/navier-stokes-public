---
theorem_id: forward-gold-c0-normalized-passage-failed-gate-minimal-theorem-20260706
created: 2026-07-06
problem: navier-stokes
route: forward-gold / c_0 / normalized relay passage failed-gate certificate
status: failed-proof-certificate-first-gate2-minimal-theorem-parent-owned-positive-record-bill-currency
target_object:
  - NormalizedRelayBillCompactnessRecordPassage.A
  - Gate2.FullTowerPositiveRecordRetention
  - ParentOwnedPositiveRecordBillCurrency.A
  - RecordMenuPressureActiveOrSilentService.A
  - RetainedRecordEdgeExactProjectorChartLicense.A
allowed_source_refs:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-c0-epsilon-extraction-direct-proof-pass-20260706.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-relay-record-reversibility-kernel-and-epsilon-extraction-20260706.md
  - problems/navier-stokes/theorem-construction/mpp-shared-participation-and-tower-coherence-law.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-full-tower-signed-total-exchange-retention-direct-attempt-20260622.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-chapter2-refereegrade-closedness-limit-20260705.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-signed-height-record-net-work-admission-proof-pass-20260703.md
  - problems/navier-stokes/spine.md
derived_refs:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-c0-parent-owned-record-lower-entrance-source-verdict-20260706.md
completion_truth: >-
  Failed-gate certificate for the active goal, not a proof of c_0 and not a
  refutation of NormalizedRelayBillCompactnessRecordPassage.A. The allowed
  sources prove the conditional extraction and the zero-bill kernel, and the
  active extraction surface records Gate1 as installed on retained same-fluid
  relay windows. The first exact failed gate is Gate2: signed adjacent
  full-tower exchange identities do not retain total positive record variation.
  The algebraic signed-cycle countermodel refutes the inference from signed
  cancellation to positive-record pricing, but it is not an actual retained
  Navier-Stokes counterexample to the normalized passage theorem. Terminal
  Part/carrier retention also does not solve the gate, because Gate2 needs
  pre-readout positive carrier, negative partner, and signed current ownership
  for the same selected positive channel. The smallest source-exposed theorem
  whose proof would immediately remove the current Gate2/Gate3/Gate4 escape is
  ParentOwnedPositiveRecordBillCurrency.A; in the lowered signed-height
  language its entrance is RecordMenuPressureActiveOrSilentService.A plus
  RetainedRecordEdgeExactProjectorChartLicense.A, giving parent root/full-
  exchange visibility or paid silent service before record readout. Current
  allowed sources prove finite-truncation native menu admission and frozen
  detector-angle algebra only; they do not prove positive-selection storage,
  detector-silent strict half-barrier, or exact-projector chart admission.
---

# Normalized relay passage: failed gate and smallest exposed theorem

## 1. What would close the extraction

The active extraction theorem is:

```math
\texttt{NormalizedRelayBillCompactnessRecordPassage.A}.
```

For retained relay windows \(W_j\), it asks:

```math
R(W_j)=1,\qquad B(W_j)\to0
\quad\Longrightarrow\quad
\exists W_*:\ B(W_*)=0,\ R(W_*)\ge1 .
```

Together with the zero-bill relay-record kernel, this gives
\(\varepsilon_*>0\) and \(c_0=2\varepsilon_*\).

The zero-bill kernel is not the missing step. It proves only the zero-level
rigidity once a zero-bill positive-record retained limit exists.

## 2. Gate1 is not the first current obstruction

The active extraction surface records the localized Field/cutoff branch as
installed on retained same-fluid relay windows. Physically, neighboring tower
difference compactness is supplied by the finite-difference tower law, the
viscous signed defect term, and the pressure-increment Field envelope.

So the current failure does not sit at `TC.31/TC.31a`.

## 3. Gate2 is the first exact failed gate

The full-tower source proves signed adjacent exchange identities for the four
same-material readings:

```math
(S_N,Q_N,C_N,G_N).
```

When summed, the signed exchange currents cancel. The positive record is a
different object:

```math
dR_N^+
=
d[\log(1+\mathfrak P_N^{mat})]_+ .
```

The source differentiates the material record into positive generated channel
measures, but those measures are total positive variation. Total positive
variation is not controlled by signed net cancellation.

At the finite algebraic level, a same-packet two-leg exchange can have

```math
M-M=0,
\qquad
[M]_+ + [-(-M)]_+ = 2M .
```

This proves a no-go for the inference:

```math
\text{signed full-tower cancellation}
+dA_{4B,N}=0
\Longrightarrow
dR_N^+=0 .
```

It does not prove a Navier-Stokes counterexample to the normalized passage
theorem. It proves the current sources do not retain the positive record from
signed cancellation alone.

## 4. Why retained terminal participation does not remove Gate2

The shared participation law distinguishes carrier retention from the Gate2
object. Terminal `Part` retains the pressure-viscosity carrier law with the
terminal tower/readout control needed by the witness.

Gate2 needs a stricter pre-readout object:

```math
(\text{positive carrier},\ \text{negative partner},\ \text{signed current})
```

on the same selected positive record channel, or else strict loss, Q-activity,
viscous/Field payment, legal residual, or stop. The full-tower source says the
retained interface criterion preserves this three-measure packet after it is
produced. It does not produce the packet from terminal participation.

Thus the `FTR.21` phrase "same-material Part-side record loss" means record-
ownership participation is missing. It is not an automatic route-out from the
retained terminal carrier class.

## 5. Smallest exposed theorem

The broad Gate2 name is:

```math
\texttt{FullTowerSignedTotalExchangeRetention.A}.
```

The exact current theorem that would remove the Gate2, Gate3, and Gate4 escape
in one stroke is:

```math
\texttt{ParentOwnedPositiveRecordBillCurrency.A}.
```

It says the selected positive record is represented before readout on the same
parent packet and is bounded in the bill currency

```math
B=dE^{Field}+dA_{4B}+dVisc .
```

In the lowered signed-height source language, the entrance is:

```math
\texttt{RecordMenuPressureActiveOrSilentService.A}
+
\texttt{RetainedRecordEdgeExactProjectorChartLicense.A}.
```

The first theorem forces every future-positive native edge to be visible to the
parent root/full-exchange detectors or already paid as detector-silent service.
The second theorem licenses the exact weighted projector and moving chart, so
the frozen detector statement is a legal reading of the retained edge.

## 6. What is already proved and what remains open

The signed-height source proves finite-truncation native menu admission:

```math
P(t)\,dt
=
\sum_{e\in\mathcal E_P(t)}\omega_e(t)\,dt
+dLegal_P+dStop_P .
```

It also derives record admission from that menu for one record interval. What
is open is positive-selection storage for all future disjoint record intervals.

The frozen detector-angle algebra is finite-dimensional after one retained
edge, packet, and frequency direction are fixed:

```math
[w_{rec}z]_+\le C|Dz|
\quad\Longleftrightarrow\quad
\ker D\subseteq\ker w_{rec}.
```

What is open is the Navier-Stokes lift:

1. a pressure-active/silent split where detector-silent positive work is paid
   before record readout;
2. an exact-projector chart/commutator admission theorem for retained record
   edges;
3. a strict original-history half-barrier for the detector-silent branch.

## 7. Consequence

The active goal has not reached any completion branch:

```math
\begin{array}{ll}
\text{A:} & \text{not installed, because Gate2 positive-record ownership is open},\\
\text{B:} & \text{not refuted, because the signed-cycle model is algebraic/source-logical,}\\
          & \text{not an actual retained same-fluid Navier-Stokes counterexample},\\
\text{C:} & \text{not reached, because the smallest exposed theorem remains open}.
\end{array}
```

The proof state is nevertheless sharper than "compactness remains." The first
exact failed gate is Gate2. The smallest current theorem is parent-owned
positive-record bill currency, with the lower entrance:

```math
\texttt{RecordMenuPressureActiveOrSilentService.A}
+
\texttt{RetainedRecordEdgeExactProjectorChartLicense.A}.
```
