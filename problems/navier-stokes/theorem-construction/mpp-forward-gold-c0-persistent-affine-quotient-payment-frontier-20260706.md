---
theorem_id: forward-gold-c0-persistent-affine-quotient-payment-frontier-20260706
created: 2026-07-06
problem: navier-stokes
route: forward-gold / c_0 / irreversible heat-scale route-b / persistent affine quotient
status: strict-reduction-pressure-memory-admission-to-persistent-root-affine-payment-open
ontology_lock:
  fluid_field_object: Navier-Stokes same-fluid velocity-pressure field
  reversible: false
  adiabatic: false
  compressible: false
  corrected_terms:
    - incompressible
    - viscous
    - dissipative
    - pressure-constrained
    - irreversible
target_objects:
  - PersistentAffineQuotientToPressureMemoryAdmission.A
  - PersistentRootAffineQuotientReturnPayment.A
  - ActiveRouteCarlesonFromNativeBirthChargePacking.A
  - ActiveWeightDefectTightnessFromOriginalHistory.A
  - SelectorCarrierNondegeneracyOrPaidReselection.A
  - StoppedFullExchangeActionCarleson.A
  - FreshPotentialAverageKillAndPositiveTransfer.A
  - CriticalWeightedResetBVFromOriginalHistory.A
  - LogScaleResetDerivativeCarrierBound.A
source_refs:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-c0-irreversibility-rebuild-reversible-frame-withdrawal-20260706.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-c0-heat-scale-route-b-surplus-zero-rigidity-reduction-20260706.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-c0-first-ratio-storage-current-carleson-reduction-20260706.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-parent-weighted-detector-terminal-service-original-data-proof-attempt-20260629.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-active-weight-bad-profile-compactness-tightness-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-root-defect-tightness-concentration-compactness-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-root-defect-tightness-original-ledger-correction-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-selector-carrier-readout-or-paid-reselection-after-dynamic-silence-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-persistent-root-affine-quotient-payment-branch-audit-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-selected-generator-storage-coercivity-hard-block-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-positive-critical-transfer-dyadic-flux-affine-threading-reduction-20260628.md
derived_reductions:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-c0-record-reset-root-payment-frontier-20260706.md
completion_truth: >-
  This is a strict reduction and ontology save, not a proof of c_0. The
  corrected Navier-Stokes object is one incompressible viscous dissipative
  pressure-constrained irreversible same-fluid field. Under that object, the
  surviving heat-scale half-tail is localized to the persistent affine/root
  quotient after inherited parent transport, selector drift, derivative-exact
  motion, legal loss, and stop loss are removed. Existing sources do not install
  the comparison from that quotient to pressure-memory entropy. They reduce the
  route-b producer to PersistentRootAffineQuotientReturnPayment.A. Current
  source checking further reduces the fresh potential-average and strict
  reset/overrun branches to one pre-readout
  OriginalHistoryRecordResetRootPayment.A / selected potential-amplification BV
  producer. No FullTower retention, first-ratio storage, strict c_0, or MPP
  closure is claimed.
---

# Persistent affine quotient payment frontier for the irreversible c_0 route

## 1. Physical object being saved

The object in this branch is the same Navier-Stokes fluid-field-object fixed in
the irreversibility rebuild:

```text
same velocity-pressure field
same incompressible fluid
viscous dissipation
pressure constraint
irreversible material history
```

The heat-scale route-b survivor is not a reversible exchange cycle. It is a
ratio-one marginal profile trying to keep positive selected record service
alive while every same-fluid payment channel is exactly balanced at the
normalized level.

In the current source chain, that survivor has the persistent affine/root
quotient form

\[
D_e^{pers}
=
(I-\Pi_{\mathcal P_{e^-}})
\mathcal C_\theta[Z_e^{fresh}].
\tag{PAQ.1}
\]

Here inherited parent-predictable transport has already been removed. Selector
motion, carrier mismatch, derivative-exact high-weight escape, legal loss, and
stop loss have already been routed to their own charges. The remaining object
is a same-carrier non-derivative affine average that still has to pay inside the
original irreversible history.

## 2. Source state

The first-ratio storage note reduces the surviving heat-scale branch to retained
Schur telescoping, fresh push-variation bounded overlap, and persistent affine
quotient admission:

\[
\text{retained Schur}
+\text{fresh push variation}
+\text{persistent affine quotient admission}
\Longrightarrow
d\Omega_N^{first}\in L^1.
\tag{PAQ.2}
\]

The parent-weighted detector attempt gives the precise backtrack. The selector
side is installed only as a relative readout theorem: in a fixed stopped
selector chart, vanishing parent carrier implies vanishing selected scalar, and
moving chart/label/order-lock motion is charged to stopped full exchange.

The remaining size statement is the persistent quotient payment

\[
\sum_{e\subset P}
\int H_{e^-}\|D_e^{pers}\|^2
\le
C_N(u_0)\mathcal R(P)
+C\operatorname{Return}_{-}(P)
+C\operatorname{PotAvg}(P)
+C\operatorname{Overrun}_{sel}(P)
+R_{\rm legal}(P)
+Stop(P).
\tag{PAQ.3}
\]

This is `PersistentRootAffineQuotientReturnPayment.A`, the same statement
recorded as `(ODP.125)`.

## 3. Pressure-memory admission is not installed

The tempting comparison would be

\[
\|D_e^{pers}\|^2
\lesssim
\|\mathcal P_{avg,J}^{fresh}\|_2^2
+R_e^{return}+R_e^{reset}+R_e^{legal}+Stop_e.
\tag{PAQ.4}
\]

That comparison is exactly the unproved admission from the persistent affine
root quotient to the stopped pressure-memory entropy coordinate. The current
compactness, selector, and zero-cost rigidity notes localize the survivor to
`(PAQ.1)`. They do not prove `(PAQ.4)`.

A valid storage producer would be

\[
d\mathcal E_P
+c\sum_{e\subset P}H_{e^-}\|D_e^{pers}\|^2
+(1-\theta)dVisc_{P,N}
\le
dPaid_P+C_N(u_0)d\mathcal R_P,
\tag{PAQ.5}
\]

with \(\mathcal E_P\ge -C_N(u_0)\). Integrating `(PAQ.5)` would give
`(PAQ.3)`, then the active route Carleson estimate, then the first-ratio
storage needed to kill the half-tail.

## 4. Sharpened remaining producer

The persistent quotient branch audit splits `(PAQ.3)` into three original-history
payment channels:

```text
fixed stopped-score return/BV
    closed relative to root-geometry currency

fresh potential-average positive transfer
    open

strict reset / overrun Carleson
    open
```

So the current sharpened frontier is

\[
\texttt{FreshPotentialAverageKillAndPositiveTransfer.A}
+
\texttt{CriticalWeightedResetBVFromOriginalHistory.A}
+/
\texttt{LogScaleResetDerivativeCarrierBound.A}.
\tag{PAQ.6}
\]

Those are the noncircular payment suppliers under
`PersistentRootAffineQuotientReturnPayment.A`. The stopped Hilbert/BMO testing
surfaces consume the resulting current; they do not create this payment by
themselves.

## 5. Proof effect on the c_0 question

The current implication chain is:

\[
\begin{gathered}
\text{persistent root affine quotient payment}
+\text{ stopped full exchange}
+\text{ selector readout}
\\
\Longrightarrow
\text{active route Carleson / native heat-lag Hardy-Carleson}
\\
\Longrightarrow
\text{first-ratio storage}
\\
\Longrightarrow
\sum_{\ell}2^\ell\nu_\ell<\infty .
\end{gathered}
\tag{PAQ.7}
\]

That would exclude the route-b half-tail and supply the missing positive gap in
the irreversible \(c_0\) rebuild.

At the current source state, `(PAQ.6)` remains open. The result saved here is the
strict reduction:

```text
c_0 heat-scale route-b
  reduces to persistent same-carrier affine/root quotient payment
  reduces to fresh potential-average transfer plus strict reset/overrun
  original-history Carleson.
```

This is narrower than the earlier reversible-cancellation story and narrower
than a broad pressure-memory admission claim. It is the current same-fluid
irreversible frontier.
