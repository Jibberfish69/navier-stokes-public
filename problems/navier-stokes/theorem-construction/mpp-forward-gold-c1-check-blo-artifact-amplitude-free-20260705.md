---
theorem_id: forward-gold-c1-check-blo-artifact-amplitude-free-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / C1 executed - B_lo was a normalization artifact
authorship: Claude (Fable), 2026-07-05. The C1 confirmation check,
  executed against the filed sources, finds against my own assembly
  in the favorable direction: the bracket constant does not need
  confirming because it does not exist - the filed amplitude-
  cancellation principle deletes it. Same-day correction against the
  cdiff-assembly note. Physical story Thomas Birnie: the spring does
  not care how big it is - I had briefly priced its size anyway, in
  one factor's units but not the other's; measured in the water's
  own clock, the size was never on the bill.
completion_truth: >-
  Executes C1 (the B_lo confirmation owed by the cdiff-assembly
  note), pending re-gate; the finding is a same-day CORRECTION:
  B_lo IS DELETED, not confirmed. (THE ERROR, owned) My assembly
  mixed two clocks: it wrote the relay rate as Omega_relay = g A
  (amplitude clock) while the period used the elliptic clock
  lambda = g sqrt(X3 - X1), leaving the spurious ratio
  A/sqrt(X3 - X1) - which I then bracketed by citing the s|D| band
  inequality, an object for helicity readouts, not for this ratio.
  Both moves are withdrawn. (THE CORRECT ASSEMBLY, amplitude-free by
  the filed principle) The race write-up states it verbatim: "Every
  factor is a coupling ratio or a phase increment; amplitude
  cancels - the bound is amplitude-free, as the physical story
  requires (the spring does not care how big it is)." Measured
  consistently in the relay's own elliptic clock: the step window
  is one relay period T = 2K(m)/lambda; the predator exponent is
  Lambda(N) = (Omega_pred/lambda) x 2K(m); and Omega_pred/lambda is
  the R2b CLOCK-RATIO - pure couplings, wavenumber objects,
  amplitude-free (relay daughter coupling q sqrt(2 delta), predator
  coefficient full strength): Omega_pred/lambda = gamma_G x
  1/sqrt(2 delta_N). No amplitude ratio survives; there is nothing
  to bracket. (THE ASSEMBLY, corrected) Lambda(N) = gamma_G x
  [1/sqrt(2 delta_N)] x 2K(m) >= gamma_G x (1/sqrt 2)
  sqrt(N/ln 2) x pi, by the sqrt-2 line (delta_N = 1 - 2^(-1/N) <=
  ln2/N) and K-monotonicity (2K >= pi): C_DIFF = pi/sqrt(2),
  EXACTLY - no B_lo, no band citation, no C1 pendency. Two lines
  survive from the original assembly (the sqrt-2 line; K >= pi/2);
  the third factor is gone because it was never there. (PROPAGATION)
  The substituted Sheet-1 inequality tightens to:
  GAMMA_ANGLE x pi sqrt(3/(2 ln 2)) >= ln(2/r_net) -
  ln(1 - 2 M_req) - TWO filed symbols remain (phi_min, r_net), plus
  exact constants; C1 is closed BY DELETION; C2 (the target-5
  frozen-period gate) still conditions the window step and remains
  named. (HONESTY) This correction, like the assembly it corrects,
  awaits re-gate; the amplitude-free principle is cited from a
  filed note whose section 5-6 chain carried its own Codex
  attention - the citation is to the displayed principle line, and
  the re-gate should check that the principle's scope (race-integral
  bounds) covers the clock-quotient use made here; if it does not,
  the fallback is the Manley-Rowe roots-within-energy line
  (X3 <= total invariant energy, X1 >= 0), which floors the old
  ratio at ONE by conservation - either way B_lo carries no unknown
  value. PRESS POINTS: the two-clocks error (one sentence, owned);
  the amplitude-free citation (verbatim); the scope check for the
  re-gate (named); the roots-within-energy fallback (one line).
  QUEUE: this note's re-gate (with the cdiff-assembly's); C2
  (target-5); the max-crossing display (r_net's gate); the G-ratio
  matching check; THE COMPARISON; B1-B3; chapter 10. Filed is not
  cleared. No closure claimed.
---

# C1 executed: B_lo was never on the bill

## 1. The error, owned

The assembly mixed two clocks - the amplitude clock g A for the
rate, the elliptic clock lambda = g sqrt(X3 - X1) for the period -
leaving a spurious ratio A/sqrt(X3 - X1), which I bracketed by
citing the s|D| band inequality (an object for helicity readouts,
not this ratio). Both moves are withdrawn.

## 2. The correct assembly, amplitude-free

The filed principle (race write-up): "Every factor is a coupling
ratio or a phase increment; amplitude cancels - the spring does not
care how big it is." In the relay's own clock:

  T = 2K(m)/lambda,
  Lambda(N) = (Omega_pred/lambda) x 2K(m),
  Omega_pred/lambda = gamma_G x 1/sqrt(2 delta_N)
    (R2b clock-ratio: pure couplings, amplitude-free),

so with the sqrt-2 line and K >= pi/2:

  Lambda(N) >= gamma_G x pi sqrt(N/(2 ln 2)),
  C_DIFF = pi/sqrt(2)   exactly.

No amplitude ratio survives; there is nothing to bracket; C1 closes
by deletion.

## 3. Propagation

  GAMMA_ANGLE x pi sqrt(3/(2 ln 2))
    >= ln(2/r_net) - ln(1 - 2 M_req)

- two filed symbols (phi_min, r_net) plus exact constants. C2
(target-5 frozen-period gate) remains named on the window step.

## 4. Scope check and fallback, for the re-gate

The amplitude-free principle is cited from its displayed line; the
re-gate should check its scope covers the clock-quotient use here.
If not: the Manley-Rowe roots-within-energy line (X3 bounded by the
total invariant energy; X1 nonnegative) floors the old ratio at ONE
by conservation - either way B_lo carries no unknown value.

## 5. Register

C1 closed by deletion; c_diff = pi/sqrt(2) exact, pending re-gate;
the inequality is two symbols wide. Press points: the two-clocks
sentence; the verbatim citation; the scope check; the fallback
line. Queue: re-gates; C2; the max-crossing display; the matching
check; the comparison; B1-B3; chapter 10. Filed is not cleared. No
closure claimed.
