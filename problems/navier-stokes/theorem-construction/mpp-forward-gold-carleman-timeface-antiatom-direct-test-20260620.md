# MPP Forward-Gold Carleman Time-Face Anti-Atom Direct Test

Date: 2026-06-20

## Status

Direct proof-method pivot complete.  Backward uniqueness, Carleman
observability, and adjoint probing are not independent suppliers for the
terminal heat-scale time-face atom from the currently installed inputs.

They become useful only after the terminal pulse has already been converted
into one of the stronger hypotheses: an interior source-residence strip,
global no-incoming boundary data on the ancient half-line, a same-carrier
source-square reserve, or a no-waste compact ancient element.

## 1. The current atom to be tested

The surviving forward-gold enemy is a selected normalized source density

```math
a_m(s)\,ds
\rightharpoonup
a\,\delta_{s=0},
\qquad
s\in[-1,0],
\qquad
a>0.
\tag{CTA.1}
```

Equivalently, on the selected physical heat packet

```math
Q_m=B_{r_m}(x_m)\times(T_m-r_m^2,T_m],
\tag{CTA.2}
```

one has a terminal moving-packet source mass with no uniform strip modulus:

```math
\lim_{\theta\downarrow0}
\limsup_m
\int_{-\theta}^{0}a_m(s)\,ds
>0.
\tag{CTA.3}
```

The desired gold supplier is an anti-atom theorem:

```math
\lim_{\theta\downarrow0}
\limsup_m
\int_{-\theta}^{0}a_m(s)\,ds
=0.
\tag{CTA.4}
```

## 2. Backward adjoint tests see the atom as boundary data

Let \(\phi\) solve a backward adjoint heat or Stokes-type equation on
\([-1,0]\) with terminal value \(\phi(\cdot,0)=\psi\).  Pairing with the
terminal source atom gives

```math
\langle a\,\delta_{s=0},\phi\rangle
=
a\,\psi.
\tag{CTA.5}
```

Thus the adjoint test detects the atom only through terminal boundary data.
When \(\psi=0\), the test misses the atom.  When \(\psi\ne0\), the atom is
visible but remains a terminal trace term.

There is no backward time interval beyond \(s=0\) in which the adjoint equation
can force this boundary measure to become an interior source residence.
Therefore the implication

```math
\text{terminal adjoint detection}
\Longrightarrow
\text{earlier same-ledger source slice}
\tag{CTA.6}
```

is false without an additional trace regularity, source residence, or
source-square input.

## 3. Finite-slab Carleman is not the missing terminal theorem

The anisotropic frozen-strain route correctly transforms the tangent equation
into an anisotropic Stokes system

```math
\partial_s w+\nabla_z q
=
\nu\nabla_z\cdot(A(s)\nabla_z w),
\qquad
\nabla_z\cdot(A(s)w)=0.
\tag{CTA.7}
```

On each finite slab \([-R,0]\), the installed reduction shows that standard
parabolic Carleman machinery applies after the pressure/divergence structure is
placed in this anisotropic form.  That gives finite-slab unique continuation
for functions satisfying the corresponding hypotheses inside the slab.

The terminal atom in `(CTA.1)` is not an interior finite-slab solution with
zero terminal trace.  It is a boundary measure at \(s=0\).  Finite-slab
Carleman estimates do not remove boundary data that the test class is allowed
to carry.

So finite-slab backward uniqueness proves at most:

```math
\text{interior finite-slab zero trace under admissible hypotheses}
\Longrightarrow
\text{no interior hidden packet}.
\tag{CTA.8}
```

It does not prove `(CTA.4)`.

## 4. The ancient half-line upgrade returns to no-incoming and unweighted action

To use backward uniqueness against the Zeno atom, one has to pass from finite
slabs to the ancient interval

```math
(-\infty,0].
\tag{CTA.9}
```

The older transform test already isolates the obstruction.  In frozen-strain
coordinates, the coefficient matrix

```math
A(s)=e^{-sS}e^{-sS^T}
\tag{CTA.10}
```

can become anisotropically singular or unbounded as \(s\to-\infty\), and
contracting directions may import mass from spatial infinity into the observed
packet.  Thus a local condition such as

```math
\lim_{s\to-\infty}\int_{B_R}|v(y,s)|^2\,dy=0
\quad(R<\infty)
\tag{CTA.11}
```

is not a global no-incoming condition.

The valid half-line theorem would be conditional:

```math
\boxed{
\text{GlobalNoIncomingFlux.A}
+
\text{GlobalUniformAnisotropicStokesCarleman.A}
\Longrightarrow
\text{no terminal source pulse}
}
\tag{CTA.12}
```

But `GlobalNoIncomingFlux.A` is already a strong terminal-tail theorem.  In the
heat-scale packet variables, it must control the complete incoming boundary
action with no radius discount.  That is another form of

```math
\text{UnweightedTerminalCriticalActionReserve.A}.
\tag{CTA.13}
```

## 5. Relation to the current heat-scale reserve wall

The current heat-scale branch has the physical-versus-normalized mismatch

```math
\sum_j r_j\,\mathcal A_j<\infty
\quad\not\Rightarrow\quad
\sum_j\mathcal A_j<\infty.
\tag{CTA.14}
```

Carleman does not change this currency.  It can propagate zero or uniqueness
once the correct global boundary and coefficient hypotheses are installed.  It
does not create the missing unweighted terminal budget from finite physical
energy, finite dissipation, local energy, or material stress telescoping.

The direct Carleman route would close only after one of these stronger inputs:

```math
\begin{gathered}
\text{TerminalMovingPacketTraceModulus.A},\\
\text{SourceSquareReserve.A},\\
\text{SelectedCriticalStrainCarleson.A},\\
\text{NormalizedCKNCarleson.A},\\
\text{StrictRescaledNoWasteLyapunov.A},\\
\text{GlobalNoIncomingFlux.A with unweighted terminal action control.}
\end{gathered}
\tag{CTA.15}
```

These are not consequences of finite-slab Carleman.  They are exactly the
unweighted reserve family already isolated in the heat-scale branch.

## Verdict

Backward uniqueness and Carleman observability are useful after the terminal
object has been turned into an interior zero-trace or globally no-incoming
ancient problem.

They do not by themselves turn a terminal boundary atom into earlier residence,
and they do not remove the radius discount in the heat-scale material ledger.

Therefore the Carleman/frequency pivot gives no independent forward-gold
anti-atom theorem at current inputs:

```math
\boxed{
\text{Carleman supplies uniqueness under stronger trace/no-incoming hypotheses;}
\quad
\text{the production of those hypotheses is the unweighted terminal reserve.}
}
\tag{CTA.16}
```

