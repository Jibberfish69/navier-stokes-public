---
theorem_id: forward-gold-cdiff-assembly-displayed-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / the c_diff row - from symbol to displayed assembly
authorship: Claude (Fable), 2026-07-05. The audit called c_diff
  symbol-only; the cleared tracing sweep said its ingredients were
  exact all along - this note assembles them into one displayed
  chain and names what remains conditional. Physical story Thomas
  Birnie: the predator's head start is the relay's own slowness
  counted in the relay's own clock - every factor of it was already
  on the books; the books just needed to be opened to the same page.
completion_truth: >-
  Assembles the c_diff row from its filed exact ingredients, pending
  re-gate; replaces "symbol-only" with a DISPLAYED ASSEMBLY carrying
  ONE named conditional and ONE citation to confirm. (THE OBJECT)
  Lambda(N) = the predator exponent per step at chop N in log
  currency = Omega_pred x T_window. (THE CHAIN, five displays)
  (A1, THE CLOCK QUOTIENT - exact, filed) reshape clause (iii),
  audited under R2b: relay daughter coupling = q sqrt(2 delta) at
  step progress delta; predator coefficient full strength; the
  couplings enter as ONE quotient in one place (the cleared sweep's
  factor-4 closure) - the side-sum part routes into gamma, the pure
  clock part is 1/sqrt(2 delta). (A2, THE CHOP'S EXACT PROGRESS) at
  chop N the step ratio is 2^(-1/N), so delta = 1 - 2^(-1/N),
  exact. (A3, THE SQRT-2 LINE - new, one line) 1 - e^(-x) <= x at
  x = (ln 2)/N gives delta <= (ln 2)/N, hence 1/sqrt(2 delta) >=
  (1/sqrt(2)) sqrt(N/ln 2): the sqrt(N/ln 2) form of the composed
  delivery rate is a valid exact floor of the exact clock identity,
  with the sqrt(2) carried into c_diff, not dropped. (A4, THE
  RELAY-CLOCK WINDOW - the g-cancellation plus K >= pi/2) The step
  window is one relay period: T_window = 2K(m)/lambda_relay with
  lambda_relay = g_relay sqrt(X3 - X1) (elliptic write-up section
  2), and Omega_relay = g_relay x (amplitude scale A), so
  W := Omega_relay T_window = 2 K(m) x A/sqrt(X3 - X1): THE COUPLING
  g CANCELS EXACTLY - the same-clock cancellation the cleared sweep
  recorded as factor-5 exactness. The elliptic factor needs NO
  compact minimum: K is increasing in the modulus with K(0) = pi/2,
  so 2K(m) >= pi EXACTLY, everywhere on the band - one monotonicity
  line. The amplitude ratio A/sqrt(X3 - X1) is bracketed by the
  two-sided band inequality (sD-multiplier-repair note, factor-6
  exactness): lower constant B_lo, the dyadic band bottom. (A5, THE
  ASSEMBLY) Lambda(N) >= gamma x (1/sqrt 2) sqrt(N/ln 2) x pi B_lo
  =: gamma c_diff sqrt(N/ln 2), i.e. C_DIFF := pi B_lo / sqrt(2) -
  a closed form; with the dyadic bracket's expected bottom
  B_lo = 1/2 this reads pi/(2 sqrt 2), DISPLAYED AS THE CANDIDATE
  VALUE, not asserted, pending the citation check below. (THE TWO
  HONEST CONDITIONS, named) (C1) B_lo's precise normalized value
  must be confirmed against the sD-repair band display - the
  assembly is exact in the symbol B_lo either way; the candidate
  one-half is the dyadic reading. (C2) Using the frozen elliptic
  period over the window rests on target 5's one-period
  perturbation estimate (root sensitivity on the split band), which
  Codex has NOT cleared - the assembly is conditional on that gate,
  named here, already in the audit queue; nothing about this note
  discharges it. (WHAT THE ROW BECOMES) The audit's "c_diff appears
  only as a Lambda symbol" is replaced by: c_diff = pi B_lo/sqrt(2),
  every factor displayed (pi from K-monotonicity; sqrt(2) from the
  one-line floor; B_lo from the band bracket), one named
  conditional (C2), one citation to confirm (C1). The remaining
  Sheet-1 rows after this note: THETA (the last), plus C1/C2's
  confirmations. PRESS POINTS: the sqrt-2 line (A3, one
  inequality); the g-cancellation with K >= pi/2 (A4, two lines);
  the honest conditional C2. QUEUE: this note's re-gate; the theta
  row; C1 band-constant confirmation; C2 = target 5's gate; then
  the one displayed inequality; B1-B3; chapter 10. Filed is not
  cleared. No closure claimed.
---

# The c_diff row: from symbol to displayed assembly

## 1. The object and the chain

Lambda(N) = Omega_pred x T_window, per step at chop N, log currency.

A1 (clock quotient, exact, filed): relay daughter coupling
q sqrt(2 delta); predator full strength; couplings enter as one
quotient (the cleared sweep's factor-4 closure) - side sums into
gamma, pure clock 1/sqrt(2 delta).

A2 (chop progress, exact): step ratio 2^(-1/N), so
delta = 1 - 2^(-1/N).

A3 (the sqrt-2 line, new): 1 - e^(-x) <= x at x = (ln 2)/N gives

  1/sqrt(2 delta) >= (1/sqrt 2) sqrt(N/ln 2).

The familiar sqrt(N/ln 2) is a valid exact floor of the exact
identity; the sqrt(2) is carried, not dropped.

A4 (the relay-clock window): one relay period,
T_window = 2K(m)/lambda_relay, lambda_relay = g sqrt(X3 - X1), and
Omega_relay = g A, so

  W = Omega_relay T_window = 2 K(m) x A / sqrt(X3 - X1)

- the coupling g cancels exactly (the sweep's factor-5 exactness).
The elliptic factor needs no compact minimum: K increases in the
modulus and K(0) = pi/2, so

  2 K(m) >= pi   exactly, everywhere on the band.

The amplitude ratio is bracketed by the two-sided band inequality;
its lower constant is B_lo, the dyadic band bottom.

A5 (assembly):

  Lambda(N) >= gamma x (pi B_lo / sqrt 2) x sqrt(N/ln 2),
  C_DIFF := pi B_lo / sqrt(2);   candidate value pi/(2 sqrt 2)
  at the dyadic reading B_lo = 1/2 - displayed, not asserted.

The predator's head start is the relay's own slowness counted in
the relay's own clock: every factor was already on the books.

## 2. The two honest conditions

C1: B_lo's precise normalized value awaits confirmation against the
sD-repair band display; the assembly is exact in the symbol B_lo
regardless.

C2: freezing the elliptic period over the window rests on target
5's one-period perturbation estimate (root sensitivity on the split
band) - NOT cleared by Codex; the assembly is conditional on that
gate, named, already queued. Nothing here discharges it.

## 3. The row, restated

Before: "c_diff appears only as a Lambda symbol" (audit). After:
c_diff = pi B_lo / sqrt(2) - pi by K-monotonicity, sqrt(2) by the
one-line floor, B_lo by the band bracket; one named conditional,
one citation to confirm. Remaining Sheet-1 rows: theta, plus C1/C2
confirmations.

## 4. Register

Assembly displayed; press points: the sqrt-2 line; the
g-cancellation with K >= pi/2; the honest conditional. Queue:
re-gate; the theta row; C1; C2 (= target 5); the one displayed
inequality; B1-B3; chapter 10. Filed is not cleared. No closure
claimed.
