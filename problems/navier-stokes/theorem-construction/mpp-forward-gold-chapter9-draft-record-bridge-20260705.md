---
theorem_id: forward-gold-chapter9-draft-record-bridge-20260705
status: chapter9-drafted-against-reread-sources-bridge-identification-filed-s3-flag-remains
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal items 4-5 / chapter 9 draft
authorship: Claude (Fable), 2026-07-05. Assembled against the re-read
  2026-07-04 signed-height sources (physical spine SRP; record-admission
  boundary ASH). Physical story Thomas Birnie; the bridge identification
  below is the two days of work recognizing each other.
completion_truth: >-
  Chapter 9 drafted against re-read sources, per the assembly spec. THE
  BRIDGE IDENTIFICATION (the mathematical content of this note): the
  July-4 spine left open exactly one proof-sized object (SRP.12/ASH.9) -
  derive the three record-admission rows from the original NS packet -
  and the July-5 campaign's three main objects ARE those rows. Row 1
  (parent admission of record work into the same edge) = Gate A's
  oriented admission / lifted no-recount law (chapter 2; the step4 note
  had already identified no-recount as "the signed-height no-clipping
  principle" - the identification was latent in the file record). Row 2
  (each positive subface locally thick or same-face paid) = the two
  payers (chapter 5): thickness-or-charge is precisely the residence-tax
  dichotomy - a thin record is a fast high-Reynolds event and pays the
  return race; a thick one sits in residence and pays viscosity; the
  single dial closes the gap between them. Row 3 (lost sign retained or
  paid as same-face return) = the no-recount routing of backscatter as
  debt, never deletion (chapter 2/Gate A). With the rows paid at the
  campaign's filed level, SRP.8 fires: S1, the peak bound, follows from
  the DEMAND-SUPPLY CROSSING - each record upcrossing demands net
  nonlinear work at least lambda (SRP.3, installed on 07-04), the record
  ladder's demands grow geometrically, while chapter 8's taxed cascade
  supplies work decaying geometrically per octave; a growing geometric
  demand against a decaying geometric supply admits finitely many
  records, so the signed critical height is bounded. S2: bounded signed
  height is a bounded critical norm; the critical Sobolev embedding
  places the solution in the space where the classical
  endpoint-regularity import (ESS class) closes gold-route smoothness -
  the import is stated as a cited classical theorem, owner's currency
  around it. S4: the chapter-2 unbounded-readout set is consumed by the
  silver CM test as designed. HONEST FLAGS: S3 (T*-plus schedule) is
  drafted at story+spec level only - the repo's T*-plus surfaces were
  NOT re-read in this window and flag F2 stands; the row payments
  inherit the campaign's pending gates and chapter-10 stratum - chapter
  9 is conditional exactly as its inputs are, no more, no less. Codex
  gate requested on the bridge identification (all three rows) and the
  demand-supply crossing. No closure claimed.
---

# Chapter 9 draft: the record bridge

## 1. The inherited criterion (07-04, installed)

Signed critical height H = half the squared critical norm; record
upcrossing from lambda to two lambda demands net nonlinear work at
least lambda after viscosity is paid (SRP.3, installed). The spine
SRP.8 reduces peak control to three rows: parent admission; local
thickness-or-charge; same-face no-cancellation. The 07-04 boundary
notes left exactly one open object: derive the rows from the NS packet
(SRP.12, ASH.9).

## 2. The bridge identification (the rows are the campaign)

ROW 1 = GATE A. "Record work admitted into a parent-known same edge"
is the oriented-admission/no-recount decomposition of chapter 2 - the
same object, already flagged in the step4 note as the signed-height
no-clipping principle. Marginal form proved-shaped; general form
Reynolds-weighted (I5 guard travels).
ROW 2 = THE TWO PAYERS. "Each positive subface locally thick or
same-face paid" is the residence dichotomy of chapter 5: a THIN record
is a fast event at high local Reynolds - the throughput dichotomy
collects the return race; a THICK record sits in residence - viscosity
collects. The single-dial crossover is exactly the statement that no
record can be too thin for one payer and too thick for the other.
ROW 3 = NO-RECOUNT ROUTING. "Lost sign retained or paid as same-face
return" is backscatter-as-debt: the oriented rays of chapter 2 route
every sign return as previously-admitted debt on the same channel,
never as deletion - the Jordan-decomposition failure mode the 07-04
notes warn against is structurally excluded by orientation.

## 3. S1 - the peak bound by demand-supply crossing

Records demand work lambda, then two lambda, then four - geometric
growth up the record ladder. Chapter 8 supplies work through the taxed
cascade: per-octave retention at most one-half times (one minus c0) -
geometric decay. A geometrically growing demand against a
geometrically decaying supply is satisfiable at most finitely often;
with the three rows paid, every record charge is finite and
same-fluid, so SRP.8 fires: the signed critical height is bounded on
the horizon. The peak bound is the crossing point of the two
geometries - the annulus later-debt story made quantitative: no
demanded instantaneous default, because demand outruns supply only
finitely far before the ledger refuses.

## 4. S2 - smoothness via the critical embedding

Bounded signed height = bounded critical norm; the critical Sobolev
embedding places the realized solution in the endpoint class whose
classical regularity import (ESS) yields gold-route smoothness. The
import is a cited classical theorem consumed at one point, wearing no
campaign vocabulary; everything before it is the owner's machinery.

## 5. S3 - T*-plus schedule (story + spec; flag F2 stands)

Dead waters: simultaneous rest; the ledger extends past T*; debt
stored in neighbors' higher rungs is payable across the infinite
horizon. DRAFTING FLAG: the repo's T*-plus surfaces must be re-read
before this section is prose; F2 remains open and is the next window's
first read.

## 6. S4 - silver pass-or-exit

The chapter-2 set where the tower readout is unbounded is the terminal
record candidate; the CM test consumes it: pass (finite charge,
absorbed) or exit (detachment) - and exit is excluded by the
schedulability of the annulus debt (row structure of section 2 applied
at the terminal cross-section). The Clay statement is sealed in
pass-or-exit form.

## 7. Conditional register (complete)

This chapter inherits, and adds nothing to: the pending Codex gates;
the chapter-10 stratum; flag F2 (T*-plus surfaces unread this window).
Gate requested on the bridge identification and the crossing argument.
