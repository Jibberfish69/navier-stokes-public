---
theorem_id: forward-gold-chapters-1-4-8-draft-and-chapter9-spec-20260705
status: chapters-1-4-8-drafted-referee-shape-chapter-9-assembly-spec-only-sources-must-be-reread
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 4-5 / chapter drafts
authorship: Claude (Fable), 2026-07-05. Assembly of filed campaign
  results; physical story Thomas Birnie in every clause.
completion_truth: >-
  Chapters 1, 4, 8 drafted at referee shape from material fully in
  hand. Chapter 9 is deliberately NOT drafted here: its ESS import and
  T*-plus/silver schedule must be assembled against the 2026-07-04
  signed-height/record-admission notes and the repo's T*-plus surfaces,
  which this window has not re-read; drafting them from memory would
  violate the lane's honesty discipline. Chapter 9 therefore gets an
  ASSEMBLY SPEC: exactly what it must contain, from which files, with
  the two import points flagged for verification in a fresh-context
  window. After this note, the item-4/5 residue is: chapter 9 assembly
  (spec here), final manuscript composition (mechanical), chapter 10
  (certification program, open), and the six pending Codex gates. No
  closure claimed.
---

# Chapters 1, 4, 8 (drafts) and chapter 9 (spec)

## Chapter 1 draft - Setting and the claim

Objects: suitable weak solutions on the Clay-facing domains; energy
inequality localized (suitability); the realized-solution frame: the
theorem concerns THE solution the fluid realizes, and the smoothness
claim is class membership - "not HOW smooth, THAT smooth." Statement,
gold: the realized solution remains in the smooth class for all time.
Statement, silver (backstop): pass-or-exit - if smoothness fails, the
failure is a class exit event with a terminal record; the campaign
excludes the exit. Both statements are theorems about participation:
pressure, viscosity, incompressibility as one membership law. Imports:
standard suitable-weak-solution existence; definitions only, no
mechanism. Referee remark placed here once: all constants in chapters
1-9 are named symbols; values live only in chapter 10.

## Chapter 4 draft - The dynamics-free layer

All statements in this chapter are consequences of the two per-triad
invariants alone (energy; helicity = signed wavenumber times energy)
and hold at ALL amplitudes with no dynamical hypothesis; by R2a
(audited) they hold verbatim for the true NS coefficients after triad
gauge. Contents, each an exact fraction or identity: the centered
same-helicity split is exactly one-half; the same-helicity octave toll
composes supermultiplicatively (chopping never helps that branch); the
mixed-helicity one-step classification (only the catalyst-flipped
branch relays; the corner retention supremum is exactly one-third,
unattained); the two-step composition identity (retention exactly
one-half - the marginal case); the invariant drain share (the child
pays the exact share (1 + mu/q)/2 to matching-helicity upscale
partners); the flipped-corner conservation ceiling. Everything here is
enemy-independent bookkeeping: what the membership law permits before
any dynamics is consulted.

## Chapter 8 draft - The floor and geometric decay

Inputs: payer 1 (chapter 5, closed-form viscous residence tax; general
Gate A in Reynolds-WEIGHTED form - the I5 guard rides every use);
payer 2 (chapter 5, throughput dichotomy: sub-f extraction is
sub-financing by definition, else the closed-form return floor r0(f));
the branch-killing layer (chapters 4 and 6: same-helicity dead at
exactly one-half; corner dead at one-third against one-half; two-step
marginality broken by compact-family positivity; fine chop dead by the
predator clock theorem with the tempo dichotomy routing each window to
its payer). Composition, single dial: both payers are monotone in the
one local Reynolds dial with opposite orientation; their maximum is
bounded below by the closed-form floor c0 = min(r0(f), c1 over the
capped parking window) with f fixed once by the closed-form
optimization; the parking cap (two independent derivations, phase
identity and adiabatic failure) removes the enemy's only depth knob.
Conclusion: per-octave forward retention is at most one-half times
(one minus c0); the doubling requirement fails by the factor
(one minus c0); critical mass decays geometrically down the tower with
ratio named, not numbered. The chapter closes by threading the
Reynolds-weighted guard: every use of payer 1 off the marginal stratum
carries the weight M explicitly into the retention bookkeeping, and
the crossover argument is run WITH the weight - no silent upgrade.
Conditional register of this chapter (complete): the chapter-10
constants (named symbols); the six pending gates.

## Chapter 9 - ASSEMBLY SPEC (not drafted; sources must be re-read)

Must contain, in order: (S1) signed-height peak bound from geometric
decay - assemble from the 2026-07-04 signed-height/record-admission
notes (active-capacity, record-admission boundary, signed-height
physical spine); verify the admission-boundary form used there matches
the chapter-8 output currency (per-octave retention on the marginal
stratum with weighted guard). (S2) Critical-embedding import (ESS):
the peak bound places the solution in the critical class whose
regularity import closes gold-route smoothness; verify the exact
import statement against the repo's ESS surface (source-forensics
canonical-equation ledger lists it); the import must be stated as a
cited classical theorem, owner's currency around it, no vocabulary
substitution. (S3) T*-plus schedule: dead-waters simultaneous rest;
the ledger extends past T*; assemble from the repo's T*-plus surfaces.
(S4) Silver pass-or-exit: the chapter-2 unbounded-readout set routes
here as the terminal-record candidate; the CM test consumes it;
default = detachment = exit excluded by the annulus later-debt
schedulability. VERIFICATION FLAGS for the assembling window: (F1) the
exact statement of the record-admission boundary in the 07-04 notes;
(F2) whether the T*-plus surface is note-grade or needs its own
referee pass. Chapter 9 is NOT claimable until S1-S4 are assembled
against re-read sources and gated.

## Ledger effect

Item 4/5 residue after this note: chapter 9 assembly per spec; final
composition pass (mechanical); chapter 10 (certification program);
six Codex gates. Nothing else.
