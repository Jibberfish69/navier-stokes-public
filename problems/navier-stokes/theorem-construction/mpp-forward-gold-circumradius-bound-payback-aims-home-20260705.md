---
theorem_id: forward-gold-circumradius-bound-payback-aims-home-20260705
status: circumradius-burden-attacked-parent-scale-partner-makes-triangles-coincide-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / Codex queue item 1 (the shape inequality)
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the spring pays back toward where the push came from - the predator's
  natural partner sits at the parent's own scale, and there the two
  triangles are one triangle. The payback aims home.
completion_truth: >-
  Attacks Codex's queue item 1 - prove or refute the circumradius-ratio
  lower bound on the financed family - pending re-gate. THREE STEPS.
  (STEP 1, THE SHARED-LEGS OBSERVATION) The relay triangle and the
  predator triangle are built on THE SAME TWO WAVEVECTORS - the waste
  and the child are the relay's own catalyst and child - so the two
  triangles share two sides exactly, differing only in the third: the
  relay closes on the parent, the predator closes on the partner. The
  partner's magnitude ranges over the packet-width cone around the
  VECTOR SUM of the shared legs - and that vector sum IS the parent
  (the relay's own closure). (STEP 2, THE PARENT-SCALE PARTNER IS
  ADMISSIBLE AND OPTIMAL) The partner slice centered at the parent's
  magnitude lies INSIDE the hyperbolic payback sector: it is below the
  child (the mixed-branch ordering, catalyst below parent below child,
  Codex-cited), matching helicity, and its drain share (one plus the
  step ratio, over two) exceeds one-half. The spring's natural payback
  channel points at the scale the push came from - the sector
  mathematics and the physical story coincide, and this is not a
  coincidence but the same fact twice. (STEP 3, THE BOUND AT THE HOME
  PARTNER) At the parent-scale partner the predator triangle equals
  the relay triangle up to the packet's angular width: shared legs
  exactly, third sides equal at the center of the slice, so the
  circumradius ratio is ONE up to width-controlled corrections, and
  the width is floored by the concentration frame's uncertainty
  relation (W1, exact scaling - a packet on one correlation volume has
  order-one angular support at window scale, already filed). The
  G-ratio floor then reads: (partner over parent - equal at the slice
  center, exact) times (circumradius ratio - one up to exact
  width-controlled corrections): an EXACT expression with no unnamed
  constant, whose only inputs are the step ratio, the packet-width
  floor, and the slice half-width choice - all filed, all exact.
  Codex's "remaining possible loss is angular thickness" is answered:
  the thickness needed is the packet's own, which the concentration
  frame supplies. HONEST FLAGS: (f1) the width-controlled corrections
  must be displayed as two-sided triangle-perturbation inequalities
  (shared legs, third side perturbed by the width - law-of-cosines
  displays, exact, queued as the note's one display debt); (f2) the
  slice's drain-share and hyperbolic-sector conditions at and around
  the parent scale are cited, but the SLICE INTEGRATION (the drain
  sums over the slice, not one partner) should be re-displayed on
  this slice - a cite-and-check against the rectification note. IF f1
  and f2 clear, queue item 1 is proved (not refuted), the G-ratio
  floor is exact algebra plus the packet width, and the symbolic
  middle-window comparisons - the last theory item - hold every
  ingredient in exact form. PRESS POINTS: the shared-legs line (the
  waste IS the relay's catalyst - one citation); the vector-sum-is-
  parent line (the relay's own closure - one line). Outstanding:
  Codex's queue; f1, f2; the comparisons; chapter 10. Filed is not
  cleared. No closure claimed.
---

# The circumradius bound: the payback aims home

## 1. Shared legs

The predator's pair is the relay's own catalyst and child: the two
triangles share two sides exactly. The relay closes on the parent;
the predator closes on the partner; and the partner cone is centered
on the vector sum of the shared legs - which is the parent itself,
by the relay's own closure. The two triangles differ only in how far
the third side wanders from home.

## 2. The home partner is admissible

The parent-scale partner slice lies inside the hyperbolic payback
sector - below the child, matching helicity, drain share above
one-half. The spring pays back toward where the push came from: the
sector mathematics and the story are the same fact twice.

## 3. The bound at home

At the home partner the triangles coincide up to the packet's
angular width; the circumradius ratio is one up to width-controlled
corrections; the width is floored by the concentration frame's
uncertainty relation (filed, exact scaling). The G-ratio floor
becomes exact algebra in the step ratio and the packet width - no
unnamed constant remains. Codex's angular-thickness worry is
answered by the packet's own thickness.

## 4. Flags and register

(f1) display the two-sided triangle-perturbation inequalities
(law-of-cosines, shared legs, perturbed third side); (f2) re-display
the slice integration of the drain on the home slice (cite-check
against the rectification note). If f1-f2 clear: queue item 1 is
PROVED, and the symbolic comparisons hold every ingredient exactly.
Press points: the shared-legs citation; the vector-sum-is-parent
line. Outstanding: Codex's queue; f1-f2; the comparisons; chapter
10. Filed is not cleared. No closure claimed.
