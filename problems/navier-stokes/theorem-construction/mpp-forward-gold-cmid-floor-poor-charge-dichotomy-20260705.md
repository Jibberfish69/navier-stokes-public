---
theorem_id: forward-gold-cmid-floor-poor-charge-dichotomy-20260705
status: codex-regated-shape-accepted-final-cmid-and-poor-price-comparisons-open
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / the two remaining TV items
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  a window that will not swing must sit, and sitting is residence, and
  residence is taxed - the poor pay by the clock what the rich pay by
  the mile.
completion_truth: >-
  Displays the two items of the tau/swing re-gate, pending re-gate.
  (ITEM 1, MIDDLE-RANGE ENDPOINTS AND THE UNIFORM C_mid FLOOR, same
  variables) Define the middle range as the middle half of the swing
  in the transfer variable: X between X2 plus one quarter of the swing
  and X2 plus three quarters of the swing - exact fractions. The
  endpoints in the elliptic variable are then u_lo = arcsn(1/2; m) and
  u_hi = arcsn(sqrt(3)/2; m) - displayed exact elliptic integrals (the
  inverse-sn values where sn squared equals one quarter and three
  quarters). The uniform cubic floor on the middle range, displayed in
  the same variables: each of the first two factors of the cubic is at
  least one quarter of the swing amplitude on the middle half; the
  third factor is at least the root spread X4 - X3, which on the
  capped band equals the swing amplitude times (1 - m)/m evaluated at
  the cap: hence  C_mid >= (swing amplitude)^3 x (1 - m_cap) /
  (16 m_cap)  - one displayed inequality, every symbol filed, positive
  strictly below the separatrix, vanishing only at the cap's approach
  to it - which the cap forbids. (ITEM 2, THE POOR-SIDE CHARGE AS AN
  OCTAVE DICHOTOMY, same currency) Let F_oct be the filed octave
  financing floor (total forward throughput demanded). DICHOTOMY,
  displayed: EITHER the rich class carries at least half the demand -
  then the traversal floors run on the rich class with the cleared
  half factor, and the octave's tax is collected by the mile - OR the
  poor class carries more than half, in which case four times the
  poor class's total amplitude exceeds half the demand, so the poor
  WINDOW COUNT is at least the demand over eight times the maximal
  swing amplitude - and each poor window, whatever its swings, is a
  RESIDENCE: it pays the per-window price floor (the cleared max of
  the two prices, floored by r0(f) tau_min) once per window; the poor
  class's total payment is at least the price floor times that
  displayed count - by the clock, not the mile - and both horns pay
  into the same octave tax ledger in the same log currency. The charge
  demanded is thus a displayed dichotomy: rich octaves pay per swing,
  poor octaves pay per residence, and the split threshold (one half)
  is an exact fraction. A window that will not swing must sit, and
  sitting is taxed. PRESS POINTS: the root-spread line (X4 - X3 =
  swing x (1-m)/m - one line from the normalized roots); the
  poor-count display's amplitude-max citation (swing amplitude bounded
  by the package ceiling - filed). WITH THESE TWO: every item of the
  TV downstream chain is displayed or cleared; the queue is this
  note's re-gate, then THE SYMBOLIC COMPARISONS, then CHAPTER 10.
  Filed is not cleared. No closure claimed.
---

# The last two displays

## Codex re-gate update, same date

Codex re-gate filed at
`mpp-forward-gold-codex-cmid-poor-charge-regate-20260705.md`.
The middle-range endpoints and root-spread route are accepted as the
right shape, but the displayed \(C_{\rm mid}\) floor still carries the
swing amplitude \(X_3-X_2\). To clear the tau floor, the note must
display either a positive lower bound for \(X_3-X_2\) on the financed
family or the normalized elliptic cancellation showing that the
amplitude factor drops out of \(\tau\). The poor-charge dichotomy and
count lower bound are also accepted as the right shape, but the final
same-currency comparison is still missing:
\[
P_{\min}\frac{F_{\rm oct}}{8A_{\max}}
\ge
\mathcal T_{\rm oct}.
\]
Until that line is written, poor windows have a residence count lower
bound, not a proved octave tax. Filed is not cleared beyond the
accepted shapes and prior narrow clearances.

## 1. Middle-range endpoints and the C_mid floor

Middle range: the middle half of the swing (exact quarters).
Endpoints: u_lo = arcsn(1/2; m), u_hi = arcsn(sqrt(3)/2; m) -
displayed elliptic integrals. Floor:

  C_mid >= (X3 - X2)^3 (1 - m_cap) / (16 m_cap),

from: two factors at least a quarter-swing each on the middle half;
the third at least the root spread, which equals the swing times
(1 - m)/m - at least its capped value. Every symbol filed; positive
because the cap ends the band before the separatrix.

## 2. The poor charge, as an octave dichotomy

With F_oct the filed octave financing floor:

  EITHER  sum over rich windows of throughput >= F_oct / 2
     (traversal floors at the cleared half factor - paid by the mile)
  OR      sum over poor windows of throughput > F_oct / 2
     => poor count >= F_oct / (8 x max swing amplitude)
     => poor payment >= (price floor) x (that count)
     (each window is a residence; residences pay the cleared
      per-window price floor - paid by the clock).

Both horns pay the same octave ledger in the same log currency; the
threshold is the exact fraction one half. The poor pay by the clock
what the rich pay by the mile.

## 3. Register

Both items displayed; press points: the root-spread line; the
amplitude-max citation. Queue: this note's re-gate; the symbolic
comparisons; chapter 10. Filed is not cleared. No closure claimed.
