---
theorem_id: forward-gold-comparison-sheets-stage-opening-20260705
status: comparisons-stage-opened-both-lines-expanded-exact-resolution-attempts-queued-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / stage opening (the comparison sheets)
authorship: Claude (Fable), 2026-07-05. The campaign's last theory
  stage, opened with its objects written in full. Physical story
  Thomas Birnie: what remains is to ask the water's own trigonometry
  two questions and write down what it answers - nothing is measured,
  everything is read.
completion_truth: >-
  OPENS the symbolic-comparisons stage by writing both comparison
  sheets in fully expanded exact form, pending re-gate; RESOLUTION IS
  NOT CLAIMED - the sheets are the stage's first display, the attempts
  follow. (SHEET 1, THE MIDDLE-WINDOW LINE, per chop number N in the
  finite window) REQUIRED side, exact: T_req(N) = ln 2 + N ln c*(s_N)
  with s_N = 2^(-1/N) and c*(s) = 2 s^3 / (2 s^3 + (1 - s)) - the
  filed catalyst-flipped composition fraction; T_req(2) = 0 exactly
  (the cleared marginality), T_req rising toward (ln 2)/2. DELIVERED
  side, exact in the filed symbols: T_del(N) = N x b(Lambda(N)) with
  the exponent Lambda(N) = gamma_home x sqrt(N / ln 2) x c_diff,
  where c_diff is the exact difference-factor constant of the predator
  clock display, and gamma_home = 1 - B(w_eff) with B(w_eff) the f1-f2
  bracket deficit at the substituted half-min width - the G-ingredient
  around the CLEARED center identity of one; the per-step drain b at
  exponent Lambda is the rectification display's forced-growth form
  with the amplitude-split seed logarithm: b(Lambda) =
  sigma_min x [1 - exp(-(Lambda - ln(1/theta))_+)] in its filed
  saturating shape, sigma_min the cleared drain-share floor. THE
  COMPARISON, per N in the window: T_del(N) >= T_req(N) - exact
  against exact, every symbol expanded above and cited. (SHEET 2, THE
  POOR-HORN LINE) r0(f) tau_min F_oct / (8 A_max) >= T_oct, expanded:
  r0(f) = f^2/(2 pi rho) (filed closed form); tau_min = the cleared
  modulus-only compact minimum; F_oct and T_oct = the filed octave
  financing floor and octave tax demand (both f-scaled closed forms of
  the chapter-8 bookkeeping); A_max = the package ceiling (filed).
  RESOLUTION TOOLKIT, cited: exact fraction arithmetic; monotonicity
  of every displayed function; the three honest exits per line
  (symbolic resolution; interval pass for that line alone; repair
  faced if negative). HONEST STATEMENT OF DIFFICULTY: sheet 1's
  resolution requires composing the saturating exponential with the
  square-root exponent against a logarithmic requirement - the
  composition is exact but not obviously one-signed; if its sign
  analysis resists the toolkit, THAT is the single point where the
  interval pass earns its keep, and the sheets make the pass's job
  one-dimensional. WITH THIS NOTE the comparisons stage is OPEN with
  its complete objects on file; the resolution attempts are the next
  and final theory units; chapter 10 waits on their outcome. PRESS
  POINTS: the b(Lambda) form's citation chain (rectification +
  amplitude split + drain-share floor - three links); the T_req
  display's use of the filed composition fraction (one citation).
  Filed is not cleared. No closure claimed.
---

# The comparison sheets

## Sheet 1: the middle window, per chop number N

REQUIRED (exact):
  T_req(N) = ln 2 + N ln c*(2^(-1/N)),
  c*(s) = 2 s^3 / (2 s^3 + (1 - s)).
  T_req(2) = 0 (cleared marginality); rising toward (ln 2)/2.

DELIVERED (exact in filed symbols):
  Lambda(N) = gamma_home x c_diff x sqrt(N / ln 2),
  gamma_home = 1 - B(w_eff)   (bracket deficit around the CLEARED
                               center identity of one),
  b(Lambda) = sigma_min x [1 - exp(-(Lambda - ln(1/theta))_+)],
  T_del(N) = N x b(Lambda(N)).

THE LINE, per N in the finite window:
  T_del(N) >= T_req(N).

## Sheet 2: the poor horn

  r0(f) tau_min F_oct / (8 A_max) >= T_oct,
  r0(f) = f^2 / (2 pi rho); tau_min cleared, modulus-only;
  F_oct, T_oct = the chapter-8 octave closed forms; A_max = the
  package ceiling.

## Difficulty, stated

Sheet 1 composes a saturating exponential with a square-root exponent
against a logarithmic requirement: exact, not obviously one-signed.
If its sign analysis resists the toolkit, that is the one point where
the interval pass earns its keep - and these sheets make the pass's
job one-dimensional. Nothing is measured; everything is read.

## Register

Stage OPEN; sheets displayed; resolution attempts are the next and
final theory units. Press points: the b(Lambda) citation chain; the
composition-fraction citation. Filed is not cleared. No closure
claimed.
