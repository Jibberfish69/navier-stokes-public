---
theorem_id: forward-gold-comparison-sheets-stage-opening-20260705
status: codex-regated-stage-open-sheets-not-fully-expanded
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / stage opening (the comparison sheets)
authorship: Claude (Fable), 2026-07-05. The campaign's last theory
  stage, opened with its objects written in full. Physical story
  Thomas Birnie: what remains is to ask the water's own trigonometry
  two questions and write down what it answers - nothing is measured,
  everything is read.
completion_truth: >-
  Codex re-gated this stage-opening note. Accepted: the symbolic
  comparison stage is now the right queue, with the middle-window line
  and poor-horn line identified. Not cleared: the sheets are not yet
  fully expanded exact comparisons. Sheet 1 must name the finite
  N-window, disambiguate gamma_home, and display or cite line-by-line
  the source chain for b(Lambda). Sheet 2 must either expand F_oct,
  T_oct, and A_max into closed forms or demote itself to a comparison
  in filed symbols. Resolution is not claimed; chapter 10 remains
  unexecuted. Filed is not cleared beyond identifying the comparison
  stage and its two live lines.
---

# The comparison sheets

## Codex re-gate update, same date

Codex re-gate filed at
`mpp-forward-gold-codex-comparison-sheets-regate-20260705.md`.
The symbolic-comparisons stage is accepted as the correct queue, with
two lines: the middle-window line and the poor-horn line. The sheets
are not yet fully expanded exact comparisons. Sheet 1 must name the
finite \(N\)-window, disambiguate \(\gamma_{\rm home}\), and display
or line-cite the source chain for \(b(\Lambda)\). In particular,
\(\gamma_{\rm home}=1-B(w_{\rm eff})\) is correct only if \(B\) is
explicitly the deficit; if \(B\) is the lower bracket itself, the
multiplier is \(B(w_{\rm eff})\). Sheet 2 must either expand
\(F_{\rm oct}\), \(T_{\rm oct}\), and \(A_{\max}\) into their closed
forms or state that it is a comparison in filed symbols, not a fully
expanded sheet. Filed is not cleared.

## Sheet 1: the middle window, per chop number N

REQUIRED (exact):
  T_req(N) = ln 2 + N ln c*(2^(-1/N)),
  c*(s) = 2 s^3 / (2 s^3 + (1 - s)).
  T_req(2) = 0 (cleared marginality); rising toward (ln 2)/2.

DELIVERED (exact in filed symbols):
  Lambda(N) = gamma_home x c_diff x sqrt(N / ln 2),
  gamma_home = 1 - B(w_eff)   (bracket deficit around the CLEARED
                               center identity of one),
  b(Lambda) = sigma_min x [1 - exp(-(Lambda - ln(1/theta))_+)],
  T_del(N) = N x b(Lambda(N)).

THE LINE, per N in the finite window:
  T_del(N) >= T_req(N).

## Sheet 2: the poor horn

  r0(f) tau_min F_oct / (8 A_max) >= T_oct,
  r0(f) = f^2 / (2 pi rho); tau_min cleared, modulus-only;
  F_oct, T_oct = the chapter-8 octave closed forms; A_max = the
  package ceiling.

## Difficulty, stated

Sheet 1 composes a saturating exponential with a square-root exponent
against a logarithmic requirement: exact, not obviously one-signed.
If its sign analysis resists the toolkit, that is the one point where
the interval pass earns its keep - and these sheets make the pass's
job one-dimensional. Nothing is measured; everything is read.

## Register

Stage OPEN; sheets displayed; resolution attempts are the next and
final theory units. Press points: the b(Lambda) citation chain; the
composition-fraction citation. Filed is not cleared. No closure
claimed.
