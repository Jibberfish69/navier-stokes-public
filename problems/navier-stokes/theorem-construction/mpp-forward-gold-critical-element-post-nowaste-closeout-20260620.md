# MPP Forward-Gold Critical Element Post-No-Waste Closeout

Date: 2026-06-20

## Status

Post-no-waste closeout of the critical-element route.

The critical-element architecture remains the cleanest native organization of
the forward-gold problem, but the current inputs do not produce the compact
no-waste ancient object that the rigidity consumers can kill.  Every tested
production shortcut leaves the same terminal heat-scale positive source atom,
now expressed as an endpoint defect, a profile-stability failure, a recurrent
local-suitable package, or a low-high two-frame molecule.

## 1. The architecture is exact

The native contradiction machine is:

```math
\text{finite-time breakdown}
\Longrightarrow
\text{minimal terminal ancient element}
\Longrightarrow
\text{no-waste local-energy visibility}
\Longrightarrow
\text{compact ancient rigidity contradiction}.
\tag{CEP.1}
```

The object is the local-energy package

```math
(U,\Pi,\mathcal E,\mathcal F,\mathcal D),
\tag{CEP.2}
```

where

```math
\mathcal E={1\over2}|U|^2,
\qquad
\mathcal D=\nu|\nabla U|^2,
\qquad
\mathcal F
=
\left({1\over2}|U|^2+\Pi\right)U
-\nu\nabla{1\over2}|U|^2.
\tag{CEP.3}
```

This is the correct native ledger.  The issue is production, not architecture.

## 2. Soft extraction leaves a boundary defect

The available compactness gives a local suitable source-residue package

```math
(U,\Pi,\mu_*^{src})
\tag{CEP.4}
```

on compact backward cylinders.  The terminal time-face pulse may converge to

```math
\mu_*^{src}(B_R\times\{0\})>0,
\qquad
\mu_*^{src}(B_R\times[-R^2,-a])=0
\quad(a>0).
\tag{CEP.5}
```

Then every open negative-time strip can be regular or trivial while the endpoint
defect remains nonzero.  Ancient rigidity cannot rule out a defect that has not
been tied to the interior orbit or evacuated by no-waste.

## 3. Compact orbit production is not profile decomposition

The compact element must be precompact modulo the allowed Navier-Stokes
symmetries and gauges:

```math
\{U(t):t<0\}
\quad\text{precompact modulo scale, center, rotation, frame, and selector.}
\tag{CEP.6}
```

Ordinary profile decomposition supplies energy orthogonality, not stability of
the selected terminal positive source carrier:

```math
\left|
\mu_{terminal}^{src,+}(u_m)
-
\sum_{j\in J_m}\mu_{terminal}^{src,+}(U^j)
\right|
\to0
\tag{CEP.7}
```

is not installed.  Positive-part selection after localization and packet choice
can keep a terminal lobe even when signed or separated profile terms are paid.

The two-frame profile branch has also been tested.  Separated pairs vanish,
duplicate parameters merge, and paraproduct capture supplies CM witness support.
The surviving near-resonant low-high same-window molecule is still

```math
\left[\langle S_{<j}^{low}w_j,w_j\rangle\right]_+
\tag{CEP.8}
```

on a terminal heat window.  It requires the same source-residence, Carleson,
strip-modulus, no-waste, or scale-nested Liouville input.  It is not an
independent compact-orbit supplier.

## 4. Bounded recurrence is visibility, not stationarity

Bounded recurrent heat-scale slabs can produce a nonzero local-suitable package
after time shifts:

```math
(v_k,q_k,\mu_k)\rightharpoonup(V,Q,\mu)
\tag{CEP.9}
```

on compact renormalized-time windows, with nontrivial local energy or critical
action.  This still need not be stationary, unique, no-waste, or compact as a
full orbit.  It can be time-periodic, heteroclinic, modulated, profile-switching,
or defect-bearing.

Ordinary physical energy monotonicity cannot run LaSalle in terminal log time.
The physical energy and dissipation carry the exponential discount:

```math
\int e^{-s/2}D_R(s)\,ds<\infty,
\tag{CEP.10}
```

whereas recurrence needs unweighted control:

```math
\int D_R(s)\,ds<\infty
\quad\text{or}\quad
\int\mathcal A_R(s)\,ds<\infty.
\tag{CEP.11}
```

Compactness does not remove this discount.  The missing input is still strict
rescaled no-waste or an equivalent unweighted critical-action reserve.

## 5. Endpoint critical-norm rigidity is a consumer

Endpoint \(L^\infty_tL^3_x\), Serrin-critical \(L^5\), and CKN epsilon
regularity kill a compact element once the whole object has the required
critical bound or smallness.  They do not produce that bound from energy.

For

```math
u_j(t,x)
=
a_jr_j^{-1}V\left({t-t_j\over r_j^2},{x-x_j\over r_j}\right),
\tag{CEP.12}
```

the physical energy/dissipation cost scales like

```math
a_j^2r_j,
\tag{CEP.13}
```

while local \(L^3\), CKN, and \(L^5\) critical charges scale like

```math
a_j^3,
\qquad
a_j^3,
\qquad
a_j^5.
\tag{CEP.14}
```

Thus finite physical cost permits unbounded critical amplitude over a terminal
dyadic stack.  Critical-norm regularity is a downstream consumer, not the
missing production theorem.

## 6. No-waste remains the production lock

The local energy defect measure

```math
\mu_{LE}
=
-
\left(
\partial_t\mathcal E+\mathcal D+\nabla\cdot\mathcal F
\right)
\ge0
\tag{CEP.15}
```

sees endpoint mass.  It does not by itself identify that mass with the selected
positive pre-Cauchy carrier after moving localization, projection, lifting,
packet selection, and positive-part extraction.

Under the heat-scale normalization \(x=x_j+r_jy\), \(t=T_j+r_j^2s\),
\(u=r_j^{-1}v_j\), define the renormalized local-energy defect on normalized
sets \(A\subset Q_1^-\) by

```math
\mu_{LE,j}^{ren}(A)
:=
r_j^{-1}\,
\mu_{LE}^{phys}
\bigl((x_j+r_jA_x)\times(T_j+r_j^2A_t)\bigr).
\tag{CEP.16}
```

Then physical local-energy defect carries one radius exactly:

```math
\mu_{LE}^{phys}(Q_j)
=
r_j\,\mu_{LE,j}^{ren}(Q_1^-).
\tag{CEP.16a}
```

Finite physical local-energy measure can therefore control

```math
\sum_j r_j\mu_{LE,j}^{ren}(Q_1^-),
\tag{CEP.17}
```

but the compact no-waste element needs unweighted terminal tightness of
\(\mu_{LE,j}^{ren}\), or source-carrier evacuation.

## 7. Final production state

At current inputs, the critical-element route has exactly these open production
locks:

```math
\begin{gathered}
\text{CompactOrbitProduction.A / selected positive profile stability},\\
\text{LocalEnergyFluxNativeResidueIdentity.A},\\
\text{TerminalLocalEnergyFluxTightness.A},\\
\text{StrictRescaledNoWasteLyapunov.A or equivalent unweighted reserve}.
\end{gathered}
\tag{CEP.18}
```

The rigidity consumers remain downstream:

```math
\text{CompactAncientRigidity.A},
\qquad
\text{endpoint }L^3/\text{Serrin/CKN consumers},
\qquad
\text{Landau/Sverak-type consumers}.
\tag{CEP.19}
```

They apply only after the first line has produced the right object.

## Verdict

The critical-element route does not currently solve the terminal heat-scale
atom.  It gives the right high-level contradiction machine, but every available
production shortcut stops before the needed compact no-waste ancient element.

The remaining forward-gold work is not another diagnostic language.  It is the
production of one genuinely new object:

```math
\boxed{
\text{a compact/no-waste terminal element with unweighted heat-scale action
tightness,}
}
\tag{CEP.20}
```

or a proof that the retained terminal pulse must be consumed by the CM
Part/Field route instead of by forward gold.
