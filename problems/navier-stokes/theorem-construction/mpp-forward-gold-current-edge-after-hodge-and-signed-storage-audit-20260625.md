---
theorem_id: forward-gold-current-edge-after-hodge-and-signed-storage-audit-20260625
status: exact-current-edge-pressure-stress-cubic-collar-positive-clock-open
logical_landing_node: global_same_packet_full_clock_from_original_data
edge_effect: >-
  Consolidates the current forward-gold edge after the material Hodge projection
  commutator reduction and the signed pressure-time storage test. The Hodge
  projection, material pressure operator, pressure-time derivative, coefficient
  frame, and transported collar are not detached obstructions; they are coupled
  coordinates of the same pressure-viscosity-incompressibility-velocity
  participation law on the original transported material history. The remaining
  unproved service is the positive pressure-stress, cubic-collar, and
  collar-stress part of that same moving material annular return. Finite
  integral of that service is exactly the remaining full-clock bridge to fixed
  H^s continuation. This note is an exact proof-state reduction, not a completed
  smoothness proof.
parents:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-material-hodge-projection-commutator-pressure-operator-reduction-20260625.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-signed-cancellation-pressure-time-cubic-gradient-test-20260625.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-full-smoothness-closure-material-stokes-hodge-20260625.md
  - problems/navier-stokes/theorem-construction/mcp-global-full-clock-stress-time-spatial-reduction-20260625.md
  - problems/navier-stokes/live-theorem-edge.yaml
---

# Current Edge After Hodge And Signed Storage

Date: 2026-06-25

## 0. Object

The object is the original smooth solution's transported material spike-wave
history.  The annulus is the same fluid around the candidate terminal
concentration, sampled through that one material history.  The pressure,
viscosity, incompressibility constraint, and velocity field are kept as one
coupled participation law.

In the material chart:

```math
\partial_t v+A^\top\nabla_a q
=
\nu\,\operatorname{div}_a(G\nabla_a v),
\qquad
\operatorname{div}_a(Av)=0.
\tag{CE.1}
```

The pressure operator is therefore:

```math
L_G q
:=
-\operatorname{div}_a(G\nabla_a q)
=
R(v,A,G),
\tag{CE.2}
```

with \(A\), \(G\), and \(R\) generated by the same material flow.

## 1. What the Hodge reduction proves

The material Hodge projection commutator is not a separate pressure problem.
For the native shell \(h_j=\Pi_jv\), the installed reduction gives:

```math
\left|\langle [\partial_t,\mathbb P_A]v,h_j\rangle\right|
\le
C\,e_j\Theta_j^{press/op}
+
C\,e_j\Theta_j^{coef}
+
r_j^{legal}.
\tag{CE.3}
```

The coefficient-frame and base-collar pieces are absorbed by the top-strain /
frame clock.  The remaining Hodge contribution is:

```math
[\mathcal R_j^{Hodge}]_+
\le
\text{paid top-strain/frame/collar coordinate}
+
C\,e_j\Theta_j^{press/RHS}
+
C\,e_j\Theta_j^{annular\ stress}
+
r_j^{legal}.
\tag{CE.4}
```

So pressure ellipticity stays coupled to participation.  The Hodge term reduces
to the same pressure/RHS and annular stress interface already present in the
material clock.

## 2. What signed pressure-time storage proves

In the gauge-reduced packet,

```math
D_tU=\nabla\cdot T^\sharp,
\qquad
T^\sharp=-PI+2\nu S(U),
\qquad
\nabla\cdot U=0,
\qquad
D_t\phi=0.
\tag{CE.5}
```

The pressure-time term in the annular stress-work derivative is:

```math
I_{2,p}=-\int D_tP\,U\cdot\nabla\phi.
\tag{CE.6}
```

With the pressure-service storage

```math
Y_\phi(t)=\int P\,U\cdot\nabla\phi\,dx,
\tag{CE.7}
```

the exact signed identity is:

```math
I_{2,p}
=
-{d\over dt}Y_\phi
+
\int P\,(\nabla\cdot T^\sharp)\cdot\nabla\phi
-
\int P\,U_i(\partial_iU_k)(\partial_k\phi).
\tag{CE.8}
```

Thus the raw symbol \(D_tP\) is removed.  The pressure service is not erased.
It reappears as pressure-stress and pressure-transport work on the same
transported annulus.

The viscous stress-time and collar-transport pieces also leave cubic collar
terms:

```math
\mathcal C_\phi^{cubic}
\sim
\nu\,U(\nabla U)(\nabla U)\nabla\phi
+
\nu\,U\,S(U)\,\nabla U\,\nabla\phi .
\tag{CE.9}
```

This cubic collar functional has no hidden sign.  It changes sign under the
frozen algebraic replacement \(U\mapsto -U\), while the collar and metric are
fixed.  It is therefore not a nonnegative loss produced by local algebra.

## 3. The exact remaining service

After the Hodge reduction and signed storage calculation, the remaining live
positive service is:

```math
d\Omega_N^{live}
\simeq
\left[\mathcal Q_\phi^{press/stress}\right]_+\,dt
+
\left[\mathcal C_\phi^{cubic}\right]_+\,dt
+
\left[\mathcal R_\phi^{collar/stress}\right]_+\,dt
\tag{CE.10}
```

on the same transported material history, with the finite-depth coefficient,
pressure, velocity, and collar records read in the material chart.

Equivalently, in the spatial pressure-stress form already installed:

```math
\int_0^{T_*}
\sum_j2^{\gamma j}
\|\Delta_j\mathcal S_\phi^\sharp(t)\|_{L^1_x}\,dt<\infty
\tag{CE.11}
```

is a sufficient coordinate for the same clock, where \(\mathcal S_\phi^\sharp\)
is the full pressure-stress-velocity density obtained after reducing
\(D_tT^\sharp\).

## 4. What closes smoothness

The forward-gold closure now has this exact form:

```math
\int_0^{T_*}d\Omega_N^{live}<\infty
\Longrightarrow
\int_0^{T_*}d\Omega_N^{full}<\infty
\Longrightarrow
\sup_{t<T_*}\|u(t)\|_{H^s}<\infty
\quad(s>5/2)
\Longrightarrow
\text{classical relaunch past }T_*.
\tag{CE.12}
```

The first implication is the current unproved mathematical line.  The later
implications are the installed material-clock and continuation readouts.

So the current edge is not a new endpoint problem and not a detached heat-scale
packet problem.  It is the finite positive action of the same moving
pressure-strain/cubic-collar return underneath the original material spike-wave
history.

## 5. Non-closure statement

The following routes have now been checked and do not close `(CE.10)`:

```math
\text{local Hodge projection commutator control},
\tag{CE.13}
```

```math
\text{pressure-time signed storage alone},
\tag{CE.14}
```

```math
\text{termwise absolute annular density control},
\tag{CE.15}
```

```math
\text{finite-time Gramian or local cross-storage symmetrizer},
\tag{CE.16}
```

```math
\text{height-width thinning or raw finite energy alone}.
\tag{CE.17}
```

Each check preserves the physical participation object.  Each check reduces a
false loose term back into the same pressure-viscosity-incompressibility-
velocity packet.  None of them proves finite positive material action for
`(CE.10)`.

The remaining theorem is therefore:

```math
\boxed{
\int_0^{T_*}
\left(
\left[\mathcal Q_\phi^{press/stress}\right]_+
+
\left[\mathcal C_\phi^{cubic}\right]_+
+
\left[\mathcal R_\phi^{collar/stress}\right]_+
\right)dt
<\infty
}
\tag{CE.18}
```

or an equivalent bounded-below same-material storage whose derivative pays this
positive service before rectification.
