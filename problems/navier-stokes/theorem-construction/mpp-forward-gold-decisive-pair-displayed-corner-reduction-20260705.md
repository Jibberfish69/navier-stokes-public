---
theorem_id: forward-gold-decisive-pair-displayed-corner-reduction-20260705
status: codex-regated-pair-displayed-corner-reduction-not-cleared
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / the decisive pair, displayed
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the whole day funnels to two lines at one chop number, and the
  family's worst case sits at a corner of its own box - the water is
  asked its hardest question exactly once.
completion_truth: >-
  Displays the decisive pair in full composition and reduces its
  family-infimum to corner evaluations, pending re-gate; THE PAIR IS
  NOT RESOLVED HERE. (THE PAIR, fully composed) LEG A (monotonicity):
  sigma_min x [1 - exp(-D_3)] >= Delta_req(3); LEG B (decision):
  3 sigma_min x [1 - exp(-D_3)] >= T_req(3); with D_3 = (gamma_home x
  c_diff x sqrt(3/ln 2) - ln(1/theta))_+, and the right sides the
  filed exact powers-of-two forms (T_req(3) = ln 2 - 3 ln(2 -
  2^(-1/3)); Delta_req(3) = T_req(4) - T_req(3), T_req(4) as filed).
  Note leg B's threshold T_req(3) is SMALLER than tier 1's half-log-
  two, and leg A's is smaller still - the proved front-loading bought
  these weaker marks. (THE FAMILY-INFIMUM STRUCTURE) The left sides
  are functions over the financed family through gamma_home (the
  three-factor bracket product at the half-min width), sigma_min (the
  slice drain-share floor), theta (the displayed choice), and c_diff
  (the difference-factor form): the comparison must hold at the
  family's INFIMUM. (THE CORNER REDUCTION, displayed) Each factor of
  the left side is MONOTONE in each family coordinate on the family's
  defining box (the bracket factors are monotone in the width and the
  angle by the displayed cosine/sine monotonicities; the drain share
  is monotone in the width; the exponent is increasing in gamma and
  decreasing in the seed logarithm; the saturation is increasing in
  the exponent): the infimum of a composition of coordinatewise-
  monotone positive factors over a box is attained AT A CORNER of the
  box - the corner where each coordinate sits at its adverse end, and
  WHICH end is adverse is displayed per coordinate by the listed
  monotonicities. The family box's corners are filed symbols: the
  width floor (concentration frame), the angle floor (angular
  appetite), the ordering margin (branch), the invariant floor
  (financed family), theta (the choice, ours - set once at its
  displayed optimum). THE DECISIVE PAIR THEREFORE REDUCES TO TWO
  CORNER EVALUATIONS: each leg compared at the single adverse corner,
  every quantity a filed closed form composed of powers of two,
  elliptic values at named moduli, and the trigonometric bracket at
  the corner arguments. (WHAT COMES NEXT, honestly) The corner
  comparison is the FINAL SYMBOLIC ATTEMPT: if the composed corner
  values order correctly by exact means (the toolkit: monotonicity,
  integer comparisons after clearing - the same finish that proved
  front-loading), sheet 1 CLOSES; if the corner comparison resists
  symbols, the interval pass = TWO COMPARISONS AT ONE CORNER - its
  final, minimal form; if a corner comparison fails, tier 2's
  equivalence makes the failure meaningful at that corner and the
  repair is faced there. PRESS POINTS: the per-coordinate adverse-end
  table (each monotonicity cited to its display); the corner's
  membership in the family (the corner must satisfy the defining
  inequalities it is built from - one consistency line). Chapter 10
  unchanged (pending the attempt's outcome). Filed is not cleared.
  No closure claimed.
---

# The decisive pair, displayed; the infimum at a corner

## Codex re-gate header

Codex re-gate installed in
`mpp-forward-gold-codex-front-loading-decisive-pair-regate-20260705.md`.
The two decisive inequalities are accepted as the right Sheet-1 display.
The family-infimum/corner reduction is not cleared here: the note names
the adverse-end table and corner-membership check, but those displays
are still owed before the corner can be treated as the worst point.

## 1. The pair

  LEG A:  sigma_min [1 - exp(-D_3)] >= Delta_req(3)
  LEG B:  3 sigma_min [1 - exp(-D_3)] >= T_req(3)
  D_3 = (gamma_home c_diff sqrt(3/ln 2) - ln(1/theta))_+

Right sides: the filed exact powers-of-two forms. The proved
front-loading bought leg thresholds strictly below tier 1's mark.

## 2. The infimum sits at a corner

Every left-side factor is coordinatewise monotone on the family's
defining box (cosine/sine bracket monotonicities; drain share in the
width; exponent in gamma and the seed; saturation in the exponent).
The infimum of such a composition over a box is attained at the
corner of adverse ends - and each end's direction is displayed by its
cited monotonicity. The corners are filed symbols: width floor, angle
floor, ordering margin, invariant floor, and theta at its displayed
optimum. Two corner evaluations decide the pair.

## 3. The three endings, restated for the corner

Symbolic ordering at the corner (the front-loading finish's toolkit)
closes sheet 1. Resistance makes the pass two comparisons at one
corner - its minimal possible form. Failure is meaningful at that
corner by the proved equivalence, and the repair is faced there. The
water is asked its hardest question exactly once.

## 4. Register

Pair displayed; corner reduction displayed; press points: the
adverse-end table; the corner-membership consistency line. Queue:
re-gate; THE CORNER ATTEMPT (the campaign's final symbolic unit);
sheet-2 expansion; chapter 10. Filed is not cleared. No closure
claimed.
