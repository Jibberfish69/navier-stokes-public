# Door 1 Shadow Ledger Attempt

Date: 2026-06-18

Status: direct Door 1 proof attempt; not closed.  The attempted shadow-ledger
proof reduces `TerminalSignedSaturation.A` to the terminal Zeno source-refill
anti-atom / rigidity branch.

2026-06-18 continuation: `mpp-forward-gold-door1-terminal-source-cost-dichotomy-20260618.md`
proves the exact cost split for the Door 1 shadow source marginal.  Uniform
same-shadow super-`L1_s` residence implies the anti-atom theorem; a surviving
terminal atom has infinite super-`L1_s` residence cost and must be handled by
`StressFluxZenoRefillRigidity.A` or `Door1InfiniteCostEndpointArtifact.A`.

Role: continue the current forward-gold Door 1 frontier:

```math
\text{TerminalSignedSaturation.A}
\quad\text{or equivalently}\quad
\text{InternalDonorSinkDepletion.A}.
\tag{SL.1}
```

This is still inside `WeightedLiftedSkewDefectLegal.A`, hence inside
`ProjectedLocalPreCauchyResidueIdentity.A`.

## Checked source state

The incoming Door 1 reduction is recorded in

```text
mpp-forward-gold-weighted-skew-to-terminal-signed-saturation-20260618.md
```

The older signed-current corpus is also decisive:

```text
mpp-sourcewall-signed-current-collapse-ledger-20260505.md
mpp-signed-samefluid-edgepairing-direct-attempt-20260505.md
mpp-low-high-terminal-signed-saturation-forward-no-free-sink-direct-attempt-20260610.md
mpp-terminal-new-production-gold-direct-attempt-20260614.md
```

Those notes already separate three facts:

1. bare signed skew pairing exists before localization, weights, lifts, and
   positive-part selection;
2. retained partners, declared legal exits, ASAC pair-weight defects, and finite
   non-Zeno donor trees are paid;
3. the terminal Zeno source-refill branch is not paid by finite `L^1_s` source
   mass or by `TerminalSignedSaturationCMFaceReduction.A`.

The CM face-reduction theorem is support for an admitted CM witness face.  It is
not a forward Door 1 no-free-sink payment.

## Target inequality

The desired terminal signed saturation statement is edgewise:

```math
d\pi^+(e)
\le
d\pi^-(e^*)
+d\mathcal D_{\mathrm{legal}}(e),
\qquad
\sum_e d\mathcal D_{\mathrm{legal}}(e)<\infty.
\tag{SL.2}
```

Here `e` is a selected positive terminal source-current edge and `e^*` is its
signed algebraic partner before the terminal operations.

## Attempt: pair-closed shadow ledger

Enlarge the selected terminal family `\mathcal F_N` to a shadow family
`\operatorname{Sh}(\mathcal F_N)` by carrying, for every selected positive edge
`e`, the bare signed counter-edge `e^*` and the same-fluid donor ancestry needed
to explain how the positive edge was fed.

The intended proof is a minimal-unpaid-edge argument.

Assume `(SL.2)` fails.  Choose an unpaid selected positive terminal edge with
minimal terminal depth among all failures after retained partners and declared
legal exits have been removed.

The signed-current partition gives four cases.

### Case 1: retained counter-edge

The signed partner stays in the same usable terminal ledger.  This is paid by
the post-ASAC positive pair-weight defect charge:

```math
\text{ASAC.A}
\Longrightarrow
\text{PositivePairWeightDefectCharge.A}.
\tag{SL.3}
```

### Case 2: legal exit

The counter-edge leaves through a declared boundary, cutoff, projection,
pressure, collar, spill, heat-action, or off-family legal loss.  These terms are
already in the legal ledgers attached to the Door 1 identity.

### Case 3: finite donor tree

The positive edge is fed by a finite same-fluid donor chain.  This is paid by
the existing donor telescope:

```math
\text{LocalDonorBalance.A}
+\text{EntranceLeafDecay.A}.
\tag{SL.4}
```

Finite non-Zeno ancestry cannot carry an unpaid terminal surplus.

### Case 4: terminal Zeno refill

The only remaining case is an infinite same-fluid donor-refill chain whose
active windows shrink to the terminal face.  In time marginal form this produces

```math
I_m=(T_m-\tau_m,T_m],
\qquad
\tau_m\downarrow0,
\tag{SL.5}
```

with nonnegative selected source marginals `g_m` satisfying

```math
\int_{I_m}g_m(s)\,ds\ge M_0>0,
\qquad
\sup_m\int_{I_m}g_m(s)\,ds<\infty.
\tag{SL.6}
```

The model family is exactly

```math
g_m(s)={a\over\tau_m}\mathbf 1_{(-\tau_m,0]}(s),
\qquad
\tau_m\downarrow0.
\tag{SL.7}
```

It has fixed mass and weak terminal concentration:

```math
\int g_m(s)\,ds=a,
\qquad
g_m(s)\,ds\rightharpoonup a\delta_0.
\tag{SL.8}
```

Finite `L^1_s` source control permits `(SL.7)`.  The shadow ledger carries the
ancestry, but it does not itself create a terminal strip modulus, a super-`L^1`
source estimate, a scale-critical source Carleson charge, or a rigid source
residue Liouville theorem.

## Result of the attempt

The shadow-ledger idea pays the finite graph-flow part of Door 1.  It does not
pay the zero-thickness terminal refill part.

Thus the strongest honest reduction is:

```math
\boxed{
\text{Door1TerminalSourceTimeAntiAtom.A}
\Longrightarrow
\text{TerminalSignedSaturation.A}.
}
\tag{SL.9}
```

where `Door1TerminalSourceTimeAntiAtom.A` is the same terminal slab modulus,
now applied to the selected positive native source carried by the Door 1 shadow
ledger:

```math
\mu_{src,N}^{+,sh}(B_R\times[T-\varepsilon,T])
\le
\omega_R(\varepsilon)+o_N(1),
\qquad
\omega_R(\varepsilon)\downarrow0.
\tag{SL.10}
```

Equivalently, the uncharged branch can be killed by a Door 1 version of Zeno
source-refill rigidity:

```math
\boxed{
\text{StressFluxZenoRefillRigidity.A}.
}
\tag{SL.11}
```

This says that a same-fluid terminal refill chain carrying nonzero selected
positive native source either produces a legal terminal residue visible to the
stress-flux/native-carrier identity or is rigidly impossible.

## Verdict

`TerminalSignedSaturation.A` is not proved by shadow selection, finite
edge-pairing, ASAC payment, legal exits, or finite donor depletion.

The actual Door 1 child after this attempt is:

```math
\boxed{
\text{Door1TerminalSourceTimeAntiAtom.A}
\quad\text{or}\quad
\text{StressFluxZenoRefillRigidity.A}.
}
\tag{SL.12}
```

This is not a new generic anti-atom route.  It is the exact terminal refill
branch left after the signed-current accounting has paid every finite retained,
legal, and donor-tree case.
