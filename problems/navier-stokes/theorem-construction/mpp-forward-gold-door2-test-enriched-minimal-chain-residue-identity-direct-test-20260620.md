# Door 2 Test-Enriched Minimal-Chain Residue Identity Direct Test

Date: 2026-06-20

## Status

Direct child test complete.  `TestEnrichedMinimalChainResidueIdentity.A` is not
installed by the scalar minimal-chain projected residue identity.

The scalar identity sees one constant selector moment.  A Door 2 modulation mode
is a nonconstant test.  A signed residual can have zero scalar moment and still
be visible to a nonconstant mode test.

## 1. Target under test

Let the selected same-shadow residual measure be

```math
dE_m
:=
q_m\,d\mu_m-\Sigma_m\xi_m\,d\mu_m
\tag{TEI.1}
```

on the minimal selected source/stress shadow.  The test-enriched identity needed
by Door 2 is

```math
\left|
\langle dE_m,\phi_{i,m}\rangle
\right|
\le
\operatorname{Legal}_{m,i}+o_m(1),
\tag{TEI.2}
```

for every legal local mode multiplier `phi_{i,m}` realizing the packet weight
`psi_i`.

## 2. What the scalar identity controls

The scalar Door 1 identity controls only constant selector moments, for example

```math
\left|
\langle dE_m,1\rangle
\right|
\le
\operatorname{Legal}_m+o_m(1),
\tag{TEI.3}
```

or the corresponding finite constant-vector selector projection.

This is weaker than `(TEI.2)`.

## 3. Exact zero-moment countermodel

Take two retained packets `P_1` and `P_2` and set

```math
dE_m
=
A\delta_{P_1}-A\delta_{P_2},
\qquad
A>0.
\tag{TEI.4}
```

Then the scalar moment vanishes:

```math
\langle dE_m,1\rangle
=
A-A
=0.
\tag{TEI.5}
```

Choose a legal mode multiplier with

```math
\phi_{i,m}(P_1)=1,
\qquad
\phi_{i,m}(P_2)=-1.
\tag{TEI.6}
```

Then

```math
\langle dE_m,\phi_{i,m}\rangle
=
2A.
\tag{TEI.7}
```

Thus scalar cancellation does not imply test-enriched cancellation.

## 4. Conditional routes that would prove the test-enriched identity

There are two exact sufficient routes.

The first is total-variation same-shadow residue evacuation:

```math
\|dE_m\|_{TV(\mathcal S_m^{sel})}
\le
\operatorname{Legal}_m+o_m(1).
\tag{TEI.8}
```

Then every bounded legal mode test satisfies

```math
|\langle dE_m,\phi_{i,m}\rangle|
\le
\|\phi_{i,m}\|_\infty
\bigl(\operatorname{Legal}_m+o_m(1)\bigr).
\tag{TEI.9}
```

The second is a finite-mode separating estimate:

```math
\|dE_m\|_{\mathcal M_i}
\le
\operatorname{Legal}_{m,i}+o_m(1),
\tag{TEI.10}
```

where `M_i` is the dual test class generated by the chosen modulation modes,
and the selected proof only needs that finite mode family.

Thus

```math
\boxed{
\text{TestEnrichedMinimalChainResidueIdentity.A}
\Leftarrow
\text{TotalVariationSameShadowResidueEvacuation.A}
\quad\text{or}\quad
\text{FiniteModeResidueEvacuation.A}.
}
\tag{TEI.11}
```

## 5. Current input gap

The current scalar minimal-chain identity does not give `(TEI.8)` or
`(TEI.10)`.

Finite donor depletion removes finite chains only after scalar signed partners
are matched or legally exported.  It does not control a zero-total signed
residual that remains inside the selected shadow and is seen by a nonconstant
mode test.

`SourceResolvedPairedTraceCompactness.A` and `SourceWeightedPolarSaturation.A`
point toward `(TEI.8)`, but current inputs do not install them at
test-enriched strength.  `ModeTestCompatibility.A` makes `phi_{i,m}` legal; it
does not prove that the residual measure vanishes against it.

## 6. Relation to mode donor-edge coherence

`ModeDonorEdgeCoherence.A` is the edgewise version of this problem.  It says
that donor pairs that cancel scalarly also cancel after multiplication by the
mode weight, up to legal action.

`TestEnrichedMinimalChainResidueIdentity.A` is the measure-level version.  It
says no zero-scalar residual remains in the selected shadow for a mode test to
detect.

Both require a stronger object than scalar residue cancellation:

```math
\text{same-shadow residual evacuation in a nonconstant test norm.}
\tag{TEI.12}
```

## Verdict

`TestEnrichedMinimalChainResidueIdentity.A` does not close from the scalar
minimal-chain identity.  The scalar identity can miss zero-total signed
residuals, and a Door 2 mode test can see them.

The bridge closes only with total-variation same-shadow residue evacuation or
with finite-mode residue evacuation on the exact selected mode family.  Current
inputs do not install either theorem.
