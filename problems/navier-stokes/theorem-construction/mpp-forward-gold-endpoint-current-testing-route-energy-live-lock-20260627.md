---
theorem_id: forward-gold-endpoint-current-testing-route-energy-live-lock-20260627
status: gold-open-parent-subtracted-current-installed-testing-and-routed-energy-open
created: 2026-06-27
problem: navier-stokes
route: forward-gold same-material selected-density / stopped endpoint-current
source_surfaces:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-routed-endpoint-source-current-from-parent-subtracted-dynamic-silence-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-stopped-adjoint-testing-data-construction-boundary-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-material-adjoint-accretive-testing-energy-boundary-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-stopped-routed-participation-current-carleson-direct-test-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-original-history-routed-current-energy-finiteness-boundary-20260627.md
attacks_hinge:
  - ParentSubtractedDynamicEndpointCurrentRepresentation.A
  - StoppedEndpointAccretiveTestingData.A
  - MaterialAdjointAccretiveTestingData.A
  - OriginalHistoryRoutedCurrentEnergyFinite.A
  - StoppedRoutedParticipationCurrentCarleson.A
---

# Endpoint current, testing, and routed-energy live lock

The supplied silent-source/local-increment shape is valid support, but it is not
the full Gold closure.  The current live object is the stopped endpoint-current
route with three noncircular burdens kept separate.

## 1. Parent-subtracted current is the installed source representation

The installed endpoint current is not an arbitrary elliptic right inverse of
the endpoint mass.  It is the parent-subtracted current generated by the
original material history before selected positive readout.

For the fresh parent-subtracted part,

```math
M_{0,parent\text{-}sub}^{fresh}
=
D_AJ_{EP}^{S,new}
+M_{0,stop}^{fresh}
+M_{0,legal}^{fresh}.
\tag{1}
```

For the full fresh endpoint source, the retained parent-predictable divergence
is still present:

```math
M_0^{fresh}
=
D_AJ_{EP}^{S,new}
+D_AJ_{EP}^{S,ret}
+M_{0,stop}^{fresh}
+M_{0,legal}^{fresh}.
\tag{2}
```

Thus the full-source divergence form is not closed by stop/legal alone.  The
retained term must be either included in the routed current with its own
Carleson estimate, or paid by the return/reset branch ledger:

```math
\left|
\int_{\widehat P}H_w\,D_AJ_{EP}^{S,ret}\,g
\right|^2
\le
C\left(
StoppedReturnBV(P)
+CriticalWeightedResetBV(P)
+R_{\rm legal}(P)
\right)
\int_{\widehat P}H_w|g|^2 .
\tag{3}
```

with

```math
StoppedReturnBV(P)+CriticalWeightedResetBV(P)
\le
C_N(u_0)\mathcal R(P)+C R_{\rm legal}(P)
\tag{4}
```

proved from original parent-known history, before selected-tail readout.

## 2. Parent-known weight does not prove endpoint accretive testing

The endpoint testing consumer is

```math
\int_{\widehat P}
|D_A^*(H_wg)|^2\,d\mathcal R^{-1}
\le
C\int_{\widehat P}H_w|g|^2
+CStop(P)+CR_{\rm legal}(P).
\tag{5}
```

Parent-knownness of \(H_w\) prevents future-tail fitting, but it does not imply
`(5)`.  The formal integration by parts

```math
\int H_wM_0^{fresh}g
=
-\int J_{EP}^S\cdot D_A^*(H_wg)
+Stop_g+Legal_g
\tag{6}
```

only exposes the needed adjoint estimate.

The exact testing producer is

```text
MaterialAdjointAccretiveTestingData.A
```

or the sharper pair

```text
StoppedAdjointNondegenerateMean.A
+ MaterialAdjointTestingDefectCarleson.A.
```

It must construct accretive stopped tests from original same-material geometry:

```math
|\langle b_P\rangle_P|\ge c,
\qquad
\|b_P\|_{L^2(P,\mathcal R)}^2
\le
C\mathcal R(P)+CR_{\rm legal}(P),
\tag{7}
```

and

```math
\|T_S(b_P\nu^S)\|_{L^2(P,\mathcal R)}^2
\le
C\mathcal R(P)+Def_{test}(P)+CR_{\rm legal}(P),
\tag{8}
```

with

```math
\sum_{P\subseteq P_0}Def_{test}(P)
\le
C\mathcal R(P_0)+CR_{\rm legal}(P_0).
\tag{9}
```

Adjoint solvability alone is not enough, because it does not give the
nondegenerate mean or Carleson packing.

## 3. Routed-current square-packing still needs finite original routed energy

The stopped routed-current branch work reduces the current Carleson problem to
return/reset defects plus routed energy:

```math
Def_{\rm test}(P)
\le
C\,RetBV(P)+C\,ResetBV(P)+CR_{\rm legal}(P).
\tag{10}
```

The local branch assembly is useful only after

```math
RetBV(P)+ResetBV(P)
\le
C_N(u_0)\mathcal R(P)+CR_{\rm legal}(P).
\tag{11}
```

and after the routed derivative energy is finite:

```math
\mathcal E_{\rm route}(P)
=
\int_{\operatorname{Hist}(P)}
w\left(
|\partial_\sigma Z|^2
+|\operatorname{turnstile}_\sigma|^2
+|\operatorname{exchange}_\sigma|^2
+|\operatorname{covector}_\sigma|^2
\right)
\le
C_N(u_0)\mathcal R(P)+CR_{\rm legal}(P)+CStop(P).
\tag{12}
```

The local projection estimate shows that the routed objects are stable finite
parent-known projections of the full material derivative data.  It does not by
itself prove `(12)` unless the original-history service density that controls
those derivatives is already finite.

The noncircular theorem is therefore:

```text
OriginalHistoryRoutedCurrentEnergyFinite.A
```

equivalently

```text
RoutedCurrentEnergySubledgerOfOriginalHistory.A
/ GaugeReducedAnnularDerivativeDensityControl.A.
```

It must prove `(12)` from original material history without importing the
downstream full-clock conclusion.

## 4. Exact Gold live edge

The current Gold closure package is:

```text
ParentSubtractedDynamicEndpointCurrentRepresentation.A
+ retained return/reset branch payment for D_AJ_EP^{S,ret}
+ MaterialAdjointAccretiveTestingData.A
+ OriginalHistoryRoutedCurrentEnergyFinite.A
+ StoppedRoutedParticipationCurrentCarleson.A
```

Only after those are proved can the route consume:

```text
FreshAffineEndpointMassStoppedTestingReduction.A
-> FreshPositiveAffineEndpointMassKill.A
-> NativeBirthAffineEndpointThreading.A
-> SelectedDensityMartingaleSquareFunctionCarleson.A
-> finite selected first-ratio action
-> H^s continuation.
```

So the local silent-kernel/source-origin normal form remains support.  The live
Gold lock is now endpoint adjoint testing plus finite routed-current energy
from the original same-material history, with retained return/reset payment
kept explicit.
