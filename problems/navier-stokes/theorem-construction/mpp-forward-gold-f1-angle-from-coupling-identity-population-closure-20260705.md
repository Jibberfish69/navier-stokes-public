---
theorem_id: forward-gold-f1-angle-from-coupling-identity-population-closure-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / FROZEN FRONTIER F1 / the R2b population gate - closed by an exact identity
authorship: Claude (Fable), 2026-07-05. First unit under the frozen
  frontier. The R2b population gate asked whether the actual packet
  supplies non-degenerate predator triangles; the answer is an exact
  identity, not an estimate: the law of sines composed with T7's
  audited closed form makes every triangle angle a formula in the
  coupling modulus - so the cleared financed coupling floor IS an
  angle floor, and the paid pushforward makes the predator's shape
  the relay's shape. Physical story Thomas Birnie: the enemy that
  finances must show angle - a flat triangle carries no coupling, so
  a window that moved its quota has already displayed its geometry;
  the water cannot pay through a shape it does not have.
completion_truth: >-
  Closes the R2b predator-cone population gate on the financed
  packet/window family, pending Codex re-gate - the last open clause
  of frontier item F1 (R2a cleared; R2b scaling cleared; R2c scope).
  (THE IDENTITY - unconditional, about the actual Leray-projected
  helical coefficients) T7's audited closed form gives |G| =
  |s_k kappa_k + s_p kappa_p + s_q kappa_q| / (4 R_circ) for the
  actual NS triad coupling; the law of sines gives sin(A_kappa) =
  kappa/(2 R_circ) for the angle opposite side kappa; eliminating
  the circumradius:
    sin(A_kappa) = 2 kappa |G| / |s_k kappa_k + s_p kappa_p +
    s_q kappa_q|
  - EXACT, for every actual helical triad, each angle. The needed
  denominator control is already cleared both ways: |Sigma s kappa|
  <= kappa_k + kappa_p + kappa_q (triangle inequality, exact), and
  the sector citations floor it below by a side length (S_relay >= p,
  S_pred >= mu - cleared; the zero-set sharpening additionally
  places weighted-sum zeros ON the collinear boundary). (COROLLARY 1
  - THE FINANCED ANGULAR APPETITE IS A FORMULA) On any financed
  window, the MG-cleared G3-A floor gives |G_relay| >= J_min/C_amp
  (rows R1-R4 paid; C_amp = T_res C_Q E_res^{3/2}; J_min > 0
  definitional for the financed family by the cleared chapter-5
  threshold dichotomy - sub-financed windows are routed to the paid
  poor horn and never enter the delivered side). Substituting:
    sin(A_kappa) >= 2 kappa_min J_min / (C_amp (kappa_k + kappa_p +
    kappa_q))
  - a strictly positive EXACT floor on every angle of the financed
  relay triangle, with band ratios exact powers of two. The angular
  appetite phi_min, assumed as a family bound all campaign, is now
  DERIVED: sin(phi_min) = the displayed formula. The appetite
  pattern's second member (magnitude, ANGLE, gross, invariant) is
  paid at identity level. (COROLLARY 2 - POPULATION BY PERMUTATION)
  The paid same-monomial pushforward makes the predator/home
  admission measure the pushed relay monomial mass through the slot
  permutation (k,p,q) -> (k,q,p): the predator triangle IS the relay
  triangle with sides relabeled - same sides, same angles, same
  floor. Codex's objection ("a selected pair can sit near a
  collinear boundary; abstract partners do not participate") is
  answered exactly: the participating mass IS the relay's own paid
  monomial mass, and its shape IS the financed relay's shape, floored
  by Corollary 1. Bounded-shape hyperbolic predator triangles are
  POPULATED, with the pushed mass, whenever the financed relay
  exists. (CONCLUSION - F1's GATE) gamma_G >= GAMMA_ANGLE(phi_min)
  on the financed family at the packet/window level, with phi_min
  the displayed formula - the cleared telescoping floor now stands
  on a derived angle, not an assumed one; the R2b no-common-zero/
  population clause is CLOSED there. SCOPE, honest: the composition
  runs at the FCR/W1 packet-window level - exactly where R2c places
  the population object ("the continuum-to-chain architecture -
  already paid"); the lift of the ledger rows themselves to raw
  suitable weak solutions is frontier item F3 by the frozen list,
  not F1. FALSIFIABILITY CHECK (per F1's clause): nothing here
  weakens R2a's verbatim lift; no evidence of sector-sign
  non-preservation appeared; the gold lane stands. PAYMENT TYPE
  under the new law: one unconditional identity about actual
  helical coefficients + a closure of a frontier clause by
  composition of cleared results - not a reformulation. QUEUE: this
  note's Codex gate; then F1 is discharged (R2a + R2b-scaling +
  R2b-population + R2c) and the front moves to F2 parallel F3.
  Filed is not cleared. No closure claimed.
---

# F1: the angle is the coupling - the population gate closes

## 1. The identity

T7 (audited): |G| = |s_k kappa_k + s_p kappa_p + s_q kappa_q|/(4R).
Law of sines: sin(A_kappa) = kappa/(2R), each angle against its
opposite side. Eliminate R:

  sin(A_kappa) = 2 kappa |G| / |s_k kappa_k + s_p kappa_p + s_q kappa_q|

- exact for every actual NS helical triad. Denominator controlled
both ways by cleared facts: above by the side sum (triangle
inequality); below by a side length (sector citations; the zero-set
sharpening puts weighted-sum zeros on the collinear boundary).

## 2. Corollary 1: the financed angular appetite is a formula

On a financed window, the MG-cleared G3-A floor |G_relay| >=
J_min/C_amp (rows R1-R4 paid; J_min definitional by the cleared
threshold dichotomy) substitutes directly:

  sin(A_kappa) >= 2 kappa_min J_min / (C_amp (kappa_k + kappa_p + kappa_q)).

Every angle of the financed relay triangle carries this exact
positive floor; band ratios are exact powers of two. phi_min -
assumed all campaign as the angular-appetite bound - is now derived:
the enemy that finances must show angle, as an identity.

## 3. Corollary 2: population by permutation

The paid pushforward identifies the predator/home admission measure
with the pushed relay monomial mass under (k,p,q) -> (k,q,p): the
predator triangle is the relay triangle relabeled - same sides, same
angles, same floor. The participating mass is the relay's own paid
mass; its shape is the financed relay's shape. Bounded-shape
predator triangles are populated whenever the financed relay exists.
The availability-versus-participation objection is answered by
identity, not by measure theory.

## 4. Conclusion and scope

gamma_G >= GAMMA_ANGLE(phi_min) on the financed family at the
packet/window level, phi_min displayed - the R2b population clause
closes there, and with it F1's last open gate (R2a cleared; R2b
scaling cleared; R2c scope). Honest scope: the composition runs at
the FCR/W1 level, where R2c places the population object; lifting
the ledger rows to raw suitable weak solutions is F3's burden under
the frozen frontier. Falsifiability check: nothing here weakens the
verbatim lift; the gold lane stands.

## 5. Register

One unconditional identity + one frontier-clause closure by
composition of cleared results - payment, not reformulation, under
the new law. Queue: Codex gate on this note; then F1 discharged and
the front moves to F2 parallel F3. Filed is not cleared. No closure
claimed.
