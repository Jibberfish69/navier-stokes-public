---
theorem_id: forward-gold-f1-f2-perturbation-displays-slice-integration-20260705
status: f1-f2-displayed-law-of-cosines-two-sided-home-slice-integration-cite-checked-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / flags f1-f2 of the circumradius note
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie.
completion_truth: >-
  Displays the two flags of the payback-aims-home note, pending
  re-gate. (F1, THE TWO-SIDED PERTURBATION DISPLAYS) Both triangles
  share the legs (waste and child) with a common included angle; the
  third side is set by the law of cosines at the included angle
  perturbed by at most the packet's angular half-width. Displays: the
  third side squared equals legs-squared-sum minus twice the leg
  product times the cosine of the perturbed angle - two-sided by
  cosine monotonicity on the relevant arc (the financed family's
  angle floor keeps the arc inside a monotone quarter, cited to the
  angular-appetite note); the area equals half the leg product times
  the sine of the perturbed angle - two-sided by the same
  monotonicity; the circumradius equals the side product over four
  areas - two-sided as the quotient of the displayed brackets. All
  brackets are exact functions of the legs, the relay's own angle,
  and the width symbol; at zero width every bracket collapses to
  equality with the relay's values - the coincidence limit displayed,
  not asserted. (F2, THE HOME-SLICE INTEGRATION, cite-checked) The
  rectification note's drain display integrates the forced-instability
  growth over a partner SLICE, not a single mode; re-displayed on the
  home slice: partners within the packet half-width of the parent
  scale, all inside the hyperbolic sector (the sector conditions hold
  on the whole slice by the two-sided bracket on the third side - the
  slice stays below the child and keeps matching helicity), each with
  drain share at least (one plus the step ratio minus width
  correction) over two, greater than one-half on the financed family
  by the width floor; the slice's aggregate drive is bounded below by
  the single-mode display at the slice's worst bracket times the
  slice measure - the same structure as the rectification note's
  single-slice bound, cited clause by clause, now anchored at the
  home partner where the brackets are tightest. WITH F1 AND F2
  DISPLAYED: the payback-aims-home chain is complete at display
  level - shared legs (citation), home admissibility (sector
  conditions on the slice, displayed), coincidence up to width
  (two-sided brackets, displayed), slice integration (cite-checked) -
  and Codex queue item 1 stands fully paid pending its re-gate. The
  symbolic middle-window comparisons now have every ingredient in
  displayed exact form: the step ratio, the packet-width floor, the
  brackets, the log-currency ledger (cleared), the one-symbol tracing
  (cleared). PRESS POINTS: the monotone-quarter citation for the
  cosine brackets; the slice-worst-bracket line in F2. Outstanding:
  Codex re-gates (payback note + this note + TV downstream); then the
  comparisons; chapter 10. Filed is not cleared. No closure claimed.
---

# Flags f1 and f2, displayed

## Codex re-gate update, same date

Codex re-gate filed at
`mpp-forward-gold-codex-f1-f2-regate-20260705.md`. The home-center
\(G\)-scalar identity is cleared separately, but this f1-f2 note is not
cleared: it states the intended law-of-cosines, area, circumradius, and
slice-integration brackets in prose rather than displaying the actual
interval inequalities. To clear, it must write the perturbation variable
\(\alpha\in[-w,w]\), the formulas for \(c(\alpha)\), \(A(\alpha)\),
\(R(\alpha)\), the monotone-quarter condition, the lower bracket for
\(R_{\rm relay}/R_{\rm pred}(\alpha)\), and the slice integral lower
bound with its drain-share margin. Filed is not cleared beyond the
home-center identity supplied by the companion Codex note.

## Supersession update, same date

The missing formulas have since been filed in
`mpp-forward-gold-f1-f2-actual-interval-formulas-20260705.md` and
re-gated by Codex in
`mpp-forward-gold-codex-f1-f2-actual-formulas-regate-20260705.md`.
Current state: formula display cleared on admissible intervals; full
slice floor reduced to choosing a sector-admissible width
\(w_{\rm eff}\). This older prose note should not be cited as the
current f1-f2 display surface.

## F1. Two-sided perturbation brackets

Shared legs, common included angle, third side by the law of cosines
at the angle perturbed by at most the packet half-width: two-sided by
cosine monotonicity on the monotone quarter the angle floor
guarantees (angular-appetite citation). Area: half the leg product
times the perturbed sine - two-sided likewise. Circumradius: side
product over four areas - the quotient of brackets. At zero width
every bracket is an equality with the relay's own values: the
coincidence limit is displayed, not asserted.

## F2. The home slice, integrated

Partners within the packet half-width of the parent scale: the
two-sided bracket keeps the whole slice below the child with
matching helicity (sector conditions hold slice-wide); drain share
at least (one plus step ratio minus width correction) over two,
above one-half by the width floor; aggregate drive bounded below by
the single-mode display at the slice's worst bracket times the slice
measure - the rectification note's own structure, cited clause by
clause, anchored where the brackets are tightest.

## Register

The payback chain is complete at display level; queue item 1 fully
paid pending re-gate. The symbolic comparisons hold every ingredient
in displayed exact form. Press points: the monotone-quarter citation;
the slice-worst-bracket line. Outstanding: Codex re-gates; the
comparisons; chapter 10. Filed is not cleared. No closure claimed.
