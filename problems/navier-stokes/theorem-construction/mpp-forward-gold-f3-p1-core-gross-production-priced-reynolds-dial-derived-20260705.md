---
theorem_id: forward-gold-f3-p1-core-gross-production-priced-reynolds-dial-derived-20260705
status: codex-gated-p1-core-gross-pricing-accepted-p2-dial-sufficiency-open
created: 2026-07-05
problem: navier-stokes
route: forward-gold / FROZEN FRONTIER F3 / P1 core - the gross production row priced; the Reynolds dial derived
authorship: >-
  Derivation: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the price of moving gross record-work through a band is the square
  root of the penny-tax paid there, times the band's own steepness,
  times one number that belongs to the whole water - its endowment
  against its honey. The dial the tax collector needed is not chosen;
  it is read off: steeper bands are taxed more lightly per unit of
  work because their work is intrinsically more expensive to perform -
  the supercritical face is not evaded, it is WHERE THE DIAL LIVES,
  visible, priced, and carried to the worksheet that must decide if
  light taxes on steep bands still collect enough.
depends_on:
  - mpp-forward-gold-codex-f3-ef1-bridge-gate-20260705.md
  - mpp-forward-gold-codex-f3-p1-row-declaration-regate-20260705.md
  - mpp-forward-gold-f3-p1-row-declaration-modified-channel-gross-transcription-20260705.md
  - mpp-forward-gold-codex-f3-ga-tax-backing-to-weighted-gate-a-hall-reduction-20260705.md
  - mpp-forward-gold-codex-f3-two-coin-cell-pricing-regate-20260705.md
completion_truth: >-
  CODEX GATE SAME DATE:
  mpp-forward-gold-codex-f3-p1-core-gross-pricing-gate-20260705.
  Accepted at estimate level: the gross-production core is priced.
  Regated: rent aggregation, M_c versus M_i, Hall carry, dK^tax, and
  downstream sufficiency remain P2/F2/F4 burdens.
  Attacks P1's last open core - the gross-production pricing the
  declaration regate named - by the displayed chain, unconditional in
  the MPP finite-energy lane; and the price DELIVERS the dial P2's
  normalization demanded. (G1 - THE PRICING, DISPLAYED) For every
  channel c and retained cell (B, I), the declared gross row
  mu_{c,Q} := |nu^fg_{c,Q}|(cell) obeys
    mu_{c,Q} <= R_g kappa_c^{1/2} (beta_B)^{1/2},
    R_g := sqrt(2) C_B C_m C_S E_0 / nu,
  by the chain: Holder 1/3 + 1/2 + 1/6 pointwise
  (|u||grad u||Ptilde_c u|); Bernstein L^3 -> L^6 on the band
  (gain C_B kappa_c^{1/2}); the modified-channel bound ||Ptilde_c
  u||_{L^3} <= C_m ||u||_{L^3} (global; localization reach is the
  already-routed family-(c) bill); Cauchy-Schwarz in time; the
  quartic-L^3 reduction int_I ||u||^4_{L^3} <= (sup ||u||_2^2)
  int_I ||u||_6^2 <= 2 E_0 C_S^2 (E_0/nu) by interpolation
  ||u||_3^2 <= ||u||_2 ||u||_6 (the gated majorant-correction route),
  Sobolev, and the global energy inequality. ONE deterministic
  prefactor R_g - the lane's global Reynolds symbol E_0/nu times
  three named self-similar constants. The kappa_c^{1/2} supercritical
  face is RETAINED AND VISIBLE - priced, not evaded. (G2 - THE DIAL,
  DERIVED) Define M_c := R_g kappa_c^{1/2}. This is deterministic,
  predictable (readable before any episode from E_0, nu, and the band
  mark alone), dimensionally the local Reynolds/tower readout shape,
  and monotone up the tower. CANDIDATE for P2's dial normalization:
  M_lambda -> C M_c with the correct scale, viscosity, reserve, and
  window content now explicit. GUARD: if chapter 5's checked
  Reynolds/tower readout M_i differs at the use site, this M_c stands
  as a lawful derived substitute PROPOSAL and the consolidation
  becomes a comparison of two named symbols - the shape survives.
  (G3 - THE DIAGONAL TAX, TWO-COIN FORM) For any dial epsilon > 0
  take the predictable density theta_c := epsilon / M_c. Then
  EXACTLY: theta_c mu_{c,Q} <= epsilon (beta_B)^{1/2}, and by AM-GM
  theta_c mu_{c,Q} <= (epsilon^2 + beta_B)/2 - the tax demand is
  backed by HALF THE CELL'S OWN RESIDENCE BUDGET plus a per-cell
  rent epsilon^2/2. The rent is a count-priced row: at finite
  readout it aggregates to (epsilon^2/2) x (retained cell count),
  owned by the worksheet's residence-time rows (T_min floors the
  episode count). The diagonal weighted Gate-A Hall inequality holds
  unconditionally on the actual class in this two-coin form:
  theta mu <= beta/2 + rent. (G4 - WHAT THE PRICE MEANS UP THE
  TOWER, honest) theta_c ~ kappa_c^{-1/2}: high bands are taxed
  lightly per unit of gross work because their work is intrinsically
  expensive - the supercritical face now sits INSIDE the dial,
  displayed. Whether kappa^{-1/2}-weighted taxes collect enough for
  the record ladder (the E-f1 demand rows, the retained crossing,
  the c_0 consumption) is NOT decided here: that sufficiency is
  exactly the F2/F4 worksheet question plus P2's remaining carry
  edges and dK^tax - the burden moves to its owners with its price
  tag attached. NOT PAID, exactly: P2's carry (prefix/suffix) Hall
  edges on the chronological queue; dK^tax routing where the queue
  overdrafts; the chapter-5 consolidation M_c vs M_i; sufficiency
  of the kappa^{-1/2} tax downstream; the per-cell rent's worksheet
  row; F2/F4 positivity; F5. Constants C_B, C_m, C_S named
  self-similar; R_g, epsilon named dials; all quarantined to
  chapter 10. Payment type: one displayed unconditional pricing
  chain closing P1's named core at estimate level + the derived
  dial + the diagonal two-coin Hall row - estimate, structure, and
  normalization in one motion, on the actual solution class. Filed
  is not cleared. No closure claimed.
---

# F3/P1 core: the gross row priced - and the dial reads itself off

## Codex gate, same date

Codex gate filed:
`mpp-forward-gold-codex-f3-p1-core-gross-pricing-gate-20260705`.

The \(L^3,L^2,L^6\) Holder chain, Bernstein band gain, Cauchy-Schwarz
in time, and energy-class quartic \(L^3\) bound are accepted. The gross
row estimate
\[
  \mu_{c,Q}
  \le
  R_g\kappa_c^{1/2}\beta_B^{1/2}
\]
pays P1's core in its named estimate sense. The derived dial
\(M_c=R_g\kappa_c^{1/2}\) and the two-coin diagonal row are accepted as
P2 inputs. The \(\varepsilon^2\) rent, \(M_c\) versus \(M_i\), carry
edges, \(dK^{\rm tax}\), and downstream sufficiency stay open in
P2/F2/F4.

## 1. G1: the pricing chain, displayed

MPP finite-energy lane. For channel \(c\), retained cell
\((B,I)\), the declared gross row:

\[
  \mu_{c,Q}
  =
  \iint_{B\times I}\big|(u\cdot\nabla u)\cdot\widetilde P_cu\big|
  \le
  \int_I\|u\|_{L^3(B)}\,\|\nabla u\|_{L^2(B)}\,
  \|\widetilde P_cu\|_{L^6}\,dt
\]

(Holder \(\tfrac13+\tfrac12+\tfrac16=1\)). Bernstein on the band and
the modified-channel bound:

\[
  \|\widetilde P_cu\|_{L^6}
  \le
  C_B\,\kappa_c^{1/2}\,\|\widetilde P_cu\|_{L^3}
  \le
  C_BC_m\,\kappa_c^{1/2}\,\|u\|_{L^3(\mathbb R^3)}
\]

(global; the localization reach is the already-routed family-(c)
bill). Cauchy-Schwarz in time:

\[
  \mu_{c,Q}
  \le
  C_BC_m\,\kappa_c^{1/2}
  \Big(\int_I\|u\|^4_{L^3}\Big)^{1/2}
  \Big(\underbrace{\int_I\|\nabla u\|^2_{L^2(B)}}_{\beta_B/\nu}\Big)^{1/2}.
\]

The quartic-\(L^3\) reduction, by the gated majorant-correction route
(\(\|u\|_3^2\le\|u\|_2\|u\|_6\)), Sobolev
(\(\|u\|_6\le C_S\|\nabla u\|_2\)), and the global energy inequality
(\(\sup\|u\|_2^2\le2E_0\), \(\nu\!\int\!\|\nabla u\|_2^2\le E_0\)):

\[
  \int_I\|u\|^4_{L^3}
  \le
  \Big(\sup_I\|u\|_2^2\Big)\int_I\|u\|_6^2
  \le
  2E_0\cdot C_S^2\cdot\frac{E_0}{\nu}
  =
  \frac{2C_S^2E_0^2}{\nu}.
\]

Combining:

\[
  \boxed{\;
  \mu_{c,Q}
  \;\le\;
  R_g\,\kappa_c^{1/2}\,\beta_B^{1/2},
  \qquad
  R_g:=\frac{\sqrt2\,C_BC_mC_S\,E_0}{\nu}.
  \;}
\]

One deterministic prefactor - the lane's endowment against its honey.
The \(\kappa_c^{1/2}\) supercritical face is retained and visible:
priced, not evaded.

## 2. G2: the dial, derived

Define

\[
  M_c:=R_g\,\kappa_c^{1/2}.
\]

Deterministic; predictable before any episode (only \(E_0\), \(\nu\),
and the band mark enter); monotone up the tower; the
Reynolds/tower-readout shape with scale, viscosity, reserve, and
window content explicit. This is the candidate normalization
\(M_\lambda\leadsto CM_c\) that the two-coin regate demanded. GUARD:
if chapter 5's checked readout \(M_i\) differs at the use site, this
\(M_c\) stands as a derived substitute proposal and the consolidation
becomes a comparison of two named symbols.

## 3. G3: the diagonal tax in two-coin form

For any dial \(\varepsilon>0\), take the predictable density

\[
  \theta_c:=\frac{\varepsilon}{M_c}.
\]

Then exactly

\[
  \theta_c\,\mu_{c,Q}\le\varepsilon\,\beta_B^{1/2},
\]

and by AM-GM

\[
  \theta_c\,\mu_{c,Q}
  \le
  \frac{\varepsilon^2+\beta_B}{2}:
\]

the tax demand is backed by HALF THE CELL'S OWN RESIDENCE BUDGET plus
a per-cell rent \(\varepsilon^2/2\). At finite readout the rent
aggregates to \((\varepsilon^2/2)\times\)(retained cell count) - a
count-priced row owned by the worksheet's residence-time machinery
(\(T_{\min}\) floors the episode count). The DIAGONAL weighted Gate-A
Hall inequality holds unconditionally on the actual class in this
two-coin form.

## 4. G4: what the price means up the tower (honest)

\(\theta_c\sim\kappa_c^{-1/2}\): steep bands are taxed lightly per
unit of gross work because their work is intrinsically expensive -
the supercritical face sits inside the dial, displayed. Whether
\(\kappa^{-1/2}\)-weighted taxes COLLECT ENOUGH for the record ladder
(the E-f1 demand rows, the retained crossing, the \(c_0\)
consumption) is not decided here: that sufficiency is exactly the
F2/F4 worksheet question plus P2's remaining carry edges and
\(dK^{\rm tax}\). The burden moves to its owners with its price tag
attached.

## 5. Register

PAID (pending gate), unconditional on the lane: the pricing chain G1
with one deterministic prefactor; the derived predictable dial G2;
the diagonal two-coin Hall row G3.

NOT PAID, exactly: P2's carry (prefix/suffix) Hall edges on the
chronological queue; \(dK^{\rm tax}\) routing; the chapter-5
consolidation \(M_c\) vs \(M_i\); sufficiency of the
\(\kappa^{-1/2}\) tax downstream; the per-cell rent's worksheet row;
F2/F4 positivity; F5.

Constants \(C_B,C_m,C_S\) named self-similar; \(R_g,\varepsilon\)
named dials; all quarantined to chapter 10.

Filed is not cleared. No closure claimed.
