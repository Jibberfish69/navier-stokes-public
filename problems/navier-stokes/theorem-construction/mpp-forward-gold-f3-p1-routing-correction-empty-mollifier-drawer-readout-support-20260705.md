---
theorem_id: forward-gold-f3-p1-routing-correction-empty-mollifier-drawer-readout-support-20260705
status: codex-gated-empty-mollifier-accepted-e-f1-interface-regated
created: 2026-07-05
problem: navier-stokes
route: forward-gold / FROZEN FRONTIER F3 / P1 routing - same-day correction of E-d/E-e and E-f1
authorship: >-
  Derivation and same-day correction: Claude (Fable), 2026-07-05,
  against my own routing-execution note, per the kept law. Physical
  story Thomas Birnie: two of the drawers I pointed to were the wrong
  drawers. The mollifier drawer is empty on this side of the ledger -
  both rows are written on the same smoothed water, so no smoothing
  coin ever changes hands between them; the smoothing is paid where it
  was always paid, at the envelope and in the exact energy partition.
  And the high tower does not fade by size - it is confined by
  address: activity above the readout line cannot stand inside the
  line's own good ground; it lives on the line's shell and beyond,
  and beyond is Silver's country, reached by the road already built.
depends_on:
  - mpp-forward-gold-codex-f3-p1-routing-execution-regate-20260705.md
  - mpp-forward-gold-f3-p1-routing-execution-families-to-terminal-channels-20260705.md
  - mpp-forward-gold-codex-target1-regate-20260705.md
  - mpp-forward-gold-target1-items-helicity-derived-curl-diagonal-20260705.md
  - mpp-forward-gold-codex-srp-high-readout-tail-routing-reduction-20260705.md
  - mpp-forward-gold-codex-frontier-f3-allocation-closedness-envelope-20260705.md
completion_truth: >-
  SAME-DAY CORRECTION of the routing-execution note's two regated
  placements; the stale citations are withdrawn and the burdens placed
  on the CURRENT surfaces. (C-1: E-d/E-e - THE MOLLIFIER DRAWER IS
  EMPTY INSIDE P1) The R-a/R-b citation was stale and is WITHDRAWN.
  Restatement: at fixed prelimit data both rows of the transcription -
  the SRP row nu^SRP_{c,Q} and the declared row nu^fg_{c,Q} - are
  evaluated on the SAME prelimit solution; the transcription is the
  scalar identity nu^SRP = kappa_c nu^fg between rows on one solution,
  so NO mollifier commutator arises between them: families (d) and (e)
  do not exist inside P1's transcription. They never did - the
  declaration made this visible. Where smoothing genuinely enters
  P1's architecture is (i) the prelimit-to-limit step, which is the
  PAID historical-envelope allocation closedness (P3), and (ii) the
  consumption of the Duchon-Robert defect, where the corrected
  target-1 surface gives the ENERGY-side channel partition as an
  EXACT identity in the linear slot with ZERO commutator (accepted by
  the target-1 regate, pending only the displayed test-form formula
  and the L^3_loc convergence statement - a named open DISPLAY, not a
  remainder family). The old R-a/R-b families were HELICITY-side
  currency under the withdrawn weight-by-definition; the corrected
  surface DERIVES the helicity weight (curl diagonal with eigenvalue
  the signed channel wavenumber, zero remainder in that slot; the
  helicity multiplier itself still open) - and helicity currency is
  NOT consumed by P1: any downstream consumer requiring it routes
  through that corrected surface on its own lane. NET: drawers (d)
  and (e) are EMPTY in P1 - removed by the sharpness of the two rows
  on one solution plus the exactness of the energy partition, not by
  estimate. (C-2: E-f1 - THE HIGH TAIL ROUTES BY ADDRESS, NOT SIZE)
  The bare finite-readout/Silver citation is WITHDRAWN (the
  zero-support shortcut was regated; L^3-vanishing is the withdrawn
  false shape for a critical-weighted tail). Correct placement, per
  the high-readout reduction: R_{>L}^SRP routes by READOUT SUPPORT -
  (i) confinement: on a T4 good level, a nonzero limiting production
  measure from channels above L inside U_{L,eta} = {kappabar < L -
  eta} would be defect-carrying activity above the readout bound,
  contradicting the upper-semicontinuous envelope; hence the tail is
  supported in {kappabar >= L - eta} - the referee-grade write-up of
  this support lemma (prelimit diagonal, null-shell hypotheses) is a
  NAMED OPEN lemma, not paid here; (ii) the transition shell {L - eta
  <= kappabar < L + eta} is T4 good-level shell exhaustion - open for
  this exact measure family; (iii) the unbounded-readout complement
  {kappabar = infinity} is the chapter-2 Silver-facing terminal
  branch, consumed by the chapter-9 same-packet/Silver consumer or as
  failure witness - open interface. E-f1 is PLACED on its true
  surface, not paid. (P1's OPEN SET AFTER THIS CORRECTION, exactly)
  (1) gross-production pricing or orientation/window theorem (the
  kappa_c^{1/2} supercritical core); (2) the E-f1 chain: active-
  readout support lemma at referee grade + T4 exhaustion for this
  family + chapter-9 consumer for unbounded mass; (3) target-1's
  pending energy test-form display. Nothing else remains in P1's
  routing map: (a)/(f3) void by declaration, (b) allocated, (c)
  executed with exact moments, (d)/(e) empty, (f2) no-side-economy.
  P2, F2/F4, F5 unchanged. Payment type: same-day correction -
  withdrawal of two stale citations, one structural emptiness proof
  (rows on one solution admit no mollifier interface), one placement
  onto the current readout-support architecture. Filed is not
  cleared. No closure claimed.
---

## Codex gate, same date

Codex gate filed:
`mpp-forward-gold-codex-f3-p1-routing-correction-gate-20260705.md`.

Accepted: C-1 is correct. In the declared modified row, P1 compares two
rows evaluated on the same prelimit velocity, and the accepted identity
is \(d\nu^{\rm SRP}_{c,Q}=\kappa_c\,d\nu^{\rm fg}_{c,Q}\). No
mollifier or off-diagonal Target-1 residue exists inside the P1
transcription. E-d/E-e are empty for P1.

Accepted as placement only: C-2 correctly puts E-f1 on the
high-readout/readout-support architecture. Regated wording: the support
lemma is not merely prelimit diagonal plus null shells. The automatic
active-readout zero-support shortcut was already rejected. The open
interface is either SRP production readout comparison
\(\bar\kappa_{\rm SRP}\le\bar\kappa\) off priced tails, or diffuse
high-channel SRP total-variation domination routed to Gate-A tax,
boundary/locality, or Silver. After that, T4 shell exhaustion and the
chapter-9/Silver consumer can run.

No P1 clearance or MPP closure is claimed.

# F3/P1 routing correction: the empty mollifier drawer; the high tail's true address

## 1. C-1: E-d/E-e withdrawn and restated - the drawer is empty

WITHDRAWN: the citation of target-1's R-a/R-b families. That
description was superseded when the helicity side was reshaped under
the exact \(s|D|\) repair.

RESTATEMENT. At fixed prelimit data \((\varepsilon,L,\text{frame})\),
both rows of the transcription,

\[
  d\nu^{\rm SRP}_{c,Q}
  \qquad\text{and}\qquad
  d\nu^{\rm fg}_{c,Q},
\]

are evaluated on the SAME prelimit solution, and the transcription is
the scalar identity \(\nu^{\rm SRP}=\kappa_c\nu^{\rm fg}\) between
them. No mollifier commutator arises between two rows written on one
solution. Families (d) and (e) do not exist inside P1's
transcription; the declaration made this visible.

Where smoothing genuinely enters P1's architecture:

1. the prelimit-to-limit step - the PAID historical-envelope
   allocation closedness (P3);
2. the consumption of the Duchon-Robert defect - where the corrected
   target-1 surface gives the ENERGY-side channel partition as an
   exact identity in the linear slot with ZERO commutator (accepted;
   pending only the displayed test-form formula and \(L^3_{\rm loc}\)
   convergence statement - a named open DISPLAY, not a remainder).

The old R-a/R-b lived on the HELICITY side under the withdrawn
weight-by-definition. The corrected surface derives the helicity
weight - the curl is diagonal on a helical channel with eigenvalue
the signed channel wavenumber, zero remainder in that slot - with the
helicity multiplier still open. Helicity currency is not consumed by
P1; any consumer requiring it routes through that surface on its own
lane.

NET: drawers (d) and (e) are EMPTY in P1 - removed by structure, not
by estimate.

## 2. C-2: E-f1 withdrawn and placed - confinement by address

WITHDRAWN: the bare finite-readout/Silver citation. The zero-support
shortcut was regated, and \(L^3\)-vanishing is the withdrawn false
shape for a critical-weighted tail.

CORRECT PLACEMENT, per the high-readout reduction:

1. CONFINEMENT: on a T4 good level, a nonzero limiting production
   measure from channels above \(L\) inside
   \(U_{L,\eta}=\{\bar\kappa<L-\eta\}\) would be defect-carrying
   activity above the readout bound, contradicting the
   upper-semicontinuous envelope. The tail is supported in
   \(\{\bar\kappa\ge L-\eta\}\). The referee-grade write-up (prelimit
   diagonal, null-shell hypotheses) is a NAMED OPEN lemma - not paid
   here.
2. TRANSITION SHELL \(\{L-\eta\le\bar\kappa<L+\eta\}\): T4 good-level
   shell exhaustion - open for this exact measure family.
3. UNBOUNDED COMPLEMENT \(\{\bar\kappa=\infty\}\): the chapter-2
   Silver-facing terminal branch, consumed by the chapter-9
   same-packet/Silver consumer or as a failure witness - open
   interface.

E-f1 is placed on its true surface, not paid.

## 3. P1's routing map after this correction

CLOSED OR EMPTY: (a) void by declaration; (b) allocated (pressure
near/far, guarded); (c) executed with exact moments
\((1/15,\ 16/225)\); (d)/(e) empty by structure; (f2) no-side-economy;
(f3) void by declaration.

OPEN, exactly three:

1. gross-production pricing or orientation/window theorem - the
   \(\kappa_c^{1/2}\) supercritical core;
2. the E-f1 chain: active-readout support lemma at referee grade, T4
   shell exhaustion for this family, chapter-9 consumer for unbounded
   mass;
3. target-1's pending energy test-form display.

P2 (dial normalization, carry edges, \(dK^{\rm tax}\)), F2/F4
positivity, F5: unchanged.

Same-day correction filed per the kept law. Filed is not cleared. No
closure claimed.
