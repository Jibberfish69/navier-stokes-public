---
theorem_id: forward-gold-f4-opening-augmented-worksheet-row-inventory-20260705
status: codex-gated-inventory-accepted-w4-rent-loaded-product-row
created: 2026-07-05
problem: navier-stokes
route: forward-gold / FROZEN FRONTIER F4 / opening - the augmented worksheet frozen as a row inventory
authorship: >-
  Derivation: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the day of arithmetic gets one sheet of paper, written the night
  before. Every row on it was fought for somewhere in the record;
  every constant on it has one name and one home; and nothing may be
  added at the table. The sheet decides. This note writes the sheet.
depends_on:
  - mpp-forward-gold-codex-f3-p2-carry-family-audit-20260705.md
  - mpp-forward-gold-codex-frontier-f2-c0-floor-reduction-20260705.md
  - mpp-forward-gold-codex-chapter10-final-worksheet-rows-20260705.md
  - mpp-forward-gold-codex-f3-p2-rent-tower-gate-20260705.md
  - mpp-forward-gold-codex-f3-p2-m-dial-consolidation-20260705.md
  - mpp-forward-gold-codex-f1-angle-population-regate-20260705.md
completion_truth: >-
  F4's OPENING UNIT under the frozen order (F1 closed; F2 reduced onto
  this surface; F3 producer content exhausted per the carry-family
  audit). This note FREEZES the augmented certification worksheet as a
  row inventory with named sources - consolidation only: NO number is
  evaluated, NO positivity is claimed; per the lane law the arithmetic
  itself is the interval pass, which executes ON this sheet and
  nowhere else. THE ROWS: (W1) R_seed := 1/2 - E_seed > 0 with E_seed
  = (4 pi rho / tau_min) exp(D_req - gamma_G pi sqrt(3/(2 ln 2)));
  source: the chapter-10 final worksheet rows; gamma_G >=
  GAMMA_ANGLE(phi_min) with GAMMA_ANGLE(phi) = sin(phi/2)/(1 +
  2cos(phi/2)) and - NEW SINCE F1 - sin(phi_min) = (1/(2 c_T))
  kappa_min J_min / (C_amp Sigma_max), c_T = 1/(4 sqrt 2): the seed
  row's exponent is now fully derived, no assumed angle remains.
  (W2) R_prod := (1/2) Delta_G6a - K_off (1 - tau_min + Omega_R4) >
  0; source: same sheet; Omega_R4 <= 4 (L_X C_z + L_edge) drift_per /
  (v_mid T_min) per the drift reduction. (W3) the two window-length
  thresholds: |I| > 2 Q_W / (L_W R_seed) and |I| > ((2 Q_W / L_W)
  Delta_G6a + 2 K_off T*) / R_prod. (W4 - NEW) RENT_P2 row: the
  escrowed rent allowance, RENT_P2(total) <= RENT_P2(envelope) x
  N_cells with the displayed closed form (exact factors 4/3 and 2 +
  sqrt 2; ingredients nu, E_0, C_B, C_m, C_S, C_3, |I|, r,
  kappa_min, Qbar'), to be covered by the chapter-9 consumer's
  prefix-escrow allowance ALLOW_ch9 - the comparison TARGET is owned
  by the chapter-9 consumer reduction and must be displayed there
  before this row can be judged; flagged as the sheet's one
  incompletely-specified comparison. (W5 - NEW) tax-sufficiency row:
  the collected diagonal tax at density theta_c = epsilon/(2 N_ov
  M_c), M_c = R_g kappa_c^{1/2}, R_g = sqrt2 C_B C_m C_S E_0 / nu,
  against the record ladder's financed demand (J_min consumption per
  the cleared chapter-5 threshold dichotomy) - the kappa^{-1/2}
  sufficiency verdict. (W6) c_0 DELIVERY AND CONSUMPTION: per the F2
  reduction, c_0 > 0 is supplied by W1-W3 positivity as the usable
  retention margin; the retained queue consumes it as E_{j+1} <=
  ((1 - c_0)/2) E_j; the diffuse demand-row tower summability
  (E-f1's merged family) then follows by the weighted geometric
  crossing - W6 is a CONSEQUENCE row, not an independent comparison.
  (W7) domain guards: compact-band domain, T_min episode flooring,
  N_ov, N_band, the declared frame's b_lo/b_hi, and the finite-energy
  lane scope (Leray pressure; E_0 cap). THE CONSTANTS, one name one
  home: {rho, tau_min, D_req, Q_W, L_W, Delta_G6a and its
  ingredients, K_off, Omega_R4 (L_X, C_z, L_edge, drift_per, v_mid,
  T_min), T*, Lambda_off, X_max, S_max, G_e, C_per, chi_W} from the
  pre-F1 cleared record; {c_T, kappa_min, J_min, C_amp = T_res C_Q
  E_res^{3/2}, Sigma_max} from the F1 chain; {C_B, C_m, C_S, C_3,
  C_K1, N_ov, N_band, R_g, epsilon, r, Qbar', E_0, nu} from this
  session's F3 chain. FREEZE CLAUSE: no row and no constant may be
  added at the table; a later note that installs a genuinely new row
  must re-open F4's sheet by name and say why the freeze failed.
  FAILURE TAXONOMY carried verbatim from the F2 reduction: a W1/W2
  failure is a finite-row failure, not a failure of R2, F1
  population, the pushforward, or the crossing recurrence, unless a
  source audit shows misuse. WHAT THIS NOTE IS NOT: not the interval
  pass; not a positivity claim; not permission to compute outside
  the sheet. NEXT: (i) Codex gate on the inventory (completeness +
  the W4 target gap); (ii) the chapter-9 consumer displays
  ALLOW_ch9; (iii) the interval pass executes the sheet. Filed is
  not cleared. No closure claimed.
---

## Codex gate, same date

Codex gate filed:
`mpp-forward-gold-codex-f4-worksheet-rent-row-consolidation-20260705.md`.

The worksheet inventory is accepted as the current F4 row freeze, with
one correction to W4. The rent row is not an independently funded
chapter-9 allowance. Chapter 9 may use rent only as prefix escrow after
F4 supplies it. Therefore W4 is folded into the product reserve by the
rent-loaded row
\[
  R_{\rm prod}^{F4}
  :=
  \frac12\Delta_{\rm G6a}
  -
  K_{\rm off}(1-\tau_{\min}+\Omega_{\rm R4})
  -
  D_{P2}
  >0,
\]
with
\[
  {\rm RENT}_{P2}(|I|)\le D_{P2}|I|+B_{P2}.
\]

The product length threshold correspondingly carries \(+B_{P2}\) in
the numerator and \(R_{\rm prod}^{F4}\) in the denominator. No
positivity or interval arithmetic is claimed.

# F4 opening: the sheet, written the night before

## The rows

W1. SEED: \(R_{\rm seed}:=\tfrac12-E_{\rm seed}>0\),
\(E_{\rm seed}=\dfrac{4\pi\rho}{\tau_{\min}}
\exp\!\big(D_{\rm req}-\gamma_G\pi\sqrt{3/(2\ln2)}\big)\).
Source: chapter-10 final worksheet rows. Since F1:
\(\gamma_G\ge\Gamma_\angle(\phi_{\min})\),
\(\Gamma_\angle(\phi)=\dfrac{\sin(\phi/2)}{1+2\cos(\phi/2)}\),
\(\sin\phi_{\min}=\dfrac{1}{2c_T}\,
\dfrac{\kappa_{\min}J_{\min}}{C_{\rm amp}\Sigma_{\max}}\),
\(c_T=\dfrac1{4\sqrt2}\): the exponent is fully derived - no assumed
angle remains anywhere on the sheet.

W2. PRODUCT: \(R_{\rm prod}:=\tfrac12\Delta_{\rm G6a}
-K_{\rm off}\big(1-\tau_{\min}+\Omega_{\rm R4}\big)>0\), with
\(\Omega_{\rm R4}\le 4(L_XC_z+L_{\rm edge})\,{\rm drift}_{\rm per}
/(v_{\rm mid}T_{\min})\).

W3. LENGTHS: \(|I|>\dfrac{2Q_W}{L_WR_{\rm seed}}\) and
\(|I|>\dfrac{(2Q_W/L_W)\Delta_{\rm G6a}+2K_{\rm off}T_*}
{R_{\rm prod}}\).

W4. RENT (new): \({\rm RENT}_{P2}\) with its displayed closed form
(exact factors \(\tfrac43\), \(2+\sqrt2\); ingredients
\(\nu,E_0,C_B,C_m,C_S,C_3,|I|,r,\kappa_{\min},\bar Q'\)), to be
covered by the chapter-9 consumer's prefix-escrow allowance
\({\rm ALLOW}_{\rm ch9}\). The comparison TARGET is owned by the
chapter-9 consumer reduction and must be displayed there before this
row can be judged - the sheet's one incompletely-specified
comparison, flagged.

W5. TAX SUFFICIENCY (new): collected diagonal tax at
\(\theta_c=\varepsilon/(2N_{\rm ov}M_c)\),
\(M_c=R_g\kappa_c^{1/2}\), against the record ladder's financed
demand (\(J_{\min}\) consumption per the cleared threshold
dichotomy) - the \(\kappa^{-1/2}\) sufficiency verdict.

W6. \(c_0\) DELIVERY AND CONSUMPTION (consequence row): W1-W3
positivity supplies \(c_0>0\) as the usable retention margin (F2
reduction); the retained queue consumes it as
\(E_{j+1}\le\frac{1-c_0}{2}E_j\); the merged diffuse demand family
then sums by the weighted geometric crossing. Not an independent
comparison.

W7. GUARDS: compact-band domain; \(T_{\min}\) flooring;
\(N_{\rm ov},N_{\rm band}\); frame interval
\([b_{\rm lo},b_{\rm hi}]\); finite-energy lane scope (Leray
pressure, \(E_0\) cap).

## The constants - one name, one home

Pre-F1 cleared record: \(\rho,\tau_{\min},D_{\rm req},Q_W,L_W,
\Delta_{\rm G6a}\) (and its ingredients), \(K_{\rm off},
\Omega_{\rm R4}\) (via \(L_X,C_z,L_{\rm edge},{\rm drift}_{\rm per},
v_{\rm mid},T_{\min}\)), \(T_*,\Lambda_{\rm off},X_{\max},S_{\max},
G_e,C_{\rm per},\chi_W\).

F1 chain: \(c_T,\kappa_{\min},J_{\min},
C_{\rm amp}=T_{\rm res}C_QE_{\rm res}^{3/2},\Sigma_{\max}\).

This session's F3 chain: \(C_B,C_m,C_S,C_3,C_{K1},N_{\rm ov},
N_{\rm band},R_g,\varepsilon,r,\bar Q',E_0,\nu\).

## Freeze clause, taxonomy, and what this is not

FREEZE: no row and no constant may be added at the table; a later
note installing a genuinely new row must re-open the sheet BY NAME
and say why the freeze failed.

FAILURE TAXONOMY (carried verbatim): a W1/W2 failure is a finite-row
failure - not a failure of R2, F1 population, the pushforward, or
the crossing recurrence - unless a source audit shows misuse.

NOT the interval pass; NOT a positivity claim; NOT permission to
compute outside the sheet. Next: Codex gate on completeness and the
W4 target gap; the chapter-9 consumer displays
\({\rm ALLOW}_{\rm ch9}\); then the pass executes the sheet.

Filed is not cleared. No closure claimed.
