---
theorem_id: forward-gold-f4-w1-correction-suboctave-declaration-eta-half-rho-seven-20260706
status: filed-same-day-correction-pending-codex-gate
created: 2026-07-06
problem: navier-stokes
route: forward-gold / F4 Stage-3 workbook / W1 - correction of the octave declaration; the sub-octave law installed; rho collapses to seven
authorship: >-
  Derivation and same-day correction: Claude (Fable), 2026-07-06,
  against my own rung-declaration note, per the kept law. Physical
  story Thomas Birnie: I set the rungs an octave apart and the
  triangle refused the ladder - a child a full octave above its
  parent leaves no room for the catalyst's hand. The carpenter's fact
  survives corrected: the child stands a half-rung above the parent,
  the catalyst grips between them, and then the whole triangle fits
  in seven spans of its smallest side - the ratio was never a
  measurement, it is the shape of the grip.
depends_on:
  - mpp-forward-gold-codex-f4-w1-band-ratio-triangle-order-catch-20260706.md
  - mpp-forward-gold-f4-w1-rung-declaration-constructed-ladder-octave-20260705.md
  - mpp-forward-gold-codex-f4-w1-rung-declaration-gate-20260705.md
  - mpp-forward-gold-f4-w1-label-map-exitb-fails-exita-octave-rung-20260705.md
completion_truth: >-
  SAME-DAY CORRECTION of my rung-declaration note, whose exact-octave
  row Codex's triangle-order catch proved EMPTY (q < k + p < 2p on
  every ordered nondegenerate triad under the accepted label map -
  the octave declaration was geometrically incompatible; the reopened
  rho payment is acknowledged; the error is mine and the lesson is
  recorded: CHECK TRIANGLE CLOSURE BEFORE DECLARING A ROW SHAPE).
  (THE CORRECTED DECLARATION) Per the catch's own sufficient row law,
  the constructed W1 row declares the SUB-OCTAVE relative separation
  with the definite value eta_p = 1/2: on every constructed financed
  relay triad, q - p >= p/2. CONSISTENCY (the check the octave
  version failed): q - p >= p/2 requires k > q - p >= p/2, so k in
  (p/2, p) - nondegenerate, ordered k < p < q, triangle-compatible,
  and q >= 3p/2 keeps q > p strictly; the cleared sector facts
  (k <= q sign/order) hold a fortiori. (THE COLLAPSE, exact, per the
  catch's displayed algebra at eta_p = 1/2) k = kappa_min on the
  ordered branch; p <= k/eta_p = 2k; q < 2p <= 4k; hence
  Sigma = k + p + q <= (1 + 3/eta_p) kappa_min = 7 kappa_min, and
  |g_1| ~ q - p >= eta_p p >= eta_p kappa_min = kappa_min/2. So
    rho <= (1 + 3/eta_p)/(2 eta_p) = 7
  - A PURE NUMBER: rho^+ = 7, no interval input needed. And the
  angle chain's geometric part collapses with it: kappa_min/Sigma
  >= 1/7, so sin(phi_min) >= (2 sqrt 2 / 7) J_min/C_amp - the
  Sigma_max^+ and kappa_min^- symbols LEAVE the W1 input list
  (kappa_min survives only inside rho's derivation, not as a
  workbook input; the boxed W1 margin now reads Gamma^- from
  s^- = (2 sqrt2/7) J_min^-/C_amp^+ and the rho term becomes
  log(8 pi * 7 / tau^-) = log(56 pi) - log(tau^-) with tau^- =
  sqrt(1 - m_cap)/6). (W1 INPUT LIST AFTER THIS NOTE, if gated)
  PAID: D_req^+ = log(375/106), tau^- = sqrt(1-m_cap)/6, rho^+ = 7,
  Sigma/kappa collapse. REMAINING, exactly three: J_min^-, C_amp^+
  (homes: chapter-5 threshold dichotomy; G3-A MG rows, C_amp =
  T_res C_Q E_res^{3/2}), and m_cap (the tau gate's home). GUARDS:
  (g1) eta_p = 1/2 is a construction declaration - if any cleared
  gold-lane note pins a different triad shape for the financed
  relay, the declaration re-derives there with that note's eta_p
  and the pure numbers shift by the displayed formulas (the catch's
  algebra is eta-generic); (g2) the scope guard of the reopened
  rung gate carries over verbatim (constructed dyadic W1 row only;
  the adversary's fine-chop stays with the confinement lane); (g3)
  nondegeneracy: eta_p = 1/2 keeps the triad strictly inside the
  collinear boundary, consistent with the derived angle floor -
  the declared shape must SATISFY sin(phi_min) <= 1, i.e.
  (2 sqrt2/7) J_min/C_amp <= 1, which is a consistency row the
  workbook checks for free when J/C arrives. Payment type:
  same-day correction (error acknowledged, lesson recorded) + the
  catch's own sufficient law installed at a definite consistent
  value + two symbols leaving the input list by exact collapse.
  Filed is not cleared. No closure claimed.
---

# F4/W1 correction: the child stands a half-rung up; the triangle fits in seven spans

## 1. The error, acknowledged

Codex's triangle-order catch is correct: under the accepted label map
\(k<p<q\), every nondegenerate triad has \(q<k+p<2p\), so my
exact-octave declaration \(q\ge2p\) described an EMPTY row. The rho
payment is reopened; the error is mine. Lesson recorded: check
triangle closure before declaring a row shape.

## 2. The corrected declaration

Per the catch's sufficient row law, the constructed W1 row declares

\[
  q-p\ \ge\ \frac p2
  \qquad(\eta_p=1/2).
\]

CONSISTENCY: \(k>q-p\ge p/2\) puts \(k\in(p/2,p)\) - ordered,
nondegenerate, triangle-compatible; \(q\ge3p/2\); the cleared
sign/order facts hold a fortiori.

## 3. The collapse (the catch's algebra at \(\eta_p=1/2\))

\(k=\kappa_{\min}\) on the ordered branch; \(p\le2k\); \(q<4k\):

\[
  \Sigma=k+p+q\ \le\ 7\,\kappa_{\min},
  \qquad
  |g_1|\sim q-p\ \ge\ \frac{\kappa_{\min}}2,
\]

\[
  \boxed{\ \rho^+\ =\ \frac{1+3/\eta_p}{2\eta_p}\ =\ 7\ }
\]

- a pure number; no interval input. The angle chain's geometric part
collapses with it:

\[
  \frac{\kappa_{\min}}{\Sigma}\ \ge\ \frac17,
  \qquad
  \sin\phi_{\min}\ \ge\ \frac{2\sqrt2}{7}\,
  \frac{J_{\min}}{C_{\rm amp}},
\]

so \(\Sigma^+_{\max}\) and \(\kappa^-_{\min}\) LEAVE the W1 input
list, and the seed row's rho term becomes
\(\log(56\pi)-\log(\tau^-)\).

## 4. W1 ledger and guards

PAID (with this note, if gated): \(D^+_{\rm req}=\log(375/106)\);
\(\tau^-=\sqrt{1-m_{\rm cap}}/6\); \(\rho^+=7\); the
\(\Sigma/\kappa\) collapse. REMAINING, exactly three:
\(J^-_{\min}\), \(C^+_{\rm amp}=T_{\rm res}C_QE_{\rm res}^{3/2}\),
\(m_{\rm cap}\).

GUARDS: (g1) \(\eta_p=1/2\) is a construction declaration - a
cleared note pinning a different financed shape re-derives the pure
numbers by the catch's eta-generic algebra; (g2) the reopened
gate's scope guard carries verbatim (constructed dyadic row only;
the fine-chop adversary stays with the confinement lane); (g3) the
consistency row \(\sin\phi_{\min}\le1\), i.e.
\((2\sqrt2/7)J_{\min}/C_{\rm amp}\le1\), is checked for free when
\(J/C\) arrives.

Filed is not cleared. No closure claimed.
