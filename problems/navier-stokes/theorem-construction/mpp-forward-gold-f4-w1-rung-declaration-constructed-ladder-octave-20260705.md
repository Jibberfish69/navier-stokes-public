---
theorem_id: forward-gold-f4-w1-rung-declaration-constructed-ladder-octave-20260705
status: codex-gated-then-reopened-by-triangle-order-catch
created: 2026-07-05
problem: navier-stokes
route: forward-gold / F4 Stage-3 workbook / W1 rho - the rung is declared, the chopper is the other lane's enemy
authorship: >-
  Derivation: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  we built the ladder. Its rungs are octaves because we set them an
  octave apart - that is a carpenter's fact, not a theorem, and it
  needs no proof, only a check that nothing we built leans on a
  half-rung. The enemy who chops rungs fine is real - the toll note
  keeps his file open - but he is climbing HIS ladder, not ours, and
  he is fought by the impedance and transversality machinery, not by
  the seed row of our own construction.
depends_on:
  - mpp-forward-gold-codex-f4-w1-rho-exitb-helicity-pin-gate-20260705.md
  - mpp-forward-gold-f4-w1-label-map-exitb-fails-exita-octave-rung-20260705.md
  - mpp-forward-gold-octave-toll-closed-form-all-helicity-branches-20260705.md
  - mpp-forward-gold-return-race-elliptic-writeup-and-w1-20260705.md
  - mpp-forward-gold-f3-p1-row-declaration-modified-channel-gross-transcription-20260705.md
completion_truth: >-
  Resolves the octave-rung question by CATEGORY, then by DECLARATION,
  with the toll note's live adversary honestly separated. (R-1 - THE
  SOURCE READ, honest) The octave-toll note does NOT pin rungs as a
  theorem: on the catalyst-flipped mixed-helicity branch, fine
  chopping is a LIVE open front there (two half-octave steps reach
  1/2; fine chopping tends to 2^{-1/2}; the remaining atomic burden
  is the phase/reverse-instability budget, owned by the
  relay-drift/transversality machinery). So the rung pinning cannot
  be cited from the toll note - the candidate home I flagged does
  not carry it as a theorem. (R-2 - THE CATEGORY) But the question
  was mis-categoried: the toll note's fine-chopper is the ADVERSARY
  climbing HIS OWN ladder - an impedance/confinement-lane burden.
  W1's rho lives on the CONSTRUCTED gold-lane ladder - the financed
  family the campaign BUILDS (the FCR/W1 packet-window construction,
  the five-mode chain marching rung to rung). The frame of our own
  construction is ours to declare - the same lawfulness pattern as
  the gated modified-channel row declaration: one's own books, one
  motion, no second set. (R-3 - THE DECLARATION) The gold-lane
  ladder is DECLARED at octave rungs: on every constructed financed
  relay triad, parent and child sit on adjacent octave rungs,
  kappa_q = 2 kappa_p exactly at the marks (within the declared band
  width). Consequently q - p >= p >= kappa_min, eta_kappa^- = 1,
  and rho^+ = Sigma_max^+/(2 kappa_min^-) closes in sheet symbols -
  Exit A paid by construction declaration. (R-4 - LAWFULNESS CHECKS)
  (i) consistency: the F1 chain's gated corollary already carries
  "band ratios are exact powers of two" for the financed family -
  the declaration matches the gated record rather than amending it;
  (ii) no half-rung lean: the constructed chain's producer notes
  (return-race five-mode chain, FCR windows, the F1 population
  chain) all march rung to rung - no gold-lane consumer requires
  chopping our own ladder; GUARD: if any cleared gold-lane note is
  found to finance sub-octave relay steps, the declaration is
  revisited there and eta_kappa re-derives from that note's step
  ratio (the freeze protocol); (iii) the ADVERSARY's fine-chop
  remains exactly where the toll note left it - the mixed-helicity
  phase/reverse-instability budget, owned by the confinement lane's
  relay-drift/transversality machinery, NOT consumed or weakened
  here; W1 does not fight the chopper, it certifies our own seed.
  (W1 LEDGER after this note, if gated) PAID: D_req^+ =
  log(375/106), tau^- = sqrt(1-m_cap)/6, rho^+ =
  Sigma_max^+/(2 kappa_min^-) [by declaration + the conditional
  gate's accepted implication]. REMAINING: the F1-chain four
  (kappa_min^-, J_min^-, C_amp^+, Sigma_max^+) and m_cap - then the
  boxed W1 margin runs with every input in sheet symbols. Payment
  type: a source-read honest negative (the toll note does not carry
  the theorem) + a category correction + a construction declaration
  with three lawfulness checks and the reopening guard - the
  pattern the row declaration already gated. Filed is not cleared.
  No closure claimed.
---

# F4/W1: we built the ladder - the rung is a carpenter's fact

## Correction header, 2026-07-06

Codex filed
`mpp-forward-gold-codex-f4-w1-band-ratio-triangle-order-catch-20260706.md`.
The exact-octave row declared below is not compatible with the accepted
W1 label map \(k<p<q\) and the nondegenerate Fourier triangle
inequality \(q<k+p\). Together they imply \(q<2p\), so \(q\ge2p\)
would make the ordered W1 triad empty or collinear. The rho payment is
therefore reopened. The surviving route is a sub-octave relative
separation floor or a side-sum cap on the constructed W1 row.

## 1. R-1: the source, read honestly

The octave-toll note does NOT pin the rung as a theorem. On the
catalyst-flipped mixed-helicity branch, fine chopping is a live open
front there: two half-octave steps reach \(1/2\), fine chopping tends
to \(2^{-1/2}\), and the remaining atomic burden is the
phase/reverse-instability budget owned by the relay-drift/
transversality machinery. The candidate home I flagged does not
carry the pinning.

## 2. R-2: the category

The toll note's fine-chopper is the ADVERSARY, climbing his own
ladder - an impedance/confinement-lane burden. W1's \(\rho\) lives
on the CONSTRUCTED gold-lane ladder: the financed family the
campaign builds, whose five-mode chain marches rung to rung. The
frame of one's own construction is one's to declare - the same
lawfulness pattern as the gated modified-channel row declaration.

## 3. R-3: the declaration

The gold-lane ladder is DECLARED at octave rungs: on every
constructed financed relay triad, parent and child sit on adjacent
octave rungs. Hence

\[
  q-p\ \ge\ p\ \ge\ \kappa_{\min},
  \qquad
  \eta_\kappa^-=1,
\]

and by the conditional gate's accepted implication,

\[
  \boxed{\ \rho^+=\frac{\Sigma_{\max}^+}{2\,\kappa_{\min}^-}\ }
\]

- Exit A paid by construction declaration, in sheet symbols, no new
symbol spent.

## 4. R-4: lawfulness checks and guard

1. CONSISTENCY: the gated F1 corollary already carries "band ratios
   are exact powers of two" for the financed family - the
   declaration matches the gated record.
2. NO HALF-RUNG LEAN: the constructed chain's producers (return-race
   five-mode chain, FCR windows, the F1 population chain) march rung
   to rung; no gold-lane consumer requires chopping our own ladder.
   GUARD: if any cleared gold-lane note is found to finance
   sub-octave relay steps, the declaration is revisited there and
   \(\eta_\kappa\) re-derives from that note's step ratio.
3. THE ADVERSARY UNTOUCHED: the fine-chop front stays exactly where
   the toll note left it - the mixed-helicity phase/reverse-
   instability budget, owned by the confinement lane. W1 does not
   fight the chopper; it certifies our own seed.

## 5. W1 ledger

PAID (with this note, if gated): \(D^+_{\rm req}=\log(375/106)\),
\(\tau^-=\sqrt{1-m_{\rm cap}}/6\),
\(\rho^+=\Sigma^+_{\max}/(2\kappa^-_{\min})\). REMAINING: the
F1-chain four (\(\kappa^-_{\min},J^-_{\min},C^+_{\rm amp},
\Sigma^+_{\max}\)) and \(m_{\rm cap}\) - then the boxed W1 margin
runs with every input in sheet symbols.

Filed is not cleared. No closure claimed.
