---
theorem_id: forward-gold-f4-w1-triage-correction-joint-door-bar-20260706
status: filed-same-day-correction-pending-codex-gate
created: 2026-07-06
problem: navier-stokes
route: forward-gold / F4 W1 / correction to the rescue triage - separate eliminations stand, the JOINT door is live with an exact bar
authorship: >-
  Derivation and same-day correction: Claude (Fable), 2026-07-06,
  against my own rescue-triage note. Physical story Thomas Birnie: I
  closed two doors testing them one at a time - but a door that will
  not open alone may open when its neighbor is unlocked with it. The
  joint door has an exact bar across it: a new way of delivering the
  angle, clearing four-fifths of perfection, with the race constants
  taken at their true size. High, but a bar is not a wall.
depends_on:
  - mpp-forward-gold-codex-f4-w1-seed-demand-cost-no-rescue-audit-20260706.md
  - mpp-forward-gold-f4-w1-rescue-triage-two-targets-eliminated-20260706.md
  - mpp-forward-gold-codex-f4-w1-gamma-ceiling-gate-20260706.md
completion_truth: >-
  SAME-DAY CORRECTION of my triage note's overclaim, prompted by
  Codex's seed-demand audit (rho > 1/12 universal on the accepted
  row; tau_min <= 1/6; demand >= log(4 pi) > 2 regardless of
  constants; current angle ceiling < 2 - target 2 closed as
  constant-sharpening, all repairs structural). THE OVERCLAIM: my
  T-3 elimination ("even gamma_G = 1 loses") was computed against
  the workbook floor that SPENDS rho^+ = 7; with rho sharpened
  toward its universal floor 1/12 and delta_m toward 1, the
  best-case demand is D_req + log(8 pi rho / tau) -> log(375/106) +
  log(2 pi / 3) + log 6 (exact best-constant form), and a PERFECT
  angle (gamma_G = 1, LHS = pi sqrt(3/(2 ln 2))) PASSES that
  best-case demand. So T-2 and T-3 are eliminated SEPARATELY (each
  alone, at the other's conservative spending) but the JOINT repair
  is arithmetically live. THE CAVEAT THAT KEEPS THE TRIAGE'S
  CONCLUSION USEFUL: within the CURRENT F1 angle-delivery mechanism
  the certificate routes through arcsin, capping phi at pi/2 and
  Gamma^- at (2 - sqrt2)/2 < 1/3 - and Codex's audit shows angle
  side < 2 < demand floor log(4 pi) REGARDLESS of constants: the
  joint door therefore REQUIRES A NEW ANGLE-DELIVERY MECHANISM (not
  a sharpening of the current one), making it structural - route
  (2) of Codex's list - exactly as all four surviving repairs are
  structural. THE JOINT DOOR'S EXACT BAR, displayed: a new
  mechanism delivering a certified floor gamma_G >= gamma* rescues
  the current row structure with best constants iff
    gamma* > [ log(375/106) + log(2 pi/3) + log 6 ]
             / [ pi sqrt(3/(2 ln 2)) ]
  (exact closed form; numerator = log(4500 pi/106) hmm displayed
  unsimplified to keep homes visible: log(375/106) + log(2 pi/3) +
  log 6 = log(375 * 2 pi * 6 / (106 * 3)) = log(4500 pi / 318) =
  log(750 pi / 53)). BAR = log(750 pi/53) / (pi sqrt(3/(2 ln 2))).
  By the audit's own crude style: numerator > 3 (750 pi/53 > 44 >
  e^3 since e^3 < 21... no: e^3 < 20.1 < 44 - yes numerator >
  3); wait 750*3/53 > 42, e^3 < 20.09, so numerator > 3; and
  denominator < 33/7 < 5; also numerator < log(45) + ... kept as
  the exact form - the note displays the bar and does NOT resolve
  its decimal size beyond the audit-style bracket 3/5 < BAR < 1
  (numerator in (3, 4): 750 pi/53 < 45 gives numerator < log 45 <
  4 since e^4 > 54 > 45; denominator in (4, 5): pi sqrt(3/(2 ln2))
  > 3 sqrt(2)... kept crude: denominator > 4 via pi > 3 and
  sqrt(3/(2 ln 2)) > sqrt(3/1.4) > 1.46 > 4/3 gives > 4). So the
  BAR sits in the exact bracket (3/5, 1): the new mechanism must
  deliver gamma_G of at least roughly three-fifths and possibly
  up to near-perfection depending on the sharpened constants -
  concrete, high, not a wall. CONSEQUENCE: the four structural
  doors stand as Codex listed, with door (2) now carrying its
  exact bar and its constant-sharpening companions (rho^+ toward
  1/12, delta_m toward 1) as prerequisites. My triage's
  door-pricing for accumulation (N >= 9 at perfection, N >= 81
  at the current ceiling) is UNAFFECTED. Payment type: same-day
  correction of a separate-vs-joint overclaim + one exact bar
  display in the audit's own lane-lawful style. Filed is not
  cleared. No closure claimed.
---

# F4/W1 triage correction: the joint door is live, and its bar is exact

## 1. The overclaim, corrected

My T-3 elimination tested the perfect angle against the workbook
floor that spends \(\rho^+=7\). Codex's audit gives the universal
floor \(\rho>1/12\) - and with \(\rho\) sharpened toward it and
\(\delta_m\to1\), the best-case demand is

\[
  \log\frac{375}{106}+\log\frac{2\pi}{3}+\log6
  =
  \log\frac{750\,\pi}{53},
\]

which a PERFECT angle (\(\gamma_G=1\),
LHS \(=\pi\sqrt{3/(2\ln2)}\)) beats. T-2 and T-3 are eliminated
SEPARATELY; the JOINT repair is arithmetically live.

## 2. The caveat that keeps the triage useful

Within the CURRENT F1 delivery the certificate routes through
\(\arcsin\), capping \(\Gamma^-\) at \((2-\sqrt2)/2<1/3\) - and the
audit shows angle side \(<2<\log(4\pi)\le\) demand REGARDLESS of
constants. The joint door therefore requires a NEW angle-delivery
mechanism - structural, Codex's route (2) - not a sharpening of the
current one.

## 3. The bar, exact

A new mechanism with certified floor \(\gamma_G\ge\gamma_*\) rescues
the current row structure with best constants iff

\[
  \boxed{\
  \gamma_*
  \ >\
  \frac{\log\!\big(750\,\pi/53\big)}
       {\pi\sqrt{3/(2\ln2)}}
  \ }
\]

- exact closed form, sitting in the crude bracket \((3/5,\,1)\) by
the audit's own style (numerator in \((3,4)\): \(e^3<21<750\pi/53
<45<e^4\); denominator in \((4,5)\)). The new mechanism must deliver
at least roughly three-fifths of perfection, with the sharpened
\(\rho^+\) (toward \(1/12\)) and \(\delta_m\) (toward 1) as
prerequisites. High - but a bar, not a wall.

## 4. The doors after this correction

Codex's four structural doors stand: (1) LongAccumulationRow (my
pricing N >= 9 at perfection, N >= 81 at the current ceiling,
unaffected); (2) new angle delivery with the exact bar above + its
constant prerequisites; (3) seed-demand restructure (the
\(1/\tau_{\min}\) or return-race cost not charged in this row);
(4) W1 architecture reopening.

Filed is not cleared. No closure claimed.
