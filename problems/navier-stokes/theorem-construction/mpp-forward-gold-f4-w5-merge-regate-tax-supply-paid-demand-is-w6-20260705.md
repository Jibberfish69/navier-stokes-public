---
theorem_id: forward-gold-f4-w5-merge-regate-tax-supply-paid-demand-is-w6-20260705
status: filed-pending-codex-gate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / FROZEN FRONTIER F4 / W5 named re-gate - the sufficiency row merges; supply is paid, demand is W6
authorship: >-
  Derivation: Claude (Fable), 2026-07-05. Named sheet re-gate per the
  constant-table audit's option 2. Physical story Thomas Birnie: the
  collector's book was double-counted on the sheet. That the tax is
  levied and backed - that is paid, with the marginal rate a number
  the sheet already owns. Whether the town THRIVES on what the tax
  leaves behind was never a second levy: it is the same question the
  seed and product rows were always asking, and the crossing answers
  it with c_0. One question, one row; the ghost row is struck, and
  what it owed the sheet is written where it always lived.
depends_on:
  - mpp-forward-gold-codex-f4-constant-table-completion-audit-20260705.md
  - mpp-forward-gold-codex-f3-ga-tax-backing-to-weighted-gate-a-hall-reduction-20260705.md
  - mpp-forward-gold-codex-frontier-f2-c0-floor-reduction-20260705.md
  - mpp-forward-gold-codex-f3-p2-rent-tower-gate-20260705.md
  - mpp-forward-gold-codex-f3-p2-m-dial-consolidation-20260705.md
  - mpp-forward-gold-f4-opening-augmented-worksheet-row-inventory-20260705.md
completion_truth: >-
  Executes the constant-table audit's demanded next unit: the W5
  merge/strike re-gate, BY NAME, against the installed sources. (M1 -
  W5's SUPPLY HALF IS PAID, WITH A MARGINAL FLOOR THE SHEET OWNS)
  The positive predictable backed tax row exists: theta_c =
  epsilon/(2 N_ov M_c), M_c = R_g kappa_c^{1/2} (M-dial
  consolidation, gated); backing theta_c mu <= beta/2 + escrowed rent
  (rent-tower gate, prefix/escrow form). On MARGINAL cells - the band
  floor kappa_c = kappa_min - the density is the FIXED positive
  formula theta_marg := epsilon/(2 N_ov R_g kappa_min^{1/2}), every
  symbol already on the frozen sheet: the "genuine fixed lower tax
  density on marginal cells" that the P2 reduction demanded EXISTS as
  a formula row, interval-ready from (epsilon, N_ov, R_g, kappa_min).
  (M2 - W5's DEMAND HALF WAS NEVER A SEPARATE ROW, source audit) The
  phrase "sufficiency of the kappa^{-1/2} tax downstream" entered the
  sheet through my own P1-core register paraphrasing the P2
  reduction. The P2 reduction itself rules: "A uniform lower floor
  for attenuation is not part of F3 P2; it is the frozen-frontier
  F2/F4 problem, already reduced to Chapter-10 positivity." And the
  F2 reduction rules: once R_seed > 0 and R_prod > 0 (with lengths),
  the usable retention margin SUPPLIES c_0, the retained queue
  consumes it, and "no extra recurrence theorem remains." AUDIT OF
  CONSUMERS: no installed note consumes an inequality of the form
  "collected diagonal tax minus financed record demand > 0" in one
  currency - the record demand localizes through ADMISSION (A^w_ret,
  the gated collapse), not through tax; the chapter-9 queue treats
  tax as an OUTFLOW with its own compensator machinery (more tax is
  a queue cost, not a queue need); the diffuse E-f1 family routes
  through the crossing, i.e., through c_0 = W6. The W5 comparison as
  named in my inventory has NO consumer on the page. (M3 - THE
  RE-GATE) Therefore, by name and under the freeze clause: W5 as an
  independent comparison row is STRUCK from the sheet; in its place
  (i) W5' := the marginal-floor DISPLAY theta_marg =
  epsilon/(2 N_ov R_g kappa_min^{1/2}) - a formula row with
  interval inputs already on the sheet, feeding wherever chapter-5's
  marginal Gate-A argument cites a fixed density; and (ii) the
  sufficiency SEMANTICS are merged into the existing chain W1-W3
  (supply c_0) -> W6 (consume c_0; diffuse family sums) - one
  question, one row, no double count. The sheet after this re-gate:
  W1, W2^{F4}, W3, W5' (display), W6 (consequence), W7 (guards) -
  every row now formula-complete; the blocker is cleared and the
  interval workbook's input list is closed: certify intervals for
  the W1 chain (rho, tau_min, D_req, kappa_min, J_min, C_amp,
  Sigma_max), the W2^{F4} chain (Delta_G6a, Q_W, X_max, S_max, G_e,
  tau_min, the R4 drift six, A_P2^max, N_time, B_time, B_bdry), and
  the shared (epsilon, N_ov, R_g, E_0, nu, r, Qbar' surrogate).
  GUARD, falsifiability-typed: if a later note installs a genuine
  same-currency consumer of "collected tax >= financed demand," W5
  re-opens BY NAME with that consumer's row - the freeze clause's
  re-opening protocol applies; nothing currently filed does so.
  WHAT THIS IS NOT: not interval arithmetic; not a positivity claim;
  not a weakening of the tax row (supply stands as gated). Payment
  type: a named sheet re-gate demanded by the audit - source-checked
  strike-and-merge with a formula replacement and a re-opening
  protocol. Filed is not cleared. No closure claimed.
---

# F4/W5 re-gate: one question, one row - the ghost row is struck

## 1. M1: the supply half is paid, and the marginal floor is a sheet formula

The positive predictable backed tax row exists on the record:
\(\theta_c=\varepsilon/(2N_{\rm ov}M_c)\) with
\(M_c=R_g\kappa_c^{1/2}\) (M-dial consolidation, gated), backed as
\(\theta_c\mu\le\beta/2+\)escrowed rent (rent-tower gate). On
marginal cells (\(\kappa_c=\kappa_{\min}\)):

\[
  \theta_{\rm marg}
  :=
  \frac{\varepsilon}{2N_{\rm ov}R_g\,\kappa_{\min}^{1/2}}
\]

- a FIXED positive formula in symbols the frozen sheet already owns.
The P2 reduction's demanded "genuine fixed lower tax density on
marginal cells" exists as a formula row, interval-ready.

## 2. M2: the demand half was never a separate row (source audit)

The phrase "sufficiency of the \(\kappa^{-1/2}\) tax downstream"
entered the sheet through my own P1-core register, paraphrasing the
P2 reduction. The sources rule:

1. P2 reduction: "A uniform lower floor for attenuation is not part
   of F3 P2; it is the frozen-frontier F2/F4 problem, already
   reduced to Chapter-10 positivity."
2. F2 reduction: once \(R_{\rm seed}>0\) and \(R_{\rm prod}>0\)
   (with lengths), the usable retention margin SUPPLIES \(c_0\); the
   retained queue consumes it; "no extra recurrence theorem remains."

CONSUMER AUDIT: no installed note consumes "collected diagonal tax
\(-\) financed record demand \(>0\)" in one currency. The record
demand localizes through ADMISSION (the gated collapse:
\(\Lambda_{\rm record}\le A^w_{\rm ret}+\) routed), not through tax.
The chapter-9 queue treats tax as an OUTFLOW with its own
compensator - more tax is a queue cost, not a queue need. The
diffuse E-f1 family routes through the crossing, i.e., through
\(c_0\) = W6. The W5 comparison, as I named it in the inventory, has
no consumer on the page.

## 3. M3: the re-gate

By name, under the freeze clause:

1. W5 as an independent comparison row is STRUCK.
2. In its place: \(W5'\) := the marginal-floor display
   \(\theta_{\rm marg}=\varepsilon/(2N_{\rm ov}R_g\kappa_{\min}^{1/2})\)
   - a formula row, interval inputs already on the sheet, feeding
   wherever chapter-5's marginal Gate-A argument cites a fixed
   density.
3. The sufficiency SEMANTICS merge into the existing chain:
   W1-W3 supply \(c_0\); W6 consumes it and sums the diffuse family.
   One question, one row, no double count.

THE SHEET AFTER THIS RE-GATE: W1, W2\(^{F4}\), W3, W5' (display),
W6 (consequence), W7 (guards) - every row formula-complete. The
blocker is cleared; the interval workbook's input list closes:

- W1 chain: \(\rho,\tau_{\min},D_{\rm req},\kappa_{\min},J_{\min},
  C_{\rm amp},\Sigma_{\max}\);
- W2\(^{F4}\) chain: \(\Delta_{\rm G6a},Q_W,X_{\max},S_{\max},
  G_{\mathfrak e},\tau_{\min}\), the R4 drift six,
  \(A_{P2}^{\max},N_{\rm time},B_{\rm time},B_{\rm bdry}\);
- shared: \(\varepsilon,N_{\rm ov},R_g,E_0,\nu,r\), the \(\bar Q'\)
  entrance surrogate.

## 4. Guard and register

GUARD (falsifiability-typed): if a later note installs a genuine
same-currency consumer of "collected tax \(\ge\) financed demand,"
W5 re-opens BY NAME with that consumer's row, per the freeze
clause's re-opening protocol. Nothing currently filed does so.

NOT interval arithmetic; NOT a positivity claim; NOT a weakening of
the tax row - the supply stands as gated. Next: Codex gate on this
re-gate; then the Stage-3 interval workbook runs on W1, W2\(^{F4}\),
W3, W5', W7.

Filed is not cleared. No closure claimed.
