---
theorem_id: forward-gold-factorwise-floor-supersedes-corner-20260705
status: codex-regated-factorwise-shape-accepted-factor-monotonicity-not-cleared
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / the floor without the corner
authorship: Claude (Fable), 2026-07-05. Codex's hold on the angle row
  is accepted - the mixed signs are real - and answered by removing
  the need: a product of positive factors is floored by the product
  of their separate floors, and each factor alone is simple. Physical
  story Thomas Birnie: when the fences tangle, price each fence alone
  - the enemy must clear every one of them anyway.
completion_truth: >-
  Supersedes the coupled-corner hunt with FACTORWISE FLOORS, pending
  re-gate; the margin single-crossing claim is WITHDRAWN as
  unnecessary. (THE MOVE) gamma_home is a product of three POSITIVE
  factors; a product of positive factors is bounded below by the
  product of the factors' SEPARATE minima over the closed family - a
  valid (not tight) lower bound requiring NO joint-corner claim, no
  full mixed derivative, no membership subtlety: each factor is
  minimized alone, and EACH FACTOR ALONE is coordinatewise monotone
  with displayed directions (the side-sum factor rises with the
  bracketed third side - law-of-cosines monotone in both angle and
  width; the ratio factor falls as the bracket widens - same display;
  the sine-ratio factor rises with the angle and falls with the width
  - the displayed derivative): each factor's worst end is a DISPLAYED
  point of the closed family, per factor, with no coupling issue
  because nothing joint is claimed. Likewise sigma_min and the seed
  term are floored by their own displayed worst ends. THE RESULT:
  gamma_home >= gamma_LB := the product of the three factor-minima -
  every piece an exact closed form at a displayed end - and the
  decisive pair runs with gamma_LB in the exponent: AT MOST the same
  four evaluations as before (the margin endpoints survive only
  inside individual factor-minima where that factor's own margin
  direction is displayed), with ALL corner-legitimacy questions gone.
  (HONESTY ABOUT TIGHTNESS) The factorwise floor is weaker than the
  true infimum: the separate minima may not be simultaneously
  attainable. Consequence, stated in advance as the fourth honest
  exit: if the decisive pair FAILS at gamma_LB, that does NOT mean
  the true comparison fails - the tighter coupled-corner analysis
  (Codex's full-derivative route) or the interval pass on the true
  infimum becomes the fallback, and the failure of the loose bound is
  only a signpost, not a verdict. If the pair PASSES at gamma_LB, the
  pass is final - a valid floor suffices for a one-sided comparison.
  The asymmetry is the move's whole logic: we only ever needed one
  side. (WITHDRAWN) The margin single-crossing claim and the
  sequential angle-row completion - both superseded; Codex's holds on
  them are mooted rather than answered, recorded as such. PRESS
  POINTS: the per-factor monotonicity table (three factors, each
  direction cited to its existing display - no new derivatives); the
  fourth-exit statement (loose-bound failure is a signpost, not a
  verdict - one sentence, load-bearing for honesty). QUEUE: this
  note's re-gate; THE FACTORWISE EVALUATION (the final symbolic unit,
  now corner-free); sheet-2 expansion; chapter 10. Filed is not
  cleared. No closure claimed.
---

# The floor without the corner

## Codex re-gate header

Codex re-gate installed in
`mpp-forward-gold-codex-factorwise-floor-regate-20260705.md`. The
factorwise lower-bound strategy is accepted: a product of positive
factors is bounded below by the product of their separate minima. The
endpoint-only monotonicity table is not cleared. In particular
\[
F_2(\phi,w)=\frac{c(\phi)}{c(\phi+w)}
\]
has
\[
\partial_\phi\log F_2
=kq\left[
\frac{\sin\phi}{c(\phi)^2}
-
\frac{\sin(\phi+w)}{c(\phi+w)^2}
\right],
\]
and the derivative of \(h(\beta)=\sin\beta/c(\beta)^2\) changes sign at
\[
\cos\beta=\frac{2kq}{k^2+q^2}.
\]
Thus separate factor minimization is still owed; the route is a valid
strict reduction, not a clearance.

## 1. The move

A product of positive factors is floored by the product of their
separate minima. Each of gamma's three factors alone is
coordinatewise monotone with already-displayed directions; each
factor's worst end is a displayed point of the closed family. No
joint corner, no mixed derivative, no membership question - nothing
joint is claimed. When the fences tangle, price each fence alone:
the enemy must clear every one of them anyway.

## 2. The floor and the pair

  gamma_home >= gamma_LB = (min F1) x (min F2) x (min F3),

each minimum an exact closed form at its own displayed end. The
decisive pair runs with gamma_LB in the exponent - the same at-most-
four evaluations, corner-free.

## 3. The fourth exit, stated in advance

The factorwise floor is not tight. If the pair FAILS at gamma_LB,
that is a signpost, not a verdict - the coupled-corner analysis or
the interval pass on the true infimum is the fallback. If the pair
PASSES, it is final: a valid floor suffices for a one-sided
comparison. We only ever needed one side.

## 4. Withdrawn

The margin single-crossing claim and the sequential angle completion
are superseded, their holds mooted. Recorded, not hidden.

## 5. Register

Factorwise floor filed; press points: the per-factor direction table
(cited, no new derivatives); the fourth-exit sentence. Queue:
re-gate; THE FACTORWISE EVALUATION; sheet-2 expansion; chapter 10.
Filed is not cleared. No closure claimed.
