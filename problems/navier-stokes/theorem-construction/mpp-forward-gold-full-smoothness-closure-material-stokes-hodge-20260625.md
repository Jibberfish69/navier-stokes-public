---
theorem_id: forward-gold-full-smoothness-closure-material-stokes-hodge-20260625
status: attempted-full-closure-demoted-log-weighted-exchange-gap-open
logical_landing_node: original_smooth_data_material_history_no_finite_breakdown
edge_effect: >-
  Demotes the previous full smoothness closure claim.  The material
  Stokes/Hodge shell correctly removes the fixed-shell pressure/constraint
  commutator, but the attempted passage from top-strain logarithmic storage to
  finite full shell pump was circular.  The derivative of
  B_j=e_j log(lambda/(delta 2^{2j}))_+ contains the log-weighted same-shell
  exchange (m_j-d_j)L_j.  Thus the full finite-breakdown contradiction remains
  conditional on proving finite positive log-weighted same-shell exchange.
parents:
  - problems/navier-stokes/live-theorem-edge.yaml
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-material-stokes-hodge-shell-clock-closure-20260625.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-top-strain-storage-vs-moving-pump-alignment-check-20260625.md
  - problems/navier-stokes/theorem-construction/mpp-material-clock-to-fixed-hs-continuation-readout-20260623.md
  - problems/navier-stokes/theorem-construction/mpp-original-smooth-data-material-history-no-finite-breakdown-20260623.md
---

# Full Smoothness Closure Audit After Material Stokes/Hodge Shell Reduction

Date: 2026-06-25

## 0. Corrected status

This file is not a completed proof of smoothness.

The valid reduction is:

```math
\text{fixed-shell pressure/constraint commutator}
\Longrightarrow
\text{native material Hodge-Stokes shell motion}.
\tag{FSCA.1}
```

The invalid step was:

```math
\text{native shell motion}
\Longrightarrow
\text{finite full material clock}
\tag{FSCA.2}
```

without paying the log-weighted same-shell exchange generated by the
top-strain storage.

## 1. What remains true

For the transported material packet

```math
\partial_t v+A^\top\nabla_aq
=
\nu\,\operatorname{div}_a(G\nabla_av),
\qquad
\operatorname{div}_a(Av)=0,
\tag{FSCA.3}
```

the native shell

```math
\Pi_j(t)=\chi_j(\mathcal S_A(t))\mathbb P_A(t)
\tag{FSCA.4}
```

keeps pressure as a normal constraint force:

```math
\langle A^\top\nabla_aq,\Pi_jv\rangle=0.
\tag{FSCA.5}
```

The shell-motion term satisfies

```math
\left|\langle\dot\Pi_jv,\Pi_jv\rangle\right|
\le
C\int\lambda_+|\Pi_jv|^2\,da
+r_j^{legal}.
\tag{FSCA.6}
```

This is real progress.  The pressure-only forcing picture is gone.

## 2. The exact gap

Set

```math
e_j={1\over2}\|\Pi_jv\|_2^2,
\qquad
L_j=\left[\log{\lambda_+\over\delta2^{2j}}\right]_+,
\qquad
B_j=e_jL_j.
\tag{FSCA.7}
```

The exact strain-log identity is

```math
{d\over dt}B_j
+e_j\lambda_+
\mathbf 1_{\{\lambda_+>\delta2^{2j}\}}
=
(m_j-d_j)L_j
+e_j{W_\lambda\over\lambda},
\tag{FSCA.8}
```

where

```math
W_\lambda
=
|\Omega e|^2
-e\cdot\nabla^2p\,e
+\nu e\cdot\Delta S\,e.
\tag{FSCA.9}
```

The live theorem is therefore:

```math
\boxed{
\int_0^{T_*}\sum_j
\left[
(m_j-d_j)L_j
+e_j{W_\lambda\over\lambda}
\right]_+\,dt<\infty.
}
\tag{FSCA.10}
```

This is the remaining log-weighted same-shell exchange closure.  It is
order-locked to the same pressure-viscosity-incompressibility-velocity packet.

## 3. Conditional smoothness chain

Once `(FSCA.10)` is proved, the chain is:

```math
\text{finite log-weighted same-shell exchange}
\Longrightarrow
\text{finite material clock}
\Longrightarrow
\sup_{t<T_*}\|u(t)\|_{H^s}<\infty
\quad(s>5/2)
\Longrightarrow
\text{relaunch past }T_*.
\tag{FSCA.11}
```

Thus the finite-breakdown contradiction is reduced exactly to `(FSCA.10)`.
