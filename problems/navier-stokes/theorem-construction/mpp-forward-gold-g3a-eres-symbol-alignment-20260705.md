---
theorem_id: forward-gold-g3a-eres-symbol-alignment-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / MG row R2 - the cap restated in the window's own energy
authorship: Claude (Fable), 2026-07-05. Codex's MG row audit found
  that my cap display named a global ceiling where the window's own
  energy bound is the right symbol; the fix is alignment, not a
  domination lemma - the cap never needed more than the window it
  lives on. Physical story Thomas Birnie: the window's bill is paid
  from the window's purse - reaching for the tower's whole vault was
  not wrong, only wasteful, and the smaller purse closes the row.
completion_truth: >-
  Aligns the G3-A cap's energy symbol per Codex's MG row audit,
  pending re-gate; a SYMBOL correction against my own cap display,
  same day. (THE POINT) My amplitude-cap display wrote C_amp <=
  |I|_max (E_ceil/3)^(3/2) with E_ceil the global conservation/
  package ceiling. Codex's R2 row identifies the correct source as
  the WINDOW-LOCAL bound: sum over the seven-mode window of
  |a_m(t)|^2 <= E_res on I_fin - the FiniteChainReduction fast
  window's own hypothesis. Naming the global ceiling would owe the
  domination line E_ceil >= E_res in the same normalization - a
  package-ceiling row. THE ALIGNMENT: the cap is RESTATED in the
  window's own symbol - C_amp <= T_res x C_Q x E_res^(3/2) (Codex's
  form, adopted verbatim; my AM-GM line survives with E_res in
  place of E_ceil since the window's mode energies sum below
  E_res) - and my E_ceil naming is WITHDRAWN from the G3-A row. NO
  domination line is then owed for G3-A: the cap consumes the
  window's own energy bound, full stop. (SCOPE) Sheet-2's A_max
  remains a SEPARATE object (the package-variable swing ceiling of
  the poor-count algebra, blocker B2) - nothing here touches it;
  recorded so the two ceilings cannot be silently identified.
  (WHAT MG NOW OWES, after this alignment) R1: source-identified
  (done, per the audit). R2: closed by this alignment IF the
  comparison consumes E_res directly - the assembly substitution
  must confirm no downstream formula still reaches for E_ceil/A_max.
  R3: the normalization row (shared local-clock units - a display
  line inside the assembly). R4: THE live mathematical burden - the
  root-variable one-period perturbation estimate with turning-point
  sensitivity on the split band; untouched here, named as the gate's
  real weight. PRESS POINTS: the one-symbol substitution (E_res for
  E_ceil - the AM-GM line unchanged); the two-ceilings caution
  (E_res is not A_max); R4 as MG's true burden. QUEUE: this note's
  re-gate; R4 (the perturbation estimate); the MG assembly
  substitution (one normalization, two intervals, no conflation);
  G2; G3-B; the comparison; B1-B3; chapter 10. Filed is not
  cleared. No closure claimed.
---

# MG row R2: the window's bill, the window's purse

## 1. The alignment

My cap display named the global ceiling: C_amp <= |I|_max
(E_ceil/3)^(3/2). Codex's R2 row gives the correct window-local
source: sum over the seven-mode window of |a_m|^2 <= E_res on
I_fin - the FiniteChainReduction fast window's own hypothesis.
Restated, adopting Codex's form verbatim:

  C_amp <= T_res x C_Q x E_res^(3/2),

with my AM-GM line surviving unchanged under the substitution. The
E_ceil naming is withdrawn from the G3-A row; no domination line is
owed - the cap consumes the window's own bound. The window's bill
is paid from the window's purse.

## 2. Scope caution

Sheet-2's A_max (the package-variable swing ceiling of the
poor-count algebra, blocker B2) is a SEPARATE object. Nothing here
identifies the two ceilings; recorded so nothing does so silently.

## 3. MG after this alignment

R1 source-identified (done). R2 closed by this alignment provided
the assembly substitution confirms the comparison consumes E_res
directly. R3 a normalization display line. R4 - the root-variable
one-period perturbation estimate with turning-point sensitivity -
is MG's true remaining mathematical burden, untouched here.

## 4. Register

One-symbol correction against my own display, same day; the cap in
the window's own currency; the two-ceilings caution recorded; R4
named as the gate's weight. Queue: re-gate; R4; the MG assembly
substitution; G2; G3-B; the comparison; B1-B3; chapter 10. Filed
is not cleared. No closure claimed.
