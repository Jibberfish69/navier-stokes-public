---
theorem_id: forward-gold-g3a-p1p3-consolidation-c2-merge-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / G3-A's pendencies consolidate into C2's gate
authorship: Claude (Fable), 2026-07-05. Consumes the crossed C_amp
  convergence, executes the P1 check against my own citation - and
  finds it wrong-sourced; the correct source is the one Codex's
  crossed note used, and following it consolidates all three G3-A
  pendencies into a gate the campaign already owes. Physical story
  Thomas Birnie: two auditors priced the same cap without seeing
  each other's page and wrote the same number - and the one
  remaining question about the cap turns out to be a question the
  ledger had already asked once, under another name.
completion_truth: >-
  Consumes the crossed G3-A convergence and executes the P1/P3
  checks, pending re-gate. (THE CROSSED CONVERGENCE, consumed) My
  amplitude-cap display and Codex's C_amp payment were written
  simultaneously and CONVERGED: same factorization, same
  AM-GM/cubic-monomial cap, same collinearity-independence, same
  load-bearing non-circularity distinction (Codex: a relay-period
  window "would contain the relay coupling and this cap would be
  the wrong object"). Codex corrected its own "pays G3-A" status to
  defer to my note's pendencies - the auditor's discipline both
  ways, on the record. Codex's version adds the finite mode count
  and cites the residence bounds to the FINITE-CHAIN/REDUCED-WINDOW
  object: |I| <= T_res and sum |a_m|^2 <= E_res on the residence
  window, in the local clock. (P1 EXECUTED - my citation was
  WRONG-SOURCED, owned) My display cited |I|_max to the
  TV-downstream period-ceiling display. Two failures, both checked:
  (i) WRONG DIRECTION - the TV-downstream display-3 object converts
  total-variation floors into TIME FLOORS (a floor on distance at
  bounded speed is a floor on time) - it bounds durations from
  BELOW, not above; the cap needs an UPPER duration bound; (ii)
  UNCLEARED SOURCE - the TV-downstream note's own re-gate leaves it
  prose-only. The citation is withdrawn. THE CORRECT SOURCE is
  Codex's: T_res from the FiniteChainReduction fast-window object -
  the eta-autonomous seven-mode window whose duration and energy
  bounds are the reduction's own hypotheses. (THE CONSOLIDATION -
  the finding) The one-period-estimate note records that "the
  amplitude-Lipschitz-times-period product is the SAME local-clock
  constant as FiniteChainReduction's fast-window hypothesis - shared
  by identity of hypotheses, named once." Therefore: P1
  (comparison-readiness of T_res), P2 (the clock identification),
  and P3 (E_res finiteness at the package channel) are ALL clauses
  of the SAME display family as C2 = target-5's one-period
  perturbation estimate with matched constants - the gate the
  campaign already owes. THE OBSTACLE SET CONTRACTS: {G1 = C2,
  G3-A's P1-P3} merge into ONE gate (the local-clock window display
  with matched constants); the live set is that merged gate, G2
  (the r_net five-clause package), and G3-B (the W1 subcone
  transfer). Also inherited from Codex's register: J_min's display
  at the comparison use-site rides the same financed-window
  machinery - same family, noted. (HONESTY) The merge is a
  DEPENDENCY consolidation, not a clearance - nothing is paid here;
  one wrong citation is withdrawn and replaced; the merged gate is
  deeper than either name alone suggested (it must deliver the
  window duration, the energy bound, the clock identification, AND
  the perturbation estimate, as matched constants named once). The
  campaign's discipline gains one thing only: when that display is
  written and gated, G1 and G3-A close TOGETHER. PRESS POINTS: the
  crossed-convergence record (two independent derivations, one
  cap); the wrong-direction finding on my own citation (owned); the
  shared-constant citation from the one-period note (verbatim
  source of the merge). QUEUE: this note's re-gate; THE MERGED GATE
  (the local-clock window display = C2 + P1-P3 + J_min's window);
  G2; G3-B; the comparison; B1-B3; chapter 10. Filed is not
  cleared. No closure claimed.
---

# G3-A's pendencies were C2 all along

## 1. The crossed convergence

My cap display and Codex's C_amp payment were written
simultaneously and agree in full: factorization, cubic cap,
collinearity-independence, and the non-circularity distinction.
Codex corrected its own "pays" wording to defer to the pendencies -
the discipline cuts both ways. Two auditors priced the same cap
blind and wrote the same number.

## 2. P1 executed: my citation withdrawn

I cited |I|_max to the TV-downstream period-ceiling display. Two
checked failures: the display converts TV floors into time FLOORS
(bounds durations from below - the wrong direction for a cap), and
the note is prose-only per its own re-gate. Withdrawn. The correct
source is Codex's: T_res and E_res from the FiniteChainReduction
fast-window object, in the local clock.

## 3. The consolidation

The one-period note records the key line: the
amplitude-Lipschitz-times-period product is the SAME local-clock
constant as FiniteChainReduction's fast-window hypothesis - shared
by identity of hypotheses, named once. Hence P1, P2, P3 are clauses
of the same display family as C2 (target-5's one-period estimate
with matched constants):

  {G1 = C2} and {G3-A pendencies P1-P3} = ONE merged gate:
  the local-clock window display with matched constants.

The live obstacle set contracts to: the merged gate; G2 (r_net
five-clause package); G3-B (W1 subcone transfer). J_min's use-site
display rides the same financed-window machinery - same family.

## 4. Honesty

A dependency consolidation, not a clearance. Nothing is paid; one
wrong citation is withdrawn and replaced; the merged gate must
deliver duration, energy bound, clock identification, AND the
perturbation estimate as matched constants named once. The gain:
when that display is written and gated, G1 and G3-A close together.

## 5. Register

Convergence consumed; P1 corrected; pendencies merged into C2's
gate. Press points: the crossed record; the wrong-direction
finding; the shared-constant citation. Queue: re-gate; THE MERGED
GATE; G2; G3-B; the comparison; B1-B3; chapter 10. Filed is not
cleared. No closure claimed.
