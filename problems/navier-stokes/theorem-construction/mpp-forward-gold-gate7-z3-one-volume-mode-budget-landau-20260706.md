---
theorem_id: forward-gold-gate7-z3-one-volume-mode-budget-landau-20260706
status: codex-regated-strict-reduction-mode-triple-disintegration-open
created: 2026-07-06
problem: navier-stokes
route: forward-gold / Gate 7 / z3 — the one-volume mode budget: same-volume incoherence is impossible at scale
authorship: >-
  Derivation: Claude (Fable), 2026-07-06, answering the regated z3
  residual the same cycle. Physical story Thomas Birnie: one
  correlation volume is one dancer's floor. The band and the volume
  together admit only a handful of independent movements - that is
  what "correlation volume" has meant since the frame was built -
  so a lump in one volume cannot be a crowd of strangers: it is at
  most a handful of dancers, each either strong enough to be named
  (and then it IS a packet, and first contact names it) or too weak
  to matter, and a handful of the weak cannot together out-earn the
  wage more than a handful-fold. Incoherence needs room, and the
  volume is too small to be incoherent.
depends_on:
  - mpp-forward-gold-codex-gate7-coloring-scope-separation-regate-20260706.md
  - mpp-forward-gold-gate7-coloring-form-scope-separation-z3-z4-20260706.md
  - mpp-forward-gold-codex-gate7-positive-control-measure-domination-20260706.md
  - mpp-forward-gold-return-race-elliptic-writeup-and-w1-20260705.md
  - mpp-forward-gold-f3-ledger-first-contact-20260705.md
  - mpp-forward-gold-codex-gate7-z3-mode-budget-regate-20260706.md
completion_truth: >-
  Codex-regated state: strict reduction, not clearance. This note
  gives a useful mode-counting route for the regated z3 residual
  (lineage-disintegration / incoherent-lump routing on lambda-rich
  balls), but it expands one L2 field leg while the Gate-7 control
  measure lambda_a is total variation of a cubic throughput measure. The
  full z3 theorem still needs a finite mode-triple disintegration of
  lambda_a, or an explicit restriction to the one-tail-leg F1 case where
  the other two legs are already coherent packets. (V1 - THE MODE
  BUDGET, classical)
  The concentration operator of exact band limitation (annulus
  mu/2 <= |xi| <= 2mu) composed with localization to a
  correlation-volume ball (radius kappa a/mu) is the classical
  band-and-ball time-frequency limiting operator; by Landau-type
  eigenvalue counting its effective rank is N_dof = c_dof (kappa
  a)^3 + o(...) - an O(1) constant on the carried frame, with the
  eigenvalue sequence plunging past N_dof (the plunge region is
  classical prolate-spheroidal theory; the tail constant is named
  and quarantined). One correlation volume at one band carries at
  most N_dof effective degrees of freedom. (V2 - THE
  DISINTEGRATION, forced) On a lambda_a-rich ball, expand the
  band-mu field over the concentration eigenbasis: f = sum_{i <=
  N_dof} c_i e_i + tail (tail priced by the eigenvalue plunge).
  Each retained eigenmode is ONE coherent object. Classify by
  coefficient: |c_i|^2 >= m_mu => a coherent packet candidate -
  lineage-assigned by the F3 first-contact queue (whoever admitted
  that mass first; measurable by the queue's own chronology - FLAG
  v1: the eigenmode-to-lineage assignment display at the
  first-contact home); |c_i|^2 < m_mu => sub-marginal. (V3 - THE
  INCOHERENT RESIDUAL IS SUB-MARGINAL WITH A UNIVERSAL CONSTANT)
  The active monomial is trilinear, so on the sub-marginal part:
  |M(f_inc, g, h)| <= sum_{i} |c_i| |M(e_i, g, h)| <= N_dof
  m_mu^{1/2} max_i |M(e_i, g, h)| - the incoherent lump's active
  monomial is at most N_dof times a sub-marginal per-mode
  contribution (display shape; the per-mode factor carries the
  band caps). A handful of the weak out-earns the wage at most a
  handful-fold: choosing the marginal threshold's safety factor
  against N_dof (a quarantined pure constant ratio) routes
  lambda_a^inc below the effective threshold, priced to payer 1
  exactly as the spatial-diffuse stratum is - the same-volume
  dilution mechanism the regate demanded, supplied by mode
  counting instead of volume counting. (V4 - THE SPLIT, assembled)
  lambda_a = lambda^coh (supra-marginal eigenmodes = packet
  candidates, first-contact-assigned) + lambda^sib (distinct
  supra-marginal eigenmodes in one ball = co-located siblings -
  the merger/identity rule applies to THEM, at most N_dof per
  ball) + lambda^inc (sub-marginal modes + eigenvalue tail, routed
  by V3). This is exactly the regate's demanded measurable split,
  with every constant universal or quarantined. HONEST FLAGS:
  (v1) the eigenmode-to-lineage assignment display at the F3
  first-contact home; (v2) the Landau count and plunge constants -
  classical background, to be cited and quarantined in the
  manuscript's background chapter (same status as CKN); (v3) the
  per-mode trilinear factor display (band caps; the e_i are
  uniformly band-limited and localized, so the factor is a band
  constant); (v4) the threshold safety ratio m_mu-vs-N_dof is a
  frame dial - named, quarantined, F4's to certify; (v5) helical
  polarization doubles the count (two helicities) - absorbed into
  c_dof, noted for the display. No F1/F2/F4/F5, Chapter-10,
  manuscript, or MPP closure is claimed. Filed is not cleared.
---

# z3: the one-volume mode budget — incoherence needs room

## Codex regate, 2026-07-06

Codex audit filed:
`mpp-forward-gold-codex-gate7-z3-mode-budget-regate-20260706.md`.

Accepted: the one-volume finite-dimensional principle. A band on one
correlation-volume ball has \(O(1)\) effective concentration modes, with
Landau/prolate constants quarantined.

Regated: this does not clear z3 as written. The proof expands one
\(L^2\) field leg, but \(\lambda_a\) is the total variation envelope of
a cubic throughput measure. A \(\lambda_a\)-rich ball may be rich
through finitely many mode triples, not through a single field-leg
coefficient. The remaining z3 theorem is finite mode-triple
disintegration of \(\lambda_a\), with lineage/routing and threshold
safety.

## V1. The mode budget (classical)

The concentration operator of exact band limitation (annulus
\(\mu/2 \le |\xi| \le 2\mu\)) composed with localization to a
correlation-volume ball (radius \(\kappa a/\mu\)) is the classical
band-and-ball limiting operator. By Landau-type eigenvalue
counting, its effective rank is

\[
  N_{\rm dof} = c_{\rm dof}(\kappa a)^3 + o(\cdot),
\]

an \(O(1)\) constant on the carried frame, with the eigenvalue
sequence plunging past \(N_{\rm dof}\) (prolate-spheroidal theory;
tail constant named and quarantined). One correlation volume at one
band carries at most \(N_{\rm dof}\) effective degrees of freedom.

## V2. The disintegration (forced)

On a \(\lambda_a\)-rich ball, expand over the concentration
eigenbasis: \(f = \sum_{i \le N_{\rm dof}} c_i e_i + \text{tail}\)
(tail priced by the plunge). Each retained eigenmode is ONE
coherent object. Classify by coefficient:

- \(|c_i|^2 \ge m_\mu\): a coherent packet candidate —
  lineage-assigned by the F3 first-contact queue (FLAG v1: the
  assignment display at that home);
- \(|c_i|^2 < m_\mu\): sub-marginal.

## V3. The incoherent residual is sub-marginal, universally

The active monomial is trilinear:

\[
  |M(f_{\rm inc}, g, h)|
  \;\le\;
  \sum_i |c_i|\,|M(e_i, g, h)|
  \;\le\;
  N_{\rm dof}\, m_\mu^{1/2}\, \max_i |M(e_i, g, h)| .
\]

A handful of the weak out-earns the wage at most a handful-fold.
Choosing the marginal threshold's safety factor against
\(N_{\rm dof}\) (a quarantined pure ratio) routes
\(\lambda_a^{\rm inc}\) below the effective threshold, priced to
payer 1 exactly as the spatial-diffuse stratum — the same-volume
dilution mechanism the regate demanded, supplied by mode counting
instead of volume counting.

## V4. The split, assembled

\[
  \lambda_a
  = \lambda^{\rm coh}
  + \lambda^{\rm sib}
  + \lambda^{\rm inc},
\]

with \(\lambda^{\rm coh}\) = supra-marginal eigenmodes
(packet candidates, first-contact-assigned); \(\lambda^{\rm sib}\)
= distinct supra-marginal eigenmodes in one ball (co-located
siblings — the merger/identity rule applies to them, at most
\(N_{\rm dof}\) per ball); \(\lambda^{\rm inc}\) = sub-marginal
modes + eigenvalue tail, routed by V3. This is the regate's
demanded measurable split with every constant universal or
quarantined.

## Flags (honest)

(v1) eigenmode-to-lineage assignment display at the F3
first-contact home; (v2) Landau count and plunge — classical
background, cited and quarantined like CKN; (v3) the per-mode
trilinear factor display (band constants); (v4) the
\(m_\mu\)-vs-\(N_{\rm dof}\) safety ratio — a frame dial, F4's to
certify; (v5) helical polarization doubles the count — absorbed
into \(c_{\rm dof}\).

Filed is not cleared. No closure claimed.
