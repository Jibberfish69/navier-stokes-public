---
theorem_id: forward-gold-gratio-matching-check-use-site-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / the matching check - gamma_home IS the G-ratio at its use site
authorship: Claude (Fable), 2026-07-05. The sheet1-rewrite named one
  matching question for the auditor; this note answers it by three
  citations to filed definitions and use sites - nothing new is
  asserted. Physical story Thomas Birnie: the toll is collected
  where the road actually runs - the tollgate's price on the road
  it stands on is the only price the ledger ever charges, and the
  gate was priced standing exactly there.
completion_truth: >-
  Executes the G-ratio matching check named by the sheet1-rewrite,
  pending re-gate; THREE CITED FINDINGS, no new mathematics.
  (M1, THE DEFINITION SITE) gamma_home is DEFINED in the f1-f2
  formulas note as the displayed lower bracket of the G-RATIO
  itself: |G_pred(alpha)|/|G_relay| >= [(q + c(-w) - k)/(q + p - k)]
  x (p/c(w)) x (m_s/sin(phi0)) - the three-factor product the
  campaign has been flooring all day IS the predator/relay G-ratio's
  home-slice bracket, by its filed definition; gamma_home is not a
  proxy for gamma_G, it is gamma_G's slice floor by construction.
  (M2, THE USE SITE) The delivered tax enters the comparison ONLY
  through the slice integral: the f1-f2 note's aggregate display -
  (2w) x [rectification single-mode display at coupling lower value
  |G_relay| x the ratio bracket] x sigma_min - collects the drain on
  the sector-admissible slice, and the composed delivery rate
  Lambda(N) = gamma_home c_diff sqrt(N/ln 2) (low-edge reduction)
  carries exactly that slice floor; no delivered-side line uses the
  G-ratio anywhere off the slice. The tracing sweep's gamma_G ("the
  R2b cross-triad G-ratio floor on the compact financed family") is
  therefore, at its one and only use site, the slice floor - the
  family's off-slice values never enter any filed display. (M3,
  CODEX'S OWN TABLES RUN ON THE SAME SLICE) The factorwise
  evaluation table and the endpoint-interiority table construct the
  candidate scan on I_w = [phi_min, A - w] with the
  sector-admissible w_eff - the same slice, cited by Codex to the
  same slice-width note; GAMMA_ANGLE was cleared as a floor on that
  slice. CONCLUSION: the matching holds BY USE-SITE COINCIDENCE -
  definition site (M1), delivery site (M2), and evaluation site
  (M3) are one slice; GAMMA_ANGLE and the candidate-list floor
  legitimately instantiate gamma_G in the Sheet-1 inequality.
  (THE ONE CAVEAT, checked and named) If any FUTURE delivered-side
  use of the G-ratio leaves the slice, the matching must be
  re-checked there; checked today: sheet 2's poor-horn line
  (r0 tau_min F_oct/(8 A_max) >= T_oct) contains NO G-ratio symbol,
  so no off-slice use exists in any filed comparison - the caveat
  is empty at present and recorded so it cannot become silently
  nonempty. PRESS POINTS: the three site citations (one line each);
  the empty-caveat check (sheet 2 scanned, no gamma). QUEUE: this
  note's re-gate (with the six pending); C2 (target-5); the
  max-crossing display (r_net); THE COMPARISON (two symbols wide,
  both chains named); B1-B3; chapter 10. Filed is not cleared. No
  closure claimed.
---

# The matching check: one slice, three sites

## 1. The definition site (M1)

gamma_home is defined in the f1-f2 formulas note as the displayed
lower bracket of |G_pred(alpha)|/|G_relay| - the predator/relay
G-ratio itself, on the home slice. It is not a proxy for gamma_G;
it is gamma_G's slice floor by construction.

## 2. The use site (M2)

The delivered tax enters the comparison only through the slice
integral (the f1-f2 aggregate display: slice measure times the
rectification single-mode display at the worst bracket times
sigma_min), and Lambda(N) = gamma_home c_diff sqrt(N/ln 2) carries
exactly that floor. No filed delivered-side line uses the G-ratio
off the slice; the family's off-slice values never enter.

## 3. The evaluation site (M3)

Codex's factorwise evaluation and endpoint-interiority tables scan
I_w = [phi_min, A - w] at the sector-admissible w_eff - the same
slice, cited to the same slice-width note. GAMMA_ANGLE was cleared
as a floor there.

## 4. Conclusion and the one caveat

Definition, delivery, and evaluation are one slice: the matching
holds by use-site coincidence, and GAMMA_ANGLE (or the tighter
candidate-list floor) legitimately instantiates gamma_G in the
Sheet-1 inequality. Caveat, checked empty: sheet 2's poor-horn line
carries no G-ratio symbol - no off-slice use exists in any filed
comparison; recorded so it cannot become silently nonempty.

## 5. Register

Matching check executed by three citations; nothing new asserted.
The toll is collected where the road actually runs. Press points:
the three site lines; the empty-caveat scan. Queue: re-gate; C2;
the max-crossing display; THE COMPARISON; B1-B3; chapter 10. Filed
is not cleared. No closure claimed.
