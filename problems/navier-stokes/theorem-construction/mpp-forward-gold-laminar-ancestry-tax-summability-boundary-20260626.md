# Laminar ancestry tax summability boundary

Status: global summability open; exact reserve isolated.

## Single-line input

For one laminar material ancestry line `A(a)`, the deterministic tax gives

```math
\sum_{\gamma\subset A(a)}a_\gamma
\le
C V_C(A(a))+C V_F(A(a))+R_{legal}(A(a)).
```

Here `V_C` is Cauchy-Green logarithmic variation in the paying complete-frame directions, and `V_F` is projective complete-frame variation.

## Global estimate needed

The first ratio moment follows from

```math
\int V_C(A(a))\,da+\int V_F(A(a))\,da+R_{legal}<\infty.
```

Then

```math
\sum_\gamma a_\gamma\simeq\sum_{\ell\ge0}2^\ell\nu_\ell<\infty.
```

## Boundary

The current installed raw ledgers control heat-time mass and fixed-collar/fixed-annulus action. The needed forest estimate is a rectified material-time variation estimate. It has the same strength as the full high-ratio action at this edge.

The next exact hinge is

```math
\boxed{\text{GlobalLaminarAncestryVariationReserve.A}}
```

Statement:

For the original smooth material history, after paid nonlaminar exits, the laminar high-ratio ancestry forest satisfies

```math
\int (V_C+V_F)\,da\le C_N(u_0)+R_{legal}.
```

This is the global reserve needed to turn the single-line tax into full first-ratio-moment summability.