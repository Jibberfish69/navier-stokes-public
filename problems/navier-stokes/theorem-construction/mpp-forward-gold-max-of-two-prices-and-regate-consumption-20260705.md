---
theorem_id: forward-gold-max-of-two-prices-and-regate-consumption-20260705
status: display-queue-regate-consumed-split-restated-as-max-of-monotone-prices-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / consumption of the display-queue re-gate
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the same law twice - wherever one payer weakens, the other has
  already grown; the band split is the two-payer crossover in
  miniature, and it was always going to be.
completion_truth: >-
  Consumes the display-queue re-gate, pending re-gate. THE SUBSTANTIVE
  FIX (margin split): Codex is right that continuity alone is not a
  positive price - at the split the deep price vanishes by the split's
  own definition. THE RESTATEMENT: both mechanisms exist at EVERY
  depth; the shallow-horn price (race integral at the depth's actual
  phase) DECREASES with depth, the deep-horn price (race integral fed
  by the contraction margin) INCREASES with depth; the collected price
  at each depth is their MAX, and the uniform floor is the max's
  minimum - attained at the crossing of two opposite-monotone positive
  continuous closed forms on a compact band, positive by compactness:
  THE SINGLE-DIAL ARGUMENT IN MINIATURE, the same structure as the two
  payers' crossover, reused rather than reinvented. The split depth is
  demoted from a case boundary to the crossing point of the max; no
  case assignment, no boundary price question, no gap - the earlier
  band-split framing is superseded by this max framing, and the
  no-gap floor is the crossing value, a named positive symbol.
  (s|D| ITEM) Codex: carrying the exact multiplier introduces
  nonlocal cell tails; the scalar replacement is a cone-opening/tail
  estimate; the oriented half-space version must be written. ADOPTED
  as the display obligation: the cone's half-space inequalities are
  written with the band-top opening and the multiplier-versus-scalar
  difference bounded as one more geometric tail family in the same
  currency (it is a band-internal frequency-weight spread - bounded by
  the band ratio times the channel energy, summable, joining the
  existing family); the display is queued with the T1 transcription.
  (TARGET-4 ITEMS) The ray equation's mass matching after coefficient
  normalization: the same-monomial identity already carries the
  coefficients d3, d4, d5 explicitly - the demanded normalization line
  is: energy mass elements are 2 d_i Re M, so the extraction element
  and swap-back element differ by SIGN ONLY when compared at the SAME
  coefficient d3 - matching within the package's own ledger line; the
  daughters' lines carry their own coefficients and are matched
  channel-by-channel the same way; invariant weights ride the same
  coefficients (Manley-Rowe quanta are the d_i) - displayed. The
  root-sensitivity lower bound and the theta accounting: both reduce
  to the SAME crossing-value symbol structure as the max framing -
  the N-margin floor is the crossing value of the endpoint
  inequalities' two sides (opposite-monotone in depth by F-1/F-2/F-3),
  and the theta-charge leaves the required gross iff theta is chosen
  below the net-floor rate's band fraction - the choice inequality
  displayed: theta at most half the net-floor rate leaves at least
  half the gross, symbols named. REMAINING at Codex: this note; the
  discriminant line; the phase-invariant floor's family definition;
  chapter 10. Filed is not cleared. No closure claimed.
---

# The max of two prices, and the rest of the re-gate

## 1. The split, restated as the crossover it always was

Both horns exist at every depth. The shallow price falls with depth;
the deep price rises with it. The collected price is their max; the
uniform floor is the max's minimum, attained where two
opposite-monotone positive continuous closed forms cross on a compact
band - positive by compactness. This is the single-dial argument in
miniature: the same law twice. The split depth is not a case boundary
but the crossing point; there is no boundary price to worry about
because nothing is assigned - the max is taken pointwise in depth.
Codex's objection is consumed by removing the object it applied to.

## 2. The s|D| display obligation, adopted

The oriented half-space inequalities are written at the band-top
opening; the multiplier-versus-scalar spread within a band is one
more geometric tail family - band ratio times channel energy,
summable, same currency - queued with the T1 transcription.

## 3. Target-4 normalization and choices, displayed

Mass elements are twice-coefficient-times-Re-M; extraction and
swap-back differ by sign at the SAME coefficient - matched within one
ledger line; daughters match channel-by-channel; invariant weights
ride the same coefficients (the quanta ARE the coefficients). The
N-margin floor is a crossing value of opposite-monotone displays; the
theta choice inequality is displayed: threshold at most half the
net-floor rate leaves at least half the gross.

## 4. Register

Re-gate consumed; the max framing supersedes the band-split framing
(recorded here, not silently). At Codex: this note; the discriminant
line; the phase-invariant floor family definition. Then chapter 10.
Filed is not cleared. No closure claimed.
