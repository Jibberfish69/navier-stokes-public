---
theorem_id: forward-gold-mg-r3-assembly-substitution-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / MG row R3 - the assembly substitution displayed
authorship: Claude (Fable), 2026-07-05. MG's R3 row asks for a
  display, not a theorem: where each of the two windows enters the
  comparison, in one clock, without ever being the same window.
  Physical story Thomas Birnie: the ledger runs two clocks-worth of
  bookkeeping through one till - the financing window stamps the
  coupling's floor, the swing window stamps the delivered exponent,
  and the till's one currency is the local clock; no entry is
  stamped twice.
completion_truth: >-
  Displays MG row R3 - the assembly substitution - pending re-gate;
  a CONSUMPTION-SITE TABLE, three rows, each a citation; no new
  mathematics. (SITE 1, WHERE I_fin ENTERS) The fixed financing
  window enters the comparison at EXACTLY ONE site: the G3-A
  coupling floor |G_relay| >= J_min/C_amp with C_amp <= T_res C_Q
  E_res^(3/2) - the cleared local cap consuming I_fin's own
  duration and energy rows (R1, R2). Its OUTPUT is a relay-coupling
  floor, which feeds the gamma_G front only THROUGH G3-B's
  shape-to-ratio transfer (open); I_fin touches nothing else in the
  chain - in particular not the delivered exponent's window factor.
  (SITE 2, WHERE I_per ENTERS) The frozen one-period window enters
  at EXACTLY ONE site: the delivered exponent's elliptic window
  factor 2K(m) in Lambda_N = gamma_G x 2K(m)/sqrt(2 delta_N) - the
  frozen-coefficient period value whose legitimacy is precisely
  R4's estimate (the true-vs-frozen one-period average bound with
  turning-point sensitivity); I_per touches nothing else - in
  particular not the coupling floor. (SITE 3, THE ONE CLOCK) Both
  windows' constants are measured in the FCR local fast clock:
  T_res is the FCR fast-window duration bound BY DEFINITION (R1),
  and the one-period note's shared-constant line places C_per's
  currency in the same local-clock product ("the amplitude-
  Lipschitz-times-period product is the SAME local-clock constant
  as FiniteChainReduction's fast-window hypothesis, shared by
  identity of hypotheses, named once") - one normalization, cited,
  not asserted. (THE NO-CONFLATION LINE, explicit) I_fin and I_per
  are DISTINCT objects consumed at DISJOINT sites: the comparison
  never evaluates the cap on I_per, never freezes coefficients on
  I_fin, and no step divides or multiplies quantities across the
  two windows except through the already-separated gamma_G and
  Lambda_N factors - the substitution table IS the proof that no
  entry is stamped twice, because each window has exactly one
  consumption site and the sites sit in different factors of the
  displayed inequality. (WHAT THIS PAYS AND WHAT IT CANNOT) R3 is a
  display row and this is its display; it clears only if the
  re-gate confirms the two site-citations and the clock citation.
  It does NOT pay R4 (the perturbation estimate legitimizing site
  2's frozen value - MG's true weight, untouched); it does not
  advance G3-B (site 1's output waits there); it runs no
  comparison. HONESTY: site 1's "exactly one site" claim is a
  CHECKED-ABSENCE claim (no other filed consumption of I_fin in the
  Sheet-1 chain - scanned: the rewrite, the C1-corrected assembly,
  the factorwise tables); same for site 2 (scanned: same files);
  the scans are named so the re-gate can repeat them. PRESS POINTS:
  the two site-citations; the clock citation (verbatim from the
  one-period note); the checked-absence scans. QUEUE: this note's
  re-gate; R4 (the one-period perturbation theorem - MG's
  remaining weight); G2; G3-B; the comparison; B1-B3; chapter 10.
  Filed is not cleared. No closure claimed.
---

# MG row R3: two windows, one till

## 1. Site 1: where the financing window enters

Exactly one site: the G3-A coupling floor,

  |G_relay| >= J_min / C_amp,
  C_amp <= T_res C_Q E_res^(3/2)   (cleared local cap, rows R1-R2),

consuming I_fin's own duration and energy. Its output - a
relay-coupling floor - feeds gamma_G only through G3-B's
shape-to-ratio transfer (open). I_fin touches nothing else; in
particular, not the delivered exponent's window factor.

## 2. Site 2: where the period window enters

Exactly one site: the delivered exponent's elliptic factor,

  Lambda_N = gamma_G x 2K(m) / sqrt(2 delta_N),

where 2K(m) is the frozen-coefficient period value - legitimized
precisely by R4's true-vs-frozen estimate, MG's remaining weight.
I_per touches nothing else; in particular, not the coupling floor.

## 3. Site 3: the one clock

T_res is the FCR fast-window bound by definition; the one-period
note places C_per's currency in the same local-clock product,
"shared by identity of hypotheses, named once." One normalization,
cited.

## 4. The no-conflation line

I_fin and I_per are distinct objects with disjoint consumption
sites in different factors of the displayed inequality: the cap is
never evaluated on I_per, coefficients are never frozen on I_fin,
and nothing crosses the two windows except through the separated
gamma_G and Lambda_N factors. Each window has one stamp; no entry
is stamped twice.

## 5. Honesty and register

The "exactly one site" claims are checked absences (scanned: the
sheet1-rewrite, the C1-corrected assembly, the factorwise tables) -
named so the re-gate can repeat the scan. This display pays R3
only; R4 is untouched and remains MG's weight; no comparison runs.
Press points: the two site citations; the verbatim clock citation;
the scans. Queue: re-gate; R4; G2; G3-B; the comparison; B1-B3;
chapter 10. Filed is not cleared. No closure claimed.
