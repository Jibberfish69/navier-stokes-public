# MPP Forward-Gold Parabolic Signed Saturation After Heat-Packet Test

Date: 2026-06-20

## Status

Direct post-heat-packet saturation test complete.  The fixed-radius terminal
atom is already paid by ordinary temporal absolute continuity, and the dyadic
heat packet shows that viscosity alone does not price heat-scale packet
creation.  The remaining signed-current question is therefore exact:

```math
\text{can the positive terminal receiver be fed by an unpaid internal donor
sink along the shrinking heat scale?}
```

The finite donor graph is payable.  The only unpriced branch is the infinite
terminal Zeno donor chain carrying inherited positive pre-Cauchy source.  Thus
`TerminalParabolicSignedSaturation.A` does not close from installed inputs, but
it reduces to the same terminal Zeno / source-square fork already isolated by
the source-wall notes.

## 1. The parabolic signed-current object

On a finite same-fluid terminal packet/time truncation, the localized signed
current has the form

```math
\partial_tE_P+D_P
=
\operatorname{div}_{\mathcal G}J(P)+L_P,
\qquad
J(P\to P')=-J(P'\to P),
\tag{PSS.1}
```

with legal remainder

```math
\sum_P\int |L_P|dt
\le
Loss_{legal}+o_N(1).
\tag{PSS.2}
```

The selected positive terminal source is

```math
\sum_{P\in\mathcal F_N}\int
[\operatorname{div}_{\mathcal G}J(P)]_+dt.
\tag{PSS.3}
```

`CycleHeatAction.A` would follow if every unit in `(PSS.3)` were boundary-fed,
legal-loss-fed, earlier-arrival-fed, or forced through a heat-paying internal
cycle.

## 2. Why heat action cannot be applied first

For an edge

```math
e:D\to R,
\qquad
J_e>0,
\tag{PSS.4}
```

`R` receives positive terminal source and `D` is the signed donor sink.  The
formal skew identity gives the negative partner at `D`, but that is useful only
if the donor side remains inside the usable parabolic ledger or is legally
paid.

A same-time sink-to-receiver transfer can occur before the flow crosses any
viscous heat edge.  Thus heat action prices the transfer only after one has
proved that the donor side cannot disappear from the usable terminal accounting.
This is exactly `ParabolicNoFreeSink.A`, equivalently
`TerminalParabolicSignedSaturation.A`.

## 3. Finite donor chains telescope

Follow the signed donor obligation backward from a selected positive terminal
edge.  At each step there are four cases.

First, the signed donor/counter-edge is retained in the same saturated terminal
ledger.  Then the selected positive contribution is paired directly with the
negative contribution.

Second, the donor side exits through pressure, cutoff, collar, projection,
off-family, boundary, stopped residual, or another named legal channel.  Then
it is paid by `(PSS.2)` and the installed legal ledgers.

Third, the donor side is a finite internal donor path.  Applying
`LocalDonorBalance.A` along the finite path telescopes interior refill terms,
and the terminal cost is reduced to retained signed partners, legal exits, or
entrance leaves.

Fourth, if the finite path reaches a preterminal entrance surface,
`EntranceLeafDecay.A` gives the high-frequency entrance leaf as `o_N(1)`.

Therefore every finite or non-Zeno donor chain is payable by the already named
signed-partner/legal/local-donor/entrance-decay package.

## 4. The surviving branch

The only branch not paid by the preceding finite argument is an infinite legal
same-fluid donor chain

```math
\cdots\to P^{-2}\to P^{-1}\to P^0
\tag{PSS.5}
```

with radii `r_m downarrow 0`, terminal times `t_m upward T_*`, and finite
parabolic length

```math
\sum_m r_m^2<\infty.
\tag{PSS.6}
```

The selected lower bound passes down the chain as inherited positive
pre-Cauchy source.  This is precisely the terminal Zeno source-refill branch:

```math
\boxed{
\text{terminal Zeno donor chain with inherited positive native source.}
}
\tag{PSS.7}
```

This branch is not priced by ordinary finite graph saturation, first-current
pruning, or heat action.  It is also not excluded by the heat-packet calculation:
the heat packet shows exactly how first moment can vanish while square reserve
remains order one on the shrinking heat scale.

## 5. Exact conditional closure

The proved reduction is:

```math
\boxed{
\begin{aligned}
&\text{retained signed partners}\\
&+\text{legal exits}\\
&+\text{LocalDonorBalance.A}\\
&+\text{EntranceLeafDecay.A}\\
&+\text{NoFreeTerminalZenoDonorChain.A}\\
&\Longrightarrow
\text{TerminalParabolicSignedSaturation.A.}
\end{aligned}
}
\tag{PSS.8}
```

The remaining child has the same two exact suppliers as the earlier two-tower
and source-wall reductions:

```math
\boxed{
\text{NoFreeTerminalZenoDonorChain.A}
\Leftarrow
\text{LocalPositiveSourceCarleson.A}
\quad\text{or}\quad
\text{ZenoBoundedClass}_{B}\text{.A}
+
\text{ZenoResidueLiouville}_{B}\text{.A}.
}
\tag{PSS.9}
```

Equivalently, the direct source-square version is

```math
\boxed{
\text{ScaleCriticalTreeCarleson.A / SourceSquareReserve.A}
}
\tag{PSS.10}
```

for the same selected native carrier.

## 6. What this rules out

This test rules out two false closures.

It rules out the claim that `CycleHeatAction.A` follows from a finite graph
flow decomposition alone.  A positive terminal receiver can be fed by an
internal donor sink before any heat edge is crossed.

It also rules out the claim that heat smoothing alone proves no-free-sink.  The
heat packet has

```math
\int_{I_m}D_m(t)\,dt\to0,
\qquad
\int_{I_m}\lambda_mD_m(t)^2\,dt=1,
```

so a square-reserve birth can be invisible to the first-moment heat ledger.

## Verdict

`TerminalParabolicSignedSaturation.A` is not discharged from installed inputs.
The exact surviving branch is no longer a vague hidden mismatch.  It is the
terminal Zeno donor chain with inherited positive native source, and it closes
only by a same-carrier positive source Carleson/source-square theorem or by
rigid production plus residue Liouville for the Zeno source-residue limit.
