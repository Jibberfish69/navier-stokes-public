---
theorem_id: forward-gold-parallel-multicurrency-review-to-primitive-pls-live-edge-20260627
status: parallel-multicurrency-review-demotes-local-currencies-to-support-and-restores-primitive-pls-live-edge-open
created: 2026-06-27
problem: navier-stokes
route: forward-gold same-material selected-density / post-augmented invisible-child review
logical_landing_node: GlobalSamePacketFullClockFromOriginalData.A
answers_prompt:
  - attack invisible-child object from different mathematical currencies
  - why local currencies do not close the Gold route
uses_parallel_checks:
  - Rellich/Pohozaev quotient-visible selected sign
  - Carleman/adjoint polarization corrected split
  - microlocal/DtN/zero-defect packet classification
source_surfaces:
  - /Users/thomasbirnie/.codex/attachments/fecb99eb-1583-4598-99bb-fa5a4a66021b/pasted-text.txt
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-augmented-stopped-selected-action-trace-coercivity-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-source-origin-kernel-classification-and-branch-refinement-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-multicurrency-invisible-child-to-stopped-compensator-synthesis-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-stopped-visible-silent-exchange-root-reserve-direct-audit-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-selector-stratum-crossing-bv-closure-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-native-birth-live-edge-after-reset-countertest-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-selected-scale-native-reserve-atom-joint-interface-reduction-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-moving-lowhigh-interface-lower-edge-direct-test-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-der-aqk-moving-lowhigh-record-return-synthesis-20260627.md
refines_current_hinge:
  - StoppedFullExchangeActionCarleson.A
  - SelectedCompressionRootReserve.A
  - PaidReselectionStoppingCarleson.A
  - StoppedReselectionNoEscape.A
to_live_edge:
  - PrimitivePLSNoFreeUpcrossing.A
  - PrimitivePLSBoundedBelowLyapunov.A
  - StoppedPrimitivePLSSignedScalePotentialLift.A
  - StoppedPotentialRecordAverageBound.A
  - StoppedNegativeScaleReturnCharge.A
  - CriticalWeightedParentChildResetCarleson.A
  - LogScaleResetDerivativeCarrierBound.A
effect: >-
  Records the parallel multicurrency review after the augmented stopped trace
  theorem.  Carleman, Rellich/Pohozaev, adjoint polarization, microlocal defect
  measure, DtN Riccati, and zero-defect classification all prove local
  quotient-visible or zero-cost split statements.  None supplies the global
  selected-critical root reserve.  The remaining live mathematical edge is the
  strict primitive PLS high-ratio tail gain, equivalently record/return descent
  or critical weighted parent-child reset from original history.
---

# Parallel multicurrency review to primitive PLS live edge

The supplied bad object was:

```math
A_\infty>0,
\qquad
\mathsf S_\infty U_\infty=0.
\tag{1}
```

Plainly, a child carries real selected critical-strain action while the parent
pressure-Hodge boundary sees no defect.

The parallel currency checks agree with the current source state: `(1)` is the
right warning, but it is too small as a final theorem.  After the silent-kernel
normal form, the correct zero-cost limit is:

```math
\mathsf S_\infty U_\infty=0,
\qquad
d\Omega_\infty^{top\text{-}strain/ex}=0,
\qquad
dD_\infty^{vis/rad}=0
\quad\Longrightarrow\quad
A_\infty=0.
\tag{2}
```

That is the local theorem already used by the augmented stopped trace note.  It
does not prove the global Gold action bound.

## 1. What the six currencies prove

Carleman / boundary observability proves only the pressure-visible quotient:

```math
A_{\rm qvis}^{stop}(S)
\le
C\langle U_S,\mathsf S_S^{stop}U_S\rangle
+CR_{\rm legal}(S).
\tag{3}
```

Adjoint polarization is the same statement in dual form.  It can expose a
polar-null packet, but it cannot make a boundary-flat pressure potential visible.

Rellich / Pohozaev gives the same local quotient through a selected-sign collar
identity:

```math
\int \mathcal Q_{A,X}(\nabla_AW,\nabla_AW)
+
\int (X\cdot\nabla_AW)\Pi_{\rm PH}(\mu_\Sigma^{sel})
\gtrsim
c\int a^2
-R_{\rm polar}.
\tag{4}
```

When the boundary term is the stopped Schur defect, `(4)` is a proof mechanism
for `(3)` or for the terminal Zeno visible-readout survivor.  It is not a root
reserve.

Microlocal defect measures, DtN Riccati flow, and zero-defect packet
classification are strongest when stated as the augmented classification `(2)`.
They rule out a retained selected packet with zero visible trace and zero
same-material exchange/viscous/radius charge.  They do not sum the exchange
charge over the stopped tree.

So the local multicurrency conclusion is:

```math
\sum_{S'\in ch_{\rm stop}(S)}A(S')
\le
C\langle U_S,\mathsf S_S^{stop}U_S\rangle
+C\Omega_S^{top\text{-}strain/ex}
+CD_S^{vis/rad}
+CR_{\rm stop}(S)
+CR_{\rm legal}(S).
\tag{5}
```

This is real support.  It is not the Gold closure.

## 2. Why the route takes long

After summing `(5)`, the visible Schur part telescopes through capacity.  The
remaining unclosed term is:

```math
\sum_{S\subseteq P}
\bigl(
\Omega_S^{top\text{-}strain/ex}
+D_S^{vis/rad}
\bigr)
\le
C_N(u_0)\mathcal R(P)+R_{\rm legal}(P).
\tag{6}
```

That is `StoppedFullExchangeActionCarleson.A`, equivalently
`SelectedCompressionRootReserve.A`.

The selector-stratum BV note already shows unpaid reselection is not an
independent second wall:

```math
R_{\rm stop}^{unpaid}(P)
\le
C_N
\sum_{S\subseteq P}
\bigl(
\Omega_S^{strain/ex}+D_S^{vis/rad}
\bigr)
+R_{\rm legal}(P).
\tag{7}
```

The native-birth nonreuse note also closes same-chain reuse: spent-source
projection prevents strict selected descendants from reading an already-spent
source direction for free.  The fresh orthogonal half-tail still survives:

```math
\|F_\ell\|_{\mathcal H_{raw}}^2={2^{-\ell}\over \ell+1},
\qquad
\sum_\ell\|F_\ell\|_{\mathcal H_{raw}}^2<\infty,
\qquad
\sum_\ell2^\ell\|F_\ell\|_{\mathcal H_{raw}}^2=\infty.
\tag{8}
```

Thus the hard part is not local invisibility, local selector crossing, or
same-chain nonreuse.  The hard part is to produce the selected heat-scale weight
from original material history before the future selected tail is named.

## 3. Current nonduplicate live edge

The checked reduction chain is:

```text
StoppedFullExchangeActionCarleson.A
<= SelectedCompressionRootReserve.A
<= SelectedScaleNativeReserveAtom.A
<= MovingLowHighInterfaceLowerEdge.A
<= PrimitivePLSNoFreeUpcrossing.A / PrimitivePLSBoundedBelowLyapunov.A.
```

In the transition language, the same wall is:

```text
CriticalWeightedParentChildResetCarleson.A
+ LogScaleResetDerivativeCarrierBound.A.
```

The record/return language gives the working proof method:

```text
StoppedPrimitivePLSSignedScalePotentialLift.A
+ StoppedPotentialRecordAverageBound.A
+ StoppedNegativeScaleReturnCharge.A.
```

These are not separate goals.  They are the same first-ratio gain seen from
three coordinates:

1. selected-scale native atom;
2. moving low-high primitive interface;
3. stopped scale-potential record/return.

## 4. Method choice after the agent pass

The next attack should not be another positive-forward local detector.  The
method should be a minimal-bad-chain / monovariant argument on the stopped scale
potential.

After paid removals, the selected positive primitive source must be lifted before
positive projection:

```math
Y=\partial_\sigma\Psi+S^{spill}+E,
\qquad
Y_+=Y+Y_-.
\tag{9}
```

On a stopped ancestry line, the scalar variation identity is:

```math
V_+(\Psi;I)
=
\Psi(t_1)-\Psi(t_0)+V_-(\Psi;I).
\tag{10}
```

Therefore a divergent selected positive half-tail forces one of two events:

```math
\sum a_m=\infty
\quad\Longrightarrow\quad
\operatorname{Rec}(\Psi)=\infty
\quad\text{or}\quad
V_-^{return}(\Psi)=\infty,
\tag{11}
```

up to spill/legal terms.  The Gold proof now needs the PDE version of `(11)`:
records are bounded from original data, and negative return is charged to the
same-material exchange / selector / collar / donor / legal ledgers.

## 5. Refined theorem statement

The next theorem should be stated as:

```text
PrimitivePLSRecordReturnHalfTailKill.A.
```

Statement: after spent-source projection, silent-source normal form, quotient
visible trace payment, selector/reselection BV payment, entrance/legal/subheat
removals, and fixed-carrier support removals, the stopped primitive selected
source has a signed scale-potential lift `(9)` such that

```math
\sum_{Q\subseteq P}A_{\rm sel}^{primitive,+}(Q)
\le
C_N\left(
\operatorname{Rec}_P(\Psi)
+V_{-,P}^{return}(\Psi)
\right)
+R_{\rm legal}(P),
\tag{12}
```

with

```math
\operatorname{Rec}_P(\Psi)
+V_{-,P}^{return}(\Psi)
\le
C_N(u_0)\mathcal R(P)+R_{\rm legal}(P).
\tag{13}
```

Equivalently, construct a bounded-below primitive PLS Lyapunov variable:

```math
d\mathscr A_N
+c_Nd\Omega_N^{PLS,primitive}
\le
dR_N,
\qquad
\mathscr A_N\ge -C_N(u_0),
\qquad
\int dR_N<\infty.
\tag{14}
```

Equations `(12)`--`(14)` kill the half-tail `(8)`.  The invisible-child
currencies are then support inputs inside the stopped lift, not the final
producer.

## Result

The parallel subagents did not produce an independent closure route.  They
confirmed that all invisible-child currencies are local sieves.  The proof work
should now stay on the strict primitive PLS high-ratio tail:

```text
PrimitivePLSNoFreeUpcrossing.A
+ PrimitivePLSBoundedBelowLyapunov.A
+ StoppedPrimitivePLSSignedScalePotentialLift.A
+ StoppedPotentialRecordAverageBound.A
+ StoppedNegativeScaleReturnCharge.A.
```

That is why the Gold route is slow: the local PDE currencies have been converted
into support, and the remaining wall is the global one-way selected
scale-potential upcrossing budget from original history.
