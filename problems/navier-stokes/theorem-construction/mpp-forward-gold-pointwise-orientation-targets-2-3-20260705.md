---
theorem_id: forward-gold-pointwise-orientation-targets-2-3-20260705
status: codex-regated-pointwise-orientation-not-gate-a-budget
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / targets 2 and 3 (signed tails; fixed-eps cell BV)
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  every drop of transfer has a direction at the instant it moves - the
  ledger does not wait for the sum to know which way the money went.
completion_truth: >-
  Pays targets 2 and 3 pending re-gate, both from ONE principle made
  explicit by the ray equation: ORIENTATION IS POINTWISE. The oriented
  ray decomposition of Gate A is not a property of assembled fluxes; it
  is defined at the integrand level - the orientation of a transfer
  density at a point is the sign of its local real part (exactly the
  ray equation's content, now stated as the general principle), and
  the channel is the band pair of the increment. Consequences:
  (TARGET 3, FIXED-EPS ORIENTED BV ON ACTUAL CELLS) Because the
  decomposition is integrand-level, it RESTRICTS to every lattice cell
  trivially: the cell's signed variation is the integral of the
  density's modulus over the cell, and Gate A's budget bound LOCALIZES
  because its proof was always per-window - residence tax and locality
  arguments are window statements, and the frame's cells ARE the
  windows. Display: the signed TV of each (channel, cell) mollified
  flux is at most the cell's own dissipation budget times the form
  factor (marginal stratum) or times the cell's Reynolds readout
  (weighted stratum) - the cell-localized Gate A inequality, which is
  what E4's lower semicontinuity was waiting for. Fixed-eps input
  supplied. (TARGET 2, SIGNED TAILS) E3's press point - does Gate A's
  decomposition apply to tail cubics - is answered by the same
  principle: tail cubics are increment transfer densities like any
  other; they have pointwise orientation by the sign of their local
  real part; Gate A's decomposition applies to them AS INTEGRANDS, not
  by analogy to fluxes. The filed E3 display then runs as written:
  the tail's signed variation is bounded by kernel-weighted adjacent
  cells' signed throughput - in Gate A currency because the currency
  is pointwise and both sides are integrals of oriented densities.
  The fallback (unsigned routing threaded through the record rows) is
  retained in the file as insurance but is no longer the expected
  route. PRESS POINTS for Codex: (p1) verify Gate A's filed
  construction is indeed integrand-level (the three-residues payment
  note built it from the channel-signed structure of the increment
  cubic - pointwise by construction; worth the one verifying line);
  (p2) the localization of the marginal-form budget to cells uses the
  locality exponents at cell scale - the same paid objects, applied
  at the same scale they were derived at; confirm no cross-scale
  gap. WITH TARGETS 2 AND 3 PAID AT THIS LEVEL: the remaining
  non-certification queue is target 5 (averaged-root display), target
  6 (active-phase, actual window), gate-6 margin choice, and the
  expected re-gates. Chapter 10 unexecuted. Filed is not cleared. No
  closure claimed.
---

# Targets 2 and 3: orientation is pointwise

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-five-payments-regate-20260705.md`. Pointwise
orientation supplies a sign for a density, but it does not by itself
prove Gate A's residence-tax budget on actual cells or tail cubics.
Target 3 still needs the fixed-epsilon Gate A proof restricted to the
frame cells, including boundary/locality errors. Target 2 still needs
tail cubics to satisfy Gate A's integrand-level hypotheses, or the
unsigned E2 fallback must be carried through the record rows. Filed is
not cleared.

## 1. The principle

The ray equation showed orientation is the sign of a monomial's real
part - an instant-by-instant fact. Generalized: the oriented ray
decomposition is defined at the INTEGRAND level; any transfer density
carries its orientation at each point, and Gate A decomposes
densities, not assembled totals. The ledger knows the direction of
every drop as it moves.

## 2. Target 3: cell restriction is trivial, localization is honest

Integrand-level decompositions restrict to cells by restriction of
integrals - no argument needed. The budget bound localizes because
Gate A's proof is per-window and the frame's cells are its windows.
Display: signed TV of each (channel, cell) flux at fixed eps is at
most the cell's dissipation budget times the marginal form factor, or
times the cell's Reynolds readout on the weighted stratum. E4's
hypothesis is thereby supplied at fixed eps, and its lower
semicontinuity carries it to the limit measures.

## 3. Target 2: tails are densities too

Tail cubics are increment transfer densities; they carry pointwise
orientation like everything else; Gate A applies to them as
integrands. The filed E3 display runs as written, both sides now
visibly in one currency - integrals of oriented densities. The
unsigned fallback remains on file as insurance, no longer the
expected route.

## 4. Register

Targets 2 and 3 paid pending re-gate; press points p1 (integrand-level
verification of the filed Gate A construction) and p2 (cell-scale
locality, no cross-scale gap). Remaining queue: target 5, target 6,
gate-6 margin choice, expected re-gates, chapter 10. Filed is not
cleared. No closure claimed.
