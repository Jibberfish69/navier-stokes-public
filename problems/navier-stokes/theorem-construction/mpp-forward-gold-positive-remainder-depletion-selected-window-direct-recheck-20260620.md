# MPP Forward Gold PositiveRemainderDepletion.A Selected-Window Direct Recheck

## Status

Direct recheck of `PositiveRemainderDepletion.A` after the heat-scale
time-face pulse reduction.

This note tests whether the positive localized pre-Cauchy remainder can be
depleted on selected terminal windows using the installed Navier-Stokes ledgers.
It cannot.  The installed post-ASAC note proves a conditional consumer only:
once `ZenoSourceResidueRigidity.A` or an equivalent terminal source anti-atom
theorem is supplied independently, `PositiveRemainderDepletion.A` follows.

The direct supplier is not installed.

## Target

For a bounded-overlap same-fluid terminal family `\mathcal F_N`, the desired
depletion estimate is

```math
\sum_{P\in\mathcal F_N}\int_{Q(P)}[\mathcal R_P]_+
\le
\theta\sum_{P\in\mathcal F_N}\int_{Q(P)}\mathcal D_P
+
\operatorname{Drain}_{parent}(\mathcal F_N)
+
\operatorname{Loss}_{legal}(\mathcal F_N)
+o_N(1),
\qquad 0<\theta<1.
```

The live main carrier is the selected positive strain-source product

```math
[\mathcal R_P^{main}]_+
=
\chi_P |w_{j_P}|^2
\left[e_{j_P}\cdot S_{<j_P}^{loc}e_{j_P}\right]_+.
```

This is a one-sided selected object.  Signed stress balance, trace-free strain,
and unweighted dyadic skew symmetry do not control it after localization,
weights, lifts, terminal selection, and positive-part extraction.

## Installed Source State

The post-ASAC conditional closure proves only

```math
ASAC.A
+ProjectedCutoffLedger.A
+LegalBoundaryLedger.A
+LocalDonorBalance.A
+EntranceLeafDecay.A
+ZenoSourceResidueRigidity.A
\Longrightarrow
PositiveRemainderDepletion.A.
```

The direct PRD attempt reached the active strain/source product

```math
|w_j|^2\left[e_j\cdot S_{<j}^{loc}e_j\right]_+
```

and found no installed packetwise sign-depletion, selected angular mixing, or
scale-critical active-square reserve theorem.

The adjoint escape route detects the terminal atom, but it stops at the same
conversion:

```math
\text{signed adjoint source work}
\Longrightarrow
\text{one-sided positive native source carrier}.
```

That conversion is not installed.

The square-reserve route gives the equivalent signed-current presentation

```math
SignedPositiveBalance.A,
```

or, in older terms,

```math
TerminalSignedSaturation.A
\Longrightarrow
WeightedLiftedSkewDefectLegal.A
\Longrightarrow
PositiveRemainderDepletion.A.
```

That signed-positive balance theorem is also not installed.

## Selected-Window Test

On one active packet, take the low strain normal form

```math
S_{<j}^{loc}(t,x)
=
\lambda(t)
\begin{pmatrix}
2&0&0\\
0&-1&0\\
0&0&-1
\end{pmatrix}
```

on the packet core, with cutoff errors routed to the legal ledger.  This is
trace-free and is locally generated by a divergence-free low mode before the
cutoff.

Choose the high packet direction

```math
e_j=e_1.
```

Then

```math
\left[e_j\cdot S_{<j}^{loc}e_j\right]_+
=
2\lambda(t)_+,
```

so the selected positive carrier is

```math
[\mathcal R_P^{main}]_+
=
2\chi_P\lambda(t)_+ |w_j|^2.
```

The negative trace directions are present in the signed strain tensor, but they
are not retained by the selected carrier on this packet.  The positive-part
operation has kept the expanding eigendirection and discarded the signed
partner needed for cancellation.

After parabolic scaling at radius `r`, a normalized profile with amplitude `A`
has

```math
u_r(t,x)=r^{-1}A\,U((x-x_r)/r,(t-t_r)/r^2),
```

and on a heat window `Q_r`

```math
\int_{Q_r}[\mathcal R_r^{main}]_+ \simeq A^3 r,
\qquad
\int_{Q_r}\mathcal D_r \simeq A^2 r.
```

The physical first moment tends to zero as `r\to0` for fixed `A`, while the
ratio of positive selected source to drain is proportional to `A`.  Thus finite
energy and physical `L^1_t` source control do not give the strict packet
depletion constant `0<\theta<1`.

At the endpoint, this is the same heat-scale time-face pulse:

```math
\sum_m r_m A_m <\infty
\quad\text{does not imply}\quad
\sum_m A_m<\infty,
\qquad r_m\downarrow0.
```

The radius factor discounts the physical ledger.  PRD needs the undiscounted
selected-window reserve.

## Why The Existing Ledgers Do Not Pay It

`LocalDonorBalance.A` and `EntranceLeafDecay.A` pay finite donor trees.  They do
not pay an infinite terminal Zeno donor chain.

`ProjectedCutoffLedger.A` and `LegalBoundaryLedger.A` pay cutoff, collar,
projection, and boundary exits.  They do not pay the selected interior
expanding-strain product.

`ASAC.A`, where supplied, pays active alignment only after the residual
absorbability, no-incoming, and backward-uniqueness inputs used by the ASAC
package.  It does not independently prove terminal source anti-atomicity.

Signed skew cancellation holds before terminal weights, lifts, localization,
and positive-part selection.  The selected weighted positive carrier remains
unless a signed saturation theorem forces its partner to remain in the same
ledger or charges the loss to a legal terminal cost.

## Exact Reduction

At this resolution,

```math
PositiveRemainderDepletion.A
```

is not a new forward-gold supplier.  It is equivalent to one of the following
uninstalled suppliers:

```math
SelectedCriticalStrainCarleson.A,
```

```math
SourceSquareReserve.A
+SelectedSourceSquareCarrierDomination.A,
```

```math
StrictRescaledNoWasteLyapunov.A,
```

```math
SelectedPositivePolarSaturation.A
+NoFreeTerminalZenoDonorChain.A,
```

or a rigid source-residue production theorem giving

```math
ZenoSourceResidueRigidity.A.
```

## Verdict

`PositiveRemainderDepletion.A` remains a conditional consumer, not an installed
supplier.

The direct selected-window obstruction is:

```math
|w_j|^2\left[e_j\cdot S_{<j}^{loc}e_j\right]_+
```

on a heat-scale terminal packet.  Existing ledgers see its physical
radius-discounted cost, while the forward-gold anti-atom theorem needs
undiscounted selected-window control.

The live forward-gold source-control wall is therefore the same wall exposed by
the six-agent recheck:

```math
UnweightedTerminalCriticalActionReserve.A
```

or an equivalent selected source-square, strict no-waste, signed-saturation, or
rigid source-residue production theorem.
