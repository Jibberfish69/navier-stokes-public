---
theorem_id: forward-gold-predictable-root-realization-branch-classification-20260627
status: realization-reduced-to-repeated-core-payment-after-finite-score-reset
created: 2026-06-27
problem: navier-stokes
route: forward-gold same-material selected-density / affine quotient transition payment
classifies_hinge:
  - PredictableRootWeightMartingaleRealization.A
  - AffineQuotientTransitionPayment.A
  - StoppedPotentialRecordAverageBound.A
  - StoppedNegativeReturnOrientedPayment.A
  - LogScaleResetDerivativeCarrierBound.A
  - CriticalWeightedParentChildResetCarleson.A
source_surfaces:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-stopped-lifted-transition-bessel-consumer-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-original-scale-memory-storage-to-affine-quotient-transition-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-affine-transition-predictable-weight-martingale-lock-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-stopped-affine-quotient-transition-square-packing-direct-attack-20260627.md
effect: >-
  Records the live branch classification after the Bessel consumer proof and
  the repeated-core / fresh-reset audits, updated after the finite-score
  selector proof. The Gold hinge is the realization of the predictable root
  Hilbert geometry and lifted martingale-difference split from the original
  same-material history. The Bessel/Pythagoras step is a consumer only. The
  fresh-reset selector branch is installed for order-locked Gold selectors; the
  repeated-core record/negative-return payment remains open.
---

# Predictable root realization branch classification

The lower-triangular scale-memory split leaves a rank-one affine quotient.
The Bessel note proves only this consumer statement:

```text
common predictable root Hilbert geometry
+ lifted martingale-difference split
  -> StoppedLiftedTransitionBesselCarleson.A.
```

It does not construct the common root geometry, the stopped affine spans, or
the split

```math
\mathcal C_\theta[Z_e]=B_e+D_e+R_e.
```

Thus the live theorem remains:

```text
PredictableRootWeightMartingaleRealization.A.
```

## 1. Realization theorem

For every retained stopped root \(P\), construct from the original
same-material history, before terminal selected positive readout:

1. a common parent-predictable root Hilbert measure;
2. parent affine spans;
3. a legal transition decomposition
   \(\mathcal C_\theta[Z_e]=B_e+D_e+R_e\);
4. stopped martingale-difference orthogonality for \(D_e\);
5. a bounded transition transform from edge weights to affine-quotient readout;
6. branch routing of \(B_e\) to repeated-core record/return and \(D_e\) to
   critical reset charge.

Once these are installed, the Bessel consumer applies. Until these are
installed, the Bessel consumer cannot be promoted to Gold closure.

## 2. Repeated-core branch status

The inherited predictable part \(B_e\) routes to repeated-core record/return:

```math
\sum_e B_e
\leadsto
\operatorname{Rec}_\infty(\Psi)+V_-^{return}(\Psi).
```

The repeated-core branch remains open. The two missing payments are:

```text
StoppedPotentialRecordAverageBound.A
```

and

```text
StoppedNegativeReturnOrientedPayment.A
```

with the scale-return form

```text
StoppedNegativeScaleReturnCharge.A.
```

The noncircular repeated-core mechanism is a stopped BV monovariant for one
material scale potential:

```math
Y_+=Y+Y_-,
\qquad
V_+(\Psi;I_M)=\Psi(t_M)-\Psi(t_0)+V_-(\Psi;I_M).
```

The missing repeated-core estimate is:

```math
\int_0^T H_w(t)
\left\|{1\over |J|}\int_J\Psi(\sigma,t)\,d\sigma\right\|_{L_x^2}^2dt
+
\int_0^T H_w(t)\|\mathcal C_\theta[Y_-](t)\|_{L_x^2}^2dt
\le
C_N(u_0)\mathcal R(P)+R_{\rm legal}(P).
```

## 3. Fresh-reset branch status

The fresh innovation part \(D_e\) routes to critical reset charge:

```math
\sum_e\int H_{e^-}\|D_e\|^2
\leadsto
\sum_{Q\to Q'}J_{\rm crit}(Q\to Q').
```

The selector-predictability part of the fresh-reset branch is now installed for
order-locked Gold selectors. The active theorem package is:

```text
LogScaleResetDerivativeCarrierBound.A
+
CriticalWeightedParentChildResetCarleson.A.
```

It is installed through:

```text
StoppedSelectorFiniteScoreOrPaidDrift.A
+
finite parent-known stopped scores
+
first-exit reset derivative representation
+
bounded-overlap witness slabs
+
active-weight Cauchy cancellation.
```

The excluded case is:

```text
future-selected moving supremum.
```

It is not a retained Gold selector transaction. It pays selector/collar visible
variation, heat-scale return failure, tie-gap/drift charge, order-lock partner
deficit, legal loss, donor loss, or reselection loss.

## 4. Current Gold status

The current route is:

```text
PredictableRootWeightMartingaleRealization.A
  -> StoppedLiftedTransitionBesselCarleson.A
  -> StoppedAffineQuotientTransitionSquarePacking.A
  -> AffineQuotientTransitionPayment.A
  -> ParentDrop / selected-density Carleson
  -> finite same-material continuation clock.
```

The installed results are the Bessel consumer under its hypotheses and the
finite-score fresh-reset selector branch for order-locked Gold selectors. The
remaining branch payment is repeated-core record/negative return, integrated
with the common predictable root Hilbert realization. These support anchors are
not full Gold closure by themselves.
