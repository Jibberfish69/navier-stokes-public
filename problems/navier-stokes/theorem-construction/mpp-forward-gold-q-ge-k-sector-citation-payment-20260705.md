---
theorem_id: forward-gold-q-ge-k-sector-citation-payment-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / paying the f1-slack note's owed citation
authorship: Claude (Fable), 2026-07-05. Nothing new is asserted here:
  every line is a citation to a filed object, two of them in Codex's
  own hand. Physical story Thomas Birnie: the helper never outweighs
  the payer - the same ordering that killed the zero set now holds
  the fence's posts up. One fact, two payments.
completion_truth: >-
  Pays the citation owed by the f1-slack note (section 5): the slack
  inequality's condition a = q - k >= 0 is a SECTOR-DEFINING FACT of
  the campaign family, not an extra hypothesis. FOUR CITATIONS, each
  to a filed object. (1) Codex's own sector-citations note
  (codex-sector-citations-g-ratio-reduction, section 1): "On the
  branch used for the reverse predator, the waste side is not larger
  than the child side: k <= q" - stated as one of the sector
  citations demanded by the previous Codex re-gate, and used twice
  downstream in the same note (S_relay >= p and S_pred >= mu both
  rest on it). (2) The zero-set convention-fixation note, section 1b
  (the demanded citation line): predator sector pair (k(-), q(+))
  with k <= q BY ADJACENCY; the population and shape notes carry the
  same inequality. (3) Same section: the relay sector is the
  catalyst-flipped branch with catalyst ratio k/q <= 1 floored by
  the appetite root a*; the anchor's cleared sector material has the
  branch classification k < p < q - STRICT on the relay branch,
  which is the branch whose triangle (catalyst k, child q) supplies
  F1's chords: on F1's own row the slack is strict. (4) Codex's
  sector-admissible-slice note introduces the family's angular cap
  arccos(k/(2q)) under k < q - the slice defining the admissible
  angles already carries the ordering. CONSEQUENCES for the f1-slack
  note: (a) the condition q >= k holds on EVERY admissible piece -
  the case-split pastes to a single case; there are no k > q pieces;
  the G-floor governs the entire F1 row; (b) the crude factorwise
  pass floor is no longer needed anywhere in the F1 row (it remains
  Codex's standing global substitute, untouched, simply unused
  here); (c) at any predator-side equality piece k = q the financing
  vanishes and F1 = G exactly - the floor is attained, and the
  inequality was filed non-strict, so no gap opens. The same sign
  structure that excluded the weighted-sum zero set - the helper
  never outweighs the payer - finances the slack inequality: one
  sector fact, two payments. PRESS POINTS: none new - every line
  cites a filed object; the re-gate's job is checking the four
  pointers. QUEUE: this note's re-gate together with the f1-slack
  note's (its owed item now paid); THE FACTORWISE EVALUATION over
  the completed floor lists (the final symbolic unit); sheet-2
  expansion; chapter 10. Filed is not cleared. No closure claimed.
---

# The ordering was always a sector fact

## 1. The owed item

The f1-slack note's floor F1 >= G = c(phi-w)/c(phi) rests on the
slack inequality, whose condition is a = q - k >= 0. The note named
the citation as owed. It is paid here, by reference.

## 2. The four citations

(1) Codex's sector-citations note (codex-sector-citations-g-ratio-
reduction, section 1): "On the branch used for the reverse predator,
the waste side is not larger than the child side: k <= q." Stated as
a demanded sector citation; used twice downstream in the same note
(S_relay >= p, S_pred >= mu).

(2) Zero-set convention-fixation note, section 1b: predator sector
pair (k(-), q(+)) with k <= q by adjacency; the population and shape
notes carry the same inequality.

(3) Same section 1b plus the anchor's cleared sector material: the
relay sector is the catalyst-flipped branch, catalyst ratio
k/q <= 1 floored by the appetite root, branch classification
k < p < q - STRICT on the relay branch. F1's chords are built on
the relay triangle's sides (catalyst k, child q): on F1's own row
the slack is strict.

(4) Codex's sector-admissible-slice note introduces the family's
angular cap arccos(k/(2q)) under k < q: the slice that defines the
admissible angles already carries the ordering.

## 3. Consequences

The condition holds on every admissible piece: the f1-slack note's
case-split pastes to a single case; no k > q pieces exist; the
G-floor governs the entire F1 row; the crude pass floor stays on
the record but is unused here. At any equality piece k = q the
financing vanishes, F1 = G exactly, the floor is attained - the
inequality was filed non-strict, so no gap opens.

The helper never outweighs the payer: the same ordering that
excluded the weighted-sum zero set holds the fence's posts up. One
fact, two payments.

## 4. Register

Owed citation paid by four pointers, nothing new asserted. Queue:
re-gate (with the f1-slack note's); THE FACTORWISE EVALUATION;
sheet-2 expansion; chapter 10. Filed is not cleared. No closure
claimed.
