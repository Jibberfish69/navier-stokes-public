---
theorem_id: forward-gold-ramp-rectification-proof-backscatter-instability-20260705
status: proved-structural-with-mechanism-correction-elliptic-sector-protected-hyperbolic-sector-pays
created: 2026-07-05
problem: navier-stokes
route: forward-gold / audit target G / ramp rectification
authorship: Claude (Fable), 2026-07-05. Lane law in force: every step below
  is an exact sign, an exact share, an exact scaling exponent, or a
  compactness statement. The physics is Thomas Birnie's spring: the
  payback direction is upscale, and the theorem says the enemy's own
  waste arms the payback.
completion_truth: >-
  Audit target (G) is PROVED, and the proof CORRECTS the mechanism as
  stated in the theory-currency reshape note. The reshape note said the
  waste ramp rectifies the predator's spring by adiabatic growth of its
  swing. That picture is WRONG as mechanism: in the exact reduced
  dynamics, the sector of the reverse cone where the small pair merely
  precesses under the child background (the elliptic sector) is
  ADIABATICALLY PROTECTED - a slow source driving a fast rotation
  transfers only at second order in the clock ratio, in BOTH directions;
  that sector neither taxes nor refunds at leading order, and no ramp
  rectifies it. The true payer is the HYPERBOLIC SECTOR: by an exact sign
  computation, the upscale partners (same helicity as the child, smaller
  wavenumber) make the reverse triad DECAY-UNSTABLE with the child as
  source; on that open sub-cone three exact facts hold at once: the
  coefficient product is positive (instability), the child pays the exact
  share (1 + mu/q)/2 > 1/2 of every transferred unit (invariant
  identity), and the relay's own waste mode is one leg of the unstable
  pair (seeding). The ramp's true role is SEEDING, not rectification: the
  relay feeds the unstable leg continuously with its own phase, so the
  instability cannot be tuned away at an instant (it is re-seeded at the
  next) and cannot be phase-starved (relay-drift lock). Rectification is
  then automatic because instabilities do not average out: growth locks
  to the unstable eigenvector. The clock-ratio theorem makes the
  instability exponent per step diverge like 1/sqrt(2 delta) under
  refinement, while the seed's smallness only costs a logarithm; a square
  root beats a logarithm structurally, so below a structural chop
  threshold the reverse swing COMPLETES: the child pays an order-one
  Manley-Rowe share, exported upscale, off the descending lineage - a net
  tax by the lifted no-recount law. CONSEQUENCES: the fine-chop closure
  STRENGTHENS (from tax-scales-with-progress to saturating order-one tax
  below the structural threshold); the quantitative stratum NARROWS to
  genuinely intermediate chop numbers where the exponent is moderate,
  with the new exact fact that the drain is monotone in the exponent; and
  the reshape note's clause (iii) carries a correction pointer to this
  note. Honest boundary: the sector decomposition and shares are exact in
  the helical normal form (R2 carries the lift, as everywhere); the
  intermediate-exponent prefactor remains quarantined. No closure
  claimed.
---

# Ramp rectification, proved - and corrected in the proving

## 1. The audit question

The reverse predator is itself a three-wave spring: periodic, so
naively it returns what it takes. The reshape note claimed the waste
ramp rectifies it. PROVE IT was audit target (G). The proof below finds
the claim TRUE but the mechanism MISATTRIBUTED, and files the
correction in the same motion.

## 2. Exact sector decomposition of the reverse cone

Reduce the reverse triad (k(-), q(+), mu) with the child q as
background. The small pair (a_k, a_mu) obeys a linear system whose
character is decided by the sign of the coupling product g_k g_mu, with
g_k proportional to (s_mu mu - s_q q) and g_mu proportional to
(s_k k - s_q q) = -(k + q) < 0. Hence, exactly:

- s_mu = +1, mu < q: product positive - DECAY-UNSTABLE (hyperbolic).
- s_mu = -1 (any mu): product positive - unstable likewise.
- s_mu = +1, mu > q: product negative - ROTATION (elliptic).

The unstable set is an open sub-cone; on any compactly contained
subcone the rate Omega = sqrt(g_k g_mu) a is uniformly positive by
compactness. No number is needed anywhere.

## 3. The elliptic sector is protected (mechanism correction)

On the rotation sector the pair precesses at the fast rate Omega = g a
while the relay sources it at the slow rate q a sqrt(2 delta). A slow
source against a fast rotation is an oscillatory integral: transfer is
second order in the clock ratio, in both directions. The elliptic
partners neither tax nor refund at leading order - and NO ramp
rectifies a protected rotation. The reshape note's "adiabatic growth of
the swing" placed the mechanism in this sector; that placement is
withdrawn. The adiabatic theorem here works FOR the enemy - and it does
not matter, because of section 4.

## 4. The hyperbolic sector pays, by three exact facts at once

On the sub-cone s_mu = +1, mu < q:
(i) INSTABILITY: coefficient product positive (section 2); the pair
(a_k, a_mu) grows exponentially at rate Omega from any seed - and
growth locked to an unstable eigenvector does NOT average out. No
rectifier is needed; instability is its own rectifier.
(ii) EXACT SHARE: the two invariants give, for k adjacent to q,
Delta E_q = -((1 + mu/q)/2) Delta E_mu: the child pays MORE THAN HALF
of every unit landing on the partner. Backscatter and drain are the
same exact bookkeeping.
(iii) SEEDING: the unstable pair's k-leg IS the relay's waste mode. The
ramp feeds it continuously, with the relay's own phase. Zeroing the
seed at an instant is useless (re-seeded the next instant); steering
the seed onto the stable eigendirection continuously requires tangency
at moving amplitudes - forbidden by the relay-drift lock. The seed is
structural.

The transfer direction is upscale (mu < q): the payback direction. The
exported energy leaves the descending lineage and re-enters only
through the oriented admission gate, re-paying tolls: a NET tax by the
lifted no-recount law. The spring pays back through the enemy's own
waste - the vortex-spring, exact.

## 5. The exponent comparison: square root beats logarithm

Per step, the instability exponent is Omega T, and the clock-ratio
theorem gives Omega T proportional to 1/sqrt(2 delta). Completing the
Manley-Rowe swing from a seed of relative size delta costs an exponent
of order ln(1/delta). Since 1/sqrt(delta) dominates ln(1/delta)
structurally, there is a structural chop threshold below which the
swing COMPLETES within the step: the child pays an ORDER-ONE invariant
share per step. Fine chopping does not merely fail to dilute the tax -
beyond the threshold it maximizes it. The enemy's only knob arms the
predator exponentially; the participation throttle in its sharpest
form.

## 6. What remains quantitative (narrowed, sharpened)

Intermediate chop numbers, where the exponent is moderate: there the
drain is MONOTONE in the exponent (comparison of forced-instability
solutions - larger exponent, larger swing, exact), and the order-one
prefactor question is unchanged and stays quarantined in the
certification stratum. The stratum is now bracketed on BOTH sides by
structure: the corner by the exact one-step fraction, the deep-fine
regime by the completed swing.

## 7. Self-test (why real cascades survive this theorem)

An efficient coarse relay has order-one exponent per step: the
predator gets no time to grow, drains only its seed's worth, and the
cascade proceeds - turbulence is safe. The predator devours only SLOW
relays, and slowness is exactly what chopping buys. The theorem taxes
the enemy's strategy, not the fluid's ordinary life. This asymmetry is
the physical story doing the selecting: participation punishes the one
who stalls.

## 8. Filing consequences

Reshape note clause (iii): correction pointer to this note (mechanism
reattributed from adiabatic rectification to seeded instability;
conclusion strengthened). Codex audit target (G): answered - the
deepest-target warning there ("if the adiabatic argument leaks, the
fine-chop closure retreats") is RESOLVED: the adiabatic argument does
leak, in the enemy's favor, in the elliptic sector - and the closure
does not retreat, because the payer was never the elliptic sector.
Handed to Codex with the rest of the structural package.
