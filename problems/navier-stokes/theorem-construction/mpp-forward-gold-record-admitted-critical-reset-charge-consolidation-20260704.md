---
theorem_id: forward-gold-record-admitted-critical-reset-charge-consolidation-20260704
status: strict-reduction-and-checked-obstruction
created: 2026-07-04
problem: navier-stokes
route: forward-gold signed-critical-height / record-admitted reset and scale-native carrier
logical_landing_node: SignedCriticalHeightSignPersistenceOrPeakBound.A
target_object: record-admitted form of CriticalWeightedParentChildResetCarleson.A
role: >
  Consolidates the shrinking-core reset wall with the signed-height record-menu
  wall. A freestanding critical reset Carleson theorem is support unless the
  reset edge is the parent-announced carrier of signed-height record work before
  readout. The direct reset Cauchy estimate pays derivative-exact increments
  only after record-menu/root/active-face visibility has been proved.
source_refs:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-shrinking-core-critical-reset-carleson-sharpener-20260704.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-parent-child-reset-critical-weight-countertest-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-announceable-active-weight-reset-carleson-cauchy-reduction-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-active-weight-bad-profile-compactness-tightness-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-signed-height-record-upcrossing-criterion-20260703.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-signed-height-record-net-work-admission-proof-pass-20260703.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-record-meter-entropy-flux-split-same-history-capacity-reduction-20260703.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-signed-height-record-admitted-scale-native-depletion-bridge-20260704.md
completion_truth: >-
  This note proves no smoothness theorem and does not prove
  CriticalWeightedParentChildResetCarleson.A. It proves a stricter custody
  reduction: the reset branch has Gold force only in record-admitted form, and
  that form reduces to record-menu root/active-face visibility plus the
  simultaneous record-admitted active/full-exchange coupled storage theorem.
  Raw reset/capacity and derivative Cauchy alone remain checked obstructions or
  conditional support.
---

# Record-admitted critical reset charge consolidation

## 1. Same physical packet

There is one original Navier-Stokes velocity field. Transport moves the same
fluid packet; pressure and incompressibility re-coordinate its work; viscosity
spends raw heat-scale energy; the selected critical readout sees high-ratio
work at smaller scales; and the signed critical height records whether that
work raises

\[
H(t)={1\over2}\|\Lambda^{1/2}u(t)\|_2^2.
\]

A shrinking-core reset is not a new physical source. It is the same parent
history being read on a smaller transported core. After inherited no-reset
children are routed to repeated-core record/return, a genuine reset means the
child selected state has changed relative to the parent:

\[
Q\to Q',\qquad
D_{Q\to Q'}^{fresh}\ne0.
\]

The physical question is not whether a reset occurred. The question is whether
that reset was already visible in a parent-known critical currency before the
child or record readout selected it as positive.

## 2. What the reset branch already proved and did not prove

The half-tail stress test is decisive:

\[
\sum_\ell \|D_\ell\|_{\rm raw}^2<\infty,
\qquad
\sum_\ell H_{I_\ell}\|D_\ell\|_{\rm raw}^2=\infty.
\tag{RCR.1}
\]

So raw pressure-Hodge capacity, raw reset distance, raw collar/turnstile cost,
raw Bessel freshness, and tree disjointness cannot pay selected critical action.
They see the heat-scale \(L^2\) mass; the selected readout needs the critical
heat-scale weight.

The useful analytic statement is narrower. If a strict reset increment has a
parent-announced derivative representation

\[
D_e(t)
=
{|I_e|}^{-1}\int_{I_e}
(\partial_\sigma Z+\operatorname{turnstile}_\sigma
+\operatorname{exchange}_\sigma+\operatorname{covector}_\sigma)\,d\sigma
+Err_e^{legal}(t),
\tag{RCR.2}
\]

then the harmonic active weight is exactly the Cauchy weight:

\[
H_{I_e}(t)\|D_e(t)\|^2
\le
\int_{I_e}
w(\sigma,t)\|\mathcal G_e(\sigma,t)\|^2\,d\sigma
+R_e^{legal}(t).
\tag{RCR.3}
\]

With stopped bounded overlap, `(RCR.3)' gives the derivative-exact part of
critical reset packing. It does not prove that the non-derivative fresh quotient
has such a representation, and it does not prove that the original-history
weighted derivative energy is finite for arbitrary record-selected edges.

Thus the Cauchy step is proof only after the physical source has already been
constructed. Used before that construction, it is support.

## 3. Record-admitted reset is the Gold-relevant form

The live signed-height reduction starts from a record upcrossing:

\[
H(a)=\lambda,\qquad H(b)=2\lambda,\qquad
\int_a^b P(t)\,dt
=
H(b)-H(a)+\nu\int_a^bD(t)\,dt
\ge\lambda.
\tag{RCR.4}
\]

That is real net work of the same velocity field. A reset charge helps Gold only
when the reset edge is one of the parent-announced native work edges that carries
this record work before readout.

The record-menu pass already builds the native work coordinates before the
record interval is chosen. The open storage half is the positive-selection
reserve:

\[
\sum_j \lambda_j^{-1}
\sum_e
\left[\int_{I_e\cap[a_j,b_j]}\omega_e\right]_+
\le C_N(u_0)+Paid+Legal+Stop.
\tag{RCR.5}
\]

A record-used fresh reset is therefore a special positive menu selection. It
must enter one of the parent critical faces before the record selects it:

\[
\left[\int_{I_e\cap[a,b]}\omega_e\right]_+
\le
C_N\left(dA_e^{root,+}+d\Xi_e^{FE,act}\right)
+Legal_e+Stop_e.
\tag{RCR.6}
\]

This is `RecordMenuRootProjectionFaithfulness.A' in reset language. Once
`(RCR.6)' holds, the TFE positive-baseline, zero-baseline/endpoint, and active
full-exchange faces are lawful payment rows. Before `(RCR.6)', a reset event can
be genuine fluid work but still invisible to the parent root/active currency
that the proof is trying to spend.

## 4. Strict reduction

The record-admitted reset form is:

\[
\begin{array}{c}
\texttt{RecordMenuRootProjectionFaithfulness.A}\\
+\texttt{RecordAdmittedActiveFullExchangeCoupledStorage.A}
\end{array}
\Longrightarrow
\text{record-admitted }\texttt{CriticalWeightedParentChildResetCarleson.A}.
\tag{RCR.7}
\]

Here the first input proves that the future-positive reset/menu edge is visible
to the parent critical/root or active full-exchange detectors before readout.
The second input supplies the simultaneous storage rows with strict spectral
radius absorption:

\[
\rho(K_P^{rec})<1.
\tag{RCR.8}
\]

Together they give a finite record charge for disjoint thin records, and the
installed signed-height record-upcrossing criterion then gives peak control.

The current lower physical entrance to `(RCR.6)' is the frozen detector test:
for a retained native work amplitude \(z\),

\[
[w_{rec}(a,\xi)z]_+
\le
C_N\left(
|m_{root}(a,\xi)z|
+|m_{FE}(a,\xi)z|
\right)
+r_{legal}+r_{stop}.
\tag{RCR.9}
\]

The unrestricted tensor version is false because detector-silent transverse
trace-free directions exist. Therefore the retained theorem is the split:

\[
\texttt{RecordMenuPressureActiveOrSilentService.A}
+
\texttt{RetainedRecordEdgeExactProjectorChartLicense.A}
\Longrightarrow
\texttt{RecordMenuRootProjectionFaithfulness.A}.
\tag{RCR.10}
\]

Pressure-active pieces satisfy the detector angle. Detector-silent pieces must
already be paid by material parabolic service, source-origin exchange,
complete-frame variation, viscous/radius service, legal exit, stop material, or
the stopped primitive record/return package before the record can count them.

## 5. Current truth state

This consolidation changes the reset branch in three ways.

First, `CriticalWeightedParentChildResetCarleson.A' remains the correct
shrinking-core branch theorem after repeated-core inheritance is removed.

Second, under the signed-height roof, freestanding selected reset packing is
support. Gold uses only the record-admitted form: the reset must carry the net
work of a signed-height record event inside a parent-known carrier before
positive readout.

Third, the live noncircular wall is visibility before storage. The proof must
show that a same-fluid high-packet/low-strain native edge with positive record
work cannot hide from both the parent root detector and the active full-exchange
detector unless its silent part is already paid or routed out.

So the current reduction is strict and smaller than another raw capacity pass:

\[
\texttt{RecordMenuPressureActiveOrSilentService.A}
+
\texttt{RetainedRecordEdgeExactProjectorChartLicense.A}
+
\texttt{RecordAdmittedActiveFullExchangeCoupledStorage.A}
\Longrightarrow
\text{signed-height record charge from critical reset/scale-native edges}.
\tag{RCR.11}
\]

That is not closure. It is the honest physical landing of the reset wall inside
the single story: a record rise gives net work; the parent must see the native
edge before readout; detector-visible work is stored in the root/active faces;
detector-silent work must pay material service or return; and only then does a
critical reset charge count toward peak control.
