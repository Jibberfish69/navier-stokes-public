---
theorem_id: forward-gold-rnet-citation-check-ftheta-half-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / the r_net citation check - the fraction was already chosen
authorship: Claude (Fable), 2026-07-05. The two-dials correction
  left the seed row waiting on a citation and a choice; the re-read
  finds the choice was already made and displayed - one-half - and
  pins the citation to its exact gate. Physical story Thomas
  Birnie: the ledger already ruled that the flicker allowance is
  half the honest rate - spend half on tolerance, keep half the
  gross - and the honest rate itself is priced where the two horn
  prices cross.
completion_truth: >-
  Executes the r_net citation check owed by the two-dials
  correction, pending re-gate; NOTHING NEW IS PROVED - two findings
  from the horn-(b) chain re-read (max-of-two-prices note and its
  Codex re-gate), each cited. (FINDING 1, THE FRACTION IS DISPLAYED)
  The max-of-two-prices note's target-4 section displays the choice
  inequality verbatim: "theta at most half the net-floor rate leaves
  at least half the gross, symbols named." So f_theta = 1/2 is the
  DISPLAYED choice, and the set-once value is the displayed
  boundary: theta_amp = r_net/2 (the largest admissible threshold
  under the inequality - most favorable for the seed logarithm,
  admissible by the displayed accounting). Hence E_theta =
  ln(2/r_net). The "f_theta optimizing display" queue item is PAID
  BY CITATION - it was filed before it was asked. (FINDING 2, R_NET'S
  EXACT GATE) The net-floor rate's closed display is NOT
  comparison-ready and its blocker is precisely Codex's open item on
  the max framing: the no-gap floor is the CROSSING VALUE of the
  shallow price P_s(K) (falls with depth) and deep price P_d(K)
  (rises with depth) on the compact band - "a named positive
  symbol," positive by compactness - and Codex's
  max-financed-family re-gate demands the actual price functions on
  one depth domain, validity of each bound where the max uses it,
  and a displayed positive floor for max(P_s, P_d) with no
  common-zero point. THE THETA SIDE OF SHEET 1 THEREFORE WAITS ON
  EXACTLY ONE GATE: the max-crossing display. When it clears, r_net
  is its crossing value and E_theta = ln(2/r_net) is closed.
  (PROPAGATION) The substituted Sheet-1 inequality now reads:
  GAMMA_ANGLE x pi B_lo sqrt(3/(2 ln 2)) >= ln(2/r_net) -
  ln(1 - 2 M_req) - filed symbols (phi_min, B_lo, r_net); NO free
  dial remains (f_theta = 1/2 displayed); V_band stays at target-4's
  spacing clause, off this path. (HONESTY) The choice inequality's
  own clearance is inside the max-of-two-prices note's re-gate
  (right shape, positive floor open) - citing the display does not
  clear it; the theta side inherits that single pendency, named.
  DEPENDENCY CHAIN, stated plainly: max-crossing display (Codex's
  open item) -> r_net closed form -> E_theta; in parallel: C1
  (B_lo), the G-ratio matching check, and the pending re-gates ->
  the left side. The comparison runs when both sides' chains clear.
  PRESS POINTS: the verbatim choice-inequality citation (finding
  1); the exact-gate pin (finding 2). QUEUE: this note's re-gate;
  the max-crossing display (Codex's gate - the actual P_s/P_d
  functions and positive crossing floor); C1-C2; the G-ratio front;
  THE COMPARISON; B1-B3; chapter 10. Filed is not cleared. No
  closure claimed.
---

# The r_net check: the fraction was already chosen

## 1. Finding 1: f_theta = 1/2, displayed

The max-of-two-prices note displays the choice inequality: theta at
most half the net-floor rate leaves at least half the gross. The
fraction is one-half; the set-once value is the displayed boundary

  theta_amp = r_net / 2,   E_theta = ln(2 / r_net).

Spend half on tolerance, keep half the gross - the ledger had
already ruled. The queue's "f_theta optimizing display" item is
paid by citation.

## 2. Finding 2: r_net's exact gate

The net-floor rate's closed display is the max framing's crossing
value: shallow price falls with depth, deep price rises, the no-gap
floor is where they cross - a named positive symbol, positive by
compactness. Codex's open demand (max-financed-family re-gate): the
actual price functions on one domain, validity of each bound where
the max uses it, a displayed positive floor with no common zero.
The theta side of Sheet 1 waits on exactly that gate.

## 3. Propagation

  GAMMA_ANGLE x pi B_lo sqrt(3/(2 ln 2))
    >= ln(2/r_net) - ln(1 - 2 M_req)

- filed symbols (phi_min, B_lo, r_net); no free dial remains;
V_band stays at target-4's spacing clause, off this path.
Dependency chain: max-crossing display -> r_net -> E_theta; in
parallel C1, the matching check, and the re-gates -> the left side.

## 4. Register

Citation check executed; the choice was already made; the citation
pinned to its gate; nothing new proved, one pendency inherited and
named. Press points: the verbatim citation; the exact-gate pin.
Queue: re-gate; the max-crossing display; C1-C2; the G-ratio front;
the comparison; B1-B3; chapter 10. Filed is not cleared. No closure
claimed.
