---
theorem_id: forward-gold-selected-root-reserve-authority-synthesis-20260627
status: active-producer-reset-to-selected-root-reserve-branch-fork-retained-as-attack-surface
created: 2026-06-27
problem: navier-stokes
route: forward-gold same-material selected-density / critical fresh source tent Carleson
sets_authority:
  - SelectedCompressionRootReserve.A
  - OriginalHistorySelectedLogAmplificationCarleson.A
attack_surface:
  - RepeatedCoreRecordReturnStorageBound.A
  - ShrinkingCoreEscapeCapacityLowerBound.A
support_closed:
  - SilentKernelSourceOriginNormalForm.A
  - AugmentedParentDropAfterSilentSource.A
  - StochasticExponentialSelectedDensityHilbertBMO.A
  - Fixed H^s continuation relay after finite global selected action
source_surfaces:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-selected-compression-root-reserve-synthesis-20260626.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-selected-log-amplification-carleson-production-target-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-critical-fresh-source-tent-carleson-codex-audit-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-nozeno-record-return-branch-routing-20260627.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-silent-source-to-selected-root-reserve-current-audit-20260627.md
---

# Selected root reserve authority synthesis

The current Gold producer is not the silent-source theorem and not either
branch name by itself.  The producer is the original-history selected root
reserve:

```math
\sum_{Q\in\mathcal T_{\rm sel}}
\left(
\langle U_Q,\mathsf S_QU_Q\rangle
+\mathcal S_{\rm normal}(Q)
+\mathcal S_{\rm covector}(Q)
+\mathcal B_{\rm select}(Q)
\right)
\le
C_N(u_0)+R_{\rm legal}.
\tag{1}
```

Equivalently, in selected-density language, the producer is the original-history
selected log-amplification Carleson theorem:

```math
\sup_{P\subseteq Q_0}
{1\over\mathcal R(P)}
\sum_{Q\subseteq P}
\left(
\|\Delta_QZ\|_{\mathcal H}^2+|e_Q|^2
\right)\mathcal R(Q)
\le
C_N(u_0)+R_{\rm legal}(P).
\tag{2}
```

The martingale/reverse-Holder step after `(2)` is already a consumer theorem.
The parent-drop-to-\(H^s\) relay after finite global selected action is also a
consumer theorem.  The source-origin normal form closes the local silent-kernel
escape.  None of those supplies `(1)` or `(2)`.

## Why the no-Zeno fork remains useful

The current attack surface is:

```text
RepeatedCoreRecordReturnStorageBound.A
+
ShrinkingCoreEscapeCapacityLowerBound.A.
```

These are branch coordinates of `(1)`.

For repeated cores, record/return BV descent says selected positive action on
one retained material ancestry line must appear as record growth or negative
scale return after the signed scale-potential lift:

```math
\sum_{Q_k\subset\mathcal A}A_{\rm sel}^{fresh}(Q_k)
\lesssim
\operatorname{Rec}_\infty(\Psi_\mathcal A)
+V_-^{return}(\Psi_\mathcal A)
+R_{\rm legal}(\mathcal A).
\tag{3}
```

For shrinking cores, the pressure-visible/null split gives the branch-local
payment:

```math
A(Q_k)\simeq1
\Rightarrow
\langle U_{Q_k},\mathsf S_{Q_k}U_{Q_k}\rangle
+\mathcal S_{\rm normal}(Q_k)
+\mathcal S_{\rm covector}(Q_k)
+\mathcal B_{\rm select}(Q_k)
\gtrsim 1.
\tag{4}
```

The missing part in both branches is the same original-history upper/root
reserve, not another local detector.

## Why this reset matters

The critical half-tail

```math
\nu_\ell={2^{-\ell}\over \ell+1}
```

has finite raw mass and divergent selected first-ratio mass:

```math
\sum_\ell\nu_\ell<\infty,
\qquad
\sum_\ell 2^\ell\nu_\ell=\infty.
\tag{5}
```

So any proof that sees only raw source mass, raw Hilbert energy, raw pressure
capacity, ordinary energy, local Schur visibility, or endpoint \(L^1\) still
permits `(5)`.

The proof must show that selected amplification is produced only through a
charged original-history increment: visible Schur defect, same-material
strain/exchange, collar-normal compression, wave-covector compression,
selection boundary tax, or paid legal/reselection error.

## Current proof target

The authority surface should therefore read:

```text
SelectedCompressionRootReserve.A / OriginalHistorySelectedLogAmplificationCarleson.A
```

with the current attack surface:

```text
RepeatedCoreRecordReturnStorageBound.A / ShrinkingCoreEscapeCapacityLowerBound.A.
```

This prevents three stale substitutions:

1. promoting the local silent-source theorem to global closure;
2. treating record/return as a third route rather than the repeated-core branch;
3. treating per-core no-escape lower bounds as enough without the original-history
   root summability.

