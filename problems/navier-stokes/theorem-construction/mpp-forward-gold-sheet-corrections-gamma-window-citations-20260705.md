---
theorem_id: forward-gold-sheet-corrections-gamma-window-citations-20260705
status: gamma-disambiguated-window-named-b-chain-cited-sheet2-status-stated-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / sheet corrections
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie.
completion_truth: >-
  Consumes the sheet re-gate's three sharpenings, pending re-gate.
  (GAMMA DISAMBIGUATED - Codex's catch accepted) The home multiplier
  is THE BRACKET ITSELF, not one-minus-a-deficit: gamma_home :=
  [(q + c(-w_eff) - k)/(q + p - k)] x (p / c(w_eff)) x
  (m_s(w_eff) / sin(phi0)) - the f1-f2 displayed lower-bracket product
  evaluated at the half-min width, each factor at most one, the whole
  equal to ONE at zero width (the cleared center identity). The loose
  "one minus deficit" phrasing is withdrawn; the sheet's exponent
  reads Lambda(N) = gamma_home x c_diff x sqrt(N / ln 2) with
  gamma_home the displayed product. (THE WINDOW NAMED) The finite
  window is the integer set from three to N-star, where N-star is the
  displayed crossing of the monotone carry - the smallest N with
  N b(Lambda(N)) at least half a log-two; and by the low-edge
  reduction (filed), if the low-edge inequality holds then the window
  is EMPTY of unresolved chop numbers - the naming and the reduction
  are consistent: the window is defined by the crossing, and the
  reduction says one inequality at its bottom decides all of it.
  (THE b-CHAIN CITED, three links) b(Lambda) = sigma_min x
  [1 - exp(-(Lambda - ln(1/theta))_+)]: link one, the forced-
  instability growth form - rectification note, sections four and
  five; link two, the seed logarithm and its exact threshold theta -
  the sD-multiplier-repair amplitude split; link three, sigma_min -
  the drain-share floor (one plus the step ratio minus width
  correction, over two) from the f1-f2 slice display with the
  financed-family invariant floor beneath it. Three links, each to a
  filed display, none to prose. (SHEET 2 STATUS STATED) The poor-horn
  line is, at present, A COMPARISON IN FILED SYMBOLS, not a fully
  expanded sheet: F_oct, T_oct, and A_max await expansion against the
  chapter-8 closed forms, which requires re-reading that bookkeeping
  before writing - stated plainly rather than half-expanded from
  memory; the expansion is the queued parallel unit. PRESS POINTS:
  the gamma product's three factors (each cited to the f1-f2 note's
  display); the crossing definition of N-star (one line from the
  carry). Filed is not cleared. No closure claimed.
---

# Sheet corrections

## 1. Gamma, disambiguated

  gamma_home = [(q + c(-w_eff) - k)/(q + p - k)]
               x (p / c(w_eff))
               x (m_s(w_eff) / sin(phi0)),

the f1-f2 lower-bracket product at the half-min width - the
multiplier IS the bracket; one at zero width by the cleared identity.
The deficit phrasing is withdrawn.

## 2. The window, named

Window = integers from three to N-star; N-star = the smallest N with
N b(Lambda(N)) at least half a log-two (the carry's crossing). The
low-edge reduction supplies a sufficient bottom test for the whole
set; it is not an equivalence unless a worst-ratio lemma is also
proved.

## 3. The b-chain, cited

Forced-growth form: rectification note (sections four and five). Seed
threshold theta: the amplitude split. Drain-share floor sigma_min:
the f1-f2 slice display over the financed-family invariant floor.
Three links, three filed displays.

## 4. Sheet 2, honestly labeled

A comparison in filed symbols; full expansion awaits a re-read of the
chapter-8 closed forms - queued, not faked from memory.

## Register

Three sharpenings consumed; press points: the gamma factors'
citations; the crossing line. Queue: this note's re-gate; the
low-edge inequality's resolution attempt; sheet-2 expansion; chapter
10. Filed is not cleared. No closure claimed.
