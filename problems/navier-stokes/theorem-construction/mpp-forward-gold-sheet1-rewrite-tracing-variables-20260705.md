---
theorem_id: forward-gold-sheet1-rewrite-tracing-variables-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / Sheet 1 rewritten in the tracing variables
authorship: Claude (Fable), 2026-07-05. Executes item 1 of Codex's
  corrected live front: the stale c_diff/theta row names expanded
  into their source-map roles, the whole sheet restated in the
  tracing variables, and the pass condition collapsed to one
  displayed inequality in three filed symbols and one exact
  constant. Physical story Thomas Birnie: the ledger had two
  placeholder columns left over from an older page; copied onto the
  current page, the columns dissolve into entries already written,
  and the page shows exactly one unpriced item - the ratio of the
  water's two triangles.
completion_truth: >-
  Rewrites Sheet 1 in the tracing variables per Codex's source-sweep
  reduction, pending re-gate; PROVES NOTHING NEW - every line is a
  substitution of filed objects, with sources cited and pendencies
  named. (THE PAIR, corrected front adopted verbatim) LEG A:
  sigma_min [1 - e^(-D_3)] >= Delta_req(3); LEG B: 3 sigma_min
  [1 - e^(-D_3)] >= T_req(3); D_N = (gamma_G F_N - E_theta)_+, with
  gamma_G the R2b predator/relay G-ratio floor on the financed
  family - THE ONLY NONEXACT DELIVERED-SIDE SYMBOL (tracing
  clearance), F_N the exact chop factor, E_theta the exact choice
  term. (F_N EXPANDED, citing the cdiff-assembly note, pending its
  re-gate) F_N = [1/sqrt(2 delta_N)] x W: delta_N = 1 - 2^(-1/N)
  exact; the sqrt-2 line gives 1/sqrt(2 delta_N) >=
  sqrt(N/(2 ln 2)); W = 2K(m) A/sqrt(X3 - X1) with the coupling g
  cancelled exactly, 2K(m) >= pi by K-monotonicity, and the
  amplitude ratio >= B_lo (band bracket; C1 = normalized-value
  confirmation pending). FLOOR: F_3 >= pi B_lo sqrt(3/(2 ln 2)).
  (E_THETA EXPANDED, citing the theta-row consolidation, pending its
  re-gate) E_theta = ln(1/theta); theta = the displayed
  spacing-floor choice = pi/V_band, so E_theta = ln(V_band/pi);
  V_band's explicit display is OWED inside target-4's open gate
  (checked absence, typed there). (THE CLEARED RIGHT SIDE) sigma_0 =
  1/2 (sigma row CLEARED, both Codex's payment and my note's
  re-gate); M_req <= T_req(3) < 1/2 (CLEARED, integer finish
  verified); D_req = -ln(1 - 2 M_req), exact, well-defined; right
  sides the filed powers-of-two forms. (THE ONE INEQUALITY, fully
  substituted) The factorwise/angle route proves Sheet 1 exactly
  when gamma_G F_3 >= E_theta + D_req; substituting every floor, the
  SUFFICIENT displayed form reads: GAMMA_ANGLE x pi B_lo
  sqrt(3/(2 ln 2)) >= ln(V_band/pi) - ln(1 - 2 M_req), where
  GAMMA_ANGLE = sin(phi_min/2)/(1 + 2 cos(phi_min/2)) (CLEARED
  row), M_req <= T_req(3) = ln 2 - 3 ln(2 - 2^(-1/3)) (CLEARED
  closed form): ONE INEQUALITY, THREE FILED SYMBOLS (phi_min, B_lo,
  V_band), ONE EXACT CONSTANT. Alternatively gamma_G may use the
  tighter candidate-list floor (Codex's evaluation and
  endpoint-interiority tables) in place of GAMMA_ANGLE - both
  routes displayed, the fourth exit unchanged. (THE MATCHING
  QUESTION, named not assumed) GAMMA_ANGLE was cleared as a floor
  for gamma_home on the sector-admissible slice; the sweep's
  gamma_G is the G-ratio floor on the financed family - whether the
  slice's floor covers the family's gamma_G is a MATCHING claim the
  re-gate must check (the f1-f2 chain built gamma_home exactly as
  the G-ratio's home-slice bracket, so the identification is the
  filed reading, but it is a citation, not a new fact). (WHAT THIS
  NOTE IS NOT) Not a pass: V_band is owed, B_lo unconfirmed,
  targets 4/5 open, the cdiff/theta expansion notes pending
  re-gate. The sheet is now STATED in its final variables; passing
  it is the remaining work, exactly enumerated. PRESS POINTS: the
  full substitution line (every symbol cited to its clearance or
  its pendency); the matching question (one sentence for the
  auditor). QUEUE: this note's re-gate; the V_band display
  (target-4 gate); C1-C2; the G-ratio front (T7 financed floor or
  chapter 10); then the comparison itself; B1-B3; chapter 10. Filed
  is not cleared. No closure claimed.
---

# Sheet 1, rewritten in the tracing variables

## 1. The corrected front, adopted

  LEG A:  sigma_min [1 - e^(-D_3)] >= Delta_req(3)
  LEG B:  3 sigma_min [1 - e^(-D_3)] >= T_req(3)
  D_N = (gamma_G F_N - E_theta)_+

gamma_G: the R2b predator/relay G-ratio floor on the financed
family - the only nonexact delivered-side symbol (tracing
clearance). F_N: the exact chop factor. E_theta: the exact choice
term. The stale c_diff/theta column names dissolve into these.

## 2. F_N, expanded (cdiff assembly, pending re-gate)

  F_N = [1/sqrt(2 delta_N)] x W,  delta_N = 1 - 2^(-1/N) exact;
  1/sqrt(2 delta_N) >= sqrt(N/(2 ln 2))   (the sqrt-2 line);
  W = 2 K(m) x A/sqrt(X3 - X1)   (g cancels exactly);
  2 K(m) >= pi   (K-monotonicity, K(0) = pi/2);
  A/sqrt(X3 - X1) >= B_lo   (band bracket; C1 pending).

  FLOOR:  F_3 >= pi B_lo sqrt(3/(2 ln 2)).

## 3. E_theta, expanded (theta consolidation, pending re-gate)

  E_theta = ln(1/theta),  theta = pi/V_band  (the displayed choice),
  E_theta = ln(V_band/pi);

V_band's explicit display is owed inside target-4's open gate - a
checked absence, already typed.

## 4. The cleared right side

sigma_0 = 1/2 (row CLEARED); M_req <= T_req(3) < 1/2 (CLEARED);
D_req = -ln(1 - 2 M_req), exact and well-defined; right sides the
filed powers-of-two forms.

## 5. The one inequality

The route proves Sheet 1 exactly when

  gamma_G F_3 >= E_theta + D_req.

Substituting every floor - the sufficient displayed form:

  GAMMA_ANGLE x pi B_lo sqrt(3/(2 ln 2))
    >= ln(V_band/pi) - ln(1 - 2 M_req),

with GAMMA_ANGLE = sin(phi_min/2)/(1 + 2 cos(phi_min/2)) (cleared)
and M_req <= T_req(3) = ln 2 - 3 ln(2 - 2^(-1/3)) (cleared). One
inequality; three filed symbols (phi_min, B_lo, V_band); one exact
constant. The tighter candidate-list floor may replace GAMMA_ANGLE;
the fourth exit is unchanged.

## 6. The matching question, named

GAMMA_ANGLE was cleared as a floor for gamma_home on the
sector-admissible slice; gamma_G is the family G-ratio floor. The
f1-f2 chain built gamma_home exactly as the G-ratio's home-slice
bracket, so the identification is the filed reading - but it is a
citation for the re-gate to check, not a new fact asserted here.

## 7. Register

Sheet 1 stated in its final variables; nothing passed. Owed and
open, exactly: V_band (target-4 gate), B_lo confirmation (C1),
target-5 gate (C2), the matching check, the pending re-gates, then
the comparison itself. Press points: the substitution line; the
matching sentence. Queue: re-gate; V_band; C1-C2; the G-ratio
front; the comparison; B1-B3; chapter 10. Filed is not cleared. No
closure claimed.
