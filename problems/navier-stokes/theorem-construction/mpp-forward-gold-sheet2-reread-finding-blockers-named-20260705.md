---
theorem_id: forward-gold-sheet2-reread-finding-blockers-named-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / sheet 2's promised re-read, executed
authorship: Claude (Fable), 2026-07-05. The sheet-corrections note
  promised sheet 2's expansion only after a re-read of the chapter-8
  closed forms - "queued, not faked from memory." The re-read is now
  done. The finding is not the expansion; it is the honest reason
  the expansion cannot yet exist, with the blockers named as filed
  queue items. Physical story Thomas Birnie: before the poor pay by
  the clock, someone must write down the size of the bill and the
  ceiling on the coin - both are named, neither is written.
completion_truth: >-
  Executes the promised chapter-8 re-read for sheet 2 and files the
  FINDING, pending re-gate. (WHAT WAS RE-READ) The chapter-8 draft
  (chapters-1-4-8 note), the cmid-floor/poor-charge dichotomy and its
  Codex re-gate, the tau-amplitude cancellation note and its re-gate,
  the financed-family note, and the f1-f2 formulas note. (THE
  FINDING) The chapter-8 draft carries NAMED SYMBOLS by design
  ("ratio named, not numbered"); it does not display F_oct, T_oct,
  A_max as closed forms, and no other filed note does either. Per
  symbol: r0(f) = f^2/(2 pi rho) - EXPANDED, filed closed form;
  tau_min - EXPANDED, cleared (clearance #9), the compact minimum of
  tau(m) = [arcsn(sqrt(3)/2; m) - arcsn(1/2; m)]/(2K(m)) on the
  capped modulus band; F_oct - FILED AS A SYMBOL with a filed
  defining role (the octave financing floor that defines the
  financed family: per-window throughput at least the financing
  floor), but its closed form as a function of the octave's filed
  quantities is not displayed anywhere; A_max - NOT FILED: the
  package ceiling (max swing amplitude of the package variable,
  the A_i <= A_max step in Codex's own count algebra) has no filed
  theorem; T_oct - NOT FILED: the required octave toll entered the
  record as a demanded symbol in Codex's cmid re-gate and has no
  filed closed form. (CONSEQUENCE, honest) Sheet 2 REMAINS what
  Codex accepted it as: a comparison in filed symbols. Its expansion
  is BLOCKED on exactly three named items, now queue entries rather
  than a vague "expansion": (B1) the F_oct closed-form display
  (financing floor of one octave in the log currency, from the
  financed-family and TV-chain filings); (B2) the A_max
  package-ceiling theorem (candidate sources: the conservation
  ceilings of chapter 4; the ledger recurrence sup H <= kappa0 E0 /
  c0; the parking cap band - a ceiling must be DERIVED, not named);
  (B3) the T_oct requirement display (the octave's demanded payment
  in the same log currency, sheet-1's T_req machinery being the
  adjacent model). No expansion is faked; the promise is discharged
  by the finding and the blockers are typed. PRESS POINTS: the
  per-symbol table above (five symbols: two expanded, one filed-as-
  symbol, two not filed); the three blocker statements. QUEUE: this
  note's re-gate; B1-B3 as the sheet-2 stratum; the decisive
  threshold with GAMMA_ANGLE (sheet 1); chapter 10. Filed is not
  cleared. No closure claimed.
---

# Sheet 2: the re-read, executed; the blockers, named

## 1. The promise

The sheet-corrections note labeled sheet 2 honestly - a comparison
in filed symbols - and queued its expansion behind a re-read of the
chapter-8 closed forms, "queued, not faked from memory." The re-read
is done: the chapter-8 draft, the cmid/poor-charge pair, the
tau-amplitude pair, the financed-family note, the f1-f2 formulas.

## 2. The per-symbol finding

  r0(f) = f^2/(2 pi rho)          EXPANDED - filed closed form.
  tau_min                          EXPANDED - cleared; compact
                                   minimum of the displayed elliptic
                                   quotient on the capped band.
  F_oct                            FILED AS SYMBOL - defining role
                                   in the financed family (the
                                   octave financing floor); closed
                                   form not displayed anywhere.
  A_max                            NOT FILED - the package ceiling
                                   (the A_i <= A_max step in Codex's
                                   count algebra) has no filed
                                   theorem.
  T_oct                            NOT FILED - entered as a demanded
                                   symbol in Codex's cmid re-gate;
                                   no filed closed form.

The chapter-8 draft carries named symbols by design; the sheet's
missing closed forms were never there to re-read.

## 3. The blockers, typed

  B1: display F_oct - the financing floor of one octave in the log
      currency, from the financed-family and TV-chain filings.
  B2: derive A_max - a package-ceiling theorem, not a named symbol;
      candidate sources: chapter-4 conservation ceilings, the ledger
      recurrence, the parking-cap band.
  B3: display T_oct - the octave's demanded payment in the same log
      currency; sheet-1's T_req machinery is the adjacent model.

Before the poor pay by the clock, someone must write down the size
of the bill and the ceiling on the coin. Both are named; neither is
written; nothing is faked.

## 4. Register

Promise discharged by finding; sheet 2 stays a comparison in filed
symbols with three typed blockers replacing the vague queue entry.
Queue: re-gate; B1-B3; the decisive threshold with GAMMA_ANGLE;
chapter 10. Filed is not cleared. No closure claimed.
