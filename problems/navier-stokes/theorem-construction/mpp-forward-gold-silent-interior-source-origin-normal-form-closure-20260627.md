# Silent interior source origin normal form closure

Date: 2026-06-27

Status: local source-origin normal form proved as the correct replacement for
Schur-only observability; global summability still lands through the already
installed full same-material exchange clock.

Related surfaces:

- `mpp-forward-gold-silent-kernel-source-origin-normal-form-20260627.md`
- `mpp-forward-gold-silent-source-hilbert-bubble-decomposition-test-20260627.md`
- `mpp-forward-gold-boundary-flat-selected-carrier-exchange-charge-audit-20260627.md`
- `mpp-forward-gold-complete-frame-tracefree-primitive-pls-payment-20260625.md`

## 1. Correct object

Boundary invisibility alone cannot exclude an interior source

```math
f=\mathcal L_A w,
\qquad
w|_{\partial Q}=0,
\qquad
\partial_{\nu_A}w|_{\partial Q}=0.
```

Such sources exist.  For example, take any smooth compactly supported
potential \(w\) inside the child collar and set \(f=\mathcal L_Aw\).
The parent Schur trace sees no boundary jump.

Therefore the correct theorem is not Schur observability of all interior
sources.  The correct theorem is the source-origin normal form:

```math
\ker(\mathsf S_Q)
\cap
\{\text{retained selected pressure-Hodge carriers}\}
\subset
\{\text{gauge/potential-only}\}
\cup
\{\text{top-strain/material-exchange charged}\}.
```

Equivalently, in zero-cost form,

```math
\mathsf S_Q U_Q=0,
\qquad
d\Omega_Q^{strain/ex}=0
\quad\Longrightarrow\quad
A_{\rm sel}^{silent}(Q)=0.
```

So an invisible child can be nonzero as a pressure potential, but it cannot be
both selected and zero-cost.

## 2. Silent pressure force is projected gauge

The material gradient force generated by \(w\) is

```math
\mathcal G_Aw=A^\top\nabla_aw.
```

The material Hodge projector is

```math
\mathbb P_A=I-\mathcal G_A\mathcal L_A^{-1}D_A.
```

Thus

```math
\mathbb P_A\mathcal G_Aw=0.
```

As a projected velocity-force component, the silent source is pure constraint
material.  That part is not selected activity.

## 3. The only selected reading is strain-Hessian reading

If the silent source is genuinely selected, it must appear through the pressure
Hessian in the material strain equation.  Schematically,

```math
D_tS+S^2+\Omega^2+\nabla_A^2q
=
\nu\Delta_AS+\mathcal C_A^{legal}.
```

For the silent potential component \(q=w\), and for a selected unit direction
\(e\),

```math
-e\cdot\nabla_A^2w\,e
=
D_t(e\cdot Se)
+e\cdot S^2e
+e\cdot\Omega^2e
-\nu e\cdot\Delta_ASe
+\mathcal C_A^{legal}.
```

Since \(\Omega\) is skew,

```math
e\cdot\Omega^2e=-|\Omega e|^2.
```

So the selected positive pressure-Hessian reading obeys

```math
\left[-e\cdot\nabla_A^2w\,e\right]_+
\le
\left[D_t(e\cdot Se)\right]_+
+|S|^2
+|\Omega e|^2
+\nu|\Delta_AS|
+|\mathcal C_A^{legal}|.
```

Every term on the right is a same-material exchange term:

- the positive derivative of \(e\cdot Se\) is top-strain logarithmic/exchange
  service;
- \(S^2\) and the complete-frame companion terms are strain service;
- the vorticity-frame contribution is part of the complete-frame
  pressure-strain packet;
- the parabolic term is viscous/tower service after integration by parts;
- coefficient and collar errors are legal only in the established residual
  normal form.

Therefore

```math
dA_{\rm sel}^{silent}
\le
C\,d\Omega^{top\text{-}strain/ex}
+C\,dD^{vis/rad}
+dR^{legal}.
```

This is the needed source-origin normal form.

## 4. Complete-frame closure of the transverse silent mode

The symbol counterexample showed that a single pressure-Hodge direction can
miss transverse trace-free strain:

```math
\xi=e_1,
\qquad
Z=
\begin{pmatrix}
0&0&0\\
0&1&0\\
0&0&-1
\end{pmatrix}.
```

That counterexample is only a one-direction pressure probe.  The complete-frame
trace-free primitive already supplies the missing companion readings.  In the
frame \((\hat\xi,p,n)\),

```math
\operatorname{tr}S=0
\quad\Longrightarrow\quad
p\cdot Sp+n\cdot Sn=-\hat\xi\cdot S\hat\xi.
```

Thus any transverse positive selected component satisfies

```math
[p\cdot Sp]_+
\le
[-\hat\xi\cdot S\hat\xi]_+
+[-n\cdot Sn]_+.
```

The silent transverse mode is therefore not a zero-service selected carrier.
If it is selected, the complete-frame same-material exchange packet sees it.

## 5. Parent-drop consequence

The parent-child coercivity line should be read with the silent-source normal
form included:

```math
\sum_{Q'\in ch(Q)}A(Q')
\le
C\langle U_Q,\mathsf S_QU_Q\rangle
+C\,\Omega_Q^{top\text{-}strain/ex}
+C\,D_Q^{vis/rad}
+C\,R^{legal}(Q).
```

On the visible branch, the Schur defect pays.  On the silent branch, the source
origin normal form pays.  In the zero-defect and zero-exchange limit,

```math
\mathsf S_\infty U_\infty=0,
\qquad
\Omega_\infty^{top\text{-}strain/ex}=0
\quad\Longrightarrow\quad
A_\infty=0.
```

That is the invisible-child rigidity theorem in its correct form.

## 6. Relation to bubble energy

`SilentSourceBubbleEnergyCarleson.A` is a stronger sufficient route: it would
bound the Hilbert energy of all hidden pressure-potential bubbles.  The theorem
above is sharper.  It does not ask for all boundary-flat bubbles to be globally
Carleson.  It only says the boundary-flat bubbles that are retained and selected
must show up as same-material top-strain/exchange service.

So the live pressure point is:

```math
\boxed{
\text{invisible selected source}
\Rightarrow
\text{same-material strain/exchange charge}.
}
```

Together with the installed complete-frame and full-tower exchange accounting,
this removes the silent-kernel escape from the parent-drop argument.
