---
theorem_id: forward-gold-target1-items-helicity-derived-curl-diagonal-20260705
status: codex-regated-helicity-multiplier-open-not-cleared
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / target-1 sharpened items
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the tower's rungs carry their own wavenumbers - the curl does not
  need to be told what a helical mode weighs; it IS the weighing.
completion_truth: >-
  Pays the four sharpened items of Codex's target-1 re-gate, pending
  re-gate, with one genuine improvement over the display note. (ITEM 1,
  PRESSURE NARROWED) The exactness claim concerns ONLY the
  Duchon-Robert anomalous flux cubic - the increment-cubic term of the
  mollified balance. The other balance terms (transport divergences,
  the pressure divergence, viscous terms) are not decomposed and need
  not be: they converge distributionally to their standard limits by
  the suitable class, and the defect is carried by the cubic alone -
  that is Duchon-Robert's own structure theorem. Narrowed as demanded.
  (ITEM 2, HELICITY DERIVED - THE IMPROVEMENT) Do not define the
  channel helicity flux by weight; DERIVE it by inserting the
  Littlewood-Paley resolution into the VORTICITY slot of the mollified
  helicity balance's increment cubic. On a helical channel the curl is
  DIAGONAL WITH EIGENVALUE the signed channel wavenumber - pointwise,
  as vector fields, not merely in norm - so the vorticity-slot
  insertion yields channel terms equal to the signed channel wavenumber
  times the matching energy-form cubic, EXACTLY, zero remainder in
  that slot: the kinematic weight is now a THEOREM of the derivation
  (curl-diagonality), not a definition. The linear-slot exactness
  argument of the energy side applies verbatim (same dominated
  convergence, one line, to be written with the DR test form). (ITEM
  3, REMAINDER LIST RELOCATED AND CHECKED) With both decompositions
  slot-exact, the remainder no longer lives in either identity; it
  lives in ONE place - the CROSS-SLOT COMPARISON: the helicity
  channels are labeled by the vorticity slot, the energy channels by
  the velocity slot, and matching them channel-to-channel leaves (i)
  diagonal terms that agree exactly by the eigenvalue identity, and
  (ii) off-diagonal cross-slot terms - the single remainder family,
  bounded by band-separation geometric tails times the local cubed
  budget (the former R-b; the former R-a curl-mollifier family is
  ABSORBED because the curl now acts inside the channel where it is
  diagonal and commutes with the mollifier up to the same off-diagonal
  tails - one family, not two). Time-mollifier cross terms: absorbed
  by the space-time mollifier convention of the filed notes, that
  dependence still named. (ITEM 4, E2 RERUN) Both reconstructions now
  run on slot-exact identities: energy side caveat-free as before;
  helicity side carries exactly ONE listed summable family plus the
  filed UV/IR displays, all in defect-measure currency. E2's open
  clause is now: display the single cross-slot family's local-variation
  bound with the mollifier and band-cutoff constants present - one
  display, named, for the T1 transcription. PRESS POINTS for Codex:
  the pointwise curl-diagonality on channels as used inside increments
  (it is the helical-basis eigenrelation applied to each Fourier mode
  of the band - exact, but worth its one line); and the absorption of
  the curl-mollifier family into the off-diagonal family (the
  commutation is exact on the diagonal by Fourier multiplication -
  the only failure is off-diagonal, hence the relocation). Chapter 10
  unexecuted. Filed is not cleared. No closure claimed.
---

# Target 1, four items: the helicity weight as a theorem

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-five-payments-regate-20260705.md`. The pressure
narrowing and energy-side linear-slot partition are accepted in shape.
The helicity claim is not cleared: curl is diagonal on helical Fourier
modes with multiplier `s|xi|`, not a scalar dyadic rung weight unless
the channel is refined to exact frequency fibers. The note must either
carry the exact `s|D|` multiplier or write the dyadic scalar replacement
as a band-width/tail estimate in defect-measure currency. Filed is not
cleared.

## 1. Pressure, narrowed

The exactness claim is about the DR anomalous flux cubic only. All
other balance terms converge to their standard limits and are not
decomposed. The defect is carried by the cubic - Duchon-Robert's own
structure. Nothing else was ever claimed; now nothing else is said.

## 2. Helicity, derived

Insert the resolution into the VORTICITY slot of the helicity
balance's increment cubic. Curl is diagonal on a helical channel with
eigenvalue the signed channel wavenumber - pointwise, as fields. The
channel helicity flux therefore EQUALS the signed channel wavenumber
times the matching energy-form cubic, exactly: the kinematic weight
is derived, not defined. The tower's rungs weigh themselves.

## 3. The remainder, relocated to one family

Slot-exact identities on both sides leave the remainder in exactly
one place: the cross-slot channel matching. Diagonal terms agree by
the eigenvalue identity; off-diagonal cross-slot terms are the single
remainder family, bounded by band-separation tails times the local
cubed budget. The former curl-mollifier family is absorbed: curl
commutes with the mollifier exactly on the channel diagonal (Fourier
multiplication); only off-diagonal failure exists, and that is the
same family. Two families became one, in the same currency.

## 4. E2, rerun

Energy: caveat-free (slot-exact). Helicity: one listed summable
family plus the filed UV/IR displays. E2's residue is a single named
display with mollifier and cutoff constants present - T1's job, at
manuscript length.

## Register

Four items paid pending re-gate; press points: the pointwise
eigenrelation line; the absorption argument. Queue: 4a-4d re-gate;
targets 2, 3, 5, 6; gate-6 margin choice; chapter 10. Filed is not
cleared. No closure claimed.
