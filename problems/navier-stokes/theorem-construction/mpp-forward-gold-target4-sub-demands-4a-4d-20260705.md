---
theorem_id: forward-gold-target4-sub-demands-4a-4d-20260705
status: codex-regated-subdemands-displayed-target4-still-open
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / target 4 sub-demands (a)-(d)
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie.
completion_truth: >-
  Displays the four sharpened sub-demands of Codex's target-4 re-gate,
  pending re-gate. (4a, THE RATE AND THE WEIGHTED MEASURE) The
  five-mode extraction monomial is the second triad's coupling against
  the conjugate pair; rho(t) is twice its real part over the package.
  The root drift does not integrate rho bare: it integrates rho times
  the ROOT-SENSITIVITY WEIGHT - the ratio N(r)/C'(r) of the displayed
  root-velocity formula, evaluated along the slow evolution. The
  weighted gross-extraction measure is therefore rho(t) times that
  weight on the forward set: DEFINED, displayed, and it is the only
  measure any root-drift statement uses. (4b, THE FLOOR TRANSFERS BY
  COMPACTNESS) Horn (b) floors the UNWEIGHTED net: the integral of
  rho X over the window is at least the f-scaled package. The positive
  part dominates the net (an identity of signed integrals), so the
  unweighted forward gross carries the same floor. The weight-to-X
  ratio is a ratio of two closed forms, bounded above and below on the
  compact capped modulus band - so the WEIGHTED forward gross inherits
  the floor up to the band's ratio bounds: a compactness transfer,
  values chapter-10-typed, positivity structural. (4c, ORIENTATION IS
  DEFINITIONAL, THEN ROUTED) The backward piece is the SAME extraction
  monomial with reversed sign of its real part - i.e. the
  reversed-orientation ray of the same channel in the chapter-2 ray
  language. That is not an analogy: orientation of a ray IS the sign
  of the transfer's real part, by the oriented-admission construction.
  No-recount then routes the reversed-ray mass as previously-admitted
  debt - the same routing every backscatter term already receives. The
  matching to D2's swap-back is the statement that the reversed ray of
  the extraction channel IS the return channel's forward ray - one
  channel, two orientations, which is D2's channel identity verbatim.
  (4d, FRAGMENTATION BY DURATION SPLIT) Split forward subwindows by
  duration against the fast period: LONG subwindows (at least one fast
  period) carry the averaged-root and active-phase lemmas as filed;
  SHORT subwindows contribute root drift that is second-order in the
  extraction rate (the two-time estimate: slow drain against fast
  oscillation over less than a period cannot rectify), so their
  contribution joins the error budget with a geometric sum over the
  countable subwindow family. The lemmas never needed an unfragmented
  window - they needed enough long forward time, which 4b's floored
  weighted gross supplies after the short pieces are charged to error.
  PRESS POINTS for Codex: the positive-part-dominates-net identity
  used in 4b (one line, but it is where gross and net meet); the
  two-time second-order estimate for short subwindows (to be expanded
  with target 5's averaged-root display - same machinery). Chapter 10
  unexecuted. Filed is not cleared. No closure claimed.
---

# Target 4, sub-demands (a)-(d)

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-target4-subdemands-regate-20260705.md`.
The variables are now displayed, but target 4 is not cleared.
Positive-part-dominates-net transfers to weighted gross only after the
root-sensitivity weight is sign-fixed and bounded below against the same
package measure on the positive-rho horn. The reversed-orientation
pieces still need a channel-level identity with D2's swap-back ray and
no-recount applied to the same mass. The short-subwindow claim needs a
two-time summability estimate in the same weighted currency. Filed is
not cleared.

## 4a. The rate and the measure the drift actually uses

rho(t): twice the real part of the five-mode extraction monomial over
the package. The root drift integrates rho times the root-sensitivity
weight N(r)/C'(r) along the slow evolution - the weighted
gross-extraction measure, defined and displayed. No other measure
appears in any root statement.

## 4b. The floor transfers by compactness

Horn (b) floors the unweighted net; the positive part dominates the
net; the weight-to-package ratio is bounded both ways on the compact
capped band. Hence the weighted forward gross inherits the f-scaled
floor up to band ratio bounds - positivity structural, values
chapter 10.

## 4c. Orientation: definitional, then routed

The backward piece is the same monomial with reversed real-part sign
= the reversed-orientation ray of the same channel - orientation IS
that sign, by the oriented-admission construction. No-recount routes
it as previously-admitted debt; and the reversed ray of the
extraction channel is the return channel's forward ray - D2's channel
identity verbatim. Matched with orientation, routed through
no-recount, exactly as demanded.

## 4d. Fragmentation: split by duration

Long forward subwindows (at least a fast period) carry the averaged-
root and active-phase lemmas. Short ones rectify nothing (two-time,
second order) and join the error budget geometrically. The lemmas
need long forward time, not an unfragmented window - and 4b supplies
it once the short pieces are charged.

## Register

4a-4d displayed; press points named (gross-versus-net line; two-time
estimate, shared with target 5). Queue: targets 2, 3, 5, 6; gate-6
margin choice; target-1 re-gate; chapter 10. Filed is not cleared.
No closure claimed.
