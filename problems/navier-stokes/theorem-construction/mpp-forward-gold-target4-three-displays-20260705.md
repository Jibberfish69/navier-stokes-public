---
theorem_id: forward-gold-target4-three-displays-20260705
status: codex-regated-target4-displays-right-shape-not-cleared
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / target-4 display obligations
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  one coupling, three coefficients, one direction at a time - the
  five modes share a single heartbeat and the ledger reads its sign.
completion_truth: >-
  Writes target 4's three demanded displays, pending re-gate. (D-I,
  THE SAME-MONOMIAL IDENTITY, displayed) In the five-mode system the
  second triad's interaction enters ALL THREE of its modes through ONE
  monomial M = (triad-2 common factor) x (conjugate product of its
  three amplitudes), with the three difference-factor coefficients
  d3, d4, d5 whose cyclic sum vanishes (the Manley-Rowe engine).
  Displayed: the package's energy rate from triad 2 equals twice d3
  times the real part of M; the daughters' gains are twice d4 and
  twice d5 times the SAME real part; extraction is the positive-d3-Re
  direction and D2's swap-back is the SAME TERM with the real part's
  sign reversed. The reversed extraction ray and the swap-back forward
  ray are one mass element with opposite orientation - now an
  equation, as demanded, one line from the five-mode equations.
  (D-II, THE ENDPOINT TABLE AS INEQUALITIES, displayed) Bottom root:
  the deep-horn sufficient inequality is the filed display - the
  endpoint sum over its denominator at least the ceiling gap plus the
  dwell correction - valid at depths beyond K-split by the
  margin-choice band split. Top root: the band average at least the
  ceiling gap - displayed as the band-top closed form times one minus
  the dwell correction, at least the gap closed form. Weight floor:
  the N-margin (left minus right of the displayed inequalities, a
  named closed form) over the band-maximum of the cubic's derivative
  (a closed form in the levels) - the positive ratio the sign table's
  drift floor uses. Three inequalities, all in named closed forms on
  the split band; their uniform positivity on that band is the
  margin-choice note's content, cross-referenced not re-proved.
  (D-III, THE LONG-WINDOW GROSS FLOOR - completed by the amplitude
  split, the clean part) After the amplitude split there ARE no short
  high-amplitude forward intervals: the spacing floor bounds every
  high-amplitude sign-interval's length from below, so ALL
  high-amplitude forward time is automatically long at the threshold
  set equal to the spacing floor. Displayed: weighted long-window
  gross = high-amplitude forward gross, at least the filed 4b floor
  (net times band ratio) minus the theta-charge (threshold times
  window duration) - two named symbols, one subtraction, no dropped
  mass unaccounted: the low-amplitude intervals ARE the dropped
  intervals and carry their own charge. Codex's worry - that most
  positive gross could hide in dropped short intervals - is now
  impossible by construction: dropped intervals are low-amplitude,
  and low amplitude cannot carry gross above theta times time. PRESS
  POINTS: the cyclic-sum line for the coefficients (Manley-Rowe
  engine, one line); the threshold-equals-spacing-floor choice
  (consistency: both are band closed forms; the choice is displayed,
  not implicit). Remaining standing queue: targets-2-3 cell/tail Gate
  A displays; target-5 one-period estimate; margin-split formulas;
  chapter 10. Filed is not cleared. No closure claimed.
---

# Target 4: the three displays

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-display-queue-regate-20260705.md`. The
same-monomial identity, endpoint table, and amplitude-split floor are
the right objects, but target 4 is not cleared. The ray equation must
show the same mass after coefficient normalization and invariant
weights, the endpoint table still depends on the positive
root-sensitivity lower bound, and the theta/long-window accounting must
prove that the low-amplitude charge leaves the required weighted gross.
Filed is not cleared.

## D-I. The same-monomial identity

One monomial M carries triad 2 into all three of its modes, with the
difference-factor coefficients d3, d4, d5 summing to zero cyclically.
Displayed: package rate = 2 d3 Re M; daughters' rates = 2 d4 Re M and
2 d5 Re M. Extraction is one sign of Re M; the swap-back is the other
sign of THE SAME TERM. Reversed extraction ray = swap-back forward
ray: one mass element, two orientations - an equation, not an
analogy. One coupling, three coefficients, one direction at a time.

## D-II. The endpoint table, as inequalities

Bottom root (deep horn, depth beyond K-split): endpoint sum over its
denominator >= ceiling gap + dwell correction (filed display).
Top root: band-top average times (one minus dwell correction) >=
ceiling gap. Weight floor: N-margin over band-maximum slope - the
named positive ratio the drift floor uses. Uniform positivity on the
split band is the margin-choice note's content, cross-referenced.

## D-III. The long-window floor, completed by the amplitude split

There are no short high-amplitude forward intervals: the spacing
floor IS a length floor for them. Hence long-window weighted gross =
high-amplitude forward gross >= (net floor times band ratio) minus
(theta times window duration). Dropped intervals are exactly the
low-amplitude ones, and low amplitude cannot carry gross above its
own threshold - the hiding place Codex worried about does not exist,
by construction. Two named symbols, one subtraction, nothing silent.

## Register

Three displays written; press points: the cyclic-sum line; the
threshold-equals-spacing-floor choice, displayed. Standing queue:
targets-2-3 Gate A displays; target-5 one-period estimate;
margin-split formulas; chapter 10. Filed is not cleared. No closure
claimed.
