---
theorem_id: forward-gold-target4-three-lines-sign-table-ray-equation-20260705
status: three-lines-paid-sign-table-ray-equation-one-sided-drop-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / target 4 final three lines
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  on a forward instant nothing the enemy does is neutral - the weight's
  own sign says every taken drop narrows the swing, so the fragments
  need no accounting; they are simply more of the same loss.
completion_truth: >-
  Pays the three line-level requirements of the 4a-4d re-gate, pending
  re-gate, with requirement (iii) DISSOLVING into requirement (i).
  (LINE i - THE SIGN TABLE) The root-sensitivity weight is the
  displayed ratio N(r)/C'(r). On forward subwindows the endpoint lemma
  gives N nonnegative at both turning roots; the displayed slope
  convention gives the cubic's slope positive at the bottom root and
  negative at the top. TABLE: bottom root - weight nonnegative, drift
  = rate times weight nonnegative: the bottom RISES; top root - weight
  nonpositive, drift nonpositive: the top FALLS. Both signs are
  contraction. Bounded below against the package measure: the weight's
  magnitude is the endpoint-margin closed form over the slope's
  band-maximum - a ratio of two closed forms on the compact capped
  band, positive, one chapter-10 symbol. (LINE ii - THE RAY EQUATION)
  Let E(t) be the five-mode extraction monomial (triad-2 coupling
  against the conjugate pair, times the package conjugate). The
  transfer rate is twice its real part. The EQUATION: the
  reversed-orientation ray of the extraction channel and the forward
  ray of D2's swap-back channel are THE SAME ORIENTATION CLASS OF THE
  SAME MONOMIAL - orientation is the sign of the real part of E, the
  extraction's forward is positive sign, the swap-back's forward is
  negative sign of the SAME E; there are not two channels but one
  monomial with two signs. No-recount is then applied to THAT mass:
  the negative-sign intervals carry mass equal to the integral of
  twice the negative part of Re E, booked as previously-admitted debt
  under the same channel's oriented admission that booked the
  positive-sign mass. One monomial, one equation, one routing, same
  mass - as demanded. (LINE iii - DISSOLVED BY THE TABLE) With the
  weight sign-fixed by line (i), the drift integrand (rate times
  weight) is POINTWISE ONE-SIGNED toward contraction on every forward
  instant, long or short. Therefore fragmentation needs no two-time
  summability estimate at all: LONG subwindows carry the averaged
  quantitative floor (target 5's display); SHORT subwindows are
  DROPPED ONE-SIDEDLY - their contribution has the favorable sign, so
  discarding it only weakens the floor we keep, never the truth of
  it. The demanded weighted-currency summability is replaced by a
  one-line monotonicity: dropping nonnegative terms lowers a lower
  bound. Nothing is estimated because nothing needs to be. PRESS
  POINTS for Codex: the sign table's dependence on the endpoint lemma
  (which is the swing-contraction algebra - the chain is: root
  identity -> endpoint conditions -> sign table -> one-sided drop; one
  chain, no circularity, each link filed); and the ray equation's
  claim that D2's swap-back is literally the same monomial (it is the
  same triad-2 interaction term read in the reverse direction - one
  line from the five-mode equations). Chapter 10 unexecuted. Filed is
  not cleared. No closure claimed.
---

# Target 4, the last three lines

## Line (i): the sign table

Weight = N(r)/C'(r). Forward subwindows: N >= 0 at both roots
(endpoint lemma). Slopes: positive at bottom, negative at top.

  bottom root:  weight >= 0,  drift >= 0  - the bottom rises;
  top root:     weight <= 0,  drift <= 0  - the top falls.

Both rows are contraction. Magnitude floor: endpoint margin over
band-maximum slope - one positive closed-form ratio on the capped
band, one chapter-10 symbol.

## Line (ii): the ray equation

One monomial E (the five-mode extraction term). Orientation = sign of
its real part. Extraction-forward = positive sign; swap-back-forward
= negative sign OF THE SAME E. The reversed extraction ray IS the
swap-back's forward ray - an equation between orientation classes of
one monomial, not an analogy between channels. No-recount applies to
that same mass: negative-sign intervals are booked as
previously-admitted debt under the same channel's admission.

## Line (iii): dissolved

With the weight sign-fixed, rate-times-weight is pointwise one-signed
toward contraction on every forward instant. Long subwindows carry
the averaged floor; short ones are dropped one-sidedly - discarding
nonnegative contributions lowers a lower bound, and that is the whole
estimate. The two-time machinery is not avoided; it is unnecessary,
and saying so is the payment.

## Register

Three lines paid pending re-gate; press points: the
endpoint-lemma-to-table chain (filed, acyclic); the same-monomial
line for D2. Queue: targets 2, 3, 5, 6; gate-6 margin choice;
target-1 re-gate; chapter 10. Filed is not cleared. No closure
claimed.
