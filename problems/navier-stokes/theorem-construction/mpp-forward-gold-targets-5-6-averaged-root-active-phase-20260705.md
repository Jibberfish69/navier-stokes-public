---
theorem_id: forward-gold-targets-5-6-averaged-root-active-phase-20260705
status: targets-5-6-paid-averaged-root-displayed-fragmentation-floor-closes-loophole-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / targets 5 and 6 (final analytic targets)
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the enemy cannot flicker faster than its own clock - the hand that
  takes is the hand that ticks.
completion_truth: >-
  Pays the last two analytic targets pending re-gate. (TARGET 5, THE
  AVERAGED-ROOT SYSTEM, DISPLAYED) Which average: the one-period
  average of the transfer variable over the FROZEN-coefficient
  pendulum period starting at the evaluation instant. Legitimacy: over
  ONE fast period the true orbit tracks the frozen orbit by a
  local-clock Gronwall with order-one exponent (the same fast-window
  legitimacy as FiniteChainReduction - one period IS a fast window),
  so the true average differs from the frozen average by a relative
  error of order the drift-per-period (rate times period). Cumulative
  error over a long forward subwindow: the per-period relative errors
  compound to one plus order-of the TOTAL extracted fraction - i.e.
  the averaged-root system is accurate to relative order f, and every
  floor conclusion carries a factor (one minus a constant times f),
  ABSORBED by the filed one-time closed-form choice of the dial f.
  Displayed: definition, one-period Gronwall, per-period error,
  cumulative factor, absorption. All symbolic. (TARGET 6, ACTIVE PHASE
  ON THE ACTUAL WINDOW - AND THE FRAGMENTATION FLOOR, the real
  content) Codex's demand: prove the window contains the active arc or
  charge the lost time. The lurking loophole: the enemy fragments its
  forward time into sub-period slivers so no fragment contains an arc.
  CLOSED STRUCTURALLY: the sign of the extraction rate is the sign of
  the real part of the five-mode monomial, whose phase evolves BY the
  five-mode dynamics at rates bounded by the band's closed forms - the
  transfer phase cannot cross zero faster than the fast clock ticks,
  so consecutive sign changes of the rate are separated by at least a
  closed-form fraction of the fast period. FRAGMENTATION BELOW THE
  FAST PERIOD IS DYNAMICALLY IMPOSSIBLE: the hand that takes is the
  hand that ticks. Consequently: set the duration split's threshold at
  the fragmentation floor; essentially all forward time lies in
  subwindows at least that long; each such subwindow contains full
  periods in proportion to its length minus one period (floor count),
  each full period contains the active arc tau(m_cap) times the
  period (filed), and the end losses are charged one-sidedly (the
  collected return is nonnegative, so partial arcs are dropped, never
  negated). Display: collection over the window is at least tau times
  (total long-forward time minus one period per subwindow), and the
  subwindow count is bounded by forward time over the fragmentation
  floor - so the loss is a band-constant fraction, named. Combined
  with 4b's gross floor and the rate's band ceiling (which floors the
  forward TIME by a closed form), the race integral collects its
  filed amount times named band constants. PRESS POINTS for Codex:
  the fragmentation floor's derivation (zero-spacing of a function
  whose derivative is bounded by band closed forms - one display,
  worth its line: it is where the loophole dies); and the one-period
  Gronwall legitimacy line (identical in kind to the FCR fast-window
  hypothesis - same object, cite once). WITH THIS NOTE the non-audit,
  non-certification queue is: the gate-6 cap/dwell margin choice
  (one display: choose the cap so the two small terms are beaten
  uniformly - the choice is a closed-form inequality in one variable)
  and CHAPTER 10. Everything else is in Codex's queue. Filed is not
  cleared. No closure claimed.
---

# Targets 5 and 6: the average, and the floor under flicker

## Target 5: the averaged-root system

The average: one frozen-coefficient period from the evaluation
instant. Legitimacy: one period is a fast window; the local-clock
Gronwall with order-one exponent tracks true against frozen - the
same legitimacy FiniteChainReduction already carries, cited once.
Per-period relative error: order drift-per-period. Cumulative over a
long subwindow: one plus order-f, absorbed by the one-time
closed-form choice of the dial. The averaged-root system is displayed
with its error budget attached; no coefficient is silently frozen.

## Target 6: the fragmentation floor

The loophole: flicker the extraction's sign so fast that no forward
fragment contains an active arc. The closure: the sign of the rate is
the sign of the real part of the five-mode monomial; its phase moves
BY the five-mode dynamics, at band-bounded rates; zeros of a function
with band-bounded derivative are separated by a closed-form fraction
of the fast period. The enemy cannot flicker faster than its own
clock. Set the duration threshold at that floor: essentially all
forward time lies in subwindows long enough to contain full periods;
each full period contains the filed active arc; end losses are
dropped one-sidedly. Collection: the filed race integral times named
band constants - lost time charged, loophole dead.

## Register

Targets 5 and 6 paid pending re-gate; press points: the
fragmentation-floor display; the one-period Gronwall citation line.
Remaining non-audit queue: gate-6 cap/dwell margin choice (one
closed-form inequality in one variable); chapter 10. Filed is not
cleared. No closure claimed.
