---
theorem_id: forward-gold-tau-amplitude-cancellation-poor-comparison-routed-20260705
status: tau-amplitude-free-displayed-poor-comparison-routed-to-symbolic-stage-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / the two final lines of the TV chain
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the fluid's active time is told in its own clock, and its own clock
  never knew the amplitude - fractions of the swing are fractions of
  the swing at every size.
completion_truth: >-
  Writes the two final lines of the tau/poor-charge re-gate, pending
  re-gate. (LINE 1, THE AMPLITUDE CANCELLATION - the clean answer) The
  active fraction is a TIME fraction, computed in the normalized
  elliptic clock u where the profile is sn-squared of u at modulus m:
  the transfer variable is the bottom root plus the swing amplitude
  times sn-squared, so defining the middle range by FRACTIONS of the
  swing (the exact quarters) makes its u-endpoints arcsn(1/2; m) and
  arcsn(sqrt(3)/2; m) - functions of the modulus ALONE; the period in
  the same clock is twice K(m) - the modulus alone. Therefore
  tau(m) = [arcsn(sqrt(3)/2; m) - arcsn(1/2; m)] / (2 K(m))
  is MANIFESTLY AMPLITUDE-FREE: the amplitude cancelled at the moment
  the range was defined fractionally, and no C_mid input is needed for
  tau at all (the C_mid floor remains filed for the speed-period link,
  where amplitude genuinely belongs). Positivity on the capped band:
  the arcsn difference is positive (the inverse of a strictly
  increasing function at two ordered arguments) and K(m) is finite
  below the separatrix: tau_min = the compact minimum of an explicit
  positive continuous function OF THE MODULUS ALONE on [0, m_cap] -
  displayed, amplitude-free, exactly as demanded. (LINE 2, THE FINAL
  COMPARISON - routed, not asserted) Codex's demanded line - the
  price floor times the poor count at least the octave tax - is a
  relation among NAMED FILED SYMBOLS: expanding each to its closed
  form, it reads  r0(f) tau_min x F_oct / (8 A_max) >= T_oct,  with
  every symbol filed and exact. This is a genuine symbol comparison,
  and pretending it is automatic would be the counterfeit this lane
  kills: it is therefore ROUTED, displayed and labeled, as the
  POOR-HORN LINE of the symbolic-comparisons stage - the same exact
  toolkit, the same currency, adjacent to the middle-window line; the
  comparisons stage now has exactly TWO displayed lines to resolve
  (the middle-window comparison and the poor-horn comparison), each
  exact-versus-exact, each with the three honest exits already filed
  (symbolic resolution; interval pass for that line alone; repair
  faced if negative). The dichotomy loses no honesty: the rich horn
  is CLEARED; the poor horn is a displayed comparison awaiting the
  same stage everything else awaits. PRESS POINTS: the fractional-
  range cancellation line (one sentence: fractions of the swing are
  amplitude-free by construction); the poor-horn line's symbol
  expansion (each of the five symbols cited to its filed closed
  form). WITH THIS NOTE the TV chain has NO remaining display debt:
  everything is cleared, displayed, or routed to the comparisons
  stage. QUEUE: this note's re-gate; THE SYMBOLIC COMPARISONS (now
  two displayed lines plus the window set); CHAPTER 10. Filed is not
  cleared. No closure claimed.
---

# The two final lines

## 1. The amplitude cancellation

The active fraction lives in the normalized clock:

  tau(m) = [arcsn(sqrt(3)/2; m) - arcsn(1/2; m)] / (2 K(m)),

modulus alone - the amplitude cancelled when the middle range was
defined as exact quarters OF THE SWING. Fractions of the swing are
fractions of the swing at every size; the fluid's clock never knew
the amplitude. Positivity: ordered arguments of a strictly increasing
inverse; finite period below the separatrix; compact minimum of an
explicit positive function of the modulus alone. No C_mid input
enters tau (C_mid stays filed where amplitude belongs: the
speed-period link).

## 2. The poor-horn comparison, routed

  r0(f) tau_min x F_oct / (8 A_max) >= T_oct

- five filed symbols, each expandable to its closed form; a genuine
exact-versus-exact comparison, NOT asserted here. Routed as the
POOR-HORN LINE of the symbolic-comparisons stage, beside the
middle-window line, with the same three honest exits already on
file. The rich horn is cleared; the poor horn waits where everything
else waits, in the same currency.

## 3. Register

The TV chain has no remaining display debt. Queue: this note's
re-gate; the symbolic comparisons (two displayed lines plus the
window set); chapter 10. Press points: the fractional-range
cancellation sentence; the five-symbol expansion. Filed is not
cleared. No closure claimed.
