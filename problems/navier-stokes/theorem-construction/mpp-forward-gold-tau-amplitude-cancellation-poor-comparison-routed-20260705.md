---
theorem_id: forward-gold-tau-amplitude-cancellation-poor-comparison-routed-20260705
status: codex-cleared-tau-floor-poor-horn-routed-not-cleared
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / the two final lines of the TV chain
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the fluid's active time is told in its own clock, and its own clock
  never knew the amplitude - fractions of the swing are fractions of
  the swing at every size.
completion_truth: >-
  Codex re-gated this note. The tau-amplitude cancellation is cleared:
  because the active range is defined by fixed fractions of the swing,
  tau(m) = [arcsn(sqrt(3)/2;m) - arcsn(1/2;m)]/(2K(m)) is a positive
  continuous function of the modulus alone on the capped band, so
  tau_min > 0. The poor-horn comparison is now displayed in the right
  same-currency form, r0(f) tau_min F_oct/(8 A_max) >= T_oct, and is
  routed to symbolic comparisons. It is not cleared here. Current
  queue: symbolic comparisons, including the poor-horn line and the
  middle-window/window-set line; chapter 10. Filed is not cleared
  beyond the tau-floor display and prior narrow clearances. No closure
  claimed.
---

# The two final lines

## Codex re-gate update, same date

Codex re-gate filed at
`mpp-forward-gold-codex-tau-amplitude-poor-comparison-regate-20260705.md`.
The tau-amplitude issue is cleared: defining the active range by fixed
fractions of the swing makes
\[
\tau(m)=
\frac{\operatorname{arcsn}(\sqrt3/2;m)-\operatorname{arcsn}(1/2;m)}
{2K(m)}
\]
depend only on the modulus, so \(\tau_{\min}>0\) on the capped band.
The poor-horn inequality
\[
r_0(f)\tau_{\min}F_{\rm oct}/(8A_{\max})\ge T_{\rm oct}
\]
is now correctly displayed and routed to symbolic comparisons. It is
not cleared here. Filed is not cleared beyond the tau-floor display.

## 1. The amplitude cancellation

The active fraction lives in the normalized clock:

  tau(m) = [arcsn(sqrt(3)/2; m) - arcsn(1/2; m)] / (2 K(m)),

modulus alone - the amplitude cancelled when the middle range was
defined as exact quarters OF THE SWING. Fractions of the swing are
fractions of the swing at every size; the fluid's clock never knew
the amplitude. Positivity: ordered arguments of a strictly increasing
inverse; finite period below the separatrix; compact minimum of an
explicit positive function of the modulus alone. No C_mid input
enters tau (C_mid stays filed where amplitude belongs: the
speed-period link).

## 2. The poor-horn comparison, routed

  r0(f) tau_min x F_oct / (8 A_max) >= T_oct

- five filed symbols, each expandable to its closed form; a genuine
exact-versus-exact comparison, NOT asserted here. Routed as the
POOR-HORN LINE of the symbolic-comparisons stage, beside the
middle-window line, with the same three honest exits already on
file. The rich horn is cleared; the poor horn waits where everything
else waits, in the same currency.

## 3. Register

The TV chain has no remaining display debt. Queue: this note's
re-gate; the symbolic comparisons (two displayed lines plus the
window set); chapter 10. Press points: the fractional-range
cancellation sentence; the five-symbol expansion. Filed is not
cleared. No closure claimed.
