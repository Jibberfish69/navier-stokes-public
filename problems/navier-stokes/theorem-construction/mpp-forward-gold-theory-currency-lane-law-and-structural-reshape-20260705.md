---
theorem_id: forward-gold-theory-currency-lane-law-and-structural-reshape-20260705
status: lane-law-adopted-decimal-content-demoted-structural-replacements-filed-quantitative-stratum-named
created: 2026-07-05
problem: navier-stokes
route: forward-gold / lane law + structural reshape of the constants layer
authorship: Claude (Fable), 2026-07-05. Lane law set by Thomas Birnie
  (owner directive, same date): theory only - no number calculations, no
  loose approximations, no running numbers. Physical story his throughout.
completion_truth: >-
  Three things happen in this note and each is stated exactly. FIRST, the
  lane law: from now on, proof currency in this lane is structural only -
  exact conservation identities, exact rational fractions, closed forms,
  monotonicity, convexity, compactness/no-zero positivity, exact scaling
  exponents, dichotomies. Decimal estimates are not proof currency at any
  strength of margin; quantitative thresholds that structure cannot cross
  are NAMED and quarantined to the certification family (the spec's
  Stage-2/3 artifact), never decimal-resolved. SECOND, the demotion: the
  certified-constants payment note (same date) is SPLIT by this law. Its
  exact content STANDS: the invariant drain identity (child pays the exact
  invariant share of every reverse unit), the un-parking derivative
  identity 2 tan(phi_t/2), the parking-cap logarithm, the exact relay and
  predator clock forms, r0 >= f^2/(2 pi rho) as a closed form with exact
  inequalities (sin x >= 2x/pi), c1 >= pi, the locality exponents, and
  the finiteness of the Gronwall data. Its decimal content is DEMOTED to
  sidecar evidence: sampled triangle factors, evaluated elliptic
  integrals, exponential-margin decimals, the corner-supremum ordering by
  decimal comparison, and every percent margin. Nothing may cite those
  numbers as proof. THIRD, the structural replacements, which are the real
  mathematics of this note: (i) the coarse flipped corner is closed by the
  EXACT one-step retention 1/3 < 1/2 in the helical normal form (the
  threshold-eigenvector fraction - his currency), with the normal-form
  lift R2 carrying the burden it always honestly had; (ii) the two-step
  octave is closed structurally: its exact scalar retention is EXACTLY 1/2
  (Codex's composition identity), so ANY strictly positive additional tax
  kills it, and positivity of the reverse tax on the compact nondegenerate
  family follows from no-zeros plus compactness - no number needed;
  (iii) the fine-chop limit is closed structurally by the PREDATOR CLOCK
  THEOREM: the relay's daughter coupling carries the exact factor
  sqrt(delta) while the reverse predator keeps full wavenumber strength,
  so the predator/relay clock ratio diverges as 1/sqrt(2 delta) - the
  finer the chop, the more the predator outruns the relay - and the waste
  ramp rectifies the predator's own spring (adiabatic growth of the
  swing), so the drain per step scales EXACTLY as the step's progress:
  the delta-cancellation as a scaling identity, not a number; (iv) what
  structure does NOT cross: for intermediate chop numbers the required
  extra tax is an exact algebraic gap and the delivered tax has matching
  scaling with an order-one prefactor - that prefactor comparison, the
  corner-lift R2, and the Stage-2/3 constants form the QUANTITATIVE
  STRATUM, named, quarantined, and stated in exact currency as compact-
  family floor problems. The completion ledger's "empty at derivation
  level" is AMENDED accordingly (correction header there): empty of
  MECHANISMS at structural level; the quantitative stratum is the
  certification program, exactly as the owner's spec always demanded.
  The two-payer floor c0 itself SURVIVES in exact currency - its
  derivation never needed a decimal. No closure claimed.
---

# Lane law and the structural reshape

## 1. The law

Proof currency: structure only. Exact identities, exact fractions, closed
forms, monotonicity, convexity, compactness, scaling exponents,
dichotomies. A decimal margin is not a proof; a thin decimal margin is
counterfeit precision - the exact failure mode the certification spec was
written to fence off. Quantitative thresholds that structure cannot cross
are named and handed to the certification family. This note applies the
law retroactively to today's own filings.

## 2. What stands (already exact)

- Invariant drain identity: for reverse partner mu with matching
  helicity, the two triad invariants force the child package to pay the
  exact wavenumber-ratio share of every unit landing in mu. Identity, not
  estimate.
- Un-parking identity: d(phi_t)/d(ln A) = 2 tan(phi_t/2); deep parking
  is punished in proportion to its own depth, shallow parking never
  suppressed the return. Exact, with the parking-cap logarithm
  K <= (1/2) ln(16/f) as its closed-form corollary.
- Return-race floor: r0 >= f^2/(2 pi rho) by the four-step chain, every
  inequality exact (sin x >= 2x/pi on the quarter period; triangle ramp).
- Viscous payer: c1 >= pi from K >= pi/2. Exact.
- Locality exponents: UV first power, IR second power, dyadic geometric
  sums. Exact exponents; mollifier-norm constants are exact inequalities.
- Two-payer floor: c0 >= min( f^2/(2 pi rho), c1/ln(1/f) ) in closed
  form. THE FLOOR NEVER NEEDED A DECIMAL. Payer coverage of every
  Reynolds regime is the single-dial monotonicity argument - structural.
- FiniteChainReduction: per-handoff Gronwall with FINITE explicit data;
  the tail 2^(-B) beats any fixed exponent - a scaling statement.

## 3. Structural replacements for the demoted content

(i) CORNER (one-step coarse flipped): closed by the exact threshold-
eigenvector retention 1/3 < 1/2 in the helical normal form. The reverse-
bite decimal route was an attempt to buy corner-closure without the
normal-form lift; under the law that trade reverses. R2 (normal-form
lift) carries the burden explicitly - as theory: prove the full NS
projection preserves the eigenvector fractions, or prove the NS analogue
directly. Geometric depletion by the incompressibility projector is
one-signed help; that sign is a theorem target, not an assumption.

(ii) TWO-STEP OCTAVE: Codex's exact composition identity puts scalar
retention at EXACTLY 1/2 - the marginal case. Marginality is killed by
positivity alone: the reverse tax is strictly positive on the compact
nondegenerate triad family (the coupling vanishes only on the degenerate
collinear boundary; the relay-drift lock forbids phase suppression at
moving amplitudes; compactness turns no-zeros into a uniform positive
floor whose VALUE is never needed). Two-step self-financing is dead
structurally.

(iii) FINE-CHOP LIMIT - the predator clock theorem: the relay daughter
coupling is sqrt((q-p)(k+p)) = q sqrt(2 delta) at step progress delta,
while the reverse predator's coefficient is (k+q), full strength. The
clock ratio predator/relay = 1/sqrt(2 delta) DIVERGES as the chop
refines: chopping weakens the relay but never the predator - this is the
sqrt(delta)-cancellation restated as an exact scaling identity. The
predator is itself a spring; what rectifies it into a net tax is the
waste RAMP: the pair grows during the step, the fast pendulum's adiabatic
swing grows with it, and the end-of-step holding is the exact invariant
share of the final pair - drain per step proportional to the step's own
progress delta. Fine chopping therefore cannot dilute the tax rate: tax
and progress scale TOGETHER, at every N. Meanwhile evading the viscous
payer under the slowed clock forces local Reynolds to grow with a
strictly larger exact exponent than the relay's slowdown, which forces
pair persistence - the dichotomy is a comparison of exact exponents, no
longer of numbers.

(iv) THE QUANTITATIVE STRATUM (what structure does not cross, named
exactly): for chop numbers three and above, the exact algebraic gap
between the composed scalar retention and one-half must be beaten by the
delivered tax, whose scaling matches but whose order-one prefactor is a
compact-family floor. This prefactor, the corner lift R2, and the
Stage-2/3 constants of the spec are the certification stratum - the
owner's spec is now the EXACT boundary of what remains quantitative.
Nothing else anywhere in the chain is numerical.

## 4. Consequences for today's filings

- Certified-constants note: correction header installed; exact content
  stands, decimal content demoted to sidecar. Its "corner supremum by
  decimal ordering" claim is withdrawn; the supremum question dissolves
  under (i)-(iii), which close each branch structurally and leave the
  stratum (iv) where all quantitative weight now lives.
- Completion ledger: correction header installed; "named ledger empty at
  derivation level" is amended to "empty of mechanisms at structural
  level, quantitative stratum = the certification program." Codex audit
  items A and C (decimal margins) are superseded by this note; items B,
  D, E, F stand and are re-addressed to the structural versions.
- The conditional chain is UNCHANGED in shape and stronger in honesty:
  floor c0 exact-currency positive; every descent branch structurally
  sub-financing or exactly marginal-and-broken-by-positivity; geometric
  decay, peak bound, critical embedding, T*-plus schedule, Silver
  pass-or-exit - conditional on R2 and the certification stratum, which
  are now the whole of what "conditional" means.
