---
theorem_id: forward-gold-theta-row-consolidation-owed-display-20260705
status: filed-pending-codex-regate
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / the theta row - consolidated, one display owed
authorship: Claude (Fable), 2026-07-05. The last unfilled row of the
  decisive threshold is a choice the campaign already made in two
  places with two phrasings; this note puts the choice in one place,
  displays its structure, and names exactly what is still owed
  rather than inventing it. Physical story Thomas Birnie: the seed
  the predator is promised is the size below which the enemy's own
  flicker pays for itself - one dial, already set by the water's
  clock; the ledger just needs the dial's engraving copied out.
completion_truth: >-
  Consolidates the theta row, pending re-gate: REPLACES "named,
  unfixed" with a one-place statement of the choice, its structural
  display, and ONE typed owed display plus ONE typed reconciliation.
  (T1, THE IDENTIFICATION - one symbol, two roles, stated
  explicitly) The theta of the decisive threshold's seed logarithm
  ln(1/theta) IS the amplitude-split threshold: the cleared tracing
  sweep uses one symbol in factor 2 (seed logarithm - exact from the
  amplitude-split display) and factor 7 (theta-charge - exact
  choice); the relay's waste seeds the predator continuously
  (rectification note section 4), and on high-amplitude intervals
  the seed is at least theta by the split's own definition - the
  promised seed and the flicker threshold are the same dial. Until
  now this identification lived implicitly across two sweep rows;
  it is here stated as a claim to be gated. (T2, THE CHOICE, cited)
  Target-4 D-III displays the choice: threshold set equal to the
  spacing floor, with the press point "both are band closed forms;
  the choice is displayed, not implicit." The spacing floor's
  structure (sD-repair catch 2): on high-amplitude intervals a sign
  change of Re M requires a phase crossing; phase crossings of the
  same sign are separated by pi in phase; so consecutive sign
  changes are separated in fast-clock time by at least pi over the
  phase-speed cap: S := pi / V_band, and the theta-row enters the
  threshold as ln(1/theta) - a displayed closed form ONCE V_band's
  expression is written. (T3, THE OWED DISPLAY, typed) V_band - the
  phase-speed upper bound in fast-clock units - is a DEMANDED clause
  of Codex's five-payments re-gate, met in the sD-repair note by the
  words "band closed forms" but NEVER displayed as an explicit
  expression in any filed note (checked: sD-repair, target-4,
  five-payments re-gate; the margin-split formulas note does not
  exist under that name). The theta row is therefore ONE DISPLAY
  away, and that display's ingredients are filed (band frequency
  bounds, fast-clock normalization, the five-mode detuning
  bookkeeping of the D2/target-4 chain). Writing V_band is typed as
  the row's owed item - not derived here, because deriving it
  requires the five-mode frequency bookkeeping re-read and this
  note's job is consolidation, not invention. (T4, THE
  RECONCILIATION, typed) The two filed phrasings of the choice -
  catch 2's "theta = a named fraction of the NET-FLOOR RATE" (an
  amplitude/rate phrasing) and D-III's "threshold set equal to the
  SPACING FLOOR" (a fast-clock phrasing) - must be displayed as one
  consistent definition with units reconciled; D-III's own press
  point flags this consistency and target-4 is NOT cleared (the
  low-amplitude charge accounting is Codex's open item there). The
  reconciliation is typed as part of the owed display, not smoothed
  over. (DOMAIN REMARK) theta enters D_3 only through
  (Lambda - ln(1/theta))_+, so the row needs theta in (0,1) with
  ln(1/theta) finite - guaranteed by any positive band closed form
  below one; the split's own low-amplitude charge requires theta
  small enough that the charge leaves the required gross (Codex's
  open target-4 item, the same gate). (WHAT THE ROW BECOMES) Before:
  "theta is named by the amplitude split but not fixed as a closed
  value" (audit). After: theta = S = pi/V_band by the displayed
  choice, ln(1/theta) = ln(V_band/pi); owed: the V_band display and
  the units reconciliation, both typed, both inside target-4's
  already-open gate - the theta row and target-4 clear TOGETHER or
  not at all. PRESS POINTS: the T1 identification (one symbol, two
  sweep rows - a claim for the auditor, stated not assumed); the
  pi-spacing line (sign changes need pi-separated phase crossings -
  one line); the T3 finding (no filed display of V_band - a checked
  absence, like the sheet-2 blockers). QUEUE: this note's re-gate;
  the V_band display + reconciliation (= the theta row's owed item,
  inside target-4's gate); C1-C2 of the cdiff assembly; then the one
  displayed inequality; B1-B3; chapter 10. Filed is not cleared. No
  closure claimed.
---

# The theta row: consolidated, one display owed

## 1. One symbol, two roles (T1)

The decisive threshold's seed logarithm and the amplitude-split
threshold are the same dial: the cleared sweep's factor 2 and
factor 7 use one symbol; the relay's waste seeds the predator
continuously, and on high-amplitude intervals the seed is at least
theta by the split's definition. The promised seed and the flicker
threshold coincide - stated here explicitly as a claim to be gated,
not left implicit across two rows.

## 2. The choice, cited (T2)

Target-4 D-III: threshold = spacing floor, the choice displayed.
Structure of the floor (sD-repair catch 2): high-amplitude sign
changes need phase crossings; same-sign crossings are pi apart in
phase; so consecutive sign changes are at least

  S = pi / V_band

apart in fast-clock time, with V_band the phase-speed cap. The row
enters the threshold as ln(1/theta) = ln(V_band/pi) - closed once
V_band is written.

## 3. The owed display, typed (T3)

V_band is a demanded clause of the five-payments re-gate, met in
the sD-repair note by the phrase "band closed forms" and NEVER
displayed explicitly in any filed note (checked: sD-repair,
target-4, the five-payments re-gate; no margin-split formulas note
exists under that name). Its ingredients are filed - band frequency
bounds, fast-clock normalization, the five-mode detuning
bookkeeping. Writing it requires the five-mode re-read; it is typed
as the row's owed item, not derived here.

## 4. The reconciliation, typed (T4)

Catch 2 phrases the choice as a fraction of the net-floor rate;
D-III phrases it as equal to the spacing floor. One definition with
reconciled units must be displayed - D-III's own press point flags
it, and target-4's low-amplitude accounting is Codex's open item at
the same gate. The theta row and target-4 clear together or not at
all.

## 5. Register

Row consolidated: theta = pi/V_band by the displayed choice;
ln(1/theta) = ln(V_band/pi); owed = the V_band display + units
reconciliation, inside target-4's open gate. Press points: the T1
identification; the pi-spacing line; the checked absence of a
V_band display. Queue: re-gate; the owed display; C1-C2; the one
displayed inequality; B1-B3; chapter 10. Filed is not cleared. No
closure claimed.
