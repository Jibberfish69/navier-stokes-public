---
theorem_id: forward-gold-tier2-correction-exact-low-edge-values-20260705
status: tier2-weaker-claim-corrected-front-loading-load-bearing-exact-low-edge-forms-filed-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / THE SYMBOLIC COMPARISONS / tier-2 correction + exact low-edge forms
authorship: Claude (Fable), 2026-07-05. A self-correction filed before
  the auditor finds it, plus the low edge's exact values - which come
  out cleaner than anyone had a right to expect. Physical story Thomas
  Birnie: at three chops the cube of the step ratio is exactly one
  half - the tower's own arithmetic hands us the corner values.
completion_truth: >-
  Two contents, pending re-gate. (CONTENT 1, THE CORRECTION - my
  "strictly weaker" claim downgraded) The worst-difference note
  claimed tier 2's threshold is strictly weaker than tier 1's. Checked
  against the crude bound available WITHOUT front-loading (every
  increment is at most the total remaining rise, since increments are
  positive and sum to the bounded limit minus the low-edge value),
  the resulting tier-2 condition is in fact STRONGER than tier 1's -
  the crude route makes tier 2 useless. THE FRONT-LOADING DISPLAY IS
  THEREFORE LOAD-BEARING, not cosmetic: tier 2's advertised weakness
  holds only if the required increments are shown to be decreasing
  (concavity of the log-composed retention in the chop number), so
  that the maximal increment is the FIRST one. The claim is corrected
  to: tier 2 is weaker CONDITIONAL on the front-loading display, which
  is now the note's single open display, upgraded from debt to
  load-bearing status - stated before the auditor said it. (CONTENT
  2, THE EXACT LOW-EDGE VALUES - the clean surprise) At three chops
  the step ratio is the cube root of one half, so ITS CUBE IS EXACTLY
  ONE HALF, and the composition fraction collapses: c*(2^(-1/3)) =
  (2 x 1/2) / (2 x 1/2 + 1 - 2^(-1/3)) = 1 / (2 - 2^(-1/3)) - one
  exact closed form, no residue. Hence, exactly:
  T_req(3) = ln 2 - 3 ln(2 - 2^(-1/3)),
  and at four chops (the step ratio's cube is 2^(-3/4)):
  c*(2^(-1/4)) = 2^(1/4) / (2^(1/4) + 1 - 2^(-1/4)),
  T_req(4) = ln 2 + 4 ln c*(2^(-1/4)),
  Delta_req(3) = T_req(4) - T_req(3) - all exact fractions and
  logarithms of powers of two: the low edge's entire required side is
  now in the lane's purest currency, ready for the tier comparisons
  with no expansion left to do. (WHAT REMAINS ON SHEET 1, exactly)
  (i) the front-loading display - concavity of N ln c*(2^(-1/N)) in N
  - one symbolic monotonicity task, now known to carry tier 2;
  (ii) the saturation-side evaluation - b(Lambda(3)) with its three
  cited links - against tier 1's threshold (Codex's exact display) or
  tier 2's (conditional on (i)); (iii) if both resist, the pass at one
  point. The required side is DONE in exact form; the delivered side
  and the concavity are the whole remainder. PRESS POINTS: the
  cube-is-half collapse (one line - the reason the low edge is clean);
  the crude-bound computation that forced the correction (displayed so
  the downgrade is checkable). Chapter 10 unchanged. Filed is not
  cleared. No closure claimed.
---

# The tier-2 correction, and the low edge in closed form

## 1. The correction

Without front-loading, the best increment bound is the total
remaining rise - and the tier-2 condition built on it is STRONGER
than tier 1's, not weaker. My "strictly weaker" claim is downgraded
to conditional: tier 2 is weaker only if the required increments are
front-loaded (the log-composed retention is concave in the chop
number), making the first increment the largest. The front-loading
display is load-bearing. Filed before the auditor said it.

## 2. The low edge, exactly

At three chops the step ratio cubes to exactly one half:

  c*(2^(-1/3)) = 1 / (2 - 2^(-1/3)),
  T_req(3) = ln 2 - 3 ln(2 - 2^(-1/3)).

At four chops:

  c*(2^(-1/4)) = 2^(1/4) / (2^(1/4) + 1 - 2^(-1/4)),
  T_req(4) = ln 2 + 4 ln c*(2^(-1/4)),
  Delta_req(3) = T_req(4) - T_req(3).

Exact fractions and logarithms of powers of two - the tower's own
arithmetic hands us the corner values. The required side of the low
edge is complete in the lane's purest currency.

## 3. What remains on sheet 1

(i) The front-loading concavity display (now load-bearing for tier
2); (ii) the delivered side b(Lambda(3)) against tier 1's exact
threshold or tier 2's conditional one; (iii) failing both, the pass
at one point. Nothing else.

## 4. Register

Correction filed; exact low-edge forms filed; press points: the
cube-is-half line; the displayed crude-bound check behind the
downgrade. Queue: re-gates; the concavity display; the delivered-side
evaluation; sheet-2 expansion; chapter 10. Filed is not cleared. No
closure claimed.
