---
theorem_id: forward-gold-tracing-sweep-finite-n-display-20260705
status: tracing-sweep-seven-items-one-symbol-finite-n-displayed-candidate-reduction-conversion-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / the certification boundary / converting the candidate reduction
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  every road the delivered tax travels passes through one tollgate -
  the ratio of the water's two triangles - and the sweep is the map
  proving there is no second gate.
completion_truth: >-
  Writes the two items Codex required to convert the one-constant
  reduction from candidate to accepted, pending re-gate. (ITEM 1, THE
  TRACING SWEEP - seven factors, one symbol) The delivered per-step
  drain at chop number N factors, by the filed chain, into exactly
  seven ingredients, swept in order: (1) the invariant drain share -
  EXACT (the two-invariant identity); (2) the seed logarithm - EXACT
  (the amplitude-split threshold is an exact choice inequality);
  (3) the exponent's dependence on the chop number - EXACT (square
  root of chop over log-two, from the difference factors, R2b scaling
  audited); (4) the rate prefactor in the exponent - THE G-RATIO, and
  here is the closure Codex asked to see: the predator's exponent is
  the predator rate TIMES the window duration, the window duration is
  set by the RELAY's own clock, and the relay clock carries the
  relay's coupling - so the two couplings enter as ONE quotient,
  predator over relay, in a single place; (5) the window duration's
  remaining factors - EXACT (the elliptic period closed form and the
  difference factors); (6) the band-scale drain bracket - EXACT
  two-sided band inequality (the s|D| repair); (7) the theta-charge -
  EXACT (the displayed choice). No eighth ingredient exists in the
  filed chain; the sweep cites each note by object. EVERY nonexact
  content routes through the single symbol gamma, as required.
  (ITEM 2, THE FINITE-N DISPLAY - linear beats bounded, exact in
  gamma) REQUIRED side: the composed scalar retention rises
  monotonically in the chop number toward its limit (Codex's own
  composition identity direction), so the required extra tax is
  bounded by half a log-two for every chop number - bounded, exact,
  displayed. DELIVERED side: per-step drain is nondecreasing in the
  exponent (filed monotonicity), the exponent is increasing in the
  chop number (item 3), so delivered at chop N is at least N times
  the per-step drain evaluated at the chop-three exponent - LINEAR in
  N with slope an exact function of gamma. THE CROSSING: the chop
  number where the linear floor passes the bounded requirement is an
  exact function of one symbol - beyond it, structure wins by the
  display; the window is the finitely many chop numbers from three to
  that crossing, exact in gamma. Both demanded items are now
  displayed; the one-constant reduction awaits only Codex's
  acceptance, and with T7's closed form for the G-ratio in the line-
  check queue, the entire remaining quantitative question of the
  campaign is: does one exact trigonometric expression clear one
  exact algebraic threshold at finitely many integers. PRESS POINTS:
  sweep item 4's quotient closure (predator rate times relay-clock
  duration - the one line where the two couplings meet); the
  monotone-composition citation on the required side. Outstanding:
  Codex's queue (this note; T7 line check; TV downstream displays);
  then the symbolic comparisons; chapter 10. Filed is not cleared.
  No closure claimed.
---

# The tracing sweep and the finite-N display

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-zero-set-tracing-regate-20260705.md`. The
one-symbol tracing sweep is accepted as the right candidate shape, not
as a clearance. It still needs a source map showing that the six
non-\(G\) factors are exact or already quarantined, and the finite-\(N\)
comparison must be written in logarithmic/multiplicative currency so
per-step taxes add without double-counting a shrinking retained
package. Filed is not cleared.

## Codex tracing clearance, same date

Codex clearance filed at
`mpp-forward-gold-codex-tracing-logcurrency-clearance-20260705.md`.
The amended source map and logarithmic-currency display clear the two
specific tracing objections: no second unnamed compactness constant
survives outside the \(G\)-ratio, and per-step taxes add as logs of
retention factors without double-counting a shrinking package. This
does not clear the \(G\)-ratio shape inequality, TV downstream displays,
or the symbolic middle-window comparisons.

## 1. Seven factors, one symbol

The delivered drain at chop N, factor by factor with its filed
source: invariant share (exact identity); seed logarithm (exact
choice); exponent-versus-chop (exact difference factors); rate
prefactor (THE G-RATIO - and the quotient closes here: the predator's
exponent is its rate times the relay-clock window, so predator and
relay couplings enter as one quotient in one place); window duration
remainder (exact elliptic period); band bracket (exact two-sided);
theta-charge (exact choice). No eighth ingredient. One tollgate on
every road.

## 1b. The source map (added per the re-gate)

The six non-G factors, each cited to its filed object: (1) invariant
drain share - the two-invariant identity, ramp-rectification note
section 4 and R2a ratio theorem (audited); (2) seed logarithm -
sD-multiplier-repair note, amplitude-split display (theta an exact
choice); (3) exponent-versus-chop - predator clock theorem,
theory-currency reshape clause (iii) as corrected, difference factors
exact under R2b scaling (audited); (5) window-duration remainder -
elliptic write-up section 2 (period closed form) and target-5
one-period display; (6) band bracket - sD-multiplier-repair note,
two-sided band inequality; (7) theta-charge - sD-multiplier-repair
and target-4 three-displays notes (choice inequality displayed). Each
is exact or already quarantined as a named choice; none carries an
unnamed constant.

## 2b. The comparison in logarithmic currency (added per the re-gate)

Per-step taxes are stated as log-energy tolls: retention factors
MULTIPLY along the octave, so their logarithms ADD - the additive
ledger is the logarithmic one by construction, and the shrinking
retained package is counted once because each step's toll is the log
of ITS OWN retention factor applied to whatever arrives. Delivered
per octave in log currency: at least the chop number times the log
of one over (one minus the per-step bite at the chop-three
exponent) - at least linear in the chop number by monotonicity of
the log. Required in log currency: the log of twice the composed
scalar retention - bounded by half a log-two. Linear beats bounded
in the SAME currency; no multiplicative-versus-additive mismatch
survives, and no package is double-counted because logs of factors
along one lineage add exactly once.

## 2. Linear beats bounded, exact in gamma

Required: monotone in chop number, bounded by half a log-two -
displayed. Delivered: at least chop number times the per-step drain
at the chop-three exponent - linear, slope exact in gamma. The
crossing chop number is exact in gamma; beyond it structure wins;
the window is finite and exact. With T7's closed form, the campaign's
last quantitative question reads: does one exact trigonometric
expression clear one exact algebraic threshold at finitely many
integers.

## 3. Register

Both conversion items displayed; press points: the quotient closure;
the monotone-composition citation. Outstanding: Codex's queue; T7
line check; TV downstream displays; the symbolic comparisons; chapter
10. Filed is not cleared. No closure claimed.
