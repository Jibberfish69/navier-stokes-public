---
theorem_id: forward-gold-tv-downstream-three-displays-20260705
status: tv-downstream-displays-written-price-functions-boundary-routing-speed-bound-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / TV downstream displays (three items)
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie.
completion_truth: >-
  Writes the three TV-downstream displays Codex left open, pending
  re-gate; the symbolic window comparisons remain BLOCKED by the
  campaign's own discipline until the T7 line check clears (nothing
  leans on the formula before the auditor's check - my filed rule,
  kept). (DISPLAY 1, THE PRICE FUNCTIONS ON ONE DOMAIN) Domain: the
  closed capped band from the shallow edge to the cap depth. The
  SHALLOW price is the filed race integral composed with the
  unsuppressed-phase closed form at the depth's modulus and the active
  fraction - every factor explicit, DEFINED ON THE WHOLE DOMAIN,
  continuous, positive at every depth up to and including the cap
  (the cap ends the band before the phase floor's asymptote - the
  capped-positivity line, now attached to an explicit function). The
  DEEP price is the race integral fed by the contraction-margin
  phase, defined on the whole domain by taking the margin's positive
  part - zero at and below the crossing, positive and increasing
  above it. VALIDITY: the shallow bound's hypothesis is the
  unconditional un-parking identity (valid everywhere on the domain);
  the deep bound's hypothesis is margin positivity (valid exactly
  where the function is nonzero - the max never consumes an invalid
  bound because the invalid region contributes zero). FLOOR:
  max of the two is at least the shallow price at the cap depth - one
  explicit positive closed form, the displayed no-common-zero floor.
  (DISPLAY 2, SWING-COUNTING BOUNDARY ROUTING) A window's total
  variation counts complete swings plus at most one incomplete swing
  at each end: complete-swing count is bracketed by throughput over
  amplitude with a boundary correction of AT MOST TWO SWINGS - a
  named integer on the supply side, exactly like the band
  multiplicity; the financing floor absorbs it because the period cap
  already forces the complete-swing count above any fixed integer on
  financed windows (few swings means long periods means the cap
  bites first). Displayed: the bracket, the integer, the absorption.
  (DISPLAY 3, THE CAPPED-BAND SPEED BOUND) The package speed is twice
  the coupling times the square root of the cubic, whose maximum over
  the swing interval is an exact closed form in the levels; the speed
  bound converts total-variation floors into period ceilings (a floor
  on distance at bounded speed is a floor on time - displayed as the
  quotient), completing the TV-to-period-cap link with no implicit
  step. STATE AFTER THIS NOTE: every display item from every Codex
  demand is filed; the only theory item remaining is the symbolic
  window comparisons, blocked on the T7 line check by our own
  discipline; chapter 10 possibly calibration-only. PRESS POINTS: the
  positive-part device in the deep price (the max never uses an
  invalid bound - one sentence); the boundary integer's absorption
  citation. Outstanding: Codex's queue; T7 check; comparisons;
  chapter 10. Filed is not cleared. No closure claimed.
---

# The three TV-downstream displays

## 1. Price functions on one domain

Domain: the closed capped band. Shallow price: race integral times
unsuppressed-phase closed form times active fraction - explicit,
continuous, positive on the whole closed domain (the cap ends the
band before the asymptote). Deep price: race integral fed by the
POSITIVE PART of the contraction margin - defined everywhere, zero
at and below the crossing, increasing above. Validity is built in:
the shallow hypothesis is unconditional; the deep bound is nonzero
only where its hypothesis holds, so the max never consumes an
invalid bound. Floor: the shallow price at the cap depth - one
explicit positive closed form.

## 2. Swing counting, boundary routed

Total variation = complete swings plus at most one incomplete swing
per window end: the count is bracketed by throughput over amplitude
with a boundary correction of at most TWO - a named supply-side
integer, absorbed because financed windows carry complete-swing
counts above any fixed integer (else the period cap bites first).
Bracket, integer, absorption: displayed.

## 3. The speed bound

Package speed = twice the coupling times the root of the cubic;
its swing-interval maximum is an exact closed form in the levels.
Distance floors at bounded speed are time floors: the TV-to-period
link is a displayed quotient, nothing implicit.

## Register

All display items from all Codex demands are now filed. Remaining
theory: the symbolic window comparisons, blocked on the T7 line
check by our own rule. Press points: the positive-part device; the
absorption citation. Outstanding: Codex's queue; chapter 10. Filed
is not cleared. No closure claimed.
