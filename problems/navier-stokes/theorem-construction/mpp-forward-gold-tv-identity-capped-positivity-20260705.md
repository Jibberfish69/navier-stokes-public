---
theorem_id: forward-gold-tv-identity-capped-positivity-20260705
status: tv-identity-collapses-financed-chain-capped-band-floors-max-noncircularity-dag-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / consumption of the max/financed-family re-gate
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  transfer IS motion - the fluid pays only when the package moves, so
  the ledger of payment and the ledger of motion are one ledger, and
  every dwell is a confession.
completion_truth: >-
  Consumes the max/financed-family re-gate, pending re-gate, around ONE
  exact identity that collapses most of the demanded estimates. (THE TV
  IDENTITY) The transfer rate IS the package variable's velocity - the
  five-mode extraction enters the package's energy as its time
  derivative - so per-window throughput equals HALF THE TOTAL VARIATION
  of the package variable over the window, exactly: an identity, not an
  estimate. Transfer only happens when the package moves; the payment
  ledger and the motion ledger are the same ledger. CONSEQUENCES, each
  demanded item in order: (i) THROUGHPUT-TO-SWING, weighted: total
  variation per window = number of swings times twice the swing
  amplitude, exactly; the throughput floor therefore floors swings
  times amplitude - displayed, an identity plus one product floor.
  (ii) BOTTOM-DWELL-TO-ROOT and the PERIOD CAP: the throughput floor
  caps the period (few swings per window means long periods; the TV
  identity converts the floor into a period ceiling), and the period is
  the SUM of the two logarithmic dwells - top and bottom - each the
  same elliptic closed form at its own turning point; so ONE line caps
  BOTH dwell logarithms: the parking cap is RE-DERIVED and the
  bottom-root floor is DERIVED, simultaneously, from the same period
  ceiling (the dwell is logarithmic in the root's distance, so a
  capped dwell floors the distance). (iii) LEVEL-GAP BOUNDS: the level
  gaps in the root identity are at least the swing amplitude (the
  bottom root sits at least one swing below the ceiling), and the
  swing amplitude is floored by item (i): the root identity's factors
  are all floored by the SAME throughput floor. (iv) NONCIRCULARITY,
  the DAG displayed: the financing requirement is DEFINITIONAL (the
  enemy's own per-octave doubling demand - horn (a) of the throughput
  dichotomy prices sub-financing windows with NO invariant input);
  the chain runs financing -> TV identity -> period cap -> dwell caps
  -> root floors -> invariant floor -> contraction margins -> deep
  price - one direction, no edge returns, displayed as a list; windows
  off the financed family are horn (a), priced independently. (v) THE
  MAX'S POSITIVE FLOOR WITH NO COMMON ZERO: on the CAPPED band the
  shallow price never vanishes - the phase floor is positive at every
  depth up to the cap (suppression is asymptotic, never total, and the
  cap ends the band before the asymptote) - so the max is at least
  the shallow price at the cap depth: a single positive closed-form
  floor on one displayed domain, monotonicity giving the minimum at
  the deep end; the deep price only improves the deep end further.
  THE PARKING CAP PAYS AGAIN: it is the cap that keeps the shallow
  price alive across the whole band - the enemy's depth limit is the
  proof's positivity. (vi) JACOBIAN TIE-BACK: the normalization map
  from the Gate-6 cubic to the normalized roots has Jacobian factors
  the swing amplitude and the root spread - the first floored by (i),
  the second bounded by the ceiling: bounded nonzero on the financed
  band, displayed. PRESS POINTS: the TV identity's one line (transfer
  rate equals package velocity - from the five-mode equations
  directly); the period-equals-sum-of-dwells decomposition (elliptic,
  standard, worth its line). Outstanding: Codex's queue; chapter 10.
  Filed is not cleared. No closure claimed.
---

# The TV identity: payment is motion

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-tv-middle-window-regate-20260705.md`. The TV
identity is cleared only in its typed gross-variation form:
throughput is half the total variation of the same package variable
after the package channel and orientation have been fixed. The
downstream claims remain open at display level: swing counting needs
boundary-term routing, period caps need the capped-band speed bound,
and the max floor needs explicit shallow/deep price functions on the
same depth domain. Filed is not cleared.

## 1. The identity

The extraction enters the package's energy as its time derivative, so
per-window throughput equals half the total variation of the package
variable - exactly. Transfer is motion; every dwell is a confession
of non-payment. One ledger, not two.

## 2. What it collapses

Throughput floor = swings-times-amplitude floor (identity plus one
product). Period cap from the same floor; the period is the sum of
the two logarithmic dwells, so one line caps both - the parking cap
re-derived and the bottom-root floor derived simultaneously. Level
gaps at least the swing amplitude, floored by the same throughput.
The root identity's every factor is floored by the enemy's own
financing.

## 3. Noncircularity, as a displayed DAG

Financing is definitional (horn (a) prices sub-financing with no
invariant input). The chain runs one way: financing, TV identity,
period cap, dwell caps, root floors, invariant floor, contraction,
deep price. No edge returns.

## 4. The max's floor, positive with no common zero

On the capped band the shallow price never vanishes: phase
suppression is asymptotic and the cap ends the band first. The max is
at least the shallow price at the cap depth - one positive closed
form on one displayed domain. The parking cap pays again: the enemy's
depth limit is the proof's positivity.

## 5. Jacobian tie-back

Normalization Jacobian factors: swing amplitude (floored by the TV
identity) and root spread (bounded by the ceiling). Bounded nonzero
on the financed band.

## Register

Re-gate consumed around one identity; press points: the TV line; the
period-as-sum-of-dwells line. Outstanding: Codex's queue; chapter 10.
Filed is not cleared. No closure claimed.
