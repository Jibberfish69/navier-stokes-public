---
theorem_id: forward-gold-upgraded-four-body-current-frontier-20260621
status: consolidated-frontier-installed-strict-cycle-payment-or-rigidity-open
logical_landing_node: upgraded_four_body_current_frontier
edge_effect: "Consolidates the upgraded four-body program after the Body I factorial tower radius-spend form, Body II participation exchange form, Body III no-loss relay form, Body IV deformation-gradient return form, packet-selector polar retention criterion, and same-carrier terminal atom accounting. The four bodies now have exact normal forms, but none alone supplies the unweighted selected terminal reserve. The remaining gold theorem is the coupled one: strict same-carrier four-body cycle payment, or rigidity of every lossless four-body cycle."
parents:
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-bodyi-factorial-tower-radius-spend-normal-form-20260621.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-bodyii-participation-exchange-normal-form-20260621.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-bodyiii-no-loss-relay-normal-form-20260621.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-bodyiv-deformation-gradient-return-normal-form-20260621.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-packet-selector-polar-retention-criterion-20260621.md
  - problems/navier-stokes/theorem-construction/mpp-forward-gold-same-carrier-four-body-terminal-atom-accounting-20260621.md
---

# Upgraded Four-Body Current Frontier

Date: 2026-06-21

## 0. Aim

The upgraded four-body program is now best read as one coupled balance loop:

```math
\text{tower/scale}
\to
\text{participation}
\to
\text{compactness/no-loss}
\to
\text{gradient/geometry}
\to
\text{tower/scale}.
\tag{4BF.1}
```

The individual body normal forms are installed.  The loop is not closed.

The exact frontier is:

```math
\boxed{
\text{strict same-carrier four-body cycle payment}
}
\tag{4BF.2}
```

or

```math
\boxed{
\text{rigidity of every lossless four-body cycle.}
}
\tag{4BF.3}
```

This note consolidates the proof state so the old four-body idea is not
flattened into one body, one estimate, or one diagnostic defect.

## 1. Body I gives tower radius-spend, not selected-carrier domination

Body I has the exact normal form

```math
\boxed{
dL_S+dD_S^{\rm rad}=dJ_{SQ}.
}
\tag{4BF.4}
```

Here \(D_S^{\rm rad}\ge0\) is the radius-spend measure generated by moving
factorial/Gevrey radii:

```math
D_S^{\rm rad}
=
\sum_{(m,\alpha)}
\left(
-m{\tau'\over\tau}
-|\alpha|{\rho'\over\rho}
\right)
w_{m,\alpha}E_{m,\alpha}.
\tag{4BF.5}
```

The factorial weights remove the mixed binomial obstruction:

```math
{\tau^m\rho^{|\alpha|}\over m!\,\alpha!}
{m\choose a}{\alpha\choose\beta}
=
{\tau^a\rho^{|\beta|}\over a!\,\beta!}
{\tau^{m-a}\rho^{|\alpha-\beta|}
\over (m-a)!\,(\alpha-\beta)!}.
\tag{4BF.6}
```

Thus Body I supplies exact tower bookkeeping and a real nonnegative
radius-spend term.  It does not prove

```math
\boxed{
dA_{\rm sel}\le C\,dD_S^{\rm rad}+C\,dR_S.
}
\tag{4BF.7}
```

That is a same-carrier domination theorem still missing.

## 2. Body II gives signed participation, not positive payment

Body II has the exact local participation form

```math
\boxed{
dM_Q+dD_Q=dJ_Q,
\qquad
dD_Q\ge0.
}
\tag{4BF.8}
```

The current \(J_Q\) is signed.  A selected positive carrier formed before
signed summation obeys

```math
dA_{\mathcal S}
\le
dJ_{\mathcal S}^-
+
dD_{\mathcal S}
+
(dM_{\mathcal S})^+
+
dN_{\rm ret}
+
dR_{\rm legal}
+
dN_{\rm miss}.
\tag{4BF.9}
```

So Body II pays selected positive activity only through retained negative
current, true loss, storage jump, retained negative local partner, legal
residual, or the explicit selector defect \(dN_{\rm miss}\).

The missing Body-II production theorem is:

```math
\boxed{
dN_{\rm miss}=0
\quad\text{or}\quad
dN_{\rm miss}\text{ is legally charged.}
}
\tag{4BF.10}
```

This is the finite-packet form of terminal signed saturation / source-weighted
polar saturation.

## 3. Body III gives weak relay, not no-loss identification

Body III has the exact relay form:

```math
dL_n+dD_n+dA_n+dJ_n
\le
d\widetilde J_n+dR_n
\quad
\Longrightarrow
\quad
dL_*+dD_*+dA_*+dJ_*
\le
d\widetilde J_*+dR_*.
\tag{4BF.11}
```

This is weak packet compactness.  The no-loss theorem is stronger:

```math
\boxed{
\mathcal P_*=\mathcal P[U,P].
}
\tag{4BF.12}
```

Failure of `(4BF.12)` is the Body-III relay defect

```math
dK_C^+
=
\left(
dK_{\widetilde J}
+
dK_R
-
dK_L
-
dK_D
-
dK_A
-
dK_J
\right)^+.
\tag{4BF.13}
```

So Body III closes only if

```math
\boxed{
dK_C^+=0
\quad\text{or}\quad
dK_C^+\text{ is a charged no-loss defect.}
}
\tag{4BF.14}
```

## 4. Body IV gives the geometry return arrow, not the unweighted clock

Body IV has the exact deformation-gradient equations:

```math
\partial_tF=(\nabla u)(X(a,t),t)F,
\qquad
\det F=1,
\tag{4BF.15}
```

and, for \(A=F^{-1}\), \(G=AA^\top\),

```math
\partial_tG
=
-2A\,S(X(a,t),t)\,A^\top.
\tag{4BF.16}
```

The same-carrier strain clock

```math
\Gamma_{A_0}(t_0,t)
=
\int_{t_0}^{t}
\|S(X(\cdot,\tau),\tau)\|_{L^\infty(A_0)}
\,d\tau
\tag{4BF.17}
```

keeps the pulled-back metric uniformly elliptic:

```math
e^{-2\Gamma_{A_0}}|\zeta|^2
\le
\zeta^\top G(a,t)\zeta
\le
e^{2\Gamma_{A_0}}|\zeta|^2.
\tag{4BF.18}
```

Thus Body IV can recertify Body I if the same-carrier deformation clock and
coefficient geometry remain controlled.

The missing Body-IV production theorem is the unweighted clock/action bound:

```math
\boxed{
\int_{\sigma_0}^{\infty}
A_{\rm strain}(\sigma)\,d\sigma<\infty
}
\tag{4BF.19}
```

or a charged geometry defect when `(4BF.19)` fails.

## 5. Same-carrier atom accounting is already abstractly solved

If the four body inequalities have the oriented same-carrier form

```math
dL_i+dD_i+c_i\,dA_i+dJ_i
\le
dJ_{i-1}+dR_i,
\tag{4BF.20}
```

then summing over the cycle gives

```math
\boxed{
dL_{4B}+dD_{4B}+dA_{4B}
\le
dR_{4B}.
}
\tag{4BF.21}
```

Consequently,

```math
\boxed{
\int_{\sigma_0}^{\infty}A_{4B}(\sigma)\,d\sigma
\le
L_{4B}(\sigma_0)-\inf L_{4B}
+
R_{4B}((\sigma_0,\infty)).
}
\tag{4BF.22}
```

This is the desired unweighted log-scale reserve.

The abstract accounting is not the open part.  The open part is producing
`(4BF.20)` from the actual Navier-Stokes bodies on the same selected carrier.

## 6. The exact coupled frontier

Putting the four installed normal forms together, the current live theorem is:

```math
\boxed{
\text{FourBodyStrictSameCarrierCycle.A}
}
\tag{4BF.23}
```

with content:

```math
\boxed{
\begin{aligned}
&\text{for a retained terminal heat-scale selected carrier,}\\
&\text{the Body I radius spend, Body II participation loss, Body III no-loss
relay, and Body IV deformation action}\\
&\text{combine on the same carrier to produce }
dA_{\rm sel}\text{ in the four-body sum,}\\
&\text{with all mismatch terms charged as legal residual.}
\end{aligned}
}
\tag{4BF.24}
```

In formula form, the needed output is

```math
\boxed{
dL_{4B}
+
dD_{4B}
+
c\,dA_{\rm sel}
\le
dR_{\rm legal},
\qquad
c>0,
}
\tag{4BF.25}
```

after internal exchange currents cancel.

The exact failure terms that must vanish or be charged are:

```math
\boxed{
dK_{4B}
\quad
\text{carrier mismatch between body exchange currents;}
}
\tag{4BF.26}
```

```math
\boxed{
dK_{\rm sel}
\quad
\text{positive-selector polar-retention failure;}
}
\tag{4BF.27}
```

```math
\boxed{
dK_C^+
\quad
\text{Body-III compactness/no-loss relay failure, including selected positive
activity disappearing in the compact limit;}
}
\tag{4BF.28}
```

and

```math
\boxed{
dK_G^+
\quad
\text{Body-IV geometry/strain-clock failure.}
}
\tag{4BF.29}
```

## 7. Lossless-cycle rigidity alternative

The same frontier can be stated without trying to force a positive payment in
one body.

Assume the selected terminal cycle is lossless:

```math
dD_S^{\rm rad}=0,
\qquad
dD_Q=0,
\qquad
dK_{\rm sel}=0,
\qquad
dK_C^+=0,
\qquad
dK_G^+=0,
\qquad
dR_{\rm legal}=0.
\tag{4BF.30}
```

Then the required rigidity theorem is:

```math
\boxed{
\text{every lossless same-carrier four-body cycle has }
dA_{\rm sel}=0.
}
\tag{4BF.31}
```

This is the same burden as `(4BF.25)`, read through rigidity rather than
payment.

It says that a nonzero terminal selected pulse cannot circulate forever through
tower, participation, compactness, and deformation geometry while every body
records zero loss and zero mismatch.

The compactness part of this statement is now sharpened.  The loop does not
need a vague continuity assumption for \(A_{\rm sel}\).  It needs selected
positive activity persistence:

```math
X_n\to X,
\qquad
A_{\rm sel}(X_n)\ge\eta,
\qquad
K_{{\rm sel},n}+K_{C,n}^+ +R_{{\rm legal},n}\to0
\Longrightarrow
A_{\rm ret}(X)\ge\eta_*(\eta)>0.
\tag{4BF.31a}
```

If `(4BF.31a)` fails, the failure is not a new fifth body.  It is charged to
the existing selector/compactness/legal defects \(K_{\rm sel}\), \(K_C^+\), or
\(R_{\rm legal}\).

The measure-level way to produce `(4BF.31a)` is also exact.  In the finite
selected tower packet, the nonnegative selected carrier measure is the measure
with density \([h_P]_+\) on the extended carrier.  Body III still must prove
tightness of those measures on the normalized same-carrier carrier and identify
their weak limit with the canonical selected carrier of the limiting
Navier-Stokes profile up to \(K_C^+\).  Signed-current compactness alone is not
enough, because it can cancel the positive carrier.

## 8. Current conclusion

The upgraded four-body resurrection has not solved the Navier-Stokes MPP.

It has changed the frontier from many disconnected supplier attempts into one
coupled theorem:

```math
\boxed{
\text{strict same-carrier four-body cycle payment}
\quad\text{or}\quad
\text{lossless four-body cycle rigidity.}
}
\tag{4BF.32}
```

The next proof work should attack `(4BF.25)` or `(4BF.31)` directly, without
respawning the four bodies as isolated routes.
