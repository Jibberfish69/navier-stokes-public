---
theorem_id: forward-gold-w-eff-substitution-20260705
status: codex-regated-original-min-definition-not-cleared-half-min-correction-installed
created: 2026-07-05
problem: navier-stokes
route: forward-gold / goal item 6 / the effective half-width substitution
authorship: Claude (Fable), 2026-07-05. Codex's own displayed conditions,
  substituted; the positivity of the effective width comes from the
  branch's own ordering. Physical story Thomas Birnie: the road home is
  wide enough exactly because the parent sits below the child - the
  hierarchy that makes the relay a relay is what keeps the payback
  lane open.
completion_truth: >-
  Codex re-gated the original substitution and found the raw minimum
  definition too wide at the closed endpoints. The valid effective
  half-width is the corrected strict-margin choice
  w_eff = (1/2) min{w_packet, phi_min, arccos(k/(2q)) - phi0}, exactly
  as filed in codex-sector-admissible-slice-width. With this correction
  the interval remains inside the monotone angle arc and the
  hyperbolic sector c(alpha)<q; the f1-f2 brackets and slice integral
  may be evaluated at w_eff. This clears the admissible-width
  substitution only in the half-min form. The symbolic comparisons
  still inherit the explicit w_eff factor; TV downstream and chapter
  10 remain open. Filed is not cleared beyond this corrected width
  choice. No closure claimed.
---

# The effective half-width, substituted

## Codex re-gate update, same date

Codex re-gate filed at
`mpp-forward-gold-codex-w-eff-tv-actual-displays-regate-20260705.md`.
The original definition in this note used the raw minimum without the
strict-margin half factor. That does not clear the admissible-slice
condition: equality at the hyperbolic endpoint gives \(c=q\), and
equality at the angle floor can touch the boundary of the monotone arc.
The corrected admissible width is
\[
w_{\rm eff}
=
\frac12\min\{w_{\rm packet},\phi_{\min},
\arccos(k/(2q))-\phi_0\}.
\]
With this correction the sector-admissible slice is cleared exactly as
in `mpp-forward-gold-codex-sector-admissible-slice-width-20260705.md`.
The symbolic comparisons still inherit the explicit \(w_{\rm eff}\)
factor. Filed is not cleared beyond this corrected width choice.

## 1. Definition (Codex's conditions, verbatim)

  w_eff = (1/2) min( w_packet, phi_min, arccos(k/(2q)) - phi0 ).

The third condition is the slice admissibility inequality solved for
the width: substituting w_eff makes the slice sector-admissible by
construction.

## 2. Positivity, from the branch's own ordering

The sector margin is positive iff p < q - the relay branch's own
ordering (k < p < q), strict on the financed family with its ordering
margin as the exact floor. The packet width has the concentration-
frame floor; the angle floor is the angular appetite. A minimum of
three floored positives is floored: w_eff carries an exact positive
floor in filed symbols. The hierarchy that makes the relay a relay
keeps the payback lane open; the enemy cannot close it without
breaking its own cascade order.

## 3. The substitution

Every bracket and the slice integral of the cleared formula display,
evaluated at w_eff: the slice measure becomes 2 w_eff; every other
factor is unchanged; at zero width everything still collapses to the
cleared home-center identity. Displayed algebra at a positively-
floored width - the shape gate's remaining exact burden, discharged
at display level.

## 4. Register

Substitution filed pending re-gate; press point: the ordering-margin
citation. Outstanding: TV downstream; the symbolic comparisons;
chapter 10. Filed is not cleared. No closure claimed.
