# Door 1 Weighted Skew To Terminal Signed Saturation

Date: 2026-06-18

Status: Door 1 continuation; `WeightedLiftedSkewDefectLegal.A` not proved;
post-ASAC core reduced to terminal signed-partner saturation.

2026-06-18 continuation: the direct shadow-ledger attempt is recorded in
`mpp-forward-gold-door1-shadow-ledger-attempt-20260618.md`.  It pays the finite
edge-pairing cases and reduces the live Door 1 child to
`Door1TerminalSourceTimeAntiAtom.A` or `StressFluxZenoRefillRigidity.A`.

Role: continue from the current actual gold frontier:

```math
\boxed{
\text{WeightedLiftedSkewDefectLegal.A}
}
\qquad\text{or}\qquad
\boxed{
\text{ZenoNoDriftUniqueTangent.A}
+
\text{RenormalizedZenoStationarity.A}.
}
\tag{WSS.1}
```

This note works the Door 1 side.

## Checked incoming attempts

The checked incoming attempt is

```text
mcp-weightedliftedskewdefectlegalattempt-a-weightedliftedskewdefectlegal-a-projectedlocalprecauchyresidueidentity-a-01109edc57.md
```

It correctly records that `WeightedLiftedSkewDefectLegal.A` is not closed from
installed inputs.

The older installed reassembly notes sharpen what the obstruction actually is:

```text
mcp-weighted-lifted-skew-defect-reassembly-after-asac-20260505.md
mpp-signed-positive-balance-post-asac-frontier-update-20260516.md
mcp-projected-cutoff-ledger-20260505.md
mcp-active-strain-alignment-cost-continuation-20260505.md
```

Those surfaces change the internal shape of Door 1.

## Paid pieces inside the weighted-skew gate

`ProjectedCutoffLedger.A` is locally discharged on the projected affine packet
branch.  Pressure gradients vanish against projected solenoidal packets,
projection commutators are legal residuals, and cutoff commutators are absorbed
through the transported packet-factor ledger.

The absolute `PairWeightFlattening.A` target is too strong for the positive
source consumer.  Absolute control returns to the source wall.  After ASAC, the
one-sided positive receiver defect is instead paid as

```math
\text{ASAC.A}
\Longrightarrow
\text{PositivePairWeightDefectCharge.A}.
\tag{WSS.2}
```

The active strain-alignment carrier inside the lifted commutator is routed by
the ASAC parent-drain assembly:

```math
\text{TPNI.A}
+\text{PacketNormalForm.A}
+\text{SourceTether.A}
+\text{LimitPass.A}
\Longrightarrow
\text{ActiveStrainAlignmentCost.A}.
\tag{WSS.3}
```

Thus the current Door 1 obstruction is not `ProjectedCutoffLedger.A`, and it is
not absolute `PairWeightFlattening.A`.  Those are either paid locally or wrong
as post-ASAC positive-source targets.

## Current Door 1 core

After the paid pieces are removed, the remaining Door 1 theorem is

```math
\boxed{
\text{TerminalSignedSaturation.A}.
}
\tag{WSS.4}
```

Its content is edgewise:

```math
d\pi^+(e)
\le
d\pi^-(e^*)
+
d\mathcal D_{\mathrm{legal}}(e),
\qquad
\sum_e d\mathcal D_{\mathrm{legal}}(e)<\infty.
\tag{WSS.5}
```

In words: every selected positive terminal source edge must keep a signed
counter-edge at the same normalization, or pay an installed legal cost.

This is the missing statement that turns the bare signed current into a usable
terminal signed ledger after localization, weighting, lifting, terminal active
selection, and positive source extraction.

## Door 1 conditional closure

The valid post-ASAC package is

```math
\boxed{
\text{PositivePairWeightDefectCharge.A}
+
\text{TerminalSignedSaturation.A}
+
\text{ProjectedCutoffLedger.A}
\Longrightarrow
\text{TerminalPositiveSkewLedger.A}.
}
\tag{WSS.6}
```

Then `TerminalPositiveSkewLedger.A` supplies the missing legal comparison in
`WeightedLiftedSkewDefectLegal.A`, which supplies
`ProjectedLocalPreCauchyResidueIdentity.A`, which closes the `\Phi_0\ne0` door
by the already-paid finite selector and positive-part step.

Thus:

```math
\boxed{
\text{TerminalSignedSaturation.A}
\Longrightarrow
\text{Door 1 closes, modulo already paid ASAC/cutoff/selector pieces.}
}
\tag{WSS.7}
```

## Why this is still open

Bare dyadic skew exchange happens before terminal localization and positive
selection.  The selected positive edge can survive in the terminal carrier while
the signed counter-edge moves outside the selected family, outside the same
terminal active window, into a donor packet not selected as bad, or into a
spill/cutoff collar.

Existing legal ledgers route many such terms, but no installed theorem proves
the edgewise saturation statement `(WSS.5)` for every selected positive
terminal source edge.

The equivalent internal formulation is

```math
\boxed{
\text{InternalDonorSinkDepletion.A}.
}
\tag{WSS.8}
```

It says that an internal legal same-fluid donor sink cannot feed the terminal
receiver without either keeping the signed partner in the ledger or paying a
finite donor/legal charge.

## Door 1 verdict

The live Door 1 frontier is sharper than the name
`WeightedLiftedSkewDefectLegal.A`:

```math
\boxed{
\text{TerminalSignedSaturation.A}
\quad\text{or equivalently}\quad
\text{InternalDonorSinkDepletion.A}.
}
\tag{WSS.9}
```

`WeightedLiftedSkewDefectLegal.A` remains the enclosing gate inside
`ProjectedLocalPreCauchyResidueIdentity.A`, but the actual unpaid mathematical
content is terminal signed-partner saturation.

## Door 2 remains unchanged

The Door 2 side remains

```math
\boxed{
\text{ZenoNoDriftUniqueTangent.A}
+
\text{RenormalizedZenoStationarity.A}.
}
\tag{WSS.10}
```

Those are the first hard production subtheorems needed before
`ZeroFluxLandauClassLiouville.A` can consume the exact Landau-class profile.
