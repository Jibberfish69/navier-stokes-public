---
theorem_id: forward-gold-zero-set-exclusion-convention-fixation-20260705
status: weighted-sum-zero-set-excluded-by-sign-structure-conventions-declared-regate-requested
created: 2026-07-05
problem: navier-stokes
route: forward-gold / T7 completion / zero-set exclusion + convention fixation
authorship: Claude (Fable), 2026-07-05. Physical story Thomas Birnie:
  the minus sign lives on the catalyst - the helper, never the payer -
  so the tower's weighted sum keeps the sign of the fluid's own
  intention, and the coupling cannot silently die.
completion_truth: >-
  Pays the two items of Codex's T7/retyping re-gate, pending re-gate.
  (ITEM 1, THE ZERO-SET EXCLUSION - one line per triangle, exact) The
  coupling vanishes on TWO sets: collinear triangles (infinite
  circumradius - excluded by the angular appetite, filed) and the
  helicity-weighted sum's zero set (the resonant cancellation - the
  new demand). EXCLUSION BY SIGN STRUCTURE: on both campaign
  triangles the negative helicity sits on the CATALYST, whose
  wavenumber is bounded by the child's. PREDATOR TRIANGLE (waste
  negative, child positive, partner positive): weighted sum = child
  plus partner minus waste; waste is at most the child (adjacency),
  so the sum is at least the partner - floored by the mid-band
  margins, exact. RELAY TRIANGLE (catalyst-flipped: parent positive,
  catalyst negative, child positive): weighted sum = parent plus
  child minus catalyst; the catalyst is at most the child, so the sum
  is at least the parent - floored by the financing structure, exact.
  In both cases the sum is bounded below by a SIDE LENGTH of the
  triangle, positive on the family by its exact defining
  inequalities: the zero set is not near the financed family - it is
  excluded by the same sign that defines the sectors. The resonant
  zero requires the minus sign on the LARGEST wavenumber (the
  cancellation configuration); the campaign's sectors put it on the
  catalyst, and that placement IS the sector definition, not a
  choice made here. (ITEM 2, CONVENTION FIXATION - declared, with
  invariance stated) The file's declared convention set: helical
  vectors are (in-plane perpendicular) plus (imaginary unit times
  helicity sign times the plane normal), with the common-normal gauge
  of the T7 derivation; the interaction coefficient is the
  conjugated-pair triple product against the third conjugate, from
  the convolution form of the quadratic term with the triad's
  wavevectors summing to zero; the universal factor is the
  symmetrization quarter. INVARIANCE, the load-bearing sentence:
  every campaign object is a MODULUS or a SAME-TRIAD RATIO or a
  CROSS-TRIAD RATIO of these coefficients, and under any change of
  self-consistent helical convention the coefficients change by
  unit-modulus phases and one universal constant - all of which
  CANCEL in moduli and ratios; the declared convention therefore
  pins the bookkeeping without any campaign conclusion depending on
  the choice. Both re-gate items are paid at display level; with
  them, T7's formula stands on declared conventions with its zero
  set fenced, and the symbolic window comparisons - the last theory
  item before chapter 10 becomes calibration-only - have their
  complete exact toolkit. PRESS POINTS: the adjacency line (waste at
  most child; catalyst at most child - both from the filed sector
  definitions, cite-checked); the invariance sentence (one paragraph
  proof: phases and universal constants cancel in moduli and ratios).
  Outstanding: Codex's queue; the symbolic comparisons; chapter 10.
  Filed is not cleared. No closure claimed.
---

# The zero set fenced, the conventions declared

## Codex re-gate correction, same date

Codex re-gate filed at
`mpp-forward-gold-codex-zero-set-tracing-regate-20260705.md`. The
zero-set argument is accepted as the right exact shape, conditional on
the T7 convention-fixed sign pattern. It is not cleared until one line
cites the sign pattern, the sector inequalities placing the negative
helicity on a catalyst/waste bounded by the child, and the positive
side-length floors on the financed family. Filed is not cleared.

## Codex zero-set sharpening, same date

Codex line check filed at
`mpp-forward-gold-codex-t7-line-check-zero-set-sharpening-20260705.md`.
The helicity-weighted factor is a signed sum of the three triangle side
lengths. On a nondegenerate triangle, any zero of such a signed sum
forces one side to equal the sum of the other two, hence lies on the
collinear boundary. The extra weighted-sum zero set is therefore not an
independent interior hazard. The campaign-specific lower bound still
needs the sector citations: negative side on catalyst/waste, bounded by
the child, with the remaining positive side floored on the financed
family.

## 1. The zero-set exclusion: the minus lives on the catalyst

The weighted sum vanishes only when the negative helicity rides the
largest wavenumber - the resonant cancellation. The campaign's
sectors place the negative on the CATALYST, bounded by the child:
predator sum = child + partner - waste >= partner; relay sum =
parent + child - catalyst >= parent. Each floored by a side length,
positive on the family by its exact defining inequalities. The zero
set is excluded by the same sign structure that defines the sectors
- the helper never outweighs the payer.

## 1b. The demanded citation line (added per the zero-set re-gate)

The sign pattern is T7's convention-fixed assignment (t7-triple-
product-closed-form note, section 1: helicities as declared in the
convention-fixation section below). The sector inequalities placing
the negative helicity on a member bounded by the child: PREDATOR
sector - waste adjacent to the child with waste wavenumber at most
the child's (rectification note section 2, sector definition
s_mu = +1, mu < q with pair (k(-), q(+)), k <= q by adjacency;
population and shape notes carry the same inequality); RELAY sector -
catalyst-flipped branch with catalyst ratio a = k/q <= 1 and floored
by the appetite root a* (octave-toll corrected note, branch
classification; shape-floor note, magnitude appetite). The positive
side-length floors on the financed family: partner floored by the
mid-band margins in exact multiples of a* (shape-floor note section
3); parent floored by the financing structure (the parent IS the
package's source; its wavenumber is the band bottom, positive by the
lattice definition - dyadic-frame note). One line, three citations,
each to a filed object.

## 2. The conventions, declared once

Helical vectors in the common-normal gauge; coefficient = conjugated
triple product from the convolution form; universal factor = the
symmetrization quarter. Invariance: every campaign object is a
modulus or a ratio; convention changes act by unit phases and one
universal constant; both cancel. The declaration pins the
bookkeeping; no conclusion depends on the choice.

## 3. Register

Both re-gate items displayed; press points: the adjacency citations;
the one-paragraph invariance proof. The symbolic window comparisons
now hold the complete exact toolkit. Outstanding: Codex's queue; the
comparisons; chapter 10. Filed is not cleared. No closure claimed.
