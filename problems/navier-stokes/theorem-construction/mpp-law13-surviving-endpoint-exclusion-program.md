# MPP Pack / Blown And Field / Jump Shell Program

## Status

Theorem-facing shell-program note.

Role: isolate the two strongest open shell programs already on disk without
pretending they are the whole remaining frontier.

## Purpose

After the installed `(Part, Dead)` entry, the current route already has two
strong shell programs on disk:

1. the pack-native blown face `packing-detached` testing `Pack`;
2. `Jump` testing `Field`.

The mixed blown face `tower-blown` remains open too, but not as a clean
single-law shell program.

What this note does **not** claim is:

```math
\text{these two programs exhaust the frontier.}
\tag{L13.0}
```

The honest claim is only:

```math
\boxed{
\text{these are the two strongest open shell programs presently organized on disk.}
}
\tag{L13.1}
```

## Installed Inputs

1. [mpp-law-endpoint-proof-matrix-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-law-endpoint-proof-matrix-note.md)
2. [mpp-shared-packing-exact-predicate-draft.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-shared-packing-exact-predicate-draft.md)
3. [mpp-pack-side-forward-preservation-reduction-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-pack-side-forward-preservation-reduction-note.md)
4. [mpp-field-coherence-remainder-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-field-coherence-remainder-note.md)

## Shell Program A: `(Pack, packing-detached)`

The exact pack-side predicate is:

```math
\mathrm{Pack}_Q(u,\Phi).
\tag{L13.2}
```

Inside the blown column, the exact pack-native shell meaning already on disk is:

```math
\boxed{
\text{`packing-detached` is the pack-native blown face.}
}
\tag{L13.3}
```

And the current theorem-facing reduction is:

```math
\boxed{
\text{the open `(Pack, packing-detached)` entry reduces to forward preservation of the packing witness.}
}
\tag{L13.4}
```

This remains the strongest pack-side shell program on disk.

The separate mixed blown face `tower-blown` is not what this shell program
reduces. It remains unresolved as a tower-amplitude branch rather than a clean
pack-side entry.

The active reduction surface is:

1. [mpp-pack-side-forward-preservation-reduction-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-pack-side-forward-preservation-reduction-note.md)

## Shell Program B: `(Field, Jump)`

The exact one-field predicate is:

```math
\mathrm{Field}_{N,r,Q}(u,p).
\tag{L13.5}
```

The exact shell meaning already on disk is:

```math
\boxed{
\text{`Jump` is the one-field manifestation shell.}
}
\tag{L13.6}
```

And the current theorem-facing reduction is:

```math
\boxed{
\text{the open `(Field, Jump)` entry reduces to coherence-fracture control under retained `Pack + Part`.}
}
\tag{L13.7}
```

So `Jump` is being used here as the native shell for `Field`, not as a direct
shell for `Pack` or `Part`.

The active reduction surface is:

1. [mpp-field-coherence-remainder-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-field-coherence-remainder-note.md)

## Boundary

This note does **not** prove:

1. that `Part` is gone from the frontier;
2. that `Blown` cannot test `Part` or `Field`;
3. that `Jump` cannot test `Pack` or `Part`;
4. that `Pack / Blown` and `Field / Jump` are the only open matrix entries.

So the correct theorem-facing read is:

```math
\boxed{
\text{`(Pack, packing-detached)` and `(Field, Jump)` are strong open shell programs, not a surviving-pair theorem.}
}
\tag{L13.8}
```
