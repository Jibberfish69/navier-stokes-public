# MPP LCI.A Pre-CSP Common-Consumer Bypass Note

## Status

Candidate theorem-facing note.

Role: isolate the exact stronger bypass still available beneath `EOC.A` after
Corollary `EOC.Ca` identifies the route-native consumer as
`CSP.A_{\delta+\delta_{seg}}`.

This note does **not** widen the lower-carrier / one-field route. It does
**not** claim that the route-native `CSP.A` dependency has been removed. Its
narrower job is to freeze the exact stronger theorem one would need if the
oscillation-side import `(LCI.3c3z20)` is to be supplied *before* `LCI.A`
without reusing `CSP.A`.

## Purpose

```math
\boxed{
\text{replace the route-native consumer }CSP.A_{\delta+\delta_{seg}}
\text{ beneath }EOC.A
\text{ by a direct pre-`CSP.A` common-consumer theorem.}
}
\tag{PCC.0}
```

Equivalently: if one wants a non-circular oscillation-side import for `RCF.1`,
then after `EOC.C` the missing stronger theorem is no longer “derive the whole
enlarged-ball oscillation theorem from scratch.” It is:

```math
\boxed{
\text{prove the common thickened collar consumer }
\mathfrak C_{N+1,\rho,\psi}^{\delta+\delta_{seg}}
\text{ directly from pre-`CSP.A` packet/reference data.}
}
\tag{PCC.0a}
```

## Installed Reduction Already On Disk

Retain the one-field transport-geometry and scale hypotheses `(OFP.10bi)` and
`(OFP.10cb)`, the bounded pack-side gauge `M_\sharp=\|\Gamma_\sharp\|_{L^\infty(I)}`,
the widened receiver-side lower-order collar return

```math
\sup_{t\in I}
\mathcal G_{N+4,N+4,\rho,\psi}^{low,\delta+5\Lambda_{\delta,\rho}^{halo}}(t)
<
\infty,
\tag{PCC.1}
```

and the initial oscillation seed `(EOC.30)`.

The enlarged-ball note already proves:

1. Corollary `EOC.C`: `(PCC.1)` together with
   `\mathfrak C_{N+1,\rho,\psi}^{\delta+\delta_{seg}}\in L^1(I)` implies
   `EOC.A`;
2. Corollary `EOC.Ca`: if one strengthens that common-consumer input to the
   packet theorem
   `\sup_{t\in I}\mathcal K_{N+1,m,\rho,\psi}^{\delta+\delta_{seg}}(t)<\infty`,
   then `EOC.A` follows route-natively by `(PC.14)`.

So the stronger bypass under `OFP.C1a` is already reduced to one exact
consumer theorem.

### Reduction `PCC.A` (Direct pre-CSP common-consumer control plus the widened return implies `EOC.A`)

Assume `(OFP.10bi)`, `(OFP.10cb)`, `(PCC.1)`, `(EOC.30)`, and

```math
\mathfrak C_{N+1,\rho,\psi}^{\delta+\delta_{seg}}\in L^1(I),
\tag{PCC.2}
```

where `(PCC.2)` is supplied directly from pre-`CSP.A` packet/reference data.

Then `EOC.A` holds.

### Proof

This is exactly Corollary `EOC.C`. The point is not a new proof, but the exact
re-identification of the stronger bypass burden: once the widened receiver-side
return is kept separate, no additional independent halo/increment theorem
remains below `EOC.A`; only the direct common-consumer theorem `(PCC.2)` does.
∎

### Corollary `PCC.B` (Direct pre-CSP thickened packet theorem)

Under `(OFP.10bi)`, `(OFP.10cb)`, `(PCC.1)`, and `(EOC.30)`, assume

```math
\sup_{t\in I}
\mathcal K_{N+1,m,\rho,\psi}^{\delta+\delta_{seg}}(t)
<
\infty,
\tag{PCC.3}
```

and that `(PCC.3)` is supplied directly from pre-`CSP.A` packet/reference
data.

Then `EOC.A` holds.

### Proof

By `(PC.14)`, `(PCC.3)` implies `(PCC.2)`. Now apply `PCC.A`. Equivalently,
this is Corollary `EOC.Ca` with the common-consumer theorem treated as a
stronger bypass input rather than as the route-native `CSP.A` surface.
∎

### Reduction `PCC.C` (The stronger packet bypass is exactly the common-object collar packet theorem at thickness `\delta+\delta_{seg}`)

The strengthened packet input `(PCC.3)` is not a new primitive theorem surface.
It is exactly the collar-Sobolev interval-propagation target `FCI.B` from
[mpp-fixed-family-and-collar-interval-propagation.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-fixed-family-and-collar-interval-propagation.md),
run at the thicker collar radius
`\delta_\ast:=\delta+\delta_{seg}` on the packet-collar common-object branch.

Indeed, the common-object note
[mpp-packet-collar-next-rung-control.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-packet-collar-next-rung-control.md)
defines the collar packet `\mathcal K_{N+1,m,\rho,\psi}^{\delta}` generically in
the thickness parameter and isolates the embedding
`(PC.14)`, while the interval-propagation queue note isolates the exact target

```math
\sup_{t\in I}
\mathcal K_{N+1,m,\rho,\psi}^{\delta}(t)<\infty
\tag{FCI.6}
```

for an arbitrary fixed collar thickness. So after substituting
`\delta\mapsto\delta+\delta_{seg}`, `(PCC.3)` is nothing more than that
existing common-object target at the widened thickness.

Therefore the stronger non-circular bypass beneath `EOC.A` does **not**
introduce a new packet theorem family. It reuses the old common-object collar
packet theorem at the one enlarged thickness forced by `(OFP.10cb)`.

Since the collar theorem surface itself already states that `CSP.A` is exactly
the supplier theorem recorded abstractly as `FCI.B`, this means the stronger
packet bypass is also exactly the widened-thickness instance
`CSP.A_{\delta+\delta_{seg}}`. So the honest remaining math debt here is not a
new bypass theorem shape. It is the same moving-collar one-rung-higher energy
law, just read at the thicker radius demanded by `(OFP.10cb)`; the companion
note
[mpp-lci-a-widened-thickness-collar-wall-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-lci-a-widened-thickness-collar-wall-note.md)
records that exact collar-side reduction. More sharply, inside the widened
receiver package the only thickness-sensitive surviving slot is the thicker
lower same-fluid carrier ledger `\Gamma_{N,m,\rho,\psi}^{low,\delta+\delta_{seg}}`,
and the companion widened-thickness lower-carrier note reduces that slot to
the old lower-carrier theorem `LCI.A` at the thicker radius.

### Theorem `PCC.SLR.A` (Still-live receiver admission supplies the direct common consumer)

The route-native reduction `(PCC.C)` returns to the old `LCI.A` / `CSP.A`
family. The non-circular bypass is supplied instead by the still-live receiver
admission theorem
[mpp-lci-a-pre-csp-still-live-receiver-admission-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-lci-a-pre-csp-still-live-receiver-admission-note.md).

Assume the common receiver set

```math
\mathcal R_{\rho,\psi}^{pre}(t)
=
\mathcal H_{\rho}^{ctr}(t)
\cup
\operatorname{supp}\chi_{\rho,\psi,t}^{\delta+\delta_{seg}}
\cup
\operatorname{supp}\vartheta_{\rho,\psi,t}^{\delta+5\Lambda_{\delta,\rho}^{halo}}
\tag{PCC.5}
```

is contained in one compact still-live receiver collar `Q^{rec,+}` for all
`t\in I`, with the finite support-volume and smoothness bounds `(SLR.4)` and
`(SLR.5)`. Then

```math
\sup_{t\in I}
\mathcal K_{N+1,m,\rho,\psi}^{\delta+\delta_{seg}}(t)
<
\infty,
\tag{PCC.6}
```

and hence

```math
\mathfrak C_{N+1,\rho,\psi}^{\delta+\delta_{seg}}\in L^1(I).
\tag{PCC.7}
```

### Proof

This is exactly Lemma `SLR.B`: still-live smoothness on the common receiver
collar bounds every derivative in `(PC.9)`, and the finite volume of the
thickened cutoff support bounds the integral. The embedding `(PC.14)` and
finite length of `I` then give `(PCC.7)`. ∎

### Corollary `PCC.SLR.B` (The common-consumer bypass no longer has a separate open wall on retained still-live windows)

On a retained window satisfying the still-live receiver-admission hypotheses,
the stronger bypass input `(PCC.3)` is no longer open. The only remaining
question is whether the same retained window also supplies the segment geometry
and widened receiver return; the companion note supplies those as `SLR.A1` and
`SLR.C`.

## Honest Boundary

This note does **not** prove `(PCC.1)` route-natively through the widened
`LCI.B` family, and it does **not** claim that `CSP.A` has been removed from
the installed `LCI` route. The new theorem `PCC.SLR.A` proves `(PCC.2)` / `(PCC.3)`
only under the separate still-live receiver-admission hypothesis `(PCC.5)`.

What it does prove is narrower and exact:

1. the anchored-center ledger `(EOC.28)` is not part of the oscillation-side
   wall beneath `EOC.C` / `EOC.Ca`;
2. the route-native `EOC` wall is
   `\text{widened return }(EOC.27) + CSP.A_{\delta+\delta_{seg}}`;
3. the stronger non-circular bypass wall beneath `OFP.C1a` is no longer the
   whole enlarged-ball theorem, but the direct pre-`CSP.A` common-consumer
   theorem `(PCC.2)`, or more strongly `(PCC.3)`;
4. the stronger packet theorem `(PCC.3)` is itself just the widened-thickness
   instance of the already-existing common-object target `FCI.B`, hence
   exactly `CSP.A_{\delta+\delta_{seg}}`; and the companion widened-thickness
   collar wall note reduces that further to the old collar wall `CSP.8` at the
   thicker radius;
5. on the direct still-live receiver-admission route, `(PCC.3)` is supplied
   without passing through that route-native fixed point.

So, absent the still-live receiver-admission hypothesis, the honest remaining
stronger bypass object is:

```math
\boxed{
\text{direct pre-`CSP.A` control of }
\mathfrak C_{N+1,\rho,\psi}^{\delta+\delta_{seg}}
\text{ or }
\mathcal K_{N+1,m,\rho,\psi}^{\delta+\delta_{seg}}.
}
\tag{PCC.4}
```

That was the theorem-facing content still missing if `(LCI.3c3z20)` was to be
imported upstream of `LCI.A` without recycling the collar theorem itself. The
new still-live receiver-admission theorem supplies this object directly on
retained still-live windows. In packet-theorem form, the route-native version
remains exactly the widened-thickness common-object target `FCI.B`, i.e. the
widened-thickness instance `CSP.A_{\delta+\delta_{seg}}`.
