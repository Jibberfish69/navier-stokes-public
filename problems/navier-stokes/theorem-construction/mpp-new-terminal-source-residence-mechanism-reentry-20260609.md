# NewTerminalSourceResidenceMechanism.A reentry

Date: 2026-06-09

## Target

The active loop target is not a new PDF status phrase. It is the theorem gap
left after `BASACReverseHolderProduction.A` failed from the present `B_ASAC`
clauses:

`NewTerminalSourceResidenceMechanism.A`. Produce a concrete mechanism stronger
than finite terminal `L^1_t` source mass that controls the native positive
source marginal by super-`L^1`, Orlicz, Morrey, trace absolute continuity,
independent source Carleson reserve, pressure/Leray time-face spread, or rigid
Zeno class production.

## Method pass

- Proof program: Navier-Stokes CM contrapositive class exit.
- Target object role: supplier theorem for the first-exit Pack-survival wall.
- Logical skeleton: same-ledger charge/first-appearance alternative feeding the
  CM witness tree, not a positive smoothness bootstrap.
- Mechanism tested: analytic trace regularity, terminal anti-atom capacity,
  signed-saturation face reduction, and reserve/charge first-birth accounting.
- Current failure mode: finite terminal source mass still allows a positive
  measure to concentrate at the terminal time face.
- Pivot: same-ledger first-reserve appearance and charge injection.

## Checked inputs

- `mpp-basac-reverse-holder-production-reentry-20260609.md` shows that the
  produced `B_ASAC` equality-class clauses do not give super-`L^1` temporal
  residence, trace absolute continuity, or an anti-atom modulus.
- `reservecreationcharge-a-theorem-creation-20260525.md` gives the only checked
  candidate that actually changes the source-residence economy: new terminal
  reserve is inherited from `Past(W)` or paid by `Charge_N(W)`.
- That candidate is explicitly open. Its recursive chain is
  `ReserveCreationCharge.A -> TerminalReserveFirstAppearanceCharge.A ->
  ChargeLedgerInjection.A -> PositiveRemainderDepletion.A`.
- The height-flux/BV chain
  `HeatDwellSampling.A -> LowStrainBVCharge.A ->
  NearBandBVToBurstOrSource.A+FrameBVCharge.A ->
  NearBandMaterialSourceCharge.A+FrameBVCharge.A -> TerminalSignedSaturation.A`
  is useful support, but the checked records leave every displayed link as a
  new subprimitive until the signed-saturation end.
- `TerminalSignedSaturationCMFaceReduction.A` is face-classification support:
  it says failures of signed saturation route into Part/Field witness
  exit. It is not a forward no-free-sink theorem and does not by itself produce
  the source residence needed by `BASACTimeFaceAntiAtom.A`.

## Judgment

`NewTerminalSourceResidenceMechanism.A` is not installed.

The analytic regularity candidates remain blocked at the terminal layer model:
unit `L^1_t` source mass with divergent `L^p_t` norms is still compatible with
the current `B_ASAC` carrier, paid exits, zero ASAC defect, and terminal Zeno
support.

The signed-saturation chain must not be promoted to closure. It can help once a
bad source packet has been admitted to the CM witness tree, because then its
failure can land in Part/Field. It does not prove that the positive source
marginal has enough temporal residence to remove the terminal atom.

The next honest theorem target is therefore
`TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A`: prove that
the minimal retained positive source-reserve birth is either inherited from the
same parent ledger or injected into the charge ledger. This is the narrow
forward supplier theorem that can feed a real source-residence mechanism back
into `BASACReverseHolderProduction.A`, `BASACTimeFaceAntiAtom.A`, terminal Zeno
rigidity, and the first-exit Pack wall.

## New live criticism

`NS-LIVE-20260609-054`: the current proof still lacks the same-ledger
first-appearance charge theorem for the terminal positive source-reserve birth.
Without `TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A`, the
loop has only terminal source face classification and analytic sufficient
conditions, not a proof that the native positive source marginal has the
residence needed to close the public Clay-facing Pack-survival wall.

## Next parent action

Attack `TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A` as the
same-ledger first-birth charge theorem. The proof attempt must show that an
uncharged genuinely new terminal source-reserve birth creates either inherited
parent mass, charge-ledger injection, or a concrete Part/Field witness
failure; it cannot spend `TerminalSignedSaturationCMFaceReduction.A` as a
forward source-residence theorem.
