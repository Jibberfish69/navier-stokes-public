# MPP NoGenuineCMExit.A Pack-Wall Parallel Merge Audit

## Authority Cap - Positive No-Exit Attempt Only

This file is retained as a positive no-exit / packet-survival proof attempt. Current CM proof-status authority is `mpp-cm-proof-completion-surface-field-audit-20260525.md` plus `mpp-completed-proof-surface-challenge-20260525.md`. The open Part/Field no-exit language here is not an active CM blocker. Reopening requires a proved legal in-class nonsmooth third branch or a proved failure of Clay terminal CM entry/exhaustion/embedding.

Date: 2026-05-23

## Status

Lane-local coordination and proof audit for the parallel reserve/CM route.

Verdict:

```math
GenuineCMExit.Equiv.A
\text{ is installed as the correct CM/smoothness equivalence,}
```

but

```math
NoGenuineCMExit.A
\text{ is not discharged by the current reserve notes.}
```

The attempted parallel tail-square closure does not prove the Field-window support source
reserve required by `ScaleCriticalTreeCarleson.A`.

## Governing target

The Clay-facing CM theorem is:

```math
OriginalSmoothData
\Longrightarrow
\neg GenuineCMExit(T_*)
```

for every alleged finite terminal time `T_*`.

With `GenuineCMExit.Equiv.A` installed, this is exactly the no-finite-terminal
non-continuation theorem.  It is therefore the final mathematical burden of the
CM contrapositive route, not a definitional cleanup.

The live face reduction is:

```text
NoGenuineCMExit.A
=> continuation-complete Part/Field packet survives
=> Field-certification source reserve wall after source-residue sorting
=> ScaleCriticalTreeCarleson.A or an equivalent source-reserve birth charge.
```

Raw terminal native source residue is already sorted as a Part/Field witness by
`TerminalSourceResidueCMExit.A`; it is not a Field anti-atom deletion burden.

## Parallel route audited

The nearby parallel reserve notes use the tail-square quantity

```math
2^{-2N}E_N(t)^2,
\qquad
E_N(t)=\sum_{j\ge N}E_j(t),
```

with dyadic definitions

```math
E_j(t)=2^{2j}\|\Delta_j u(t)\|_2^2,
\qquad
D_j(t)=2^{4j}\|\Delta_j u(t)\|_2^2.
```

This is not the source-wall reserve in `ScaleCriticalTreeCarleson.A`, which is

```math
R_N(I)
=
\int_I
\sum_{k>N}2^k
\left(\sum_{\ell>k+4}D_\ell(t)\right)^2dt.
```

The repo already contains both sides of the attempted parallel path:

```text
tail-square-reserve-installed-by-energy-absorption.md
full-tail-square-reserve-discharge.md
```

claim a tail-square absorption, while

```text
tail-square-reserve-promotion-blocked-by-lpas.md
tail-square-reserve-promotion-blocked-by-shell-amplitude.md
tail-square-reserve-promotion-blocked-by-square-source.md
```

record that the far-separated bridge still requires LPAS / active-square /
square-source strength.  Those blocker notes are the correct promotion gate for
the CM Pack wall.

## Mismatch proof

Even a valid estimate for `2^{-2N}E_N^2` cannot be promoted to
`ScaleCriticalTreeCarleson.A` without a separate bridge.

Use the displayed scalar first-pulse test from the reserve obstruction notes.
Let `I_m` have length `h_m`, put one active shell at `L=m+5`, and set

```math
D_L(t)=A_mh_m^{-1}{\bf 1}_{I_m}(t),
\qquad
D_\ell(t)=0 \quad(\ell\ne L).
```

Then the source reserve contribution at `k=m` is

```math
\int_{I_m}
2^m
\left(\sum_{\ell>m+4}D_\ell(t)\right)^2dt
=
2^mA_m^2h_m^{-1}.
```

Choose

```math
A_m=2^{-2m},
\qquad
h_m=2^{-3m}.
```

Then the first-moment high-frequency dissipation tends to zero:

```math
\int_{I_m}D_L(t)dt=A_m\to0,
```

while the source reserve remains bounded below by the fixed value `1`:

```math
2^mA_m^2h_m^{-1}=1.
```

For the tail-square quantity, however,

```math
E_L(t)=2^{-2L}D_L(t),
```

so

```math
\int_{I_m}2^{-2m}E_L(t)^2dt
=
2^{-2m}2^{-4L}A_m^2h_m^{-1}
\le
C_{TSQ}
2^{-7m}.
```

Thus the parallel tail-square reserve can be small while the
`ScaleCriticalTreeCarleson.A` source reserve is bounded below by the fixed
value `1`.  The quantities live
at different scale weights.  A tail-square discharge is not a Pack-wall
discharge unless it is accompanied by a theorem converting terminal
source-reserve birth into that tail-square reserve.

## Exact missing bridge

The noncircular bridge needed to close the Pack wall is:

```math
SourceReserveBirthCharge.A:
```

on every first terminal same-fluid reserve-birth window `W`,

```math
\left[
R_N(W)
-(1-\delta)R_N(Past(W))
-Loss_{legal}(W)
\right]_+
\le
Charge_N^{native}(W)+o_N(1),
```

where the native charge must land in one of the CM witnesses:

```text
Pack-gain / packing detachment,
Part participation failure,
licensed Field source charge,
or a terminal Zeno residue already sorted as Part/Field exit.
```

This is the same hard atom previously named through

```text
ReserveCreationCharge.A,
HeightFluxControl.A,
BASACTerminalSourceAntiConcentration.A,
SourceWeightedTerminalAngularDepletion.A,
or ActiveHeightFluxTrichotomy.A.
```

The first-pulse/minimal-window proof skeleton is sound after
`SourceReserveBirthCharge.A`: choose the first window where `R_N(W)>=eta`,
iterate the contraction backward, and use vanishing high-tail native charge to
contradict the positive reserve.  The current repo does not yet supply the
native charge theorem itself.

## Closure of this merge

The parallel thread's usable contribution is a warning and a split:

```text
tail-square reserve work may support collar/product estimates,
but it cannot be promoted into the CM Pack source-reserve wall.
```

This matches the parallel full-agency proof audit:

```text
mpp-terminal-cm-no-exit-full-agency-proof-audit-20260523.md
```

That audit keeps `TerminalCMNoExit.FaceReduction.A` as a valid face reduction
and keeps Part and Field no-exit open from original smooth data.  Its
fresh `ScaleCriticalTreeCarleson.A` creation readback also keeps the Field-window support
reserve route open at:

```text
ScaleCriticalTreeCarleson.A.CoreSubprimitive
```

The coordinated live target is therefore not another tail-square absorption.
It is the source-reserve birth theorem above, or a genuinely equivalent
structural theorem that prices first creation of the scale-critical donor
reserve inside Part/Field/Zeno class-exit grammar.

Until that bridge is proved, the correct status is:

```math
TerminalCMNoExit.A / NoGenuineCMExit.A
\text{ remains open exactly at the Field-window support source-reserve birth charge.}
```
