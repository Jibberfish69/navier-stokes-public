# MPP PDF CMI Bridge Arbitration

Date: 2026-06-09

Status: active proof-core arbitration from the live PDF completion loop.
CMI finality remains open.

## Trigger

The active completion loop found a conflict between two repo readings.

One direct-live surface says the generic Clay bridge blocker was reconciled by
finite-obstruction inventory:

```text
finite terminal obstruction row
=> CM-test entry
=> first Part/Field face
=> Exit(Q):=not Member(Q).
```

The current rendered papers use that reading to claim the Clay conclusion.

The independent CMI reread found the missing payment: a finite Clay breakdown
can be exactly the loss of the terminal service being classified.  Therefore a
classification into class exit is not automatically a contradiction of finite
breakdown unless the paper proves why every valid finite Clay counterexample
must either pass the continuation readout or fail to be a valid counterexample,
not merely fail to be a class member at the terminal edge.

## Checked Surfaces

### 1. The installed inventory surface

`mpp-clay-bridge-reconciled-by-finite-obstruction-inventory-20260608.md`
states that the old generic demand

```text
classified Exit(Q;O_work) cannot be the actual finite Clay obstruction
```

is not the active theorem target.  It replaces that demand with a finite
obstruction inventory criterion:

```text
every promoted finite terminal obstruction row
=> CM-test entry
=> first failed Part/Field face
=> Exit(Q):=not Member(Q).
```

This is useful as a route-control criterion.  It prevents old source-wall or
supplier rows from being promoted unless they name a same-solution terminal row
and a first Part/Field.

It does not by itself answer the CMI referee question:

```text
why is face-derived class exit impossible as the actual finite breakdown?
```

### 2. The terminal-witness invalidation note

`mpp-clay-terminal-witness-invalidation-20260523.md` proves entry and
face-exhaustion for a CM-test-admissible Clay terminal object.  It correctly
separates CM-test admissibility from `Member(Q)`.

The proof still has to make the final contradiction explicit.  The step

```text
terminal witness loses Part or Field
=> no Clay counterexample
```

is not a formal consequence of the Clay negation unless the terminal witness is
proved to owe Part and Field as validity conditions for being the
original branch's finite terminal obstruction.  Otherwise the conclusion says
only that a breakdown object has broken the class.

### 3. The current manuscript language

The compact source states that the fail branch is not a second in-class
nonsmooth branch and that the whole-space final theorem has the advertised
scope.  The human source similarly says the fail side records that the alleged
terminal object is not an in-class nonsmooth terminal member.

Those sentences are not yet the same as the Clay conclusion.  A finite-time
singularity is not required to be an in-class nonsmooth terminal member.  The
paper must prove that the official finite Clay negation cannot be satisfied by
that class-exit event.

## Parent Judgment

The loop must not treat the June 8 inventory reconciliation as CMI finality.
It is an installed route-control and row-consumption criterion.  It is not yet a
reader-facing Clay counterexample exclusion theorem unless the paper supplies a
noncircular final bridge.

The exact live theorem object is:

```text
ClayCMCounterexampleExclusion.A
```

with this burden:

```text
Finite Clay negation
=> same-solution terminal record Q.

If Q passes Part and Field
=> H^s readout
=> same-solution continuation.

If Q fails a first Part/Field face
=> prove this is not merely the alleged finite breakdown itself,
   but invalidates Q as a Clay counterexample object.
```

The last line is the unpaid CMI bridge.

## Route Consequence

There are two honest routes from here.

1. Prove the final bridge directly:

```text
face-derived Exit(Q)
=> no valid finite Clay counterexample
```

without defining a counterexample to require the conclusion.

2. Prove a no-genuine-exit theorem:

```text
OriginalSmoothData => not GenuineCMExit(T_*).
```

On the Pack side that reopens the known wall:

```text
OriginalSmoothData => terminal Pack survival,
```

with the first concrete source-reserve atom:

```text
RetainedPositiveScaleNativeReserveBirthCharge.A
/ TerminalReserveFirstAppearanceCharge.A.
```

Source-reserve Pack survival is therefore not automatically the primary target.
It becomes primary only if the finite-obstruction bridge cannot be proved
directly.  Under the June 8 inventory criterion it remains demoted support
unless a manuscript-used terminal row lacks same-solution CM entry or a derived
first Part/Field.

## PDF Consequence

Both PDFs remain non-final.

Before either PDF can be called CMI-ready, the main spine must state and prove
the exact exclusion bridge, or it must explicitly reopen the no-genuine-exit
work and stop short of the Clay conclusion.

The next live loop action is to arbitrate `ClayCMCounterexampleExclusion.A`
against the current paper text and theorem notes, then either repair the paper
around a proved bridge or continue object-level proof work on the no-genuine-
exit route.
