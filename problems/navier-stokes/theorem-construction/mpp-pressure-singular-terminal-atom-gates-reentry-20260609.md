# MPP Pressure Singular Terminal Atom Gates Reentry

## Status

Active-completion reentry. This note does not close the public Clay bridge.

It checks whether the pressure/Leray family supplies a real theorem for the
current leaf:

```math
TerminalPositiveSourceAtomEscape.A
```

equivalently:

```math
BASACTerminalSourceAntiConcentration.A.
```

## Object under test

The surviving source-wall object is still the terminal native positive source
atom in `B_ASAC`:

```math
d\mu_m^{src}(y,s)
=
\rho(y)m1_{(-1/m,0]}(s)\,dy\,ds
\rightharpoonup
\rho(y)\,dy\otimes\delta_{s=0}.
```

It carries the one-sided native positive source measure

```math
\left[\left\langle S_{<j}^{loc}w_j,w_j\right\rangle\right]_+\,dx\,dt.
```

The pressure question is whether spatial pressure recovery, pressure-Hessian
sustain, or localized Leray recombination forces this terminal atom to spread
in time, enter the same source ledger, become legal loss, or land as a
Part/Field failure.

## Local energy trace is not enough

The local-energy trace route asks for a no-positive-jump theorem at the
terminal time face. Current inputs give bounded variation of the localized
source-balance primitive.

Bounded variation permits a terminal jump. The model

```math
g_m(s)=m1_{(-1/m,0]}(s)
```

has unit mass and produces a positive terminal jump in the primitive. Weak
linear traces and the local energy inequality do not eliminate the quadratic
one-sided selected source jump.

So the local-energy route returns to one of the already isolated production
theorems: super-`L^1_s` source residence, positive-source trace absolute
continuity, or source Carleson reserve.

## Pure pressure branch

The pure pressure-source singular residue is

```math
\mu_*^{press\perp src}>0,
\qquad
\mu_*^{press\perp src}\perp(\nu_*^{src}+\nu_*^{legal,src}).
```

It represents pressure-Hessian sustain while avoiding the native source-current
carrier.

The trace-free symmetric tangent

```math
S=\operatorname{diag}(2,-1,-1),\qquad \Omega=0
```

has positive pressure-Hessian sustain and zero native vorticity source-current
carrier. That test blocks any route that tries to force native source-current
support from pressure algebra alone.

## Pressure legal gate

`TerminalPressurePureStrainLegal.A` remains open.

Existing pressure legal ledgers cover collar and boundary work, harmonic tails,
cutoff/projector commutators, off-family pressure tails, and packets outside
the retained terminal family. The pure pressure-sustain residue is selected
after those exits have been removed. It is an interior retained principal-local
pressure-Hessian lobe.

A legal closure would need a new terminal legal class and a terminal-weight
summability theorem:

```math
\mu_*^{press\perp src}
\le
Legal^{pure\text{-}strain}_{press,*}.
```

That is a new pressure depletion theorem, not an installed legal escape.

## Pressure Liouville gate

`PurePressureSourceSingularLiouville.A` remains open.

Calderon-Zygmund pressure structure ties pressure sustain to a quadratic-gradient
carrier. Current source-wall inputs supply local compactness and residue
selection, not the retained-class rigidity needed for a pressure Liouville
theorem.

A closure still needs a production theorem such as

```math
PressureRigidClassProduction.A
```

plus a Liouville theorem for that rigid class, or an equivalent pressure
trace/angular saturation theorem.

## Independent Leray gate

The localized Leray identity

```math
\chi\nabla P_{Leray}(u\cdot\nabla u)
=
P_{Leray}(\chi\nabla(u\cdot\nabla u))
+[\chi,P_{Leray}]\nabla(u\cdot\nabla u)
```

recombines pressure and convection before terminal selection. It does not by
itself produce a one-sided same-fluid positive source edge at the terminal
weights.

The independent Leray route still needs all three gates:

```math
TerminalLerayOrientation_{ind}.A,
\qquad
TerminalLerayCommutatorLegal_{ind}.A,
\qquad
TerminalLeraySaturation_{ind}.A.
```

Without those gates, Leray recombination can lose orientation through
cancellation, leave an unpriced terminal commutator, or lose the positive
partner during one-sided terminal selection.

## Field-side diagnostic only

The pressure branch does support the diagnostic statement:

```math
PressureResidueFieldFaceDiagnostic.A.
```

The diagnostic says an uncharged retained pure pressure-sustain residue belongs
on the Field/readout side of the CM witness ledger. It does not supply
`PressureSourceAC.A`, `PurePressureSourceSingularLiouville.A`,
`TerminalPressurePureStrainLegal.A`, or the independent Leray terminal gates.

So pressure/Leray cannot currently be used as a supplier closure for
`TerminalPositiveSourceAtomEscape.A`.

## Verdict

Pressure/Leray has been checked as a route family and remains a failed closure
route at current authority.

The current theorem leaf stays:

```math
TerminalPositiveSourceAtomEscape.A
\quad/\quad
BASACTerminalSourceAntiConcentration.A.
```

Further progress requires new mathematics in one of these exact forms:

```text
terminal time-face anti-atom modulus,
positive-source trace absolute continuity,
source-parent concentration reserve,
transported no-incoming plus terminal time-face control,
pressure-specific legal or Liouville gate,
independent Leray terminal orientation/commutator/saturation,
production into a rigid anti-atom subclass.
```

Until one of those production theorems is proved, public Clay finality and both
PDF finality surfaces remain open.
