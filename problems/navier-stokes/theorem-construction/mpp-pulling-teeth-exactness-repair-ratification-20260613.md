---
ns_viewer:
  theorem_id: pulling-teeth-exactness-repair-ratification-20260613
  status: exactness-repair-accounted-specific-cone-funnel-active-square-case-conditional-silver-boundary
  proof_role: exactness_accounting
  logical_landing_node: cone_funnel_active_square_exactness_repair
  source_authority:
    - problems/navier-stokes/submission-bundle/authoring/lab/transcripts/pulling-teeth/pulling-teeth-transcript.pdf
    - problems/navier-stokes/theorem-construction/mpp-parabolic-funnel-forward-control-analysis-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-cone-payment-closure-package-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-forward-positive-cone-tower-active-square-exhaustion-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-direct-lpas-raw-active-square-false-shear-counterexample-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-scale-uniform-annular-supplier-goal-resolution-20260613.md
    - problems/navier-stokes/theorem-construction/mpp-annular-limit-cm-face-landing-20260611.md
    - problems/navier-stokes/theorem-construction/mpp-annular-selection-readout-torus-atlas-payment-20260611.md
  edge_effect: "Records the exact repair from the transcript audit: non-exact scale language is no longer allowed to carry the cone/funnel/active-square argument. The live forward target is residual/source-balanced annular control; the silver CM-exit branch is licensed only for a surviving admitted residual terminal packet with selection, same-solution positive-radius carrier, and readout paid."
---

# Pulling Teeth Exactness Repair Accounting

Date: 2026-06-13

## 1. Transcript Failure Recovered

The transcript exposed one precise failure pattern.  The useful bridge was not
cylinder-scale commentary.  It was the moving-radius substitution

```math
\rho(t)=\lambda\sqrt{T-t}
```

into the fixed-slice oscillation inequality

```math
\Delta_{\rho(t)}u(t)
\le
2\rho(t)\sup_{B_{\rho(t)}(x0)}|\nabla u(\cdot,t)|.
```

All later cone and funnel reasoning must start from the exact substituted
quantity, then state any extra profile, volume, pressure, boundary, or residual
source hypothesis separately.

## 2. Exact Replacements Installed

The repaired cone/funnel chain now uses exact objects.

```math
u(x,t)
=
(T-t)^{-1/2}
v\left({x-x0\over\sqrt{T-t}},-\log(T-t)\right)
```

defines the fixed-ball funnel profile.  Persistence is a lower-bound statement
on unweighted rescaled mass, not a physical-energy slogan.

```math
A_{\mathrm{osc}}(r,t)
:=
{r\,\Delta_r u(t)\over\nu}
```

is the oscillation number, and the cone payment benchmark is

```math
E_{\mathrm{bench}}(r;A)=\omega_3A^2\nu^2r,
\qquad
P_{\mathrm{bench}}(r;A)=\omega_3A^2{\nu^3\over r}.
```

The derivative tower uses

```math
S_q(r,t)
:=
{r^{2q-1}\over\nu^2}
\int_{B_r(x0)}|\nabla^q u(x,t)|^2\,dx.
```

A critical retained tower rung means

```math
c_q\le S_q(r,t)\le C_q.
```

The active-square pulse obstruction uses the exact heat-scale lower bound

```math
c_h2^{-2j}\le |I_j|\le C_h2^{-2j},
\qquad
D_j(t)={M_j\over |I_j|}\mathbf 1_{I_j}(t),
```

so that

```math
\int_{I_j}2^{-j}D_j(t)^2\,dt
=
2^{-j}{M_j^2\over |I_j|}
\ge
C_h^{-1}2^jM_j^2.
```

This is the exact reason first-moment drain does not pay the active square.

## 3. Gold Standard

The gold forward-positive target is not raw `DirectLPAS.A`.  The live gold
target is one of these residual scale-critical suppliers:

```text
ParabolicFunnelScaleCriticalBudget.A
FunnelProfileLyapunov.A
AnnularLimitForwardControl.A
ResidualSourceBalancedLPAS.A
ResidualDirectCTSOrFCTS.A
StrongerSourceDepletion.A
```

Each must control the residual source-balanced object after heat descendants,
through-flow, legal losses, and paid dissipation are removed.

## 4. Impassable Obstruction Recorded

The installed exact ledgers do not prove the gold supplier.

Physical energy, physical wall flux, cone volume, first-moment drain,
Volterra visibility, Schur summation, raw tower accounting, and universal
square-source control do not control the unweighted residual active-square
cost.  Raw LPAS is false by the exact heat-shear counterexample.  The surviving
forward-positive obstruction is the residual source-balanced terminal pulse.

## 5. Silver CM-Exit Test

Silver is licensed only after the residual terminal packet is admitted as the
same-fluid object under test.  A surviving admitted residual packet is not a
positive smoothness target.  For this annular case, Pack is the pre-admission
carrier/selection boundary; after admission the remaining CM test is:

```text
Part_{N,Q} first, then Field_{N,r,Q}.
```

For the annular residual object, Pack is a pre-admission no-carrier boundary.
After admission, the surviving finite nonsmooth branch exits `Member(Q)`
through Part or Field.  This supports

```math
Exit(Q):=\neg Member(Q).
```

## 6. Completion Truth For This Case

This specific cone/funnel/active-square estimate case is repaired at the
source-object level: non-exact scale language has been replaced by exact
normalized definitions, exact inequalities, exact obstruction status, and the
gold-to-obstruction-to-silver route.  The stronger gold supplier is still not
proved.  The silver branch is complete only for branches whose admission and
readout boundary has already been paid, such as the periodic `Q_atlas` branch
recorded in `mpp-annular-selection-readout-torus-atlas-payment-20260611.md` and
the separate whole-space `R3.WholeSpaceCMCompletion.A` surface.  An unadmitted
private annular diagnostic is still support-only until it is admitted as a
selected same-solution terminal object with a positive-radius carrier and
readout data visible.

So the current completion truth is exact but conditional: the hand-waved case
has been converted to exact objects and exact route boundaries, the gold route
has a hard obstruction, and the silver branch consumes only objects with
admission, same-solution positive-radius carrier, and readout paid.  This note
does not claim that the full Navier-Stokes proof is closed,
that the stronger annular supplier is proved, or that every older theorem note
in the repo has been exactness-swept.

## 7. Live Exactness Status

The transcript failure was systemic, but classifier counts and generated
inventories are only finding aids.  They do not prove that a live theorem
surface has been mathematically repaired.  A live surface is repaired only when
the surface itself names the old loose claim, replaces it by the exact object
or inequality being spent, states the hypotheses, and carries the consequence
into the branch it controls.

For this note, the repaired live content is the moving-radius oscillation
replacement in Sections 1--2 and the gold/obstruction/silver branch boundary in
Sections 3--6.  The exact downstream burden is unchanged by any sweep count:
the stronger residual source-balanced annular supplier remains open, and the
silver branch applies only after same-solution admission, positive-radius
carrier, and readout have been paid on the admitted object.

Older broad-sweep outputs, TeX marker reports, and generated classifications
may be used to find candidate residues.  They are not proof authority and must
not be cited as completion of the systemic hand-wave repair unless the
importing live manuscript, theorem, frontier, verdict, or active construction
note contains the exact replacement and its downstream consequence in its own
text.
