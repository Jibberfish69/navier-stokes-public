# R3 Exterior High-Order Coefficient Ledger No-Go

## Status

Failed discharge. This note records that the exterior high-order coefficient ledger is not derivable from the current `OriginalSmoothData_R3` inputs without adding a new noncircular exterior cascade theorem.

## Target

The desired theorem is

```math
OriginalSmoothData_{R^3}
\Longrightarrow
\int_0^{T_*}\mathcal E_s^{tail}(R,t)\,dt\to0
\qquad (R\to\infty),
```

where `E_s^{tail}` controls the commutator, cutoff, and pressure terms in the exterior `H^s` identity.

## Exterior high-order identity

For an exterior cutoff `eta_R`, the localized `H^s` energy identity has the form

```math
\frac d{dt}\|eta_R\Lambda^s u\|_2^2
+\nu\|eta_R\nabla\Lambda^s u\|_2^2
\le
C_s\|\nabla u\|_{L^\infty}\|eta_R\Lambda^s u\|_2^2
+C_{s,\nu}\mathcal E_s^{tail}(R,t).
```

The coefficient being spent is exactly

```math
C_s\|\nabla u(t)\|_{L^\infty}.
```

The tail ledger `E_s^{tail}(R,t)` contains the annular cutoff commutators and pressure commutators generated by localization. A Gronwall closure would require

```math
\int_0^{T_*}\|\nabla u(t)\|_{L^\infty}\,dt<\infty,
```

or an equivalent exterior high-order cascade ledger, and that is not supplied by the current `R^3` inputs.

## Why current inputs do not close it

The current `R^3` inputs provide:

```math
u\in L_t^\infty L_x^2,
\qquad
\nabla u\in L^2_{t,x},
```

uniform exterior `L^2` tightness, and compact-core tower bounds. These do not control high-frequency derivative energy in the far field.

A smooth packet sequence can be placed at radii tending to infinity with amplitudes tending to zero so that the exterior `L^2` mass vanishes while the exterior `H^s` norm remains large. This shows that no functional implication

```math
\text{exterior }L^2\text{ tightness}+\text{compact-core bounds}
\Longrightarrow
\text{exterior }H^s\text{ tightness}
```

is available.

The coefficient requirement named above is continuation-grade. Spending it would assume the type of regularity the `R^3` branch is meant to prove.

## Exact noncircular theorem still needed

The correct missing theorem is an exterior cascade statement:

```math
ExtCascade_s(R):
\qquad
OriginalSmoothData_{R^3}
\Longrightarrow
\int_0^{T_*}\mathcal E_s^{tail}(R,t)dt\to0
```

with no use of the global `H^s` norm, no BKM integral, and no pointwise endpoint closure at infinity.

## Verdict

The exterior high-order coefficient ledger is not discharged from the current inputs. The remaining `R^3` obstruction is precisely high-frequency derivative escape at spatial infinity, represented by `TailFace_R3^Hs` unless a new exterior cascade theorem is proved.
