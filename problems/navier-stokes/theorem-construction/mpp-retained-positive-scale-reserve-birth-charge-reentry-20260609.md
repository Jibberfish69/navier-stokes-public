# MPP RetainedPositiveScaleNativeReserveBirthCharge.A Reentry

Date: 2026-06-09

Status: support/Gold-supplier reentry with CM-boundary accounting. This note
does not close public Clay finality. Under the Field-certification CM contrapositive
audit, the terminal zero-thickness `B_ASAC` residue lands Part-side when the same original-NS record remains and true packet/window evidence only when that record is absent;
the retained positive-scale reserve branch below is proof-force only after a
same-ledger payment theorem is proved or after an admitted CM-test witness has
a first Part/Field failure.

## Parent target

The public-finality route is:

```text
NoGenuineExitFromSmoothData.A
<- OriginalSmoothDataPackSurvival.A
<- ScaleCriticalTreeCarleson.A
<- RetainedPositiveScaleNativeReserveBirthCharge.A.
```

The target tested here is the last displayed theorem. It must pay the minimal
retained positive-scale first-created native donor-square/source reserve
surplus, or replace payment by a noncircular proof that the same branch cannot
be the first Pack exit from original smooth data.

## Installed reductions checked

`mpp-source-reserve-birth-charge-direct-attempt-20260525.md` and
`mpp-charge-ledger-injection-proof-method-audit-20260523.md` show that the
first-birth descent removes inherited past reserve, ledger-visible residual,
finite donor trees, and legal losses. The remaining object is the selected
positive first-created source carrier.

`ActiveHeightFluxTrichotomy.A`, `SignedPairReserveVisibility.A`, and
`TerminalSignedSaturationCMFaceReduction.A` are valid only in the CM
witness direction. They say that a failed same-ledger signed balance lands
in Part/Field support for `Exit(Q):=not Member(Q)`. They do not prove
that original smooth data cannot produce the first Pack exit.

Thus face-consumption is not enough for the public-finality theorem. The parent
route still needs a forward payment, source depletion, or terminal residue
Liouville theorem.

## Direct pass

Work on the minimal terminal window `W` after inherited, represented, finite,
and legal pieces are removed. The selected positive reserve birth has four
cases.

1. A same-ledger signed partner is retained. This case is paid only under an
   explicit same-witness signed-payment theorem, for example a proved
   `ASAC.A` together with `PositivePairWeightDefectCharge.A` and the required
   packet-residual, no-incoming, and backward-uniqueness hypotheses. The
   installed CM-face reductions alone do not pay the positive mismatch; they
   classify failed signed balance as Part/Field support for
   `Exit(Q):=not Member(Q)`.
2. The partner exits through a named boundary, projected, cutoff, pressure,
   collar, off-family, or heat-action ledger. This is legal loss.
3. The partner is a finite or non-Zeno internal donor sink. `LocalDonorBalance.A`
   and `EntranceLeafDecay.A` pay it.
4. The branch is a terminal Zeno native source residue after all paid exits are
   removed.

Cases 2 and 3 are paid by installed ledgers. Case 1 is paid only under the
same-witness signed-payment hypotheses above. After those paid exits and any
proved signed-payment alternative are removed, the remaining terminal Zeno
branch is the produced `B_ASAC` equality class:

```text
ASACDefectMeasure.A
+ TerminalNoFreeSinkEqualityClassProduction.A
=> B_ASAC.
```

The produced class has nonzero inherited native positive source residue, no
earlier selected source slice, zero ASAC defect, no legal exit, no finite or
non-Zeno donor exit, and terminal Zeno source support.

The already-checked direct attempt on `ZenoResidueLiouville_{B_ASAC}.A` does
not close from those clauses. The terminal layer model

```text
g_m(s) = m 1_{(-1/m,0]}(s)
```

still has bounded local `L^1_s` source mass, no fixed earlier selected slice,
and a nonzero terminal time-face atom.

## Result

`RetainedPositiveScaleNativeReserveBirthCharge.A` is not proved from the
installed inputs.

The forward supplier split is:

```text
ASAC.A + PositivePairWeightDefectCharge.A
```

with the packet-residual, no-incoming, and backward-uniqueness hypotheses needed
to pay the retained signed-partner branch on the same witness, or

```text
ZenoResidueLiouville_{B_ASAC}.A
```

or any equivalent Gold-supplier theorem that proves the produced `B_ASAC`
terminal source residue has no terminal time-face atom:

```text
BASACTerminalSourceAntiConcentration.A,
UniformTemporalSourceIntegrability_{p,B_ASAC}.A,
TerminalSourceReverseHolder_{B_ASAC}.A,
BASACTimeFaceAntiAtom.A,
ProductionIntoRigidBASACTimeFaceSubclass.A,
or a no-incoming/backward-uniqueness theorem that also removes the terminal
time face.
```

Work on terminal anti-atom is a child supplier for the forward-positive Gold
route. It is not the whole public-finality target, and under the Field-certification
CM contrapositive route the terminal zero-thickness `B_ASAC` residue is consumed
as Field-window support non-survival rather than as a required deletion theorem.

## Nonfinality

The PDFs remain nonfinal. This note cannot support a Clay-facing claim of
`NoGenuineExitFromSmoothData.A` by spending ASAC/pair-weight language as an
installed ledger. Any future use of this route must either prove the same-witness
signed-payment hypotheses above, use the Field-certification CM Part/Field landing for an
admitted terminal witness, or replace the supplier route by an exact
Pack-survival theorem.
