# MPP ScaleCriticalTreeCarleson.A Direct Irreducibility Note

## Status

This note fixes the direct post-reconcile naming and route custody on the
reopened source wall.

On the direct Carleson branch there is now one exact remaining theorem object:

```math
\boxed{
ScaleCriticalTreeCarleson.A.
}
```

The phrase

```math
ScaleCriticalPulseReserve.A
```

may be used as a descriptive alias for the same scalar reserve, but it is not a
separate theorem target and is not tracked as an independent live obligation.

## Installed Direct Reduction

The direct branch currently runs:

```math
ParentSquareEmbed.A
+ScaleCriticalTreeCarleson.A
\Longrightarrow
LocalPositiveSourceCarleson.A
\Longrightarrow
SOURCE.NO\text{-}PULSE.A.
```

Here `ParentSquareEmbed.A` is already installed by the transported packet
Bessel calculus.  It removes the bounded-parent-selection obstruction and leaves
exactly the scalar reserve

```math
\int
\sum_{k>N}
2^k
\left(
\sum_{\ell>k+4}D_\ell(t)
\right)^2dt.
```

That scalar reserve is the theorem named `ScaleCriticalTreeCarleson.A`.

## Direct Supplier Routes Do Not Lower The Wall

The native direct supplier languages have all been audited, and none lowers the
direct wall beneath `ScaleCriticalTreeCarleson.A`.

### Terminal coherence route

The bridge

```math
TerminalSourceCoherence.A
\Longrightarrow
PositiveSourceDepletion.A
\Longrightarrow
ScaleCriticalTreeCarleson.A
```

is valid, but `TerminalSourceCoherence.A` is not installed from current energy,
pressure, vorticity-direction, Beltrami/helical, signed-commutator, or
same-fluid inputs.

### Reverse-Holder parent route

The inverse-Littlewood-Paley / reverse-Holder parent route does not close from
finite banding, raw orthogonality, or same-fluid transport.  Its exact missing
strengthening is the same finite scale-normalized donor reserve:

```math
ReverseHolderParentConcentration.A
\equiv_{\mathrm{route}}
ScaleCriticalTreeCarleson.A.
```

### Bellman reserve route

The tree-Bellman attempt does not derive the reserve from installed
first-moment, local-energy, or donor-balance ledgers.  Any Bellman functional
whose endpoint values are already paid by those ledgers can spend only
first-moment or cumulative-tail quantities.  A Bellman derivative that pays

```math
2^k\left(\sum_{\ell>k+4}D_\ell\right)^2
```

already contains the missing active-window Carleson reserve.  So the Bellman
route is circular unless an independent theorem installs that reserve.

### Other direct presentations

The direct positive-source / active-square presentations likewise do not lower
the wall.  They are route-equivalent packaging of the same donor reserve:

```math
PositiveSourceDepletion.A
\equiv_{\mathrm{route}}
ActiveSquareCarleson.A
\equiv_{\mathrm{route}}
ScaleCriticalTreeCarleson.A.
```

## Exact Direct Verdict

The direct post-reconcile source wall has one irreducible live theorem target:

```math
\boxed{
ScaleCriticalTreeCarleson.A.
}
```

It is the unique exact direct root after:

```math
ParentSquareEmbed.A
\quad\text{installed,}
```

and after the negative audits of terminal coherence, reverse-Holder parent
concentration, and the tree-Bellman reserve attempt.

Therefore the honest root split is:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```

No live authority surface tracks `ScaleCriticalPulseReserve.A` as a
separate theorem object from `ScaleCriticalTreeCarleson.A`.
