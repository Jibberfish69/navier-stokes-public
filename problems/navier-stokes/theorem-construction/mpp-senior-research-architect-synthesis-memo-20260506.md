# Senior Research Architect Synthesis Memo

Date: 2026-05-06

Status: repo-wide audit-open mathematical architecture memo; not a theorem
discharge or promotion surface.

Post-audit correction: `FirstHeightCreationDichotomy.A` was later pursued to a
terminal direct attempt and collapsed as an independent theorem atom.  It is
kept only as historical target selection; the live direct path must attack
`HeightFluxControl.A` through a real structural mechanism.

Scope:

```text
problems/navier-stokes/**
```

Mandate: turn the Navier-Stokes lane from a pile of theorem fragments into a
convergent proof architecture.

## Executive Thesis

The current center of gravity is the class-membership contrapositive program.

The primitive object is:

```text
Exit(Q; O_NS^work) := not Member(Q; O_NS^work).
```

The route certificate is:

```text
CM_{N,r,Q}:=Part_{N,Q} and Field_{N,r,Q}.
```

The installed theorem-grade direction is:

```text
CM => Member.
```

The installed contrapositive use is first-exit / nonsmooth witness extraction:

```text
NonSmooth(Q) or first class exit
=> not Part_{N,Q} or not Field_{N,r,Q}.
```

The repo is therefore read as a class-exit atlas.  Every theorem note,
branch, source-wall packet, receiver estimate, endpoint row, compactness
argument, or signed-current idea must identify which of the three CM Part/Fields it
serves and what licensed bridge moves it into Part or Field witness, class-membership direction, or an
endpoint row.

If a branch does not do that, it is not proof spine.  It may be source recovery,
diagnostic context, or a future theorem idea, but it cannot be promoted.

## 1. Proof Spine

The proof spine is:

```text
O_NS^work
=> Member(Q;O_NS^work)
=> no class exit
=> no finite-time singular endpoint.
```

Operationally, the route uses the CM witness:

```text
Pack_Q
Part_{N,Q}
Field_{N,r,Q}
```

and the forward class-membership chain:

```text
RSCB.NKF
=> NKF.Native
=> ACT.KX
=> ACT.X-Readout
=> ACT.A
=> RCF.A
=> LCI.A
```

then:

```text
LCI.A + FCI.5f
=> CSP.A
=> OFP.A
=> CFI.A.
```

Endpoint closure is downstream:

```text
END.Pack
+ END.Field
+ END.TowerBound
+ END.Cross
+ END.Exh
=> End_NS.
```

Terminal contradiction:

```text
CFI.A + End_NS
=> no finite-time class exit.
```

This is the spine.  Everything else is supplier, readout, endpoint consumer,
alternate coordinate, or false branch until bridged into this spine.

## 2. Major Nodes

### Primitive CM nodes

```text
Member
CM
Pack
Part
Field
Exit
```

These are the proof-bearing objects.

### Receiver/source nodes

```text
ACT.KX
RCF.A
LCI.A
FCI.5f
CSP.A
OFP.A
CFI.A
```

These supply Field preservation and class-membership forward invariance.

### Endpoint/readout nodes

```text
END.Pack
END.Field
END.TowerBound
END.Cross
END.Exh
READ.COVER_downstream
Field.Read
DTC.Read
READ.END
```

These are terminal consumers.  They are not upstream proof suppliers unless a
separate bridge-license theorem says so.

### Source-wall nodes

```text
ScaleCriticalTreeCarleson.A
LocalPositiveSourceCarleson.A
SOURCE.NO-PULSE.A
SourcePulseExclusion.A
NativePositiveSourceControl.A
```

These are Field-face residual source-wall presentations.

### Direct source-wall subnodes

```text
ReserveCreationCharge.A
SquareReserveEvolution.A
DonorHeightCreation.A
HeightFluxControl.A
```

These are the current shortest audit-open direct path into the source wall.

### Zeno alternate nodes

```text
TemporalNonAtomicSource.A
ZenoBoundedClass_B.A
ZenoResidueLiouville_B.A
```

These are the live alternate source-wall route.

### Signed-current support nodes

```text
LocalizedSkewCurrent.A
TerminalSkewLocalizationLedger.A
WeightedLiftedSkewDefectLegal.A
CycleHeatAction.A
ParabolicEdgeResistance.A
TerminalSignedSaturation.A
ParabolicNoFreeSink.A
```

These are not lower roots unless one proves an independent
weighted/lifted/signed theorem that does not return to the same positive source
wall.

## 3. Hidden Equivalences

The repo contains many different names for the same obstruction.  The following
must be collapsed unless a future proof note installs a genuinely independent
theorem.

### Positive source packet

```text
LocalPositiveSourceCarleson.A
SOURCE.NO-PULSE.A
SourcePulseExclusion.A
```

All mean the terminal packet positive source estimate once the Field-face
branch is selected.

### Scale-critical tree/source carrier

```text
ScaleCriticalTreeCarleson.A
ActiveSquareCarleson.A
SourcePulseExclusion.A
```

After edge disintegration, each requires control of the source-pulse carrier;
none proves a distinct lower root.

### Active square-density obstruction

```text
ActiveSquareResidualTail.A
ActiveShellAmplitudeGain.A
SquareSource^{norm}.A
SignedLocalSource.A
```

All name the same spike-sensitive square density.

### Signed counter-edge obstruction

```text
ParabolicNoFreeSink.A
TerminalSignedSaturation.A
SignedPositiveBalance.A
```

All name the same missing no-free-sink law.

### Amplitude reserve obstruction

```text
CriticalFluxAmplitudeSmall.A
ParabolicEdgeResistance.A
TerminalAmplitudeGain.A
```

Each still requires an independent bound on the source reserve; naming a
different reserve theorem does not prove it.

## 4. Real Proof Load vs Naming Noise

The real load is not:

```text
PCTP.hard
TTU.A
AWG.A
TGC.A
Jump_avg
READ.COVER
SourcePulseCMExit.A
```

as primitive targets.

Those are downstream consumers, branch manifestations, terminal-tail readouts,
or support machinery.

The real load is:

```text
Field-face source-wall exclusion
```

after the CM witness split selects `not Field`.

More exactly:

```text
not Field
=> VisibleJump_Field
   or not ReceiverFieldSupplier
   or not ReadCover/NoJumpSupplier
   or not NativePositiveSourceControl.A
   or ZenoResidue_B.
```

After visible-jump / receiver / readout / legal-loss alternatives are routed,
the remaining source-wall burden is:

```text
ScaleCriticalTreeCarleson.A
or
ZenoBoundedClass_B.A + ZenoResidueLiouville_B.A.
```

That is the honest proof load.

## 5. False Branches and Illegal Promotions

The following promotions are illegal in the current architecture.

```text
SOURCE.NO-PULSE.A as primitive proof from OriginalSmoothData.
```

Reason: it is a Field-face source-wall supplier/readout theorem, not the CM
primitive.

```text
PCTP.hard / TTU.A as proof of CM.
```

Reason: terminal-tail production is downstream unless bridged into Pack, Part,
or Field; Member is downstream of the CM witness certificate.

```text
READ.COVER as upstream cover production.
```

Reason: downstream `READ.COVER` lives after averaged endpoint closure.  The
upstream replacement is `GoodScaleNonCollapse.A`.

```text
END.Field as proof of Field.
```

Reason: `END.Field` spends `Field + OFP.A`; it cannot prove the same row it
consumes.

```text
FieldSourceResidueJump.A as endpoint contradiction.
```

Reason: it classifies a source residue as `(Field,Jump)`; contradiction
requires the noncircular `END.Field` row.

```text
signed-current algebra as source-wall discharge.
```

Reason: bare skewness occurs before terminal weights, cutoffs, same-fluid
selection, lifted commutators, and positive-part extraction.  The hard gate is
`WeightedLiftedSkewDefectLegal.A`.

```text
energy area controlling square pulses.
```

Reason: energy controls `int sum D_j dt`, while the wall needs spike-sensitive
control of `int sum 2^{-j}D_j^2 dt` or an equivalent donor-reserve carrier.

## 6. Shortest Current Route

The shortest viable route is the direct Field-face source-wall route.

```text
HeightFluxControl.A
=> DonorHeightCreation.A
=> ReserveCreationCharge.A
=> SquareReserveEvolution.A
=> ScaleCriticalTreeCarleson.A
=> LocalPositiveSourceCarleson.A
=> FCI.5f source support
=> Field-face closure
=> CM
=> Member
=> endpoint exclusion
=> no finite-time class exit.
```

This is shorter than trying to prove a global no-pulse theorem directly.

Why: `ParentSquareEmbed.A` has already eliminated diffuse parent multiplicity.
The remaining direct obstruction is first-time creation of the donor square
reserve.  The donor square reserve has now been sharpened to first-time
creation of a donor height:

```text
H_N(t)=sup_{k>N} 2^k sum_{\ell>k+4}D_\ell(t).
```

Shell balance then sharpens that to:

```text
HeightFluxControl.A
```

control of the positive weighted shell-flux carrier:

```text
sup_{k>N} [ 2^k sum_{\ell>k+4}2^{2\ell}F_\ell(t) ]_+.
```

This is the current shortest direct atom.

## 7. Alternate Route

The alternate route is Zeno rigidity.

```text
TemporalNonAtomicSource.A
=> ZenoBoundedClass_B.A
=> ZenoResidueLiouville_B.A
=> no terminal source-residue atom
=> source-wall closure.
```

The issue is that the current compactness class is too weak if it allows
terminal source-residue atoms.  The route needs a theorem that forbids the
source-residue measure from charging the terminal time slice.

The Zeno route is worth keeping because it attacks the same source wall from a
compactness / ancient-residue direction rather than from shell-flux control.

## 8. How Nonsmoothness Becomes Class Exit

The contrapositive grammar is:

```text
finite nonsmooth endpoint
=> first failure of Member(Q;O_NS^work)
=> first witness failure
=> not Pack or not Part or not Field.
```

Pack failure means the same-fluid object cannot maintain finite material
separation coherence.

Part failure means the same-fluid tower cannot maintain shared participation
through the pressure-viscosity law.

Field failure means the one-field readout cannot maintain a coherent
pressure-viscosity evolution across the same terminal carrier.

The source-wall work belongs to the Field failure branch.  It says: if the
receiver/source/readout alternatives are routed, then a Field failure must
manifest as a native positive source-wall residue.  Eliminating that residue
removes the Field face.  With Pack and Part retained, CM persists.  With CM,
Member persists.  With Member and endpoint exclusion, finite class exit is
impossible.

That is the nonsmoothness-as-class-exit architecture.

## 9. Audit Lenses

### CM core lens

Ask:

```text
Does this theorem prove Part or Field witness, class-membership direction, or a first-exit face split?
```

If not, it is not CM core.

### Source-wall lens

Ask:

```text
Is this genuinely lower than ScaleCriticalTreeCarleson.A,
or just another name for the active square/source pulse wall?
```

If it returns to square reserve, active amplitude, or no-free-sink without a
new theorem, collapse it.

### Endpoint/readout lens

Ask:

```text
Is this being spent upstream?
```

If yes, reject unless a bridge-license note explicitly reverses the direction.

### Compactness/Zeno lens

Ask:

```text
Does the limiting class forbid terminal source atoms?
```

If not, it is only `B_min`, not the time-rigid class required for Liouville.

### Skeptic/failure-mode lens

Ask:

```text
Where did positive-part extraction lose signed structure?
Where did a downstream endpoint row get used as an upstream supplier?
Where did a generated surface advertise closure while the source wall stayed open?
```

### Corpus recovery lens

Ask:

```text
Does an older note already prove a bridge into Part or Field witness, class-membership direction, or endpoint?
```

If not, recover it as context, not proof.

## 10. Missing Bridges

The critical missing bridges are:

```text
HeightFluxControl.A
```

Control positive weighted shell flux after height differentiation.

```text
DonorHeightCreation.A
```

Charge first-time creation of large normalized donor height.

```text
ReserveCreationCharge.A
```

Charge first-time creation of donor square reserve.

```text
TemporalNonAtomicSource.A
```

Forbid terminal time atoms in the Zeno source-residue measure.

```text
WeightedLiftedSkewDefectLegal.A
```

Show weighted/lifted signed-current commutators are legal without spending the
unproved positive source-wall estimate.

```text
GoodScaleNonCollapse.A
```

Upstream replacement for downstream `READ.COVER`.

```text
PackGainLedger.A or RetainedPackGainProduction.A
```

If the amplitude route is revived, prove accumulated positive low-strain gain
is charged by Pack/Part, not merely bounded by finite net deformation.

## 11. Ranked Theorem Atoms

### Rank 1: `HeightFluxControl.A`

Highest leverage direct atom.  If proved noncircularly, it gives donor height
creation charge and closes the direct reserve route.

Required idea: signed/decorrelation/depletion control of

```text
sup_k [2^k sum_{\ell>k+4}2^{2ell}F_ell]_+.
```

### Rank 2: `TemporalNonAtomicSource.A`

Highest leverage alternate atom.  If proved, it produces the time-rigid Zeno
class needed for residue Liouville.

Required idea: terminal source-residue measures cannot charge the final time
under same-fluid Navier-Stokes scaling.

### Rank 3: `WeightedLiftedSkewDefectLegal.A`

Highest leverage signed-current atom.  It is difficult but would make the
source-current route real rather than decorative.

Required idea: the weighted/lifted commutator principal strain term is canceled
or charged by geometry, not absolute-value estimates.

### Rank 4: `GoodScaleNonCollapse.A`

Important for readout cleanup, but not lower than the source wall unless proved
from independent source-wall or Zeno data.

### Rank 5: `PackGainLedger.A / RetainedPackGainProduction.A`

Useful if the amplitude route is revived.  Currently blocked because finite
Pack gauge controls net deformation, not accumulated positive strain action.

## 12. What Would Actually Move the Project

The project moves if one of the following happens.

### Move A: Direct source-wall closure

Prove:

```text
HeightFluxControl.A
```

by a native Navier-Stokes theorem controlling positive weighted shell flux.

This is the shortest route.

### Move B: Zeno closure

Prove:

```text
TemporalNonAtomicSource.A
```

then the rigid class and residue Liouville theorem.

This is the current compactness-route candidate.

### Move C: Signed-current legalization

Prove:

```text
WeightedLiftedSkewDefectLegal.A
```

without invoking a critical flux amplitude theorem equivalent to the source
wall.

This would revive the signed-current program as more than support.

### Move D: Corpus recovery miracle

Find an older source-backed theorem that already proves one of:

```text
HeightFluxControl.A
TemporalNonAtomicSource.A
WeightedLiftedSkewDefectLegal.A
```

with compatible carrier, scale, endpoint, and proof direction.

Anything else is likely naming noise.

## 13. Operating Rules for the Repo

Every future theorem-crank pass must:

```text
1. classify the target as CM core, face supplier, source root, readout,
   endpoint, export, or quarantine;
2. run the bridge-license gate;
3. run the endpoint-spend gate;
4. collapse aliases before naming a new theorem;
5. attack one bounded theorem atom;
6. record the exact installed part and exact obstruction;
7. sync live surfaces;
8. refuse export/promotion while source-wall-root-after-reconcile is open.
```

The repo does not accept another branch as "new" unless it proves why it is
not route-equivalent to:

```text
HeightFluxControl.A
TemporalNonAtomicSource.A
WeightedLiftedSkewDefectLegal.A.
```

## Final Architecture Statement

The Navier-Stokes repo is now best treated as:

```text
a CM class-exit proof architecture
with one residual Field-face source wall.
```

The shortest current proof path is:

```text
HeightFluxControl.A
=> DonorHeightCreation.A
=> ReserveCreationCharge.A
=> SquareReserveEvolution.A
=> ScaleCriticalTreeCarleson.A
=> Field-face source-wall closure
=> CM persistence
=> Member persistence
=> endpoint exclusion
=> no finite-time class exit
=> smoothness.
```

The strongest alternate path is:

```text
TemporalNonAtomicSource.A
=> ZenoBoundedClass_B.A
=> ZenoResidueLiouville_B.A
=> source-wall closure.
```

The signed-current path becomes serious only if:

```text
WeightedLiftedSkewDefectLegal.A
```

is proved without circularly spending the same source wall.

That is the convergent proof architecture.  The rest of the repo is now
organized around proving, refuting, or source-recovering those atoms.
