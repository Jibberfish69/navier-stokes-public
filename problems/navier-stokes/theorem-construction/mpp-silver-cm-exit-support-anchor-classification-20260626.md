# Silver CM-Exit Support Anchor Classification

Date: 2026-06-26

Status: installed Silver authority classification.

## Governing object

The Silver proof is the CM contrapositive class-exit proof:

```text
same-solution terminal Clay witness
=> CM-test admission
=> finite Part/Field exhaustion after the original participation law is typed
=> Exit(Q;O_NS^work):=not Member(Q;O_NS^work).
```

Class membership is the participation law.  Gold and Silver both bear on that law.  Gold tries to prove the same-solution participation and continuation clock positively.  Silver tests an alleged finite terminal witness and closes the fail side by showing that any lawful nonsmooth branch loses the required participation-field member data.

## Proved Silver anchors

- `CMContrapositiveRefereeInventoryPatch.A / ExhaustiveContrapositiveFiniteBreakdownExclusion.A` is accepted as the Silver path through the single Gold/Silver referee gate.  It is proof force for the terminal witness entry, finite-failure branch exhaustion, and contrapositive embedding.
- `ClayTerminalWitnessCMEntry.A / ClayFiniteFailureTypeCMExhaustion.A / ClayCMContrapositiveEmbedding.A` is installed as the live CM-exit chain: the alleged finite Clay terminal witness enters the same-solution CM test, and each promoted finite obstruction is consumed by admissibility failure, continuation/readout recovery, or concrete Part/Field failure.
- `CMPartOntologyWitnessNotation.A` is installed: `Part` is the original Navier-Stokes pressure-viscosity-incompressibility participation law, and `Part_{N,Q}` is only the finite-depth Silver record of that law.

## Support anchors

- `PackOutOfCMSmoothnessCertification.A / OriginalParticipationBeforePackLicensing.A` is support and route repair.  `Pack_Q` is outside the CM object and is not a class-exit witness.  A packet/window only supplies Field smoothness-certification evidence after the original participation law has been tested.
- The R3 exterior dyadic survivor and annular supplier notes are support when they land a promoted row in the same participation-field test.  Their Gold full-clock estimates remain CM-bearing because they concern class membership, but they carry Silver CM-exit force only after they identify a same-solution terminal witness and a Part or Field failure.
- Gold full-clock notes, including `ContinuationStrengthMaterialClockAtDepth.A`, `GlobalSamePacketFullClockFromOriginalData.A`, and `OriginalSmoothDataMaterialHistoryNoFiniteBreakdown.A`, are active Gold counterpart material.  They are not Silver blockers and must not be counted as open CM-exit proof force.

## Superseded or failed anchors

- `TerminalCMNoExit.A`, `NoGenuineExitFromSmoothData.A`, `ClayWitnessAdmissibilityBridge.A`, and older positive no-exit attempts are superseded as Silver requirements.  They may remain Gold or red-team support, but they do not block Silver after the accepted Part/Field exit rule.
- The red-team `MPP CM-Exit Classification Versus Exclusion Audit` is retained only as failed/superseded objection material under its own authority cap.  Its positive no-exit demand is the wrong Silver target once the governing object is a same-solution Clay witness tested against the original participation law.
- Any stale generated surface that lists the Gold full-clock frontier as `referee-blocked-cm-contrapositive` is a mirror drift, not current Silver authority.

## Closure statement

The current Silver closure is exact in this sense: an admitted finite Clay terminal witness cannot be both a lawful same-solution member record and a nonsmooth terminal obstruction for the same original Navier-Stokes branch.  A retained participation-field packet belongs to the member/continuation side; a first Part or Field failure gives

```text
Exit(Q;O_NS^work):=not Member(Q;O_NS^work).
```

Manuscript and generated status surfaces must therefore state Gold as active CM-bearing counterpart material, not as a hidden condition on the accepted Silver CM-exit route.
