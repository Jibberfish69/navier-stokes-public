# Silver CM Face-Failure Finality Correction

Date: 2026-06-19

Status: installed correction.

## Correction

The following burden is rejected for the Silver CM route:

```text
After a terminal witness lands in Part/Field failure, prove separately
that this failure branch cannot itself be the finite-time breakdown.
```

That burden imports a forward no-exit theorem into the contrapositive route.

For the Silver CM route, a same-solution finite terminal obstruction is consumed
when it satisfies the installed branch law:

```text
CM-test entry
=> admissibility failure
   or continuation/readout recovery
   or concrete Part/Field failure
=> Exit(Q):=not Member(Q).
```

The same-solution obstruction is first read under the original Navier--Stokes
participation law.  A later `Part_{N,Q}` label is the localized finite record
of that law inside the Silver witness tree, not the source of participation.

The Part/Field failure is the contrapositive witness. It is not a leftover branch that
must then be deleted by `NoGenuineExitFromSmoothData.A`.

## Authority Consequence

`NoGenuineExitFromSmoothData.A`,
`ClayCounterexampleExclusionBridge.A`,
`OriginalSmoothDataPackSurvival.A`, first-Pack survival, terminal anti-atom, and
related positive supplier attempts may remain useful Gold/positive-route or
manuscript/release support. They are not Silver blockers after
`CMContrapositiveRefereeInventoryPatch.A /
ExhaustiveContrapositiveFiniteBreakdownExclusion.A` is accepted.

A future Silver blocker must name a concrete promoted finite-breakdown row that:

```text
enters the CM test
and does not continue/read out
and does not land in a real Part/Field failure.
```

Generic demands to prove that `Exit(Q)` is inadmissible, or that Part/Field failure
cannot be finite breakdown, are superseded wrong targets.
