# MPP SourcePulse First Unpaid Self-Attempt

## Status

Bounded self-attempt after
`mpp-endpoint-production-cell-route-audit-20260515.md`.

Target:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

This target is the branch killer needed for the averaged
`ACT.KX/DTC.A_avg` endpoint-production route. It is not the CM primitive and is
not a fourth witness face. It is source/readout support until a checked bridge
lands the downstream consequence in `Pack`, `Part`, or `Field`.

## Attempt 1: local source / active-square route

The most direct local route tries to prove a heat-scale source-control theorem:

```math
OriginalSmoothData
\Longrightarrow
QDrain.A
\quad\text{or}\quad
MSC.BadAC
\Longrightarrow
SourcePulseExclusion.A.
```

The installed audits do not supply this implication. Local energy, pressure
Poisson structure, and finite spacetime integrability give raw finite carriers,
but they do not control the normalized terminal active-square density on a
shrinking family of selected heat-scale windows.

Equivalently, the following presentations are still route-equivalent to the
same missing theorem:

```math
ActiveSquareResidualTail.A,
LocalActiveCarleson.A,
SignedLocalSource.A,
PositiveResidualReserve.A,
MSC.BadAC.
```

They name useful faces of the obstruction, but none is currently a lower
installed theorem from original data. A proof on this side requires a new
positive-part strain depletion, heat-scale square-source estimate, active-shell
amplitude gain, or direct active-window Carleson theorem.

## Attempt 2: ancestry / donor-depletion route

The clean ancestry route is:

```math
OriginalSmoothData
\Longrightarrow
TwoTowerDonorDepletion.A
\Longrightarrow
ParentConcentrationOrDiffuseCharge.A
+BackwardDrainFunctional.A
\Longrightarrow
SourcePulseExclusion.A.
```

This is sharper than the local-source presentation because
`TwoTowerDonorDepletion.A` has a theorem-shaped statement: every legal
same-fluid source-parent edge must spend a finite donor reserve, up to already
licensed summable losses.

The installed material does not yet prove it. `LocalDonorBalance.A` separates
the algebraic possibilities into drain, legal loss, or refill from source
parents, but it does not install the global donor-reserve telescoping needed to
forbid an infinite terminal source-parent chain.

## Result

The endpoint-production audit should not be marked closed.

The honest live burden remains:

```math
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
```

The narrowest selected next attack inside that burden is:

```math
TwoTowerDonorDepletion.A.
```

That sub-burden is selected only as the most concrete ancestry-side route. It
does not replace `SourcePulseExclusion.A` on the live frontier, and it does not
license use of `Field`, `OFP.A`, `End_NS`, `READ.COVER`, `DTC.Read`,
`Field.Read`, or endpoint closure upstream.
