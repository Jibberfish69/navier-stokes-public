# MPP SourcePulse Irreducible Core and Route-Equivalence Note

## Status

Route-equivalence closure for the current Navier-Stokes frontier:

```math
\boxed{
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
}
```

This note does not prove `SourcePulseExclusion.A`.  It proves the bookkeeping
closure that all currently audited target names are presentations of one
irreducible source-pulse core.

## Source-Pulse Core

A terminal source-pulse packet is a same-fluid family

```math
\mathcal P_n=(Q_n^\Phi,j_n,I_n)
```

with

```math
j_n\to\infty,
\qquad
|I_n|=2^{-2j_n},
\qquad
t_n\uparrow T_\ast,
```

selected by the `Jump_avg` branch, such that on the same windows:

```math
\sup_{t\in I_n}\gamma_{j_n}(t)\ge \eta>0,
\tag{SPE.1}
```

the active nonlinear source / mixed bilinear packet carries nonvanishing
normalized mass,

```math
\liminf_{n\to\infty}
\int_{I_n}
\mathfrak S_{j_n,loc}^{active}(t)\,dt
>0,
\tag{SPE.2}
```

and the already-installed signed reserve, cumulative tail reserve, local energy
charge, pressure/cutoff supplier, and active-window Carleson ledgers do not pay
for that mass on the same selected windows.

The irreducible theorem is:

```math
\boxed{
SourcePulseExclusion.A:
\quad
\text{no terminal source-pulse packet } \mathcal P_n
\text{ is generated by } OriginalSmoothData.
}
```

## Route-Equivalent Presentations

The following targets are route-equivalent presentations of the same core:

```math
SignedLocalSource.A,
\qquad
LocalSource.NoPulse.A,
\qquad
SOURCE.NO\text{-}PULSE.A,
\qquad
MSC.BadAC,
\qquad
ActiveShellSourceNormalize.A,
```

```math
PositiveResidualReserve.A,
\qquad
ActiveSquareResidualTail.A,
\qquad
ActiveSquareCarleson.A,
\qquad
SquareSource.A,
\qquad
ActiveShellAmplitudeGain.A,
```

```math
LocalPositiveSourceCarleson.A,
\qquad
PositiveSourceDepletion.A,
\qquad
ReverseHolderParentConcentration.A,
\qquad
TerminalSourceCoherence.A,
\qquad
PositiveRemainderDepletion.A,
```

```math
QDrain.A,
\qquad
NOHOP.A,
\qquad
PressureVortexDeplete.A,
\qquad
HelicalCoherenceDeplete.A,
\qquad
AntiBeltramiCarleson.A,
\qquad
ActiveVorticityCoherence.A,
```

```math
LocalizedDrain.A,
\qquad
PackStrainDeplete.A,
\qquad
ParabolicNoPulse.A,
\qquad
ViscousNoRefill.A,
\qquad
MSC.Localize^{src}+Ancient.NoPulse^{src},
```

and the ancestry spelling

```math
SourceParent.Measure
+
\bigl(
AncestryDrain.A
\ \vee\
AncestryCompactBound.A+SourceResidueLiouville.A
\bigr).
```

The formal source-parent object is defined in
`mpp-sourceparent-measure-formal-definition-note.md`, and its five axioms are
audited directly in `mpp-sourceparent-measure-axiom-direct-audit-note.md`.
That audit records that source recovery, parent lower density, same-fluid edge
fidelity, summable loss, and charge alternative are not supplied by current
installed estimates.
The localized-core bypass note
`mpp-sourcepulse-localized-core-bypass-of-msclocalize-note.md` records that
ancestry may start directly from the localized source-pulse packet, without an
extra `MSC.Localize^{src}` theorem.  The remaining drain/Liouville alternatives
are audited in `mpp-ancestry-drain-liouville-alternatives-direct-audit-note.md`.

The formal parent-measure object behind this ancestry spelling is defined in
`mpp-sourceparent-measure-formal-definition-note.md`.

The localized-core bypass
`mpp-sourcepulse-localized-core-bypass-of-msclocalize-note.md` records that
ancestry should start from failure of the localized `SourcePulseExclusion.A`
core, not from the global `MSC.BadAC` product carrier.  The parent-measure
split `mpp-sourceparent-edge-disintegration-and-antidiffuse-split-note.md`
then reduces `SourceParent.Measure` to `EdgeDisintegrate.A+AntiDiffuseParent.A`,
with the second factor as the live theorem.
The direct attempt `mpp-antidiffuseparent-a-direct-attempt-note.md` sharpens
that live theorem to `ParentConcentrationOrDiffuseCharge.A`.
The split audit
`mpp-parent-concentration-or-diffuse-charge-direct-audit-note.md` records that
neither bounded parent concentration nor diffuse-parent charge is installed.
The downstream ancestry closures are audited in
`mpp-ancestrydrain-a-direct-attempt-note.md` and
`mpp-ancestrycompactbound-liouville-direct-attempt-note.md`: the first requires
`BackwardDrainFunctional.A`, while the second requires
`BoundedAncestryClass.A+SourceResidueLiouville.A`.
The conditional assembly is recorded in
`mpp-sourcepulse-ancestry-route-conditional-closure-note.md`: either
`ParentConcentrationOrDiffuseCharge.A+BackwardDrainFunctional.A` or
`ParentConcentrationOrDiffuseCharge.A+BoundedAncestryClass.A+SourceResidueLiouville.A`.
Either package implies `SourcePulseExclusion.A`.
The two-tower donor slogan is formalized in
`mpp-two-tower-donor-depletion-formalization-note.md`: a genuine
`TwoTowerDonorDepletion.A` theorem implies both
`ParentConcentrationOrDiffuseCharge.A` and `BackwardDrainFunctional.A`, but the
note records that raw energy/local-energy flux bookkeeping does not install the
required scale-normalized donor reserve.
The local shell-balance component is proved separately in
`mpp-two-tower-donor-balance-modulo-refill-note.md`; it reduces the donor route
to `RefillTreeWellFounded.A`, the theorem that terminal source-refill ancestry
must stop, charge, or compactify rigidly.
The direct attempt `mpp-refilltree-wellfounded-direct-attempt-note.md` sharpens
that target to the Zeno alternatives `ScaleCriticalTreeCarleson.A` or
`ZenoSourceResidueRigidity.A`.
The no-hidden Zeno note
`mpp-terminal-zeno-source-refill-no-hidden-frontier-note.md` then identifies
the operational Zeno receiver pair as `LocalPositiveSourceCarleson.A` or
`ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A`.
The local-positive side is consolidated in
`mpp-local-positive-source-carleson-final-collapse-note.md`; its coherence
supplier split is audited in
`mpp-terminal-source-coherence-supplier-split-audit-note.md`.

## Equivalence Direction

Each presentation above attacks one of the same three slots:

1. **remove active mass**: show `(SPE.2)` cannot occur;
2. **charge active mass**: force the pulse to spend an installed finite reserve;
3. **rigidify active mass**: localize/extract the pulse and prove its residue
   impossible.

The audited notes show that none of the installed estimates already performs
any of these three jobs.  Instead, each presentation asks for a new theorem
that rules out, charges, or rigidifies the same packet `\mathcal P_n`.

The three slots have also been tested directly:

```text
mpp-sourcepulse-remove-active-mass-direct-attempt-note.md
mpp-sourcepulse-charge-active-mass-direct-attempt-note.md
mpp-sourcepulse-rigidify-active-mass-direct-attempt-note.md
```

Those attempts confirm that the installed energy, pressure, dyadic exchange,
reserve, scheduler, compactness, and source-supplier surfaces do not discharge
any slot without a new terminal source-control theorem.

Thus the live equivalence is:

```math
\boxed{
OriginalSmoothData\Longrightarrow X
\quad\text{for any audited presentation }X
\quad
\Longleftrightarrow_{\mathrm{route}}
\quad
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
}
```

This is route equivalence, not formal equality of definitions.  The bridge is
the shared terminal packet `(SPE.1)`--`(SPE.2)` on the same-fluid active bad
windows.

## Consequence

The next mathematical proof cannot be obtained by renaming the target.  It must
install one genuinely new estimate of the form:

```math
\boxed{
\text{terminal nonlinear source-control on the same heat-scale active windows.}
}
```

Concretely, a successful proof must deliver one of:

```math
\mathfrak S_{j,loc}^{active,+}
\text{ is depleted on } Bad_N(\eta),
```

```math
\mathfrak S_{j,loc}^{active}
\text{ is Carleson / square-source controlled on } Bad_N(\eta),
```

```math
\text{every source-balanced active pulse spends an installed finite reserve},
```

or

```math
\text{every localized source-residue pulse has a forbidden ancient/ancestral
limit}.
```

Until one of these is proved from `OriginalSmoothData`, the frontier remains:

```math
\boxed{
OriginalSmoothData\Longrightarrow SourcePulseExclusion.A.
}
```

The direct self-attempt
`mpp-sourcepulse-direct-core-self-attempt-note.md` tests these three proof modes
against the installed original-data package and records that none is currently
supplied.
The normal-form audit
`mpp-sourcepulse-normal-form-and-unspent-reserve-audit-note.md` sharpens the
failure packet further: if `SourcePulseExclusion.A` fails, a terminal
same-fluid packet may be selected with native positive active source mass while
energy, preterminal smoothness, scheduler bad-time smallness, signed exchange,
pressure/cutoff/source ledgers, local energy/compactness, and same-fluid
transport have all either been spent, absorbed as legal losses, or shown not to
charge that native carrier.
The native measure decomposition
`mpp-sourcepulse-native-source-measure-decomposition-note.md` expresses this as
a singular component of the native positive active source measure relative to
the installed route-ledger measure.  Eliminating that singular component is the
current narrowest measure form of `SourcePulseExclusion.A`.
