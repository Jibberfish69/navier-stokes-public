# MPP SourcePulse Positive-Scale Reserve Promotion Reentry

## Status

Active-completion reentry after the donor / refill-tree pass.

This note is not a final proof.  It records the parent-thread judgment on the
Carleson / charge side of the source-pulse wall and identifies the next
theorem unit that is smaller than the broad source-wall label.

## Governing Branch

The current chain is:

```math
SourcePulseExclusion.A
\Leftarrow
TwoTowerDonorDepletion.A
\Leftarrow
RefillTreeWellFounded.A
\Leftarrow
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoSourceResidueRigidity.A.
```

The Carleson side has been sharpened to first-time creation of the donor square
reserve on terminal heat windows:

```math
ReserveCreationCharge.A.
```

The direct square-reserve attempt proves only this reduction.  It does not
install the charge theorem.

## What Is Already Sorted

The Pack-first source-reserve audit gives a typed classification for the
source-reserve branch.

Already sorted as Pack-first face failure:

```math
\text{terminal zero-radius source residue}
\quad\text{or}\quad
\text{selected unpaid infinite donor-refill ancestry}
\Longrightarrow
\neg Pack_Q.
```

Already installed as support:

```math
ParentSquareEmbed.A
```

under the transported packet Bessel hypotheses, and:

```math
LocalDonorBalance.A+EntranceLeafDecay.A
```

for finite and non-Zeno source-refill trees.

These do not pay the retained positive-scale first-created native reserve.

## What Remains Quarantined

The surviving charge-side atom is:

```math
RetainedPositiveScaleNativeReserveBirthCharge.A
/
TerminalReserveFirstAppearanceCharge.A.
```

The promotion condition is precise:

```math
\boxed{
\text{the retained positive-scale first-created native donor-square reserve
surplus is either paid on the same ledger, or it is admitted to the CM terminal
test and routed to a first Pack/Part/Field face failure.}
}
```

Generated theorem-creation surfaces for `ReserveCreationCharge.A`,
`TerminalReserveFirstAppearanceCharge.A`, and `ActiveHeightFluxTrichotomy.A`
cannot be used as proof.  Their own promotion plans are conditional or
support-only, and the Pack-first audit keeps the retained positive-scale
surplus in supplier quarantine until the promotion condition above is proved.

## Direct Proof Attempt

Choose a first minimal terminal heat window `W` where the donor-square reserve
surplus appears:

```math
R_N(W)>(1-\delta)R_N(Past(W))+Loss_{legal}(W)+\eta.
```

After inherited past reserve, child residual, and legal losses are removed, the
remaining object is a first-created positive-scale native reserve surplus.

The scalar short-pulse obstruction remains:

```math
Past(W_m)=0,\qquad
Charge_N(W_m)\to0,\qquad
R_N(W_m):=1.
```

This does not produce a Navier-Stokes counterexample.  It shows that
first-moment dissipation, static parent-square embedding, finite donor balance,
and raw local energy cannot prove the dynamic charge theorem.

The active-height route also does not close as a forward theorem.  The
zero-first-moment scalar pulse can carry order-one one-sided reserve while its
first signed moment collapses.  Therefore `ZeroMomentReserveVisibility.A` and
forward no-free-sink language cannot be spent as proof.

## Collapse To Terminal Source-Atom Escape

The only noncircular way to promote the retained positive-scale reserve surplus
is to keep the same native positive source carrier and prove that a surviving
terminal source atom must escape through one of the CM-visible doors.

The sharp current theorem is:

```math
BASACAtomEscapeDichotomy.A:
\quad
\mu_\ast^{src}(B_R\times\{0\})>0
\Longrightarrow
BASACNoIncomingSourceDrain.A^{fail}
\vee
TerminalSourceReverseHolder_{B_{ASAC}}.A^{gain}
\vee
PressureEigenframeSustain.A.
```

This is not installed.  It is the next smaller target because it coordinates
the three remaining ways to turn the terminal native source atom into paid
source-drain, temporal residence / reverse Holder gain, or pressure/eigenframe
sustain.

If that dichotomy is proved and the three exits are discharged, the chain is:

```math
BASACAtomEscapeDichotomy.A
\Longrightarrow
SourceWeightedTerminalAngularDepletion.A
\Longrightarrow
PositiveRemainderDepletion.A
\Longrightarrow
ReserveCreationCharge.A
\Longrightarrow
ScaleCriticalTreeCarleson.A
\Longrightarrow
SourcePulseExclusion.A.
```

## Verdict

`ReserveCreationCharge.A` is not installed.  The current proof search has
reduced the charge side to the terminal atom-escape dichotomy:

```math
\boxed{
BASACAtomEscapeDichotomy.A.
}
```

The rigid side remains the parallel alternative:

```math
ZenoBoundedClass_{\mathcal B}.A
+
ZenoResidueLiouville_{\mathcal B}.A.
```

The PDFs remain non-final until this source-pulse branch is proved and then
written into the rendered papers as a normal reader-facing proof.

## Next Loop Target

Attack `BASACAtomEscapeDichotomy.A` directly.  The proof method is an extremal
terminal-atom / adjoint-pairing split:

1. select the minimal terminal source atom and normalized backward transported
   adjoint packet;
2. retain the positive source-weighted carrier through the identity;
3. prove that failure of incoming flux and failure of temporal residence force
   pressure/eigenframe sustain rather than disappearing into signed-current
   bookkeeping.

Any route that converts the one-sided positive carrier into signed cancellation
or active-square reserve has returned to the same wall and does not count as a
proof.
