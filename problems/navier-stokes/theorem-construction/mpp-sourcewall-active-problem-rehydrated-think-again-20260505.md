# MPP Source-Wall Active Problem: Rehydrated Think-Again Note

## Status

Rehydration after the averaged endpoint / `Jump_avg` branch and the
CM witness-face bridge-license repair.

The old active targets are no longer the live obstruction:

```math
Jump_{avg}\Rightarrow\bot
```

is downstream of the source-wall supplier, and

```math
CM.CrossFaceSynthesis.A
```

is installed as the aggregate bridge-license theorem for synchronized
Pack/Part/Field face certificates on the same `Q,N,r`, same-fluid carrier, and
still-live window.  It does not discharge the source wall.

The live source-wall root remains:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```

## Direct Current Route After Rethink

The preferred direct route preserves signed Navier--Stokes source exchange as
long as possible:

```math
TerminalSkewLocalizationLedger.A
+ParabolicNoFreeSink.A
+ParabolicCycleCoercivity.A
+FirstCurrentPruning.A
\Longrightarrow
CycleHeatAction.A
\Longrightarrow
ScaleCriticalTreeCarleson.A.
```

The finite graph/flow part is not the remaining wall.  Existing notes already
show:

```math
CurrentRectification.A
+FirstCurrentPruning.A
+ParabolicDwell.A
+HeatEdgeBudget.A
\Longrightarrow
CycleHeatAction.A
```

as an abstract conditional reduction.  The unresolved content is PDE-native:
rectification without total-variation loss, terminal signed saturation, and
heat-scale resistance of legal source-current edges.

## Why `ParabolicEdgeResistance.A` Is Not A Lower Proof Target

The direct LP/Cauchy--Young estimate gives only:

```math
|J_e(t)|
\le C_{\mathrm{PER}}
\mathfrak a_e(t)D_e(t)+Loss_e(t),
```

and therefore

```math
\int |J_e|
\le
\varepsilon\int D_e
+
C_\varepsilon
\int \mathfrak a_e(t)^2D_e(t)\,dt
+
\int Loss_e.
```

After summing over the terminal packet graph, the remaining term is:

```math
\int_I
\sum_{k>N}
2^k
\left(\sum_{\ell>k+4}D_\ell(t)\right)^2
dt.
```

That is exactly the square reserve in `ScaleCriticalTreeCarleson.A`.

Hence:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\Longrightarrow
ParabolicEdgeResistance.Aggregate.A
}
```

is valid and useful downstream, but the reverse proof is circular unless a new
independent theorem controls the scale-critical flux amplitude:

```math
\boxed{
CriticalFluxAmplitudeSmall.A
}
```

or an equivalent positive active Carleson reserve / strain-decorrelation input.

## Exact Native Primitive

The current direct branch is best compressed to one primitive:

```math
\boxed{
NativePositiveSourceControl.A
}
```

Statement:

For every selected bounded-overlap same-fluid terminal packet family
`\mathcal F_N`, the localized active source carrier satisfies

```math
\int_I
\sum_{P\in\mathcal F_N}
\left(\mathfrak S^{active}_{N,loc}(P,t)\right)_+
dt
\le
o_N(1)+Loss_{legal}.
```

Equivalent current-language faces:

```math
TerminalSignedSaturation.A,
\qquad
SignedPositiveBalance.A,
\qquad
WeightedLiftedDefectCarleson.A,
\qquad
CriticalFluxAmplitudeSmall.A.
```

Taking absolute values returns to `ScaleCriticalTreeCarleson.A`.  Passing to
compactness returns to the Zeno route:

```math
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
```

Thus these are not independent lower burdens; they are different legal
presentations of the same native positive source-control wall.

## What Is Solved

The following routing facts are now closed:

```math
CM.CrossFaceSynthesis.A
\quad\text{is installed.}
```

```math
ParabolicEdgeResistance.A
\quad\text{is not a lower theorem below }ScaleCriticalTreeCarleson.A.
```

```math
ParabolicNoFreeSink.A
\equiv_{\mathrm{route}}
TerminalSignedSaturation.A
\equiv_{\mathrm{route}}
SignedPositiveBalance.A.
```

```math
TerminalSkewLocalizationLedger.A
```

requires a weighted/lifted signed-balance theorem; saturated-hull bookkeeping
alone does not install it.

## Active Problem Reoriented Through CM

The source wall must not be read as the primitive theorem direction

```math
OriginalSmoothData\Longrightarrow NativePositiveSourceControl.A.
```

That formulation is only a possible sufficient positive-continuation theorem.
The CM proof program uses it lawfully only after the witness-face split has
selected the `Field` row.

The active Field-face problem is therefore:

```math
\boxed{
\neg Field_{N,r,Q}
\Longrightarrow
\mathrm{VisibleJump}_{Field}
\ \vee\
\neg NativePositiveSourceControl.A
\ \vee\
ZenoResidue_B
}
```

after Pack, Part, receiver/readout, no-visible-jump, and legal-loss alternatives
have been separately synchronized on the same CM witness carrier.  If
`\mathrm{VisibleJump}_{Field}` occurs, the branch belongs to the endpoint
matrix rather than the source-wall estimate.

Equivalently, the two Field-face closures are:

```math
\boxed{
ScaleCriticalTreeCarleson.A
}
```

or

```math
\boxed{
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```

No endpoint exclusion, `Jump_avg`, `Field_avg`, or installed
`CM.CrossFaceSynthesis.A` can be spent to prove this supplier wall.  Those
objects are downstream consumers or bridge-license context.  The route
orientation is recorded in
`mpp-cm-field-face-sourcewall-reorientation-note-20260505.md`.
