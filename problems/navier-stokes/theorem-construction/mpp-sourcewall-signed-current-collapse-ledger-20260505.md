# MPP Source-Wall Signed-Current Collapse Ledger

## Status

Consolidation ledger for the parabolic signed-current route after the direct
attempts on its subtargets.

Verdict:

```math
\boxed{
\text{the signed-current route is a sharper presentation of the direct source
wall, but it is not a lower installed closure.}
}
```

The only installed signed input is the bare unweighted dyadic skew algebra:

```math
BareDyadicSkewCurrent.0.
```

Everything needed to promote that algebra to the selected terminal same-fluid
positive source ledger returns to:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```

## Attempted Targets

### `LocalizedSkewCurrent.A`

Audit result:

```math
\boxed{
LocalizedSkewCurrent.A\text{ is not theorem-grade installed.}
}
```

The missing ledger is:

```math
TerminalSkewLocalizationLedger.A
\quad\text{or equivalently}\quad
TerminalSignedSaturation.A.
```

Bare dyadic skewness acts before one-sided weights, lifted/cutoff
commutatorization, terminal same-fluid selection, and positive-part
extraction.

### `TerminalSkewLocalizationLedger.A`

Direct attempt result:

```math
\boxed{
TerminalSkewLocalizationLedger.A
\text{ is not installed.}
}
```

Strongest valid reduction:

```math
TerminalSignedSaturation.A
\Longrightarrow
TerminalSkewLocalizationLedger.A
\Longrightarrow
LocalizedSkewCurrent.A.
```

The saturated-hull repair closes only finite unweighted bookkeeping.  On the
live weighted/lifted terminal surface it still requires weighted/lifted defect
control and signed saturation.

### `CycleHeatAction.A`

Direct attempts result:

```math
\boxed{
CycleHeatAction.A
\text{ is not installed from the current signed-current ledger.}
}
```

The first direct attempt isolates closed-cycle coercivity:

```math
ParabolicCycleCoercivity.A
\quad\text{or}\quad
TerminalParabolicSignedSaturation.A.
```

The no-free-sink attempt shows an even sharper obstruction: a positive
terminal divergence node can be fed by an internal donor sink before any heat
edge is crossed.  Therefore the corrected route needs:

```math
ParabolicNoFreeSink.A
\quad\text{or}\quad
TerminalSignedSaturation.A.
```

### `FirstCurrentPruning.A`

Audit result:

```math
\boxed{
\text{the strong pruning inequality is not installed.}
}
```

First-current selection only removes earlier positive arrivals of the same
selected class.  It does not charge a same-window negative donor sink feeding a
terminal receiver.  The missing input is again:

```math
ParabolicNoFreeSink.A
\equiv_{\mathrm{route}}
TerminalSignedSaturation.A.
```

### `ParabolicCycleCoercivity.A`

Direct attempt result:

```math
\boxed{
ParabolicCycleCoercivity.A
\text{ is not installed.}
}
```

Strongest reduction:

```math
TerminalParabolicSignedSaturation.A
+ParabolicEdgeResistance.A
\Longrightarrow
ParabolicCycleCoercivity.A.
```

The donor-sink obstruction is the signed-current version of the same terminal
positive source wall.

### `ParabolicEdgeResistance.A`

Direct attempt result:

```math
\boxed{
ParabolicEdgeResistance.A
\text{ is not installed.}
}
```

The legal edge flux estimate leaves a scale-critical flux-amplitude remainder:

```math
\sum_e\int \mathfrak a_e(t)^2D_e(t)\,dt.
```

After packet summation this is the same active-square reserve:

```math
ScaleCriticalTreeCarleson.A.
```

Thus:

```math
ScaleCriticalTreeCarleson.A
\Longrightarrow
ParabolicEdgeResistance.A
```

is useful as consistency, but circular as a proof of the source wall.

### `TerminalSignedSaturation.A`

Direct attempt result:

```math
\boxed{
TerminalSignedSaturation.A
\text{ is not installed.}
}
```

It splits into:

```math
WeightedLiftedDefectCarleson.A
+OffFamilyCounteredgeCharge.A
+ParabolicNoFreeSink.A.
```

None of these is lower than the source wall.

### `OffFamilyCounteredgeCharge.A`

Direct attempt result:

```math
\boxed{
OffFamilyCounteredgeCharge.A
\text{ is not installed.}
}
```

The installed subcharge is only geometric spill.  The hard case is an
unselected same-fluid signed partner outside the selected terminal ledger or
time window with no cutoff, collar, pressure, residual, heat, boundary, or
earlier-arrival flag.

Strongest reduction:

```math
TerminalSignedSaturation.A
\Longrightarrow
OffFamilyCounteredgeCharge.A.
```

### `WeightedLiftedDefectCarleson.A`

Direct attempt result:

```math
\boxed{
WeightedLiftedDefectCarleson.A
\text{ is not installed.}
}
```

The exact obstruction is the positive terminal active strain-production
carrier:

```math
|w_j|^2
\left[
e_j\cdot S^{loc}_{<j}e_j
\right]_+.
```

Current signed balance, bare skew exchange, tangent-weight, pressure/cutoff,
and legal-loss ledgers do not charge this carrier.

Strongest reduction:

```math
PositiveStrainDecorrelation.A
\ \vee\
PositiveActiveCarlesonReserve.A
\ \vee\
\bigl(
MSC.Localize^{src}
+ZenoBoundedClass_B.A
+ZenoResidueLiouville_B.A
\bigr)
\Longrightarrow
WeightedLiftedDefectCarleson.A.
```

In route terms this returns to:

```math
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
```

## Collapse Statement

The parabolic signed-current branch factors honestly as:

```math
\boxed{
\begin{aligned}
&BareDyadicSkewCurrent.0\\
&\quad+
TerminalSignedSaturation.A\\
&\quad+
ParabolicEdgeResistance.A\\
&\Longrightarrow
ParabolicSourceCurrent.A
\Longrightarrow
ScaleCriticalTreeCarleson.A.
\end{aligned}
}
```

But:

```math
\boxed{
TerminalSignedSaturation.A
\not\Leftarrow_{\mathrm{installed}}
OriginalSmoothData,
\qquad
ParabolicEdgeResistance.A
\not\Leftarrow_{\mathrm{installed}}
OriginalSmoothData.
}
```

and the direct attempts show:

```math
TerminalSignedSaturation.A
\rightsquigarrow
SignedPositiveBalance.A
\rightsquigarrow
\text{native positive source-control wall},
```

while:

```math
ParabolicEdgeResistance.A
\rightsquigarrow
ScaleCriticalTreeCarleson.A.
```

Therefore the signed-current route should not be promoted as a solved lower
route.  It should be retained as the most structural direct attack, but the
live source-wall burden remains:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```

## Closed Loop

The following names are closed as independent lower targets under current
installed inputs:

```math
LocalizedSkewCurrent.A,\quad
TerminalSkewLocalizationLedger.A,\quad
CycleHeatAction.A,\quad
FirstCurrentPruning.A,
```

```math
ParabolicCycleCoercivity.A,\quad
ParabolicNoFreeSink.A,\quad
OffFamilyCounteredgeCharge.A,\quad
WeightedLiftedDefectCarleson.A.
```

They may be cited as conditional route pieces only.  A successful next theorem
must add genuinely new content to one of:

```math
\boxed{
ScaleCriticalTreeCarleson.A
\quad\text{or}\quad
ZenoBoundedClass_B.A+ZenoResidueLiouville_B.A.
}
```
