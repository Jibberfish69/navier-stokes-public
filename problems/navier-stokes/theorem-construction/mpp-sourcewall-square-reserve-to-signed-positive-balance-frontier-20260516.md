# MPP Source-Wall Square Reserve To Signed Positive Balance Frontier

## Status

Continuation note after the retained native-residue bridge.

The previous bridge proved the CM-facing routing

```math
\mu_{\ast}^{sing}>0
\Longrightarrow
\neg_{sel}ScaleCriticalTreeCarleson.A
```

on the retained source family covered by the localized pre-Cauchy source decomposition and `ParentSquareEmbed.A`. The installed Carleson diagnostic then sends the retained branch to `Field` under `Pack_Q+Part_{N,Q}`.

This note continues the direct positive source-control branch and identifies the exact next theorem primitive.

## Direct square-reserve branch

The direct remaining estimate is

```math
ScaleCriticalTreeCarleson.A:
\quad
\int_I\sum_{k>N}2^k\left(\sum_{\ell>k+4}D_\ell(t)\right)^2dt
\le o_N(1)+Loss_{legal}.
```

Write

```math
T_k(t):=\sum_{\ell>k+4}D_\ell(t),
\qquad
S_N(t):=\sum_{k>N}2^kT_k(t)^2,
\qquad
\mathcal R_N(I):=\int_I S_N(t)dt.
```

`ParentSquareEmbed.A` is static. It embeds the diffuse parent cloud into `\mathcal R_N`. It supplies no dynamic evolution law for `\mathcal R_N`.

The direct dynamic target was

```math
SquareReserveEvolution.A:
\quad
\mathcal R_N(W)
\le
Charge(W)+(1-\delta)\mathcal R_N(Past(W))+Loss_{legal}+o_N(1).
```

The scalar terminal pulse test blocks derivation from the first-moment tail ledger. One can keep

```math
\int_W\sum_{\ell>N}D_\ell(t)dt\to0
```

while `\mathcal R_N(W)` stays bounded below by a fixed `c_0>0` on a short heat window. Thus the direct square-reserve evolution route needs a square-amplitude charge law.

## Replacement target

The replacement theorem is

```math
SquareChargeEvolution.A:
\quad
{d\over dt}\Phi_N(t)+cS_N(t)
\le Ch_N(t)+Loss_N(t),
```

with

```math
\int Ch_N=o_N(1),
\qquad
\int Loss_N\le Loss_{legal}+o_N(1),
```

and legal boundary control for `\Phi_N`.

The installed conditional pass says

```math
DonorTailAmplitudeGain.A\Longrightarrow SquareChargeEvolution.A.
```

Here

```math
DonorTailAmplitudeGain.A:
\quad
S_N(t)\le \varepsilon\nu D_N(t)+r_N(t),
\qquad
\int r_N=o_N(1)+Loss_{legal}.
```

## Donor-tail amplitude route

The shell-flux attempt reduces `DonorTailAmplitudeGain.A` to control of the weighted lifted skew defect:

```math
WeightedLiftedSkewDefectLegal.A
\Longrightarrow
DonorTailAmplitudeGain.A.
```

The hard principal pieces are terminal weight mismatch and active strain alignment:

```math
(W_P-W_{P'})J(P'\to P),
```

and

```math
2^{2j}\langle S_k(x_P,t)e_P,e_P\rangle |c_P(t)|^2.
```

Bare skew exchange cancels only on the closed unweighted graph. One-sided terminal weights, lifted localization, terminal selection, and positive-part extraction leave the weighted positive residue.

## Zeno square-pulse branch

Failure of the square charge law extracts a terminal square-dominant pulse forest with

```math
\mathcal R_{N_m}(I_m)\ge\eta,
\qquad
\int_{I_m}\sum_{\ell>N_m}D_\ell(t)dt\to0.
```

The compactness side supplies an ancient local suitable source-residue object with inherited nonzero residue. The rigid residue vanishing theorem is available for the cancellation class `\mathcal B_{sq}`.

The open class-production theorem is

```math
ZenoSquareCancellationClassProduction.A:
\quad
\text{every square-dominant same-fluid Zeno pulse limit belongs to }\mathcal B_{sq}.
```

The direct attempt reduces this to the same two source-current gates:

```math
TerminalWeightCoherence.A
\quad\text{and}\quad
ActiveStrainAlignmentCost.A.
```

## Signed-current formulation

The signed formulation packages the same primitive as

```math
TerminalSignedSaturation.A:
\quad
\int(\mathfrak S_{N,loc}^{active})_+
\le
\int(\mathfrak S_{N,loc}^{active})_-+o_N(1)+Loss_{legal}.
```

A sufficient theorem is

```math
TerminalWeightRigidity.A
+ActiveStrainAlignmentCost.A
+ParabolicNoFreeSink.A
\Longrightarrow
TerminalSignedSaturation.A
\Longrightarrow
WeightedLiftedSkewDefectLegal.A.
```

This is the shared primitive across the three presentations.

## Frontier Theorem `SignedPositiveBalance.A`

The exact next target is the following source-wall primitive:

```math
\boxed{
SignedPositiveBalance.A:
\quad
\text{on every selected retained terminal source ledger, every positive weighted lifted source edge is either paired with a same-ledger negative edge or paid by legal terminal charge.}
}
```

A proof may enter through any one of these equivalent sufficient forms:

```math
TerminalWeightRigidity.A,
```

```math
ActiveStrainAlignmentCost.A,
```

```math
ParabolicNoFreeSink.A,
```

or a combined signed-current theorem proving `TerminalSignedSaturation.A` directly.

## Current Verdict

The continuation reaches a hard source-wall primitive. The retained native residue already lands in `Field` through the installed Carleson diagnostic. The remaining positive source-control theorem is equivalent, at current route resolution, to proving signed positive balance on the selected weighted terminal ledger.

Thus the next concrete theorem-building step is `SignedPositiveBalance.A`, with the three sufficient entrances above.
