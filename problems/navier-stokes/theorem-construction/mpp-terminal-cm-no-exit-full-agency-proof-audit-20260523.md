# TerminalCMNoExit.A full-agency proof audit

## Authority Cap - Positive No-Exit Attempt Only

This file is retained as a positive no-exit / packet-survival proof attempt. Current CM proof-status authority is `mpp-cm-proof-completion-surface-field-audit-20260525.md` plus `mpp-completed-proof-surface-challenge-20260525.md`. The open Part/Field no-exit language here is not an active CM blocker. Reopening requires a proved legal in-class nonsmooth third branch or a proved failure of Clay terminal CM entry/exhaustion/embedding.

## Target

`TerminalCMNoExit.A / NoGenuineCMExit.A` asks for original smooth Navier-Stokes data to leave at least one continuation-complete Part/Field packet on every alleged finite terminal tail.

The governing proof program is CM contrapositive class exit. `TerminalSourceResidueCMExit.A` already sorts raw terminal source residue as Part/Field witness; it is not a forward anti-atom deletion burden.

## Method pass

- Proof program: CM contrapositive class exit.
- Target object: existential survival of at least one continuation-complete Part/Field packet.
- Logical skeleton tested: contradiction by first loss of all lawful continuation packets, then Part/Field case split.
- Pivot methods tested: minimal first-loss, compactness/Zeno extraction, energy-budget invariant, and supplier-chain pressure test.
- Result: the case split localizes the obstruction, but installed inputs do not exclude every face from original smooth data.

## Installed reductions

The following inputs are accepted for the CM-facing reduction:

1. `GenuineCMExit.Equiv.A`: finite terminal non-continuation is equivalent to loss of every continuation-complete Part/Field packet.
2. `CanonicalTerminalPacketCapture.A` and `AnyFiniteFailureWitnessCMExit.A`: terminal failure witnesses are captured by the Field-certification packet tree.
3. `TerminalSourceResidueCMExit.A`: raw terminal source residue lands in Part/Field, not in a fourth residue face.
4. `TerminalCMNoExit.FaceReduction.A`: reduces genuine no-exit to the Field-certification face tree, with Part/Field first, then Part, then licensed Field if Pack and Part survive.

These reductions do not prove `TerminalCMNoExit.A`. They identify the exact places where a proof must prevent total packet loss.

## Face audit

### Pack

`TerminalPackStrainIntegrabilityNoExit.A` remains open. The Pack no-exit load is:

```text
OriginalSmoothData => OriginalSmoothDataScaleCriticalTreeCarleson.A
```

The current supplier chain is:

```text
OriginalSmoothDataScaleCriticalTreeCarleson.A
=> PositiveRemainderDepletion.A
=> SourceWeightedTerminalAngularDepletion.A
=> BASACTerminalSourceAntiConcentration.A
=> UniformTemporalSourceIntegrability_p_BASAC.A / TerminalSourceReverseHolder_{B_ASAC}.A
```

That chain is supplier-quarantined. It may support positive no-pulse/PCTP production, but it is not installed as Pack no-exit from original smooth data.

### Part

`TerminalPartClosednessInputNoExit.A` remains open. What is installed is only the conditional closedness theorem:

```text
Field_{N,r,Q} + A_{N+1,Q} in L^\infty + preterminal Part
=> terminal Part
```

The missing Part primitive is a noncircular terminal tower/readout bound on the retained same-fluid carrier:

```text
Field-window evidence is present + preterminal Part
=> uniform A_{N_s+1,Q} and K_k pressure-viscosity/source readout through T_*
```

This cannot import `Field`, `OFP.A`, `End_NS`, `READ.COVER`, `DTC.Read`, `Field.Read`, `Member`, or endpoint closure upstream.

### Field

`TerminalLicensedFieldCoherenceNoExit.A` remains open. The installed Field notes classify receiver/coherence escape as a Field-Part/Field failure once Pack and Part are retained. They do not produce Field coherence from original smooth data.

The missing Field primitive is the pre-endpoint same-family DTC/tower package through depth `N+3`, equivalently a noncircular `PreEnd.ACTKX_Q` with same-cover pressure readout, without using endpoint Field/readout closure upstream.

## Failed closure attempt

The direct proof attempt closes only the reduction:

```text
finite terminal non-continuation
<=> genuine loss of every continuation-complete packet
=> first failed Part/Field face
```

It does not close the positive theorem:

```text
OriginalSmoothData => exists one surviving continuation-complete Part/Field packet.
```

The obstruction is mathematical, not clerical: installed energy and compactness inputs allow terminal witness extraction and face sorting, but they do not provide the scale-critical Pack Carleson bound or the retained tower/readout package needed to prevent total packet loss.

## Readback

- `TerminalCMNoExit.A / NoGenuineCMExit.A` remains the exact Clay-facing CM target.
- `TerminalCMNoExit.FaceReduction.A` is a conditional face-reduction support theorem, not closure.
- `ScaleCriticalTreeCarleson.A` remains the Field-window support CM-facing primitive.
- `NativeSourceAC.A`, `HeightFluxControl.A`, and `BASACTerminalSourceAntiConcentration.A` remain positive supplier branches unless a separate theorem promotes them through Part/Field.
- Euler-mirror, readout, endpoint, and public-export material remains quarantined support for this target.
