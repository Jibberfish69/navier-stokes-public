# MPP TerminalCollarStrainAntiAtom_BASAC.A Reentry

Date: 2026-06-09

Status: active-completion proof attempt. This note does not close public Clay
finality.

## Target

The material-collar Pack-survival attempt reduced the first-exit Pack wall to:

```text
TerminalCollarStrainAntiAtom_{B_ASAC}.A.
```

Concrete form: the produced `B_ASAC` terminal source-residue class must not
carry a first Pack-failure collar with infinite terminal common-collar
stretching:

```text
int_{I_*} Lambda_sharp(t) dt = infinity.
```

If this theorem were proved, the installed pack-side forward-preservation
theorem would reinstall `Pack_Q` on the terminal material family and feed
`OriginalSmoothDataPackSurvival.A`.

## Normal form under test

The surviving branch has:

```text
same-fluid terminal carrier,
zero ASAC defect,
no paid legal/projected/cutoff/boundary exit,
no finite or non-Zeno donor exit,
no fixed earlier selected source slice,
finite local L^1_s source mass,
terminal Zeno source support,
positive terminal native source residue.
```

The collar-strain version of the obstruction adds:

```text
selected high-shell packets remain aligned with an expanding strain direction
on shrinking terminal heat-scale windows.
```

This is sharper than a bare time-face source atom. It is a terminal
strain-alignment atom.

## Case test

### 1. Signed strain cancellation

The signed route would prove:

```text
PositiveStrainDecorrelationOnTerminalPackets.A
```

or:

```text
StrainSignCancellationForWeightedLiftedRemainder.A.
```

The existing weighted-lifted-remainder and active-eigendirection attempts show
that incompressibility only gives trace-free strain. It does not forbid a
selected terminal high-shell packet from aligning with an expanding eigenvector.
The global dyadic cancellation occurs before one-sided weights, cutoffs,
terminal selection, lifted remainders, and positive-part extraction. It does
not pay the selected terminal collar-strain atom.

So the signed branch does not prove the anti-atom from installed inputs.

### 2. Positive active-square payment

The positive route would prove:

```text
ActiveSquareResidualTail.A
```

or:

```text
SourceBalancedActivePulseExclusion.A.
```

The current active-square attempts show the same obstruction: finite
first-moment dissipation does not control square density on shrinking terminal
active windows. A heat-scale pulse can have bounded local `L^1_s` source mass
while keeping order-one square reserve on the selected packet.

So positive active-square payment does not prove the collar-strain anti-atom
from installed inputs.

### 3. Pressure / Leray tether

The pressure route would prove:

```text
PressureLerayCollarStrainTether.A
```

or:

```text
SelectedPressurePositivePartComparison.A.
```

The installed pressure/Leray material is spatial and signed. Riesz transforms,
Leray projection, and pressure Hessian identities are not positivity-preserving
after the selected positive-part extraction. They do not force the native
positive strain carrier to be legal loss or to spread in time.

So pressure does not prove the anti-atom from installed inputs.

### 4. Transported flux / no-incoming

The transported-cylinder route separates boundary inflow from terminal
time-face production. Existing no-incoming attempts show that lateral
transported flux may vanish while an interior terminal source layer remains:

```text
g_m(s)=m 1_{(-1/m,0]}(s).
```

That model can still carry terminal strain alignment unless a separate
terminal time-thickness, angular decorrelation, or strain-alignment Liouville
theorem is proved.

So no-incoming geometry does not prove the anti-atom from installed inputs.

## Result

`TerminalCollarStrainAntiAtom_{B_ASAC}.A` is not proved from the installed
inputs.

The sharpened surviving obstruction is:

```text
TerminalStrainAlignmentAtom_{B_ASAC}.A.
```

It is the produced `B_ASAC` terminal source-residue class together with
positive terminal alignment of selected high-shell packets against expanding
collar strain on shrinking terminal windows.

The next non-alias theorem is:

```text
TerminalStrainAlignmentExclusion_{B_ASAC}.A
```

or an equivalent theorem producing one of these payments:

```text
terminal angular decorrelation,
source-weighted positive adjoint escape,
pressure/Leray positive-part domination,
or a rigid B_ASAC strain-alignment Liouville theorem.
```

Without one of those payments, `OriginalSmoothDataPackSurvival.A` remains open,
`NoGenuineExitFromSmoothData.A` remains open, and both PDFs remain nonfinal.

## New live criticism

`NS-LIVE-20260609-079`: the material-collar child theorem has now been tested
against the installed signed, positive-square, pressure/Leray, and transported
no-incoming entrances. None excludes a terminal `B_ASAC` strain-alignment atom.
The loop must attack `TerminalStrainAlignmentExclusion_{B_ASAC}.A` or an
equivalent angular, adjoint, pressure-positive, or rigid-Liouville payment
before any CMI-final PDF claim is lawful.
