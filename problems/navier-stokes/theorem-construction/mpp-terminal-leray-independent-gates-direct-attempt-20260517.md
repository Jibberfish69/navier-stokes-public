# MPP Terminal Leray Independent Gates Direct Attempt

Date: 2026-05-17

## Status

Direct attempt on the independent Leray gate package after the ancestry-side alternatives returned to `B_ASAC^closed,min`.

Outcome: open from current inputs. The localized Leray identity supplies an algebraic pressure/convection recombination, while terminal one-sided packet selection still requires three independent gates.

## Target package

The target package is

```math
\boxed{
TerminalLerayOrientation_{ind}.A
+TerminalLerayCommutatorLegal_{ind}.A
+TerminalLeraySaturation_{ind}.A.
}
```

A successful package would turn the pure pressure-source singular residue into a same-fluid source-current edge or a paid legal residue, closing the pressure singular branch.

## Localized Leray identity

The finite-packet identity is

```math
\chi\nabla P_{Leray}(u\cdot\nabla u)
=
P_{Leray}(\chi\nabla(u\cdot\nabla u))
+[\chi,P_{Leray}]\nabla(u\cdot\nabla u).
```

This identity recombines pressure and convection before terminal selection. It gives the correct algebraic splitting for the pressure branch.

## Gate 1: terminal orientation

`TerminalLerayOrientation_ind.A` asks the projected local pair to determine a same-fluid signed source edge at the terminal packet weights.

The current identity is signed and spatial. After one-sided positive terminal selection, orientation information can be lost through cancellation and packet mixing. The installed source-current ledger carries this orientation only inside the existing signed no-free-sink route. Importing that ledger here would make the pressure fork circular.

Thus the orientation gate remains open.

## Gate 2: commutator legality

`TerminalLerayCommutatorLegal_ind.A` asks the terminal commutator term

```math
[\chi,P_{Leray}]\nabla(u\cdot\nabla u)
```

to be priced by legal/projected/cutoff pressure ledgers at the same one-sided terminal weights.

Existing legal ledgers price standard cutoff, projection, harmonic, collar, boundary, and off-family errors. The retained pressure singular branch is selected after those exits have been removed. The terminal weighted commutator at this positive selection level therefore needs a fresh legal theorem.

Thus the commutator legality gate remains open.

## Gate 3: terminal saturation

`TerminalLeraySaturation_ind.A` asks the recombined pressure/source partner to remain retained after terminal one-sided positive selection.

The obstruction is partner loss: the signed partner may cancel before selection, exit into legal ledgers, become an internal donor sink, or Zeno-refill into the same terminal atom. Paying these cases returns to the known signed source-current wall: partner retention, legal/collar exit, donor depletion, or time-face anti-atom.

Thus the saturation gate remains open.

## Conditional closure

The valid implication is:

```math
TerminalLerayOrientation_{ind}.A
+TerminalLerayCommutatorLegal_{ind}.A
+TerminalLeraySaturation_{ind}.A
\Longrightarrow
\text{pure pressure-source singular branch closes.}
```

The package would provide a noncircular pressure-to-source bridge. Current surfaces supply the algebraic Leray identity and lack the three terminal one-sided gate theorems.

## Verdict

The independent Leray package is a genuine future production mechanism. It is open at all three gates:

```math
TerminalLerayOrientation_{ind}.A,
\quad
TerminalLerayCommutatorLegal_{ind}.A,
\quad
TerminalLeraySaturation_{ind}.A.
```

The next pressure-side item is `TerminalPressurePureStrainLegal.A`.
