# MPP TerminalParaproductSignLocalization.A Proof And Coefficient Gap

Date: 2026-05-22

## Status

The sign-selection part of `TerminalParaproductSignLocalization.A` is closed
once the retained Leray-adjoint paraproduct is identified with the native
low-strain contraction on the same selected packet family.

The live gap is therefore sharper:

```math
TerminalLerayParaproductCoefficientIdentification.A.
```

This is still inside the CM contrapositive program. The output is witness
capture on the CM ledger, not a forward source-control theorem.

## Algebraic Sign Lemma

Assume the retained same-ledger paraproduct work has already been reduced to
the native contraction:

```math
G_N
:=
\sum_{P\in\mathcal F_N}
\int_{Q(P)}
\langle S_{<j_P}^{loc}w_{j_P},w_{j_P}\rangle
\,dxdt
\ge c_0.
```

Let

```math
E_N
:=
\{(x,t)\in \bigcup_{P\in\mathcal F_N}Q(P):
\langle S_{<j_P}^{loc}w_{j_P},w_{j_P}\rangle>0\}.
```

Then

```math
\nu_N^{src}(E_N)
=
\sum_{P\in\mathcal F_N}
\int_{Q(P)}
\left[
\langle S_{<j_P}^{loc}w_{j_P},w_{j_P}\rangle
\right]_+
\,dxdt
\ge
\sum_{P\in\mathcal F_N}
\int_{Q(P)}
\langle S_{<j_P}^{loc}w_{j_P},w_{j_P}\rangle
\,dxdt
\ge c_0.
```

Thus no separate sign theorem is needed after exact coefficient
identification. A retained positive lower bound for the native contraction
already contains the native source witness measure.

## Remaining Gap

The real theorem is:

```math
\boxed{
TerminalLerayParaproductCoefficientIdentification.A:
\quad
\text{same-ledger Leray-adjoint nonlinear source work}
\Longrightarrow
G_N\ge c
\vee
\neg_{sel}ScaleCriticalTreeCarleson.A
\vee
\text{paid Pack/Part/legal exit.}
}
```

In words: after `L3DuhamelResponseWorkLocalization.A`, the retained part of

```math
-\int (u\otimes u):\nabla P\Phi
```

must be the native low-strain/high-packet contraction on the same selected
family, unless the work is carried by one of the already meaningful CM exits.

## Proof Shape For The Coefficient Identification

The unprojected high-low identity is algebraic. For a divergence-free low
coefficient and a high packet `w_j`,

```math
-\int (w_j\otimes w_j):\nabla S_{<j}^{loc}u
=
\int \langle S_{<j}^{loc}w_j,w_j\rangle
```

up to the fixed sign convention for the source work. The antisymmetric part
of the low gradient drops out because `w_j\otimes w_j` is symmetric.

Therefore every failure of coefficient identification is a failure before this
algebraic point:

```text
Leray orientation:
  the projected adjoint packet does not determine the same native source edge;

terminal commutator legality:
  the localized Leray/cutoff commutator is not paid by the legal pressure,
  cutoff, collar, or projection ledgers;

terminal saturation:
  the signed partner needed for the native edge is lost under one-sided
  terminal selection;

high-high / donor-square refill:
  the work is carried by the selected scale-critical source-wall channel.
```

These are not new terminal targets. The first three are the L3-translator
version of the installed terminal Leray gate package, and the last is selected
Pack-side/source-wall failure.

## Reduction

The closed sign lemma gives:

```math
TerminalLerayParaproductCoefficientIdentification.A
\Longrightarrow
TerminalParaproductSignLocalization.A.
```

Combined with the May 22 direct attempt:

```math
TerminalLerayParaproductCoefficientIdentification.A
\Longrightarrow
ParaproductNativeSourceCapture.A.
```

The public `L^3` translator chain is now:

```math
L3DuhamelResponseWorkLocalization.A
+
TerminalLerayParaproductCoefficientIdentification.A
\Longrightarrow
DuhamelInverseNativeSourceLocalization.A
\Longrightarrow
CriticalL3ConcentrationToNativeSourceResidue.A.
```

## Boundary

This leaves the closed `B_ASAC^closed` terminal zero-thickness CM target closed.
The terminal zero-radius Zeno residue is Part-first when the same original-NS
pointwise participation record remains and true `not Pack_Q` only when that
record is absent. The live work is only the retained L3 translator's
Leray/coefficient identification on the same CM witness ledger.
