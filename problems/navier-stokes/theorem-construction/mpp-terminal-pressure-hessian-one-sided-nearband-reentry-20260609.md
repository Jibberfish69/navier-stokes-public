# MPP TerminalPressureHessianNoSustain.A / OneSidedNearBandMaterialStrainBV.A Reentry

Date: 2026-06-09

Status: active-completion proof attempt. This note does not close public Clay
finality.

## Target

The current native-face leaf is:

```math
TerminalPressureHessianNoSustain.A
```

or, in near-band material form,

```math
OneSidedNearBandMaterialStrainBV.A.
```

In the active loop this theorem is not a standalone pressure project. It is the
pressure/eigenframe subterm that could supply:

```text
UniformLedgerAC.A / NativeTrilinearDefectDomination.A
=> SourceTimeConcentrationCost.A
=> ExternalTerminalTimeThicknessMechanism.A
=> OriginalSmoothDataPackSurvival.A
=> NoGenuineExitFromSmoothData.A.
```

## Method pass

Proof program: CM contrapositive public-finality repair.

Object role: supplier theorem for the same-ledger native positive source cost.

Logical skeleton: terminal normal-form case split with a failed direct proof
converted into smaller theorem endpoints.

Mechanism tested: pressure partner graph, terminal positive selection,
residue-to-source carrier comparison, and Field-face diagnostic classification.

## Checked pressure normal form

The installed pressure-Hessian attempt and partner-graph normal form reduce the
desired estimate to:

```math
TerminalCZPressureCancellation.A
+ TerminalPressureCollarLegal.A
+ PressureDonorNoFreeSink.A
\Longrightarrow TerminalPressureHessianNoSustain.A.
```

The pressure Poisson law and Calderon-Zygmund bounds do not prove this estimate.
Absolute bounds return to active-square / source-Carleson strength. Trace and
incompressibility do not force the directional pressure sign. The local
trace-free strain tangent

```math
S=\operatorname{diag}(2,-1,-1),\qquad \Omega=0
```

keeps positive pressure-Hessian sustain at the algebraic pressure-law level
without a native vorticity source carrier.

## Terminal CZ cancellation test

Calderon-Zygmund pressure cancellation is real before terminal selection. The
selected terminal bill applies one-sided weights, near-band projection,
same-fluid packet selection, and positive-part extraction. A positive pressure
lobe can remain selected while its negative angular partner is unselected,
diffuse, below threshold, or outside the usable ledger.

The checked direct cancellation theorem is only conditional:

```math
TerminalPressurePartnerSaturation.A
+ CZPressurePairWeightDefectCharge.A
+ TerminalPressureCollarLegal.A
\Longrightarrow TerminalCZPressureCancellation.A.
```

Thus pressure cancellation does not by itself pay the one-sided near-band
material strain bill.

## Pressure partner saturation test

The saturation attempt reduces an interior missing negative pressure partner to:

```math
PressureLobeSourceTether.A.
```

That tether is not a consequence of pressure ellipticity. A pressure lobe is an
instantaneous Calderon-Zygmund influence, not a material same-fluid source edge.
The only finite-packet tether available from the checked notes recombines the
convective and pressure parts through the Leray projector, which returns to the
terminal skew/source-current wall:

```math
TerminalSkewLocalizationLedger.A
+ TerminalPressureCollarLegal.A
\Longrightarrow PressureLobeSourceTether.A.
```

Using that implication here would be circular for the independent
pressure/eigenframe route.

## Limit residue test

Failure of the pressure graph produces a positive pressure-sustain residue. To
feed the source-wall branch it must imply a native source residue:

```math
PressureSustainResidueToSourceResidue.A.
```

The checked direct limit attempt proves only that a positive pressure residue
comes from a quadratic-gradient pressure carrier. It does not prove absolute
continuity with respect to the native positive source-current carrier

```math
|w_j|^2[e_j\cdot S_{<j}^{loc}e_j]_+.
```

The missing theorem is:

```math
PressureSourceAC.A
```

or the finite-packet independent Leray version:

```math
LocalizedLeraySourceTether.A_{ind}.
```

The checked independent Leray attempt says those require either

```math
QuadraticDefectSourceDomination.A
```

or all three independent terminal Leray gates:

```math
TerminalLerayOrientation_{ind}.A
+ TerminalLerayCommutatorLegal_{ind}.A
+ TerminalLeraySaturation_{ind}.A.
```

None is installed.

## Pure pressure residue test

The pressure endpoint can also be treated as a pure pressure-sustain residue.
The checked legal/Liouville/endpoint notes give:

```math
PressureResidueFieldFaceDiagnostic.A
```

as a valid diagnostic landing, but not a witness theorem. Existing pressure
legal ledgers pay collar, harmonic, cutoff/projector, boundary, and off-family
pressure work. They do not classify an interior retained pure pressure-Hessian
sustain residue as legal loss. Pure-pressure Liouville and terminal pure-strain
legal classification remain open.

## Result

The active leaf is not discharged:

```text
TerminalPressureHessianNoSustain.A: open
OneSidedNearBandMaterialStrainBV.A: open
```

The proof attempt narrows the next reentry target. A real closure must prove
one of:

```text
QuadraticDefectSourceDomination.A,
TerminalLerayOrientation_ind.A + TerminalLerayCommutatorLegal_ind.A
  + TerminalLeraySaturation_ind.A,
PurePressureSustainResidueLiouville.A,
TerminalPressurePureStrainLegal.A.
```

The first endpoint is the sharpest next target because it would directly place
the pressure quadratic-gradient carrier on the native source-current carrier
needed by `PressureSourceAC.A`, then by `TerminalPressureHessianNoSustain.A`,
then by `SourceTimeConcentrationCost.A`.

## Loop consequence

The public Clay finality chain remains open:

```text
TerminalPressureHessianNoSustain.A: open
=> SourceTimeConcentrationCost.A: open
=> ExternalTerminalTimeThicknessMechanism.A: open
=> OriginalSmoothDataPackSurvival.A: open
=> NoGenuineExitFromSmoothData.A: open
=> pdfs_final: false.
```
