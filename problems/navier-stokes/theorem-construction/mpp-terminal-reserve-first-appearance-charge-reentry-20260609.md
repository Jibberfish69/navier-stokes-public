# TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A reentry

Date: 2026-06-09

## Target

The active supplier leaf is:

```text
TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A.
```

It must prove that a genuinely new terminal positive source-reserve birth is
inherited from `Past(W)`, injected into `Charge_N(W)`, or becomes a concrete
Pack/Part/Field witness-face failure.

## Checked reduction

The installed first-birth descent is valid bookkeeping. On a minimal terminal
window `W`, it removes inherited parent reserve, represented child residual,
finite donor trees, legal exits, cutoff/boundary/projection loss, and non-Zeno
refill. The surviving object is the retained positive-scale first-created
native source carrier.

The non-promotion audit for `ChargeLedgerInjection.A` is decisive:

```text
WeightedAdjRes.A prices ledger-visible residual.
```

It does not prove that every retained positive first-created source-reserve
birth is ledger-visible. The missing theorem is the same sign/depletion or
anti-concentration statement:

```text
PositiveRemainderDepletion.A
```

on the minimal retained positive-scale source carrier.

## Direct proof attempt

Assume an uncharged genuinely new terminal reserve birth after the first-birth
descent. The candidate contradiction would be:

```text
new positive reserve with no parent, no legal exit, and no charge ledger
=> zero-cost packet creation
```

That implication is the theorem to be proved, not a consequence of the current
inputs. The local energy ledger, finite source mass, same-fluid transport, and
weighted adjoint residual estimates still allow a selected terminal source
carrier whose mass concentrates in a vanishing final time layer.

The surviving branch is exactly the post-ASAC terminal Zeno branch:

```text
PositiveRemainderDepletion.A
<- SourceWeightedTerminalAngularDepletion.A
<- ZenoSourceResidueRigidity.A
<- TerminalTimeFaceAntiAtom.A / TerminalSourceReverseHolder_{B_ASAC}.A.
```

## Cycle diagnosis

The present proof search has a real cycle:

```text
TerminalReserveFirstAppearanceCharge.A
-> ChargeLedgerInjection.A
-> PositiveRemainderDepletion.A
-> SourceWeightedTerminalAngularDepletion.A
-> B_ASAC terminal source residue
-> BASACReverseHolderProduction.A
-> NewTerminalSourceResidenceMechanism.A
-> TerminalReserveFirstAppearanceCharge.A.
```

This is not a failure of bookkeeping. It is a proof-method failure: the current
charge, reserve, ASAC, and reverse-Holder labels are all asking for the same
missing independent fact about the terminal positive source atom.

## Judgment

`TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A` is not
proved from the current installed inputs. It reduces back to
`PositiveRemainderDepletion.A` and the produced `B_ASAC` terminal source atom.

The next loop target cannot be another equivalent reserve/charge label. It must
be an independent terminal-source anti-atom mechanism, such as:

- an equation-level trace regularity theorem stronger than finite source
  `L^1_t`;
- a pressure/Leray spread theorem proving that a positive source atom cannot
  remain supported on the terminal time face;
- a zero-ASAC equality-rigidity theorem that rules out a nonzero `B_ASAC`
  ancient residue;
- a backward/adjoint probe that forces terminal atom mass to appear on an
  earlier same-ledger source slice without assuming trace absolute continuity.

## New live criticism

`NS-LIVE-20260609-055`: the active supplier route has entered a closed proof
search cycle. `TerminalReserveFirstAppearanceCharge.A / ChargeLedgerInjection.A`
does not close the source-residence obstruction; it reduces to
`PositiveRemainderDepletion.A` and the same `B_ASAC` terminal source atom. The
loop must pivot to an independent terminal-source anti-atom theorem before any
PDF can honestly claim CMI finality.
