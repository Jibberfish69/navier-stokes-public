# MPP Terminal Signed Pressure Saturation B_ASAC Attempt

Date: 2026-06-09

Status: active-completion proof attempt. This note does not close public Clay
finality.

## Target

The dynamic no-freeze attempt reduced the first-Pack-survival branch to:

```text
TerminalSignedPressureSaturation_{B_ASAC}.A.
```

The theorem would say that a selected terminal positive source receiver in the
`B_ASAC` strain-aligned branch cannot be fed by an internal signed
donor/pressure partner on shrinking terminal windows after all legal, ASAC,
finite donor, cutoff, pressure, and participation exits have been removed.

## What Is Already Paid

From the signed-current and post-ASAC no-free-sink notes, the following pieces
are already paid at current authority:

```text
retained signed partner with ASAC pair-weight charge,
named legal exits,
projected/cutoff/boundary/collar losses,
finite non-Zeno donor trees by LocalDonorBalance.A + EntranceLeafDecay.A.
```

Thus the live object is not a generic graph-flow sink. The generic graph
obstruction has already been sharpened.

## Pressure Partner Branch

A selected pressure-Hessian sustain lobe has a Calderon-Zygmund opposite-sign
partner. If that partner is retained, cancellation is usable. If it exits
through a named pressure collar, it is legal.

The hard case is an interior missing pressure partner. Pressure Poisson gives
an elliptic influence:

```text
pressure lobe => quadratic-gradient pressure carrier.
```

It does not by itself give:

```text
native same-fluid source-current edge,
same-fluid donor ancestry,
or terminal positive source measure.
```

The missing bridge is:

```text
PressureSourceAC_{B_ASAC}.A
```

or finite-packet form:

```text
LocalizedLeraySourceTether_{B_ASAC,ind}.A.
```

Without that bridge, the pressure branch produces a pressure-sustain residue,
not the native source residue needed by the source-current/no-free-sink route.

## Native Donor Branch

If the partner is already a native same-fluid donor edge, the post-ASAC
no-free-sink theorem reduces the internal donor case to the Zeno branch:

```text
LocalDonorBalance.A
+ EntranceLeafDecay.A
+ ZenoSourceResidueRigidity.A
=> TerminalWeightedNoFreeSink.A.
```

The non-Zeno trees telescope and are paid. The remaining uncharged tree is a
terminal Zeno source-refill branch. Its non-circular closure is:

```text
ZenoSourceResidueRigidity_{B_ASAC}.A
```

or, at the source-time level:

```text
TemporalNonAtomicSource_{B_ASAC}.A.
```

This is exactly the terminal time-face residue atom seen in the earlier
`B_ASAC` anti-atom attempts.

## Result

`TerminalSignedPressureSaturation_{B_ASAC}.A` is not proved as one theorem from
the installed inputs.

The attempt splits it into two smaller unpaid atoms:

```text
1. PressureSourceAC_{B_ASAC}.A
   / LocalizedLeraySourceTether_{B_ASAC,ind}.A
   for interior missing pressure partners;

2. ZenoSourceResidueRigidity_{B_ASAC}.A
   / TemporalNonAtomicSource_{B_ASAC}.A
   for native internal donor sinks after non-Zeno trees are paid.
```

This is not a return to the generic no-free-sink label. The signed-current
branch has been reduced to a pressure-to-source bridge plus the already known
terminal Zeno source-residue atom.

The next non-alias theorem to try first is:

```text
PressureSourceAC_{B_ASAC}.A.
```

It is the only branch in this split that is not already the old terminal
source-time anti-atom. If it fails, the loop must record that the pressure
route also collapses back to the native Zeno source-residue rigidity target.

## New Live Criticism

`NS-LIVE-20260609-087`: `TerminalSignedPressureSaturation_{B_ASAC}.A` is not
installed. Retained signed partners, legal exits, ASAC pair-weight charge, and
finite non-Zeno donor trees are paid, but the pressure branch still needs
`PressureSourceAC_{B_ASAC}.A` / independent localized Leray source tether, and
the native donor branch still needs `ZenoSourceResidueRigidity_{B_ASAC}.A` /
`TemporalNonAtomicSource_{B_ASAC}.A`. The next object-level theorem is
`PressureSourceAC_{B_ASAC}.A`, because it is the remaining pressure-specific
bridge not already identical with the terminal time-face source atom.
