# MPP TerminalStrainAlignmentExclusion / Face-Bridge Reentry

Date: 2026-06-09

Status: active-completion proof attempt. This note does not close public Clay
finality.

## Target

The current first-exit Pack-survival branch names:

```text
TerminalStrainAlignmentExclusion_{B_ASAC}.A
```

as the next theorem after the material-collar and collar-strain attempts. The
object is a produced `B_ASAC` terminal source-residue class whose selected
high-shell packets stay positively aligned with an expanding collar strain
direction on shrinking terminal windows.

The governing program remains the CM contrapositive:

```text
Exit(Q):=not Member(Q)
```

through `Pack_Q`, `Part_{N,Q}`, and `Field_{N,r,Q}`. The alignment atom is not
a class member. It is either a candidate Pack/Field Part/Field failure, or a positive
no-exit theorem must exclude it from original smooth data.

## Installed Inputs

The installed `B_ASAC` terminal atom diagnostic says:

```text
B_ASAC terminal source atom
=> not Pack_Q
```

unless a positive-scale retained-carrier bridge is supplied. The installed
`B_ASAC^closed` participation classification says that a positive terminal atom
cannot remain a finite-CM residue with no incoming participation flux; it must
produce flux, CM-envelope blowout, or paid exit.

The newest attempted entrances do not prove bare exclusion:

```text
StrainSignCancellationForWeightedLiftedRemainder.A: not installed.
ActiveEigendirectionDecorrel.A: not installed.
ActiveSquareResidualTail.A: not installed.
PositiveSourceAdjointEscapeIdentity.A: not installed.
TerminalLerayOrientation/Commutator/Saturation for B_ASAC: not installed.
TerminalPressurePureStrainLegal.A: not installed.
```

These failures are not independent proof closures; they all fail at the same
positive one-sided terminal carrier after selection.

## Direct Exclusion Test

Bare `B_ASAC` data still allow the terminal layer model:

```text
g_m(s)=m 1_{(-1/m,0]}(s).
```

Add a fixed expanding strain direction and keep the selected packets aligned
with it. This model has finite local `L^1_s` source mass, no fixed earlier
selected source slice, terminal support at the time face, and no angular
turnover charge. The alignment does not contradict `B_ASAC`; it is exactly the
hard branch left after signed cancellation, pressure/Leray, adjoint, and
active-square routes are removed.

So the bare theorem

```text
TerminalStrainAlignmentExclusion_{B_ASAC}.A
```

is not proved from installed inputs.

## CM Face-Bridge Test

The object becomes useful only after it is typed by the witnesses.

Case 1: the terminal strain-aligned atom has zero heat-time support and no
positive-scale retained carrier. Then the installed `B_ASAC` terminal atom
diagnostic applies directly:

```text
zero terminal source support + no earlier selected slice
=> not Pack_Q.
```

This is a lawful CM class-exit landing. It is not a proof that the terminal
atom vanishes.

Case 2: the atom keeps a positive-scale retained material or field carrier.
Then the zero-radius Pack diagnostic cannot be spent by itself. The missing
bridge is a positive-scale face bridge:

```text
TerminalStrainAlignmentFaceBridge_{B_ASAC}.A:
  a first-exit terminal strain-aligned B_ASAC atom either
  (a) has zero terminal heat-time support / no retained carrier, hence not Pack_Q;
  or
  (b) has a retained positive carrier whose aligned source lower bound forces
      forall r>0 not Field_{N,r,Q} under retained Pack_Q and Part_{N,Q}.
```

This is smaller than bare exclusion. It does not ask the atom to disappear; it
asks the atom to stop being an untyped third branch.

## Result

The next correct theorem is not another alias of angular depletion or pressure
depletion. It is:

```text
TerminalStrainAlignmentFaceBridge_{B_ASAC}.A
```

or, if the public-finality route insists on no first Pack exit from original
smooth data, the stronger theorem:

```text
OriginalSmoothDataNoFirstPackExit_{B_ASAC-strain}.A.
```

Current installed inputs prove neither. The first theorem is the better next
object because it stays inside the CM Part/Field grammar and blocks the
bad substitution where a terminal strain-aligned atom is treated as a class
member or as a finished Clay proof.

## New Live Criticism

`NS-LIVE-20260609-080`: the loop must not chase bare
`TerminalStrainAlignmentExclusion_{B_ASAC}.A` as though `B_ASAC` plus alignment
already implies Liouville. The installed facts support only a zero-radius
Pack-Part/Field landing unless a positive-scale retained-carrier bridge is proved.
The next work unit is
`TerminalStrainAlignmentFaceBridge_{B_ASAC}.A`, with the stronger
`OriginalSmoothDataNoFirstPackExit_{B_ASAC-strain}.A` remaining open if public
Clay finality requires exclusion rather than CM Part/Field landing.
