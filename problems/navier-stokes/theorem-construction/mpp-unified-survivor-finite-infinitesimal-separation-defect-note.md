# MPP Unified Survivor Finite-Infinitesimal Separation Defect Note

## Status

Theorem-facing candidate object.

Role: identify the proposed common survivor object `\mathcal S_\ast` beneath the
survivor atlas as terminal finite--infinitesimal separation incoherence of the
same material flow.

This note does not prove that `\mathcal S_\ast` exists as a nonzero terminal
measure, nor that all branch readouts already factor through it. It defines the
candidate core and records the exact theorem burden needed to promote it.

## Primitive Material Geometry

The working object carries both the true finite material separation

```math
h(a,r,t):=\Phi(a+r,t)-\Phi(a,t)
\tag{USS.0}
```

and the infinitesimal deformation

```math
F(a,t):=D_a\Phi(a,t).
\tag{USS.1}
```

The affine separation defect is

```math
\mathfrak D_{h/F}(a,r,t)
:=
h(a,r,t)-F(a,t)r.
\tag{USS.2}
```

Using the finite-separation law in the ontic object package,

```math
h(a,r,t)=\int_0^1F(a+s r,t)r\,ds,
\tag{USS.3}
```

one has the exact identity

```math
\mathfrak D_{h/F}(a,r,t)
=
\int_0^1\bigl(F(a+s r,t)-F(a,t)\bigr)r\,ds.
\tag{USS.4}
```

So `\mathfrak D_{h/F}` is the first geometric place where finite scale,
infinitesimal deformation, scale memory, packing, selector direction, affine
quotient, and transported finite differences all meet.

## Scale-Window Defect Measure

The raw expression

```math
\frac{|\mathfrak D_{h/F}(a,r,t)|^2}{|r|^2}\,da\,\frac{dr}{|r|^3}
\tag{USS.5}
```

must not be used without a scale window. The theorem-facing object is the
truncated scale-window measure. For a label cutoff `\psi`, and scales
`0<\eta<\rho`, define

```math
d\mu_{t}^{h/F;\psi,\eta,\rho}(a,r)
:=
\mathbf 1_{\eta\le |r|\le\rho}\,
\psi(a)^2
\frac{|\mathfrak D_{h/F}(a,r,t)|^2}{|r|^2}
\,da\,\frac{dr}{|r|^3}.
\tag{USS.6}
```

Equivalently,

```math
\mu_{t}^{h/F;\psi,\eta,\rho}
\text{ measures terminal scale memory in }
h(a,r,t)-F(a,t)r
\text{ on the label packet }(\psi,\eta,\rho).
\tag{USS.7}
```

The truncation is part of the object. It prevents the atlas from hiding a scale
change in the notation. A passage `\eta\downarrow0`, `\rho\uparrow\rho_\ast`,
or `t\uparrow T_\ast` must be licensed by a scale-window theorem.

## Candidate Unified Survivor

A terminal finite--infinitesimal survivor is a nonzero weak-* terminal scale
class

```math
\mathcal S_\ast^{h/F}
\in
\left[
\operatorname*{w^\ast\!-\!lim}_{n\to\infty}
\mu_{t_n}^{h/F;\psi_n,\eta_n,\rho_n}
\right]_{\mathrm{surv}}
\tag{USS.8}
```

along a terminal schedule

```math
t_n\uparrow T_\ast,
\qquad
0<\eta_n<\rho_n,
\tag{USS.9}
```

with all packets generated by one same-fluid object

```math
(u,p,\Phi,F)
\tag{USS.10}
```

satisfying the incompressible pressure-viscosity law.

The intended unified obstruction is:

```math
\boxed{
\mathcal S_\ast
=
\text{terminal finite--infinitesimal separation incoherence of the same
material flow.}
}
\tag{USS.11}
```

In plain route language:

```math
\boxed{
\text{the flow keeps an infinitesimal deformation }F
\text{ while finite separations }h
\text{ retain incompatible terminal scale memory.}
}
\tag{USS.12}
```

## Enhanced Survivor Refinement

The scalar measure `\mathcal S_\ast^{h/F}` is not enough to carry all terminal
readouts. It remembers positive defect mass, but it forgets signed and
tensorial data. The proof-bearing replacement is conditional on the extraction
hypotheses below: the enhanced packet

```math
\boxed{
\mathbb S_\ast^{h/F}:=(J_\ast,M_\ast).
}
\tag{USS.12a}
```

For a licensed logarithmic schedule, set

```math
V_n(a,\omega,\theta)
:=
\frac{\mathfrak D_{h/F}(a,r_n(\theta)\omega,t_n)}{r_n(\theta)}.
\tag{USS.12b}
```

Then define vector and tensor Radon measures

```math
J_n:=V_n\,da\,d\omega\,d\theta,
\qquad
M_n:=V_n\otimes V_n\,da\,d\omega\,d\theta.
\tag{USS.12c}
```

The finite `L^2` defect budget gives bounded total variation for `M_n`, and on
the finite base packet also for `J_n` by Cauchy--Schwarz. Therefore, after
subsequence extraction,

```math
J_n\stackrel{*}{\rightharpoonup}J_\ast,
\qquad
M_n\stackrel{*}{\rightharpoonup}M_\ast.
\tag{USS.12d}
```

The old scalar survivor is the trace shadow:

```math
\mathcal S_\ast^{h/F}=\operatorname{tr}M_\ast.
\tag{USS.12e}
```

This refinement is the correct common source object for signed gap-kernel,
quadratic gap-flux, affine-moment, shell-stress, and selector readouts. Signed
or cancellation-sensitive readouts factor through `J_\ast`; positive quadratic
and stressified readouts factor through `M_\ast`.

## Relation To Existing Packets

### Pack Readout

Packing reads `\mathfrak D_{h/F}` as failure of the finite separation to remain
coherent with the infinitesimal deformation model:

```math
c_F|F(a,t)||r|\le |h(a,r,t)|\le C_F|F(a,t)||r|.
\tag{USS.13}
```

This is the geometric content already used by the relative packing ledgers and
the increment-stability bridge.

### Tower / Field Readout

The tower and one-field routes read the same defect through the comparison

```math
\delta_{h(a,r,t)}U_k
-
\delta_{F(a,t)r}U_k.
\tag{USS.14}
```

Increment stability turns this into a product of the geometric defect
`\mathfrak D_{h/F}` and a next-rung segment carrier. Thus terminal scale memory
in `\mathfrak D_{h/F}` is one direct source of cross-rung finite-difference
residue.

### Deformation / Affine Readout

By `(USS.4)`, the same object is exactly the finite-scale variation of `F`:

```math
\int_0^1(F(a+s r,t)-F(a,t))r\,ds.
\tag{USS.15}
```

The affine survivor quotient and affine-excess packets are therefore not
separate primitives at this level. They are affine-coordinate readouts of the
same finite-to-infinitesimal mismatch, provided their carrier and scale windows
are preserved.

### Selector Readout

When the pack-side gauge keeps `h` and `F r` nondegenerate, the defect controls
angular/projective drift:

```math
\frac{h(a,r,t)}{|h(a,r,t)|}
\quad\text{versus}\quad
\frac{F(a,t)r}{|F(a,t)r|}.
\tag{USS.16}
```

Thus selector/projector defects can be read as direction-only shadows of
`\mathfrak D_{h/F}`. This is only a readout unless a theorem proves the
necessary nondegeneracy and scale transfer.

### Endpoint Readout

The endpoint matrix reads nonzero terminal `h/F` scale memory as living on the
surviving pair:

```math
\mathcal S_\ast^{h/F}
\leadsto
(Pack,Blown)
\quad\text{or}\quad
(Field,Jump).
\tag{USS.17}
```

This note does not choose the endpoint cell. It says the same defect is a
candidate common source for the surviving pack-side and field-side readouts.

## Family A / Family B As Scale Cuts

The lower-prefix / cumulative-readout family is the lower scale cut of the same
candidate measure:

```math
A(\rho,t)
:=
\int_{|r|\le\rho}
\psi(a)^2
\frac{|\mathfrak D_{h/F}(a,r,t)|^2}{|r|^2}
\,da\,\frac{dr}{|r|^3}.
\tag{USS.18}
```

The gap-kernel / upper-tail family is the upper or far-gap weighted cut:

```math
B(\rho,t)
:=
\int_{|r|>\rho}
K_\rho(r)
\psi(a)^2
\frac{|\mathfrak D_{h/F}(a,r,t)|^2}{|r|^2}
\,da\,\frac{dr}{|r|^3}.
\tag{USS.19}
```

The two-family collapse theorem is reformulated as the following unpaid target:

```math
\boxed{
A\text{ and }B\text{ are two licensed scale cuts of the same terminal defect
measure }\mathcal S_\ast^{h/F}.
}
\tag{USS.20}
```

This is stronger and sharper than saying that Family A and Family B are
analogous.

## Theorem Target `USS.A` (Terminal Defect Measure)

There is a terminal scale schedule and a nonzero finite measure class
`\mathcal S_\ast^{h/F}` generated by `(USS.6)`--`(USS.10)`, with no change of
same-fluid carrier, label family, endpoint cell, or proof direction.

This theorem requires:

1. tightness or a replacement compactness statement for the scale-window
   measures;
2. nontriviality of the terminal defect class;
3. a licensed scale schedule;
4. preservation of the same `M_Q(u,p)` carrier.

## Theorem Target `USS.B` (Branch Projection)

Every serious route-native survivor readout factors as a legal projection of
`\mathcal S_\ast^{h/F}`:

```math
\mathcal S_\ast^{h/F}
\longrightarrow
\begin{cases}
\text{tail leakage},\\
\text{tower flux-debt},\\
\text{deformation commutator},\\
\text{selector defect},\\
\text{affine survivor quotient},\\
\text{pack / field endpoint failure}.
\end{cases}
\tag{USS.21}
```

Each arrow must be classified in the survivor atlas as an identity, readout, or
bridge edge. Any arrow that changes carrier, scale-limit object, endpoint cell,
or proof direction without a theorem is an illegal carrier change.

## Theorem Target `USS.C` (Two-Family Collapse)

The bridge obligations `(B1)`--`(B3)` are attempts to construct or compare the
common source object:

```math
A
\longleftarrow
\mathcal S_\ast^{h/F}
\longrightarrow
B.
\tag{USS.22}
```

More exactly:

1. `(B1)` must identify the signed gap-kernel route with an affine quotient
   readout of `\mathcal S_\ast^{h/F}`;
2. `(B2)` must identify the positive gap-flux carrier as a square shadow of
   the same defect measure;
3. `(B3)` must install the stressification map comparing shell strain
   barriers to affine quotient moments of the same defect.

If these are proved as legal atlas edges, then Family A and Family B are not
merely bridged by analogy. They are two readout cuts of one terminal survivor
object.

## Conditional Theorem Packet Audit

The proposed completed `h/F` survivor packet is audited in
[mpp-unified-survivor-conditional-theorem-packet-audit-note.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/mpp-unified-survivor-conditional-theorem-packet-audit-note.md).
The audit accepts the packet as a conditional theorem:

```math
USCP.A+USCP.B+USCP.C+ACT.KX+FCI.5f+\mathsf{End}_{NS}
\Longrightarrow
\text{no terminal same-fluid class exit},
\tag{USS.23}
```

but keeps `(USCP.S1)`, `(USCP.S2)`, branch projection, and `(B1)`--`(B3)` as
named theorem atoms rather than treating them as solved by the notation
`\mathcal S_\ast^{h/F}`.

The rigorous replacement packet inside that audit is `USCP.R`: it upgrades the
scalar survivor to `\mathbb S_\ast^{h/F}=(J_\ast,M_\ast)` and localizes the live
load to native pack norm restoration, native tower segment control, selector
transverse visibility, and same-carrier identification of the historical
Family A/B packets.
The downstream ten-cell completion ledger is `USCP.T`, which appends
`ACT.KX`, the source channel theorem `FPCR.C+FSCR.C+FCC.C1+FCC.C1a -> FCI.5f`, and the
terminal endpoint matrix `End_NS`.

## Boundary

This note does not prove:

1. weak-* compactness of `(USS.6)` along terminal schedules;
2. nonzero survival of the defect measure;
3. that every selector/projective readout is already a projection of
   `\mathcal S_\ast^{h/F}`;
4. that Family A and Family B already collapse;
5. that endpoint exclusion follows from naming the survivor.

It only installs the candidate unified obstruction and the exact promotion
burdens.
