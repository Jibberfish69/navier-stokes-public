# MPP WeightedCommutatorResidualTail.A Reentry

## Status

Active-completion proof attempt after
`mpp-moving-dyadic-energy-trace-tightness-reentry-20260609.md`.

This note does not close public Clay finality.

## Target

The moving-shell energy trace attempt reduced back to the source-control core:

```math
WeightedCommutatorResidualTail.A.
```

The intended effect is:

```math
\text{signed weighted commutator-defect control}
+
\text{residual-tail control}
\Longrightarrow
SOURCE.NO\text{-}PULSE.A.
```

That would forbid the nonlinear shell flux from supplying source-balanced
terminal heat-scale refill pulses.

## Method Choice

The right test here is not another positive estimate.  This is an alias-cycle
audit: check whether the weighted commutator label contains a new proof
mechanism, or whether it merely renames the already-consumed source-pulse
routes.

## Signed Branch

The bare separated commutator estimate is true for the wrong reason.  In the
geometry

```math
j+M<k<\ell-4,
```

Fourier support gives

```math
\Delta_j(a_k\cdot\nabla\Delta_\ell u)=0,
\qquad
\Delta_j\Delta_\ell u=0.
```

So

```math
[\Delta_j,a_k\cdot\nabla]\Delta_\ell u=0
```

for the bare pairwise term.  That does not touch the real object left after
weights, cutoffs, packet reductions, and lifted summation.

The live signed object is the weighted lifted remainder

```math
G_N^{lift,\sigma}
=\sum W_{j,\ell}^{\sigma}R_{j,k,\ell}^{lift}.
```

Its principal term is a strain interaction,

```math
R_{j,k,\ell}^{lift}
:=
2^{2j}\langle C_j(\nabla a_k,\Delta_j u),\Delta_j u\rangle .
```

One-sided weights and terminal positive-part selection destroy the global
dyadic skew cancellation.  Expanding strain eigendirections can still carry
same-sign positive source.

Thus the signed side reduces to the already-audited open targets:

```math
StrainSignCancellationForWeightedLiftedRemainder.A
```

or

```math
ActiveEigendirectionDecorrel.A.
```

Both are not installed from the current inputs.

## Residual-Tail Branch

Putting absolute values before summation collapses the estimate to the positive
carrier branch.  Young/Cauchy leaves the active-square residual tail:

```math
\int_I \sum_{j\ge N}2^{-j}D_j(t)^2\,dt.
```

The installed first-moment dissipation and cumulative tail-square reserves do
not control this shellwise square density on selected terminal heat-scale
windows.  The residual-tail side reduces to:

```math
ActiveSquareResidualTail.A,
```

then to:

```math
SourceBalancedActivePulseExclusion.A,
```

then through the already-audited parent / Zeno / `B_ASAC` production chain to:

```math
BASACTimeFaceAntiAtom.A
```

or a genuinely new terminal time-thickness theorem.

## Alias-Cycle Verdict

`WeightedCommutatorResidualTail.A` is not proved.

It also should not stay as the next loop target.  At the current theorem
resolution it is not a new independent wall.  It is an alias for two consumed
families:

```math
\text{signed strain / eigendirection cancellation}
```

and

```math
\text{active-square / source-balanced pulse control}.
```

Both routes have already been driven to the same surviving object:

```math
B_{ASAC}^{closed,min}
```

with a possible zero-thickness terminal native positive source atom.

## Next Loop Target

The loop should return to the non-alias terminal time-thickness work.  The
sharpest next attack is the import/identification bridge:

```math
NativeTimeFaceAtomToTransferActiveBurst.A
```

or

```math
TimeFaceAtomInFinalDefect.A.
```

That bridge must show that a terminal native positive source atom in the
produced `B_ASAC` class is visible as a transfer-active burst or final
active-frame defect strong enough to import a first-threshold / active-frame
rigidity mechanism.

If that bridge also fails, the active loop must continue with a new
zero-ASAC equality-rigidity or pressure/Leray time-face spread theorem.  It may
not recycle the weighted-commutator label as fresh progress.
