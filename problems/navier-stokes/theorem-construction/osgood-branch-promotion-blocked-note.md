# Osgood Branch Promotion Blocked Note

## Branch tested

This branch would promote finite lifted carrier energy and a uniform active-shell threshold from

```math
\int_{E_0}^{\infty}{dr\over C\Phi_s(r)}=\infty.
```

## Result

The Osgood condition is not available on the current lifted carrier surface. The controlling test gives

```math
\Phi_s(r):=1+r+r^{3/2}
```

or stronger polynomial growth from the displayed lifted tame product estimates.

Since

```math
\int_R^\infty {dr\over r^{3/2}}<\infty,
```

the scalar comparison permits finite-time escape. Therefore this branch does not promote

```math
\sup_{t<T}\mathcal E_D^{(s)}(X(t))<\infty
```

or a uniform active-shell threshold.

## Consequence

The loop must proceed through the superlinear branch: isolate and repair the exact lifted high-derivative commutator or normalization that generates the `r^{3/2}` growth.
