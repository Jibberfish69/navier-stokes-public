# Post-Collar Upstream Frontier: Terminal-Tail Bridge Promotion Audit

## Purpose

The final low-pass commutator collar route is now indexed and conditionally closed as a local theorem-note chain. The next upstream Navier-Stokes frontier returned by the lane state is not another collar estimate. The promoted frontier is:

```text
periodic terminal-tail bridge promotion audit after PCTP.hard discharge
```

This note records the handoff from the collar route to that upstream audit frontier.

## Current lane signal

The live local membership target remains

```text
M_Q(u,p)
```

witnessed through the route vocabulary

```text
Pack / Part / Field
```

The lane state reports that the six-burden stack

```text
LCI.A
CJ.A_low.7
FCI.5f
OFP.A
CFI.A
End_NS
```

is terminally closed under the retained H1-H6 conditional packet.

The averaged terminal-tail bridge is recorded as grounding

```text
PCTP.hard / TTU.A
```

through

```text
TGC.A + AACT.Global.noJump + AVG.END.A + READ.END => PCTP.hard.
```

## Audit burden

The required next action is a promotion-facing audit artifact for the periodic terminal-tail bridge. The audit should verify, from visible canonical surfaces, that:

```text
1. TGC.A is installed and points to the terminal geometric control input.
2. AACT.Global.noJump is installed and supplies the no-jump continuity needed by the bridge.
3. AVG.END.A is installed and supplies the averaged endpoint transition.
4. READ.END is installed and supplies the readout endpoint conversion.
5. The composition TGC.A + AACT.Global.noJump + AVG.END.A + READ.END => PCTP.hard is explicitly represented by canonical surfaces.
6. The result remains a local theorem-program audit artifact, not a public Clay release claim.
```

## Relation to the collar route

The collar route supplies a local coherent coefficient absorption chain:

```text
carrier-localized active readouts
-> proper carrier collar
-> global plateau selector
-> amplitude/speed supplier
-> coherent coefficient inputs
-> collar absorption
```

The terminal-tail bridge audit is upstream of this local collar chain in the lane governance state. It concerns source-frontier promotion and route fidelity after `PCTP.hard`, not further harmonic-analysis collar closure.

## Next concrete work item

Write the visible audit note:

```text
problems/navier-stokes/theorem-construction/periodic-terminal-tail-bridge-promotion-audit-result.md
```

The audit should cite and inspect canonical surfaces for:

```text
TGC.A
AACT.Global.noJump
AVG.END.A
READ.END
PCTP.hard
TTU.A
M_Q(u,p)
Pack / Part / Field
LCI.A / CJ.A_low.7 / FCI.5f / OFP.A / CFI.A / End_NS
```

## Boundary

This handoff does not assert a Millennium-problem proof promotion. It records the next local theorem-note/audit artifact requested by the current lane state.

## Source surfaces

- `problems/navier-stokes/live-theorem-edge.md`
- `problems/navier-stokes/live-theorem-edge.yaml`
- `problems/navier-stokes/theorem-packet.yaml`
- `problems/navier-stokes/source-frontier.yaml`
- `problems/navier-stokes/proof-obligation-matrix.yaml`
- `problems/navier-stokes/proof-assembly.yaml`
- `problems/navier-stokes/theorem-construction/final-collar-route-index.md`
- `problems/navier-stokes/theorem-construction/final-collar-route-status-summary.md`