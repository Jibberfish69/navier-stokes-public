# Production Six-Burden Replay Blocked By Shell Amplitude

## Replay chain

The intended chain is

```math
HFG
\Rightarrow
Part+Field+\mathfrak A_{N,Q}
\Rightarrow
H1+H2+H6+ORIGIN.Retain
\Rightarrow
LCI.A+CJ.A_{low}.7+FCI.5f+OFP.A+CFI.A+End_{NS}.
```

## Precondition check

`HFG` remains blocked because weighted carrier closure is not installed. Weighted carrier closure remains blocked by active-square Carleson / LPAS, and the shell-amplitude gain route did not install that theorem.

## Result

Production and six-burden replay cannot be promoted on this branch. The exact live mathematical input remains one of:

```math
\text{active-square Carleson,}
```

```math
\text{shell-amplitude gain on active intervals,}
```

```math
\text{direct LPAS theorem,}
```

or another independent route to `HFG`.