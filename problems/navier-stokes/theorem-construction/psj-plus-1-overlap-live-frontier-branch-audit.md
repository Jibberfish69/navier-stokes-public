# `PSJ+.1` Overlap Live Frontier Branch Audit

## Status

Active theorem-facing branch-audit note for the localized-projector
exact-potential packet.

Role: decide the live `PSJ+.1` overlap frontier after the fine localizer notes
and the coarse retained-cover notes are read together. This note does not add a
new packet. It only records which already-installed branch is the narrower
honest live wall.

Closure update: the terminal overlap closure is now recorded in
[psj-plus-1-overlap-terminal-closure.md](/Users/thomasbirnie/Workspace/ToE/Research-Consolidation/problems/navier-stokes/theorem-construction/psj-plus-1-overlap-terminal-closure.md).
The installed close chooses the coarse normalized-kernel realization and routes
the scalar retained-cover object to the retained-family supplier layer. The fine
localizer-modulus route remains a sharper sibling, not a live blocker for the
installed `PSJ+.1` branch.

## Fine Branch

The fine route keeps the exact `PSJ+.1` localizer geometry:

```math
\texttt{kernel mismatch}
\Longrightarrow
\texttt{localizer modulus}
\Longrightarrow
\texttt{center-spacing on overlaps}.
\tag{PBA.1}
```

Its surviving primitive is the weighted localizer-modulus theorem from
`LOM.8`:

```math
\int_I\int_{\mathcal J}
\frac{A_J(z)^2}{m_\ast^2\overline\gamma_\ast^2}
\omega_{\phi_r}\!\bigl(\delta_{J,\ell\ell'}^{ctr}(z)\bigr)^2
d\mu_J^{pair}\,dt
\le
C\bigl(\mathcal E_J^{obs}+\mathrm{Err}_{J,\mathrm{pair}}\bigr).
\tag{PBA.2}
```

Under a Lipschitz localizer design this becomes the weighted center-spacing
theorem `LOM.10`:

```math
\int_I\int_{\mathcal J}
\frac{A_J(z)^2}{m_\ast^2\overline\gamma_\ast^2}
\delta_{J,\ell\ell'}^{ctr}(z)^2
d\mu_J^{pair}\,dt
\le
\frac{C}{L_r^2}
\bigl(\mathcal E_J^{obs}+\mathrm{Err}_{J,\mathrm{pair}}\bigr).
\tag{PBA.3}
```

The measurable selected-center route does not escape this wall. It collapses
back to the same weighted center-spacing theorem. The compact-support radius
route is a stronger sufficient realization of `(PBA.3)`, not the minimal live
primitive.

## Coarse Branch

The coarse route normalizes local kernels and prices overlap through retained
cover geometry:

```math
\texttt{normalized local kernels}
\Longrightarrow
\texttt{coarse overlap consumer}
\Longrightarrow
\texttt{retained-cover counting}.
\tag{PBA.4}
```

Before endpoint specialization, the surviving primitive is the overlapwise
summability theorem `LOS.1`:

```math
\mathrm{Overlap}_{J,\mathrm{coarse}}
\le
C\bigl(\mathcal E_J^{obs}(I)+\mathrm{Err}_{J,\mathrm{pair}}(I)\bigr).
\tag{PBA.5}
```

On the installed normalized-kernel retained-cover realization, this sharpens to
the scalar retained-cover counting law `LRC.1` / `RCP.0`:

```math
\sum_k |I_k|\,\mu_J^{pair}(\mathcal J_k)
\le
C\bigl(\mathcal E_J^{obs}(I)+\mathrm{Err}_{J,\mathrm{pair}}(I)\bigr).
\tag{PBA.6}
```

The broader familywise source-summation theorem is therefore not the sharp live
primitive on this branch. It is an upstream source-level packaging that has
already been narrowed to the overlapwise summability slot and then, in the
normalized-kernel endpoint, to `(PBA.6)`.

## Frontier Decision

The live narrow branch uses the coarse endpoint:

```math
\boxed{\texttt{one scalar retained-cover counting theorem }(PBA.6)}
```

provided the route is allowed to use the installed normalized-kernel coarse
realization, bounded multiplicity, and retained-box subordination hypotheses.

The fine localizer-modulus route remains an honest sibling route, but it is no
longer the minimal remaining overlap wall after the coarse endpoint notes are
admitted. It stays as the sharper-geometry subbranch for contexts that require
preserving pointwise localizer comparison.

## Guardrails

1. Do not widen this frontier back to the full `V3` or familywise packet.
2. Do not replace `(PBA.6)` by `F.11l` active-strip mass pricing or `F.11c`
   tubular low-gap control; the boundary notes show those are distinct objects.
3. Do not promote the compact-support radius or adiabatic packing schedules as
   the minimal wall. They are stronger sufficient continuations beyond the
   installed endpoint.
4. Do not claim `(PBA.6)` is proved here from weaker hypotheses. The terminal
   closure note closes `PSJ+.1` by routing that scalar object to the admitted
   retained-family supplier layer, not by erasing the distinction between
   `PSJ+.1` overlap debt and retained-cover infrastructure.
