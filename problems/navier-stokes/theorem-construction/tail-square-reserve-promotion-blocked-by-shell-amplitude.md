# Tail-Square Reserve Promotion Blocked By Shell Amplitude

## Target

The tail-square reserve is

```math
\int_0^T2^{-2N}E_N(t)^2dt
\le
\varepsilon\nu\int_0^TD_N(t)dt+C2^{-2\delta N}.
```

## Dependency

The reserve has been reduced to near-diagonal absorption plus the far-separated bridge. The far-separated bridge requires LPAS or an equivalent active-square closure.

Direct LPAS remains blocked by the missing active-square theorem, equivalently by the missing shell-amplitude gain on active intervals.

## Result

The tail-square reserve remains conditional on active-square Carleson or direct LPAS.