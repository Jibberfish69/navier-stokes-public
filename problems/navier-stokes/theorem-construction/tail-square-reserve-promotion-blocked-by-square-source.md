# Tail-Square Reserve Promotion Blocked By Square-Source

## Target

The tail-square reserve is

```math
\int_0^T2^{-2N}E_N(t)^2dt
\le
\varepsilon\nu\int_0^TD_N(t)dt+C2^{-2\delta N}.
```

## Dependency

The reserve is reduced to near-diagonal absorption plus the far-separated bridge. The far-separated bridge requires LPAS or an equivalent active-square closure.

LPAS remains blocked by the missing square-source estimate for `F_j`, or an equivalent active-shell amplitude theorem.

## Result

The tail-square reserve remains conditional on the unresolved active-square/LPAS input.