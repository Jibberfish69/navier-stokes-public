# Thomas's actual-incidence all-complement persistence and record-refund boundary

**Date:** 2026-08-04

**Status:** noncanonical exact smooth compact-cutoff tensor identity and
same-history inference boundary inside Thomas Birnie's one-fluid,
pressure-explicit binomial/VPI reversible-intersection method.

This note answers the exact whole-record overlap question `(TAC.26)` using
the **actual** high-rate material selector from `(CNR.18)--(CNR.25)`, not an
arbitrary frozen choice `chi=0`.  It keeps the canonical failed-record
normalization `(DRF.25)`, the FIM/double-recent ancestry
`(CDR.11)--(CDR.64)`, the selector refund `(ISR.43)` and `(CIF.44)`, the
material three-label receiver `(STK.17)--(STK.19)`, and the eight tensor
blocks `(TPR.1)--(TPR.17)` in one continuing fixed-viscosity history.

The exact result is negative at the present theorem level.  The material
selector records where the adverse strain geometry occurs, but it selects
only the one-dimensional contracting component there.  Its algebraic
complement always contains the full two-dimensional expanding plane with
coefficient one, even when the material selector equals one.  Consequently
the all-complement `ccc` block has a selector-independent
expanding-plane-three-leg subblock which no amount of material-set overlap
removes.  Moreover, the complete membership and contracting-line response is
an exact signed transfer between `ccc` and the other seven blocks.  It is not
a loss from `ccc` and cannot be charged again as a datum reserve.

Thus `(TAC.26)` is not a theorem about overlap of the FIM core with the
adverse material set.  It requires a new signed **vector receiver-angle**
theorem for the actual pressure-complete three-label kernel, or a direct
datum payment for its pure expanding-plane subblock.  Neither conclusion is
contained in the canonical dynamic-core, double-recent, ISR, CNR, CIF, STK,
or TPR identities.

The physical object remains one velocity and pressure history from one
smooth datum.  The Fourier core gate, recent heat ancestry, material adverse
set, contracting line, co-normal, and three tensor legs are simultaneous
coordinates of that same history; none is a separately prescribed field.

## 1. The two selectors are different same-history coordinates

The receiver-time FIM gate is the symmetric Fourier-triad weight

\[
 \omega_\tau(t)=\eta\!\left({Z(t)\over Z_\tau^\dagger}\right),
 \qquad Z(t)=\|\Lambda u(t)\|_2^2,
 \tag{ACP.1}
\]

inside the complete core mask `chi_core(k,p,q;t)` of `(CDR.11)`.  It is
frozen before the current and the two recent parents are split.  It enforces
the static minimum-leg floor, the dynamic upper gate, the high output, all
three receiver choices, and both parent orders.  It is never differentiated
in the double-recent argument.

The material incidence selector is instead, at jointly regular levels,

\[
 \chi(x,t)=\mathbf 1_{E_{\lambda,L}(t)}(x),
 \qquad
 E_{\lambda,L}(t)
 =\{x:\mathfrak q(x,t)>\lambda,\ c(x,t)>L\},
 \tag{ACP.2}
\]

or its smooth version

\[
 \chi_{\varepsilon,\delta}
 =\theta_\varepsilon(\mathfrak q-\lambda)
  \eta_\delta(c-L).
 \tag{ACP.3}
\]

It is pulled through the same volume-preserving material flow and its
derivative is the pressure-complete membership row `(CNR.20)--(CNR.25)`.
No established identity identifies `(ACP.1)` with `(ACP.2)`, identifies a
Fourier leg with a material strain eigenline, or lower-bounds their overlap.
The fact that both are generated by the same history preserves custody; it
does not make them the same selector.

## 2. The actual complement retains the expanding plane exactly

On the support of `(ACP.2)`, let

\[
 P_-=e_-\otimes e_-,
 \qquad P_+ := I-P_-,
 \qquad W:=\chi P_-,
 \qquad C:=I-W.
 \tag{ACP.4}
\]

Here \(P_+\) denotes the entire plane perpendicular to the unique
contracting line; it is not one of the two individual expanding
eigenvectors, so the formula remains collision safe when the expanding
eigenvalues agree.  Off the support of the smooth selector, where the line
need not be simple, fix any measurable orthogonal rank-one extension solely
to write the component identity; because \(\chi=0\) there, its selected
factor is zero and the recombined tensor is independent of that extension.
The name ``expanding plane'' below refers to the selector support.  The
actual algebraic complement has the exact form

\[
 \boxed{C=P_+ +(1-\chi)P_-.}
 \tag{ACP.5}
\]

Therefore

\[
 \boxed{
 CP_+=P_+C=P_+,
 \qquad
 \|C\|_{\mathrm{op}}=1,
 \qquad
 \|C\|_F^2=2+(1-\chi)^2\ge2.}
 \tag{ACP.6}
\]

These identities hold for the actual value of \(\chi\), including
\(\chi=1\).  In particular, material membership can remove at most the
contracting-line component.  It never makes the complementary operator
small and never touches a vector in \(e_-^\perp\).

At three actual material labels, `(ACP.5)` gives

\[
 \begin{aligned}
 C_a\otimes C_b\otimes C_c
 ={}&P_{+,a}\otimes P_{+,b}\otimes P_{+,c}\\
 &+\sum_{\varnothing\ne S\subset\{a,b,c\}}
 \bigotimes_{\ell\in S}(1-\chi_\ell)P_{-,\ell}
 \bigotimes_{\ell\notin S}P_{+,\ell},
 \end{aligned}
 \tag{ACP.7}
\]

with the factors kept in their label order.  The first term has coefficient
one and is independent of every incidence threshold and membership value.
It is a rank-eight component subspace: each of the three legs has two
expanding-plane degrees of freedom.

Let `J_perpperpperp` denote `(TPR.5)` with
\(P_{+,a},P_{+,b},P_{+,c}\) in the three slots, while retaining all six
receiver assignments, both parent orders, the reality completion, and the
same kernel and the just-fixed off-support extension.  Its separate value can
depend on that harmless extension, whereas its sum with the seven remainder
terms cannot.  Equation `(ACP.7)` proves the exact compact-cutoff identity

\[
 \boxed{
 J_{ccc}=J_{\perp\perp\perp}
 +\sum_{\varnothing\ne S\subset\{a,b,c\}}J_{ccc,S}.}
 \tag{ACP.8}
\]

Every remainder in `(ACP.8)` contains at least one factor
\((1-\chi)P_-\); the first term contains none.  Even perfect material-set
overlap, \(\chi_a=\chi_b=\chi_c=1\), leaves

\[
 \boxed{J_{ccc}=J_{\perp\perp\perp}.}
 \tag{ACP.9}
\]

Equivalently, if the three actual leg values at one kernel tuple lie in their
corresponding expanding planes, then every block containing an `s` factor
vanishes and the `ccc` contraction equals the complete contraction at that
tuple, independently of \(\chi\).  This is not the artificial choice
\(\chi=0\); it is the nontrivial kernel of the actual rank-one component
selector for every \(0\le\chi\le1\).

## 3. Projector motion is exact transfer, not payment

For smooth selectors use the objective response already proved in `(CIF.4)`:

\[
 \mathring D_tW
 =(D_t\chi)P_-
 +\chi(\mathcal R_-\otimes e_-+e_-\otimes\mathcal R_-),
 \qquad
 \mathring D_tC=-\mathring D_tW.
 \tag{ACP.10}
\]

In the three component slots put

\[
 \mathbb C:=C_a\otimes C_b\otimes C_c,
 \qquad
 \mathbb N:=I_a\otimes I_b\otimes I_c-\mathbb C.
 \tag{ACP.11}
\]

The operator \(\mathbb N\) is exactly the sum of the other seven TPR block
operators.  The three-slot product rule gives

\[
 \boxed{
 \begin{aligned}
 \mathring D_t\mathbb C
 ={}&-(\mathring D_tW_a)\otimes C_b\otimes C_c
      -C_a\otimes(\mathring D_tW_b)\otimes C_c\\
    &-C_a\otimes C_b\otimes(\mathring D_tW_c),\\
 \mathring D_t\mathbb N
 ={}&-\mathring D_t\mathbb C.
 \end{aligned}}
 \tag{ACP.12}
\]

After contraction with the actual two-separation kernel and leg fields,
`(ACP.12)` says that every membership atom, contracting-line response, and
rigid-frame connection entering `ccc` occurs with the opposite sign in the
sum of the seven non-`ccc` blocks.  It is the subtotal version of the full
TPR refund `(TPR.10)--(TPR.12b)`.  Both separation transports, all three
pressure-complete leg responses, the recent source endpoints, and the moving
FIM gate remain outside this internal projector transfer and still appear in
the derivative of both subtotals.

For a sharp selector, `(ACP.12)` passes to a measure identity only under the
same lawful one-sided traces, blockwise products, finite BV, and sharp-block
limits required in CIF and TPR.  Under those hypotheses the entry and exit
atoms are still opposite.  Without them there is only the smooth
compact-cutoff identity; no finite sharp payment is inferred.

Thus taking the signed co-normal response from the seven selected blocks and
then estimating `ccc` separately double counts an internal transfer.  Taking
absolute variation avoids cancellation only by introducing the unpaid total
variation already isolated in `(CNR.32)` and refunded in `(CIF.44)`.

## 4. What the canonical failed-record chain does and does not control

The signed record normalization is

\[
 \boxed{
 \int_{I_n}J_n^{\rm rr}(t)\,dt\ge {A_n\over16}.}
 \tag{ACP.13}
\]

The dynamic-core/FIM/double-recent construction used to prove `(ACP.13)`
supplies all of the following on that same record:

1. the complete symmetric six-assignment Fourier mask and the static
   minimum-leg floor;
2. a high current receiver and two high nonlinear Duhamel parents;
3. both parents recruited during one shrinking recent heat window; and
4. the pressure-complete source-ancestry fourth-moment pulse `(CDR.53)`.

Every item is a frequency, ancestry, or source-magnitude statement.  None
contains a lower bound for

\[
 |P_-F^{\rm cur}|,
 \qquad |P_-F^{\rm rec}_1|,
 \qquad |P_-F^{\rm rec}_2|,
 \tag{ACP.14}
\]

or a signed contraction of one of those components with the actual tensor
kernel.  Fourier incompressibility says that each Fourier polarization is
perpendicular to its frequency.  It does not orient the pulled-back leg
value toward the local contracting strain line.

On the material side, the fixed-selector imbalance branch and
`(ISR.38)` localize positive strain-shape mass to the set `(ACP.2)`.  They do
not involve the vector components in `(ACP.14)`.  The exact CNR trichotomy
controls an episodewise co-normal initialized on \(e_-\); it does not identify
that co-normal with a current or recent leg.  The signed CIF endpoint then
refunds its complete motion.  On the alternative complete-defect branch of
`(DRF.38)`, there is not even a recordwise lower bound for the positive
strain-shape mass from which material incidence could be inferred.

Accordingly, the existing branch structure cannot prove a uniform positive
constant in

\[
 A_n^{\not c}:=
 \sum_{\sigma\ne ccc}\int_{I_n}J_{n,\sigma}^{\rm rr}(t)\,dt
 \ge\eta\int_{I_n}J_n^{\rm rr}(t)\,dt.
 \tag{ACP.15}
\]

On the imbalance branch it lacks the vector receiver angle `(ACP.14)`; on
the complete-defect branch it lacks even the required adverse-set overlap.
The statement is about the exact deductions available from the canonical
same-history identities.  It is not a separately prescribed Navier--Stokes
counterhistory and does not assert that a failed record realizing pure
`ccc` exists.

## 5. Exact remaining whole-history theorem

Equations `(ACP.5)--(ACP.12)` rule out a scalar-incidence or
projector-variation proof of `(TAC.26)`.  A surviving theorem must control
the actual signed kernel contraction.  One possible exact form is a
record-uniform estimate

\[
 \boxed{
 \operatorname{sgn}(A_n)
 \int_{I_n}\!
 \bigl(J_n^{\rm rr}(t)-J_{n,ccc}^{\rm rr}(t)\bigr)\,dt
 \ge \eta |A_n|-\varepsilon_n,}
 \tag{ACP.16}
\]

with one \(\eta>0\), a datum-summable error
\(\sum_n\varepsilon_n<\infty\), the complete six-assignment reality sum,
both kernel transports, all pressure-complete leg responses, and every sharp
membership atom retained.  Since `(ACP.12)` refunds the projector response,
the proof of `(ACP.16)` must use a signed relation between the actual receiver
kernel and at least one of the three components `(ACP.14)`, not merely the
amount or variation of material incidence.

The alternative is a direct datum payment for the persistent subblock

\[
 \boxed{
 \sum_n\left|
 \int_{I_n}J_{n,\perp\perp\perp}^{\rm rr}(t)\,dt
 \right|<\infty,}
 \tag{ACP.17}
\]

together with payments for the seven remainder terms in `(ACP.8)`.  No
canonical row proves `(ACP.17)`: \(P_+\) is order zero, the tensor kernel
remains critical, and the known energy and source-first-moment bounds do not
control the signed double-recent receiver pairing.

The exact checked boundary is therefore

\[
 \boxed{
 \begin{gathered}
 \text{actual material incidence can remove only the contracting line},\\
 \text{the all-complement block always retains the pure expanding-plane
 three-leg receiver},\\
 \text{all selector/projector responses are internal signed transfer},\\
 \text{so neither FIM/double-recent ancestry nor ISR/CNR/CIF refund proves
 `(TAC.26)` or pays `ccc`}.
 \end{gathered}}
 \tag{ACP.18}
\]

This is a rigorous same-history obstruction to the proposed overlap
mechanism, not a proof that no stronger pressure-complete receiver-angle
theorem exists.  It proves no datum endpoint, no `(TIR.37a)`, and no MPP
closure.
