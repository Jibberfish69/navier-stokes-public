# Thomas's BPT--DCT.47 heat-age covariance/tangent bridge

**Date:** 2026-08-04

**Status:** exact cross-bridge proved inside Thomas Birnie's one-fluid,
pressure-explicit binomial/VPI reversible-intersection method.  The
heat-normalized three-leg storage in `(DCT.24)--(DCT.27)` is exactly the
heat-age integral of the fully orientation-recombined BPT radial pair
covariance.  Its derivative in the enstrophy-tangent direction is exactly the
DCT phase/reconvergence insertion isolated in `(DOE.17)--(DOE.21)`.

The axial-wobble variation, finite-chord covariance variation, balanced-record
selection, and heat-normalized Fourier exterior therefore belong to one
derivative.  After the moving-
\(Z\) collar is restored, that derivative telescopes in the normalized
storage coordinate.  Restoring the enstrophy-radius term and the DCT endpoint
then returns exactly the signed dynamic-core prefix.  No datum-finite boundary
is produced, `(TIR.37a)` is not proved, and the MPP is not closed.

The one physical object remains the fixed-viscosity Navier--Stokes history
generated by one smooth finite-energy datum.  Heat age below is an analytic
readout of one physical-time slice, not a second velocity history.  Material
pairs, Eulerian radial increments, Fourier triads, and the dynamic core are
recombined representations of that same slice; no chord is assigned to one
triad and no positive part is taken before recombination.

## 1. Both coordinates start from the primitive pressure-explicit law

Put \(A=\Lambda^2=-\Delta\) and write

\[
 u_t+\nu Au=Q(u),
 \qquad
 Q(u)=-(u\cdot\nabla)u-\nabla p,
 \qquad
 -\Delta p=\partial_i\partial_j(u_i u_j).
 \tag{BCD.1}
\]

For any smooth divergence-free field \(w\), define its complete signed
critical current by

\[
 \mathcal P(w):=(Q(w),\Lambda w)_2.
 \tag{BCD.2}
\]

The pressure is fixed by the displayed Poisson equation.  Its global pairing
with \(\Lambda w\) vanishes by incompressibility, but it remains inside every
finite-chord, selected, or parent-resolved response until the complete field
is recombined.

At one physical time, set

\[
 w_s:=e^{-\nu sA}u,
 \qquad s\ge0.
 \tag{BCD.3}
\]

The heat-age Stokes primitive is

\[
 \boxed{\mathcal B(u):=\int_0^\infty\mathcal P(w_s)\,ds.}
 \tag{BCD.4}
\]

The identity is first read in a pressure-complete Galerkin truncation or for
a periodic zero-mean trigonometric polynomial.  It passes to every smooth
class in which the recombined integral is absolutely convergent.

Let \(c_\tau\) be the fully signed three-leg current from `(DCT.13)`, with

\[
 S_\tau=|k|^2+|p|^2+|q|^2.
 \tag{BCD.5}
\]

Heat acts on all three simultaneous legs.  Hence

\[
 c_\tau(w_s)=e^{-\nu sS_\tau}c_\tau(u),
 \tag{BCD.6}
\]

and direct integration gives

\[
 \boxed{
 \mathcal B(u)
 =\sum_\tau{c_\tau(u)\over\nu S_\tau}
 =\sum_\tau b_\tau(u).}
 \tag{BCD.7}
\]

Thus the DCT denominator is not an added multiplier.  It is exactly the
three-parent heat age generated by the original pressure-complete binomial
law.

## 2. The same primitive is the heat-age integral of BPT covariance

For a divergence-free field \(w\), put

\[
 d_w(x,r,n):=w(x)-w(x-rn),
 \qquad
 L_w:=d_w\cdot n,
 \qquad
 Q_w:=|d_w|^2.
 \tag{BCD.8}
\]

The full orientation-recombined BPT covariance is

\[
 \mathscr C_w(x,r)
 :=\int_{\mathbb S^2}Q_wL_w\,d\sigma(n).
 \tag{BCD.9}
\]

Incompressibility gives \(\int_{\mathbb S^2}L_w\,d\sigma=0\), so `(BCD.9)`
is also the covariance of \(Q_w-\overline Q_w\) with \(L_w\).  The exact
pair law `(BPT.7)` is

\[
 \mathcal P(w)
 =-{1\over\pi^2}\int_{\mathbb R^3}\int_0^\infty
 r^{-3}\mathscr C_w(x,r)\,dr\,dx.
 \tag{BCD.10}
\]

Applying `(BCD.10)` to every heat readout \(w_s\) and using `(BCD.4)` gives
the cross-bridge

\[
 \boxed{
 \mathcal B(u)
 =-{1\over\pi^2}\int_0^\infty\!\int_{\mathbb R^3}
 \int_0^\infty r^{-3}\mathscr C_{w_s}(x,r)\,dr\,dx\,ds.}
 \tag{BCD.11}
\]

Equations `(BCD.7)` and `(BCD.11)` are the same scalar functional.  The first
keeps every pressure-complete Fourier parent and the second keeps every
orientation of every pair.  Neither is a model for the other.

## 3. Exact variation of the finite-chord covariance

Let \(h\) be any smooth divergence-free variation and set

\[
 h_s:=e^{-\nu sA}h,
 \qquad
 d_h(x,r,n):=h(x)-h(x-rn).
 \tag{BCD.12}
\]

Differentiating `(BCD.9)` before estimating gives

\[
 \boxed{
 \begin{aligned}
 \mathscr T(w,h)(x,r)
 :={}&D\mathscr C_w(x,r)[h]\\
 ={}&\int_{\mathbb S^2}
 \left[
 2(d_w\cdot d_h)(d_w\cdot n)
 +|d_w|^2(d_h\cdot n)
 \right]d\sigma(n).
 \end{aligned}}
 \tag{BCD.13}
\]

The spherical mean terms generated by writing a covariance cancel because
both \(w\) and \(h\) are divergence free.  Differentiating `(BCD.11)` now
gives

\[
 \boxed{
 D\mathcal B(u)[h]
 =-{1\over\pi^2}\int_0^\infty\!\int\!\int_0^\infty
 r^{-3}\mathscr T(w_s,h_s)(x,r)\,dr\,dx\,ds.}
 \tag{BCD.14}
\]

This is the exact finite-chord response of the DCT heat-normalized storage.

Its infinitesimal limit retains the complete strain--rotation geometry.  Put

\[
 M=\nabla w,
 \qquad
 S={M+M^T\over2},
 \qquad
 N=\nabla h,
 \qquad
 N_S={N+N^T\over2},
 \tag{BCD.15}
\]

and

\[
 \mathcal T_0(M):=\operatorname{tr}(M^TMS).
 \tag{BCD.16}
\]

Differentiation of the BPT fourth-moment identity gives

\[
 \boxed{
 \lim_{r\downarrow0}{\mathscr T(w,h)(x,r)\over r^3}
 ={8\pi\over15}D\mathcal T_0(M)[N],}
 \tag{BCD.17}
\]

where

\[
 \boxed{
 D\mathcal T_0(M)[N]
 =\operatorname{tr}\left[
 (N^TM+M^TN)S+M^TMN_S
 \right].}
 \tag{BCD.18}
\]

On the perfectly balanced strain face,

\[
 \mathcal T_0(M)
 =6a^3+{a\over4}|\omega_\perp|^2
 -{a\over2}|\omega_\parallel|^2.
 \tag{BCD.19}
\]

Thus `(BCD.18)` differentiates the favorable balanced-star term, the axial
wobble term, the transverse rotation term, and the motion of the balanced
axis simultaneously.  The sign of the state value `(BCD.19)` does not assign
a sign to its tangent derivative.

## 4. Axial wobble and the finite-chord defect form one derivative

Let \(\zeta_s=\zeta(w_s;x,r)\) be any smooth radial/balanced selector and put

\[
 \ell_{\zeta_s}(x):=\int_0^\infty\zeta_s(x,r)\,dr.
 \tag{BCD.20}
\]

The exact BPT defect at heat age \(s\) is

\[
 \mathcal E_{\zeta_s}(w_s)
 :=-{1\over\pi^2}\int\!\int_0^\infty r^{-3}
 \left[
 \mathscr C_{w_s}
 -\zeta_s r^3{8\pi\over15}\mathcal T_0(M_s)
 \right]dr\,dx.
 \tag{BCD.21}
\]

If the selector depends on the field, denote its directional derivative by

\[
 \dot\zeta_s[h_s]:=D\zeta(w_s)[h_s],
 \qquad
 \dot\ell_{\zeta_s}[h_s]:=\int_0^\infty\dot\zeta_s[h_s],dr.
 \tag{BCD.22}
\]

Direct differentiation of `(BCD.21)` and use of `(BCD.14)` give the exact
joined identity

\[
 \boxed{
 \begin{aligned}
 D\mathcal B(u)[h]
 ={}&-{8\over15\pi}\int_0^\infty\!\int
 \left[
 \dot\ell_{\zeta_s}[h_s]\mathcal T_0(M_s)
 +\ell_{\zeta_s}D\mathcal T_0(M_s)[N_s]
 \right]dx\,ds\\
 &+\int_0^\infty
 D\mathcal E_{\zeta_s}(w_s)[h_s],ds.
 \end{aligned}}
 \tag{BCD.23}
\]

The first line contains balanced-record selection and the full infinitesimal
balanced-star variation, including axial wobble.  The second contains every
finite chord and every pair not represented by that selected first jet.  All
selector-derivative terms occur once on each side of the definition and are
therefore already recombined in `(BCD.23)`.

Equation `(BCD.23)` answers the first cancellation question exactly.  Axial
wobble and finite-chord covariance do not supply two additive bills.  They
are complementary pieces of the one derivative `(BCD.14)`.  They may cancel
for a particular full field, but no sign or datum boundary follows from
separating them because the derivative of the favorable state term has no
sign either.

## 5. Fourier-core recombination produces the DCT tangent phase

For the physical enstrophy

\[
 Z=(u,Au)_2,
 \tag{BCD.24}
\]

let \(\omega_\tau(Z)\) be the smooth dynamic-core selectors from `(DCT.21)`
and define

\[
 \begin{aligned}
 \mathcal B_Z^{\rm core}(u)
 &:=\sum_\tau\omega_\tau(Z)b_\tau(u),\\
 \mathcal B_Z^{\rm out}(u)
 &:=\mathcal B(u)-\mathcal B_Z^{\rm core}(u).
 \end{aligned}
 \tag{BCD.25}
\]

The sum ranges over the statically admitted triads.  Equivalently,
\(\mathcal B_Z^{\rm core}\) is the heat-age integral of the triad-selected
current with the physical-time selector held fixed through heat age.  The
second line is the exact heat-normalized Fourier exterior; it is not assumed
paid merely because the current-level FIM exterior is paid.

The actual enstrophy-tangent response is

\[
 v:=u_t-{Z'\over2Z}u
 =Q(u)-\nu Au
 -{(Q(u),Au)_2-\nu\|Au\|_2^2\over Z}u,
 \qquad
 (v,Au)_2=0.
 \tag{BCD.26}
\]

Its heat-age variation is consequently

\[
 \boxed{
 v_s=e^{-\nu sA}Q(u)-\nu Aw_s-{Z'\over2Z}w_s.}
 \tag{BCD.26a}
\]

The first term in `(BCD.26a)` is the heat readout of the actual
pressure-complete source at the physical slice.  It is not replaced by
\(Q(w_s)\), which would turn heat age into a second nonlinear history.

Since \(DZ(u)[v]=0\), differentiating `(BCD.25)` in the direction \(v\)
does not move any \(\omega_\tau\).  Therefore

\[
 \boxed{
 \begin{aligned}
 t_{\rm core}
 :={}&\sum_\tau\omega_\tau Db_\tau(u)[v]\\
 ={}&-{1\over\pi^2}\int_0^\infty\!\int\!\int_0^\infty
 r^{-3}\mathscr T(w_s,v_s)(x,r)\,dr\,dx\,ds
 -D_u\mathcal B_Z^{\rm out}(u)[v].
 \end{aligned}}
 \tag{BCD.27}
\]

This is the requested exact material/Fourier cross-bridge.  The first term is
the complete heat-age finite-chord radial covariance variation; the second
removes the exact Fourier exterior without assigning a pair to a triad.

Combining `(BCD.23)` and `(BCD.27)` gives the balanced-coordinate form

\[
 \boxed{
 \begin{aligned}
 t_{\rm core}
 ={}&-{8\over15\pi}\int_0^\infty\!\int
 \left[
 \dot\ell_{\zeta_s}[v_s]\mathcal T_0(M_s)
 +\ell_{\zeta_s}D\mathcal T_0(M_s)[\nabla v_s]
 \right]dx\,ds\\
 &+\int_0^\infty
 D\mathcal E_{\zeta_s}(w_s)[v_s],ds
 -D_u\mathcal B_Z^{\rm out}(u)[v].
 \end{aligned}}
 \tag{BCD.28}
\]

The last line is the exact finite-chord/Fourier-exterior residual.  The first
line retains the favorable balanced-star variation, axial wobble, transverse
rotation, axis motion, and balanced-record selection.  Deleting any one term
breaks `(BCD.27)`.

## 6. The cross-bridge telescopes, but only as the open storage endpoint

Put

\[
 B_{\rm core}(t):=\mathcal B_{Z(t)}^{\rm core}(u(t)),
 \qquad
 \mathfrak R_{\rm core}:=\sum_\tau\omega_\tau r_\tau,
 \qquad
 X_{\rm core}:=\sum_\tau\omega_{\tau,Z}(Z)Z'b_\tau,
 \qquad
 \delta={Z'\over2Z}.
 \tag{BCD.29}
\]

Cubic homogeneity and `(BCD.26)--(BCD.27)` give

\[
 \boxed{
 B_{\rm core}'=3\delta B_{\rm core}
 +t_{\rm core}+X_{\rm core}.}
 \tag{BCD.30}
\]

Thus, with

\[
 G_{\rm core}:={B_{\rm core}\over Z^{3/2}},
 \tag{BCD.31}
\]

the enstrophy-radius contribution cancels exactly:

\[
 \boxed{
 G_{\rm core}'
 ={t_{\rm core}+X_{\rm core}\over Z^{3/2}}.}
 \tag{BCD.32}
\]

The BPT finite-chord/axial tangent and the DCT moving-
\(Z\) collar therefore do telescope:

\[
 \boxed{
 \int_0^T{t_{\rm core}+X_{\rm core}\over Z^{3/2}}\,dt
 =G_{\rm core}(T)-G_{\rm core}(0).}
 \tag{BCD.33}
\]

This is not yet the prefix needed in `(DCT.47)`, which is unnormalized.  In
the original first-binomial variables,

\[
 \mathfrak R_{\rm core}
 =\mathcal P_{1/2}^{\rm core}
 +3\delta B_{\rm core}+t_{\rm core},
 \tag{BCD.34}
\]

so `(BCD.30)` gives

\[
 \boxed{
 \mathfrak R_{\rm core}+X_{\rm core}
 =B_{\rm core}'+\mathcal P_{1/2}^{\rm core}.}
 \tag{BCD.35}
\]

After the DCT endpoint is restored,

\[
 \boxed{
 B_{\rm core}(0)-B_{\rm core}(T)
 +\int_0^T(\mathfrak R_{\rm core}+X_{\rm core})\,dt
 =\int_0^T\mathcal P_{1/2}^{\rm core}\,dt.}
 \tag{BCD.36}
\]

Thus unnormalizing `(BCD.33)` restores the enstrophy-radius term, and that
term restores exactly the original signed core current in `(BCD.36)`.  The
terminal value \(G_{\rm core}(T)\), or equivalently the unnormalized cubic
storage \(B_{\rm core}(T)\), has no proved datum-uniform bound which pays the
right side.  The telescope is an endpoint alias, not a new finite stock.

## 7. Exact-flow audit of the recombination

The decaying ABC solution `(BPT.26)--(BPT.33)` is a direct audit.  It has

\[
 Q(u)=0,
 \qquad
 u_t=-\nu N^2u,
 \qquad
 {Z'\over2Z}=-\nu N^2,
 \qquad
 v=0.
 \tag{BCD.37}
\]

Hence \(t_{\rm core}=0\) in `(BCD.27)`, and every directional derivative in
`(BCD.28)` vanishes because its direction \(v\) is zero.  At the distinct
current level, the BPT first jet is strictly balanced and favorable at its
material stagnation point, while the axial-wobble boundary is saturated at
another point of the same fluid.  Those nonzero state readings cancel only
after the finite chords, other orientations, and Fourier exterior are
recombined in `(BPT.16)` and then integrated through heat age in `(BCD.11)`.
The example therefore audits both identities without conflating a state
current with its enstrophy-tangent derivative.

The exact heat shear `(BPT.34)--(BPT.37)` gives the same whole-field verdict
with returning finite chords: \(Q=0\), \(v=0\), and
\(t_{\rm core}=0\), although individual pair currents change sign.  These
histories prove the recombination, not a universal sign for its parts.

Conversely, the two pressure-complete constant-enstrophy tangent data in
`thomas-dct47-exact-one-form-and-enstrophy-sphere-phase-boundary-20260804.md`
have \(Z'(0)=0\) but opposite nonzero \(t_{\rm core}\) in the strict FIM
core.  Through `(BCD.27)`, the fully recombined BPT finite-chord variation
minus its exact Fourier exterior therefore retains both signs.  The zero
results for ABC and shear are structural cancellations of those particular
whole fields, not a sign theorem.

## 8. Exact remaining term

The cross-bridge proves

\[
\boxed{
\begin{aligned}
&\text{heat-age BPT radial covariance}
&&=\text{DCT three-leg storage},\\
&\text{BPT axial/finite-chord tangent}-\text{Fourier exterior tangent}
&&=t_{\rm core},\\
&t_{\rm core}+\text{moving-}Z\text{ collar}
&&=Z^{3/2}G_{\rm core}',\\
&\text{DCT endpoint}+\text{full first-binomial refill}
&&=\text{signed core prefix}.
\end{aligned}}
 \tag{BCD.38}
\]

Accordingly, axial wobble plus finite-chord covariance does telescope after
the full material/Fourier recombination, but only into the normalized DCT
storage.  It does not become datum-finite.  After every exact cancellation is
restored, the explicit unpaid quantity is still

\[
 \boxed{
 \sup_{T<T_*}\int_0^T
 \mathcal P_{1/2}^{\rm core}(t)\,dt.}
 \tag{BCD.39}
\]

In BPT balanced coordinates the same remaining upper target is `(BPT.18)`:

\[
 \boxed{
 \mathcal R_{\rm bal}^{\zeta}
 ={4\over15\pi}\int\ell_\zeta a|\omega_\parallel|^2dx
 +\mathcal E_\zeta-\mathcal P_{1/2}^{\rm out},}
 \tag{BCD.40}
\]

with its record-selection, moving-gate, pressure--viscous, and complementary
pair terms kept joined as in `(BPT.20)--(BPT.25)`.  The favorable balanced
line proves
\(\mathcal P_{1/2}^{\rm core}\le\mathcal R_{\rm bal}^{\zeta}\); no
datum-finite upper prefix for `(BCD.40)` is proved.

The smallest new causal content exposed by the bridge is the signed
finite-chord/Fourier-exterior tangent in the last line of `(BCD.28)`, joined
to the infinitesimal axial/selection variation.  Bounding those pieces
separately would destroy `(BCD.23)` and return a generic magnitude estimate.
A successful continuation must instead control their actual one-history
recombination strongly enough to bound `(BCD.39)`.
