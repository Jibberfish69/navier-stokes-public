# Thomas's causal actual-receiver/co-normal pulse and complement-refund boundary

**Date:** 2026-08-04

**Status:** noncanonical exact smooth compact-cutoff causal pulse identity and
conditional sharp-record dichotomy inside Thomas Birnie's one-fluid,
pressure-explicit binomial/VPI reversible-intersection method.

This note continues only the material three-label receiver
`(STK.17)--(STK.19)`, its reality-completed six-assignment tensor partition
`(TPR.1)--(TPR.12)`, and the whole-history target `(TAC.27)--(TAC.29)`.  It
does not substitute a scalar pair current, a frozen receiver angle, an
independently prescribed projector, or another proof method.

The physical and historical object remains one continuing fixed-viscosity
fluid generated by one datum.  Thomas's pulse or spring winding, the
imbalanced stretched vortex, and the perfectly balanced rip-cord remain in
that order.  The three material labels, both transported separations, the
moving FIM gate, the current leg, both recent legs, pressure, viscosity,
membership, and contracting-line motion are simultaneous readings of that
one history.

The exact advance is a temporal-row correction and its causal consequence.
The line-projector response in `(TAC.27)` is a term in
`dJ_sigma/dt`, not in `J_sigma`.  Its unweighted time integral therefore
lives one temporal row above the record pulse `int J_sigma dt`.  The lawful
comparison requires one causal material-time primitive.  Multiplication by
the record age `theta_n(t)=t-s_n` and one integration by parts give an exact
identity:

1. the current pulse equals a time-weighted actual-receiver line response
   plus its endpoint, two-separation, moving-gate, pressure-complete leg, and
   membership remainder;
2. the line response has an exact co-normal-aligned part and an exact
   transverse receiver residual, with no assumed sign or angle;
3. on any canonical record where the differentiated sharp blocks have lawful
   limits, either that co-normal-aligned term carries a fixed signed fraction
   of the block pulse or the complete remainder carries it; and
4. after all eight tensor blocks are recombined, the co-normal-aligned,
   transverse, and membership responses cancel label by label and the
   remainder is exactly the original `(STK.19)` pulse.

Thus the causal join does not produce the datum budget in `(TAC.29)`.  It
proves exactly why integration by parts returns the same signed pulse that
`(TIR.37a)` must bound.  No MPP closure is claimed.

## 1. Objective block derivative with every response retained

Use the notation of `(TPR.1)--(TPR.12)`.  For one of the six assignments
`rho in A_6`, write

\[
 (Z_a^\rho,Z_b^\rho,Z_c^\rho)
 :=(F_a^\rho,G_b^\rho,H_c^\rho),
 \qquad
 U_{\ell,\rho}^{\sigma_\ell}
 :=W_\ell^{\sigma_\ell}Z_\ell^\rho,
 \tag{CRJ.1}
\]

where `sigma in {s,c}^3`, `eta_s=1`, and `eta_c=-1`.  For compactness, put

\[
 \langle K;A,B,C\rangle:=K_{ijk}A_iB_jC_k
 \tag{CRJ.2}
\]

and let `d lambda=da db dc`.  Every expression below is summed over all six
assignments together with the reality-conjugate completion already contained
in `K^rho`.

Define the response which does **not** differentiate the selected projector:

\[
\boxed{
\begin{aligned}
 \mathfrak B_\sigma(t):={}&
 \sum_{\rho\in\mathcal A_6}\iiint
 \langle\mathring{\mathcal T}_{abc}K^\rho;
 U_{a,\rho}^{\sigma_a},U_{b,\rho}^{\sigma_b},
 U_{c,\rho}^{\sigma_c}\rangle\,d\lambda\\
 &+\sum_{\ell\in\{a,b,c\}}
 \sum_{\rho\in\mathcal A_6}\iiint
 \langle K^\rho;
 U_{a,\rho}^{\sigma_a},\ldots,
 W_\ell^{\sigma_\ell}\mathring D_\ell Z_\ell^\rho,
 \ldots,U_{c,\rho}^{\sigma_c}\rangle\,d\lambda .
\end{aligned}}
\tag{CRJ.3}
\]

Here the differentiated leg occupies its own displayed slot.  The objective
kernel response is

\[
 \mathring{\mathcal T}_{abc}K
 =\partial_tK+\nabla_RK\cdot V_{ab}+\nabla_SK\cdot V_{ac}
 -\Omega_a\mathbin{\cdot_1}K
 -\Omega_b\mathbin{\cdot_2}K
 -\Omega_c\mathbin{\cdot_3}K.
 \tag{CRJ.4}
\]

Consequently `(CRJ.3)` retains both independent separation transports, the
moving instantaneous gate in `partial_t K`, all three connection terms, and
all three leg responses.  Expanding the objective notation reproduces the
ordinary response in `(STK.19)`: the current leg contains
`-nabla p+nu Delta u`; each recent leg contains its present pressure-complete
source, heat term, material-transport commutator, and delayed recent-window
endpoint.  A differentiated source endpoint retains the first pressure-
explicit binomial response.  Nothing in `(CRJ.3)` is a freely prescribed
acceleration.

Use

\[
 \mathring D_\ell W_\ell
 =(D_\ell\chi_\ell)P_{-,\ell}
 +\chi_\ell(\mathring D_\ell P_{-,\ell}),
 \qquad
 \mathring D_\ell P_{-,\ell}
 =\mathcal R_{-,\ell}\otimes e_{-,\ell}
  +e_{-,\ell}\otimes\mathcal R_{-,\ell}.
 \tag{CRJ.5}
\]

For each slot define the signed membership response

\[
\boxed{
 \mathfrak M_{\sigma,\ell}(t)
 :=\eta_{\sigma_\ell}
 \sum_{\rho\in\mathcal A_6}\iiint
 (D_\ell\chi_\ell)
 \langle K^\rho;U_{a,\rho}^{\sigma_a},\ldots,
 P_{-,\ell}Z_\ell^\rho,\ldots,
 U_{c,\rho}^{\sigma_c}\rangle\,d\lambda }
 \tag{CRJ.6}
\]

and the signed actual-receiver line response

\[
\boxed{
 \mathfrak C_{\sigma,\ell}(t)
 :=-\eta_{\sigma_\ell}
 \sum_{\rho\in\mathcal A_6}\iiint
 \chi_\ell
 \langle K^\rho;U_{a,\rho}^{\sigma_a},\ldots,
 (\mathring D_\ell P_{-,\ell})Z_\ell^\rho,\ldots,
 U_{c,\rho}^{\sigma_c}\rangle\,d\lambda .}
 \tag{CRJ.7}
\]

For a sharp selector, `(CRJ.6)` includes every entry, exit, and record-end
membership atom with its one-sided trace.  Equations `(TPR.9)` and
`(TPR.12a)--(TPR.12b)` now give the exact scalar identity

\[
\boxed{
 {d\over dt}J_\sigma
 =\mathfrak B_\sigma
 +\sum_{\ell\in\{a,b,c\}}\mathfrak M_{\sigma,\ell}
 -\sum_{\ell\in\{a,b,c\}}\mathfrak C_{\sigma,\ell}.}
 \tag{CRJ.8}
\]

No receiver assignment or parent order has been assigned a positive sign in
`(CRJ.8)`.

## 2. The unweighted line response is one temporal row above the pulse

Integrating `(CRJ.8)` on `I=(s,t_+)` gives

\[
\boxed{
 \sum_\ell\int_I\mathfrak C_{\sigma,\ell}\,dt
 =\int_I\mathfrak B_\sigma\,dt
 +\sum_\ell\int_I\mathfrak M_{\sigma,\ell}\,dt
 -[J_\sigma]_{s}^{t_+}.}
 \tag{CRJ.9}
\]

This is the exact causal content of the unweighted expression in
`(TAC.27)`.  The record pulse `int_I J_sigma dt` does not occur in
`(CRJ.9)`.  A comparison of `(TAC.27)` with that pulse must insert one time
primitive or an equivalent temporal normalization.  A pointwise receiver
angle cannot repair this temporal-row mismatch.

Put

\[
 \theta_I(t):=t-s,
 \qquad 0\le\theta_I\le\Delta_I:=t_+-s,
 \qquad \theta_I'=1.
 \tag{CRJ.10}
\]

Multiplying `(CRJ.8)` by `theta_I` and integrating by parts yields

\[
\boxed{
\begin{aligned}
 A_{\sigma,I}
 :=\int_IJ_\sigma(t)\,dt
 ={}&\Delta_IJ_\sigma(t_+)
 -\int_I\theta_I\mathfrak B_\sigma\,dt
 -\sum_\ell\int_I\theta_I\mathfrak M_{\sigma,\ell}\,dt\\
 &+\sum_\ell\int_I\theta_I\mathfrak C_{\sigma,\ell}\,dt.
\end{aligned}}
 \tag{CRJ.11}
\]

This is the lawful record-pulse version of the desired whole-history join.
The equivalent backward-age identity is

\[
\boxed{
\begin{aligned}
 A_{\sigma,I}
 ={}&\Delta_IJ_\sigma(s)
 +\int_I(t_+-t)\mathfrak B_\sigma\,dt
 +\sum_\ell\int_I(t_+-t)\mathfrak M_{\sigma,\ell}\,dt\\
 &-\sum_\ell\int_I(t_+-t)\mathfrak C_{\sigma,\ell}\,dt.
\end{aligned}}
 \tag{CRJ.12}
\]

Changing which end anchors the primitive changes neither the response terms
nor their order.  One endpoint of the actual block remains, and every
derivative transferred away from the projector lands on the actual kernel or
one of the three pressure-complete legs.

## 3. Exact co-normal projection of the actual receiver contraction

Equation `(CRJ.7)` is the actual receiver contraction; it is not yet the
co-normal scalar in `(CIF.9)`.  The exact relation can be written without a
frozen-angle hypothesis.

After the other two slots have been contracted, let `z` be the effective
covector seen by the active slot, put

\[
 e:=e_-,\qquad r:=\mathcal R_-,\qquad
 P^\perp:=I-e\otimes e,
 \tag{CRJ.13}
\]

and let `F` denote the unprojected field in the differentiated slot.  Then

\[
\boxed{
 z\cdot[(r\otimes e+e\otimes r)F]
 =g(F,z;e)\cdot r,}
 \tag{CRJ.14}
\]

where

\[
 \boxed{
 g(F,z;e):=(e\cdot F)P^\perp z+(e\cdot z)P^\perp F
 \in e^\perp.}
 \tag{CRJ.15}
\]

Let `mu=n dot e` and `h:=P^perp n=n-mu e`.  Since `r perp e`,

\[
 n\cdot r=h\cdot r.
 \tag{CRJ.16}
\]

Use the rank-one Moore--Penrose projection

\[
 \Pi_h:=
 \begin{cases}
 h\otimes h/|h|^2,&h\ne0,\\
 0,&h=0,
 \end{cases}
 \qquad
 g_\parallel:=\Pi_hg,
 \qquad
 g_\perp:=g-g_\parallel.
 \tag{CRJ.17}
\]

Then

\[
\boxed{
 g\cdot r=g_\parallel\cdot r+g_\perp\cdot r,
 \qquad
 g_\parallel\cdot r
 =\alpha\,n\cdot\mathcal R_-,}
 \tag{CRJ.18}
\]

where `alpha=(g dot h)/|h|^2` on `h ne 0`, while the product on the
right is defined as `g_parallel dot r` at `h=0`.  The vector
`g_parallel` is bounded in magnitude by `|g|`, but the scalar `alpha` has
no sign, no positive lower bound, and no uniform upper bound as `h` tends to
zero.  The residual `g_perp dot r` is an independent transverse component
of the same actual receiver.

Applying `(CRJ.17)--(CRJ.18)` inside every integrand of `(CRJ.7)` gives the
exact split

\[
 \boxed{
 \mathfrak C_{\sigma,\ell}
 =\mathfrak C_{\sigma,\ell}^{\parallel}
 +\mathfrak C_{\sigma,\ell}^{\perp}.}
 \tag{CRJ.19}
\]

The parallel term contains the signed co-normal response
`-n dot mathcal R_-`, but multiplied by the actual six-assignment receiver
coefficient and by `chi`.  The perpendicular term is not deleted.  This is
an exact decomposition, not a claim that the parallel coefficient is a
nonnegative receiver weight.  In particular, `(CRJ.18)` identifies the exact
extra property required by `(TAC.28)` and proves that whole-history
integration does not manufacture it algebraically.

## 4. The canonical signed dichotomy

For one record define

\[
 \begin{aligned}
 \mathcal C_{\sigma,I}^{\parallel}
 &:=\sum_\ell\int_I\theta_I(t)
 \mathfrak C_{\sigma,\ell}^{\parallel}(t)\,dt,\\
 \mathcal R_{\sigma,I}^{\rm all}
 &:=\Delta_IJ_\sigma(t_+)
 -\int_I\theta_I\mathfrak B_\sigma\,dt
 -\sum_\ell\int_I\theta_I\mathfrak M_{\sigma,\ell}\,dt
 +\sum_\ell\int_I\theta_I
 \mathfrak C_{\sigma,\ell}^{\perp}\,dt.
 \end{aligned}
 \tag{CRJ.20}
\]

Equations `(CRJ.11)` and `(CRJ.19)` give

\[
 \boxed{A_{\sigma,I}
 =\mathcal C_{\sigma,I}^{\parallel}
 +\mathcal R_{\sigma,I}^{\rm all}.}
 \tag{CRJ.21}
\]

No absolute value and no time-dependent maximizer is used.  Hence, whenever
`A_{sigma,I}>0`,

\[
\boxed{
 \mathcal C_{\sigma,I}^{\parallel}\ge{A_{\sigma,I}\over2}
 \quad\hbox{or}\quad
 \mathcal R_{\sigma,I}^{\rm all}\ge{A_{\sigma,I}\over2}.}
 \tag{CRJ.22}
\]

For every fixed canonical preterminal failed record, the FIM compact roof and
the sharp-block theorem `(PSL.15)--(PSL.23)` already give all eight
undifferentiated block values in `L^1(I_n)`, exact recombination, and one
fixed pattern `sigma_n` satisfying

\[
 \int_{I_n}J_{n,\sigma_n}^{\rm rr}\,dt\ge {A_n\over128},
 \tag{CRJ.23}
\]

Thus no additional value-limit hypothesis remains in `(CRJ.23)`.  If, in
addition, its kernel, leg, membership, parallel-line, and
transverse-line response products have compatible finite-measure limits (or
uniformly integrable `L^1` limits), endpoint traces, and passage after
multiplication by the bounded Lipschitz age `theta_n`, then `(CRJ.21)` passes
to that sharp block and

\[
\boxed{
 \mathcal C_{n,\sigma_n}^{\parallel}\ge {A_n\over256}
 \quad\hbox{or}\quad
 \mathcal R_{n,\sigma_n}^{\rm all}\ge {A_n\over256}.}
 \tag{CRJ.24}
\]

The additional derivative-component passage is not implied by convergence of
the undifferentiated blocks.  On a fixed jointly regular threshold set the
projector derivative is a lawful classical or measure pairing, but the block
value theorem supplies no cutoff-uniform or record-uniform bound for its
product with the actual receiver.  Separate limits or uniform integrability
are still required for `D_t chi`, the moving-gate response,
`mathring D_t P_-`, the two separation transports, and all pressure-complete
leg responses.  Without that passage, `(CRJ.24)` cannot assign the canonical
pulse to a co-normal measure; only the fully recombined distribution remains
defined.

The first branch of `(CRJ.24)` is still not `(TAC.29)`.  Its coefficient is
the later actual receiver in `(CRJ.15)--(CRJ.18)`, and its age-weighted sum
has not been bounded by the datum.  The second branch retains the same
record endpoint, moving gate, two transported separations, three complete
leg responses, membership atoms, and transverse line response.  It is not a
lower-order error.

## 5. Complement refund returns exactly the original pulse

The selected and complementary bits use the same unprojected field in the
projector-response term and have opposite signs.  For every slot `ell`, at
every smooth compact cutoff,

\[
\boxed{
 \sum_{\sigma\in\{s,c\}^3}\mathfrak M_{\sigma,\ell}=0,
 \qquad
 \sum_{\sigma\in\{s,c\}^3}
 \mathfrak C_{\sigma,\ell}^{\parallel}=0,
 \qquad
 \sum_{\sigma\in\{s,c\}^3}
 \mathfrak C_{\sigma,\ell}^{\perp}=0.}
 \tag{CRJ.25}
\]

The cancellation is label by label.  It retains every assignment and uses
only `W_ell^s+W_ell^c=I` and `eta_s+eta_c=0` after the other two bits have
been recombined.  It therefore includes the all-complement block rather than
discarding it.

Moreover,

\[
 \boxed{
 \sum_\sigma\mathfrak B_\sigma={dJ\over dt},
 \qquad
 \sum_\sigma J_\sigma=J.}
 \tag{CRJ.26}
\]

Summing `(CRJ.21)` over all eight blocks gives

\[
\begin{aligned}
 \sum_\sigma A_{\sigma,I}
 &=\sum_\sigma\mathcal R_{\sigma,I}^{\rm all}\\
 &=\Delta_IJ(t_+)-\int_I\theta_I(t){dJ\over dt}(t)\,dt\\
 &=\int_IJ(t)\,dt.
\end{aligned}
 \tag{CRJ.27}
\]

Thus the full complement refund is not merely bounded by the old pulse.  It
is **exactly** the old pulse.  Integration by parts moves the actual
co-normal response into the endpoint and the complete `(STK.19)` response;
recombination cancels its selected/complementary internal transfer and
restores the same six-assignment double-recent current.

## 6. Exact verdict for `(TAC.27)--(TAC.29)`

The causal whole-history join is now exact at the smooth compact-cutoff level:

\[
\boxed{
\begin{gathered}
 \text{unweighted projector response}
 =\text{one temporal row above the record pulse},\\
 \text{record pulse}
 =\text{record-age-weighted actual-receiver co-normal part}
 +\text{complete causal remainder},\\
 \text{co-normal part or complete remainder carries half of every positive}
 \text{ lawfully differentiated block pulse},\\
 \text{eight-block recombination cancels all projector/membership transfer}
 \text{ and returns exactly the original `(STK.19)` pulse}.
\end{gathered}}
 \tag{CRJ.28}
\]

Therefore the missing theorem is not an unweighted frozen-angle inequality.
To obtain `(TAC.29)`, one must prove from the same pressure-complete history a
datum-summable bound for the **age-weighted, actual-receiver** co-normal term
in `(CRJ.20)` together with the complete remainder, or prove a signed
cancellation between them before the eight-block refund.  Bounding either by
integrating the projector derivative alone reproduces `(CRJ.27)` and hence
the same canonical pulse whose datum bound is `(TIR.37a)`.

No datum-summable co-normal charge, no proof of `(TIR.37a)`, and no MPP
closure is established here.
