# Thomas's DCT.47 exact one-form and enstrophy-sphere phase boundary

**Date:** 2026-08-04

**Status:** exact \(((b_\tau),Z)\)-local normal-form classification, exact
enstrophy-radius/shape decomposition, and exact true-Leray tangent-phase
countertest proved below inside Thomas Birnie's one-fluid,
pressure-explicit binomial/VPI reversible-intersection method.  The only
\(((b_\tau),Z)\)-local primitive which cancels the dynamic-core quartic refill together
with the moving-
\(Z\) endpoint term is the DCT storage already present in `(DCT.25)`.  Its
remainder is exactly the fully recombined signed core current.  Thus this
calculation does not prove `(TIR.37a)` or close the MPP.  It proves that no
additional datum payment is hidden in a local cancellation of the two terms
in `(DCT.47)` and identifies the non-scalar same-history phase motion which a
further argument would have to control.

The physical object remains one smooth fixed-viscosity Navier--Stokes
velocity-pressure history generated by one datum.  The enstrophy, dynamic
core, triads, heat-normalized currents, and their phases are simultaneous
readouts of that same field.  In particular, the decomposition below of the
actual response into an enstrophy-radius part and an enstrophy-tangent part is
not a decomposition into two fluids or two independently supplied forces.

## 1. The joined DCT residual is an exact one-form

Use the notation of
`thomas-dynamic-core-time-oriented-three-leg-current-boundary-20260804.md`.
For every complete pressure-coupled triad \(\tau\), put

\[
 b_\tau={c_\tau\over d_\tau},
 \qquad
 r_\tau={a_\tau\over d_\tau},
 \qquad
 d_\tau=\nu S_\tau,
 \tag{DOE.1}
\]

so that the original first binomial row gives

\[
 \boxed{b_\tau'=r_\tau-c_\tau.}
 \tag{DOE.2}
\]

Every occurrence of \(a_\tau\), and therefore of \(r_\tau\), differentiates
all three physical legs through the complete simultaneous Navier--Stokes
source.  No incident parent is deleted.

Let

\[
 z_\tau:=Z_\tau^\dagger,
 \qquad
 \omega_\tau(Z):=\eta(Z/z_\tau),
 \qquad
 Z=\|\Lambda u\|_2^2,
 \tag{DOE.3}
\]

where \(z_\tau\) is the exact FIM threshold `(DCT.19)`.  For a finite
Galerkin truncation define

\[
 \begin{aligned}
 B&:=\sum_\tau\omega_\tau b_\tau,
 &C&:=\sum_\tau\omega_\tau c_\tau,\\
 R&:=\sum_\tau\omega_\tau r_\tau,
 &X&:=\sum_\tau\omega_{\tau,Z}Z'b_\tau.
 \end{aligned}
 \tag{DOE.4}
\]

The sums in `(DOE.4)` range over the statically admitted triads; their
dynamic membership is exactly the factor \(\omega_\tau\).

For a smooth preterminal solution the same identities follow by cutoff and
passage to any absolutely convergent recombined sum.  Differentiating \(B\)
and using `(DOE.2)` gives

\[
 \boxed{R+X=B'+C.}
 \tag{DOE.5}
\]

Consequently

\[
 \boxed{
 B(0)-B(T)+\int_0^T(R+X)\,dt
 =\int_0^TC\,dt.}
 \tag{DOE.6}
\]

In the sharp-selector limit, \(B(0)-B(T)+\int X\) is precisely the clipped
entry/exit telescope in `(DCT.28)`.  Equation `(DOE.6)` is therefore
`(DCT.47)` before an upper bound is asserted.  It proves that the endpoint
and quartic terms are not two physical payments: they are the exact
differential of one heat-normalized current plus the original core current.

## 2. Uniqueness of the \(((b_\tau),Z)\)-local cancellation

The exactness in `(DOE.5)` is rigid.  Treat the finitely many
\((b_\tau)\), \(Z\), \((r_\tau)\), \((c_\tau)\), and \(Z'\) as the formal
coordinates supplied by the first binomial row.  Suppose a \(C^1\)
state-local correction

\[
 F=F((b_\tau),Z)
 \tag{DOE.7}
\]

is required to cancel coefficientwise all appearances of the refill
\((r_\tau)\) and the moving-boundary rate \(Z'\) in \(R+X\), before any
absolute value or estimate is taken.  Thus

\[
 R+X={dF\over dt}+G((b_\tau),Z,(c_\tau)),
 \tag{DOE.8}
\]

where \(G\) contains neither \(r_\tau\) nor \(Z'\).  By `(DOE.2)`,

\[
 {dF\over dt}
 =\sum_\tau F_{b_\tau}(r_\tau-c_\tau)+F_ZZ'.
 \tag{DOE.9}
\]

Matching the independent first-row coefficients gives

\[
 F_{b_\tau}=\omega_\tau(Z),
 \qquad
 F_Z=\sum_\tau\omega_{\tau,Z}(Z)b_\tau.
 \tag{DOE.10}
\]

The compatibility equations in `(DOE.10)` integrate uniquely to

\[
 \boxed{F=\sum_\tau\omega_\tau(Z)b_\tau+\text{constant}=B+\text{constant}.}
 \tag{DOE.11}
\]

Substitution into `(DOE.8)` forces

\[
 \boxed{G=C=\sum_\tau\omega_\tau c_\tau.}
 \tag{DOE.12}
\]

Allowing an additional \(\phi(Z)\) only replaces `(DOE.12)` by
\(C-\phi'(Z)Z'\), whose integral is the new endpoint
\(\phi(Z(T))-\phi(Z(0))\).  If the \(Z'\) term is also to be cancelled,
\(\phi\) is constant.  Nonlinear cross-triad dependence in \(F\) does not
help: the first equations in `(DOE.10)` already fix every component of its
\(b\)-gradient.

Thus the whole normal-form family built only from the DCT current coordinates
\((b_\tau)\) and the global threshold coordinate \(Z\) is classified.  The
unique primitive is the DCT storage, and the unique remainder is the signed
core prefix which `(FIM.49)` reduced from the full current.  This is an
in-method obstruction only.  It does not rule out a full-field correction or
a genuinely nonlocal same-history cancellation among the pressure-complete
incident phases.

## 3. Exact enstrophy-radius and enstrophy-shape coordinates

The classification also shows which physical information is absent from a
local \((b,Z)\) correction.  Put

\[
 A=\Lambda^2,
 \qquad
 Z=(u,Au)_2>0,
 \qquad
 \delta:={Z'\over2Z},
 \tag{DOE.13}
\]

and define the enstrophy-tangent part of the **actual** response by

\[
 \boxed{v:=u_t-\delta u.}
 \tag{DOE.14}
\]

Equivalently, with the pressure-complete source \(Q\) from `(DCT.2)`,

\[
 \boxed{
 v=Q(u)-\nu Au
 -{(Q(u),Au)_2-\nu\|Au\|_2^2\over Z}\,u.}
 \tag{DOE.14a}
\]

This is one coordinate of the same velocity history.  It satisfies

\[
 \boxed{(v,Au)_2=0.}
 \tag{DOE.15}
\]

Every \(b_\tau\) is a cubic functional of the current velocity field: its
denominator \(d_\tau\) depends only on the fixed triad geometry and
viscosity.  Euler homogeneity therefore gives

\[
 Db_\tau(u)[u]=3b_\tau(u).
 \tag{DOE.16}
\]

Define the complete tangent-phase insertion

\[
 t_\tau:=Db_\tau(u)[v].
 \tag{DOE.17}
\]

Equations `(DOE.14)`--`(DOE.17)` give the exact same-history split

\[
 \boxed{b_\tau'=3\delta b_\tau+t_\tau.}
 \tag{DOE.18}
\]

Combining `(DOE.18)` with the first-binomial identity `(DOE.2)` gives

\[
 \boxed{r_\tau=c_\tau+3\delta b_\tau+t_\tau.}
 \tag{DOE.19}
\]

Now normalize only the amplitude of the same field:

\[
 g_\tau:={b_\tau\over Z^{3/2}}.
 \tag{DOE.20}
\]

Since \(3\delta=(3/2)Z'/Z\), the entire enstrophy-radius response cancels:

\[
 \boxed{g_\tau'={t_\tau\over Z^{3/2}}.}
 \tag{DOE.21}
\]

Thus changing the scalar enstrophy radius changes \(b_\tau\) by its cubic
homogeneity but does not change the normalized current.  Every change in
\(g_\tau\) comes from the tangent evolution of the complete incident
geometry: phase, polarization, reconvergence, orientation, and recruitment
inside the same pressure-coupled field.  This is the information which the
scalar threshold \(Z=z_\tau\) cannot determine.

## 4. Exact sharp-crossing formula, including clipped endpoints

Let

\[
 I_\tau(T):=\{t\in(0,T):Z(t)>z_\tau\}
 \tag{DOE.22}
\]

and write it as a disjoint union of intervals whose endpoints are clipped at
\(0\) and \(T\).  Let

\[
 \mathcal C_\tau(T)
 :=\sum_j\bigl[b_\tau(\alpha_j)-b_\tau(\beta_j)\bigr]
 \tag{DOE.23}
\]

be the DCT endpoint telescope.  On \(I_\tau(T)\), put

\[
 \theta_\tau(t):=\left({z_\tau\over Z(t)}\right)^{3/2}.
 \tag{DOE.24}
\]

At every genuine entry or exit, \(Z=z_\tau\), so
\(b_\tau=z_\tau^{3/2}g_\tau\).  Integrating `(DOE.21)` interval by interval
and correcting only the two possible clipped endpoints gives

\[
 \boxed{
 \begin{aligned}
 \mathcal C_\tau(T)
 ={}&-\int_{I_\tau(T)}\theta_\tau(t)t_\tau(t)\,dt\\
 &+\mathbf1_{\{Z(0)>z_\tau\}}
 \left[1-\left({z_\tau\over Z(0)}\right)^{3/2}\right]b_\tau(0)\\
 &-\mathbf1_{\{Z(T)>z_\tau\}}
 \left[1-\left({z_\tau\over Z(T)}\right)^{3/2}\right]b_\tau(T).
 \end{aligned}}
 \tag{DOE.25}
\]

This formula identifies the exact content of repeated threshold crossings.
After the forced cubic normalization, their accumulated values change only
through the enstrophy-tangent insertion \(t_\tau\).  The initial clipped
term is fixed by the datum.  The terminal clipped term is a critically
weighted cubic storage of the current field and has no proved datum bound.

Substitution of `(DOE.25)` into `(DCT.47)` produces no hidden gain.  Indeed,
on \(I_\tau(T)\),

\[
 \theta_\tau'=-3\delta\theta_\tau
 \tag{DOE.26}
\]

and `(DOE.18)` gives

\[
 \boxed{
 {d\over dt}\bigl[(1-\theta_\tau)b_\tau\bigr]
 =3\delta b_\tau+(1-\theta_\tau)t_\tau.}
 \tag{DOE.27}
\]

Equations `(DOE.19)` and `(DOE.27)` show that every apparent remainder
created by the normalization is exactly the clipped endpoint in `(DOE.25)`.
After it is rejoined, one returns to

\[
 \mathcal C_\tau(T)+\int_{I_\tau(T)}r_\tau\,dt
 =\int_{I_\tau(T)}c_\tau\,dt.
 \tag{DOE.28}
\]

Hence amplitude normalization removes a false scalar degree of freedom, but
it does not manufacture a datum payment.

## 5. A true-Leray constant-enstrophy tangent countertest

The tangent insertion in `(DOE.21)` is genuinely nonzero and sign-indefinite
for lawful Navier--Stokes data.  On \(\mathbb T^3\), take the three
representative wavevectors

\[
 k_1=(1,0,0),\qquad k_2=(0,2,0),\qquad k_3=(-1,-2,0),
 \tag{DOE.29}
\]

with helicities \((+,+,-)\), both input orders, the conjugate modes, the
complete Leray projection, and every generated incident output retained.
Use

\[
 \begin{aligned}
 h_+(k_1)&=(0,-i/\sqrt2,1/\sqrt2),\\
 h_+(k_2)&=(i/\sqrt2,0,1/\sqrt2),\\
 h_-(k_3)&=(2i/\sqrt{10},-i/\sqrt{10},1/\sqrt2),
 \end{aligned}
 \tag{DOE.30}
\]

and amplitudes

\[
 \widehat u(k_1)=h_+(k_1),\qquad
 \widehat u(k_2)=\rho h_+(k_2),\qquad
 \widehat u(k_3)=1.6e^{i\phi}h_-(k_3).
 \tag{DOE.31}
\]

For a base field \(w\), set

\[
 Z_0=(w,Aw)_2,
 \quad
 W_0=\|Aw\|_2^2,
 \quad
 P_{1,0}=(Q(w),Aw)_2,
 \tag{DOE.32}
\]

and let \(C_0\) be its complete critical current and \(R_0\) the complete
Stokes-normal-form refill \(DB(w)[Q(w)]\).  Direct evaluation of the full
pressure-complete convolution gives

\[
\begin{array}{c|c|c|c|c|c}
(\rho,\phi)&Z_0&W_0&P_{1,0}&C_0&R_0\\ \hline
(1.8,\pi/12)
&53.52&233.68&9.212373506582212
&3.005425582559174&-0.06120370944134024\\
(0.45,23\pi/12)
&29.22&136.48&2.3030933766455512
&0.7513563956397926&0.4645131796213645.
\end{array}
 \tag{DOE.33}
\]

Both nonlinear enstrophy productions are positive.  For \(\nu=1\), multiply
the whole base field by

\[
 A_*={W_0\over P_{1,0}}.
 \tag{DOE.34}
\]

The actual Navier--Stokes enstrophy derivative at that datum is exactly

\[
 Z'(0)=2A_*^3P_{1,0}-2A_*^2W_0=0.
 \tag{DOE.35}
\]

Thus \(\delta(0)=0\), every dynamic selector has zero instantaneous
\(Z\)-crossing rate, and the complete tangent insertion is

\[
 t(0)=A_*^4R_0-A_*^3C_0.
 \tag{DOE.36}
\]

For the two rows of `(DOE.33)`, respectively,

\[
 \boxed{
 t(0)=-74390.2357346\ldots,
 \qquad
 t(0)=5571971.05561\ldots.}
 \tag{DOE.37}
\]

These are opposite, nonzero signs on the same constant-enstrophy tangent
slice.  Dilating all wavevectors by an integer \(N\) and taking
\(A_N=NA_*\) preserves `(DOE.35)` and multiplies `(DOE.37)` by \(N^5\).
For every fixed choice of the FIM constants, all finitely many triads in the
first derivative lie strictly inside `(FIM.49)` for sufficiently large
\(N\): their thresholds are \(O(N)\), whereas the tuned enstrophy is
\(O(N^4)\).  The static lower conditions are also strict because
\(\|A_Nw_N\|_2\asymp N\).

Each tuned field is a real smooth divergence-free trigonometric polynomial,
so it is lawful initial data for one actual local classical Navier--Stokes
history.  The calculation is not a prescribed source, a decimated triad
model, a singular history, or a counterexample to `(TIR.37a)`.  Its exact
conclusion is narrower: neither \(Z'=0\), strict dynamic-core membership, nor
the moving-threshold term determines the sign or size of the normalized
incident-phase motion.

## 6. Exact boundary for the DCT.47 attack

The attempted cancellation has now been exhausted at the state-local
first-binomial level:

\[
\boxed{
\begin{aligned}
&\text{quartic refill plus moving-}\!Z\text{ endpoint}
&&=\text{the derivative of }B+\text{the signed core current},\\
&\text{state-local coefficientwise cancellation}
&&\Longrightarrow F=B\text{ uniquely},\\
&\text{enstrophy-radius normalization}
&&\Longrightarrow g_\tau'=Z^{-3/2}t_\tau,\\
&\text{true-Leray constant-}\!Z\text{ data}
&&\Longrightarrow t_\tau\text{ retains both signs in the strict core}.
\end{aligned}}
 \tag{DOE.38}
\]

Therefore no datum-finite cancellation between the two displayed terms of
`(DCT.47)` follows from their \(((b_\tau),Z)\)-local algebra.  After the unique exact
\(((b_\tau),Z)\)-local cancellation is performed, the smallest still-unpaid quantity at this level
is exactly

\[
 \boxed{
 \sup_{T<T_*}\int_0^T
 \mathcal P_{1/2}^{\rm core}(t)\,dt,}
 \tag{DOE.39}
\]

with the already paid FIM exterior understood.  In causal coordinates, the
new information which any further proof must use is a joined one-history
bound on the enstrophy-tangent incident-phase motion `(DOE.17)--(DOE.25)`
together with its terminal deep-core storage.  Those pieces may not be
bounded separately, assigned to different parents, or replaced by their
positive parts: `(DOE.27)--(DOE.28)` prove that their recombination is the
original core current.

A datum-finite upper bound for `(DOE.39)` would prove `(TIR.37a)` after
`(FIM.34)--(FIM.49)` are restored.  No such bound is proved here.  The
advance is the exact removal of a false local-cancellation option and the
identification of the genuinely non-scalar same-fluid history which remains.
