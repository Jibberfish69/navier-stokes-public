# Thomas's signed first cutoff moment: exact three-leg triad boundary

Status: proved exact reduction and sharp fixed-storage obstruction inside
Thomas Birnie's one-fluid, pressure-explicit binomial/VPI reversible-
intersection method.  The complete signed high-pass layer cake is written
below as a fully symmetrized three-leg true-Leray triad current before any
positive part is taken.  Energy, signed helicity, and incompressibility reduce
that current exactly to the unique-helicity leg of each heterochiral triad.

The result does **not** bound the first signed prefix from the datum.  It proves
that homochiral motion cancels, identifies the exact multiplier which survives,
and shows that neither an energy/helicity representative nor any fixed
Fourier-diagonal helical quadratic endpoint can turn that survivor into a
kinetic-energy-paid stock.  A phase- and history-dependent cancellation among
the complete pressure-coupled triads remains possible and is precisely the live
source of `(TIR.37a)`.

The formulas below are the present rigorous development of Thomas's supplied
method; they are not attributed to him as verbatim historical equations.

The one object throughout is the smooth fixed-viscosity Navier--Stokes field
generated by \(u_0\).  Its Fourier legs and helical signs are simultaneous
readouts of that same field.  They are not separate packets, successor fluids,
or independently selectable interactions.

## 1. Pressure-complete high-pass current

Write

\[
 u_t+\nu\Lambda^2u=Q,
 \qquad
 Q:=-(u\cdot\nabla)u-\nabla p
   =-\mathbb P(u\cdot\nabla u),
 \tag{SFT.1}
\]

and let \(P_{>K}\) be the sharp radial Fourier projection.  The complete
nonlinear input into the high-pass kinetic-energy row is

\[
 \Phi_K(t):=\operatorname{Re}(P_{>K}u,P_{>K}Q)_2.
 \tag{SFT.2}
\]

Its signed cutoff prefix is

\[
 F_K(T):=\int_0^T\Phi_K(t)\,dt.
 \tag{SFT.2a}
\]

For a periodic Fourier polynomial, the exact Leray source is

\[
 \widehat Q(k)
 =-i\mathbb P_k\sum_{p+q=k}
       (\widehat u(p)\cdot q)\widehat u(q),
 \qquad
 \mathbb P_k=I-{k\otimes k\over |k|^2}.
 \tag{SFT.3}
\]

The same formula is an integral on \(\mathbb R^3\).  The projector in
`(SFT.3)` is the simultaneous pressure completion.  It disappears only in the
final scalar pairing because \(k\cdot\widehat u(k)=0\); it is not deleted from
the field evolution.

Choose each real geometric triad

\[
 k+p+q=0
 \tag{SFT.4}
\]

together with its conjugate once, split every leg into curl eigenvectors

\[
 ik\times h_s(k)=s|k|h_s(k),
 \qquad s\in\{+1,-1\},
 \tag{SFT.5}
\]

and let

\[
 T_{\tau,k},\quad T_{\tau,p},\quad T_{\tau,q}
 \tag{SFT.6}
\]

be the three real modal kinetic-energy rates generated by that one helical
subtriad through `(SFT.3)`.  Thus both input orders and the conjugate triad are
already included in `(SFT.6)`.  With this fixed no-overcount convention, the
complete high-pass current is

\[
 \boxed{
 \Phi_K(t)
 =\sum_{\tau}
 \left(
  \mathbf 1_{\{|k|>K\}}T_{\tau,k}
 +\mathbf 1_{\{|p|>K\}}T_{\tau,p}
 +\mathbf 1_{\{|q|>K\}}T_{\tau,q}
 \right).}
 \tag{SFT.7}
\]

On \(\mathbb R^3\), the sum in `(SFT.7)` is the corresponding integral over
\(k+p+q=0\), with the same real-triad quotient.  Equation `(SFT.7)` retains all
three legs and their phases before any positive part or input colouring.

## 2. Exact layer cake before positive clipping

Put

\[
 \kappa:=|k|,\qquad \pi:=|p|,\qquad \chi:=|q|.
 \tag{SFT.8}
\]

Integrating the indicator functions in `(SFT.7)` gives

\[
 \boxed{
 \int_0^\infty\Phi_K(t)\,dK
 =\sum_\tau
 \left(
 \kappa T_{\tau,k}
 +\pi T_{\tau,p}
 +\chi T_{\tau,q}
 \right)
 =\mathcal P_{1/2}(t).}
 \tag{SFT.9}
\]

Therefore, for every preterminal prefix,

\[
 \boxed{
 \int_0^\infty F_K(T)\,dK
 =\int_0^T\!\int_0^\infty\Phi_K(t)\,dK\,dt
 =\int_0^T\mathcal P_{1/2}(t)\,dt.}
 \tag{SFT.10}
\]

There is no hidden positive measure in `(SFT.9)`.  If the three radii are
ordered \(r_1\le r_2\le r_3\), and the corresponding transfers are
\(T_1,T_2,T_3\), then triadic energy conservation gives the signed face
profile

\[
 \Phi_K^\tau=
 \begin{cases}
 0,&0<K<r_1,\\
 -T_1,&r_1<K<r_2,\\
 T_3,&r_2<K<r_3,\\
 0,&K>r_3,
 \end{cases}
 \tag{SFT.11}
\]

and hence

\[
 \int_0^\infty\Phi_K^\tau\,dK
 =-(r_2-r_1)T_1+(r_3-r_2)T_3
 =\sum_{i=1}^3r_iT_i.
 \tag{SFT.12}
\]

The cancellation in `(SFT.12)` happens between adjacent faces of the same
triad.  Replacing either lobe by its positive part destroys the identity.

## 3. Energy and helicity leave exactly the unique-helicity leg

For a helical subtriad set

\[
 h_k=s_k\kappa,\qquad h_p=s_p\pi,\qquad h_q=s_q\chi.
 \tag{SFT.13}
\]

Incompressibility and the full Euler/Leray convolution give the two exact
triadic invariants

\[
 T_{\tau,k}+T_{\tau,p}+T_{\tau,q}=0,
 \qquad
 h_kT_{\tau,k}+h_pT_{\tau,p}+h_qT_{\tau,q}=0.
 \tag{SFT.14}
\]

Equivalently, for one real phase-geometric coefficient \(\gamma_\tau\),

\[
 \boxed{
 (T_{\tau,k},T_{\tau,p},T_{\tau,q})
 =\gamma_\tau
 (h_q-h_p,\ h_k-h_q,\ h_p-h_k).}
 \tag{SFT.15}
\]

The coefficient \(\gamma_\tau\) retains the actual three-mode amplitudes,
polarizations, phases, and Leray geometry.  No sign is assigned to it.

If the triad is homochiral with common sign \(\sigma\), then
\((\kappa,\pi,\chi)=\sigma(h_k,h_p,h_q)\), so `(SFT.14)` gives

\[
 \boxed{
 \kappa T_{\tau,k}+\pi T_{\tau,p}+\chi T_{\tau,q}=0.}
 \tag{SFT.16}
\]

If the triad is heterochiral, let \(o\) be its unique-helicity leg and let
\(\sigma\) be the common sign of the other two legs.  In the quotient by the
two invariant covectors one has the exact vector identity

\[
 \boxed{
 (\kappa,\pi,\chi)-\sigma(h_k,h_p,h_q)
 =2\kappa_o e_o.}
 \tag{SFT.17}
\]

Pairing `(SFT.17)` with the transfer vector proves

\[
 \boxed{
 \kappa T_{\tau,k}+\pi T_{\tau,p}+\chi T_{\tau,q}
 =2\kappa_oT_{\tau,o}.}
 \tag{SFT.18}
\]

This is the sharp three-leg cancellation boundary.  Positive critical work is
exactly kinetic-energy acquisition by the unique-helicity leg, but the sign is
still the sign of the full phase-resolved transfer \(T_{\tau,o}\).  Summation
over all triads must occur before any positive part.

For example, if \(o=k\), then `(SFT.15)` gives

\[
 \mathcal P_{1/2}^\tau
 =2\sigma\gamma_\tau\,\kappa(\chi-\pi).
 \tag{SFT.19}
\]

The triangle inequality gives \(|\chi-\pi|\le\kappa\), but this is only a
radial-gap null form:

\[
 |\mathcal P_{1/2}^\tau|
 \le2\kappa^2|\gamma_\tau|.
 \tag{SFT.20}
\]

For any fixed nondegenerate heterochiral triangle with unequal paired radii,
simultaneous dilation by \(\lambda\) makes the multiplier in `(SFT.19)` grow
like \(\lambda^2\).  The cancellation supplies no uniform inverse-frequency
factor.  It annihilates homochiral and equal-paired-radius motion exactly, and
nothing more.

## 4. Why the singleton is not a triadwise endpoint of the full field

For one isolated heterochiral triad, \(2\kappa_oT_{\tau,o}\) is the derivative
of the unique leg's critical kinetic stock, apart from its viscous term.  That
does not define an endpoint for the complete field.  A fixed helical Fourier
mode can be the unique-helicity leg in one simultaneously active triad and a
paired-helicity leg in another.  Its nonlinear rate is the sum of both roles,
and the full Leray equation also generates their remaining outputs.

Consequently a rule that colours each occurrence of a mode by its role in one
input triad is not a quadratic functional of \(u(t)\).  It is an interaction
ledger.  Turning `(SFT.18)` into a lawful endpoint requires a single
mode-consistent multiplier after all incident triads have been recombined.

## 5. Classification of every fixed diagonal quadratic endpoint

Let a real rotation-invariant helical multiplier have weights
\(m_+(r),m_-(r)\), and define

\[
 \mathscr S_m(u)
 :={1\over2}\sum_{s=\pm}\int m_s(|\xi|)
       |\widehat u_s(\xi)|^2\,d\xi.
 \tag{SFT.21}
\]

Suppose its nonlinear source equals the complete critical current for every
true-Leray triad.  Then \(m_s(r)-r\) annihilates every helical transfer vector.
The energy/helicity classification of the full triad family forces

\[
 \boxed{
 m_s(r)=r+a+bsr,
 \qquad
 m_+(r)=a+(1+b)r,
 \qquad
 m_-(r)=a+(1-b)r.}
 \tag{SFT.22}
\]

Conversely, every multiplier in `(SFT.22)` has exactly the required nonlinear
source, because the additions are kinetic energy and signed helicity.

This classification gives the sharp endpoint obstruction.

1. At least one of the two slopes \(1+b\) and \(1-b\) is at least \(1\).
   Hence \(\mathscr S_m\) is not bounded above by the kinetic energy uniformly
   in frequency.
2. On a balanced state with equal plus and minus kinetic mass at the same high
   radius, the helicity addition cancels and the sum of the two weights is
   \(2a+2r\).  The storage retains the entire balanced critical common mode.
3. If both weights are made nonnegative, the viscous part of the storage law is
   positive but the terminal stock is the unpaid critical endpoint.  If a
   weight is allowed to become negative, the viscous part loses the sign needed
   for a datum-paid remainder.

Indeed, differentiating `(SFT.21)` along `(SFT.1)` gives exactly

\[
 \boxed{
 \mathcal P_{1/2}
 =\mathscr S_m'
 +\nu\sum_{s=\pm}\int
  m_s(|\xi|)|\xi|^2|\widehat u_s(\xi)|^2\,d\xi.}
 \tag{SFT.23}
\]

For `(SFT.22)`, equation `(SFT.23)` is the original critical row plus a linear
combination of the kinetic-energy and signed-helicity rows.  It is an exact
repartition of the same prefix, not a bounded replacement for it.

## 6. The first phase-retaining normal form also leaves the same boundary

The canonical Stokes time normal form goes beyond fixed quadratic storage.  If
\(S_\sigma=e^{-\sigma\Lambda^2}\) and \(\mathcal P_{1/2}(u)\) is viewed as its
homogeneous cubic functional, put

\[
 \mathfrak B_\nu(u)
 :={1\over\nu}\int_0^\infty
   \mathcal P_{1/2}(S_\sigma u)\,d\sigma.
 \tag{SFT.24}
\]

Exact differentiation gives

\[
 \int_0^T\mathcal P_{1/2}(u(t))\,dt
 =\mathfrak B_\nu(u_0)-\mathfrak B_\nu(u(T))
  +\int_0^T\mathfrak R_\nu(u(t))\,dt,
 \tag{SFT.25}
\]

where \(\mathfrak R_\nu\) is the complete quartic full-network insertion.
The already checked true-Leray normal-form calculation gives

\[
 |\mathfrak B_\nu(u)|
 \le {C\over\nu}\|u\|_{\dot H^{1/2}}^3,
 \tag{SFT.26}
\]

and lawful periodic comparison data for which the quartic remainder has both
signs while the instantaneous critical current is positive. Thus the first
phase-retaining endpoint is scale-critical and the full insertion is not a
datum-paid signed remainder. Equations `(SFT.24)`--`(SFT.26)` give the exact
boundary used here, with its normal-form calculation checked by
`check_stokes_singleton_normal_form_test.rb`.

This does not rule out the complete binomial/VPI history resummation.  It shows
what that resummation must newly prove: a signed cancellation among the
recombined heterochiral phase histories which controls both the terminal
critical common mode and every pressure-complete insertion, uniformly at the
dangerous critical scale.

## 7. Exact consequence for `(TIR.37a)`

Combining `(SFT.10)` and `(SFT.18)` gives the pressure-complete form of the sole
unpaid implication:

\[
 \boxed{
 \sup_{T<T_*}
 \int_0^T\sum_{\tau\ \mathrm{heterochiral}}
     2\kappa_{o(\tau)}T_{\tau,o(\tau)}(t)\,dt
 <\infty.}
 \tag{SFT.27}
\]

The sum in `(SFT.27)` is signed and is taken over the complete simultaneous
triad field before clipping.  Homochiral motion is already absent.  Energy and
helicity have already supplied all algebraic invariant cancellation available
to a fixed multiplier.  What remains is not another spatial moment or an
unpaid absolute estimate; it is the time-oriented, pressure-complete
heterochiral phase cancellation inside Thomas's original one-fluid VPI and
binomial history.
