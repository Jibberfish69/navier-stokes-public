# Weighted Carrier Closure Blocked By Shell Amplitude

## Target

The weighted carrier closure is

```math
\int_0^T(\Lambda_N+\Theta_N^\sharp)(t)E_N(t)dt
\le
\varepsilon\nu\int_0^TD_N(t)dt+C2^{-2\delta N}.
```

## Dependency

The collar product closes after the tail-square reserve is promoted. The tail-generated part of the low-mode product also closes after the same reserve is promoted; the finite-mode part is controlled on terminal windows.

The tail-square reserve currently depends on active-square Carleson or direct LPAS. Direct LPAS remains blocked by the missing shell-amplitude gain or equivalent active-square theorem.

## Result

Weighted carrier closure remains blocked by the active-square/shell-amplitude theorem.