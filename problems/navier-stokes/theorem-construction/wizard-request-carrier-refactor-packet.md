# WIZARD Request: Carrier Refactor Packet

## Status

This is a theorem-authority request packet for the `NS` lane after the carrier
ontology correction.

It is meant for `WIZARD`, not as a local theorem claim.

This request has now been answered by
[carrier-ontology-authority-decision.md](/Users/thomasbirnie/Documents/Research-Consolidation/problems/navier-stokes/theorem-construction/carrier-ontology-authority-decision.md).

## Current Local Conclusion

The ontology source stack forces the following correction:

```text
carrier = persistent coherent support,
selector/window = derived theorem-control machinery.
```

So the `NS` route should keep theorem direction but change object-language.

See:

- [carrier-ontology-and-selector-correction-note.md](/Users/thomasbirnie/Documents/Research-Consolidation/problems/navier-stokes/theorem-construction/carrier-ontology-and-selector-correction-note.md)
- [carrier-ontology-refactor-obligations.md](/Users/thomasbirnie/Documents/Research-Consolidation/problems/navier-stokes/theorem-construction/carrier-ontology-refactor-obligations.md)
- [carrier-selector-structural-audit.md](/Users/thomasbirnie/Documents/Research-Consolidation/problems/navier-stokes/theorem-construction/carrier-selector-structural-audit.md)

## Theorem-Authority Questions

`WIZARD` is asked to decide the following.

### 1. Canonical support object

What is the canonical theorem-facing support object on the ontology-correct
`NS` bridge?

Options in play:

1. keep `K_D`, but freeze its meaning to
   ```text
   persistent coherent support only
   ```
2. retire `K_D` and replace it with a less overloaded support symbol
3. use a short stack:
   ```text
   \mathfrak R \supset \mathfrak K_{\mathrm{pers}} \xrightarrow{\rho} u_{\mathrm{coh}}
   ```
   or equivalent.

We need theorem-authority on the canonical choice.

### 2. Selector hierarchy

What is the canonical notation and hierarchy for derived selectors/localizers?

The local lane now needs a selector object for statements of the form

```text
support -> coherent readout -> selector-localized coherent readout
```

without making the selector look ontological.

### 3. Minimal export interface stack

What is the minimal correct theorem stack for the bridge?

The local proposed order is:

```text
persistent support
\to
coherent readout
\to
selector-localized coherent readout
\to
classical low-mode comparison / transport target.
```

`WIZARD` should confirm or replace this stack.

### 4. Misframed note classes

Which note classes should now be treated as structurally misframed because they
collapse support, readout, and selector into one overloaded carrier object?

Local candidates are:

1. notes that start from `K_D(j_0)` before defining `u_{\mathrm{coh}}`,
2. notes that ask a thresholded family to define the carrier,
3. notes that force the zero-mode coherent-Laplacian route to generate the
   threshold selector family,
4. transport-export notes phrased on a thresholded block instead of on support
   plus selector-localized readout.

### 5. Fate of the coherent-Laplacian route

Given the zero-mode obstruction, should the lane:

1. keep the coherent-Laplacian route only as a selector-realization theorem,
2. explicitly de-authorize it as a candidate ontic definition of the carrier,
3. or replace it with a different operator attached to the persistent support?

## Local State After Refactor

The local lane has already done the following:

1. proved spectral realization of the thresholded selector family on the
   persistent carrier:
   [holonomy-refinement-projector-spectral-realization-theorem.md](/Users/thomasbirnie/Documents/Research-Consolidation/problems/navier-stokes/theorem-construction/holonomy-refinement-projector-spectral-realization-theorem.md)
2. proved the zero-mode obstruction to identifying that selector family with
   `(I+\mathcal L_{\mathrm{coh}})^{1/2}` on the current zero-mode carrier route:
   [coherent-laplacian-zero-mode-obstruction-lemma.md](/Users/thomasbirnie/Documents/Research-Consolidation/problems/navier-stokes/theorem-construction/coherent-laplacian-zero-mode-obstruction-lemma.md)
3. refactored the bridge notes so they treat thresholded families as selectors,
   not as the carrier itself.

## Requested Output From WIZARD

Please return:

1. the canonical theorem-facing support object name and role,
2. the canonical selector notation/hierarchy,
3. the authoritative bridge stack,
4. the notes or note classes that should now be considered deprecated or in
   rewrite-required status,
5. any theorem-authority correction to the local refactor notes above.
